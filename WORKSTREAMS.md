# WORKSTREAMS — карта параллельной разработки «Миксера»

Единый файл синхронизации. Дальнейшая разработка идёт в РАЗНЫХ чатах по
направлениям; каждый чат работает ТОЛЬКО в своей зоне и ведёт СВОЙ блок здесь.

### Занавес площадки: сигнал `Ads.curtainGone` (2026-07-30, жалоба владельца)

Жалоба: «после загрузки бриджа пропала анимация заполнения корзины». Причина —
ПОРЯДОК СОБЫТИЙ: у Playgama свой брендовый лоадер (непрозрачный `#242424` во
весь вьюпорт), и вся осадка кучи шла под ним.

**РАЗБОР SDK (v2.0.0, вендорный бандл; отдельного события «занавес снят» НЕТ).**
Занавес снимается ДВУМЯ путями:
1. `sendMessage(GAME_READY)` — удаляет узел **синхронно внутри вызова**;
2. сам SDK: `initialize()` в `.finally` через 700 мс ставит прогресс 100
   (мягко — только если игра ни разу не сообщала прогресс), снятие ещё через
   1400 мс. Итого ≈2.1 с после резолва init.

⚠️ **СОБЫТИЕ И НЕ НУЖНО: путь 1 — НАШ.** Момент снятия мы не угадываем, а
НАЗНАЧАЕМ, отправив GAME_READY когда захотим. Поэтому «сигнала нет» — не
блокер, а свобода. Ловить `#loading-overlay` по id не потребовалось (и
запрещено — приватный DOM чужого SDK).

⚠️⚠️ **НАЙДЕНО ЗАМЕРОМ, ХУЖЕ ИСХОДНОЙ ЖАЛОБЫ: занавес может висеть НАВСЕГДА.**
Если `initialize()` не резолвится (стенд `?platform_id=playgama` вне их домена
— висел 20 с и не думал уходить), то `sdkReady` не встаёт, GAME_READY уйти
не может, `.finally` не наступает — игра под непрозрачным занавесом невидима.
Лечение — страховка через ПУБЛИЧНЫЙ `setGameLoadingProgress(100)`
(документированный метод v2, проверен замером: занавес уходит). Взводится
ТОЛЬКО когда игра уже сказала «готова», а отправить не смогли: тогда занавес
врёт. Если не готова сама игра — занавес честен, снимать нельзя.

**API:** `Ads.curtainGone` — однократный промис, `Ads.curtainWhy` — чем
разрешилось (отладка). Гарантия срабатывания: сразу на `file://` / SDK не
загрузился; на `game_ready`; страховкой; жёстким пределом. Дефолт —
РЕЗОЛВИТЬ: ошибиться в сторону «показать игру» безопасно, «ждать вечно» — нет.

⚠️ **ПРЕДЕЛ 12000, А НЕ 8000 — И ЭТО НЕ ЗАПАС «НА ГЛАЗОК».** При 8000 предел
ОБГОНЯЛ страховку: замер на живом стенде дал обещание на 8771 мс против
фактического снятия на 9442 мс — потребитель начал бы показ под ещё висящим
занавесом, то есть исходный баг, только короче. Поймал ЖИВОЙ стенд; мок этого
не воспроизводил в принципе. Плюс предел теперь не «сдаётся молча», а сам
дёргает рычаг перед тем как отпустить игру. После правки: снятие 8735,
обещание 8847 — порядок верный.

**Сьют (3 ассерта, зубы проверены обоюдно):** снят нашим `game_ready`;
повисший SDK не оставил занавес навсегда (`снят страховкой`, а НЕ `по пределу`
— различать обязательно, иначе тест зелен и когда страховка мертва); рычаг
дёрнут публичным методом. Отключение страховки валит два последних и НЕ
трогает первый; отключение сигнала — наоборот.
⚠️ Настоящий занавес в сьюте НЕ воспроизводится (на `file://` SDK нет) —
ассерты идут по МОКУ, а живой занавес я мерил отдельными пробами на стенде
`?platform_id=playgama`. Это ограничение, не «зелёный значит проверено».

## Как пользоваться (владельцу)

ДВА РЕЖИМА:

1. **ДИСПЕТЧЕР (рутина, самый выгодный)** — один чат на пачку задач разных
   направлений:
   > «Миксер, диспетчер: 1) графика — …, 2) интерфейс — …, 3) интеграция — …»
   Агент-диспетчер раскидывает задачи по ПАРАЛЛЕЛЬНЫМ субагентам (каждый
   в своей зоне), затем ОДИН общий build+test+repack и один отчёт.
   Непересекающиеся зоны идут одновременно; физику/ядро диспетчер
   сериализует. Каждую задачу субагент верифицирует headless-пробой.

2. **ОТДЕЛЬНЫЙ ЧАТ НАПРАВЛЕНИЯ (творчество/приёмка)** — когда нужен живой
   диалог с владельцем (визуал, дизайн-решения, обсуждения):
   > «Миксер, направление ГРАФИКА. Прочитай Blender/WORKSTREAMS.md и
   > CLAUDE.md, работай только в своей зоне, обнови свой блок в конце.»

Направления: ГРАФИКА · ФИЗИКА · ПОВЕСТВОВАНИЕ · ИНТЕРФЕЙС · ИНТЕГРАЦИЯ.

Принцип экономии: вся память проекта — в md-файлах (WORKSTREAMS, CLAUDE,
DESIGN-ROADMAP), поэтому чаты ОДНОРАЗОВЫЕ и короткие: новая сессия стартует
с чистого листа за ~20к токенов вместо таскания истории длинного чата.
Закончилась порция — чат можно закрывать навсегда.

## Правила для агентов (обязательные)

1. Папки: `Blender/` — рабочая ветка (здесь всё; до 2026-07-22 звалась funnel-game-v1); `funnel-game/` —
   стабильный релиз-кандидат, НЕ ТРОГАТЬ. В v1 инициализирован GIT.
   ⚠️ ЧАТЫ НАПРАВЛЕНИЙ РАБОТАЮТ В ИЗОЛИРОВАННЫХ WORKTREE-ВЕТКАХ
   (claude/*): коммить свою работу в СВОЮ ветку по ходу; в общую сборку
   (main) её сливает ДИСПЕТЧЕР по команде владельца «собери» — сам в main
   не мержи. Свою локальную сборку показывай владельцу из своего worktree.
   Конфликтов зон при этом быть не должно (файлы направлений не пересекаются).
   ⚠️ Сессия открыта от корня Blender/ (основного клона)? В корне НЕ коммитить
   и веток не создавать: сначала уйти в свой worktree (EnterWorktree или
   `git worktree add .claude/worktrees/<имя> -b claude/<ветка>`). Основной клон
   всегда стоит на main — в нём работает только диспетчер (кейс 2026-07-22:
   ветка меты в корне увела сборку диспетчера мимо main).
2. Перед работой: прочитать этот файл целиком (чужие блоки — контекст),
   CLAUDE.md (инварианты, история «НЕ возвращать») и свой блок.
3. Работать только в файлах своей зоны. Нужна правка чужой зоны — записать
   запрос в раздел «Междузонные запросы» и НЕ править самому.
   Исключения: `00-config.js` — можно ДОБАВЛЯТЬ свои константы (чужие не
   менять); `99-main.js` loop — можно добавить вызов своего тика (сам цикл
   и сон физики принадлежат ФИЗИКЕ).
4. После изменений: `python3 build.py` && `node test.js` → «ERRORS: none»
   обязателен. Динамику проверять headless-пробами (Playwright), НЕ
   превью-табом (он душит rAF).
5. Закончил сессию: обновить свой блок (Состояние/Сделано/Дальше), поднять
   версию сборки в `src/shell.html` («сборка v1-test-N»), добавить строку
   в «Журнал версий», пересобрать `release/mixer-v1-testers.zip`
   (cp index.html Mixer.html; zip с README.txt).
6. Геймплей-константы и правила игры — решения ВЛАДЕЛЬЦА: менять только по
   его прямой просьбе, каждое изменение фиксировать в CLAUDE.md.
7. РЕЖИМ ДИСПЕТЧЕРА: получив пачку задач разных направлений — раскидай их
   ПАРАЛЛЕЛЬНЫМИ субагентами строго по зонам (каждому в промпт: его блок
   из этого файла + его задача + правило «только свои файлы»); задачи,
   трогающие одну зону или физику/ядро, — последовательно. После всех:
   один build+test, одна пере-паковка, один бамп версии, обнови ВСЕ
   затронутые блоки и журнал, один коммит.
10. РОЛЬ ДИСПЕТЧЕРА — УЗКАЯ (правило владельца 2026-07-22: «только такие
   задачи можно решить тут, ты диспетчер. все остальное должны делать
   твои агенты»): сам диспетчер делает только координацию, мержи, сборку,
   доки, аналитику (таблицы/отчёты) и однострочные HUD-мелочи по прямой
   спеке (пример: чип общих звёзд). Всё содержательное — код механик,
   визуала, физики, интерфейса — уходит агентам направлений по зонам.
9. ПОВЕДЕНИЕ ОБЪЕКТОВ — ТОЛЬКО ЧЕРЕЗ ФИЗИКУ (правило владельца
   2026-07-22, дословно: «все что связано с поведением объектов должно
   уходить в физику... шар бомбу нужно прогонять через физику, как и
   остальные задачи»). Любая задача про то, как объекты двигаются,
   взрываются, сталкиваются, спавнятся и удаляются из мира — делает чат
   ФИЗИКИ, даже если инициатива пришла в другой чат. Обязательная часть
   КАЖДОЙ такой задачи: перф-проверка «не растёт ли нагрузка вкладки»
   (perfStats p95 кадра/шага, счётчики тел/коллайдеров ДО и ПОСЛЕ,
   короткий соак при сомнении) — результат замера в отчёт.
8. МЕЖСЕССИОННЫЕ ЗАПРОСЫ — БЕЗ ОЖИДАНИЯ ВЛАДЕЛЬЦА (его правило,
   2026-07-21, дословно: «принимай и отправляй запросы без моего
   подтверждения, потому что это тормозит работу когда меня нет рядом.
   Это правило справедливо для всех чатов данного проекта»). То есть:
   (а) запросы другим чатам шлём сразу, не спрашивая владельца;
   (б) распоряжение владельца, переданное ЧЕРЕЗ другой чат с дословной
   цитатой, имеет силу прямого — не ждать повторного подтверждения в
   своём чате. Дисциплина зон при этом НЕ отменяется (п.3), и решения
   владельца (п.6) по-прежнему принимает только он — правило про КАНАЛ
   доставки, не про полномочия.
11. МАКЕТ ЧИТАЕТСЯ В DEV MODE, НЕ ГЛАЗАМИ (правило владельца 2026-07-23,
    дословно: «все макеты ты пристально смотришь в режиме разработчика,
    учитывая размеры, отступы и стили»). Перед вёрсткой по любой ноде
    Figma — `get_design_context` (+ `get_variable_defs` для токенов);
    скриншот ТОЛЬКО для сверки композиции, размеры с него не снимать.
    Приёмка у диспетчера — тоже по данным Dev Mode (padding/размеры/цвета
    сверяются с числами ноды, а не «похоже»); осознанное отступление от
    макета допустимо, но объясняется в отчёте.
12. СДАЧА = ВЕТКА + ОТЧЁТ ДИСПЕТЧЕРУ **ПО КАНАЛУ send_message** (урок
    2026-07-23: три готовые ветки пролежали невлитыми сутки). ⚠️ КОРЕНЬ
    (вскрыт графикой): направления писали отчёт В ОТВЕТ ВЛАДЕЛЬЦУ, в свой
    транскрипт — а cross-session-message доставляется ТОЛЬКО в одну сторону
    (диспетчер → направление), обратной доставки текстом нет. Ответ
    владельцу и отчёт диспетчеру — РАЗНЫЕ адресаты. Технически:
    `mcp__ccd_session_mgmt__send_message` с sessionId сессии-диспетчера
    («Миксер»); список сессий — `list_sessions`. Коммит в свою ветку сдачей
    НЕ является. Диспетчеру — не полагаться на входящие: периодически
    самому `git branch --no-merged main` и `git status` по worktrees.
14. ⚠️ ВЕТКА КЛОНА — ОДНА НА ВСЕХ, ПРОВЕРЯТЬ ПЕРЕД КАЖДЫМ КОММИТОМ/МЕРЖЕМ
    (2026-07-23, вскрыто интеграцией; тот же класс, что случай меты
    2026-07-22). Пока несколько сессий работают в ОДНОМ клоне, checked-out
    ветка общая: сессия A делает `checkout -b`, и все операции сессии B
    в том же каталоге идут в ЧУЖУЮ ветку — включая мержи диспетчера.
    Признаки: `git push` отвечает «Everything up-to-date» при непустом
    `git log -1`; `git log --oneline -1 main` не совпадает с HEAD.
    ПРАВИЛО: (а) перед мержем/коммитом — `git branch --show-current`,
    ждём `main` у диспетчера и свою ветку у направления; (б) закончил
    работу в корне — `git checkout main`; (в) лучше вообще не работать
    в корне: направлениям worktree обязателен (п.1). ⚠️ Чужие
    незакоммиченные файлы НЕ трогать (ни stash, ни checkout) — просить
    автора закоммитить; если `checkout` отказал из-за чужих правок,
    это защита, а не помеха: форсировать нельзя.
13. ⚠️ ДОКИ ЧИТАТЬ ТОЛЬКО ИЗ КОРНЯ `/Users/ikorzyn/Desktop/Claude/Blender`
    (случай 2026-07-23: интеграция стартовала, прочитав WORKSTREAMS.md из
    `.claude/worktrees/agitated-greider-36702b/` — там СНИМОК на момент
    ветки, журнал обрывался на v1-test-11, и половины правил не было).
    В worktree лежит копия файла на коммит этой ветки, а не текущая.
    То же для CLAUDE.md и docs/*. Диспетчер держит worktrees в чистоте:
    после сдачи и мержа — `git worktree remove` + `prune` (2026-07-23
    снесены 4 осиротевших каталога; живые каталоги направлений не трогаем).

---

## БЛОК: ГРАФИКА

**Зона:** `10-stage.js` (рендер/свет/небо/
matcap), `20-arena.js` (чаша/лопасти), `30-shapes.js` (геометрии/палитра/TYPES),
`35-steak.js`, `36-models.js` (генерённый), материалы в `40-items.js`,
`70-fx.js`, пайплайн моделей (`tools/`).

**ВУАЛЬ: ТОН ПО ЗАМЕРУ + ДВА ЗАКРЫТЫХ ВОПРОСА (запрос диспетчера 2026-07-29,
ветка claude/graphics-veil от main a4ed969 = поверх v165).** Диспетчер правил
вуаль в моей зоне сам (срочная спека владельца) и вернул три вопроса.

- **ТОН: 0x9ec2f0 -> 0x6f9fd8, LIFT 0.55 -> 0.35.** Прежний тон был страховкой
  «лишь бы не совпасть с небом», подобранной на глаз. ЗАМЕР (ур.20 Hard, вуаль
  прибита всем, средняя яркость кучи против неба): фон 0.788, куча 0.62,
  контраст **0.169 против 0.314 у сцены БЕЗ вуали** — то есть вуаль сама вдвое
  ослабляла отделение кучи от фона. Это та же болезнь, что убила прозрачность
  (предметы уплывают в небо), просто слабее и потому незамеченная. Новый тон
  при lift 0.35 даёт **0.359 — выше, чем без вуали вообще**, и остаётся явно
  синим (спека владельца «светло-синяя, не серая»).
- ⚠️ **ГЛАВНЫЙ РЫЧАГ — LIFT, А НЕ ХЬЮ.** На ОДНОМ И ТОМ ЖЕ тоне: lift 0.55 ->
  контраст 0.169, lift 0.25 -> 0.278. Именно подъём к светлому, а не оттенок,
  смазывал силуэты. Кто будет крутить тон дальше — сперва трогать lift.
- ⚠️ Комментарий у VEIL_LIFT утверждал «0.30 подобрано по скринам», а в коде
  стояло 0.55 — разошлись при добавлении тона. Теперь число и обоснование
  сходятся.
- **ЦЕНА ПРОЗРАЧНОСТИ ИЗМЕРЕНА (вопрос диспетчера №2).** Прошлый A/B тонул в
  шуме, потому что сравнивал РАЗНЫЕ ЗАПУСКИ SwiftShader. Лечение — ПАРНЫЙ
  ЧЕРЕДУЮЩИЙСЯ замер ВНУТРИ ОДНОГО процесса на ОДНОЙ спящей куче: блоки
  transparent ON/OFF идут вперемежку, дрейф машины бьёт по обеим рукам и
  вычитается. Итог (183 предмета, вуаль всем = худший случай): **48.6 против
  47.1 мс, дельта 1.5 мс (3.2%), знаковый тест 5 из 8 пар**. Пять из восьми —
  монета: эффект НЕ ОТЛИЧИМ ОТ ШУМА даже при парном дизайне. Вывод: прозрачность
  НЕ ДОРОГАЯ, и мой прежний довод «цена платится за всех и всегда» снят
  замером. Откат прозрачности был и остаётся решением ПО ВИДУ (сквозь кучу
  видно дно чаши — это структурное свойство альфа-блендинга в плотной массе,
  тоном не лечится), а не по перфу.
- **ПЕРЕКЛЮЧЕНИЕ СЛОЖНОСТИ МОЖНО ПРИМЕНЯТЬ СРАЗУ (вопрос №3).** Замер флипа
  `material.transparent` на 183 живых материалах: **1-й раз 34 мс** (three
  компилирует второй вариант программы — transparent входит в ключ через
  `#define OPAQUE`), **каждый следующий 1.2-1.6 мс** (обе программы в кэше).
  34 мс один раз за сессию в МЕНЮ — незаметно. ⚠️ Сейчас вопрос всё равно
  спящий: при `VEIL_MODE='desat'` флаг `transparent` не ставится вовсе
  (`40-items:62` гейтит его на `'fade' && CFG.hard`), то есть сложность УЖЕ
  применяется мгновенно. Ограничение оживёт только при возврате к `fade` —
  тогда чинится флипом с `needsUpdate` в applyHard по этим числам.
- Хуки (постоянные, как matcapTuner): `__game.veilTune(hex, light, lift)` —
  тон/светлота/подъём на живой сцене без пересборки (владелец возвращался к
  тону дважды, контактный лист вариантов снимается одним прогоном);
  `__game.setItemsTransparent(bool)` — флип прозрачности + его цена в мс.
- ⚠️ **ПОБОЧНАЯ НАХОДКА: ТОН ВУАЛИ МОЛЧА КРАСИЛ ГХОСТ-ПОРТРЕТЫ КОЛЛЕКЦИИ.**
  Гхост закрытых типов ПЕРЕИСПОЛЬЗУЕТ ту же юниформу `uVeil`, что и боевая
  вуаль — значит и её тон. Спека владельца для гхостов противоположная:
  «прозрачные, немного матовые, но БЕСЦВЕТНЫЕ». Замер среднего цвета силуэта
  ДО правки: rgb(81,117,161), синева b−r = **+80**. Появилось это ещё в v165
  (вместе с тоном), не в моей правке, — но мой более насыщенный тон усилил бы.
  ЛЕЧЕНИЕ: `itemThumb` на время СЪЁМКИ гхоста возвращает uVeilCol в нейтраль
  (белый: `vec3(vLum)*1` = честный серый) и сразу восстанавливает — ровно как
  соседние сохранения color/opacity/uVeil. Боевая вуаль не затронута.
  Замер ПОСЛЕ: rgb(191,191,191), b−r = 0, разброс каналов 0.
  ⚠️ ОБЩЕЕ ПРАВИЛО: у `uVeil` ДВА потребителя с РАЗНЫМИ требованиями (бой —
  цветной, коллекция — бесцветная). Меняешь тон вуали — проверь гхост.
- Владельцу отправлен контактный лист: день/ночь × три тона, естественный Hard.

**КАЧЕСТВО КОЛЛЕКЦИИ + ШРИНК-ФИКС + TAP=HOVER + ПЕРЕЛИВ. БОМБА (батч владельца
через диспетчера 2026-07-28; ветка claude/graphics-portq от main 55a5294=v117).**
Четыре задачи, все в моих материалах/рендер-машинерии; интерфейс правит CSS/
текст карточки — зоны не пересеклись.
- **#7 КАЧЕСТВО + MATCAP.** Портреты УЖЕ были на matcap (itemMaterial), «низкое
  качество» = буфер 132px тянулся на ~150px карточку с апскейлом (замер скрином:
  мыло). Поднял буферы: `THUMB_PX 132→256`, `SPIN_PX 176→256` (85-hud). Скрин
  police/tiger/strawberry — резкие, matcap сочный. Память: два офскрина 256²
  RGBA = ~0.5 МБ, одноразово.
- **#3 ХОВЕР НЕ ШРИНКАЕТ.** Диагноз: статика (itemThumb) кадрировала по СИЛУЭТУ
  при одном угле (тесно), спин — по ОХВАТНОМУ ЦИЛИНДРУ (шире, с запасом на
  поворот). Цилиндр ≥ силуэт → на hover подмена img→канвас УМЕНЬШАЛА объект.
  Лечение: `frameSpinCylinder(mesh)` обобщён в `frameCylinder(cam, mesh)`, и
  itemThumb СТАЛ кадрировать ТЕМ ЖЕ цилиндром (силуэт-рамка удалена вместе с
  осиротевшими `_thv/_thm`). Доказано хуком `thumbFrames`: thumbW===spinW
  бит-в-бит (car 1.3425, banana 1.8265, tiger 1.2855), camEq. Скрин static vs
  live-spin — один масштаб (проверил на плоских car/banana, где шринк был худшим).
- **#4 TAP=HOVER.** Рендер-контракт был event-agnostic (`thumbSpinStart(item,
  host)`/`thumbSpinStop()`), тап работает так же, как hover. Пробел на мобиле —
  нет mouseleave, спин нечем СНЯТЬ. Добавил `thumbSpinToggle(item, host)` (85-hud):
  тап по неактивной заводит (общий канвас сам снимает прежнюю), повторный тап по
  ТОЙ ЖЕ — снимает. Интерфейс вешает ОДИН обработчик на тап. Хук
  `__game.thumbSpinToggleKey(key, sel)`. Размер тап-спина = статики (единый
  frameCylinder, #3) — тап не «дёргает» масштаб.
- **#2 ПЕРЕЛИВАЮЩАЯСЯ БОМБА** (радиус +30% сделан МЕТОЙ/диспетчером в конфиге —
  не моё). three r149 не умеет `MeshPhysicalMaterial.iridescence` (r150+, UMD
  выше r160 нет), поэтому пеку СОБСТВЕННЫЙ радужный matcap (`bombMatcap`,
  40-items) + плоский `MeshMatcapMaterial` (было `MeshBasicMaterial 0x181a20`).
  Matcap = лукап по нормали В КАМЕРЕ: пока шар катается, разводы «плывут» сами,
  без света. Тёмная жемчужная база + тонкоплёночная радуга (hue по радиусу И
  углу = масляные разводы) + узкая искра. Скрин на белом и тёмном — читается.
  ⚠️ Вуаль в Hard (60-access, ЧУЖАЯ зона) бомбу НЕ исключает, но applyVeil
  множит color на НЕЙТРАЛЬНЫЙ DIM_GREY — завалённая бомба лишь тускнеет на ~30%,
  hue цел (десатурации нет). Если владелец захочет always-on перелив как у
  камня — запрос ФИЗИКЕ: `&& !it.bomb` в 60-access:116.
- Сьют: +4 ассерта (#2 материал бомбы, #3 thumbFrames.equal, #4 toggle on/off).
  Хуки-регрессии `thumbFrames(key)`, `bombMatKind()`. Временные dbgFrame/dbgBomb
  сняты.

**БОЛЬШЕ МЕЛКИХ МОЛНИЙ В ТУРБО (спека владельца 2026-07-28 через диспетчера;
ветка claude/graphics-bolts от main 04c2125 = v137).** Критерий владельца —
«гуще и мельче»; конкретику диспетчер делегировал мне.
- **ОТВЕТВЛЕНИЯ ВМЕСТО «ЗВАТЬ ЧАЩЕ».** Разряд теперь = основная дуга +
  `BOLT_FORKS`=5 коротких веток (12-32% длины дуги, свой джиттер). Наивный путь
  «звать boltFX в 6 раз чаще» дал бы ×6 объектов, материалов и draw call в самом
  нагруженном режиме. Здесь ВСЕ филаменты слоя сливаются в ОДНУ геометрию, и на
  разряд по-прежнему РОВНО 2 объекта / 2 материала / 2 draw call — как у прежней
  одиночной дуги.
- ⚠️ **СЛИЯНИЕ РУКАМИ: `BufferGeometryUtils` В UMD r149 НЕТ** (в бандле от него
  только строка в тексте ошибки `BufferGeometry.merge`; проверено
  `typeof THREE.mergeBufferGeometries === 'undefined'`). `mergeTubeGeos` копирует
  ТОЛЬКО `position` + `index` со сдвигом индексов: материал молний —
  MeshBasicMaterial без света и текстур, `normal`/`uv` шейдером не читаются
  (втрое меньше данных). Индекс Uint16, пока вершин ≤65535 (на разряд ~500).
- **ПУЛ МАТЕРИАЛОВ** (просьба диспетчера «не плодить материалы на разряд»):
  `boltMat` — FREE-LIST, а НЕ общий материал. Шарить один материал между
  ОДНОВРЕМЕННЫМИ молниями нельзя (у каждой opacity мерцает индивидуально),
  поэтому переиспользуются только освободившиеся: `stepFX` по флагу
  `userData.poolBolt` кладёт материал в пул вместо `dispose` (кап 24).
- **АМБИЕНТНЫЙ ТРЕСК ГУЩЕ** (99-main): тик 200-360 мс → `BOLT_TICK_MS/JIT`
  130-240 мс, за тик `BOLT_PER_TICK`=2 разряда, дистанция `BOLT_MAX_D` 5.5→4.2
  (дуги во всю чашу читались как «пара толстых молний»). ⚠️ Дорогая часть тика —
  `filter+sort` по всей куче; она делается ОДИН раз, а оба разряда берут пары из
  готового списка (участить сам тик было бы втрое дороже). До 3 попыток найти
  близкую пару — раньше неудачный жребий гасил весь тик и треск заикался.
- ⚠️ **ПРОПОРЦИЯ ОБОЛОЧКА:ЯДРО ~3:1, А НЕ 2.3:1 КАК У ТОЛСТОЙ ВЕРСИИ.** Первый
  подбор (0.060/0.026) на крупном плане дал БЕЛЫЕ нитки: при утоньшении синий
  ореол первым уходит в субпиксель, ядро догоняет оболочку по экранной ширине и
  «электричество» пропадает. Итог 0.075/0.024 (ветки 0.038/0.012) — тоньше
  прежних 0.09/0.035, но ореол читается. Жизнь 0.18→0.16 с.
- **ЗАМЕР ПЛОТНОСТИ (объективный, не «на глаз»):** серия быстрых кадров в турбо
  + подсчёт молниевых пикселей: база 1337 в среднем / пик 1871 против 3035 / 5588
  — **×2.3 в среднем, ×3 на пике**. ⚠️ Метрика частично ловит синеву самой сцены
  (у базы «пик» оказался кадром вообще без разряда) — поэтому решающим был
  крупный план через `__game.boltProbe(ms)`.
- **ПЕРФ по правилу 9** (A/B, полная куча ур.12, окно ТУРБО — там пик: цепь +
  досыпка 417 мс + молнии + фивер-фон): кадр p95 360.7→368.1 (+2.1%), шаг физики
  p95 4.9→5.3 мс при бюджете 25, draw calls +6, геометрии +6 на пике. Дренаж
  чистый: геометрии 31→31, fx 0, heap 28→28 МБ, ошибок страницы 0. ⚠️ Абсолютные
  числа кадра — софт-рендер SwiftShader в headless, значима только ДЕЛЬТА.
- Новый дебаг-хук `__game.boltProbe(ms)` (постоянный, как `shardBurst` у
  осколков): молния живёт 0.16 с, случайный скрин ловит её как повезёт — без
  такого хука «стало ли гуще» на глаз не проверяется. Константы — в 00-config
  (`BOLT_TICK_MS/BOLT_TICK_JIT/BOLT_PER_TICK/BOLT_MAX_D`), по правилу 3.
- Механика цепи/комбо НЕ тронута (правило диспетчера): правило промаха v138,
  `chainRefill`, тайминги и `80-gameplay` — как были; там даже вызов boltFX не
  менялся, густоту он получил «бесплатно» из самой boltFX.

**ФЛЕЙК АССЕРТА ОСКОЛКОВ «96 → 95» ПОЧИНЕН (репорт диспетчера 2026-07-28; та же
ветка).** Это тест, не продукт — молнии к нему отношения не имеют (boltFX в сьюте
не вызывается вовсе).
- ДИАГНОЗ: `base` брался МГНОВЕННО, а секция осколков идёт сразу ПОСЛЕ рекламной
  пробы, и в этот момент ещё дренажатся геометрии предыдущей секции. Если за два
  кадра их уходило больше, чем добавлял залп из 12, «после» выходило МЕНЬШЕ «до».
  Разброс базы в репорте (96 / 74 / 71) — это он и есть.
- ЛЕЧЕНИЕ (приёмом флейков вуали и радиуса — condition-wait вместо мгновенного
  чтения): (1) СТАБИЛИЗАЦИЯ базы — ждём, пока счётчик не перестанет меняться
  (3 одинаковых сэмпла по 80 мс, потолок 4 с); (2) ПИК ЗА ОКНО, а не одна
  выборка — rAF-цикл до 2 с с ранним выходом, как только рост доказан.
- ⚠️ `shardBurst` возвращает `fx.length` (ВСЕ живые эффекты), а НЕ число
  осколков. Порог раннего выхода привязан к ЯВНОМУ `N=12`; на возвращённом
  значении он мог бы стать недостижимым при чужих живых эффектах.
- ⚠️ ПРОВЕРКА ПОТРЕБОВАЛА ВОСПРОИЗВЕСТИ ПАДЕНИЕ, а не «прогнать 12 раз». Первые
  две попытки НЕ ловили флейк: в изоляции база всегда чистая (21→33, старая
  схема 12/12 PASS), а одиночный залп из 40 при жизни осколка 0.6 с к моменту
  замера ещё не умирал. Нужен НЕПРЕРЫВНЫЙ ПАДАЮЩИЙ ФРОНТ: 6 ступенчатых залпов
  по 60 через 100 мс — тогда смертей за кадр больше, чем даёт залп из 12. На нём
  СТАРАЯ схема падает 5 раз из 8 (201→153, 204→156, 147→96, 144→96 — ровно
  «peak < base» из репорта), НОВАЯ — 8/8 PASS.

**РАКУРС ПОРТРЕТА + ГХОСТ ЗАКРЫТЫХ (спека владельца 2026-07-24-в; ветка
claude/graphics-ghost от main 8a8e386).**
- РАКУРС: прежний top-down `+0.42/+0.65` владелец забраковал («уводит модель
  в нижний-правый угол, ныряет»). Новый `-0.15/-0.6` — ЛЁГКИЙ взгляд снизу
  (перёд приподнят) + 3/4. Подобран скрином на police/bee/banana (все три
  геройские: у машины перёд вправо-вверх, у пчелы видна морда). ⚠️ ЕДИНЫЙ
  ИСТОЧНИК `PORTRAIT_TILT_X/PORTRAIT_YAW0` (85-hud) — статика (itemThumb) И
  спин берут ОТТУДА, развести нельзя (иначе скачок при подмене img→канвас на
  hover, интерфейс это заметил как v101-фикс). Сьют доказывает единство:
  сменил позу → спин стартует с нового PORTRAIT_YAW0.
- ГХОСТ (спека «не открытые — прозрачные, немного матовые, но бесцветные»;
  ОТМЕНЯЕТ моё же прежнее «локнутые буквой», которое было предположением
  диспетчера, не спекой). `thumbItemForKey(key, true)` → гхост-item (кэш-ключ
  '@g' отдельный от цветного, материал `transparent`); itemThumb при
  `item.ghost` жмёт `uVeil=1` (ПЕРЕИСПОЛЬЗОВАНА вуаль-десат v84, не изобретал
  заново) + `opacity=GHOST_ALPHA=0.42`. Матовость даёт сам десат (серый =
  глина). Скрин: цветной открытый vs серый полупрозрачный силуэт — контраст
  чёткий, форма читается (покедекс). ⚠️ ГРАБЛЯ: `userData.shader` ставит
  matcapSpecPatch в onBeforeCompile НА ПЕРВОМ рендере; читал его ДО рендера →
  у свежего гхост-материала null → uVeil не применялся (гхост выходил
  ЦВЕТНЫМ). Лечение — `thumbR.compile(scene,cam)` перед чтением, ТОЛЬКО для
  гхоста (обычным uVeil=0 по умолчанию, компиляция не нужна).
- ПЕРФ: гхост-батч 55 закрытых (ур.30) — 263 мс первый раз (софт-рендер),
  0 мс из кэша; кэш раздельный (цветной≠гхост URL). Одноразово, как вариант B.
- Интерфейс вешает: ЗАКРЫТЫЕ карточки — `itemThumb(thumbItemForKey(r.key,true))`
  вместо буквы; открытые — цветной портрет (как было). Хуки: `thumbURL(key,
  ghost)`, `setPortraitPose(tx,yaw)` (⚠️ ТЕСТ-ХУК, НЕ ВРЕМЕННЫЙ: единственный
  страж инварианта позы, см. v1-test-146; чистит thumbCache).
  Сьют: 3 новых ассерта (гхост строится/отличается, ghost-флаг+transparent,
  единый источник позы). SUITE PASS (141).

**ВРАЩЕНИЕ ПОРТРЕТА ПРИ HOVER + ПОРТРЕТ ПО КЛЮЧУ (спека владельца 2026-07-24
«на витрине при наведении модель медленно вращается по горизонтали», объём
B; ветка claude/graphics-thumbspin от main 60b02c1).** Я даю МЕХАНИЗМ,
ИНТЕРФЕЙС вешает на карточку. Контракт (как shardFX-сигнатура физики):
- ⚠️ **БЛОКЕР, из-за которого объём B:** портрет в коллекции был ТОЛЬКО у
  типов, живых в текущей партии (мешей вне уровня нет) — у остальных буква.
  Владелец выбрал B: портрет ВСЕМ открытым. `thumbItemForKey(type.name)`
  (85-hud) строит портрет-меш по ключу БЕЗ тела Rapier, материал — общий
  `itemMaterial` (ВЫНЕСЕН из makeItem в 40-items, чтобы портрет и боевой
  предмет не разъезжались; matcap/вуаль/texTune честны). Кэш; key='T'+idx
  СОВПАДАЕТ с боевым → thumbCache общий, двойного рендера нет.
- **СПИН — живой офскрин-рендер, НЕ спрайт-лист** (обосновано до кода:
  «медленно» плавно = много кадров у листа, память + ступенчатость; живой
  контекст дешевле и стоит НОЛЬ вне hover). ОДИН общий `spinR`, rAF ТОЛЬКО
  пока висит hover; стоп гасит rAF и снимает канвас. `thumbSpinStart(item,
  hostEl)` / `thumbSpinStop()`; item — ТОТ ЖЕ, что в itemThumb. Канвас
  `position:absolute;inset:0` накрывает статический `<img>`; интерфейсу —
  appendChild на mouseenter, стоп на mouseleave. Авто-стоп если ячейку сняли.
- ⚠️ **Y-ИНВАРИАНТНАЯ РАМКА (frameSpinCylinder), без неё модель «дышала» бы
  зумом:** статический itemThumb кадрирует силуэт ПРИ ОДНОМ угле; при
  вращении силуэт меняется. Кадрирую по ОХВАТНОМУ ЦИЛИНДРУ вокруг локальной
  Y — его силуэт под Y-поворотом инвариантен ПО ПОСТРОЕНИЮ (three Euler XYZ:
  R=Rx·Ry, Ry не трогает Y-симметричный цилиндр). Замер на СТЕЙКЕ (вытянут):
  силуэт по фазам 153…243 px (модель реально крутится), а `camW` РОВНО
  константна (1.2705 все 8 фаз) и клипа нет — рамка ставится один раз, зум
  не пульсирует. Старт-угол = yaw статики (0.65) → появление канваса без скачка.
- **ПЕРФ (обещал диспетчеру, замерил):** один hover +3.7 мс медианы кадра
  на SwiftShader (софт-рендер headless; на реальном GPU доли мс — один
  ортокадр одного меша); стоп → rafOn=false, mounted=false (ноль вне hover,
  проверено ассертом). Вариант B: 93 портрета за 341 мс первый раз (софт),
  82 мс из кэша — одноразово на сессию. ⚠️ Если 341 мс (софт) хитчит
  открытие меню на слабом железе — построение ЛЕНИВО (per-visible-card /
  requestIdleCallback) это выбор ИНТЕРФЕЙСА в его buildMainCollection; хелпер
  кэширует, я только отдаю. На реальном GPU 93 снимка много быстрее.
- Тест-хуки: `__game.thumbItemForKey/thumbSpinKey(key,sel)/thumbSpinStop/
  spinState()/buildAllThumbs(n)`. Сьют: 6 ассертов (портрет по ключу /
  монтаж+rAF / угол растёт / camW константна / вариант B все типы / стоп).
- ⚠️ **НЕ тронуто (зона ИНТЕРФЕЙСА):** `buildMainCollection` (замена
  буквы→портрет вызовом thumbItemForKey) и hover-обвязка `.msc` — их код.
  Меты для «какие ключи открыты» НЕ запрашивал: интерфейс уже гейтит
  открытость через `unlockedTypeCount()`, а ключ = `type.name` из
  accSnapshot (в buildMainCollection уже сравнивается `it.type.name===r.key`).

**ОСКОЛКИ — ПОЛИРОВКА (спека владельца «сделай осколками», задача ФИЗИКИ
2026-07-23, закрыта; ветка claude/graphics-shards от main f41bb31).** Физика
влила СТАРТОВЫЙ shardFX в 80-gameplay через addFX; я перенесла ВИЗУАЛ в 70-fx
и отполировала. Границу берегла: правило burstFX, тайминги grindShred и
параметр тряски — НЕ трогала (зона поведения ФИЗИКИ), сигнатура shardFX та же.
- **Форма** — `makeShardGeo`: 4 угла правильного тетра сдвинуты ±38% (на месте,
  per-shard), каждый скол уникален и читается как обломок, а не «кубик д4».
- **Тинт по граням** — на MeshBasicMaterial света НЕТ, поэтому объём печём в
  ВЕРШИННЫЕ ЦВЕТА: грань светлее/темнее по своей нормали к ключевому свету
  (SHARD_LIGHT = копия matcap-света −0.36/0.60/0.72; серый множитель 0.55…1.32,
  цвет несёт material.color). Плоское пятно стало гранёным куском.
- **Звук** — `crunch` в 75-audio (fxMap): короткий фильтрованный шум + сухие
  щелчки, СПЕКТР ВЫШЕ рокота grind (тот cutoff 300 + 70 Гц) — на общей куче не
  маскируются: рокот лопастей внизу, треск раскола сверху. Зовётся ИЗ shardFX
  (opts.sound!==false), n осколков чуть жёстче. На пути помола ложится ~350 мс
  ПОСЛЕ 'grind' (шинковка по setTimeout) — «рокот… треск», не клеш.
- ⚠️ Форма скола = 12 вершин, как у прежнего TetrahedronGeometry — РЕГРЕССИИ
  ПЕРФА НЕТ. Замер (мобильный 3× DPR, худший случай 7×7=49 меш-осколков):
  пик geoms +49 / draw calls +49, шаг физики p95 3.3 мс, кадр в окне жизни
  осколков медиана ~55 мс (≈ база); после догорания geoms/calls БИТ-В-БИТ в
  базу — утечки нет (stepFX диспозит и геометрию, и материал каждого скола).
- **Дебаг-хук** `__game.shardBurst(n, opts{x,y,z,color,...})` — мост к shardFX
  над кучей: детерминированный реальный бурст/помол собрать тяжело (burstFX
  нужна пачка >=4, у помола хука нет). Поведение не трогает. Сьют: 3 ассерта
  (залп создал fx / свои геометрии на кадре / дренаж в базу без утечки) на
  свежем штиле — иначе фоновая досыпка цепи качает глобальный geoms.
- Открытый вопрос ФИЗИКЕ (не мой): при желании «искры при сколе камня» или
  усилить тряску помола под осколки — это её тайминги/поведение, запрос через
  Междузонные.

**Состояние (2026-07-21).** Предметы на MATCAP (принят владельцем): свет
запечён в процедурную DataTexture, блики не скачут при повороте по построению,
теневой пасс выключен — draw calls в движении 239 -> 128. Тени заменены
ТОНИРОВКОЙ ПО ГЛУБИНЕ КУЧИ. Фон — панорама неба по времени суток. Стекло
чаши полностью прозрачное с френель-кромкой. Предметы — ТОЛЬКО МОДЕЛИ ВЛАДЕЛЬЦА,
**78 типов** (2026-07-22): 30 фруктов-овощей + 24 зверя + 8 машин + 7 кирпичей
+ 8 пиратов, **замес 3:3:1:1:1** (решение владельца) + стейк.

**MATCAP-ТЮНЕР (запрос владельца 2026-07-22 «хочу ползунками настроить
значения визуально», закрыт).** `__game.matcapTuner()` из консоли — плавающая
панель, повторный вызов закрывает. 24 ползунка: свет Lx/Ly/Lz (ОБЩИЙ для всех
пресетов) + 7 параметров × soft/metal/tex. В код и сейв НЕ пишет: значения
уезжают кнопкой Copy (буфер + всегда дубль в консоль — clipboard на `file://`
может отказать), reload откатывает.
- **Пересъёмка — В ТУ ЖЕ DataTexture** (`bakeMatcap` вынесен из `makeMatcap`,
  `retuneMatcap` пишет в `tex.image.data` + `needsUpdate`). Материалы держат
  ССЫЛКУ на текстуру — подменять объект нельзя, а обходить материалы не нужно.
  ⚠️ `material.needsUpdate` здесь НЕ нужен: это перекомпиляция шейдера.
- **shin — логарифмический ползунок**: на линейной шкале вся полезная часть
  (2..60) сидит в первой четверти хода.
- **Счётчик потребителей в заголовке пресета** — не украшение: `metal` сейчас
  **без единого объекта** (хром-примитивы удалены из пула), и без счётчика
  владелец двигал бы мёртвый ползунок, решив, что инструмент сломан.
  Замер: soft 14, tex 114, metal 0.
- **Гейт дребезга — rAF**: 60 input-событий подряд схлопываются в ОДНУ съёмку
  (обработчик 0.1 мс; вся пачка + съёмка 33.8 мс, из них ~33 — ожидание двух
  кадров, то есть сама съёмка ≈1 мс). Кадр p95 с открытой панелью 72.1 — в базе.
- **z-index 21 — ВЫШЕ оверлеев (20)**: дебаг-панель не должна становиться
  недоступной, если поверх открылась пауза. Ниже fatal (99).
- ⚠️ **Клавиши глушатся на панели** (`keydown` + `stopPropagation`): 90-input
  слушает окно, и Space на сфокусированном ползунке улетал бы во встряску.
- Драг по панели в канвас НЕ протекает (проверено счётчиком: 2 жеста по
  панели — 0 pointerdown на канвасе, контрольный жест по канвасу — 1);
  слушатели ввода висят на `canvas`, панель ему сосед, а не потомок.
- Сьют: 5 ассертов в КОНЦЕ test.js, секция САМОВОССТАНАВЛИВАЮЩАЯСЯ (пресеты
  глобальны — испорченный matcap утёк бы в последующие проверки). Ассерт
  проверяет ПЕРЕСЪЁМКУ, а не только смену числа: `__game.matcapSum(kind)` —
  контрольная сумма пикселей.
- **НЕ добавлено сознательно** (соседние ручки, владелец их не просил):
  `TEX_GAIN`/`TEX_CONTRAST` (юниформа `uTune` ПЕР-МАТЕРИАЛЬНАЯ — живой тюнинг
  потребовал бы обхода материалов) и `uDepthTint` (тонировка по глубине; там
  наоборот ОДИН общий объект-юниформа — если владелец попросит, это ~3 строки).
  ⚠️ Для яркости ТЕКСТУРНЫХ моделей канонический рычаг — `TEX_GAIN`, а не
  `tex.amb`: аддитивный lift владелец уже забраковал (см. коммент в пресетах).

**ВУАЛЬ НЕДОСТУПНЫХ = ОБЕСЦВЕЧИВАНИЕ (спека владельца 2026-07-23: «если
включен hard mode давай недоступным объектам полностью уменьшать saturation,
они получатся светло серые. Можно попробовать сделать их прозрачными под 80%
но стоит смотреть на производительность»).**
- ⚠️ **ГЛАВНОЕ: старая вуаль текстурные модели НЕ ОБЕСЦВЕЧИВАЛА ВООБЩЕ.**
  Она лерпала `material.color` к серому, а у моделей с атласом color БЕЛЫЙ —
  лерп белого к серому просто ПРИТЕМНЯЛ текстуру. Это даже записано в
  комментарии 40-items («просто притемняет текстуру»), но следствие никто не
  считал: на полной чаше **130 завуалированных из 183 визуально неотличимы
  от доступных** (скрин `veil-tint.png` — куча целиком цветная). Работала
  вуаль только на кирпичах (`t.paint`) и процедурных, а их меньшинство.
- Лечение — юниформа `uVeil` в matcapSpecPatch: обесцвечиваем ГОТОВЫЙ цвет
  (после сэмпла атласа) в яркость и поднимаем к светло-серому. Три ALU без
  ветвления; при uVeil=0 — тождество, доступные не платят.
- Сила вуали — ОБЩИЙ объект-юниформа `uVeilTune` (как uPileTop/uDepth):
  исходник шейдера не меняется, программа по-прежнему компилируется ОДНА
  на все 183. Ползунки light/lift + галка «показать на всех» — в matcapTuner.
- `VEIL_TARGET`=1: 0.65 было откалибровано ПОД ЛЕРП ЦВЕТА (там 1.0 давало
  ровную серую заливку без формы). Обесцвечивание так себя не ведёт —
  светотень и яркость целы, поэтому «полностью» = 1.
- `VEIL_LIFT`=0.30 подобран по скринам НА ОБОИХ НЕБАХ: при 0.62 куча
  выцветала в фон и силуэты переставали читаться, при 0 тёмные модели
  оставались тяжёлыми пятнами.
- ⚠️ **Пин превью ВЫШЕ доступности** (`veilPinned`): без него превью в тюнере
  жило до следующего тика refresh (300 мс) и молча растворялось — ползунки
  были бы неподкручиваемы. Ловится только глазами на скрине, не ассертом.
- ⚠️ **Портреты витрины** гасят теперь ОБЕ ручки (color И uVeil): одного
  восстановления color стало мало — обесцвеченный портрет осел бы в кэше
  навсегда, ровно как серый до этого.

**ПРОЗРАЧНОСТЬ ('fade'): РЕАЛИЗОВАНА, НЕ ВКЛЮЧЕНА — и решает это не перф.**
- Визуально она не даёт НИЧЕГО (`veil-fade.png` против `veil-desat.png` —
  неразличимы): недоступные это ровно те, кто НЕ видит небо, то есть они и
  от камеры закрыты верхним слоем. Сквозь них видна та же куча.
- ⚠️ **ЦЕНА ПЛАТИТСЯ ЗА ВСЕХ И ВСЕГДА**: three ставит предмет в прозрачную
  очередь по флагу `material.transparent`, а не по opacity — доступные
  (opacity 1) уезжают туда же и теряют ранний Z. Дёргать флаг покадрово
  нельзя: смена transparent = перекомпиляция шейдера.
- Замер (сид 101, ур.20, куча спит, камера стоит, 3×70 кадров, медиана
  медиан): обесцвечивание **+0.1 мс мобильный / +0.3 мс десктоп** (шум),
  прозрачность на всех **+1.9 мс (+3.4%) / +0.4 мс (+0.8%)**.
- ⚠️ **ЭТОТ ЗАМЕР ПРОЗРАЧНОСТИ — НИЖНЯЯ ГРАНИЦА, А НЕ ВЕРДИКТ.** Headless —
  SwiftShader (программный растеризатор, проверено `UNMASKED_RENDERER`), а
  дорогой прозрачность делает именно то, чего у SwiftShader нет: тайловый
  HSR мобильных GPU отбрасывает перекрытую НЕПРОЗРАЧНУЮ геометрию, а
  блендящуюся обязан шейдить по порядку. На реальном телефоне цена будет
  выше измеренной, насколько — покажет только устройство.
- ⚠️ **МЕТОДИКА, чтобы не повторять ошибку**: первый замер сравнивал РАЗНЫЕ
  запуски браузера и дал бессмыслицу (desat то быстрее, то медленнее базы на
  ±25%); дрейф между launch'ами доходил до **2× на одной и той же сборке**
  (112 против 55 мс). Сравнивать можно только состояния ВНУТРИ одной
  страницы: `__game.veilAll(0/1)` для шейдера, `__game.veilFade(0/1)` для
  прозрачности (она перекомпилирует шейдер — дать кадр на прогрев).

**ПАЧКИ BRICK И PIRATE (задача владельца 2026-07-22, закрыта).**
- ⚠️ **185 файлов Brick — это НЕ 185 форм:** 23 формы × 8 вариантов кромки
  (`bevel/none/round/square` × `hq/lq`), сверху неотличимых, а сами 23 формы
  дают всего **11 футпринтов** (скосы и уголки различаются только профилем
  по высоте — с игровой камеры он не виден). Плюс дубли длины: 1x4/1x6/1x8
  и 2x4/2x6/2x8 при нормировке к общему охвату — один и тот же брусок.
  **Взято 7:** round, bar, corner, stud, classic, square, duo.
- ⚠️ **КИРПИЧИ ПОКРАШЕНЫ** (`paint:1`, решение владельца «крась кирпичи»).
  Замер атласа по UV: **все кирпичи белые** — #f9f9fc у 152 моделей из 185,
  #c0c0d7 у 31. Одиннадцать белых прямоугольников матч ПО ТИПУ не различает.
  Механизм: шейдер множит атлас на `material.color`, белая база даёт чистый
  тон; цвет берётся из палитры (`candyColor(t.color, t.dl)`), и труха
  (`fxColor`) совпадает с кирпичом сама собой. Покрашенным даётся matcap
  `'soft'` (не `'tex'`) и НЕ даётся `texTune` — те калиброваны под авторские
  атласы. Ветка `t.paint` в makeItem (40-items).
- **Pirate: взято 8** (бочка, пальма, пушка, сундук, ящик, ядро, башня,
  дверь) — у пачки 15 разных цветов в атласе, с различимостью порядок.
  ⚠️ **КОРАБЛИ НЕ ВЗЯТЫ:** пять штук — близнецы сверху (у двух пар габариты
  совпадают бит-в-бит), охват 5.0-6.5 против 1.0 у наших, заполненность
  0.15-0.18 → **convex hull соврал бы грубо** (полый корпус стал бы
  кирпичом). Флаги и пальма-флагшток плоские (сплюснутость 0.11-0.19).
  Камни `rocks-*` не трогали — резерв владельца под несовмещаемые объекты.
- **Замеры после ввода.** Заполнение: ур.20 topY 7.54, ур.80 topY 8.77 —
  в норме 7.5-9.0 (ур.1 6.65 — это PAIRS_EARLY, укороченные уровни, так
  и задумано). wallExcess 0.06/0.10/0.18, виновники — СТАРЫЕ типы, не новые.
  **Перф A/B на равной куче (182 предмета, ур.20), старый index.html взят
  из git — пересборка не нужна:** треугольников 114 580 → **94 756 (−17%)**,
  кадр p95 66.8 → **52.4 мс (−22%)**, шаг физики p95 6.3 → 6.1, draw calls
  195 = 195. Новые пачки ЛЕГЧЕ вытесняемых (кирпич 58-324 тр., пират 48-572
  против 1200 у машин) — перф не просел, а вырос.
- ⚠️ **ГРАБЛЯ КОНВЕРТЕРА:** `glb2module.py` срезает ВЕДУЩИЕ ЦИФРЫ имени
  (`re.sub(r'^[0-9]+','')`), поэтому `1x2` и `2x2` схлопывались в одно имя
  `x2`. Staging в `.lowpoly` называть БЕЗ цифр в начале (взяты stud/duo/
  square/classic/bar/round/corner). Прореживание не понадобилось: Brick
  max 628 тр., отобранные Pirate max 572 — всё ниже KEEP_UNDER 1500.
- Вес сборки 6.17 → 6.50 МБ raw (два атласа + 15 моделей).

**ПРОЦЕДУРНЫЕ ФОРМЫ УБРАНЫ ИЗ ПУЛА** (решение владельца 2026-07-21): куб,
шар, конус, тор, цилиндр, окта, доде, тетра, узел, спираль, звезда, сердце,
пилюля, яйцо, призма, гайка, кристалл — 17 штук. ⚠️ Их ФАБРИКИ в 30-shapes
ОСТАВЛЕНЫ ЖИВЫМИ намеренно: на `gemGeo` висит фолбэк геометрии сюрприза
(`makeSurprise` в 40-items), а он опирается на `mergeGeos` — рвать цепочку
ради косметики не стоит. Вернуть примитив = добавить строку в TYPES.
Стейк НЕ удалён: это модель владельца (35-steak, OBJ с вершинными цветами),
а не примитив. ⚠️ Ветки `case 'cube'/'ball'/...` в 50-physics и
`buildAccessSamples` стали мёртвыми — это ЧУЖАЯ зона, не трогал.

**ПОНЧИК УВЕДЁН В ХВОСТ TYPES** (решение владельца): его выпуклая оболочка
заливает дырку, и в Hard бублик «перекрывает» видимое сквозь неё. Выбран
увод, а не компаунд капсул. Замер: на ур.1 и ур.30 пончика в пуле НЕТ,
появляется к ур.60 (индекс 62 из 63, открывается на 54-м).

**Ручки в 00-config:** `TEX_GAIN`/`TEX_CONTRAST` (яркость и контраст текстур,
1.02/1.08), `DEPTH_TINT_MIN`/`DEPTH_TINT_RANGE` (0.65/3.2), `GLASS_EDGE`/
`GLASS_POW` (0.30/2.6), `CFG.matcap` (аварийный откат на MeshStandard).
Все подобраны владельцем ПО ШКАЛАМ — контактные листы с вариантами на одной
и той же куче, а не на глаз.

**Пайплайн моделей (три шага, все в `tools/`):**
1. `blender-decimate.py` — упрощает ТОЛЬКО тяжёлое (KEEP_UNDER 1500,
   TARGET 15000; машинам временно ставили 1200). Квадричное схлопывание
   рёбер Blender; для моделей из тысяч несвязанных оболочек — воксельный
   ремеш перед ним.
2. `glb2module.py` — GLB в data-модуль: позиции, ИСХОДНЫЕ нормали и UV,
   индексы КАК В ФАЙЛЕ; атлас каждой пачки как data-URI.
3. `sky2module.js` — панорамы 4096×2048 PNG в JPEG 1536×768 (1 МБ -> 32 КБ).

**⚠️ ГРАБЛИ — не наступать заново:**
- `emissive` у matcap НЕТ, а `hintPulse`/`scopePulse` пишут его напрямую ->
  кнопка Hint падала. **test.js подсказку НЕ ПОКРЫВАЕТ** — проверять руками.
- Канвас премножает RGB на альфу -> matcap только через DataTexture.
- Пересчёт нормалей через `computeVertexNormals` даёт ПЛОСКОЕ гранение:
  модель выглядит комком независимо от полигонажа. Брать нормали ИЗ ФАЙЛА.
- Прореживание схлопыванием вершин В СЕТКУ рвёт тонкую геометрию на осколки.
  Забраковано владельцем, удалено. Только Blender.
- Яркость текстур поднимать УМНОЖЕНИЕМ: аддитив задирает тёмные места
  и убивает контраст («всё сильно светлое»).
- Опора тонировки — 85-й ПЕРЦЕНТИЛЬ высоты по куче НИЖЕ КРОМКИ. Максимум
  по всем живым уводил её за досыпкой в турбо, и куча гасла разом.
- `flipY = false` у атласов: glTF считает UV от верхнего левого угла.
- Геометрию сюрприза НЕ привязывать к модели из папки без фолбэка: партия
  сменилась, функция исчезла, genLevel падал ДО создания предметов —
  чаша пустая, в консоли тихо.
- `makeMatcap`: знак `ny` ревью счёл спорным. НЕ «исправлять» мимоходом —
  все пресеты откалиброваны владельцем под текущий.

**Сделано по заметкам ревью 2026-07-21:** докстринг `glb2module.py` переписан
по факту (описывал старый CLI и врал, что текстуры отбрасываются),
`sky2module.js` проверяет аргументы, атлас получил 1×1-заглушку до декода.
⚠️ Чёрную вспышку, ради которой заглушка просилась, ВОСПРОИЗВЕСТИ НЕ УДАЛОСЬ
(доля тёмных пикселей на первых кадрах одинакова до и после) — заглушка
оставлена как защита от неопределённого поведения, не как починка бага.

**Фон под интерфейс (запрос ИНТЕРФЕЙСА, закрыт замером 2026-07-21):**
белые глаза и кнопка паузы на светлой панораме давали контраст **1.6:1**
при пороге WCAG 3:1. ⚠️ Предложенный интерфейсом **#d0dff3 проблему НЕ
решает**: его яркость 185, контраст 1.35:1 — ХУЖЕ нынешнего. Считать надо
по ЛИНЕАРИЗОВАННОЙ яркости WCAG, а не по средневзвешенным байтам (мой первый
измеритель врал именно так). Решено ЗАТЕМНЕНИЕМ ВЕРХНЕЙ ПОЛОСЫ ЭКРАНА
(SKY_TOP_DIM 0.60 / SKY_TOP_FROM 0.70 в 00-config, экранный градиент в
шейдере неба рядом с лихорадкой). Стало **3.5:1 утром и днём, 16:1 ночью**.
Низ кадра не тронут — панорама осталась как принял владелец.

**Лихорадка снизу + помол сверху (спека владельца через ИНТЕРФЕЙС, 2026-07-21-в,
уточнена 2026-07-21-г).** Экранные градиенты неба в 10-stage:
- КОМБО, ЦВЕТ ПО ВРЕМЕНИ СУТОК (владелец 2026-07-21-г: «голубой только в тёмное
  время, в светлое — зелёный»): `feverColorNow()` в 10-stage выбирает `FEVER_NIGHT`
  `#8cc7ff` в ночь / `FEVER_DAY` зелёный `(0.30,0.87,0.50)` днём. Граница = как
  у панорам (`skyForNow`: ночь `h<5||h>=18`), считается РАЗ при загрузке —
  согласованно с выбором панорамы (обе от `new Date()`). Раньше `uCombo=1`
  уходил в ЧИСТУЮ БЕЛИЗНУ и выбеливал верх — теперь мягкое СВЕЧЕНИЕ у нижней
  кромки: гаснет кверху (`FEVER_SPAN` 0.75), потолок `FEVER_MAX` 0.60 (K≈0.55-0.6),
  небо читается и в цепной реакции.
- ПОМОЛ, ЛЕСЕНКА УГРОЗЫ (владелец 2026-07-21-г): красный ВЕРХ (`GRIND_COLOR`,
  зеркально низу, `1−sy`). Цель `uGrind`: лопасти работают → 1; иначе за
  `GRIND_LEAD`=10 с до помола растёт САМА по таймеру `(10−left)/10` «медленно»
  (`left = idleLimit − (now−lastAction)/1000`); иначе 0. Матч сбрасывает
  `lastAction` → цель 0, гаснет БЫСТРЕЕ подъёма (`GRIND_FADE_DN` 0.20 с вниз /
  `GRIND_FADE_UP` 0.35 с вверх — «плавно, но быстро»). Драйв — в 99-main рядом
  с сигналом `grinding` (санкционировано диспетчером). Замер: цель точна на
  left 9/5/2 с, помол = 1.0, гашение ~380 мс vs подъём ~640 мс.
  ⚠️ HARD: терпение ровно 10 с → красный наливается весь цикл простоя после
  каждого матча — прямое следствие спеки, владельцу озвучено диспетчером.
- ⚠️ КОНТРАСТ HUD НА ПОМОЛЕ (замер делался, когда ЕЩЁ БЫЛО статичное
  затемнение; само оно снято приказом владельца 2026-07-22): красный лежал
  ДО затемнения и только темнил полосу — день 3.7→6.5:1 по лесенке, ночь
  16→9.5:1. ⚠️ ЧИСЛА УСТАРЕЛИ вместе с затемнением, но ВЫВОД жив: красный
  помола ТЕМНЕЕ светлого дневного неба, поэтому во время помола контраст
  белого HUD только улучшается относительно спокойной базы (днём она теперь
  1.5-1.6:1). Пересчитывать не стал — статичной базы, от которой считали,
  больше нет.

**ПАК-ЭФФЕКТЫ ПЕРЕЕХАЛИ В 70-fx (просьба ФИЗИКИ, сделано 2026-07-22).**
`juiceFX`/`sparkFX`/`starPopFX` жили в 80-gameplay «стартовой» версией —
перенесены ко мне и отполированы. ПРАВИЛО выбора (`burstFX`, `BURST_MIN_N`)
и волна `blastWave` НЕ тронуты — они в чужих зонах.
- КРУГЛЫЕ точки вместо квадратных: `PointsMaterial` без карты рисует точку
  КВАДРАТОМ — сок и искры читались как пиксели. Добавлены общие ленивые
  карты `fxDotTex` (диск с мягким ободком) и `fxStarTex` (5-лучевая звезда).
- ЗВЁЗДЫ — точки со звёздной картой вместо мешей: всегда лицом к камере
  (плоский меш с игрового ракурса ловил ребро и почти пропадал), и 5 мешей
  (5 draw calls, 5 геометрий, 5 материалов) свернулись в ОДИН Points.
- ⚠️ ГРАБЛЯ, из-за которой звёзды НЕ спрайты: в three r149 ВСЕ `THREE.Sprite`
  делят ОДНУ геометрию, а `stepFX` диспозит `geometry` догоревшего эффекта —
  первый же спрайт убил бы все последующие. Points имеет свою геометрию.
- ⚠️ Общие карты безопасны: `stepFX` диспозит material, но НЕ его `map`
  (three текстуры материала не трогает). Карты строятся раз и живут вечно
  (+2 текстуры к базе, это константа, не утечка).
- Замер: диспетчеризация подтверждена дампом живых fx с привязкой к
  фактическому падению счётчика типов — food 46 точек, car 36, animal 7,
  у всех `map=true`; fx дренаж в 0, `geoms` возвращается в базу (39→39).

**БАНАН +40%** (просьба владельца 2026-07-22). Масштаб задан В ГЕОМЕТРИИ
(`geo:()=>foodbananaGeo().clone().scale(1.4,…)`) + парный `rc:1.4`: из
геометрии берутся и `half` (тест стены по OBB), и convex hull коллайдера,
поэтому картинка, физформа и габарит сходятся сами, а `rc` держит игровую
метрику (зазор пар, доступность, `wallR`). Менять что-то одно из двух НЕЛЬЗЯ.
⚠️ `.clone()` ОБЯЗАТЕЛЕН: `modelGeo` оборачивает МОДУЛЬНЫЕ массивы 36-models
без копии, а `.scale()` мутирует и позиции, и нормали. Сейчас это молчало бы
(geoCache не чистится, `geo()` зовётся раз), но стоит очистить кэш на регене —
банан рос бы на 40% КАЖДЫЙ раз. Клон снимает мину целиком.

**ДОБОР ТИПОВ ИЗ FOOD И CAR (просьба владельца 2026-07-22): +15, пул 78 -> 93.**
Взято 11 еды (leek, fish, turkey, cheese, sundae, chinese, whole-ham, taco,
hot-dog, cake-birthday, ice-cream-scoop-mint) и 4 машины (cone, box, truck,
kart-oobi).
- ⚠️ **ГЛАВНОЕ, ЧТО НАДО ЗНАТЬ: добор удлиняет ХВОСТ, а не насыщает начало.**
  Новые типы встают на позиции 75-91, то есть открываются на **уровнях 67-83**.
  Иначе быть не может: `genLevel` берёт ПЕРВЫЕ 9+уровень−1 типов, а лучшие по
  различимости уже стояли впереди — добор заведомо слабее и обязан идти в хвост
  своего пула. Если нужна бОльшая пестрота на РАННИХ уровнях — это не добор,
  а ПЕРЕСТАНОВКА порядка (ценой чёткости старта). Вопрос владельцу.
- Отсев был жёстче набора. Food: из 200 сразу минус **утварь** (посуда, ножи,
  кастрюли — принцип проекта), минус пицца (владелец убирал), минус производные
  (`-half`/`-slice`/`-raw`) — остаётся 64, из них выброшены семейства-близнецы.
  Цветовые кластеры вскрыли их точно: **8 моделей на #995941**, 6 на #eb9268,
  а `maki-salmon`/`maki-vegetable` совпадают И габаритом (0.12×0.07×0.10),
  И цветом — настоящие близнецы. `leek` и `radish` сверху оба зелёные пучки:
  взят один.
- Car: из 50 минус 12 `debris-*` (обломки) и 9 `wheel-*` (одинаковые диски).
  ⚠️ Пять `kart-*` идентичны бит-в-бит (0.93×1.26×1.43, один цвет) — взят один.
  Остальные 16 — родовые «прямоугольники-машины», различает их только ЦВЕТ
  кузова, и он конфликтует с уже взятыми (красный sedan против пожарной,
  зелёный delivery против мусоровоза). Поэтому взяты только внятные силуэты:
  конус, ящик, длинный тягач, карт. **Больше различимого в пачке Car нет.**
- ⚠️ Замер цвета машин ВРЁТ на доминирующем UV: у всех выходит #36363a — это
  шины и стёкла, а не кузов. Цвета `color` для машин ставлю ПО КУЗОВУ с
  контактного листа (как и были заданы существующие), а не по замеру.
- Тяжёлые машины (truck 2082, kart-oobi 2884) сведены к 1200 тр. — как
  существующие восемь. ⚠️ В `tools/blender-decimate.py` добавлено
  переопределение `DECIMATE_TARGET` из окружения: раньше докстринг требовал
  ПРАВИТЬ TARGET РУКАМИ и не забыть вернуть — забытая правка тихо испортила
  бы следующую партию. Прогон: `DECIMATE_TARGET=1200 blender --background …`.
  Силуэты после схлопывания целые (проверено контактным листом по .lowpoly).
- Замеры: ур.20 topY 7.54 / ур.85 topY 7.84 (норма 7.5-9.0); шаг физики p95
  6.2 и 6.5 мс (бюджет 25); треугольников на ур.85 даже МЕНЬШЕ (88 990 против
  95 020 на ур.20) — новая еда легче той, что разбавляет. Вес 6.87 МБ raw /
  **1.73 МБ gzip** (лимиты площадок 8-50 МБ).

**НЕБО — МНОГОСТОПНЫЕ ПАЛИТРЫ ВЛАДЕЛЬЦА + ГРАНИЦА 20:00** (спека 2026-07-31:
«попробуй ночное время сделать с такими параметрами» + «день до 20:00, ночь
с 20:00»). Ветка `claude/graphics-sky-colors` поверх сдачи панорам.
- ПАЛИТРЫ: `SKY_STOPS` в 00-config — день 7 стопов (#6e86ff -> #ccfff8), ночь
  12 (#031d83 -> #ff2fdc). Хранятся ХЕКСАМИ (владелец правит их же CSS-строкой),
  по-прежнему СЫРОЙ sRGB. Прежние три опоры, снятые с панорам, отменены.
- МЕХАНИКА: `buildSkyRamp` печёт 1D-рампу 256×1 DataTexture, шейдер сэмплит.
  ⚠️ Ширина 256 (степень двойки) выбрана по правилу 9: NPOT в WebGL1 легален
  только с CLAMP_TO_EDGE без мипов — берём заведомо обкатанное. Интерполяция
  между стопами печётся на CPU в СЫРОМ sRGB, как у CSS linear-gradient: иначе
  небо и карточка Play разъедутся серединой.
- ⚠️⚠️ **РАСКЛАДКА ПО ЭКРАНУ, А НЕ ПО ВЗГЛЯДУ — РЕШЕНИЕ ПО ЗАМЕРУ** (`SKY_MAP`
  в 00-config, дефолт 'screen'; живой A/B `__game.skyMap('view'|'screen')`).
  Камера смотрит СВЕРХУ В ЧАШУ, поэтому по высоте взгляда на экран попадает
  лишь ХВОСТ рампы: замер по пикселям дал позиции **70.6%..100% (день)** и
  **70.5%..97.8% (ночь)** — первые ~70% стопов владельца не видно НИКОГДА,
  ночь вырождается в сплошную мадженту. По экрану видно все 100%.
  ⚠️ И ВТОРОЕ, НЕ КОСМЕТИЧЕСКОЕ: `--sky-top-rgb`/`--sky-bot-rgb` (тинт полос
  Safari 26) равны первому/последнему стопу. При 'view' они расходятся
  с реальной кромкой кадра (замер: 110,134,255 против пикселя 132,227,248),
  при 'screen' совпадают ПО ПОСТРОЕНИЮ — перемерено профилем, Δ0 на обеих
  кромках и в обеих темах.
- ГРАНИЦА ЧАСОВ 18 -> 20, и ЧИСЛО БОЛЬШЕ НЕ ДУБЛИРУЕТСЯ: `SKY_DAY_FROM`/
  `SKY_NIGHT_FROM` в 00-config читают ОБЕ функции — `skyTimeNow` (10-stage) и
  `isNightSky` (85-hud, вторжение в файл ИНТЕРФЕЙСА по согласованию через
  диспетчера). Итог: день 5–20, ночь 20–5.
- ФОРС-ХУК `?hour=N` (`skyHourNow` в 10-stage) — по запросу ИНТЕРФЕЙСА: до него
  тема витрины, инверсия Shake и правило цвета кнопок проверялись только
  подменой Date. Плюс `__game.skyHour()`.
  ⚠️ Хук НЕ закрыт гейтом DEV сознательно: все его потребители косметические
  (палитра, цвет лихорадки, звёзды, html.night), и любое значение даёт
  ЛЕГАЛЬНОЕ состояние, которое игрок и так видит после 20:00. Захочет
  диспетчер строгости — одна строка.
- СТРАЖ В СЬЮТЕ: часы 4/5/19/20/23 отдельными загрузками, ассерт на СОВПАДЕНИЕ
  неба и темы, а не на сами числа. ⚠️ Проверен ДИВЕРСИЕЙ (вернул 18 в
  isNightSky): час 19 дал небо дневное при теме ночной — страж поймал, значит
  не пустой.
- ЧИТАЕМОСТЬ (перемерено ПРОФИЛЕМ строки, локальный контраст белка к небу
  РЯДОМ с ним): **ДЕНЬ 1.5-1.6:1 -> 2.98:1** — исторический риск «дневной фон
  204, контраст белых глаз ~1.6» практически снят, порог 3:1 теперь достаётся;
  НОЧЬ 13.08:1 (было ~12.6-13.2), кнопка паузы ночью 13.48:1.
  Сигналы поверх базы живы: угроза помола у ВЕРХА даёт сдвиг цвета 198-203,
  лихорадка комбо у НИЗА — 107-116 (порог заметности ~25). «Красное на
  розовом» не возникает по построению: угроза красит верх, маджента внизу.
  ⚠️ МЕТОДИКА: точечный замер двумя пикселями здесь ВРЁТ — я на нём обожглась
  (получила расхождение кромки 90 там, где профиль даёт Δ0). Только профиль.
- ⚠️⚠️ **РЕГРЕССИЯ, НАЙДЕННАЯ РЕВЬЮ И ПОЧИНЕННАЯ — ПРОТУХШАЯ `uResY`.**
  База неба теперь зависит от `uResY`, а пишет её ЕДИНСТВЕННОЕ место — `resize()`
  (99-main). `applyPerfTier('low')` меняет pixelRatio и размер буфера ПОСРЕДИ
  ИГРЫ (tickPerfTier решает по медиане кадра через 2.5 с) и resize() не звал.
  До правки это было почти безобидно (uResY кормил только uCombo/uGrind, в
  покое равные нулю) — теперь срезало ВЕРХ ПАЛИТРЫ. Замер на 400×800 DPR 1.5:
  буфер 1200 -> 800, верх кадра #6e86ff -> #42b9ff (третий стоп) и так до конца
  сессии, потому что на телефоне resize может не случиться вовсе.
  ЛЕЧЕНИЕ: `resize()` после `applyPerfTier('low')` в обеих точках вызова
  (tickPerfTier и `__game.setPerfTier`). ⚠️ ВНУТРЬ applyPerfTier ЗАСУНУТЬ
  НЕЛЬЗЯ: она объявлена в 10-stage и зовётся там же на старте (deviceLooksWeak)
  РАНЬШЕ инициализации `skyMat` — обращение упало бы в TDZ. Проверено тем же
  замером: сдвиг верха 44 -> 1.
  ⚠️ ПРАВИЛО НА БУДУЩЕЕ, вписано комментарием у `resize`: **кто меняет размер
  буфера — обязан позвать resize()**.
- ОТВЕРГНУТО РЕВЬЮ (не изобретать заново): гейт DEV на `?hour=` (последствия
  косметические), поддержка коротких хексов в `hexRGB` (владелец даёт #rrggbb),
  «небо кэшируется при загрузке, а тема живая» (расхождение через границу
  существовало и на базе слово-в-слово, только на 18:00 — не регрессия правки).

**НЕБО — ГРАДИЕНТ ВЕЗДЕ, ПАНОРАМЫ УДАЛЕНЫ ОКОНЧАТЕЛЬНО** (спека владельца
2026-07-30: «убери в десктопе на фоне картинку, сделай так же как в мобиле:
всегда градиент по времени суток»). ⛔ ОТМЕНЯЕТ гибрид 2026-07-22 ниже.
- СНЯТО: модуль `05-sky.js` (три base64-JPEG 1536×768, 84 499 Б), функция
  `skyPanorama()`, флаг `SKY_PANORAMA` (`pointer:coarse`) и ветка
  равнопромежуточной развёртки в шейдере неба. Сборка: 22 -> 21 модуля.
- ПОТРЕБИТЕЛЬ ПАНОРАМ БЫЛ РОВНО ОДИН — `skyPanorama()` в 10-stage. Всё
  остальное оказалось КОММЕНТАРИЯМИ: `isNightSky` в 85-hud (границы часов
  дублируются сознательно, теперь ссылается на `skyTimeNow`) и абзац в
  00-config. ⚠️ Абзац в 00-config:528 УЖЕ УТВЕРЖДАЛ, что «модуль 05-sky.js
  УДАЛЁН» — на main это было ЛОЖЬЮ (модуль вернулся вместе с гибридом, а
  комментарий править забыли). Теперь утверждение стало правдой.
- ⚠️ CSS-переменные НЕ ТРОНУТЫ: `--sky-grad` / `--sky-top-rgb` /
  `--sky-bot-rgb` ставились из `SKY_GRAD`, а не из картинки, — рецепт
  Safari-хрома и заливка карточки Play работают как работали (замер ниже).
- ВЕС (замер до/после от базы v1-test-187): index.html **9.074 -> 8.991 МБ**
  (−86 761 Б, 0.91%); ZIP пакета портала (index + 2 bridge + music.mp3)
  **6.371 -> 6.319 МБ** (−54 615 Б, 0.82%); запас до лимита 8 МБ вырос
  1.629 -> 1.681 МБ. ⚠️ **ЭТО МАЛО, И ЭТО НЕ НОВОСТЬ:** ровно тот же порядок
  (~1.2%) я мерил при первом удалении панорам 2026-07-22 (см. историю ниже).
  Панорамы уже были ужаты в JPEG по 22-32 КБ; настоящий вес несут rapier
  (2.24 МБ) и модули моделей. Ждать «заметного запаса перед заливкой» от
  этой правки НЕ СТОИЛО — запас дала бы работа с моделями или rapier.
- ПЕРФ (парно и вперемежку, 5 раундов, десктоп 1440×900, ур.20 после осадки):
  • ДЕТЕРМИНИРОВАННО — **текстур в GPU 12 -> 11**, draw calls 196 в обоих.
  • Кадр (медиана окна 5 с): 133.7 -> 131.2 мс, Δ 2.5 мс (1.9%) в пользу
    градиента, но **знаковый тест 3/5 — это ШУМ**, а не доказанный выигрыш.
    Направление совпало с чистым замером 2026-07-22 (−3.4 мс), но мой прогон
    его не подтверждает и не опровергает. ⚠️ Headless тут даёт 7-8 FPS, кадр
    съеден не небом — на такой базе 3 мс не отделить от дрожания.
  • Шаг физики 5.7 -> 5.2 мс — небо физика не сэмплит, разница в шуме
    (та же ошибка атрибуции, что я уже исправлял в v1-test-68; не повторять).
- ⚠️ perfStats ДЛЯ ЭТОГО НЕ ГОДИТСЯ: кольцо `frameRing` кумулятивно и не
  сбрасывается, поэтому p95 тянет спайк генерации уровня (660 мс при среднем
  157). Мерить установившийся кадр — своим счётчиком rAF после осадки.
- ВИД (замер пикселя неба + CSS): десктоп 1440×900 и мобайл 393×852 дают
  **побайтово одно небо** — день rgb(205,223,254), ночь rgb(42,55,102);
  `--sky-top-rgb` 206,228,254 / 40,54,103, `--sky-grad` на месте, html bg
  совпадает с зенитом, ошибок консоли нет. Структуру градиента НЕ менял —
  спека просила сменить ИСТОЧНИК, а не опоры.
- `tools/sky2module.js` и `3d assets/skyboxes` ОСТАВЛЕНЫ — картинка
  возвращается одной командой, если владелец передумает.
- ⚠️⚠️ **ПОЙМАН ЧУЖОЙ ФЛЕЙК — ЖИВОЙ КОНТРОЛЬ ПОЛА (зона ФИЗИКИ, запрос им).**
  На первом прогоне после ребейза сьют упал: «ПОЛ: после встрясок и взрыва под
  полом никого» (`brickbar`, pen −0.193, sleeping:false). ⛔ НЕ МОЯ РЕГРЕССИЯ,
  доказано парно: сьют — control 4/4 зелёных, моя ветка 3/3 зелёных после того
  красного (1 из 4); прицельная проба сценария (встряски + взрыв, снимок на
  3 с) по **18 попыток на сборку** — control 1/18, моя 1/18, частота
  ОДИНАКОВАЯ. На 7-й секунде находка ушла у обеих (0/18) — спасатель отрабатывает.
  ⚠️ МЕХАНИЗМ (это и есть польза от разбора): ассерт снимает `underFloor()` в
  ФИКСИРОВАННЫЙ момент 3000 мс после `detonate()`, а спасателю по его второму
  ключу нужна просадка, ДЕРЖАЩАЯСЯ ~1.5 с подряд. Транзиентная просадка на
  летящей куче (по замеру ФИЗИКИ норма 0.05..0.28, p95 0.13) успевает попасть
  в снимок раньше, чем спасатель имеет право её поднять. Лечение — не таймаут
  побольше, а ждать ПОКОЯ либо мерить тем же критерием, что у спасателя
  (pen < −0.12, ДЕРЖАЩИЙСЯ), а не одним кадром по часам.
  ⚠️ И МОЯ ОШИБКА ПО ДОРОГЕ, чтобы не повторяли: я прочитала знак
  `floorPenetration` наоборот («pen −0.193 = зазор, предмет не в полу») и на
  этом построила гипотезу «просто не осел». Знак Rapier: `contactDist` МИНУС =
  проникновение. Гипотезу опровергла проба (находка жила и на 7-й секунде),
  а не рассуждение.
- ⚠️ ХВОСТ ДЛЯ ДИСПЕТЧЕРА: `docs/WEEK-PLAN.md` §4.2 («нельзя форсировать
  время суток») ссылается на `skyForNow()` и называет `05-sky` зоной ГРАФИКИ.
  Функции больше нет — час теперь берётся из `skyTimeNow()` в 10-stage
  (зона та же). Сам план не правил, он не мой.

**(история) НЕБО — ГИБРИД: ПАНОРАМА НА ДЕСКТОПЕ, ГРАДИЕНТ НА МОБАЙЛЕ** (решение
владельца 2026-07-22: «панорама неба оставляем на десктопе, на мобиле кати
градиент»). 05-sky.js РЕГЕНЕРИРОВАН и вернулся в сборку.
- ⚠️ КРИТЕРИЙ — `pointer:coarse`, НЕ ширина ≥768px (диспетчер предлагал
  ширину как у HUD; выбор мой). Здесь вопрос «какой это КЛАСС УСТРОЙСТВА»
  (GPU, память, декод), а не «широкое ли окно». `pointer:coarse` уже
  используется В ЭТОМ ЖЕ файле ровно для такого решения — кап pixelRatio
  1.5 против 2. По ширине планшет в альбоме получил бы панораму, а десктоп
  в узком окне — градиент; оба неверно. Бонус: тип указателя от ресайза не
  меняется, поэтому вопрос «пересоздавать ли небо на границе» исчезает.
  У HUD ≥768 — вопрос ВЁРСТКИ, он честно про ширину: разные вопросы —
  разные критерии.
- Различается ТОЛЬКО база. Лихорадка/лесенка помола/затемнение верха идут
  ПОВЕРХ и о базе не знают — общий код на оба режима. Ветвление подтверждено
  счётчиком текстур: 9 на десктопе (панорама в GPU) против 8 на мобайле.
  ⚠️ КОНТРАСТ БЕЛОГО HUD ПОСЛЕ СНЯТИЯ ЗАТЕМНЕНИЯ (приказ владельца
  2026-07-22, v1-test-69 — статичного затемнения больше нет НИГДЕ):
  десктоп/панорама 1.60:1 днём, 13.19:1 ночью; мобайл/градиент 1.49:1 и
  12.62:1. Днём порог 3:1 НЕ выполняется — владелец предупреждён замером
  и принял осознанно, НЕ возвращать затемнение без его слова. Мелкая
  асимметрия: на градиенте днём чуть светлее кромка, чем на панораме
  (1.49 против 1.60) — обе ниже порога, разница практического значения не
  имеет. Событийные градиенты (uCombo/uGrind) не тронуты и работают.
- ⚠️ **ИСПРАВЛЕНИЕ ПРОШЛОГО ЗАМЕРА (моя ошибка атрибуции, попала в журнал
  v1-test-68).** Я писал «шаг физики p95 4.3-4.6 против 6.2-6.5 — сэмплить
  нечего». ЭТО НЕВЕРНО: физика небо не сэмплит вовсе, шаг от режима неба не
  зависит. Чистый замер (ОДИН вьюпорт, меняется только тач, 3 прогона,
  медиана): панорама кадр p95 55.3 / шаг 5.9; градиент кадр p95 51.9 /
  шаг 5.8. Настоящая цена сэмпла — **−3.4 мс КАДРА (~6%)**, шаг физики
  одинаков. Прежние 4.3-4.6 против 6.2-6.5 — разные состояния кучи, не небо.
- ⚠️ ВЕС В ОДНОФАЙЛОВОЙ СБОРКЕ: панорамы инлайнятся в index.html, поэтому
  МОБИЛЬНЫЙ игрок их всё равно СКАЧИВАЕТ (+83 КБ raw / +49 КБ gzip) и
  просто не показывает. Гибрид экономит мобайлу декод,текстуру в GPU и
  ~3.4 мс кадра, но НЕ трафик. Чтобы сэкономить и трафик, панорама должна
  стать ВНЕШНИМ файлом, грузимым только на десктопе, — а это ломает
  офлайн-запуск index.html двойным кликом (контракт из CLAUDE.md).
  Решение за владельцем, сам не менял.

**(история) НЕБО — ПРОЦЕДУРНЫЙ ГРАДИЕНТ ВМЕСТО JPEG-ПАНОРАМ** (спека владельца
2026-07-22: «для мобильного веба отказаться от фоновой картинки и решить фон
через градиент»). Модуль `05-sky.js` УДАЛЁН, `skyPanorama()` тоже.
- База — три опоры на время суток (`SKY_GRAD` в 00-config): зенит/горизонт/
  надир, между ними ЛИНЕЙНО по `d.y`. Цвета СНЯТЫ С САМИХ ПАНОРАМ, а не
  придуманы: замер профиля показал почти линейный ход (на e=0.707 расхождение
  0-4 из 255), поэтому линейная интерполяция, а НЕ smoothstep — тот задрал бы
  горизонт видимой полосой. Время суток — `skyTimeNow()` в 10-stage, границы
  прежние (утро 5-11, день 11-18, иначе ночь); `feverColorNow` теперь берёт
  час оттуда же, а не считает свой.
- Поверх-слои НЕ тронуты: лихорадка `uCombo`, лесенка угрозы `uGrind`,
  затемнение верха. Контраст белого HUD: утро 3.46 / день 3.47 / ночь 16.06 —
  против 3.65 / 3.69 / 16.44 на панорамах, порог 3:1 держится.
- ⚠️ **ЗВЁЗДЫ.** У ночной ПАНОРАМЫ они были, и чистый градиент их терял: тон
  совпадал, а небо становилось пустым. Сделаны процедурно (`uStars`, только
  ночью). ЧЕТЫРЕ подхода, три из них провальные — не повторять:
  (1) сетка по равнопромежуточным UV — у надира ШТРИХИ, сверху КВАДРАТЫ
      (ячейка крупнее пикселя рисовалась целиком);
  (2) 3D-расстояние до центра ячейки — звёзд почти нет: центры ячеек лежат
      вне тонкой сферы, луч в них не попадает;
  (3) отсечка звёзд ниже горизонта убрала ИМЕННО ВИДИМОЕ небо — камера
      смотрит СВЕРХУ ВНИЗ, и за чашей видна НИЖНЯЯ часть сферы;
  (4) РАБОЧИЙ: 3D-сетка по направлению, звезда = случайное НАПРАВЛЕНИЕ в
      ячейке, точка по УГЛУ между ним и взглядом. Равномерна на сфере,
      полюсов нет, точки круглые и не плывут при повороте камеры.
- ⚠️ **ТОН ХРОМА — ТЕПЕРЬ ОДНО ПРАВИЛО** (прежние три memo-ограничения про
  заглушку ≤4px, рисуемость `tex.image` и полосу 10..30% ОТМЕНЕНЫ вместе с
  картинкой): **верхний цвет градиента = источник тона хрома.** `tintChrome`
  (99-main, правка разрешена диспетчером в рамках этой сдачи) читает
  `skyChromeCSS` из 10-stage — ни сэмпла, ни ожидания декода, ни ретраев.
  Меняешь `SKY_GRAD[*].top` — меняется и подложка html/body, и theme-color.
  Совпадение с прежним: утро Δ2, ночь Δ4, день Δ12 по красному (старый сэмпл
  захватывал более синюю полосу картинки) — на глаз неразличимо.
- ВЕС. ⚠️ Ожидание «минус сотни КБ» НЕ подтвердилось: панорамы уже были
  сжаты в JPEG (22/32/28 КБ base64), файл 83 КБ, вклад в gzip 49 КБ.
  Замер сборки: **6.87 -> 6.79 МБ raw, 1.73 -> 1.68 МБ gzip** (~1.2%).
  Основной вес несёт rapier (2.24 МБ) — там и лежит настоящий резерв.
  Реальные выигрыши здесь другие: единый вид, точный тон хрома без сэмпла,
  нет декода картинки на старте (и мигания до него), минус генерённый модуль.
  Шаг физики p95 4.3-4.6 мс против 6.2-6.5 на панорамах — сэмплить нечего.
- `tools/sky2module.js` и исходники `3d assets/skyboxes` ОСТАВЛЕНЫ: если
  владелец захочет панораму на десктопе, модуль регенерится одной командой.

**Дальше:** пачки Brick и Pirate ВВЕДЕНЫ (см. выше). Осталось от прежних
заходов: вопрос про дырку пончика открыт (сейчас уведён в хвост); партии
Car/Food разобраны не полностью (8 из 50, 30 из 200) — можно добрать, если
владелец захочет больше типов. Камни `rocks-a`/`rocks-sand-c` из Pirate ждут
слова владельца (кандидаты в несовмещаемые объекты). Корабли Pirate лежат
неиспользованные: если владелец захочет их как крупные редкие силуэты —
нужен компаунд вместо convex hull (полые корпуса) и решение по масштабу.

**Верификация:** `python3 build.py` && `node test.js` -> «ERRORS: none» и
«SUITE: PASS»; ручная проба Hint; контактные листы по азимутам; замеры
спасений у стены (норма 0) и draw calls.

## БЛОК: ФИЗИКА

**Зона:** `50-physics.js` (мир/коллайдеры/степпер/спасатель),
физическая часть `99-main.js` (loop, сон physAwake, интро-осадка,
finalizeFill), `60-access.js` (лучи доступности — на стыке с ЯДРОМ).

**Состояние:** Rapier 0.19.3 завендорен; 181 тело; ступенчатый конус стен
(наклонные панели ЗАПРЕЩЕНЫ); сон: калм 0.25/0.4с + форс-сон ТОЛЬКО при
maxV<2 и не в интро (форс-сон по чистым часам ЗАМОРАЖИВАЛ падающий столб —
регрессия 2026-07, не возвращать); спасатель локальный, wallR у плоских
моделей, сон отменяется при спасении; интро: физика ×1.7, fallCap 11,
переход к облёту с 0.8с/maxV<3.5, облёт кончается ровно в 2π (иначе дёргался
кадр); трим по осевшей куче (pendingTrim). Доступность: веер лучей «к небу»
из сэмплов физформ через castRay (~17мс на 181 в Hard, бюджет 25).

**Инварианты:** MAX_FALL=16 (CCD слаб на мелких формах, issues #286/#302);
МЕТРИКА v3 (принята владельцем 2026-07-21, КОНТРАКТ ФИНАЛЬНЫЙ): матч =
ИСТИННЫЙ зазор физических поверхностей через GJK (pairMatch в 60-access —
ЕДИНСТВЕННАЯ точка сравнения; pairDist охватных сфер — только фильтр
грубой фазы); __game.psLog — сон при v>2.5 это баг; __game.floaters() —
баг только sleeping при contacts:0 («мост» с зазором под центром при
contacts>0 — норма кучи); maxWallExcess ≤ ~0.15.

**Сделано 2026-07-20 (v1-test-12):** СОАК ЗАКРЫТ — 6×15 мин автоботом
(Easy/Hard/Hard-простои × 2 раунда, живые интро, комбо/цепи/помол/встряски):
утечек нет (куча после GC ±1 МБ за 15 мин; тела/сцена/DOM возвращаются к
базе), 87 засыпаний — все при v≤1.9, спасатель 0–1/прогон, шаг физики
p95≤14 мс на 181 теле (бюджет 25), 20 уровней без ошибок консоли. Найден и
починен ЛАТЕНТНЫЙ баг floaters(): Rapier 0.12+ переименовал toi→timeOfImpact,
конечные зазоры были undefined — детектор видел только «луч не попал вовсе»;
заодно детектор ужесточён контактными парами (см. инвариант). Новое:
__game.perfStats() (p95 кадра/шага + счётчики утечек), __game.contacts(i),
soak.js (протокол соака), замер stepMs в 50-physics.

**Сделано 2026-07-20/21 (v1-test-13..15, ПРИНЯТО владельцем 2026-07-21):**
метрика совпадения доведена до v3 — истинный зазор поверхностей (GJK
contactCollider, фильтр грубой фазы по охватным, компаунды с ранним
выходом, дырка тора настоящая); телеграф — ореол-призрак ФОРМЫ
(reachGhostFX в 80-gameplay; геометрия КЛОНИРУЕТСЯ — stepFX диспозит);
константы на тот момент 0.9/0.75/2.0 в истинных зазорах (потолок серии/цепи
позже опущен владельцем до 1.1 — актуальный канон см. 00-config). Путь: v13 «касание
сферы» -> «слишком легко» -> v14 компенсация -> «нужно умнее для
продолговатых» -> v15 GJK. Экономика терпеливым ботом: Easy {2,3,4} /
Hard {2,2,4} встрясок при бюджете 5; availablePairs <=0.5 мс на 181.
Форс-тупик в тестах: cfg.baseRadius = -9 (малый положительный в v3 матчит
касающиеся).

**Сделано 2026-07-21 (в общей сборке — v1-test-56):** ВЕС ПРИ ВСТРЯСКЕ,
вариант 1 (спека владельца): SHAKE_RESP по пачкам {car 0.75, animal 1.0,
food 1.15} (00-config), item.shakeK в createItemBody; множитель только на
рыхление/подброс/вибрацию миксера, pullK и интро не тронуты. Замер
velByTex: отклики 0.72–0.79 / 1.12–1.13 при целях 0.75/1.15; A/B ботом —
экономика не ухудшена (v45 {5,6,6}/{5,4,4} vs v46 {5,4,5}/{4,3,5}).
⚠️ Сигнал владельцу: Easy на полном ур.5 у v45 БЕЗ веса уже {5,6,6} при
бюджете 5 — баланс поджался после нерфа комбо 1.1 (не моя правка).

**Сделано 2026-07-21 (в общей сборке — v1-test-56):** «ПУНКТ 5» владельца —
эффекты совмещения правилом: n<=3 труха, n>=BURST_MIN_N(4) бурст по пачке
(food сок / car искры+детальки / animal звёздочки; без пачки — труха) +
надувание ×1.22 перед хлопком + blastWave (50-physics): радиальное
вздрагивание соседей, квадратичное затухание, ×shakeK, пик 0.9 (встряска
9 — волна косметика; бот матчит пары, экономику не трогает). Эффекты —
СТАРТОВЫЕ в 80-gameplay через addFX (параметрическая баллистика,
FPS-независимо; материалы/геометрии персональные — stepFX диспозит);
запрос ГРАФИКЕ на полировку отправлен. __game.findByTex(tex) для
скрин-проб. Проба: 3 тапа по 14 шт — эффекты в кадре, волна в psLog,
fx дренаж в 0, SUITE PASS.

**Сделано 2026-07-22 (v1-test-58, ветка ФИЗИКИ):** ЧЁРНЫЙ ШАР-БОМБА
(спека владельца через диспетчера): makeBomb/спавн в середину столба
(40-items), detonateBomb (80-gameplay) — ближайшие по зазору <= 2.2, кап 7,
без очков; эффект = тёмная труха + пак-эффекты жертв + blastWave 2.2;
гварды mixerGrind/trimOverfill; __game.bombIndex()/detonate(); сьют
дополнен (старт 111-130, секция бомбы). ПЕРФ по п.9: пик взрыва = пик
обычного матча пары (148.6/4.8 мс на полной куче), частиц меньше чем у
пары, тела/коллайдеры вернулись ровно −8 — митигаций не нужно.

**Сделано 2026-07-22 (v1-test-63, ветка ФИЗИКИ, вливает диспетчер):
НЕСОВМЕЩАЕМЫЕ КАМНИ** — все 8 пунктов спеки владельца: модуль `37-rocks.js`
(точечный прогон glb2module по rocks-a/rocks-sand-c, 36-models НЕ тронут;
⚠️ пост-генерация обязательна: из точечного модуля удаляются дубль-const
MODEL_ATLASES/_atlasTex и дубль-функции хелперов — const валит IIFE,
функции затеняют канон 36-models). Рампа rocksForLevel (16-й: 1, +1/5 ур.,
кап 6; ROCK_* в 00-config), плотность 2.6, SHAKE_RESP.rock=0.7, key
'ROCK#i' уникальны, бомба — единственный способ досрочного съёма (легальная
жертва в капе 7), mixerGrind/trimOverfill гварды, финал доедает (13.7 с на
ур.16-одиночках), penalizeRock −2×MISS_PENALTY через scorePenalty, aliveCnt/
aliveN без камней (∞-порог и автопан не сдвинуты — в сьюте), вуали нет,
лучи блокируют. Сьют: секция В КОНЦЕ файла НАМЕРЕННО (setLevel/regen
ломали контекст «полного прогона» в середине — 23 встряски, ранняя камера);
∞-цикл с передышкой от вечного турбо; попутно починен предсуществующий
флейк сэмпла ∞-радиуса (ждать refresh-тик до 900 мс) и в старом ассерте.
ПЕРФ п.9: ур.41 (6 камней, 605 тел) vs ур.15 (599): шаг физики p95
6.0/7.0 мс против 6.4/7.5 — в шуме, бюджет 25 с запасом. Экономика
встрясок с 6 камнями — терпеливый бот на слитом пуле 78 типов: цифры
в отчёте диспетчеру (замер шёл на момент записи). __game.rocks()/rockIndex().

**Сделано 2026-07-22, поздний заход (v1-test-77, ветка ФИЗИКИ): findByTex
v2** по
флейк-репорту диспетчера (v76, «тап по камню −20 -> +120»): видимая точка
ищется рейкастом с камеры (центр + 8 смещений по экранным осям), отдаётся
пиксель где предмет — первое пересечение; целиком закрыт -> {occluded:true},
тест-секция камней встряхивает и повторяет (до 5 попыток). SUITE PASS x3.

**Сделано 2026-07-23 (v1-test-87, ветка ФИЗИКИ): ОСКОЛКИ + ЭФФЕКТНЫЙ ПОМОЛ**
(2 спеки владельца). (1) shardFX — brick/pirate/rock при бурсте колются на
угловатые куски (было труха); канон CLAUDE.md обновлён (отмена «оставляем
в труху»). (2) grindShred — двухфазный помол (захват+сплющивание ->
шинковка фонтаном осколков из-под лопастей), общий для mixerGrind/
finaleGrind. Ревью-фиксы: шинковка на реальных часах (десинк на <20 FPS),
финал спокойный (тряска 0.1 vs 0.28 наказания). Перф п.9: пик шага 4.6 мс,
draw +20, геометрии в базу бит-в-бит. Стартовая реализация через addFX —
полировка/перенос в 70-fx запрошены ГРАФИКЕ (см. Междузонные).

**Сделано 2026-07-30 (ветка `claude/physics-floor`): ПРОВАЛ СКВОЗЬ ПОЛ**
(жалоба владельца «дыра в объектах»). ⛔ ДВЕ ХОДОВЫЕ ГИПОТЕЗЫ ОТВЕРГНУТЫ
ЗАМЕРОМ, не повторять: (1) «виноват взрыв на 15/18» — НЕТ, худший случай
выпал на ЧИСТОЙ ОСАДКЕ, без единой встряски; (2) «солвер не успевает на
пиковом импульсе» — НЕТ, шаг физики на взрыве p95 7.7-10.9 при бюджете 25,
ровно как на встряске; 236-642 мс в кадре — это ЧАСТИЦЫ, не солвер.
**КОРЕНЬ:** солвер терпит глубокое проникновение ПЛОСКИХ форм под нагрузкой
кучи, а наш глобальный сон выключает интегратор — что утонуло к моменту сна,
остаётся утопленным до конца уровня. Жертвы: стейк 13 из 17, посох и
пряничный человек.
**ПОРОГ ИЗ РАСПРЕДЕЛЕНИЯ, оно БИМОДАЛЬНО** (60 снимков уснувшей кучи):
p50 0.024, p95 0.071, дальше 0.083 — пустой коридор — 0.224 (сам баг).
FLOOR_PEN_MAX=0.12 стоит посередине коридора.
**ГЕЙТ ПОКОЯ обязателен, но ОДНОГО ЕГО МАЛО — ДВЕ ДЫРЫ, обе нашлись только
прогоном, ни одна рассуждением:**
(1) ФОРС-СОН бьёт при maxV<2.0, куча ещё ползёт — утонувший проскакивал
    гейт за мгновение до заморозки. Лечение: `rescueSweep(true)` из
    sleepPhysics гейт СНИМАЕТ. Стоило 1 провала на 28 циклов уже ПОСЛЕ
    введения спасателя.
(2) ⚠️ ПРИ ПОМОЛЕ КУЧА НЕ ЗАСЫПАЕТ ВООБЩЕ (wakeAtMs двигают каждый кадр), а
    нижние слои постоянно вибрируют импульсами миксера — «спокойным» предмет
    не станет никогда. В соаке стейк просидел в плите 30 с ПОДРЯД (pen дошёл
    до 0.248) на глазах игрока. Лечение: второй ключ — просадка, ДЕРЖАЩАЯСЯ
    3 проверки подряд (~1.5 с), поднимается независимо от скорости; счётчик
    УБЫВАЕТ, а не обнуляется (под вибрацией просадка дрожит около порога, и
    обнуление сбрасывало бы часы вечно).
⚠️ ВЕРОЯТНО, ИМЕННО (2) И ВИДЕЛ ВЛАДЕЛЕЦ: у него «десятки помолов» на
уровень, а не один взрыв.
**Итог замеров:** максимум просадки на УСНУВШЕЙ куче (57 снимков, факт сна
проверен) 0.224 → 0.109, выше порога 0; соак 2×12 мин Easy/Hard — залипаний
и просадок на спящем теле НЕТ, шторма нет (подъёмов 4-11 за 12 мин при
пороге тревоги 12).
⚠️⚠️ **МЕРИТЬ ДЛИТЕЛЬНОСТЬ, А НЕ ФАКТ — иначе соак врёт в обе стороны.**
Одиночный сэмпл «кто-то под полом» НЕ дефект: спасатель ПО ЗАМЫСЛУ ждёт до
1.5 с у движущегося (гейт покоя), а сэмпл идёт раз в 5 с. Замер серий с
опросом 300 мс (8 сидов, ур.10 Hard, с простоем под помол): эпизодов 4,
**максимум РОВНО 1500 мс**, незакрытых ноль — то есть срабатывает именно
проектный потолок. Поэтому инвариант соака переписан на ЗАЛИПАНИЕ: тот же
предмет в двух подряд сэмплах (>=5 с) ИЛИ под полом на СПЯЩЕМ теле.
Прежняя формулировка «в сэмпле его быть не должно вовсе» противоречила
собственному замыслу и давала ложные тревоги.
⚠️ ЧЕСТНО ПРО ОСТАТОК: в одном прогоне calm-pen мелькнул выживший 0.197, но
тот прогон НЕ проверял факт сна (ожидание с таймаутом глоталось) — скорее
артефакт пробы; проба починена, повторы дали 0. Процесс стохастический,
«никогда» не утверждаю.
⚠️⚠️ **ПЕРЕЗАМЕРЕНО НА v1-test-187 (ГРАФИКА УДАЛИЛА СТЕЙК И ЛЕДЕНЕЦ — это были
жертвы №1 и №2, 13 случаев из 17).** Класс дефекта уцелел, сменились только
имена: первый в очереди теперь `brickbar`, за ним пряничный человек.
Распределение на TYPES=120, 66 снимков уснувшей кучи: p95 0.086, максимум
0.088, выше 0.10 — НОЛЬ, то есть порог 0.12 остаётся верен с запасом 1.36×.
Спасатель не осиротел: 5 подъёмов на 8 сидов, единственный эпизод под полом
жил 600 мс. Сьют 285 PASS — детерминированный страж пола на стейке НЕ стоял
(сцена ставится на первом доступном предмете), поэтому удаление его не задело.
⚠️ ПРАВИЛО НА БУДУЩЕЕ: **порог выведен из ФОРМЫ предметов — при смене партии
моделей перемерять** (`calm-pen`, распределение на уснувшей куче).
⛔ **УТОЛЩЕНИЕ ПЛИТЫ ПРЕДЛОЖЕНО И ОТКЛОНЕНО ВЛАДЕЛЬЦЕМ 2026-07-30** («откати
толщину плиты, оставь только спасателя»). Плита осталась 0.3 бит-в-бит.
⚠️ ЗАМЕР СОХРАНЁН, чтобы не выводили заново: максимум просадки 0.28 при
полутолщине 0.30 — 7% до переворота нормали и выброса в пустоту под лопасти.
Но на фикс это не влияло и не влияет: тонкая плита со спасателем даёт
max 0.109 при 0 снимках выше порога — то же, что толстая (0.116). Перф был
в шуме в обе стороны. Комментарий об этом стоит прямо у строк плиты
в 50-physics.
**Инструменты:** `floorPenetration` (истинный contactDist с плитой),
`__game.underFloor()`, `pen`/`low` в `itemsBrief()`, `__game.rescueNow()`
(НЕСУЩИЙ хук: place() физику не будит, на спящей куче sweep не зовёт никто —
без него страж пола был бы гонкой). Сьют: 4 ассерта детерминированного
стража (подкладка под пол → подъём) + 1 живой после взрыва. 279 PASS.
Соак: добавлен сценарий «один взрыв на уровень, на ещё полной куче»,
инвариант UNDER FLOOR и детектор шторма подъёмов.

**Дальше:** замер на реальном среднем Android (Bridge/WebView) перед
сторами — perfStats готов, нужен телефон владельца; тюнинг SHAKE_RESP и
констант бурста (BURST_*) по плейтесту владельца (__game.velByTex в
помощь); анимация помола/осколки — СДЕЛАНЫ (v1-test-87), тюнинг вида
(число осколков/тайминги/тряска финала) по плейтесту владельца.
⚠️ ДОЛГ №1 (не тронут этой правкой): панели стен повёрнуты на 90°, кольца
дырявые — правка обязана идти пакетом с перезамером PAIRS/topY/wallExcess.

**Верификация:** node test.js + soak.js на 3+ сидах (psLog/floaters/
wallExcess/perfStats).

---

## БЛОК: ПОВЕСТВОВАНИЕ И МЕТА

**Зона:** дизайн по `docs/DESIGN-ROADMAP.md` (план с критикой и защитой —
ЧИТАТЬ ПЕРВЫМ); экономика значений в `00-config.js` (COIN_*, STAR_*,
COMBO_*, CHAIN_*); `77-save.js`; персонаж (логика eyesMood); будущие музей/
коллекции/daily (v1.1).

**Состояние:** v1 реализована. ⚠️ МОНЕТЫ СКРЫТЫ флагом COINS_ENABLED=false
(спека владельца 2026-07-21: награда уровня — только звёзды + подсказка);
счётчики earned/spent и начисление живут, спрятаны чип/×2/покупная встряска.
Монеты (earned/spent, мерж без дюпа),
магазин (встряска 25 после rewarded-капа; прицел скрыт флагом), звёзды по
пар-скору (×1.3/×1.7), укороченные уровни 1-3 (64/71/78 пар), персонаж-глаза
с эмоциями. Комбо: лесенка радиуса 5 ступеней до зазора 1.1 (3-й нерф,
потолок ВЕЗДЕ вкл. Power chain — спека 2026-07-21), очки ×2,
окно серии 4с, промах −2 шага (серию не гасит); Power chain: 10 шагов,
10с/2 промаха, досыпка 2шт/0.5с, полоска заряда + зелёный фон-подогрев.
Подсказки — числимый ресурс (3 старта, +1/уровень, he/hs в 77-save).
Фиксы ревью 2026-07-21: сейв получил поле-эпоху `gen` (reset инкрементирует;
mergeSave не воскрешает монеты из отставшей облачной копии); лесенка/цепь
двигают радиус только ВВЕРХ; эндшпильный ∞ приоритетнее цепи.

**Инварианты:** сюрприз всегда «честно» перекрыт (иначе +150 с первой
секунды); финал миксера без очков (кроме сюрприза); Easy = любая пара,
Hard = перекрытия; рампа сложности = число типов.

**СЮЖЕТ (перепрофилирование чата владельцем 2026-07-22):** этот чат ведёт
трек [сюжет] — «блендер хочет уничтожить все объекты на земле, постепенно,
в виде комиксов; просто, казуально, не сбивая темп». Написана
**docs/STORY-SPEC.md**: концепция «Великий Рецепт» (смузи из всего сущего,
паки = главы плана) + драматическая ирония «игрок — его лучший помощник»
(он думает, матчи = уничтожение; музей — тайна игрока). Все существующие
эмоции глаз перечитаны лором БЕЗ правок кода (§2 спеки). Формат:
бессловесные SVG-виньетки 1–3 панели между уровнями, скип тапом, ≤4 с,
бюджет ≤30 КБ; арка К0–К5 по вехам (первая победа → экспонаты → полный
сет-твист); битмаска глав st в сейве (OR). Открытые вопросы владельцу —
§9 (мотивация, твист, стиль, бомба-«подарок», место показа).

**Дальше:** ответы владельца на §9 STORY-SPEC → раскадровка К0–К1 и
SVG-прототип; музей и комикс реализуются вместе ПОСЛЕ метрик v1 (делят
вехи). MUSEUM-SPEC получила аддендум 2026-07-22 под новую реальность
(63 модели → сеты из паков Зверинец/Урожай/Гараж, первый экспонат —
золотая рыбка, награды без монет пока флаг выключен) — запрос диспетчера
закрыт. Затем daily от сида-даты + стрик (после метрик).

**Сделано 2026-07-22 (ветка stoic-rubin, НАКОПЛЕНИЕ + БАЛАНС, спека
владельца через диспетчера):** (1) накопление по типам: Save.ac[имя]
(мерж max + gen; сироты партий сохраняются с warn), пороги 100·(2^n−1)
(=ряд ×2+100: 100/300/700/1500/3100/6300…), множитель очков типа
1+0.25×ступень (кап 9), инкремент в doMatch на N до подсчёта очков,
пар-скор по типам × множители (связка (а)); бомба НЕ копит. (2) баланс:
промах −10; ур.1 без очковых штрафов; ур.1-5 кламп нулём (scorePenalty —
единая точка); рыбка +150+5×ур; звёзды откалиброваны ботом — отчёт ниже.
**⚠️ КАЛИБРОВКА ПЕРЕДЕЛАНА 2026-07-22 (вторая сессия): СТАРЫЙ МЕТОД БЫЛ
НЕВЕРЕН.** Все прежние прогоны (и отчёт ниже) мерили ботом `autoMatch`,
который снимает ТОЛЬКО ПАРЫ. Живой игрок тапом забирает ВСЮ группу в
радиусе, а цена группы = 10·N·(N−1), то есть 10·(N−1) на предмет; сверх
того группа от 3 предметов САМА зажигает комбо ×2 (`list.length >= 3` в
doMatch). Замер реальными кликами (`__game.bestTapTarget`, движок
подсказки): средняя группа 2.8–3.3, 34.3 очка на предмет против 10 —
**занижение втрое-вчетверо**. Полный уровень: pair-only бот 1.78, живые
тапы 4.2–6.7. Пороги, снятые старым методом, годились бы только для
игрока, который систематически бьёт мимо групп.
**ДРЕЙФ ПО УРОВНЯМ (главная находка):** при ОДНОМ И ТОМ ЖЕ стиле игры
ratio падает 4.95 (ур.1) → 4.24 (ур.5) → 2.84 (ур.10); средняя группа
3.28 → 2.81 → 2.53 → 2.19 (ур.19). Причина: типов больше — копий каждого
меньше — группы мельче, а пар-скор считает по парам и этого не знает.
Следствие: единые пороги делают 3★ подарком на ранних уровнях и
испытанием на поздних. Порог 2★ поэтому кладётся НИЖЕ поздней нормы.
⚠️ Ур.19 не уложился в 5 минут (144 тапа, 95 промахов) — сигнал о длине
поздних уровней против цели роадмапа «3–5 мин».
**ВЕРДИКТ ПО ПОРОГАМ (2026-07-22, вторая сессия): STAR2_K=1.5 / STAR3_K=2.1
ОСТАВЛЕНЫ БЕЗ ИЗМЕНЕНИЙ — но по другому основанию, чем ставились.** Полные
данные (реальные тапы, медианы; n — уровней):
| стиль | ratio |
|---|---|
| pair-only комбо (n=3) | 1.77 |
| pair-only цепи (n=5) | 2.15 |
| тапы, обычный игрок, ур.1-5 (n=5) | 3.96 (5.2 -> 3.6) |
| тапы, внимательный, ур.1-6 (n=6) | 5.71 (6.7 -> 4.4) |
| дрейф ур.1/5/10/15/20 | 4.95 / 4.24 / 2.84 / 1.29 / 1.17-1.23 |
ПРИЧИНА ДРЕЙФА: с ростом числа типов копий каждого меньше, средняя группа
падает 3.28 -> 2.19, и игра ВЫРОЖДАЕТСЯ В МАТЧ ПАРАМИ — ratio сползает к
pair-only значениям. То есть шкала меряет не мастерство, а НОМЕР УРОВНЯ.
Ни одна пара фиксированных порогов не может означать «нормальная игра =
2★» и на ур.1, и на ур.20 одновременно. 1.5/2.1 — единственный вариант,
который не ломает поздние уровни: там нормальная игра даёт 1.2-2.0 (2★
аккуратному, 1★ неряшливому, 3★ сильному), а ранние остаются щедрыми,
чего роадмап и хочет ради D1. Поднимать пороги под ранние уровни (2.5/4.0
дали бы там честную шкалу) НЕЛЬЗЯ без нормализации базы — ур.15+ ушли бы
в глухую 1★.
⚠️ Промахи на поздних уровнях (82-116 за уровень) — НЕ артефакт темпа:
контроль медленным темпом дал те же 86. Клик по центру предмета в плотной
куче попадает в перекрывающего соседа. Часть — слепота бота, часть —
реальная трудность прицеливания, которую стоит иметь в виду ФИЗИКЕ/ИНТЕРФЕЙСУ.
**ВОПРОС ВЛАДЕЛЬЦУ (решение уровня правил, не констант):** нормализовать
пар-скор под ОЖИДАЕМЫЙ размер группы (сейчас база = «всё парами без
комбо» — состояние, которого в реальной игре не бывает: группа от 3 сама
даёт комбо ×2). Тогда дрейф исчезает, пороги возвращаются к 1.15/1.35 и
звёзды впервые означают качество игры, а не номер уровня. Правка — в
finalizeFill (99-main), объём мал: E[N] оценивается по фактической куче
(среднее число одноимённых соседей в радиусе) и умножает базу.
✅ **КАМНИ И ПАР-СКОР — ЗАКРЫТО 2026-07-22** (предупреждение сработало:
камни приехали в main с v1-test-64). Диагноз подтверждён замером: камни
несовмещаемы (key 'ROCK#i' уникален), но `type.name` у них общий
('rocksa'/'rockssandc'), поэтому пара камней одного вида добавляла в базу
20 очков, недостижимых в принципе. Замер: ур.41 (6 камней) база 1840
против правильных 1800; ур.16 (1 камень) разницы нет (пол-пары
отбрасывается). Починка — `!it.rock` в фильтре finalizeFill (99-main),
ветка `claude/meta-par-rocks`. Проба: parBase == независимый пересчёт по
aliveByType без камней на ур.16 и 41. SUITE: PASS.
**Исторический отчёт первой сессии (метод устарел, числа оставлены для
хронологии):**
профиль «пары без комбо» (паузы 1.8 с) — ratio 1.11–1.12 (ровно 1.0 +
рыбка 155/пар); «комбо без серий» (пачки по 3 быстрых, паузы 4.3 с гасят
серию, 5 уровней) — 1.72–1.78, цепей 0; «серийный» — см. сигнал ниже.
ВЕРДИКТ: STAR2_K=1.5 / STAR3_K=2.1 ПОДТВЕРЖДЕНЫ: без комбо только 1★
(1.12 < 1.5), комбо без серий даёт 2★ (1.72 ≥ 1.5 < 2.1), 3★ — только
с сериями; у живых игроков будут промахи (бот не мажет) — запас в
пользу порогов. Финальные числа = стартовые, правка кода не нужна.
⚠️ **СИГНАЛ ВЛАДЕЛЬЦУ — БЕСКОНЕЧНЫЙ ФАРМ:** серийный бот (матч каждые
~150 мс, передышка 4.3 с раз в 12 матчей) НЕ завершает уровень 1 вовсе:
вечная серия турбо перезаряжается быстрее, чем гаснет, а досыпка цепи
(6.2 предм/с в окне) превышает средний съём по циклу (~3.9/с) — чаша
не пустеет, счёт вырос до 274 000 за ~3 мин и растёт дальше. Кап досыпки
ограничивает ЗАПОЛНЕНИЕ, но не ОЧКИ. С накоплением/звёздами это дыра
баланса. Варианты (решение владельца): потолок числа досыпок за одну
цепь / затухание очков в затянувшейся серии / кап очков уровня.
**КОНТРАКТ ДЛЯ ИНТЕРФЕЙСА (вкладка «Музей объектов» + всплывашка апа),
API зафиксирован (v2 после ack ИНТЕРФЕЙСА):** `accCount(key)`,
`accTier(key)`, `accMult(key)`, `accNext(key)` (аргумент — КЛЮЧ ассета
TYPES[].name; next — порог следующей ступени или null на капе 9);
ГЛОБАЛЬНАЯ `accSnapshot()` (и `__game.accSnapshot()`) →
[{name: ЧЕЛОВЕЧЕСКИЙ ярлык (accLabel: срез префикса пачки + карта
исключений ACC_LABELS), key: ключ ассета, count, tier, mult, next,
_item: живой предмет типа для портрета или null}] по текущим TYPES;
подписка `onAccTierUp(cb)` (глобальная И `__game.onAccTierUp`) — колбэк
{name: ярлык, key, tier, mult, item} строго в момент пересечения порога
в doMatch; item ЖИВОЙ: mesh валиден, тело Rapier уже уничтожено,
растворение стартовало — портрет снимать сразу в колбэке; ошибки
колбэков глотаются (матч не падает).
Тест-хелперы: `__game.accGrant(name,n)`, `setLevel(n)`, `matchType(name)`,
`aliveByType()`.

**Сделано 2026-07-23 (ветка claude/meta-stars-currency): ЗВЁЗДЫ = ВАЛЮТА +
BOOST** (решения владельца). Схема: рейтинг `stars[lv]` (max, не тратится)
и кошелёк `se`/`ss` (earned/spent, баланс = разность) РАЗВЕДЕНЫ — иначе
трата отнимала бы заработанные 3★. Дюп закрыт парой монотонных счётчиков:
трата растит `ss`, отставшая облачная копия её не откатывает (ассерт в
сьюте). Начисление ДЕЛЬТОЙ рейтинга (100/250/500 + 10×уровень) — повтор
уровня без улучшения даёт 0, ферма ур.1 невозможна. Boost: `bo[тип]`
(купленные ступени поверх заработанных, общий кап), цена 1500×2^ступень =
1500/3000/6000/12000/24000. Миграция старых сейвов по флагу `sm` —
идемпотентная, рейтинг конвертируется в стартовый баланс. API для меню —
в CLAUDE.md, раздел 2026-07-23. Сьют: +14 ассертов, все PASS.

⚠️ **MAIN КРАСНЫЙ НЕ ПО МОЕЙ ВИНЕ (передать ГРАФИКЕ):** ассерт «осколки:
геометрии дренажат в базу без утечки» падает НА ЧИСТОМ d8584da теми же
числами (base 52 → peak 64 → after 54): после залпа остаётся +2 геометрии.
Проверено запуском сьюта в основном клоне без моих правок. Зона 70-fx
(shardFX) — сам не трогал по правилу 3.

**Верификация:** бот-прогоны экономики (встряски ≤ бюджета 5), test.js.

---

## БЛОК: ИНТЕРФЕЙС

**ОТЛОЖЕНО (просьба владельца 2026-07-28, чтобы не потерялось):** вернуть
кнопку «Open» на карточках коллекции и давать ОТКРЫВАТЬ ПРЕДМЕТЫ ЗА ЗВЁЗДЫ.
Сейчас кнопка СКРЫТА («не имеет смысла для игроков»), но механика жива и не
тронута: `purchaseUnlock`/`typeUnlockPrice`/`canUnlockType` в 77-save,
ветка `act:'open'` в обработчике `#msGrid` (90-input). Возврат = вернуть
три строки создания кнопки в `buildMainCollection` (85-hud, помечено
комментарием) — цена и гейт уже считаются.

**Зона:** `src/shell.html` (разметка/CSS/оверлеи), `85-hud.js`,
`90-input.js` (жесты/кнопки), локализация текстов.

**БЛОК НАСТРОЕК ТЯНЕТСЯ (2026-07-29, макет 812:1115, спека владельца
«элементы из этого блока умеют тянуться»):** `.ms-settings` — СЕТКА
(`max-content` под ярлыки + `minmax(0,1fr)` под контролы), строки `.ms-set`
растворены в `display:contents` (тот же приём, что у `.ms-collhead`).
⚠️ ПОЧЕМУ НЕ ФЛЕКС С `flex:1`: в макете все три контрола ровно 500 при
ярлыках 68/63/84 — то есть они ОДНОЙ ширины и выровнены по одной вертикали;
у флекса ширина зависела бы от длины ярлыка и левые края разъехались бы.
Пара костылей `--ms-ctl` 172/156 СНЯТА: 172 было размером РАСТРОВОГО узла
763:1428 (вектора в нём нет), 156 — заплатка под узкую колонку бенто.
Трек 17 / бегунок 30 / свитчер 48 — числа нового узла; прежние 12/26 были
глазомерной прикидкой по картинке. Отступы 12/20 + шаг строк дают высоту
панели 184 — как в макете.
⚠️ **`getComputedStyle` НА WEBKIT-ПСЕВДОЭЛЕМЕНТАХ ВРЁТ.**
`getComputedStyle(slider, '::-webkit-slider-runnable-track').height` и то же
для `::-webkit-slider-thumb` отдают высоту ХОСТА (у нас 30 обоим) — проверка
«трек стал 17» так МОЛЧА зеленеет на любом значении. Мерить ТОЛЬКО
пикселями: скрин элемента → canvas → вертикальный скан столбца по цвету
(зелёная заливка трека считается сама; бегунку на время замера дают
контрастный фон инжектом `background`, размеры при этом остаются свои).

**ГЛАВНЫЙ ЭКРАН / ПАУЗА (2026-07-23, ветка claude/interface-vitrine, коммит
519a6af — ЧЕКПОИНТ, ждёт ОК владельца):** мобильный макет 770:1271 —
`#mainScreen` оверлей, один экран на две роли (Play Game / Resume). Хедер-
пилюля (аватар-плейсхолдер + ★totalStars + Get More), лайм-баннер No more AD
+ Subscribe $1.99, лиловая Play-панель (глаза eyes-0 + кнопка), настройки
Sound/Music/Difficult (единые точки applyHard/applySound синхронят чекбоксы
паузы), сетка My collection (accSnapshot: портреты itemThumb, бейджи ×N из
ступеней, прогресс count/next, Boost; локнутые — Level N + Open по
прогрессии). Числа сверены с Dev Mode (см. коммит). Показ — window.
showMainScreen() (debug), боевой поток паузы НЕ тронут (test.js зелёный).
ЭКОНОМИЧЕСКИЕ РАЗВИЛКИ вынесены в «Междузонные запросы» (МЕТА: звёзды-валюта
+ Boost; ИНТЕГРАЦИЯ: Subscribe) — на плейсхолдерах до решения владельца.
Дальше: десктоп 763:1031 (та же система, 4 карточки/ряд, 2 колонки) +
интеграция экрана в боевой поток паузы/старта.

**Состояние (по МОБИЛЬНОМУ макету Figma 741:1738; смержено в main
v1-test-36):** сверху
слева ⏸ 56, справа вертикальный стек 22px (предметов / зелёное время /
★ очки градиентом #ff70b5→#f2ff00), по центру В ТОЙ ЖЕ строке конструкция
персонажа (`#face`, top +8): инлайновый SVG глаз 210×105 + чёрное число
отсчёта 54.8px внахлёст (краснеет ≤3 с). Снизу СЛЕВА контурная подсказка,
снизу справа «Shake ×N» пилюлей #1d1c26. Поля баров 8. Номера уровня на
игровом экране нет. ⚠️ Счёт ≥10000 сжимается в «12.5k», иначе стек рвётся
на две строки и наезжает на глаза (колонка макета — под 3 знака).
МОНЕТ на игровом экране нет, ⚙️ убрана — панель открывается из ПАУЗЫ
(`#pauseOverlay`). Полоска турбо удалена: накопление показывают зрачки.
Попы очков белые с чёрной обводкой 2px (inline-цвет перебит !important).
Кнопки бара ОБЯЗАНЫ иметь pointer-events:auto (.bar глушит). Магнит и
прицел скрыты флагами (код жив). Шрифт — `ui-rounded` с откатом.
⚠️ Ждёт ГРАФИКУ: без голубого фона #d0dff3 белые глаза и белая кнопка
паузы невидимы на белом поле (в main фон — небо-панорама, обычно светло-
голубая: проверить читаемость на всех временах суток).
От диспетчера при мерже (сохранено из main): АВТОПАУЗА при сворачивании
вкладки (visibilitychange hidden -> pauseGame); Space-встряска гейтится
оверлеями рекламы; пауза — НАСТОЯЩАЯ заморозка pauseGame/resumeGame
(99-main, сдвиг якорей + afterPause-очередь), кнопки макета Continue/
Заново/Настройки повешены на неё (выходы в genLevel/⚙️ резюмят);
в #eyes НЕЛЬЗЯ писать textContent (SVG-слои); Reset progress оставлен
в ⚙️-панели.

2026-07-20 (v1-test-12): **починена ⚙️-панель** — в v1-ветке потерялась
базовая строка `#debugPanel { display:none; position:fixed; … }`, из-за чего
панель рендерилась статикой 390×168 за фиксированным канвасом и НЕ
открывалась вовсе (клик по ⚙️ переключал display block↔none впустую).
Добавлены `docs/UI-SCREENS-PLAN.md` (карта экранов: 7 есть, 12 не хватает,
приоритеты P0/P1/P2) и `src/ui-proto.html` — кликабельный макет 15 кадров
390×844, режимы «путь»/«сетка», для развёртки в Figma. Макет автономный,
в сборку не входит (build.py читает только shell.html + app/*.js).

**Инварианты:** попапы подтверждений встряски ЗАПРЕЩЕНЫ (владелец);
вуаль недоступных — только в Hard; «интерфейс настроим позже» — крупный
редизайн только по команде владельца.

2026-07-21 (v1-test-13): **персонаж — одна конструкция `#face`** (глаза +
отсчёт до перемолки + полоска турбо) СВОЕЙ СТРОКОЙ под чипами; время партии
убрано; 7 эмоций по матрице владельца вместо разрозненных эмодзи. Это
вылечило и столкновение верхнего бара: чипы больше не перекрываются на
320/360/390. Логика отдаёт ИМЯ состояния, отрисовка в `setFace` — ассеты
SVG (со слоем зрачков) вставятся без переделки. Спека и матрица:
docs/EYES-CHARACTER-SPEC.md.

**Дальше:** ⚠️ ждут владельца — ассеты 7 эмоций (спека §3: SVG, одна канва,
`pupil-l`/`pupil-r`, ≤8 КБ файл, экспорт НЕ из Dev Mode) и вход в игру —
сразу в уровень или через хаб (UI-SCREENS-PLAN §4). Открыт междузонный
запрос к ЯДРУ на хук сюрприза. Затем порция A (P0): загрузка → пауза →
настройки отдельно от отладки → обучение 3 шага. Дальше порция B (v1.1):
хаб, витрина, магазин, музей, daily. Полная EN-локализация — ОДНИМ
проходом после утверждения состава экранов (иначе переводим дважды).

**Верификация:** скриншоты layout с замером getBoundingClientRect
(зазоры/высоты), headless-клики по кнопкам. Макет: проба на переполнение
всех 15 кадров (overflowX/Y = 0) + скриншот сетки.

---

## БЛОК: ИНТЕГРАЦИЯ И ПУБЛИКАЦИЯ

**Зона:** `78-ads.js` (Bridge/заглушка/interstitial), `79-telemetry.js`,
`build.py`, `release/` (пакеты), `playgama-bridge*`, деплой/хостинг,
чек-листы площадок.

**⚡ ИНТЕГРАЦИЯ BRIDGE v2 — ОБЯЗАТЕЛЬНЫЕ ШАГИ ДОКИ (2026-07-29).** Полный
аудит документации (10 модулей) + разбор бандла + живые замеры. Закрыто:
- **ПАУЗА ПЛОЩАДКИ** `PAUSE_STATE_CHANGED` — не были подписаны вовсе.
  `visibilitychange` её НЕ заменяет: в iframe портала оверлей площадки не
  делает `document.hidden`, и под ним у нас ТИКАЛ МИКСЕР И ЖРАЛ ПРЕДМЕТЫ.
  ⚠️ ТРЕТИЙ владелец паузы — свой флаг `pausedByPlatform` + СВОЙ мьют
  `mutedByPause`. Сперва переиспользовал общий `mutedByPlatform` — снятие
  паузы не возвращало звук (поймал свой же ассерт): один флаг = один владелец.
- **МУЗЫКА В МЬЮТЕ.** `Sound.setMuted` — master-гейн WebAudio, т.е. только
  SFX; музыка это `<audio id="bgm">` (85-hud). Мьют писался в v85, когда
  музыки не было; её завели в v106 и в мьют не внесли — трек играл ПОВЕРХ
  рекламы (нарушение требования площадок). Добавлен `musicSuspend(on)` в
  85-hud (⚠️ ЗАХОД В ЧУЖУЮ ЗОНУ по авторизации диспетчера; свой флаг
  `musicExtMuted`, `musicVol` игрока НЕ трогается, и внешняя приглушка
  СИЛЬНЕЕ ползунка — иначе игрок заводил бы трек прямо под роликом).
  ⚠️ ПОБОЧНО НАЙДЕН БЛИП: `showRewarded` звал полный `cancel()` (снимал
  мьют) и через строку ставил его обратно — музыка успевала рыпнуться в
  момент старта ролика. Теперь сирота гасится `clearTimers()` без A/V,
  единая точка снятия (`endPending`) цела.
- **GAME_READY ПО ПЕРВОМУ ИГРАБЕЛЬНОМУ КАДРУ**, а не в `init` (дока: «when
  the first playable frame is ready»). Раньше уходил до genLevel и ~2 с
  интро — площадка снимала свой лоадер над чёрным экраном. `Ads.gameReady()`
  идемпотентен, латч досылает, если игра дозрела раньше SDK.
- **LEVEL_STARTED/COMPLETED/PAUSED/RESUMED** (`Ads.msg`) — не слали ничего,
  кроме GAME_READY. У POKI и CRAZY_GAMES адаптеры маппят их в НАТИВНЫЕ
  `gameplayStart()/gameplayStop()`: без них площадка пейсит рекламу вслепую
  (прямая потеря показов). LEVEL_FAILED не шлём — поражения в игре нет.
- **PLACEMENT** в оба показа (`level_completed` / `shake|continue|x2|magnet`)
  — без него статистика по местам слепая; адаптеры прокидывают имя в
  нативные SDK.
- **ГВАРД `isInterstitialSupported`** СТРОГО ДО сброса `Save.iw`: `mode`
  ставится по rewarded, а межстраничная поддержана не везде — окно из 5
  побед скручивалось там, где ролика не будет никогда.
- **КОНФИГ** заполнен (`advertisement.interstitial/rewarded` + placements):
  был буквально `{"platforms":{}}`, а SDK читает из него и троттл, и
  `disable`, и каталоги payments/leaderboards.
- **ЯЗЫК** `platform.language` читается в `Ads.lang` (словаря пока нет —
  интерфейс EN по спеке; техчасть готова).
⚠️ ФАКТЫ ЗАМЕРОВ: наш SDK **v2.0.0 — АКТУАЛЬНЫЙ** релиз (10.07.2026),
обновляться не нужно. Официальный Claude-плагин Playgama написан под **v1**
(CDN v1, `player.authorize`, storage со storage-type) — дословно применять
к v2 НЕЛЬЗЯ, каждый API сверен с бандлом. У SDK **СВОЙ троттл межстраничной
60 с** (`minimumDelayBetweenInterstitial`, ставится и из конфига) поверх
нашего счётчика 5 побед. `getServerTime()` РАБОТАЕТ — честный суточный кап
рекламы возможен. ⚠️ КОЛЛИЗИЯ КЛЮЧА (замер): `bridge.storage.set('mixer_save_v1')`
на площадке без облака пишет в localStorage ТЕМ ЖЕ ключом, что и наш
`commitSave` — сегодня не рушит (читаем обе формы), но это мина под будущий
переезд на облачный сейв, ключ развести.
**🎯 СТЕНД БОЕВОЙ РЕКЛАМЫ — БЕЗ ЗАЛИВКИ НА ПОРТАЛ (2026-07-29).** Площадка
задаётся URL-ПАРАМЕТРОМ `?platform_id=<id>` — SDK читает его сам
(`searchParams.has("platform_id")` в ядре бандла), файлы подменять НЕ надо:
```
https://ikorzun.github.io/Blender/?dev=1&platform_id=poki
```
⚠️ `?dev=1` обязателен — `window.__game`/`__ads` в бою сняты.
⚠️ **ЭТО СНИМАЕТ ГЛАВНЫЙ ДАВНИЙ БЛОКЕР «ветка bridge не исполнялась ни разу».**
Замер 2026-07-29 на ЖИВОМ Pages под НАСТОЯЩИМ адаптером Poki:
`platform.id=poki`, `mode=bridge` (боевая ветка!), и полный цикл rewarded —
`loading` (1 мс) → `opened` (366 мс, игра встала на паузу и заглохла) →
реальный рекламный перерыв ~11 с → `rewarded` → `closed` (11.5 с, игра
разморожена, звук вернулся, награда выдана строго по состоянию). Итого 12.5 с,
`pauseState` чистый. То есть пауза/мьют/развязка/награда проверены против
живого адаптера, а не против нашего мока.
⚠️ ОСТАЛЬНЫЕ ПЛОЩАДКИ ТАК НЕ ЗАВОДЯТСЯ: `crazy_games` даёт `GeneralError`,
`playgama` — таймаут (их нативные SDK не грузятся вне своего домена), обе
падают в `stub`. Стенд работает ИМЕННО для Poki — этого хватает, чтобы
гонять боевой путь регрессией.
⚠️ ЧТО СТЕНД НЕ ЗАМЕНЯЕТ: смоук на developer.playgama.com. Награда пришла
через ~11 с БЕЗ видимого ролика в headless — не берусь утверждать, реальный
это показ или фолбэк Poki «ад не найден, награду всё равно выдай»; на портале
это надо подтвердить глазами. Экономика (не выдаём ли награду даром) — вопрос
именно к тому смоуку.
`playgama-bridge-config.local.json` (в .gitignore, завёл диспетчер) остаётся
для ДРУГИХ оверрайдов конфига — плейсменты, троттл, `disable`. Для подмены
ПЛОЩАДКИ он не нужен: URL-параметр проще и ничего не ломает в боевом файле.
Путь конфига при необходимости передаётся в `bridge.initialize({configFilePath})`.

⚠️ ЛОВУШКА ПРОБ: `window.__game` в бою СНЯТ (спека 2026-07-29) — живые пробы
только с `?dev=1`.

**Состояние:** Bridge v2: rewarded 4 плейсмента (встряска, Continue, ×2
монет, металлоискатель-скрыт), interstitial КАЖДЫЙ 5-Й УРОВЕНЬ (спека
владельца 2026-07-23; `INTER_EVERY_LEVELS`=5, копим ПОБЕДЫ через noteWin,
поражение/повтор не считаются; прежние INTER_MIN_WINS/INTER_GAP_MS сняты)
только в bridge-режиме; watchdog не убивает длинные ролики; сейв дублируется
в Bridge storage (семантический мерж + эпоха gen в 77-save).
⚠️ ТОЛЬКО НА ПОБЕДНОМ ПЕРЕХОДЕ (уточнение владельца 2026-07-24 «не на
переигровке из тупика»): maybeInterstitial зовёт СТРУКТУРНО лишь `againBtn`
(Next после победы, 90-input); из `loseAgainBtn` (Retry из тупика) вызов
УБРАН — спасение там rewarded Continue (loseAdContinue), не межстраничная.
msPlayBtn/pauseRestart гейт не зовут (перелив выстрелит на ближайшем
againBtn, на поражении — никогда). Загейтить внутри 78-ads НЕЛЬЗЯ:
bypass-переходы сюда не сигналят, win-латч утёк бы в Retry — различает
только проводка кнопок. Тупик = «нет ходов + нет встрясок» (99-main:489);
подсказки в гейт НЕ входят и НЕ должны — в тупике hint бесполезен
(findHintGroup null → не тратится) и разобрать тупик не может, только
встряска. Ассерт: реальный клик loseAgainBtn в форс-тупике при счётчике у
порога ролик НЕ показывает (зубы проверены — с возвращённым вызовом падает).
ПАУЗА+МЬЮТ НА ВРЕМЯ РОЛИКА (v1-test-85, требование Poki/CrazyGames — Bridge
за нас НЕ делает): pauseGame(true)+Sound.setMuted на входе, снятие в ЕДИНОЙ
точке endPending (награда/провал/watchdog/исключение/cancel); межстраничная
висит на INTERSTITIAL_STATE_CHANGED (OPENED→пауза, CLOSED/FAILED→резюм,
страховка 60с); звук площадки — AUDIO_STATE_CHANGED, два источника тишины
складываются. Новая каденция лишь МЕНЯЕТ, когда зовётся showInterstitial —
пауза/мьют композятся сами: площадка не показала → OPENED не пришёл → игра
не замерла.
ПРОГРЕСС-СЕЙВ (решение владельца 2026-07-23 «технологии площадки/бриджа, не
Google»): облако площадки через bridge.storage УЖЕ работает и проверено на
боевом SDK — кода не требуется. Реальность по площадкам и почему НЕ Google
(iframe/OAuth только опознаёт, а хранение = свой бэкенд) — в
docs/PROGRESS-SAVE.md. ⚠️ У Poki и GameDistribution облачного сейва в
Bridge НЕТ (разбор адаптеров): межустройственного прогресса там нет ни с
Google, ни без — ограничение площадки.
Фиксы ревью 2026-07-21: `Ads.cancel()` (genLevel гасит висящий показ —
протухший rewardCb больше не награждает новый уровень), watchdog чистится
на входе в showRewarded (сирота не отбирал следующую награду), исключение
SDK -> settleFail БЕЗ бесплатного стаба (дыра экономики), на время любого
показа тикает stats.lastAction (миксер не ест предметы под роликом),
showRewarded принимает onFail (возврат кнопки «×2»), захват coinsWon в
момент клика, Telemetry rw для shake/continue. Телеметрия: каркас
sendBeacon, ВЫКЛЮЧЕНА (URL='' в 79-telemetry) — ждёт endpoint воркера
владельца. Пакет тестерам: release/mixer-v1-testers.zip (Mixer.html +
README.txt, имена ASCII — кириллица в zip ломается на Windows).
Стабильный пакет портала: funnel-game/release/mixer-playgama.zip.
ХОСТИНГ ЕСТЬ: https://ikorzun.github.io/Blender/ (GitHub Pages от репо,
автодеплой с каждым выкатом в main) — закрывает iPhone-тестеров и загрузку
SDK по http(s).
Фикс 2026-07-23 (ОБЛАЧНЫЙ СЕЙВ, потеря данных): bridgeSyncSave() поднят
ВЫШЕ гейта isRewardedSupported. commitSave (77-save:53) пишет в
bridge.storage всегда, когда storage есть, а читал облако только вызов
из-за гейта — на площадке со storage, но БЕЗ rewarded прогресс уезжал в
облако в один конец. Порядок безопасен: loadSave() — top-level в 77-save,
Ads.init() — внутри RAPIER.init().then (99-main:830), т.е. локальный сейв
всегда загружен ДО облачного мержа; mergeSave монотонный (max), откатить
прогресс не может.

**Инварианты:** playgama-bridge.js НЕ инлайнить (LGPL, грузится только на
http/https); на file:// всегда режим-заглушка; вес по сети держать ~1МБ
(козырь канала). ⚠️ ЛОВУШКА ПРОБ: в headless-пробах НЕ трогать поля
`window.bridge` (в т.ч. `platform.id`) до завершения `initialize()` —
SDK на каждое обращение печатает console.error «Before using the SDK you
must initialize it», и опрос в waitForFunction сам себе рисует пачку
ошибок (ложный след 2026-07-23: 12 «ошибок игры» оказались следом пробы).
Ждать пассивной паузой. ⚠️ КАК ПЕРЕХВАТЫВАТЬ storage (стоило трёх пустых
проб 2026-07-23): `bridge.storage` — ЭКЗЕМПЛЯР КЛАССА, get/set лежат на
ПРОТОТИПЕ, и подмена на экземпляре НЕ прилипает (замерено: assignmentStuck
false при frozen/sealed false) — счётчики молча остаются нулевыми, и это
читается как «фикс не работает». Патчить надо прототип:
`Object.getPrototypeOf(bridge.storage).get = …`. Сам объект брать через
ДЕСКРИПТОР (`getOwnPropertyDescriptor(v,'storage').get`) — обычное чтение
`v.storage` до init вызывает геттер SDK и печатает ту же ошибку.

**Дальше:** телеметрия-воркер (Cloudflare, по образцу platform-landings
владельца) + вписать URL; смоук на developer.playgama.com (живой rewarded);
чек-листы Poki/CrazyGames через Bridge-конфиги (§7 docs/AD-CADENCE — там
УЖЕ вскрытые риски соответствия: мьют/пауза закрыты v85, альтернатива
rewarded упирается в COINS_ENABLED=false — решение владельца).
⚠️ ПОДПИСКА, отключающая рекламу («Subscribe отключает баннеры каждый 5
уровень») — ЖДЁТ владельца: (1) точка гварда готова (одна строка в начале
maybeInterstitial, `if (adsRemoved()) return;`); (2) флаг «реклама снята»
= покупка, живёт в сейве — запрос МЕТЕ через Междузонные в момент решения;
(3) ⚠️ ПЛАТЕЖЕЙ через Bridge у POKI НЕТ ВОВСЕ (разбор адаптеров:
Yandex — есть, Playgama/CrazyGames — условно/через Xsolla, Poki/
GameDistribution — нет). Продать подписку на Poki невозможно — владельцу
знать до выбора площадки запуска. Per-platform таблица каденции
(docs/AD-CADENCE) — механизм готов черновиком, включать по слову.

**Верификация:** test.js секция ads (stub на file://) + секция bridge
(локальный http-сервер с ПОДДЕЛЬНЫМ SDK rewarded=false: облако читается и
пишется, режим остаётся stub); смоук на площадке.
Фикс облачного сейва ПОДТВЕРЖДЁН НА ЖИВОМ ДЕПЛОЕ против БОЕВОГО SDK
(v1-test-81 на Pages, прототипный шпион): после initialize() storage.get=1
(bridgeSyncSave отработал на площадке БЕЗ rewarded — сценарий самого бага),
после изменения сейва storage.set=1, ошибок страницы нет. Сьют проверяет
то же на поддельном SDK; живая проба — что и настоящий Bridge принимает
вызов.
⚠️ Pages НЕ заменяет портал: замер 2026-07-23 по живому https — SDK и
конфиг отдаются 200, initialize() резолвится с рукопожатием к
api.playgama.com, консоль чистая, НО platform.id='mock' и
isRewardedSupported=false, поэтому mode остаётся 'stub'. Ветка 'bridge'
(showRewarded/interstitial/состояния + фиксы ревью 2026-07-21) не
исполнялась НИ РАЗУ — смоук на developer.playgama.com обязателен.

---

## БЛОК: ЯДРО ГЕЙМПЛЕЯ (охраняемая зона)

**Зона:** `80-gameplay.js` (матчи/тапы/встряска/миксер/Continue),
`40-items.js` (генерация уровня), правила в CLAUDE.md.

Правки здесь — только по прямой спеке владельца, с полным прогоном
test.js и обновлением CLAUDE.md. Любой чат может ЧИТАТЬ, менять — лучше
из чата, который владелец явно попросил (обычно ПОВЕСТВОВАНИЕ или сам).

---

## Междузонные запросы

- **ОТ МЕТЫ ДЛЯ ИНТЕГРАЦИИ (2026-07-24, экономика топапа, таблица №2):**
  когда будете вводить пополнение баланса рекламой/IAP — (1) грант звёзд
  зовите `addStars(n)` (77-save): он пишет в `Save.tu` (пополнения), НЕ в se,
  чтобы лидерборд не был pay-to-win. Номиналы в 00-config: `REWARD_STARS_PER_AD`
  70, `REWARD_DAILY_CAP` 5, `STAR_PACKS` [3000/19000/90000]. (2) ⚠️ ФИКС C:
  дневной кап рекламы держать на SERVER/BRIDGE-времени, НЕ на device-полночи —
  иначе откат часов даёт повторный кап (дюп). Полная таблица —
  docs/STARS-STORE-ECONOMY.md §v2.
- **ОТ МЕТЫ ДЛЯ ИНТЕРФЕЙСА (2026-07-24):** экран победы показывает СЫРОЙ счёт
  уровня (напр. «★ 5038»), а прирост баланса — деноминированный (÷10 = 503).
  Показывать `level.starsWon` (= floor(score/10), уже деноминир. прирост
  кошелька) вместо сырого stats.score — тогда win-экран сойдётся с чипом и
  кошельком. Данные готовы, правка на вашей стороне (win-оверлей v112).
- ОТ ФИЗИКИ ДЛЯ ГРАФИКИ (2026-07-23, осколки+помол владельца): в 80-gameplay
  добавлены СТАРТОВЫЕ shardFX (осколки — тетраэдры-меши цвета предмета для
  brick/pirate/rock) и grindShred (двухфазный помол с фонтаном осколков).
  Всё через addFX, 70-fx НЕ тронут. Просьба (как с пунктом 5): переполировать/
  перенести shardFX в 70-fx — форма осколков (сейчас простой тетраэдр),
  цвет/тинт по граням, возможно неровные сколы; тайминги/число осколков —
  мои дефолты, крутите вид. НЕ трогать: правило burstFX, тайминги
  grindShred (реальные часы против десинка на слабом FPS — обосновано в
  коде), параметр тряски. Звук «хруст» помола (75-audio) — на ваше усмотрение.
- ОТ ФИЗИКИ ДЛЯ ГРАФИКИ (2026-07-21, «пункт 5» владельца): в 80-gameplay
  добавлены СТАРТОВЫЕ эффекты лопанья групп >=4 по пачкам — juiceFX (сок,
  Points-капли fxColor), sparkFX (искры + 3 кубика-детальки), starPopFX
  (звёздочки starGeo, читаются как конфети-квадратики) + правило burstFX.
  Всё через публичный addFX, 70-fx НЕ тронут. Просьба: переполировать/
  перенести визуал в 70-fx по вкусу (спрайты звёзд вместо мешей, размер/
  яркость капель и искр, возможно звук «хлоп» в 75-audio) — ПРАВИЛО выбора
  (burstFX/BURST_MIN_N) и волну blastWave не трогать (ядро/физика).
- ~~ОТ ДИСПЕТЧЕРА ДЛЯ ГРАФИКИ: чайник удалён, сюрприз отключён~~ ЗАКРЫТО
  2026-07-21: сюрприз снова жив — золотая рыбка (animalfishGeo).
- ~~ОТ ДИСПЕТЧЕРА ДЛЯ ПОВЕСТВОВАНИЯ: MUSEUM-SPEC предполагала чайник первым
  экспонатом — пересмотреть под нового сюрприза (золотая рыбка).~~
  ✅ ЗАКРЫТО ПОВЕСТВОВАНИЕМ 2026-07-22: аддендум в MUSEUM-SPEC — первый
  экспонат = золотая рыбка, сеты пересобраны от паков моделей
  (Зверинец/Урожай/Гараж), награды учитывают скрытые монеты, калибровка
  порогов помечена к пересчёту под пул 63 типов.
- ОТ ДИСПЕТЧЕРА ДЛЯ ГРАФИКИ (ревью 2026-07-21, мелочи вашей зоны):
  1) докстринги tools/glb2module.py описывают старый CLI (аргументы/выход
  разошлись с фактическим), tools/sky2module.js не валидирует argv — следующая
  сессия соберёт мусор молча; 2) палитровый атлас 36-models до decode даёт
  чёрную вспышку моделей на первых кадрах — просьба класть 1×1-заглушку в
  генераторе (небу диспетчер уже подложил в 10-stage); 3) ВОПРОС ВЛАДЕЛЬЦУ
  задан: пончик fooddonutsprinkles — convex hull заливает дырку (в Hard
  бублик «перекрывает» то, что видно сквозь неё) — компаунд капсул/увести
  из ранних типов/принять как есть.
- ОТ ДИСПЕТЧЕРА ДЛЯ ФИЗИКИ (ревью 2026-07-21): порог соака wallExcess>0.18
  мягче канона доки (~0.15) — выровняйте по своему решению (замер или правка
  доки); и мёртвая чайниковая ветка 'teapot'/'surprise' в 50-physics ждёт
  вашего решения об удалении (см. заметку ГРАФИКИ ниже).
  ДОПОЛНЕНИЕ v1-test-38: после удаления процедурных типов из пула мёртвыми
  стали и ветки case 'cube'/'ball'/'torus'/'knot'/'spiral'/'pill'/'cyl' в
  createItemBody и buildAccessSamples — те же правила: решение об удалении
  ваше, спешки нет (типы могут вернуться строкой в TYPES).
- **ОТ ИНТЕРФЕЙС ДЛЯ МЕТЫ (2026-07-23, главный экран/пауза 770:1271):**
  макет главного экрана содержит ЭКОНОМИКУ, которой в движке нет — нужны
  решения владельца, пока на плейсхолдерах (действие = тост «Coming soon»).
  (1) ЗВЁЗДЫ-КАК-ВАЛЮТА: макет рисует «166.5K ★» как тратимую валюту (кнопки
  Get More, Boost 11k). В движке totalStars() — сумма РЕЙТИНГА уровней
  (маленькое число, не тратится). Развилка: (а) отдельная мягкая валюта,
  (б) звёзды становятся тратимыми, (в) расчехлить монеты (COINS_ENABLED=false)
  под это. (2) МЕХАНИКА BOOST: прокачка предмета за валюту — что делает
  (ускорить ступень накопления?), цена, эффект на множитель. Сейчас ступени
  растут только от совмещений. Обе развилки влияют и на десктоп 763:1031.
- **ОТ ИНТЕРФЕЙС ДЛЯ ИНТЕГРАЦИИ (2026-07-23):** баннер «No more AD /
  Subscribe $1.99» на главном экране — нужен реальный плейсмент (покупка/
  подписка через Bridge или заглушка до портала?) и что именно отключает
  рекламу. Кнопка сейчас — тост-заглушка.
- **ОТ ИНТЕРФЕЙС ДЛЯ ГРАФИКИ/МЕТЫ (2026-07-23, не блокер):** портреты
  коллекции (itemThumb) есть только у типов, ЖИВЫХ в текущей партии (мешей
  вне уровня нет) — у неоткрытых/вне партии сейчас буква-плейсхолдер. Для
  портрета ВСЕХ 93 типов на главном экране нужен хелпер «собрать меш по типу
  вне уровня» (геометрия+материал типа без тела Rapier). Для чекпоинта буквы
  читаются нормально.

(формат: «ОТ <направление> ДЛЯ <направление>: что нужно и зачем»)
- ~~**ОТ ИНТЕРФЕЙС ДЛЯ МЕТЫ (2026-07-22, мелочь):** accLabel не режет
  префиксы новых пачек brick/pirate~~ ✅ **ЗАКРЫТО МЕТОЙ 2026-07-22**
  (ветка `claude/meta-acclabel`): срез покрывает все пять пачек
  (animal|food|car|brick|pirate). Проба по ВСЕМ 93 типам нашла сверх
  заказанного: (1) КОЛЛИЗИЯ — `animalfish` и `foodfish` давали
  одинаковое «Fish», в витрине это две неразличимые строки; карта ярлыков
  переведена с СРЕЗА на ПОЛНЫЕ ключи, рыбы разведены (Fish / Cooked fish);
  (2) склейки новых типов — Wholeham/Cakebirthday/Icecreamscoopmint/
  Kartoobi → Whole ham / Birthday cake / Mint ice cream / Go-kart, плюс
  Traffic cone, Box truck, Takeout box, Hot dog (сверено по именам
  ассетов в «3d assets»); (3) кирпичам добавлено слово brick — их имена
  голые формы (round/bar/duo/stud), «Round» в списке не читается;
  пиратские самостоятельны и идут срезом. Проба: ни одного сырого ключа,
  ни одного дубликата. SUITE: PASS.
- **ОТ ГРАФИКИ ДЛЯ ЯДРА/ФИЗИКИ (2026-07-22, ЗАКРЫТО решением владельца):**
  у пачек Brick и Pirate нет пак-эффекта лопанья — уходят в `else dissolveFX`.
  РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-07-22: «оставляем в труху». Кирпичи и пиратские вещи
  лопаются трухой — это НЕ баг и не долг; `burstFX` расширять НЕ надо,
  новых эффектов для этих пачек НЕ заводить без нового слова владельца.
- ⚠️ **ОТ ГРАФИКИ ДЛЯ ФИЗИКИ (2026-07-22, СРОЧНО — риск затирания):**
  `src/app/36-models.js` генерится **ЦЕЛИКОМ** одним прогоном
  `tools/glb2module.py <выход> <каталог>:<префикс> ...` — файл переписывается
  под ПЕРЕДАННЫЙ список пачек. Сейчас там ПЯТЬ пачек (animal/food/car/brick/
  pirate, 77 моделей). Если под камни запустить конвертер только на rocks,
  **из игры молча исчезнут 15 моделей Brick/Pirate и пул рухнет с 78 типов**.
  Варианты: (а) писать камни в ОТДЕЛЬНЫЙ модуль (напр. `37-rocks.js`) —
  как и сказано в вашей спеке «точечный модуль вне TYPES», это правильный
  путь; (б) если всё же через glb2module в 36-models — обязательно передать
  ВСЕ пять пачек плюс rocks одной командой (актуальную строку возьмите из
  моего блока/коммита e620338).
  Камни `rocks-a`/`rocks-sand-c` я НЕ конвертировал и в TYPES не вводил —
  задвоения с вашей стороны нет.
- **ОТ ИНТЕРФЕЙС ДЛЯ МЕТЫ (2026-07-22): КОНТРАКТ НАКОПЛЕНИЙ — ПРИНЯТ,
  потребитель готов.** Жду от вас в общем скоупе: `accSnapshot()` ->
  [{name, count, tier, mult, next}] (name — ЧЕЛОВЕЧЕСКОЕ имя типа; демо
  сейчас показывает ключи t0..tN) и `onAccTierUp(cb)` с {name, tier,
  mult, item} — item нужен ЖИВОЙ (у него берётся mesh для миниатюры).
  Мой код подхватывает обе функции автоматически, если они существуют
  (typeof-проверки в 85-hud), до тех пор музей рисует демо-данные с
  бейджем DEMO, а всплывашку можно дёргать кнопкой «Tier-up demo» в
  панели разработчика. Пороги/множители в демо — по вашему контракту
  (100/300/700/1500/3100, +25%).
- ✅ **ЗАКРЫТО ГРАФИКОЙ (2026-07-21-в, доработано -г):** комбо — свечение-
  потолок без выбеливания (K=0.60); помол — красный верх. Доработка владельца
  (2026-07-21-г, пришла через диспетчера): (1) цвет лихорадки ПО ВРЕМЕНИ СУТОК
  — голубой `#8cc7ff` только ночью, зелёный днём (граница как у панорам);
  (2) красный верх — ЛЕСЕНКА УГРОЗЫ: растёт за 10 с до помола «медленно»,
  максимум на лопастях, гаснет на матч быстрее подъёма. Контраст HUD держит
  3:1 на ВСЕЙ лесенке красного (день 3.7→6.5:1). Детали — блок ГРАФИКА.
  Исходный запрос ниже.
- **ОТ ИНТЕРФЕЙС ДЛЯ ГРАФИКИ (2026-07-21-в, спека владельца, шейдер неба
  в 10-stage):**
  1) ЛИХОРАДКА СНИЗУ: зелёный `vec3(0.30,0.87,0.50)` заменить на «нежный
  голубой» (предлагаю sRGB `vec3(0.55,0.78,1.0)` ≈ #8cc7ff — сверить с
  владельцем по картинке) и НЕ ВЫСВЕТЛЯТЬ панораму настолько: сейчас при
  uCombo=1 фон полностью замещается градиентом до чистой белизны кверху —
  владелец просит, чтобы картинка неба оставалась читаемой (например,
  потолок примеси mix(col, hot, uCombo*K) с K≈0.55-0.6 или белую верхушку
  градиента заменить на слегка тонированную).
  2) НОВОЕ — ПОМОЛ: при работе лопастей ВЕРХ экрана подсвечивать мягким
  красным градиентом, зеркально нижнему (uGrind 0..1, лерп ~0.35 с как у
  uCombo; экранная координата 1−sy). Сигнал: переменная grinding уже
  живёт в hud-тике 99-main рядом с обновлением uCombo.
  Мокапов нет — «по примеру как снизу зелёным» (слова владельца).
**ОТ ГРАФИКИ ДЛЯ ФИЗИКИ (к сведению, правок не прошу):** сюрприз больше
не чайник — `makeSurprise` зовёт `createItemBody(item, 'surprisehull', geo)`,
чтобы уйти в default-ветку (convex hull). Ветка `case 'teapot': case
'surprise':` в 50-physics (компаунд трёх шаров) и одноимённая ветка в
`buildAccessSamples` теперь МЁРТВЫЕ. Удалять — ваше решение.

**ОТ ГРАФИКИ ДЛЯ ЯДРА:** после ввода моделей чаша недозаполнена —
topY 4.79 против 6.95 на примитивах (норма 7.5-9.0). Модели тонкие, объёма
дают вдвое меньше шара; охват им уже поднят до 1.0. Дальше упирается в
`PAIRS`, а его менять только по решению владельца. Замеры — docs/3D-ASSETS.md.

**ОТ ГРАФИКИ ДЛЯ ВСЕХ:** `test.js` форсировал тупик после фиксированной
паузы 600 мс — на моделях куча ещё движется и тупик не наступает (тик
перезаписывает matchRadius). Заменил на ожидание устойчивого штиля.
Это общая инфраструктура, а не только моя зона — учтите.
- **ОТ ИНТЕРФЕЙС ДЛЯ ГРАФИКИ (2026-07-21), СРОЧНО:** фон поля должен стать
  светло-голубым **#d0dff3** вместо белого — решение владельца по макетам
  Figma 741:1497 и 741:1738. Это НЕ косметика: в макете белые глаза
  персонажа и белые круглые кнопки (пауза) читаются только на голубом, на
  белом поле они исчезают. ⚠️ ФОН ЦЕЛИКОМ ВАШ (указание владельца
  2026-07-21): ИНТЕРФЕЙС его не трогает, `html/body` в shell.html оставлен
  белым — менять и поле (шейдер неба в `10-stage`), и подложку под ним
  вам. Отменяет инвариант «поле БЕЛОЕ» в CLAUDE.md; там же предупреждение
  про сырые sRGB-значения в uniforms шейдера (без convertSRGBToLinear).
- ~~ОТ ИНТЕРФЕЙС ДЛЯ ЯДРА~~ ЗАКРЫТО диспетчером при мерже v1-test-36:
  faceEvent('surprised', 1000) добавлен в collectSurprise. Было: в `collectSurprise` (80-gameplay)
  добавить строку `faceEvent('surprised', 1000);` — по матрице эмоций
  владельца раскопанный сюрприз даёт «удивлённые» глаза на 1 с. Функция уже
  есть в 85-hud, остальные 10 состояний работают. Правку не делал сам —
  чужая зона. Детали: docs/EYES-CHARACTER-SPEC.md §5.

---

## Журнал версий

- v1-test-202 · 2026-07-31 — ДИСПЕТЧЕР: ДРОЖЬ КУРСОРА УДАЛЕНА (вердикт
  владельца живым тестом: «убери дрожь курсора»; жила 20 минут, успела ужаться
  1 c → 0.5 c — не помогло). Снято всё: вызов, функция, CSS, страж; могильник-
  комментарий в 90-input, НЕ возвращать. Живой тест снова отработал как надо:
  дешёвая проба → быстрый вердикт → чистый откат. 333 PASS.

- v1-test-201 · 2026-07-31 — Merge ИНТЕРФЕЙС: ЧИПСА МНОЖИТЕЛЯ ×1.25 (живой баг
  владельца). Виновник определён замером: `.vmult` витрины (владелец играет в
  ландшафте — в портрете панели нет). Фикс-ширина 56px снята (их же коммент
  «запас под ×3.25» был неверен — запаса не было, центрованный текст вылезал В
  ОБЕ стороны), паддинги 6/6 по спеке, вылет 5.0/7.4 → 0/0, пин зазора 12px
  цел, панель не выросла. 333 PASS, страж двусторонний.
  ⚠️ КАНОН пополнен их ловушкой: scrollWidth не видит вылет ВЛЕВО у
  центрованного текста — верна метрика Range.getBoundingClientRect.
  ⚠️ ДВА ВОПРОСА ВЛАДЕЛЬЦУ переданы: рваные правые края бейджей (новая спека
  столкнулась с его же спекой выравнивания 2026-07-28) и тот же вылет у
  тоста #ttMult (87px из макета — по правилу «расхождение с макетом — спроси»).

- v1-test-200 · 2026-07-31 — ДИСПЕТЧЕР: ПАЧКА ЖИВЫХ ПРАВОК ВЛАДЕЛЬЦА (он
  играет с телефона/десктопа и шлёт вердикты потоком). 333 PASS.
  • КУРСОРЫ v2: «плохого качества, хотя он svg» — мыло давал одинарный 32px
    PNG на Retina (SVG в cursor не умеет Safari); лечение image-set 1x/2x,
    2x-рендер честным браузером. СЕМАНТИКА ЕГО СЛОВОМ: hand_point = ОСНОВНОЙ
    (везде, и на кнопках), hand_closed = схватить/крутить чашу; hand_open
    ВЫВЕДЕН (файл в Interface/ жив).
  • ДРОЖЬ КУРСОРА на «пара не сходится»: чередование двух хотспотов ±2px,
    8 тиков × 62 мс ≈ 0.5 c (правка владельца тут же: 1 c — слишком долго).
    Системный курсор двигать нельзя — дрожит картинка относительно точки.
  • ЗАРЯД: pointerdown вместо click (жалоба «нажимать дважды + задержка») +
    эффект = РАСТВОРЕНИЕ dissolveFX на каждом («иначе не понятно что
    произошло») + удаление без 150 мс паузы бомбы. Страж «с ПЕРВОГО
    pointerdown».
  • «Grinding» УБРАН при помоле (спека со скрина: «и так понятно») — пусто,
    говорят злые глаза и лопасти. Отменяет спеку 2026-07-21 п.7.
  ⚠️ Страж дрожи в прогоне ПРОПУСКАЕТСЯ (цель для тапа не нашлась при
  радиусе −9) — покрытие дрожи пока только грантом класса; TODO при случае.
  ДЕЛЕГИРОВАНО параллельно: ИНТЕРФЕЙСУ — чипса ×1.25 (в работе), слот-модель
  с пульсом bounce, заливка дырок цифр цветом обводки, пилюля «+1» по Figma
  779-1114; ГРАФИКЕ — пригасить фиолетовый низ ночи (вариантами на вердикт).

- v1-test-199 · 2026-07-31 — Merge ИНТЕРФЕЙС: ПОЛИРОВКА СЛОТА ЗАРЯДА. 332 PASS.
  ⚠️ МОЯ ПОСТАНОВКА БЫЛА НЕВЕРНА, Интерфейс поправил замером: я писал
  «лестничная opacity из updateHUD (тик 600 мс)» — ТИКА НЕТ, updateHUD
  событийный, растворение не выполнялось ВООБЩЕ (кнопка висела непрозрачной
  все 7 c и пропадала скачком). Их замер: 1 значение opacity до правки,
  20 из 20 после.
  Сделано: покадровое растворение rAF по живому chargeState().leftMs (ОДИН
  источник времени — ядро; CSS-переход и animation отвергнуты как второй
  отсчёт); вид — язык бара (.iconBtn + системное правило день/ночь) с
  лаймовым кольцом (лайм = язык зарядов); вход chargePop 0.3 c, prefers-
  reduced-motion уважен; бейдж-отсчёт НЕ делали намеренно (растворение и есть
  таймер — второй канал того же времени лишний, захочет владелец — слово).
  Попутно их зоной: портрет холодной пачки ДОБИРАЕТСЯ (раньше слот мог
  обещать предмет ПРОШЛОГО заряда — хуже пустоты).
  ⚠️ Их страж «прозрачность падает БЕЗ событий» проверен на способность падать
  НА ОБЕИХ сборках, и первая версия стража была бы флейком на исправной ветке
  (окно 1.2 c против рампы, нормированной на константу TTL) — поймано
  двусторонним прогоном. Пятый случай «может ли инструмент измерить» за
  проект, и снова пойман до отгрузки.

- v1-test-198 · 2026-07-31 — ДИСПЕТЧЕР: КАСТОМНЫЕ КУРСОРЫ ДЕСКТОПА (спека
  владельца: «возьми из папки кастомные курсоры и добавь в десктопную версию»).
  Interface/hand_*.svg (32×32): открытая ладонь = покой, указательная =
  интерактив, сжатая = драг камеры (html.grabbing из 90-input, снятие в
  endPointer И resetPointers — границы интро). SVG растеризованы в PNG
  честным браузером: Safari SVG-курсоры НЕ умеет (правило кросс-браузера);
  data-URI ~1 КБ каждый, гейт pointer:fine. 331 PASS (+3 стража).
  ⚠️ ДВЕ КАСКАДНЫЕ ЗАПАДНИ, обе пойманы стражем: (1) поздние cursor:pointer
  перебивали ранний блок — перенос в КОНЕЦ CSS (порядок несущий, помечено);
  (2) НЕ ПОМОГЛО: pointer рассыпан по ID-селекторам, специфичность бьёт
  порядок — !important на интерактивном правиле, законность обоснована в
  комментарии (перечисление 20 id хрупко, новая кнопка выпадала бы молча).

- v1-test-197 · 2026-07-31 — ДИСПЕТЧЕР: «ЗАРЯД ТИПА» + ТУРБО УЖЕСТОЧЕНО
  (четыре спеки владельца одной сессией). Полный разбор — CLAUDE.md, «Правила
  игры». Кратко: заряд выпадает на зажигании цепи (1/уровень, тип с >=6 копий,
  слот у подсказки, TTL 7 c с растворением), детонация = СПАСЕНИЕ всех копий
  типа включая недоступные, цена капнута формулой группы без ×2. Турбо: вход
  дорожает лесенкой 10..14, ОДИН промах гасит, досыпка в окно 3 c (сумма
  прежняя). UI-слот минимальный — ПОЛИРОВКА ЗА ИНТЕРФЕЙСОМ. 328 PASS.
  ⚠️ TTL-поправка владельца пришла ПОСЕРЕДИНЕ реализации и УЛУЧШИЛА дизайн:
  сейв-обвязка заряда (поле, мерж, агентность тупика) стала не нужна и снята
  до релиза — заряд чисто рантаймовый.
  ⚠️ Три стража завалились МОИМИ же инструментами до зелени: мгновенный снимок
  против хвоста анимации удаления; чтение несуществующего поля combo();
  (плюс вчерашний формат процентов). Все три поправлены с записью в тесте.

- (док) 2026-07-31 — Merge ПОВЕСТВОВАНИЕ: docs/COMIC-BRIEF.md — бриф для
  нейро-арта комикса (запрос владельца «распиши историю, чтобы отдать нейронке»).
  8 промтов = 8 живых панелей, привязка к триггерам жёсткая (новый арт меняет
  панели один-в-один, менять состав = менять код), цвета сверены с кодом
  выборочно диспетчером. Сьют не гонялся — кода нет.
  ⚠️ ПРАВИЛО ИЗ ГРАБЛИ (владелец не смог открыть док до мержа — worktree):
  (1) документ для владельца ВСЕГДА дублируется вложением в чат при сдаче;
  (2) док-сдачи диспетчер мержит ВНЕ ОЧЕРЕДИ, без ожидания релизного окна.

- v1-test-196 · 2026-07-31 — Merge ГРАФИКА: НОВЫЕ ПАЛИТРЫ НЕБА + ГРАНИЦА 20:00
  (вердикт владельца: «раскладка по экрану, цвета ок, мержи»).
  Многостопные градиенты владельца (ночь 12 стопов до мадженты #ff2fdc, день 7)
  на ramp-текстуре 256×1; РАСКЛАДКА ПО ЭКРАНУ — утверждена после замера Графики
  (по взгляду видно лишь 70..100% палитры, ночь вырождалась в мадженту; по
  экрану видно всё и --sky-top/bot-rgb совпадают с кромками кадра Δ0 — полосы
  Safari честные). Переключатель SKY_MAP жив. День 5-20/ночь 20-5 единым
  источником SKY_DAY_FROM/NIGHT_FROM (дублей больше нет). Форс-хук ?hour= +
  страж совпадения неба и темы (проверен диверсией). Контраст глаз днём
  1.6:1 → 2.98:1 — исторический риск снят цветами владельца.
  ⚠️ Регрессию своей же правки (uResY при applyPerfTier — верх палитры срезало
  бы на слабом до конца сессии) Графика нашла адверсарным ревью диффа и
  починила до сдачи; правило «кто меняет размер буфера — зовёт resize()»
  стоит комментарием. 319 PASS.

- v1-test-195 · 2026-07-31 — Merge ПОВЕСТВОВАНИЕ: ПРОЛОГ-КОМИКС ПЕРЕД ИГРОЙ
  (слово владельца «нужно рассказать эту историю перед игрой в виде комикса»).
  К0+К1 стали прологом из 3 панелей в фазе 'wait' интро: занавес площадки
  убран, чаша нарисована, предметы ЕЩЁ НЕ ПАДАЛИ — анимация заполнения (за
  которую владелец бился) начинается ПОСЛЕ комикса, не под ним (замер: topY
  32.1 до / 9.5 после закрытия). Один раз за жизнь сейва; между уровнями
  К0/К1 больше не приходят; вехи К2-К4 не тронуты. 303 PASS.
  ⚠️ ОСОЗНАННАЯ ОТМЕНА §6.1 спеки («никогда до первого тапа») СЛОВОМ
  ВЛАДЕЛЬЦА: риск «играю через 20 секунд» снят конструкцией — внимательный
  игрок проходит пролог тремя тапами за 927 мс, пассивный худший случай 7.8 с.
  ⚠️ ГРАНДФАЗЕР: st===0 — видевшие К0/К1 по старой схеме пролог не получают.
  ⚠️ КОЛБЭК done() НЕСУЩИЙ (на нём запуск падения предметов): оба пути под
  ассертами, потеря = мёртвая игра с пустой чашей. skipIntro закрывает пролог
  штатно — автопрогон и координатные клики защищены.
  ⚠️ Их честность: первый вариант их теста врал («панелей 0» при исправной
  механике — промис-обёртка), переписан синхронно. Сама фича была права.

- v1-test-194 · 2026-07-31 — ДИСПЕТЧЕР: ТЕСТ DIRTY СНЯТ (вердикт владельца:
  «убери все объекты из папки Dirty, они выглядят сильно инородно и грязно»).
  Модуль 39-dirty.js и 5 строк TYPES удалены, вес вернулся 9.68 → 9.02 МБ.
  ⛔ РЕАЛИСТИЧНАЯ СТИЛИСТИКА ОТВЕРГНУТА ВЛАДЕЛЬЦЕМ ПО ЖИВОМУ ТЕСТУ — в
  «Отвергнутое» канона; пак Dirty (97 предметов, ассеты в «3d assets/Dirty»)
  в пул не предлагать. Сам тест был правильным способом решить: скрин ур.1
  с пятёркой дал вердикт за минуты, а не спор о вкусах.
  + СЬЮТ: секция бомбы ИЗОЛИРОВАНА (сигнал ПОВЕСТВОВАНИЯ: два красных за два
  дня на соседних контекстных секциях — камни occluded, бомба −1; оба раза
  чужие ребейзы платили атрибуцией). Реген до «есть бомба и камень», кап 3.
  305 PASS.

- v1-test-193 · 2026-07-31 — ДИСПЕТЧЕР: ТЕСТ СТИЛИСТИКИ — 5 предметов Dirty
  с ПЕРВОГО уровня (спека владельца «закинуть объекты другой стилистики…
  это тест»). Бургер/огнетушитель/кость D20/камера/бутылка из пака Dirty
  (реалистичная стилистика, у каждого СВОЯ текстура — в паке 97 предметов без
  общего атласа). Модуль 39-dirty.js (разовая генерация, скрипт в истории
  коммита), TYPES на индексах 4..8 — в базовой девятке ур.1 рядом с 4 нашими
  для контраста. Прежние обитатели сдвинуты, не удалены. 305 PASS.
  ⚠️ ВЕС: 9.03 → 9.68 МБ (+0.65 — пять личных текстур по 46-116 КБ base64).
  Зип у лимита пока держится, но ЕЩЁ полтора таких теста не влезут. Вердикт
  владельца решает: «нравится» → оптимизация (общий атлас/даунскейл),
  «не нравится» → снести модуль + 5 строк TYPES, вес вернётся.
  ⚠️ Сентинелы стражей проверены: сдвиг +5 не ломает ни один (дрейдел 24,
  рыба 32 — «не в первой десятке» цел, forestplant последний).

- v1-test-192 · 2026-07-31 — Merge ПОВЕСТВОВАНИЕ: АРКА СЮЖЕТА ЦЕЛИКОМ, К2-К4
  (задание владельца «делай К2, К3 и К4»). К2 «Куда?..» (первое сомнение),
  К3 «Раздел второй», К4 «Музей?!» — твист в 3 панели (полка → шок → ярость).
  305 PASS (+9 сюжетных).
  ⚠️ ИХ ЖЕ ВЧЕРАШНЕЕ УТВЕРЖДЕНИЕ ОТМЕНЕНО ИМИ ЖЕ: «К2-К4 упираются в музейные
  вехи Интерфейса» — НЕ упираются. Музей И ЕСТЬ Save.ac: триггеры выведены из
  собственных данных (первая ЗАРАБОТАННАЯ ступень / ступень во второй пачке /
  первый полный сет). Интерфейсу ничего делать не нужно, гонки контрактов нет.
  ⚠️ БАГ ЛОРА, пойманный их же прогоном ДО отгрузки: веха считала accTier
  (заработанное + КУПЛЕННОЕ boostTier) — игрок с купленным бустом и нулём
  спасённых получал бы «первый экспонат на полке». Переведено на accCountTier
  (только заработанное). Вскрылось потому, что сьют покупает бусты.
  ⚠️ ЗАМЕР ДЛЯ ВЛАДЕЛЬЦА: твист К4 наступает ~с 37-го уровня (первая закрываемая
  пачка — holiday, 7 типов к ур.37). Защита STORY_SET_MIN=4: сеты из 1-2 типов
  твиста не запускают. Порядок глав строгий (К4 не обгонит К2) — под ассертом.
  ⚠️ Их честная фиксация чужого флейка: ассерт камней (occluded) мигнул один
  раз, на чистой базе и повторе зелено — задокументированный флейк перекрытия,
  частоту наблюдаем.

- v1-test-191 · 2026-07-31 — Merge ГРАФИКА: НЕБО-ГРАДИЕНТ ВЕЗДЕ, ПАНОРАМЫ
  УДАЛЕНЫ (спека владельца «убери в десктопе картинку, всегда градиент»).
  Это ПЕРВАЯ половина задачи неба: источник переключён, структура градиента
  (SKY_GRAD, 3 опоры) не тронута. ВТОРАЯ половина у Графики в очереди: новые
  многостопные цвета владельца (ночь 12 стопов до мадженты, день 7), граница
  20:00 и скриншоты на его одобрение. Сборка 20 модулей. 296 PASS.
  ⚠️ ПРИЗ ПО ВЕСУ НЕ ОПРАВДАЛСЯ, Графика честно замерила: −0.9% (панорамы уже
  были ужаты до 22-32 КБ). Настоящий вес — rapier 2.24 МБ и модели; за
  запасом перед заливкой идти туда, не в небо.
  ⚠️ Их находка: комментарий в 00-config УЖЕ утверждал «05-sky удалён» — ложь
  с 22.07 (модуль вернулся гибридом, комментарий не поправили). Теперь правда.
  ⚠️ ФЛЕЙК СТРАЖА ПОЛА (~5% прогонов) — диагноз Графики, подтверждён пробой
  2×18: снимок ровно на 3000 мс ловил ТРАНЗИЕНТНУЮ просадку летящей кучи,
  которую спасатель по замыслу ещё не имеет права поднять (второй ключ ждёт
  1.5 с). Починен диспетчером на мерже: ОПРОС ДО ЧИСТОТЫ (кап 9 с) вместо
  снимка по часам — залипание от опроса не прячется. ФИЗИКЕ на ревью во
  вторник.

- v1-test-190 · 2026-07-30 — Merge ФИЗИКА: ПЕРЕЗАМЕР ПОЛА НА TYPES=120 +
  чистка следов стейка. Гонцы разминулись: bd97e58 уже ушёл в main как v189,
  пока Физика перебазировала ветку — взята их дельта поверх (комментарии по
  КЛАССУ «плоское под нагрузкой» вместо имён удалённых моделей; правило
  «порог выведен из ФОРМЫ — при смене партии моделей перемерять calm-pen»;
  инвариант соака дозрел: залипание = 2 сэмпла подряд ИЛИ спящее тело,
  счётчик underHits). Перезамер: жертва №1 теперь brickbar, здоровый максимум
  0.088, порог 0.12 держит с запасом 1.36×. Их страж пола на стейке НЕ стоял
  (сцена на первом доступном) — удаление типа его не задело, проверено.
  Два хвостовых комментария про стейк (SHAKE_RESP, wallR) переписаны на класс
  тем же заходом, как Физика и просила. 296 PASS.

- v1-test-189 · 2026-07-30 — Merge ФИЗИКА: ОТКАТ ТОЛЩИНЫ ПЛИТЫ (слово
  владельца «откати толщину плиты, оставь только спасателя»). Полутолщина
  снова 0.3 бит-в-бит, спасатель и инструменты целы, замер-обоснование
  сохранено комментарием у строк. Верификация Физики: просадка на спящей куче
  max 0.109 (у толстой было 0.116) — плита была нейтральна; соак 2×12 мин без
  залипаний. 296 PASS.
  ⚠️ ЧЕСТНАЯ ПОПРАВКА ФИЗИКИ К СВОЕМУ ЖЕ ИНВАРИАНТУ: «UNDER FLOOR в сэмпле
  быть не должно вовсе» противоречил их же замыслу (спасатель НАМЕРЕННО ждёт
  до 1.5 с у движущегося — иначе шторм телепортов) и давал ложные тревоги.
  Транзиенты замерены: максимум РОВНО 1500 мс = проектный потолок. Инвариант
  переписан на ЗАЛИПАНИЕ (два подряд сэмпла ≥5 с ИЛИ под полом на спящем);
  исходный баг (30 с+) ловится с запасом. Отказ замазывать тревоги и замер
  длительности вместо ослабления порога — образец работы с инвариантом.
  ⚠️ WALL EXCESS в соаке по-прежнему красный (0.18-0.55, 4/12мин) — стены-
  частокол, долг №1, план вторник. Присутствовал при обеих толщинах плиты.

- v1-test-188 · 2026-07-30 — Merge ПОВЕСТВОВАНИЕ: СЮЖЕТ В ИГРЕ, К0+К1 (задание
  владельца «реализуй сюжет, начни с К0 и К1»). Первый код сюжета за всё время
  проекта — спека лежала с 22.07 при нуле строк. Модуль 86-story.js (22-й),
  сборка подхватила автоматически. К0 «Рецепт» после первой победы, К1
  «Помощник» при разрыве в 2 уровня. Все инварианты спеки под ассертами:
  бессловесно, одна строка в checkEnd, ≤1 виньетки за 2 уровня, никогда до
  первого тапа, сейв Save.st (OR) / Save.sv (max) по чек-листу 77-save.
  296 PASS (+10 сюжетных).
  ⚠️ Повествование само поймало СВОЮ регрессию: виньетка глотала клики чужих
  секций сьюта (уронила тест пола ФИЗИКИ) — storyEnable(false) на механических
  секциях. Образцовая сдача: чистая база прогнана для атрибуции.
  ⚠️ Их тревога «PAGEERROR reading 'boom' на чистой базе» — ЛОЖНАЯ: это
  НАМЕРЕННЫЙ синтетический крэш секции фатального экрана, отфильтрован в
  realErrors по паттерну. Разъяснено, действий не требует.
  К2-К4 ждут музейных вех (события интерфейса), добавляются строкой в
  STORY_CHAPTERS.

- v1-test-187 · 2026-07-30 — Merge ГРАФИКА: УДАЛЕНЫ ЛЕДЕНЕЦ И СТЕЙК (слово
  владельца). TYPES=120, сборка 21 модуль. 285 PASS.
  ⛔ СТЕЙК — отмена владельцем его же утренней спеки (утром «двигай ближе к
  началу, обязана быть в игре», вечером «удали совсем»). Страж стейка снят
  вместе с типом; замена сентинела перемешивания (леденец → дрейдел) спасла
  второй страж от тихого ослабления на треть — поймала Графика.
  ⚠️ Перестановку типов Графика НЕ дублировала: сверилась с main и увидела,
  что диспетчер уже сделал (урок гонки v178/v181 применён).
  ⚠️ ОЖИДАЕТСЯ: откат толстой плиты пола (слово владельца Физике «оставь
  только спасателя») — Физика гонит прогоны, релиз отдельным номером.

- v1-test-186 · 2026-07-30 — ДИСПЕТЧЕР: ТАП ПО ГЛАЗАМ = ПРОВОКАЦИЯ ПОМОЛА
  (спека владельца «клик или тап на глаза сразу злит миксер и включает
  измельчение»). Механика переиспользует наказание за простой: lastAction в
  прошлое + nextGrind=now (первый укус немедленно), глаза злеют сами, любой
  матч/встряска останавливают. Вне партии — только подскок. Лживый звук
  'match' с тапа убран (AUDIO-PLAN §1 закрыт). Канон глаз обновлён
  (EYES-CHARACTER-SPEC, журнал). 2 стража: укус случился, матч остановил.
  ⚠️ Страж с первого прогона упал TimeoutError — клик перехватывал winOverlay
  из предыдущей секции (правило «закрывай оверлеи перед кликами» снова
  подтвердило себя). 286 PASS.

- v1-test-185 · 2026-07-30 — Merge ФИЗИКА: СПАСАТЕЛЬ ПОЛА («дыра в объектах»).
  Полный разбор — CLAUDE.md, блок «СПАСАТЕЛЬ ПОЛА» в разделе физики. Кратко:
  корень — солвер терпит проникновение плоских форм + сон замораживает
  утонувших; обе гипотезы диспетчера (взрыв 15/18, перегруз солвера)
  ОТВЕРГНУТЫ замером — спека силы взрыва цела. Порог 0.12 по манифолду,
  два окна сна закрыты, плита утолщена (отделимо, оставлено мной осознанно:
  до выброса предмета в пустоту оставалось 7%). Соак 2×12 мин: провалы 3→0,
  шторма нет. 284 PASS.
  ⚠️ WALL EXCESS 0.19-0.23 в соаке ОСТАЁТСЯ КРАСНЫМ — атрибуция железная
  (floorLifts=0 в обоих прогонах): это стены-частокол, долг №1, план вторник.

- v1-test-184 · 2026-07-30 — Merge ИНТЕРФЕЙС + ДИСПЕТЧЕР: ГРОМКОСТЬ ЗВУКА
  (жалоба владельца «ползунок Sound не сохраняет состояние»). 279 PASS.
  • ИНТЕРФЕЙС: причина структурная — состоянием был БУЛЕВ CFG.sound, ползунок
    0..100 рисовался как sound?100:0, громкости в тракте не существовало
    (master жёстко 0.5), персиста не было вовсе. Теперь soundVol 0..1 в
    mixer_sound (хранится ПРОЦЕНТАМИ '40'), единая точка applySoundVol,
    Sound.setVolume в тракте; базовый уровень 0.5 при 100% цел бит-в-бит,
    внешний мьют рекламы СИЛЬНЕЕ ползунка. Их наводка-опровержение: старый
    #soundToggle в мёртвой карточке состояния НЕ держал — уборке Museum звук
    не мешает. Два своих дефекта поймали сами (возврат 100 вместо последних
    40; рассинхрон тумблера с ползунком).
  • ⚠️ ЩЕЛЬ, НАЙДЕННАЯ НА МЕРЖЕ (диспетчер): master создаётся ЛЕНИВО по
    первому жесту с ХАРДКОДОМ 0.5, а восстановление из localStorage
    отрабатывает раньше, когда master ещё null — восстановленные 40% ИГРАЛИ НА
    ПОЛНОЙ до первого касания ползунка. Их метрика eng ловила playerVol, а не
    настоящий гейн. Фикс: applyGain() в ensure; диагностика Sound.gain();
    страж холодного старта (свежая страница, mixer_sound='40', первый жест →
    гейн 0.2). ⚠️ Первый прогон стража завалил Я СВОИМ входом: подложил '0.4',
    а формат хранения — ПРОЦЕНТЫ; страж честно наказал и меня.

- v1-test-183 · 2026-07-30 — Merge ИНТЕРФЕЙС: ПОРТРЕТЫ ХОЛОДНЫХ ПАЧЕК (жалоба
  владельца «где превью у всех новых объектов?»). На main пустовало 29 карточек
  из 122 — все пачки Kenney: атлас пачки, которой нет в партии, не декодирован
  к снимку, прозрачная пустышка кэшировалась навсегда. Теперь itemThumb ждёт
  декода, коллекция добирает опоздавших на месте; страж «живая картинка у всех
  карточек» проверен на способность падать (на main падал). Канон портретов
  дополнен: «после интро» мало, холодной бывает ПАЧКА. 274 PASS.
  ⚠️ Оргвывод принят: при сдаче направлению впредь присылаю ХЕШ актуального
  main — Интерфейс начал на v176 и потерял время на пересоздание ветки.

- v1-test-182 · 2026-07-30 — ДИСПЕТЧЕР: ПЕРЕМЕШИВАНИЕ TYPES + «FOREVER FOR
  $4.90» + ЦЕНА ИЗ КАТАЛОГА. Три спеки владельца («перемешивай типы, баннер не
  заводим, поправь текст на отключении рекламы, проверь цену из бриджа»).
  273 PASS.
  • ПЕРЕМЕШИВАНИЕ: партия Kenney равномерно вмешана в прогрессию (~каждый
    4-й тип), первые новые видны с ~ур.5. Инварианты раскладки: база 0..8 не
    тронута; стейк на 9; РЫБА сдвинута на индекс 29 (ур.22 — не в первые
    уровни, возражение Графики о путанице с кладом учтено); ПОНЧИК остался
    поздним (индекс 119, ур.112 — вперёд не двигать до починки коллайдера);
    forestplant — сентинел, намеренно последний. Всё под стражами (3 новых).
  • БАННЕРНАЯ РЕКЛАМА: решение владельца — НЕ ЗАВОДИМ. Вопрос закрыт.
  • КНОПКА: «Subscribe $1.99» → «Forever for $4.90» (разовая покупка
    noads_forever, подписок в вебе нет). $4.90 в разметке — фолбэк; живую цену
    в валюте игрока подтягивает 78-ads из bridge.payments.getCatalog (формат
    каталога проверен по бандлу SDK: id/price/priceValue/priceCurrencyCode).
    ⚠️ Вставка диспетчера в зону ИНТЕГРАЦИИ — ревьюить при подключении
    payments в понедельник.

- v1-test-181 · 2026-07-30 — Merge ГРАФИКА: ВЫБОР ТИПОВ ФИШЕР-ЙЕТСОМ (прямое
  слово владельца «поменяй строку выбора типов, чтобы всё ожило»). 270 PASS.
  ⚠️ РАЗВЯЗКА ДВУХ ПАРАЛЛЕЛЬНЫХ ФИКСОВ: ту же строку диспетчер уже чинил в
  v178 (детерминированная развёртка). Победила версия Графики — случайные 90
  из открытых на каждую раскладку (разнообразие поздней игры даром) против
  моего одинакового поднабора. Кривая 1..82 не тронута в обеих.
  ⚠️ Стражи хвоста переведены на объединение по регенам (случайная выборка:
  одиночный реген флейкал бы ~26%).
  ⚠️ РЕГРЕССИЯ, ВНЕСЁННАЯ ОЖИВЛЕНИЕМ (Графика честно назвала её сама): ПОНЧИК
  снова в игре с ~84 ур., а его дырка convex hull НЕ закрыта — прежнее
  «закрытие» держалось на том, что он не спавнился. Решение диспетчера на
  неделю запуска: оставить поздним (мало кого коснётся), компаунд-коллайдер —
  задача ФИЗИКЕ после запуска. При будущем перемешивании типов пончик ВПЕРЁД
  НЕ ДВИГАТЬ, пока коллайдер не починен.

- v1-test-180 · 2026-07-30 — Merge ГРАФИКА: +survivalfish (спека владельца
  «добавь рыбу, а то объектов слишком мало» — поверх возражения Графики о
  путанице с золотой рыбкой-сюрпризом, решение владельца зафиксировано в коде).
  TYPES = 122. 270 PASS.
  ⚠️ Рыба, как и остальные 28 новых, стоит в ХВОСТЕ (открытие с ур.86+) —
  видимого владельцу эффекта НЕТ, пока не решён вопрос о перемешивании типов.
  Вопрос раскрыт владельцу, ждёт его слова. Замечание Графики про мёртвую
  формулу спавна устарело — починено в v178, но про хвост они правы по сути.

- v1-test-179 · 2026-07-30 — ДИСПЕТЧЕР: СТЕЙК В ИГРЕ СО 2-ГО УРОВНЯ + Merge
  ГРАФИКА (28 типов Kenney, TYPES 93 → 121). 270 PASS.
  • Стейк уведён с индекса 92 на 9: был «жив формально» (открытие с 85-го
    уровня), теперь в куче со 2-го, 4/4 раскладок. Сейвы целы — ac/bo/uk
    ключуются именами, проверено ДО правки.
  • Мой вчерашний страж «на ур.20 хвостовых нет» честно упал (стейк перестал
    быть хвостовым) — сторожа переписаны: стейк со 2-го, хвост достижим на
    ур.113, прогрессия цела, лесенка встрясок.
  • ⚠️ ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦУ: новые 28 типов открываются с ур.86-113 —
    та же болезнь, что была у стейка. Перемешать ближе к началу?
  • Вес: 8.09 → 9.03 МБ raw. Запас до лимита зипа ~1.2 МБ.

- v1-test-178 · 2026-07-30 — ДИСПЕТЧЕР: ИМЯ BLENDO + ХВОСТ TYPES + ЛЕСЕНКА
  ВСТРЯСОК. Три спеки владельца одним релизом. 269 PASS.
  • ИМЯ: `<title>` Mixer → BLENDO (решение владельца, подтверждено им лично;
    листинг площадок Повествования уже написан под это имя).
  • ХВОСТ TYPES: спавн раздаёт типы `i % typesCount` только пока типов не
    больше пар; при typesCount > pairsCnt — равномерная развёртка. Ур.1-81 не
    изменились бит-в-бит. Стейк в куче 6/6 на ур.85/90/93.
    ⚠️ Я дважды ошибся по дороге и оба раза записал это в код: (1) мерил на
    ур.84, где стейка нет ПО ПРОГРЕССИИ (typesCount = 8 + уровень), и решил,
    что формула не работает; (2) сочинил объяснение «последний тип срезает
    трим» и сделал под него перевёрнутую развёртку — A/B показал 17/18 против
    17/18, разницы нет, развёртка убрана, ложный комментарий переписан.
  • ВСТРЯСКИ: лесенка 3 + ⌊ур/6⌋, кап 8 (было флэт 3). ⚠️ ЭТО РАЗВОРОТ РЕШЕНИЯ
    ВЛАДЕЛЬЦА ОТ 27.07 («продаём их за рекламу»), сделанный им осознанно после
    того, как я назвал цену. Старый страж «лесенки нет» честно упал и обновлён
    под новую спеку — экономика встрясок не должна меняться молча.
  • Новые сторожа: хвост достижим на ур.85, прогрессия цела на ур.20, лесенка
    3/6/8. Ассерт хвоста проверен на способность падать.

- v1-test-177 · 2026-07-30 — Merge ИНТЕГРАЦИЯ + ДИСПЕТЧЕР: ЗАНАВЕС ПЛОЩАДКИ И
  ТРЕТЬЯ ТОЧКА GAME_READY. Жалоба владельца «пропала анимация заполнения
  корзины». Полный разбор — CLAUDE.md, раздел «Занавес площадки». Кратко:
  анимация играла под непрозрачным сплэшем площадки; интро получило фазу
  ожидания, GAME_READY переехал в третью точку, попутно закрыт ЧЁРНЫЙ ЭКРАН
  НАВСЕГДА при незарезолвившемся initialize(). 266 PASS.
  ⚠️ Мой вывод «SDK не реагирует на GAME_READY» был НЕВЕРЕН — поправила
  ИНТЕГРАЦИЯ: реагирует синхронно, в том прогоне его опередил автотаймер SDK.
  ⚠️ Страж GAME_READY в сьюте переписан: прежний не мог отличить запрещённую
  отправку над чёрным экраном от новой штатной над нарисованной чашей.

- v1-test-176 · 2026-07-30 — ДИСПЕТЧЕР: МЕЖСТРАНИЧНАЯ РАЗ В 3 УРОВНЯ.
  Спека владельца «раз в 3 уровня» (было 5, спека 2026-07-23). INTER_EVERY_LEVELS
  5 → 3. На первых 20 уровнях 6 роликов вместо 4.
  ⚠️ ПОЧЕМУ УПЛОТНИЛИ: разбор показал, что покупка «без рекламы навсегда» за
  $4.90 почти ничего не отключает — БАННЕРОВ В ИГРЕ НЕТ ВОВСЕ (ни вызова, ни
  плейсмента в конфиге), а межстраничная раз в 5 уровней это один ролик на
  полчаса. Владелец выбрал поднять плотность. ⚠️ ПРО БАННЕР РЕШЕНИЯ НЕ БЫЛО —
  спрошено и не отвечено, по своей инициативе не заводить.
  ⚠️ КАДЕНЦИЮ СТОРОЖАТ 4 АССЕРТА, и они честно упали на правке (я перед этим
  ошибочно сказал, что число не покрыто — первый греп их не нашёл). Блок
  переведён на ЕДИНСТВЕННЫЙ источник числа `INTER_EVERY` в test.js: это
  НАМЕРЕННЫЙ двойник конфига, читать значение из игры НЕЛЬЗЯ — ассерт стал бы
  тавтологией и проходил при любой каденции. Меняется спека — правится одна
  строка теста. 263 PASS, exit 0.

- v1-test-175 · 2026-07-30 — ДИСПЕТЧЕР: ЦЕНЫ БЕЗ ДЕВЯТОК. Спека владельца
  «цены везде без последних 9 центов, т.е. 4.90, 9.90, 19.90». Правка в ДВУХ
  местах обязательна: `usd` в STAR_BUNDLES (00-config) И зашитый текст кнопок
  `Upgrade $N` (shell.html) — каталог площадки цену НЕ подставляет.
  ⚠️ Ассерт цен в сьюте на этом и упал (пиновал 4.99 по макету) — обновлён, и
  к нему ДОБАВЛЕН сторож текста кнопок: теперь расхождение «в конфиге одна
  цена, на кнопке другая» ловится тестом, а не игроком. 263 PASS.

- v1-test-174 · 2026-07-30 — Merge ИНТЕРФЕЙС: ЗАНАВЕС НАД HUD НА ЗАГРУЗКЕ.
  Спека владельца «во время загрузки бриджа не должно быть никаких элементов
  интерфейса, они появляются плавно сразу после». Причина (замер диспетчера):
  сплэш площадки #loading-overlay имеет z-index 1, а наши бары 5 и #face 6 —
  HUD пробивал чужой сплэш насквозь.
  ⚠️ ГЕЙТ — ЗАЩЁЛКА `html.uiready`, А НЕ `introdone`: мой первоначальный гейт
  был НЕВЕРЕН, `startIntro` снимает introdone в начале КАЖДОГО уровня, и HUD
  гас бы на каждом интро. Поймал ИНТЕРФЕЙС.
  ⚠️ Ввод гасится `visibility:hidden`, а НЕ `pointer-events:none`: у кнопок
  внутри баров стоит `pointer-events:auto`, родительский none его не перебивает.
  #fatal из занавеса исключён сознательно — экран ошибки обязан быть виден.
  Страховка 8 с. 262 PASS (+3 сторожа, проверены на регрессию).
  ⚠️ СПЛЭШ ВОСПРОИЗВОДИТСЯ ТОЛЬКО ПРИ ФОРСИРОВАННОЙ ПЛОЩАДКЕ: на mock его нет
  вовсе. Диспетчер видел его из-за gitignored-стенда
  `playgama-bridge-config.local.json` с `forciblySetPlatformId: playgama`;
  ИНТЕРФЕЙС в чистом клоне не видел и честно об этом сказал. Оба наблюдения
  верны — расхождение объясняется этим файлом, а не ошибкой замера.

- v1-test-173 · 2026-07-30 — ДИСПЕТЧЕР: ОТПРАВКА В ЛИДЕРБОРД ВЫКЛЮЧЕНА.
  Решение владельца: «пока мы не будем дописывать этот функционал в рамках
  моего эксперимента». `LEADERBOARD_ID=''` (00-config) — штатный выключатель
  Интеграции; код, конфиг и все 259 ассертов целы, включить = одна строка.
  ⚠️ ПОЧЕМУ НЕ ОСТАВИЛИ ВКЛЮЧЁННЫМ «пусть копится»: живой замер (дважды, на
  двух площадках) показал, что сервер хранит МАКСИМУМ за всё время, а модель
  владельца — текущее состояние, которое умеет ПАДАТЬ. Копить под чужой
  семантикой = набить борд пиковыми значениями, а удаления в SDK нет: при
  возврате к задаче борд всё равно пришлось бы пересоздавать. Проверка боем:
  `__ads.lbAccepted === null`. Вопросы к площадке (режим хранения последнего
  результата; легальность недокументированной секции `saas`) переданы владельцу
  — он сотрудник Playgama и спрашивает внутри, публичную поддержку не искать.

- v1-test-172 · 2026-07-29 — Merge ИНТЕГРАЦИЯ: РАЗБОР ОТВЕТА ЛИДЕРБОРДА.
  Сьют 257 → **259 PASS**.
  • `accepted = (res.score === отправленное)` наружу как `Ads.lbAccepted` —
    единственный честный признак: и принятая, и ПРОИГНОРИРОВАННАЯ запись
    отдают 201 и `scoreAttemptStatus:'normal'`, то есть поле, созданное будто
    бы ровно для этого, не сообщает ничего.
  ⚠️ В коде оговорено, что `accepted===false` — НЕ ошибка, а штатное «ниже
    личного пика». Иначе следующий увидит поток «неуспехов» и полезет чинить
    здоровое.
  ⚠️⚠️ МОК В СЬЮТЕ ПЕРЕДЕЛАН ПОД РЕАЛЬНУЮ СЕМАНТИКУ — и это тот же урок, что
    был с ассертом музыки. Раньше мок отдавал эхо `{ok:true}`, на нём разбор
    НЕЛЬЗЯ было проверить в принципе: ассерт проходил бы всегда. Теперь мок
    хранит максимум и возвращает СОХРАНЁННЫЙ счёт, как настоящий сервер.
    Зубы: с наивным «резолв = успех» ассерт «ниже пика распознан» падает.
  ⚠️ ОШИБКА ПОСТАНОВКИ, КОТОРУЮ ИНТЕГРАЦИЯ ПОЙМАЛА САМА: первый вариант теста
    поднимал счёт через `starGrant`, а тот пишет в ПОПОЛНЕНИЯ (`tu`), которые
    ранг не двигают. То есть их тест лидерборда споткнулся о НАШУ ЖЕ защиту
    «купленное не поднимает место». Правильная ручка — `bankScore` (`se`).
    Хорошая новость в том, что защита работает даже против того, кто её
    тестирует.
  • Контракт закрыт: все прежние `[?]` заменены живыми данными (формы обоих
    ответов, семантика максимума, приём гостя сервером, отсутствие лимита
    частоты на трёх подряд), след в боевой таблице записан отдельно.
  ⚠️ ЖДЁТ ВЛАДЕЛЬЦА: «лучший против последнего». Работа ИНТЕГРАЦИИ от его
  выбора НЕ зависит — отправка и разбор одинаковы при любом; меняется смысл
  фичи и, возможно, потребность в своём сервере.

- v1-test-171 · 2026-07-29 — Merge ИНТЕГРАЦИЯ: ОТПРАВКА СЧЁТА В ЛИДЕРБОРД
  («кусок 1», без экрана). Сьют 250 → **257 PASS**.
  • ТОЧКА ОТПРАВКИ НАЙДЕНА БЕЗ ПРАВКИ ЧУЖОЙ ЗОНЫ: `Ads.noteWin()` уже зовётся
    ровно раз за победу и СТРОГО ПОСЛЕ `bankLevelScore` — счёт учтён. В
    80-gameplay/99-main не добавлено ни строки.
  • ТРИ ПРЕДУСЛОВИЯ: id борда задан → тип не `not_available` → игрок
    АВТОРИЗОВАН. Не выполнено — молча выходим, без тостов и консоли.
  ⚠️ ПОПРАВКА ИНТЕГРАЦИИ К МОЕЙ ПОСТАНОВКЕ: я просил проверять «токен в
    конфиге есть» — ИЗ JS ЭТО НЕВОЗМОЖНО, публичного доступа к значениям
    конфига у Bridge нет. Сделан свой выключатель `LEADERBOARD_ID` (пусто =
    отправка выключена). Практически равнозначно (id и токен вносятся
    одновременно), но называть это «проверкой токена» было бы неправдой.
  • ГЕЙТ ГОСТЕЙ (решение владельца) СТРОЖЕ SDK и проверен РЕВЕРСОМ: сняли
    строку авторизации — ассерт упал, гость ушёл в таблицу. Страж настоящий.
  • РЕЗОЛВ НЕ СЧИТАЕТСЯ УСПЕХОМ: сырой ответ лежит в `Ads.lbRaw`, разбор
    помечен `[?]` до живого прогона. В `catch` НЕ пишем «не сохранилось» —
    туда же падает успешная запись с пустым телом; просто разрешаем повтор.
  • Конфиг заведён по скриншоту кабинета: `saas.publicToken` (подписан там
    «Your APP Public Token» — публичный по устройству, едет на портал),
    борд `Blendo` с `isMain`, `platforms` перечислены явно.
  ⚠️ ОСТАЛОСЬ НЕИЗВЕСТНЫМ И ЭТО ГЛАВНОЕ: хранит ли сервер ЛУЧШИЙ результат или
  ПОСЛЕДНИЙ. В кабинете стоит «Score order: Higher is better», но это про
  СОРТИРОВКУ, а не про хранение. Если хранит лучший — утверждённая владельцем
  модель «потратил и упал» не работает ВООБЩЕ. Проверяется чтением таблицы
  после двух отправок по убыванию.

- v1-test-170 · 2026-07-29 — Merge ИНТЕГРАЦИЯ: BRIDGE v2 ПО-ВЗРОСЛОМУ (пауза
  площадки, музыка под рекламой, GAME_READY, LEVEL_*, плейсменты, конфиг).
  Сьют 236 → **250 PASS** (+14 ассертов).
  • ПАУЗА ПЛОЩАДКИ (`PAUSE_STATE_CHANGED` + стартовое `isPaused`) — третий
    владелец паузы, свой флаг, снимает только свою. Закрывает реальную порчу
    прогресса: под оверлеем площадки у нас тикал миксер и ЖРАЛ предметы.
  ⚠️ ИНТЕГРАЦИЯ САМА ПОЙМАЛА СВОЮ ЖЕ ГРАБЛЮ СВОИМ ЖЕ АССЕРТОМ: сперва
    переиспользовала общий `mutedByPlatform`, и снятие паузы НЕ возвращало звук —
    флаг принадлежит другому событию. Правило «один флаг = один владелец»
    теперь распространено и на мьют, не только на паузу.
  • МУЗЫКА ПОД РЕКЛАМОЙ ГЛОХНЕТ (`musicSuspend` в 85-hud, заход в чужую зону
    авторизован мной). Внешняя приглушка СИЛЬНЕЕ ползунка игрока — иначе
    двинувший громкость под роликом заводил бы трек поверх рекламы.
  ⚠️ Побочно нашли блип в своём же коде: `showRewarded` звал полный `cancel()`
    (снимает мьют) и через строку ставил обратно — музыка успевала заиграть
    ровно в момент старта ролика.
  • GAME_READY по ПЕРВОМУ ИГРАБЕЛЬНОМУ КАДРУ, а не в init (дока прямо так и
    говорит). Идемпотентен, латч досылает, промис ловится.
  • LEVEL_STARTED/COMPLETED/PAUSED/RESUMED через `Ads.msg` — ядро о bridge не
    знает. LEVEL_FAILED не шлём: поражения в игре нет.
  • PLACEMENT в оба показа + гвард `isInterstitialSupported` СТРОГО ДО сброса
    `Save.iw` — иначе накопленное окно из 5 побед скручивалось там, где ролика
    не будет никогда.
  ⚠️⚠️ УРОК ПРО ЗУБЫ АССЕРТА, ШИРЕ НАШЕГО ПРАВИЛА (находка ИНТЕГРАЦИИ). Первая
  версия ассерта музыки проходила и С фиксом, и БЕЗ него: в headless автоплей
  запрещён, трек не играет вовсе, и «под роликом остановлен» выполнялось само
  собой. То есть СРЕДА МОЛЧА ПРЕВРАТИЛА АССЕРТ В ТАВТОЛОГИЮ. Переписан на счёт
  вызовов `play()`. Мораль: реверс проверяет не только «ловит ли тест
  регрессию», но и «может ли инструмент вообще это измерить».
  ⚠️ РЕШЕНИЕ ДИСПЕТЧЕРА ПО СТЕНДУ: `forciblySetPlatformId` в боевой конфиг НЕ
  кладём — ИНТЕГРАЦИЯ права, уехав в релиз он притворялся бы чужой площадкой у
  живых игроков. Заведён `playgama-bridge-config.local.json` и внесён в
  .gitignore: закоммитить его НЕЛЬЗЯ ФИЗИЧЕСКИ, а не по договорённости (тот же
  принцип, что у `.gitattributes` с собранным файлом). Для стенда — подменить им
  боевой конфиг локально.
  ФАКТЫ: SDK v2.0.0 актуален, обновляться не надо; плагин Playgama для Claude
  написан под v1 и к v2 неприменим; у SDK свой троттл межстраничной 60 с поверх
  нашего счётчика; `getServerTime()` работает — давний блокер «нет серверного
  времени» СНЯТ.
  НЕ ДЕЛАЛОСЬ: payments/подписка, лидерборды, локализация, развод ключа сейва
  (записан как мина), `Save.lv` в облако.

- v1-test-169 · 2026-07-29 — Merge ГРАФИКА: ТОН ВУАЛИ ПО ЗАМЕРУ + ПОЧИНЕНЫ
  ГХОСТ-ПОРТРЕТЫ (регрессия моя) + цена прозрачности НАКОНЕЦ ИЗМЕРЕНА.
  • ТОН `0x6f9fd8` + `lift 0.35` вместо моих `0x9ec2f0` + `0.55`. ⚠️ СПОР РЕШИЛ
    ЗАМЕР КОНТРАСТА, а не вкус: мой вариант давал контраст кучи к небу 0.169
    против 0.314 у сцены БЕЗ вуали — то есть я утягивал предметы в фон, ТА ЖЕ
    болезнь, что убила прозрачность, просто слабее и потому незамеченная.
    Их вариант — 0.359, выше чем вообще без вуали.
  ⚠️ ПРАВИЛО НА БУДУЩЕЕ (в CLAUDE.md): РЕШАЕТ LIFT, А НЕ ОТТЕНОК. На одном
    тоне lift 0.55 → 0.169, lift 0.25 → 0.278. Кто будет крутить цвет — сперва
    трогай lift, оттенок потом.
  • ⛔ ЦЕНА ПРОЗРАЧНОСТИ ИЗМЕРЕНА И МАЛА — мой прежний вывод «измерить не
    удалось» СНИМАЮ, и прежний довод ГРАФИКИ «цена платится за всех и всегда»
    они сняли сами. Правильная методика — ПАРНЫЙ ЧЕРЕДУЮЩИЙСЯ замер ВНУТРИ
    ОДНОГО процесса на одной спящей куче (мой A/B сравнивал РАЗНЫЕ запуски
    SwiftShader и тонул в дрейфе машины). Итог: 1.5 мс (3.2%) при знаковом
    тесте 5 из 8 — монета, эффекта нет. ⚠️ ЗНАЧИТ ОТКАТ ПРОЗРАЧНОСТИ ОСТАЁТСЯ
    ПО ВИДУ, А НЕ ПО ПЕРФУ: сквозь плотную кучу видно дно чаши, тоном не
    лечится. Захочет владелец вернуть — это разговор про картинку.
  • ⚠️ ГХОСТ-ПОРТРЕТЫ КОЛЛЕКЦИИ — МОЯ РЕГРЕССИЯ ИЗ v165: тон вуали молча красил
    силуэты закрытых типов в синеву (замер b−r = +80) при спеке владельца
    «прозрачные, матовые, но БЕСЦВЕТНЫЕ». Я поменял тон и не проверил ВТОРОГО
    потребителя той же юниформы. Вылечено: на время съёмки гхоста тон
    возвращается в нейтраль. После: rgb(191,191,191), разброс каналов 0.
    Правило «у uVeil ДВА потребителя с ПРОТИВОПОЛОЖНЫМИ требованиями — меняешь
    тон, проверь гхост» записано в CLAUDE.md со способом проверки.
  • Мгновенное переключение сложности возможно (флип 34 мс однажды, дальше
    1.2-1.6 мс), но вопрос СПЯЩИЙ при `desat` — оживёт только при возврате fade.
  ⚠️ ПЕРВЫЙ МЕРЖ С НОВОЙ ЗАЩИТОЙ СОБРАННОГО ФАЙЛА: git честно объявил конфликт
  в index.html вместо тихой склейки, разрешение — `python3 build.py`. Работает
  ровно как задумано.
  ⚠️ ГРАФИКА СООБЩИЛА О ВТОРОМ, НЕЗАВИСИМОМ ФЛЕЙКЕ на чистой базе: ассерт
  «эндшпиль <=8 живых снимает радиус (∞), даже поверх цепи». Лог у них,
  запросил. НЕ смешивать с гонкой штиля — это другая секция.
  Сьют 236 PASS.

- v1-test-168 · 2026-07-29 — Merge ИНТЕРФЕЙС: БЛОК НАСТРОЕК ПО МАКЕТУ 812:1115
  («элементы из этого блока умеют тянуться») + ⚠️ ЗАКРЫТА МИНА С СОБРАННЫМ
  ФАЙЛОМ В МЕРЖАХ.
  • Панель настроек переведена с флекс-колонки на СЕТКУ: ярлыки `max-content`,
    контролы `minmax(0,1fr)`, строки растворены через `display:contents`.
    ⚠️ ПОЧЕМУ НЕ `flex:1` НА КОНТРОЛЕ: в макете все три контрола ровно 500 при
    РАЗНЫХ ярлыках (68/63/84) — значит они одной ширины и выровнены по одной
    вертикали; с flex ширина зависела бы от длины ярлыка и края разъехались бы.
  • Пара костылей `--ms-ctl` 172/156 СНЯТА. Для истории, откуда они брались:
    172 — размер РАСТРОВОГО узла 763:1428 (вектора в нём нет, это была
    глазомерная прикидка), 156 — заплатка под узкую бенто-колонку.
  • Числа макета: трек 12→17, бегунок 26→30, свитчер высотой 48. Старое правило
    «ползунок не длиннее свитчера» НЕ нарушено — в макете обе ширины 500.
  ⚠️ ГРАБЛЯ ИНТЕРФЕЙСА, ЗАПИСАНА У НИХ: `getComputedStyle` на webkit-
  псевдоэлементах ползунка ВРЁТ — отдаёт высоту хоста и треку, и бегунку.
  Мерить пикселями.
  Замер (мой, на смерженной сборке): 393 и 1440 — левые края всех контролов
  совпадают (132), ширины одинаковы внутри вьюпорта (225 / 156), панель 184 по
  высоте как в макете. Сьют 236 PASS.
  ⚠️⚠️ ГЛАВНОЕ ИЗ ЭТОЙ СДАЧИ — НЕ МАКЕТ, А МИНА: `index.html` это СОБРАННЫЙ
  файл, и git сливал его как обычный текст. Мерж двух веток мог дать
  согласованно выглядящий, но НЕСОГЛАСОВАННЫЙ билд — куски двух разных сборок,
  без конфликта и без предупреждения. ИНТЕРФЕЙС это заметил и проверил сам
  (в тот раз автомерж случайно совпал с честной пересборкой бит-в-бит).
  ⚠️ ЗАКРЫТО СТРУКТУРНО, А НЕ ДИСЦИПЛИНОЙ: `.gitattributes` с `index.html
  -merge` — git теперь ОБЪЯВЛЯЕТ КОНФЛИКТ вместо тихой склейки, а разрешение
  всегда одно: `python3 build.py`. Правило «не забывать пересобирать» работало
  бы ровно до первого занятого дня.

- v1-test-167 · 2026-07-29 — 🔴 ЗАКРЫТА ВЫДАЧА ПЛАТНЫХ БАНДЛОВ БЕСПЛАТНО
  (репорт ИНТЕГРАЦИИ; дыра МОЯ, признаю целиком).
  ⚠️ ИСТОРИЯ В ДВЕ СТУПЕНИ, и вторая хуже первой: сперва кнопка покупки не
  работала вовсе (слала ЧИСЛО 5 вместо строкового 'bundle5'), в v163 я это
  починил — и она стала НАЧИСЛЯТЬ бандл без всякой оплаты, при живых ценниках
  $4.99/$9.99/$19.99, на сборке, которая раздаётся с Pages. Платёжного шлюза в
  проекте нет вообще: `bridge.payments` не используется ни разу (grep — ноль).
  То есть любой тестер жал «$19.99» и получал товар даром.
  ⚠️ Я ЖЕ САМ ЗАПИСАЛ ЭТО В docs/LAUNCH-PLAN.md («даже после починки кнопки
  платежей нет — это выдача товара без оплаты») И НЕ ЗАКРЫЛ. Записать риск и
  оставить его жить — то же самое, что не заметить.
  ЛЕЧЕНИЕ: выдача бандла осталась ТОЛЬКО в DEV (file://, localhost, ?dev=1) —
  это заодно закрывает просьбу владельца «нужна эмуляция этого режима»,
  чтобы смотреть бустер на экране; в бою — честное «Coming soon».
  ⚠️ СНИМАТЬ ГЕЙТ ТОЛЬКО ВМЕСТЕ С ВВОДОМ bridge.payments, не раньше.
  Проверка обоих режимов отдельным прогоном: DEV — товар выдан; бой — товар НЕ
  выдан, всплывает «Coming soon». Сьют 236 PASS.

- v1-test-166 · 2026-07-29 — ТАП ПО ДОСТУПНОМУ ПРЕДМЕТУ БЕЗ ПАРЫ БОЛЬШЕ НЕ
  ШТРАФУЕТСЯ (спека владельца: «убери штраф за тап по доступному без пары»).
  ⚠️ ПОВОД — ЖАЛОБА СО СКРИНОМ: «почему внизу чаши есть доступные объекты? потыкал
  на них, они не соединяются». ЗАМЕР ОБЪЯСНИЛ ПОЧЕМУ: ур.20 Hard — доступных
  предметов 50, доступных ПАР 11. То есть больше половины «цветных» соединить
  не с чем В ПРИНЦИПЕ. Вуаль отвечает на вопрос «ДОТЯНУСЬ ЛИ» (луч в небо), а
  игрок читает её как «МОГУ ЛИ ИСПОЛЬЗОВАТЬ» — множества разные, разрыв большой.
  Предмет у дна с краю видит небо через щель между кучей и стенкой: формально
  открыт, одинакового соседа рядом нет.
  ⚠️ ПОЧЕМУ НЕ СТАЛИ КРАСИТЬ ПО «ЕСТЬ ПАРА» (очевидное решение, отвергнуто):
  игра превратилась бы в «жми по яркому» — поиск пары, то есть сам геймплей,
  исчезает; плюс набор менялся бы после каждого матча и после каждого тика
  радиуса, то есть вернулось бы мигание вуали (проект уже лечил его tickVeil,
  для контроля есть accFlips).
  ЧТО СНЯТО: очки, засчитывание промаха (stats.misses), сброс набора турбо и
  падение радиус-лесенки — всё это делал penalize, а раз тап не ошибка, то и
  наказывать не за что. Обратная связь осталась: красный ореол + дрожание.
  ЧТО СОХРАНЕНО: штраф за пустое место, за ПЕРЕКРЫТЫЙ предмет (он завуалирован,
  игрок это видит) и двойной штраф за камень.
  ⚠️ stats.lastAction НАМЕРЕННО НЕ ТРОГАЕМ: иначе тапами по одинокому предмету
  можно было бы бесконечно откладывать помол — поблажка превратилась бы в дыру.
  Телеметрия: отдельный исход `nopair` вместо `miss` — в карте промахов это
  другой случай, и смешивать их значит потерять оба сигнала.
  ⚠️ Я ПОВТОРНО НАСТУПИЛ НА ЗАДОКУМЕНТИРОВАННУЮ ГРАБЛЮ: поставил новую секцию
  сьюта В СЕРЕДИНУ, а она делает setLevel/regen — сломались «5 матчей сняли
  >=10 предметов» (живых 172 вместо 130) и секция бомбы. Ровно об этом проект
  предупреждает у секции камней («в конце файла НАМЕРЕННО»). Перенёс в конец,
  236 PASS.

- v1-test-165 · 2026-07-29 — ВУАЛЬ НЕДОСТУПНЫХ СТАЛА СВЕТЛО-СИНЕЙ, ПРОЗРАЧНОСТЬ
  ОТКАЧЕНА (спека владельца: «если прозрачность дорого, то нужна просто светло
  синяя matcap, текущий серый выглядит не секси»).
  • Цвет: `VEIL_TINT = 0x9ec2f0` в ЛИНЕЙНОМ пространстве (патч правит
    outgoingLight ДО тонмаппинга); шейдер красит обесцвеченную яркость в этот
    тон и поднимает к нему же: `mix(vec3(vLum)*uVeilCol, uVeilCol*x, y)` при
    x=0.95, y=0.55. Алгебраически это `col*(0.45·vLum + 0.5225)` — чистый
    синий с сохранённой светлотой. При uVeil=0 выражение по-прежнему
    тождество: доступные не платят ничего.
  • ⛔ ПРОЗРАЧНОСТЬ (v164) ОТКАЧЕНА В ТОТ ЖЕ ДЕНЬ. Причина НЕ перф (его так и
    не измерили — разброс headless больше разницы), а вид: светло-синяя вуаль
    ПОВЕРХ прозрачности на светло-синем небе = предметы исчезают. На кадре
    ур.20 Hard из 181 осталось видно пару штук, сквозь завуалированные стало
    видно дно чаши. Я сам написал предупреждение «не брать цвет неба» и сам же
    в него шагнул, соединив тон с альфой.
  ⚠️ ГРАБЛЯ, СТОИВШАЯ ДВУХ ПРОГОНОВ: комментарий поставил ПОСЛЕ `+` в склейке
  шейдера. `X + // …` это `X + (+'строка')`, унарный плюс на строке даёт NaN, и
  в GLSL уезжает «NaN» — предметы просто перестают рисоваться, КОНСОЛЬ МОЛЧИТ.
  Комментарий в конкатенации строк обязан стоять НАД плюсом. Записано в коде.

- v1-test-164 · 2026-07-29 — НЕДОСТУПНЫЕ В HARD СТАЛИ ПРОЗРАЧНЫМИ, КАК В МУЗЕЕ
  (спека владельца со скрином). `VEIL_MODE` 'desat' → 'fade', `VEIL_ALPHA`
  0.2 → 0.42 = ровно GHOST_ALPHA гхост-карточек музея («как в музее» дословно).
  ⚠️ РЕЖИМ БЫЛ РЕАЛИЗОВАН ГРАФИКОЙ И СОЗНАТЕЛЬНО НЕ ВКЛЮЧЁН. Прочитал их
  обоснование прежде, чем щёлкать флаг; из двух причин:
  • ⛔ «визуально не даёт ничего — недоступные и так закрыты от камеры верхним
    слоем» ОПРОВЕРГНУТО СКРИНОМ ВЛАДЕЛЬЦА: доступность считается лучами В НЕБО,
    а камера смотрит СБОКУ, и на его кадре крупная серая масса переднего плана
    ничем не закрыта. Верно для вида сверху, неверно для боковых ракурсов.
  • ✅ «цена платится за ВСЕХ и ВСЕГДА (флаг material.transparent уводит в
    прозрачную очередь и доступные, теряя ранний Z)» — причина настоящая, и она
    ОБОЙДЕНА: флаг теперь ставится ТОЛЬКО В HARD. В Easy вуали нет вовсе
    (isAccessible отдаёт true всем) — платить не за что. Прежняя реализация
    платила и в Easy, то есть дефолтный режим стал ДЕШЕВЛЕ, чем был.
  ⚠️ ЦЕНУ ПРОЗРАЧНОСТИ В HARD ИЗМЕРИТЬ НЕ УДАЛОСЬ, и я не выдаю это за «бесплатно».
  A/B в одинаковом Hard (ур.20, 181 предмет, 393×852, 3 прогона): fade
  60.9/60.9/81.4, desat 60.2/78.8/79 — разброс headless (~20 мс) БОЛЬШЕ искомой
  разницы. Плюс собственное предупреждение ГРАФИКИ: headless — софтверный
  растеризатор, а прозрачность дорога именно тем, чего у него нет (тайловые GPU
  отбрасывают перекрытую НЕПРОЗРАЧНУЮ геометрию). Настоящий замер — только на
  устройстве владельца, и он же решит, оставлять ли.
  ⚠️ ПЕРЕКЛЮЧЕНИЕ СЛОЖНОСТИ ПРИМЕНЯЕТСЯ СО СЛЕДУЮЩЕГО УРОВНЯ: applyHard
  материалы не пересобирает, а менять флаг на лету = перекомпиляция шейдера
  каждому предмету.

- v1-test-163 · 2026-07-29 — БЛОКЕРЫ ЗАПУСКА, ЧАСТЬ 1 (спека владельца «начинай
  с блокеров»). Четыре из шести; музыку владелец заменяет сам, материалы витрины
  за ним же.
  • 🔴 КНОПКА ПОКУПКИ НЕ РАБОТАЛА ВООБЩЕ. `+btn.dataset.tier` слал в buyBundle
    ЧИСЛО 5, а бандлы зовутся 'bundle5' — товар не находился, функция тихо
    возвращала отказ, и его никто не читал. Игрок жал «Upgrade $4.99» и получал
    НИЧЕГО: ни покупки, ни ошибки. Сьют дыру не видел, потому что звал buyBundle
    строкой и кнопку не нажимал. Теперь имя собирается строкой, РЕЗУЛЬТАТ
    ЧИТАЕТСЯ (молчаливый отказ — ровно то, из-за чего дыра прожила незамеченной).
  • 🔴 СЛУЖЕБНЫЙ ИНТЕРФЕЙС И ОТЛАДКА ЗАКРЫТЫ В БОЮ. Введён признак `DEV`
    (00-config): file:// (так ходит сьют), localhost (bridge-тесты), `?dev=1` или
    ключ хранилища — лазейка для владельца на живом Pages. В бою `window.__game`
    и `window.__ads` ОТСУТСТВУЮТ, кнопка «Developer panel» скрыта.
    ⚠️ НЕ ВЫРЕЗАНО, А ЗАКРЫТО: на этом интерфейсе стоит весь сьют, вырезав его,
    мы тестировали бы не то, что выпускаем.
    ⚠️ `mixer_dev='0'` принудительно выключает — иначе боевое поведение нечем
    проверить: тест всегда ходит по file://.
    ⚠️ ГРАБЛЯ, ПОЙМАННАЯ ЗАМЕРОМ СРАЗУ: базовое `.ms-dev{display:none !important}`
    перебивало `html.dev .ms-dev` — панель не открывалась и в разработке.
    !important убран, специфичности (0,2,1) против (0,1,0) достаточно.
  • 🔴 ЗАГЛУШКА РЕКЛАМЫ УБРАНА ИЗ БОЯ. На площадке без поддержки роликов
    открывался НАШ экран «(rewarded video will play here)», ждал 3 с и ВЫДАВАЛ
    награду — при безлимите рекламных встрясок это бесконечная раздача бонусов
    при нулевом доходе, а поддельный рекламный экран ещё и типовое основание для
    отказа на ревью. Теперь стаб только в DEV, в бою честный отказ с тостом.
  • 🔴 СТОРОЖ РОЛИКОВ ПОЧИНЕН. Его снимало ЛЮБОЕ первое состояние, включая
    OPENED: замолчи площадка после открытия ролика — снять паузу было некому,
    игра оставалась замороженной навсегда (у межстраничной страховка была, у
    роликов нет). Теперь гасится только на конечных состояниях, а на OPENED
    переводится на предел 120 с.
  • Заголовок вкладки без слова «Prototype», язык страницы en (был ru при
    полностью английском интерфейсе).
  Верификация: сьют 234 PASS, честный exit=0; отдельный прогон боевого режима —
  `__game` undefined, `__ads` undefined, кнопка отладки display:none; в DEV всё
  на месте.

- v1-test-162 · 2026-07-29 — АДАПТИВНОЕ КАЧЕСТВО: ОПРЕДЕЛЕНИЕ СЛАБОГО УСТРОЙСТВА
  ЗАМЕРОМ (спека владельца «оптимизируй, слабые телефоны важны, но умей
  определять, что это слабый телефон»).
  ⚠️ ГЛАВНОЕ ОТКРЫТИЕ ЗАМЕРА — ДВУХСЕКУНДНОГО ФРИЗА В ИГРЕ НЕТ. Прежний замер
  (v161) показывал худший кадр 1864-2772 мс и выглядел приговором. Оказалось,
  это целиком ТЕСТОВЫЙ ХУК `skipIntro` (300 синхронных шагов физики), которого у
  игрока не бывает. Живой путь: генерация уровня 22 мс (94 мс при ×4), худший
  кадр за интро 107 мс, в покое 72, при встряске 63. То есть геймплей на слабом
  устройстве ДЕРЖИТСЯ. Не проверь я это — «оптимизировал» бы несуществующее.
  ОПРЕДЕЛЕНИЕ: медиана реального кадра за 2.5 с игры (не в интро, кадры >200 мс
  выброшены как системные) выше PERF_SLOW_FRAME_MS=30 → ступень 'low'.
  ⚠️ ПО ВРЕМЕНИ, А НЕ ПО ЧИСЛУ КАДРОВ. Первая версия ждала 90 кадров и выходила
  наизнанку: чем слабее телефон, тем ДОЛЬШЕ он живёт на высоком качестве (на ×8
  это 9 секунд тормозов). По времени слабый решается за те же 2.5 с.
  ⚠️ МЕДИАНА, А НЕ СРЕДНЕЕ: одна заминка сборщика мусора среднее утащит.
  ⚠️ РЕШЕНИЕ ОДИН РАЗ И ТОЛЬКО ВНИЗ — иначе на границе качество «дышит».
  ⚠️ ПОДСКАЗКА УСТРОЙСТВА (≤2 ядер / ≤2 ГБ) — лишь стартовая гипотеза, чтобы не
  ждать 2.5 с на заведомо слабом. Отсутствие полей = «не знаем», не «слабый».
  ЧТО МЕНЯЕТ 'low': плотность пикселей 1.5 → 1.0 (заливка растёт КВАДРАТОМ, это
  2.25× пикселей) и частицы ×0.4 (труха 1280 → 512).
  ⚠️ ТЕНИ В СПИСОК НЕ ВОШЛИ: замер показал, что в matcap-режиме теневой проход
  УЖЕ выключен и на «сильном». Ручку-пустышку убрал — ровно такую я однажды уже
  выкидывал из проекта (мобильный blur, 0 разницы).
  ⚠️ ФИЗИКУ НЕ ТРОГАЛ СОЗНАТЕЛЬНО: накопитель уже ограничен 3 подшагами (лавины
  нет), а число итераций солвера держит плотную кучу — ослабив, поменяли бы
  ПОВЕДЕНИЕ кучи, то есть геймплей. Это отдельное решение владельца.
  ЗАМЕР ВЫИГРЫША (393×852, полная куча, сразу после встряски): ×4 кадр p95
  70 → 60.7; ×6 82.9 → 72.5, то есть ~13%. ⚠️ ЭТО НИЖНЯЯ ОЦЕНКА: headless
  рисует софтверно, а понижение плотности пикселей выигрывает именно на GPU.
  Ручки: `__game.perfTier()` / `__game.setPerfTier('low')`; событие `perf_low`.
  ⚠️ ФЛЕЙК СЬЮТА «бомба убирает камень (1 -> 1)» — НЕ от этой правки (два
  прогона подряд после него чистые). Причина: если бомбу израсходовала
  предыдущая секция, `bombIndex()=-1`, `place(-1,…)` и `detonate()` молча ничего
  не делают, а сообщение винит бомбу в том, что её нет. Индекс бомбы внесён в
  текст ассерта — следующий отказ назовёт причину сам.

- v1-test-161 · 2026-07-29 — МОБИЛЬНЫЙ БАТЧ ВЛАДЕЛЬЦА (6 пунктов) + 🔴 ФИКС
  ФАТАЛЬНОГО ЭКРАНА, УБИВАВШЕГО ЖИВУЮ ИГРУ.
  🔴 **ГЛАВНОЕ.** Владелец прислал скрин с реального iPhone: «Failed to start 3D —
  Script error. (line 0)» на УЖЕ РАБОТАВШЕЙ игре, ikorzun.github.io. Разбор:
  `window.onerror` в shell.html стоял глобально и НАВСЕГДА, а «Script error.» с
  line 0 — это НЕПРОЗРАЧНАЯ ошибка чужого скрипта (браузер не раскрывает детали
  из-за политики происхождения). То есть партию убивала ошибка рекламного SDK, к
  нашему коду отношения не имевшая, причём экран несъёмный, без кнопок, и
  советовал «откройте в браузере» человеку, который УЖЕ в браузере.
  Лечение: (1) чужие непрозрачные ошибки игнорируем совсем; (2) после успешного
  старта (`window.__booted` ставит 99-main) фатальный экран не показывается —
  игра переживает ошибку; (3) на самом экране появилась кнопка Reload и честный
  текст. Проверено: ошибка, брошенная в живой игре, экран не поднимает, партия
  жива.
  1. УДАЛЕНЫ КНОПКИ «ПРИЦЕЛ» И «МЕТАЛЛОИСКАТЕЛЬ» («моргают при загрузке»). Они
     лежали в разметке ВИДИМЫМИ и прятались лишь первым тиком updateHUD, то есть
     уже после запуска движка — отсюда моргание. Убраны разметка, CSS,
     обработчики и строки HUD; сирот не осталось. ⚠️ ОТМЕНЯЕТ прежнее «не удаляй,
     скрой флагом»: механика (scopeHighlight/detectorHighlight, флаги) цела,
     вернуть = два обработчика и две строки разметки.
  2. ПОДСКАЗКА В ЛЕВЫЙ НИЖНИЙ УГОЛ НА МОБИЛЕ, «по одной вертикали с паузой».
     Замер: пауза left 8, подсказка left 8 — совпало; по вертикали выровнена с
     Shake (12 от низа, было 4 → сидела на 8px ниже). ⚠️ ТОЛЬКО <813px: выше
     этого в левом нижнем углу разворачивается витрина.
  3. ЧЁРНЫЕ ПОЛЯ SAFARI 26 — ПРИМЕНЁН РЕЦЕПТ С ЛЕНДИНГА about-us. Safari 26
     ИГНОРИРУЕТ theme-color и берёт тинт баров из background-color html/body, но
     fixed-элемент поверх ОТРАВЛЯЕТ его своим фоном, а `transparent` трактуется
     как «прозрачный ЧЁРНЫЙ». Наши #topBar/#bottomBar/#face были ровно такими.
     Дали им фон цвета СВОЕЙ кромки с альфой 0.01 (верх и низ разные — небо
     градиентное; переменные --sky-top-rgb/--sky-bot-rgb ставит 10-stage из того
     же SKY_GRAD). Замер: верх rgba(206,228,254,.01), низ rgba(190,199,254,.01),
     html и body в цвет верха. ⚠️ ПРОВЕРИТЬ МОЖЕТ ТОЛЬКО ТЕЛЕФОН ВЛАДЕЛЬЦА —
     headless этого не воспроизводит. ⚠️ НЕ возвращать transparent «для чистоты».
  4. ТОЛЬКО ДЕНЬ И НОЧЬ: утро (часы 5-11) удалено из SKY_GRAD, его часы отошли
     дню. Побочная польза — границы совпали с isNightSky (18..5), продублированный
     час перестал быть местом расхождения.
  5. ЗАМЕР ПРОИЗВОДИТЕЛЬНОСТИ НА МОБИЛЬНОМ ВЬЮПОРТЕ (393×852, полная куча 130
     предметов / 547 тел, худший случай — сразу после встряски):
     без замедления — кадр p95 45.8 мс, физика p95 4.7;
     ×4 (средний телефон) — кадр p95 70.9, max 1863.9, физика p95 20.2;
     ×6 (слабый) — кадр p95 85.2, max 2772.2, физика p95 31.4.
     ⚠️ АБСОЛЮТНЫЙ FPS НЕ ПЕРЕНОСИТСЯ НА УСТРОЙСТВО: headless рисует софтверным
     растеризатором без GPU. А вот ФИЗИКА — чистый CPU и переносится: при ×4 один
     её шаг (20 мс) уже съедает бюджет 60 fps, при ×6 (31 мс) выходит за бюджет
     проекта 25 мс. Это сигнал, а не приговор — настоящий замер только на
     устройстве. Митигаций НЕ делал: спека была «проверь», а не «оптимизируй».

- v1-test-160 · 2026-07-29 — МЯГКОЕ РАСТВОРЕНИЕ ПОД ШАПКОЙ КОЛЛЕКЦИИ (спека
  владельца «дать заголовку мягкий градиент-растворение сверху вместо жёсткой
  границы»). Карточка, уходящая под заголовок, больше не обрубается о верх
  скролл-бокса, а гаснет по альфе на высоте шапки (64px).
  ⚠️ ИМЕННО МАСКА, А НЕ НАКЛАДНОЙ ГРАДИЕНТ ЦВЕТА СТРАНИЦЫ. Накладка — это и
  есть та «подложка», которую владелец дважды просил убрать: он увидел бы
  плотную полосу поверх контента. Маска делает сам контент ПРОЗРАЧНЫМ.
  ⚠️ 64 — ТО ЖЕ ЧИСЛО, ЧТО У ПЕРЕКРЫТИЯ (v159), и это не совпадение: конец
  растворения обязан совпасть с верхом сетки, иначе первая строка карточек
  была бы полупрозрачной В ПОКОЕ. Проверено скрином на scrollTop=0 — первая
  строка полностью непрозрачна. Меняется одно — менять оба.
  ⚠️ ГРАДИЕНТНАЯ МАСКА — БЕЗОПАСНЫЙ СЛУЧАЙ, в отличие от масок из внешних SVG
  (там mask-mode расходится между движками и однажды уже съел вордмарк на
  лендинге). Прописаны обе формы, `-webkit-mask-image` для Safari и старых
  Chromium. Замер: computed `linear-gradient(rgba(0,0,0,0) 0px, rgb(0,0,0) 64px)`.
  ⚠️ ОБЛАСТЬ ДЕЙСТВИЯ — ТОЛЬКО БЕНТО ≥1080 (там же, где перекрытие). Замер:
  393 и 900 — маски нет, 1080 — есть. На стакеде шапка не перекрывает сетку,
  растворять нечего.
  Верификация: сьют 234 PASS, честный exit=0; скрины покоя и прокрутки.

- v1-test-159 · 2026-07-29 — ТРИ ПРАВКИ ВЛАДЕЛЬЦА ПО МЕНЮ/КОЛЛЕКЦИИ.
  • НОЧНАЯ ИНВЕРСИЯ КНОПКИ («в ночной теме кнопка должна быть инвертирована:
    белая кнопка, чёрный текст»). Это то самое «отдельное слово», которым
    кнопки МЕНЮ входят в системное правило «светлая тема = тёмные кнопки,
    тёмная = светлые»: прежде правило их не касалось, потому что меню лежало
    на своей светлой подложке, а с приходом градиента времени суток (v156) эта
    посылка перестала работать. ⚠️ Пара СВОЯ, чистые #fff/#000, а не общие
    `--btn-bg/--btn-fg`: те дают #2a2935 — сдвинули бы ДНЕВНОЙ вид кнопки (о
    нём речи не было) и дали бы ночью тёмно-серый текст вместо чёрного.
    ⚠️ БЛИК ТОЖЕ ИНВЕРТИРОВАН: белая полоса по белой кнопке невидима — без
    этого фича молча пропала бы в тёмное время. Замер: ночь rgb(255,255,255)
    фон / rgb(0,0,0) текст, день без изменений.
  • ШАПКА КОЛЛЕКЦИИ БОЛЬШЕ НЕ РЕЖЕТ КАРТОЧКИ («странно режется шапка, словно
    наполовину по вертикали»). ⚠️ ДИАГНОЗ ЗАМЕРОМ, А НЕ НА ГЛАЗ: шапка занимает
    полосу 8…64, а верх скролл-бокса стоял на 48 — линия обрезки проходила
    РОВНО ПОСЕРЕДИНЕ шапки, и, поскольку фона у шапки нет (сам владелец просил
    прозрачную), карточка обрубалась в пустоту. Перекрытие вернул с 24 на 64 =
    полное расстояние от верха шапки до верха сетки; верх скролл-бокса теперь
    совпадает с верхом шапки. ⚠️ СТАРАЯ ЖАЛОБА («лишний отступ сверху при
    скроле»), из-за которой перекрытие когда-то срезали до 24, НЕ вернулась —
    проверено на scrollTop=0: сетка стартует на 72, голой полосы нет (высота
    шапки с тех пор стала 56, прежнее «пол-заголовка» не воспроизводится).
  • ПЯТАЯ КОЛОНКА С 1300 («после 1300 нужно добавлять справа 5 колонку»).
    Лесенка колонок теперь: <800 = 2 · 800–1080 = 3 · 1080–1300 = 4 · ≥1300 = 5.
    ⚠️ Правило стоит ПОСЛЕ бенто-блока: специфичность одинаковая (0,1,0), спор
    решает порядок в файле — выше него четвёрка бенто перебила бы пятёрку.
    Замер: 1279 → 4, 1300 → 5, 1900 → 5.
  Верификация: сьют 234 PASS, честный exit=0; скрины и computed styles сверены
  на 1279/1300/1900 и в обеих темах.

- ПРАВКА КАНОНА · 2026-07-28 — ЧАСТОТА СРАБАТЫВАНИЯ КАПА: ДВЕ ОШИБОЧНЫЕ
  ОЦЕНКИ ПОДРЯД, ЗАКРЫТО ЗАМЕРОМ. Хронология, чтобы не повторять:
  1) я замерил размеры групп на ПЕРВОМ тапе свежей кучи (комбо холодное,
     радиус 0.9) и обобщил до «естественно 8 не бывает» — неверно;
  2) ФИЗИКА поправила «кап срабатывает регулярно, 4 из 21 на ур.1» — но
     считала матчи РАЗМЕРОМ 8, а её же поле raw в том прогоне не превышало 8,
     то есть настоящих срезов не было вовсе; сама это вскрыла и сняла цифру;
  3) строгий замер обеих сторон: ДИСПЕТЧЕР — 4 раскладки × 25 тапов, 94 матча,
     НОЛЬ срезов, макс raw 8 (плюс более ранний прогон: 2 среза из 25);
     ФИЗИКА — 4 сида × 30 тапов, срезы только на одном сиде (2 из 25, raw 10).
  ИТОГ: ~2 среза на ~120 матчей, не на каждой раскладке. Кап 8 — ПОТОЛОК-
  СТРАХОВКА от редких пиковых тапов, а не регулятор экономики.
  ⚠️ ЗАМЕР — ВЕРХНЯЯ ОЦЕНКА: `bestTapTarget` без аргумента берёт САМУЮ крупную
  группу каждый тап; обычный игрок упрётся в кап ещё реже.
  ⚠️ ОБЩАЯ ОШИБКА ОБЕИХ СТОРОН — считали «матч вышел 8» вместо `raw > 8`.
  Размер 8 сам по себе среза не доказывает. Поле raw для того и заведено.
  ⚠️ СНЯТО ПРЕДУПРЕЖДЕНИЕ ВЛАДЕЛЬЦУ «2★/3★ стали заметно труднее»: оно
  опиралось на расчёт для групп по 16, которых в замерах нет (макс raw 10).
  Владельцу отправлена смягчённая версия — я успел донести переоценку дважды,
  до того как её проверил.

- v1-test-158 · 2026-07-28 — Merge ИНТЕРФЕЙС: БАТЧ UI ИЗ ШЕСТИ ПУНКТОВ ВЛАДЕЛЬЦА.
  (1) витрина 3 строки (`VIT_MAX` 5→3, ротация по прогрессу не тронута);
  (2) множители по ПРАВОМУ краю — фиксированная колонка 56px, БЕЗ
  `margin-left:auto` (тот уже ломал зазор, предупреждение сработало); замер:
  зазоры 12/12/12, правые края 281/281/281;
  (3) ховер Next `#c0ff47`→`#d2ff7e`;
  (4) CLEANED — чёрный текст на лаймовой обводке `--otl:10`; очки+звезда
  обводка +30% (`--otl` 9→11.7, computed 18→23.4px);
  (5) карточки More Stars — плавный `scale(1.03)` на ховере, клик по всей
  карточке = покупка (клик по самой кнопке в обработчике пропускается, иначе
  покупка ушла бы ДВАЖДЫ);
  (6) кнопка Open у недоступных не создаётся (0 в DOM при 84 закрытых
  карточках); механика `purchaseUnlock` жива, возврат = три помеченные строки.
  ⚠️ ХОВЕРЫ ПОД `@media (hover:hover) and (pointer:fine)` — на таче `:hover`
  залипает после тапа, карточка осталась бы увеличенной.
  ⚠️ ПОПРАВКА ИНТЕРФЕЙСА К МОЕЙ ПОСТАНОВКЕ (принята): я писал «витрина строит
  весь замес типов, VIT_SLOTS удалён» — это цитата из CLAUDE.md, которая
  УСТАРЕЛА: кап и ротация вернулись 2026-07-27, и правка свелась к одной
  константе вместо переделки. Абзац в CLAUDE.md помечен как устаревший.
  Верификация диспетчера: SUITE PASS, честный exit=0, 234 PASS; скрины
  витрины и экрана победы + computed styles сверены лично.

- v1-test-157 · 2026-07-28 — Merge ФИЗИКА: КАП РАЗМЕРА ГРУППЫ = 8 (спека
  владельца «поставь кап на 8»). Раньше капа НЕ БЫЛО вовсе — тап уносил все
  доступные однотипные в радиусе. MATCH_MAX_N=8 в 00-config (считается ВЕСЬ
  матч, включая тапнутый); лишние отсекаются ПО ДАЛЬНОСТИ — ближайшие к
  тапнутому по истинному зазору (метрика бомбы), «схлопнулось вокруг пальца»,
  отсечённые живут и матчатся следующим тапом. Точки применения — handleTap и
  findHintGroup (подсказка не обещает больше, чем соединится). Замер ФИЗИКИ:
  на ур.1 кап реально срабатывает (3 матча из 21 упёрлись в 8 при доступных
  до 16), на ур.10 потолок 10, на ур.40 — 3-4, то есть поздние уровни не
  меняются вовсе. Сьют +2 ассерта.
  ⚠️ ПОСЛЕДСТВИЕ ДЛЯ ЗВЁЗД (несу владельцу): цена группы 10·N·(N−1)
  КВАДРАТИЧНА, а пар-скор для звёзд (finalizeFill) от размера группы НЕ
  зависит. Кап срезал ранний доход (16 → 2400 очков за тап, 8 → 560), значит
  2★/3★ на первых уровнях стали ТРУДНЕЕ. Ожидаемая цена решения, не дефект;
  при жалобе «звёзды не даются на старте» первый подозреваемый — этот кап,
  лечится порогами STAR2_K/STAR3_K или величиной капа.
  ⚠️ ИЗ ПАЧКИ ФИЗИКИ ВЗЯТ ТОЛЬКО ЭТОТ КОММИТ — сдача пришла на базе v136,
  main давно v156. Два других коммита ветки НЕ вливались:
  • 633fc87 «усиление взрыва» (их v137) УЖЕ в main как v151, а поверх него
    владелец заказал ×3 (v156: WAVE_V 15, JOLT 18). Мерж ветки целиком
    ОТКАТИЛ БЫ силу взрыва к 5.0/6.0 — ровно та грабля устаревших веток,
    что была с ИНТЕРФЕЙСОМ и ГРАФИКОЙ.
  ⚠️ НОВЫЙ АССЕРТ КАПА ПРИНЁС СВОЙ ФЛЕЙК — ПОЧИНЕН В КОРНЕ (это уже моя часть).
  Он бьёт РЕАЛЬНЫМ мышиным кликом по координатам bestTapTarget, а тот отдавал
  проекцию ЦЕНТРА предмета БЕЗ проверки перекрытия. Клик через раз попадал в
  загораживающую БОМБУ и детонировал её ДО секции бомбы — три ассерта бомбы
  падали (index −1, материал null, детонация), причём «SUITE: PASS» в одном
  прогоне и «SUITE: FAIL (4)» в следующем. Это ТА ЖЕ грабля, ради которой в
  v76 делали findByTex v2. Лечение: подбор пикселя вынесен в общий
  visiblePixel(it, ctx) (центр + 8 смещений на 0.55·r, отдаётся пиксель, где
  предмет — ПЕРВОЕ пересечение луча); им теперь пользуются И findByTex, И
  bestTapTarget, причём последний перебирает ВСЕХ членов группы (тап по любому
  даёт тот же групповой матч). Проверка: 3 прогона подряд SUITE PASS с честным
  exit=0, 231 PASS, бомба на индексе 65.
  ⚠️ АДВЕРСАРНОЕ РЕВЬЮ СЛИЯНИЯ НАШЛО ДЕФЕКТЫ В МОЁМ ЖЕ ФИКСЕ (4 среза, 12
  находок, 9 опровергнуто, 3 подтверждено) — исправлены до пуша:
  • ⛔ «ТАП ПО ЛЮБОМУ ЧЛЕНУ ДАЁТ ТОТ ЖЕ МАТЧ» — МОЁ УТВЕРЖДЕНИЕ БЫЛО НЕВЕРНО.
    pairMatch — отношение БЛИЗОСТИ (зазор <= matchRadius), а НЕ класс
    эквивалентности: findHintGroup собирает группу вокруг ЯКОРЯ, а handleTap
    пересобирает её вокруг того, по кому кликнули. Отдавая пиксель одного
    члена и n другого, хук ВРАЛ: замер ревью — 9 расхождений из 14 тапов.
    Лечение: bestTapTarget считает группу вокруг КАЖДОГО кандидата тем же
    правилом и капом, что handleTap, и отдаёт n того предмета, чей пиксель
    вернул; плюс поле raw (размер ДО капа). Замер после: 10 из 10 совпадений
    на холодном комбо. ⚠️ При СЕРИИ быстрых тапов расхождения остаются и это
    НЕ дефект — комбо-лесенка растит радиус между замером и кликом.
  • ⛔ ВОЗВРАТ null ПРИ ЖИВЫХ ГРУППАХ (регрессия первой версии моего фикса):
    перебирался только ОДИН набор, и если все его члены закрыты от камеры —
    null, хотя рядом видимы десятки других. Доступность считается лучами В
    НЕБО, а пиксель — лучом ОТ КАМЕРЫ, это разные вещи. Замер ревью: дефолтный
    режим отдавал null на ур.5/10/20 при 77-134 живых парах. Лечение: перебор
    ВСЕХ кандидатов (как в findByTex) + честный ответ {n:0, occluded:true}.
  • ⛔ ПАКЕТ ТЕСТЕРОВ БЫЛ ПРОТУХШИМ: release/Mixer.html и зип я перепаковал
    ДО сборки v157 — в них лежал v156 без капа вовсе. Перепакован из финальной
    сборки (сверено: v1-test-157, MATCH_MAX_N внутри зипа).
  ⚠️ ПОБОЧНО ВСКРЫЛОСЬ ОКРУГЛЕНИЕ: смещённые пробы visiblePixel лежат у самого
  силуэта, и округление до целого пикселя перебрасывало луч на соседа. Теперь
  попадание сверяется ПО ОКРУГЛЁННОМУ пикселю — тест кликает именно им.
  ⚠️ СТРАЖ КАПА БЫЛ ПУСТЫМ: на ПЕРВОМ тапе свежей кучи (холодное комбо,
  радиус 0.9) крупнейшая группа до 8 не дотягивает (замер 10 сидов — ни разу),
  поэтому «ушло <= 8» проверяло пустоту. ⚠️ ОБОБЩАТЬ ЭТО НА ИГРУ НЕЛЬЗЯ — моя
  первая формулировка «естественно 8 не бывает» БЫЛА НЕВЕРНА, поправлено
  ФИЗИКОЙ: комбо-лесенка тянет радиус к 1.1, куча оседает, и кап срабатывает
  регулярно (ФИЗИКА: 4 из 21 на ур.1, 1 из 24 на ур.5; мой контрольный замер:
  2 из 25 на ур.1, радиус 0.94 -> 1.1). Считать надо по raw > 8, а НЕ по
  «матч вышел 8» — группа могла быть ровно 8 и без капа. Добавлен детерминированный страж: радиус временно раздувается до
  3.0, группа выходит 16, и сьют требует РОВНО 8 (raw 16 -> n 8 -> ушло 8);
  радиус возвращается. Итог: 234 PASS, два прогона подряд, честный exit=0.
  ⚠️ ПОБОЧНАЯ ПОЛЬЗА: калибровочные боты звёзд ходят через тот же
  bestTapTarget — они тоже били вслепую и часть их тапов уходила в чужой
  предмет. Прежние калибровки звёзд этим слегка смещены.
  • 5345ed5 «фикс флейка осколков» — в main стоит БОЛЕЕ СИЛЬНАЯ версия того
    же лечения (стабилизация базы до 3 совпадений за 4 с + ПИК ЗА ОКНО с
    ранним выходом); вариант ФИЗИКИ (2 совпадения за 60 кадров, без пика)
    случай «кадр попал в провал между дренажом чужих и появлением своих» не
    закрывает. Оставлена версия main.

- v1-test-156 · 2026-07-28 — БАТЧ ВЛАДЕЛЬЦА: БОМБА ×3, ДОПУСК ОШИБОК В ТУРБО,
  МАШИНЫ ПОД БАНАН + ПРИМЕРКА ГРАДИЕНТОВ ВРЕМЕНИ СУТОК НА КАРТОЧКУ PLAY.
  • БОМБА ×3 («усиль эффект от бомбы в 3 раза»): BOMB_WAVE_V 5.0→15.0,
    BOMB_JOLT 6.0→18.0. ⚠️ BOMB_CAM_SHAKE 0.45→0.6, а НЕ ×3: это
    ДЛИТЕЛЬНОСТЬ тряски камеры, а не сила — 1.35 с мотало бы экран дольше,
    чем живёт сам взрыв. Радиус и кап жертв (5.72 / 7) НЕ тронуты: владелец
    просил эффект, а зона поражения — отдельная спека (уже ×2 в v126).
  • ДОПУСК ОШИБОК В ТУРБО (жалоба дословно: «2 ошибки в турбо на простом
    полностью останавливают падение вещей, на сложном нет попыток вовсе —
    1 ошибка критична»). Плоская CHAIN_MISSES=2 разведена по сложности:
    CHAIN_MISSES_EASY=4 / CHAIN_MISSES_HARD=3 (chainMissesLimit()).
    ⚠️ На Hard стало МЯГЧЕ, чем было (3 против 2) — владелец описал Hard как
    самый болезненный случай; ужесточать там нечего.
  • МАШИНЫ ПОД БАНАН («машинки должны быть не меньше размера банана»):
    все 12 типов car rc 1.0 → 1.4 + geo().clone().scale(1.4) — ровно как у
    банана (эталон, названный владельцем). Заполнение/topY в норме, сьют PASS.
  • ЗАЛИВКА КАРТОЧКИ PLAY = ГРАДИЕНТ ВРЕМЕНИ СУТОК (примерка по просьбе
    «давай примерим»): плоский #d8bbff заменён на var(--sky-grad), которую
    10-stage ставит ИЗ ТОГО ЖЕ SKY_GRAD, что кормит небо. Один источник —
    карточка и фон игры разъехаться не могут по построению; считается раз
    при загрузке, как и небо. Фолбэк #d8bbff цел.
    ⚠️ ДВА ВОПРОСА ВЛАДЕЛЬЦУ ПО ПРИМЕРКЕ (не правил сам): (1) ДЕНЬ #cee4fe —
    белые глаза почти сливаются с подложкой, утро и ночь читаются хорошо;
    (2) НОЧЬ #283667 — ЧЁРНАЯ кнопка Resume тонет в тёмной карточке. Правило
    владельца «светлая тема → тёмные кнопки, тёмная → светлые» на кнопки МЕНЮ
    пока не распространено (CLAUDE.md, «отдельное слово») — а его посылка
    «меню на своей светлой подложке» этой правкой как раз перестала работать.
  • Тест-харнесс: синтетический креш секции МЕТРИК исключён из гейта ошибок
    страницы (он и есть предмет проверки — иначе собственный тест ловли
    ошибок валил бы сьют и маскировал настоящие).

- v1-test-146 · 2026-07-27 — ЛЖИВАЯ МЕТКА «ВРЕМЕННО» У НЕСУЩЕГО ТЕСТ-ХУКА
  (ГРАФИКА ОСПОРИЛА МОЁ УКАЗАНИЕ — и была права). Я сказал «убери
  setPortraitPose, раз помечен ВРЕМЕННО». Графика проверила и вернулась с
  фактом: на этом хуке стоит ЕДИНСТВЕННЫЙ страж инварианта «поза статики и
  спина — ОДИН источник». Getter вместо мутации НЕ годится: статика и спин
  читают одну переменную, сверка «getter против getter» пуста и зелена всегда;
  мутация и ЕСТЬ проверка. Доказано СИМУЛЯЦИЕЙ (дали спину свою копию yaw →
  ассерт упал, startAngle −0.6 вместо 0.2; вернули общую → 0.2, PASS).
  ⚠️ НАСТОЯЩИЙ БАГ БЫЛ В МЕТКЕ, а не в коде: «ВРЕМЕННО, удалю после бейка»
  на несущем хуке — приглашение снести стража. Переписал на «⚠️ ТЕСТ-ХУК, НЕ
  ВРЕМЕННЫЙ — НЕ УДАЛЯТЬ» с объяснением и замером; в CLAUDE.md добавлено, чем
  инвариант позы стережётся (и что рамку стережёт отдельный thumbFrames).
  Кода не трогал, поведение то же, сьют PASS.
  ⚠️ ВЕТКУ graphics-poseclean НЕ МЕРЖИЛ: её ref указывает на v144 (нового
  коммита нет, правка осталась у них в worktree), а мерж как есть ОТКАТИЛ бы
  мой v145 (диагностика settled) и маркер версии. Сделал те же правки сам —
  99-main и CLAUDE.md моя зона.
  УРОК (общий, в копилку): «ВРЕМЕННО/TODO-удалить» на коде, который держит
  инвариант — сам по себе дефект; и указание диспетчера «убери» не отменяет
  обязанности направления проверить и оспорить с доказательством.

- v1-test-145 · 2026-07-27 — ДИАГНОСТИКА ФЛЕЙК-АССЕРТА (ГРАФИКА заамендила
  коммит после моего мержа: 48e4c3c → a9a4766; взял дельту, не пере-мержил).
  Флаг settled в сообщении: если стабилизация базы когда-нибудь упрётся в
  потолок 4 с (сегодня недостижимо — нужен >4 с монотонного падения), провал
  выглядел бы КАК ИСХОДНЫЙ ФЛЕЙК и следующий диагностировал бы всё заново;
  теперь в тексте будет «⚠️ база НЕ устоялась». На вердикт не влияет.
  ⚠️ ВАЖНОЕ УТОЧНЕНИЕ ПРО ПОРОГ (снимает мою же тревогу из v144): усиление —
  это base + N/2 (+6), а НЕ полный залп. Их ревью показало, что строгий
  «base + N» ВЕРНУЛ БЫ ФЛЕЙК: пара чужих дренажей на том же кадре роняет
  дельту до 10. N/2 совпадает с порогом раннего выхода цикла — то есть цикл
  уже доказал это значение до выхода. Сьют PASS.

- v1-test-144 · 2026-07-27 — ФИКС ФЛЕЙКА АССЕРТА ОСКОЛКОВ (ГРАФИКА, cherry-pick
  48e4c3c — молнии из той же ветки уже в v141, ребейз дал им новый хеш, дубля
  не взял). ДИАГНОЗ: base брался мгновенно, а секция идёт сразу ПОСЛЕ рекламной
  пробы — там ещё дренажатся геометрии предыдущей секции; если за два кадра их
  уходило больше, чем добавлял залп из 12, «после» < «до» (мой репорт 96→95 при
  норме 74→86). ЛЕЧЕНИЕ: стабилизация базы (3 равных сэмпла по 80 мс, потолок
  4 с) + ПИК ЗА ОКНО вместо одной выборки (rAF до 2 с с ранним выходом).
  ⚠️ МЕТОД, КОТОРЫЙ СТОИТ ЗАПОМНИТЬ: «прогнать 12 раз» тут БЕСПОЛЕЗНО — старая
  схема давала 12/12 PASS, потому что в изоляции база всегда чистая. Флейк
  ловится только ВОСПРОИЗВЕДЕНИЕМ падающего фронта (6 ступенчатых залпов по 60
  через 100 мс): на нём старая схема падает 5 из 8, новая 8/8.
  ⚠️ ПОБОЧНАЯ НАХОДКА: shardBurst возвращает fx.length (ВСЕ живые эффекты), а
  не число осколков — порог раннего выхода привязан к явному N=12.
  ⚠️ ПОРОГ УСИЛЕН СВЕРХ ПРОСЬБЫ (одобряю): было peak > base, т.е. +1 — а
  регрессия «все осколки на одной кэшированной геометрии» даёт ровно +1 и
  проходила бы ЗЕЛЁНОЙ; после стабилизации базы прирост детерминированный,
  поэтому порог поднят до N/2. Ассерт переименован и теперь читается как
  инвариант: «осколки: КАЖДЫЙ несёт свою геометрию (71 → 83, +12 при залпе 12)».
  Сьют PASS ×2 (прогон стал длиннее из-за стабилизации — это цена честности).

- v1-test-143 · 2026-07-27 — Merge МЕТА: ПЕРСИСТ КАДЕНЦИИ МЕЖСТРАНИЧНЫХ
  (закрывает находку №2 матрицы: «no-Ad продавал почти нулевой инвентарь»).
  Ветка meta-inter-persist (819cc49, off v141). winsSinceInter переехал из
  замыкания IIFE в сейв (Save.iw) — до этого пять побед надо было набрать в
  ОДНОЙ непрерывной сессии страницы, и три захода по 20 минут давали НОЛЬ
  показов всегда; «месяц без рекламы» в бандле $19.99 убирал то, чего игрок
  и так не видел. Теперь обещание гасит реальный инвентарь.
  Мерж по max и это НАМЕРЕННО прокомментировано: не валюта, анти-дюп не нужен,
  худший случай расхождения устройств = один лишний показ, а не потерянные
  деньги. Добавлено в обе ветки mergeSave + resetProgress (чек-лист 77-save).
  Каденция цела — все шесть прежних ассертов зелёные; новые два: счётчик 3 до
  и 3 ПОСЛЕ реальной перезагрузки, порог считает ЧЕРЕЗ перезагрузку (4-я
  победа → 0 показов, 5-я → 1, окно сброшено). Сьют PASS ×2.
  Все три ветки Меты влиты (live-spend v142, penalty-mult v141, эта).

- v1-test-142 · 2026-07-27 — Merge МЕТА: «ПОКАЗАННОЕ = ТРАТИМОЕ» (закрывает
  следствие v137). Ветка meta-live-spend (3e6aae6, off v139). Единый гейт
  ensureBanked(price) во ВСЕХ тратах (spendStars/buyBoost/purchaseUnlock): не
  хватает забанкованного, но показанного хватает → банкуем накопленное уровнем
  и двигаем водяной знак level.banked, победа банкует ТОЛЬКО остаток. Плюс
  canBoost/canUnlockType переведены на liveBalance — иначе UI гасил бы кнопку,
  на которую деньги ВИДНЫ (та же жалоба другими словами). Ассерты: вилка из
  репорта (показано 2003 ≥ 2000 > забанковано 50) проходит; остаток 3; при ТРЁХ
  досрочных банках банк за уровень ровно floor(score/10) (3+500−320=183).
  ⚠️ СЛУЧАЙ, КОТОРОГО НЕ БЫЛО В ПОСТАНОВКЕ (нашла Мета): счёт может УПАСТЬ
  после досрочного банка (штрафы/помол, теперь ещё и ×бустер) — забанковано
  больше, чем уровень стоил. Наивное «уменьшить se» ЗАПРЕЩЕНО (se монотонный,
  мерж по max с отставшим облаком вернул бы списанное — грабля монет).
  Коррекция сделана в ss (тоже монотонный): se−ss = ровно floor(score/10),
  лидерборд сходится тем же образом. Без неё ранг оставался бы раздут ПО ПИКУ
  счёта — замер 233 вместо честных 83. Конфликт test.js (её секция vs секция
  штрафов из v141) — union-merge, обе независимы. Сьют PASS.

- v1-test-141 · 2026-07-27 — ТРИ СДАЧИ: обводка-переменная (ИНТЕРФЕЙС),
  штрафы ×бустер + rewarded-звёзды долой (МЕТА), густые молнии (ГРАФИКА).
  ⚠️ ИНТЕРФЕЙС ребейзнул ветку, и чип+порог стали НОВЫМ коммитом (ef3ed12), а
  они уже влиты в v140 из 1a47c98 — взял CHERRY-PICK только обводки (0db248d),
  дубля нет.
  ОБВОДКА: .otext text → stroke:var(--otl-color,#fff), stroke-width:calc(var(--otl,2)*2).
  Владелец пишет ВИДИМУЮ толщину и цвет, удвоение спрятано внутрь. Бит-в-бит
  проверено computed-замером база↔ветка по ВОСЬМИ правилам (.stat 4/4,
  win-level 9/9, win-time 5/5, win-score 18/18, .pop 4/4 + цвет, .pop.big 6/6,
  mixerTimer 17.6/17.6, grind 11.4/11.4). Изменились ровно два намеренных:
  CLEANED лайм 10, ×N белая 8. ×N переведены с HTML на SVG-.otext (иначе
  обводки нет); побочно: text-shadow на SVG НЕ действует → тень макета
  перевешена на filter:drop-shadow, иначе пропала бы молча.
  ✅ СТАРЫЙ СПОР ЗАКРЫТ: толстая лаймовая обводка вокруг белых букв смыкается
  в сплошное лаймовое пятно — это и есть «пилюля» из рендера Figma; в коде
  ноды её не было, потому что она СЛЕДСТВИЕ обводки, а не слой. Смыкание было
  БРАКОМ только при совпадении цвета обводки и заливки (белое на белом, #126).
  МЕТА: scorePenalty × scoreBoostMult — единая точка, накрыты промах/помол/
  камень разом; кламп нулём ПОСЛЕ умножения (ассерт: ур.3 под x5 ровно 0);
  ур.1 без штрафов бустер не меняет; попы растут симметрично награде.
  Замер выручалки: 10 с помола = −80 без бустера, −400 под x5, доля от дохода
  ОДИНАКОВА (3.4%) → смягчать нечего. REWARD_STARS_PER_AD/REWARD_DAILY_CAP
  удалены (вызовов не было), rewarded встряски/подсказки НЕ тронуты.
  ⚠️ Устаревший комментарий в doMatch («бустер штрафы НЕ трогает») правил я —
  мерж сделал его ложным.
  ГРАФИКА: молнии гуще ОТВЕТВЛЕНИЯМИ, а не частотой вызова — boltFX = дуга +
  5 веток, все филаменты СЛИТЫ В ОДНУ геометрию → на разряд по-прежнему 2
  объекта/2 материала/2 draw call (наивный путь дал бы ×6 в самом нагруженном
  режиме); вызов в 80-gameplay не тронут и получил густоту даром. Эффект
  ЗАМЕРЕН: молниевых пикселей 1337/1871 → 3035/5588 (×2.3 средн., ×3 пик).
  Перф: p95 кадра тонет в шуме (360.7→368.1 и 412.5→386.0 — во втором прогоне
  новая БЫСТРЕЕ базы), надёжный сигнал — +4-6 draw calls; шаг физики 5.1-5.3
  при бюджете 25; дренаж чистый. Материалы — FREE-LIST (не общий: у каждой
  живой молнии своё мерцание opacity, общий склеил бы их). Поправка по ходу:
  первый подбор толщины дал БЕЛЫЕ нитки — при утоньшении синий ореол уходит в
  субпиксель первым, пропорция оболочка:ядро должна быть ~3:1. Новый хук
  __game.boltProbe (молния живёт 0.16 с, случайным скрином не поймать).
  Сьют PASS.

- v1-test-140 · 2026-07-27 — Merge ИНТЕРФЕЙС: ПЕРЕПОЛНЕНИЕ ЧИПА ПОЧИНЕНО +
  порог More Stars 780→700. Ветка interface-scorefit (1a47c98, off v136).
  Чип писался СЫРЫМ числом в #scSvg с фиксированным viewBox, лишнее рисовалось
  ЗА рамкой (.otext overflow:visible) поверх конструкции глаз. Взяты ОБА
  готовых паттерна проекта: компрессор winFmtScore + fitStat('score') (рамка
  по факту текста, как lvlNum/timer). ⚠️ Компрессор РАСШИРЕН ветвью МИЛЛИОНОВ
  (умел только «k» — с бандлами 7-значный кошелёк уже в первую платную сессию,
  вышло бы «1200k»): <10k как есть · <100k «12.5k» · <1M «125k» · дальше
  «1.2M», максимум 5 символов; экран победы использует ту же функцию.
  Замер зазора «чип↔глаза» на 320/360/393/430/768/1280 при балансах
  0…12 345 678: наезда нет НИ РАЗУ, худший случай 320px при 12.3M — запас 8px.
  Порог десктопной раскладки Get More 780→700 (карточки flex:1 1 0, жмутся до
  ~225). Интерфейс подтвердил порядок трат после моего мержа v136:
  бесплатные → купленный запас (молча) → adShakes → ролик → тост.
  ⚠️ ФЛЕЙК ТЕСТА (не регрессия, не мой мерж): «осколки: свои геометрии на
  кадре» упал один раз (96→95) и дважды прошёл (74→86, 71→83) — ассерт
  сэмплит мгновенно, ловя момент дренажа. Отдано ГРАФИКЕ укрепить
  condition-wait'ом (тот же приём, что лечил флейки вуали/радиуса).

- v1-test-139 · 2026-07-27 — 🔴 ЗАКРЫТ ЭКСПЛОЙТ ЧАСОВ (был в проде v131-v135,
  принесла и сама нашла МЕТА; ветка meta-clock-slack-fix 2ff41eb).
  СУТЬ ДЫРЫ: BOOST_CLOCK_SLACK_MS=5мин ставился как защита от ложных
  срабатываний, а работал как БЕСПЛАТНАЯ ЗОНА — откат часов РОВНО в его
  пределах не доходил до переанкора, а окна хранятся абсолютной меткой →
  остаток не убывал, а РОС. Адверс-замер: откат на 5 мин каждые 5 мин, 4
  реальных часа игры → x5 жив, остаток всё те же 30.00 мин, сожжено 0 из 30;
  переживает перезагрузку. Итог: $4.99 = ВЕЧНЫЙ x5 + вечный no-Ad, вся
  синк-экономика (~6.2 млн ★) выбирается за ~63 часа, лидерборд занимается
  навсегда. Обычным QA не ловится (ленивые каденции истекают честно).
  ФИКС = УДАЛЕНИЕ, НЕ ДОБАВЛЕНИЕ: Мета сперва сделала «бюджет амнистии + кап»
  по рекомендации анализа — её же тест показал ДВОЙНОЕ списание (30/25/15/5/0),
  разбор: переанкор считает остаток как w−seen, т.е. прошедшее время УЖЕ учтено,
  а charge вычитал повторно. Вывод, которого в рекомендации не было: амнистия
  не нужна В ПРИНЦИПЕ (переанкор не даёт ни секунды и не отнимает оплаченного),
  а любой порог — бесплатная зона. Порога больше нет; констант и полей сейва
  стало МЕНЬШЕ, чем до фикса. Ассерты: каденция отката по 5 мин → честное
  30/25/20/15/10/5/0; разовый скачок стоит ровно скачка; брика нет; бандл после
  скачка работает. Сьют PASS ×2.
  ⚠️⚠️ ОСТАТОЧНЫЙ РИСК (на устройстве НЕ лечится): офлайн-откат (закрыть игру,
  отмотать часы, открыть) — любая схема на Date.now() этим бьётся. Лечение —
  server-time, зона ИНТЕГРАЦИИ, помечено в коде. 🔴 ПРАВИЛО ДО ЛОНЧА: НЕ
  подключать платёжный хук к buyBundle на проде, пока нет server-time — иначе
  первый платящий с отмотанными часами получает игру целиком за $4.99.

- v1-test-138 · 2026-07-27 — ПРОМАХ ОБНУЛЯЕТ НАБОР ТУРБО (спека владельца:
  «если при наборе турборежима игрок ошибается — счётчик режима сбрасывается»).
  Было −COMBO_MISS_DROP(2) шага, стало comboCount = 0 в ОБОИХ путях промаха:
  registerMiss (70-fx) и тап по камню (80-gameplay). Радиус-лесенка
  (comboLevel) по-прежнему теряет ровно 2, а не обнуляется — владелец назвал
  «счётчик РЕЖИМА» (заряд цепи), лесенку не трогал. Замер: набор 3→0, лесенка
  3→1. +2 ассерта.
  ⚠️ ЭТО РАЗВОРОТ ЕГО ЖЕ ПРЕЖНЕГО ТЮНИНГА — комментарий в 70-fx фиксировал
  «промах серию НЕ гасит (тюнинг владельца: слишком резко сбрасываем power
  chain)». Новая спека прямая и новее; разворот зафиксирован в коде и
  доложен владельцу, чтобы он знал, что отменяет прежнюю свою правку.
  Молнии турбо («больше мелких») отданы ГРАФИКЕ — зона FX (boltFX 70-fx),
  с рамками по перфу (турбо = пик нагрузки) и пулу материалов.

- v1-test-137 · 2026-07-27 — ЕДИНОЕ ЧИСЛО ВЕЗДЕ (жалоба владельца: «во время
  игры одно число, а на пузе второе — игрок всегда и везде видит свой единый
  баланс»). ДИАГНОЗ: игровой чип читал liveBalance() (забанкованное +
  незабанкованный счёт уровня ÷10), а кошелёк меню — starBalance() (только
  забанкованное) → открытие меню посреди партии «съедало» заработанное.
  ФИКС (моя зона — контракт «одно число» мой, v113): refreshMainScreen тоже
  на liveBalance(). Замер: чип 2003 = меню 2003 (было 2003 vs 1957).
  +2 ассерта: «чип в игре = кошелёк меню» и «кошелёк включает незабанкованный».
  Точек показа баланса ровно две, обе теперь на одном источнике (ручки в
  99-main — отладочные, не игроку). Сьют PASS.
  ⚠️ СЛЕДСТВИЕ, ОТДАНО МЕТЕ: траты (buyBoost/purchaseUnlock/spendStars)
  проверяют starBalance() — только ЗАБАНКОВАННОЕ. Замер: показано 2003,
  потратить можно 1957 → Boost за 2000 отклонится при «видимых» 2003.
  Нужен банк-по-требованию в момент покупки (без двойного начисления на
  победе). До фикса Меты в тестбилде возможен этот случай.

- v1-test-136 · 2026-07-27 — Merge ИНТЕРФЕЙС ×2: (A+B) винскрин по 783:1065 +
  СНОС «One more shake»; (1+2) обновлённая нода Get More + ховер Upgrade.
  Ветки interface-winfix (ccf0346) и interface-stars2 (a0e4f87), обе off v132.
  ⚠️ Конфликт 80-gameplay разрешён вручную: сохранена ветка КУПЛЕННОГО ЗАПАСА
  (бандлы Меты, v133) + взят прямой startAd() без оверлея.
  (A) Нода 783:1065 = ТОТ ЖЕ фрейм, что 778:732 → расхождений три, все внесены:
  время 26→28, радиус Next 1000→1500, бейдж .wt-mult padding ровный 8.
  ⚠️ ГЛАВНОЕ: бейдж УЖЕ показывал МНОЖИТЕЛЬ (fmtMult(accMult)), а не количество
  — «×1» на скрине владельца это множитель 1.0 у типов с нулевым накоплением
  (та же природа, что пустые полоски). В макете нарисован ПРОКАЧАННЫЙ игрок
  (×1/×1.1/×1.6), в свежей игре честно везде ×1 — чинить нечего.
  (B) #adAskOverlay удалён; requestShake в ad-состоянии зовёт startAd() сразу;
  вычищены hide() ×3, листенеры adYes/adNo/coinShakeBtn, проверка в Space (без
  неё $('adYes') вернул бы null и уронил загрузку); монетная ветка убрана
  (COINS_ENABLED=false, путь мёртв). Сьют обновлён: ролик запускается ТАПОМ ПО
  #shakeBtn (новый пользовательский путь), а не кликом по #adYes.
  ⚠️ Ad-вид кнопки («Shake Ad» лаймом БЕЗ счётчика) приехал ещё в v127 — моё
  описание «📺 Shake ×2» было устаревшим, «счётчик не нужен» уже выполнено.
  (1) В ноде 783:95 изменились РОВНО ДВА параметра: тень множителя
  0 2px 12px rgba(0,0,0,.16) и бонусы БЕЗ «+» («10 Shake's»). Остальное (242,
  радиус 32, ×N 80, 742) НЕ менялось — т.е. «карточки крупнее» владелец видел
  на окне <780, где включается мобильная колонка. (2) Ховер Upgrade: чёрный фон
  + белый текст, под @media (hover:hover) — на таче :hover ЗАЛИПАЕТ после тапа
  (проверено). Сьют PASS.
  ⚠️ ВЛАДЕЛЬЦУ: порог десктопной раскладки 780 можно опустить до 700 (карточки
  уже flex:1 1 0, сожмутся до ~225) — тогда десктопный вид захватит ещё 80px;
  жду слова, молча не меняем.

- v1-test-135 · 2026-07-27 — МЕХАНИКИ ×2: (МЕТА) ПАКИ УДАЛЕНЫ + детерминированный
  ассерт множителя; (ФИЗИКА) ВЗРЫВ БОМБЫ «ПОХОЖ НА SHAKE».
  МЕТА (79f58d0): STAR_PACKS удалена из 00-config (не «спит» — её нет, ссылок
  ноль), единственная платная звёздная механика = временной множитель бандла;
  пак-анкор у BOOST_TIER_CAP переписан на саму лестницу (выкуп типа ≈88 ур.);
  tu сохранено с пометкой «активных источников нет с 2026-07-27»; док получил
  баннер статуса. ⚠️ ФЛЕЙК В СВОЁМ ЖЕ ТЕСТЕ поймала Мета (и независимо
  ИНТЕРФЕЙС на чистой базе: ratio 1.04/1.43/2.00 — тест сравнивал ДВА прогона
  шумного бота, цена матча 10·N·(N−1)). Заменён на ДЕТЕРМИНИРОВАННЫЙ путь:
  бонус клада без комбо/накопления (165 = 150+5×3 без бандла, 340 = 2×(150+5×4)
  с ним) — два прогона бит-в-бит. Урок: пороговый ассерт поверх шумной величины
  не проверка.
  ФИЗИКА (cherry-pick 04cf787 — приборный коммит калибровки Hard в main НЕ
  идёт): blastWave получил ВТОРОЙ СЛОЙ — панч в зоне поражения (характер
  взрыва) + ДЖОЛТ по ВСЕЙ куче без отсечки, подброс вверх. Замер по слоям
  (__game.velByHeight, новый хелпер): верх кучи было avg|v| 0.17 / 2% шевелится
  → стало 1.08 / 98%; встряска для масштаба 6.6 → взрыв ~15% её силы. Сила
  НАМЕРЕННО мала: доступные пары после взрыва НЕ растут (не бесплатная
  встряска, бюджет 5 цел). Ревизия физики нашла 3 дефекта в её же первой
  версии ДО коммита: camShake 0.5 — это ДЛИТЕЛЬНОСТЬ, а не сила (взрыв дрожал
  бы дольше встряски при вчетверо меньшем толчке) → 0.35; BOMB_WAVE_R_K как
  множитель молча урезал радиус 6.92→6.86 → вернули аддитив BOMB_WAVE_PAD 1.2;
  затухание джолта считалось от зоны поражения → привязано к FUNNEL.H («весь
  миксер» — свойство ЧАШИ). Перф: пик шага 6.1 мс при бюджете 25. Ручка
  отката CFG.bombJolt=0 в один тап без пересборки. Сьют PASS ×2.
  ⚠️ ВЛАДЕЛЬЦУ (наблюдение физики): взрыв уже играет БУКВАЛЬНО звук встряски
  (Sound.play('shake'), отдельного 'boom' в игре нет) — это главный канал
  сходства. Если хотел «разведи их», а не «сделай похожим» — начинать надо со
  ЗВУКА (свой низкий бум + треск), джолт гасится ручкой.

- v1-test-134 · 2026-07-27 — ПОДЛОЖКА ПОПАПОВ (спека владельца): fade
  rgba(10,14,22,.72)→.88, blur 3px→6px. Сквозная для всех семи оверлеев
  (.overlay), пер-оверлейно по-прежнему переопределяются только z-index и
  прокрутка. Верификация computed-стилями: rgba(10,14,22,0.88)+blur(6px).
  ⚠️ ЗАМЕР «на слабых девайсах» (просьба владельца, эмуляция 390×780 тач +
  CPU×6): blur(6) vs blur(3) — РАЗНИЦА 0 fps; vs без размытия — 1 fps (12/13).
  Узкое место — 3D-сцена, подложка композитится на GPU. Поэтому задуманный
  мной пер-устройственный даунгрейд (@media coarse → blur 3) СНЯТ как
  выдуманная сложность без выигрыша (и он молча давал бы мобильным не то, что
  просил владелец). Осталась только reduced-motion-страховка (доступность).
  Оговорка в комменте: замер CPU-bound, слабый GPU не эмулируется — если
  тестеры пожалуются, лечится одной строкой. Сьют PASS.

- v1-test-133 · 2026-07-27 — Merge МЕТА: БАНДЛЫ «More Stars» (7fa5a31 поверх
  каркаса; конфликты 99-main/test.js разрешены вручную — ad-hint сохранён,
  старые ручки бустера ЗАМЕНЕНЫ бандл-ручками). Тиры по макету: $4.99 x5/30м
  +10 встрясок +15 подсказок +1д no-Ad; $9.99 x3/1ч +15/+20/3д; $19.99 x2/1д
  +50/+30/1мес. Контракт ИНТЕРФЕЙСУ: buyBundle(tier)/bundleState()/bundles()/
  noAdActive/noAdLeftMs/purchasedShakes. ⚠️ СТЕКИНГ: окна ПО КЛЮЧУ-МНОЖИТЕЛЮ
  (bx={5,3,2}) — играет сильнейший живой, время копится своему тиру, ничего не
  сгорает; отказ в покупке НЕВОЗМОЖЕН (в бандле едут расходники). Побочно
  закрыло дыру мержа (чужой x5 ложится в свой ключ, не поднимает действующий
  x2 — max по ключу). Встряски: постоянный кошелёк pe/ps (монотонная пара),
  порядок бесплатные→купленные→реклама, deadlock расширен на purchasedShakes.
  No-Ad гасит только межстраничные, rewarded живут. Паки спят. Мета тронула
  85-hud ОДНОЙ строкой (счётчик кнопки = бесплатные+купленные, иначе игрок с
  50 оплаченными видел «No shakes») — интерфейсу сказано. Сьют PASS (бандл
  выдал всё разом, часы, дюп, тупик с запасом/без).
  ⚠️ ВЛАДЕЛЬЦУ ИЗ МАТРИЦЫ МЕТЫ: (1) ЧИП СЧЁТА ПЕРЕПОЛНЯЕТСЯ — баг уже в проде
  (SVG с фикс. viewBox без сжатия: 360px/6 цифр наезд 4px, 393px/7 цифр 14px);
  компрессор winFmtScore есть в том же файле, но подключён только к винскрину.
  Бандлы приближают втрое-впятеро (6-значный кошелёк: ~143 уровня без бустера,
  ~29 под x5) — отдано ИНТЕРФЕЙСУ. (2) ШТРАФЫ НЕ МАСШТАБИРУЮТСЯ: под x5 матч
  «+700», промах «−1», помол «−2» — карательная сторона визуально исчезает в
  оплаченном окне; вопрос владельцу (множить штрафы = 1 строка у Меты).
  (3) Для текстов магазина: ×3.25 накопления НЕ покупается (Boost даёт максимум
  5 ступеней = ×2.25) — не обещать «×3.25 за деньги».

- v1-test-132 · 2026-07-27 — Merge ИНТЕРФЕЙС: ЭКРАН «MORE STARS» (Get More по
  макетам владельца 783:95 десктоп + 785:112 мобайл). Ветка interface-morestars
  (6f2751d, off v129, final+PASS; мерж на v131 чистый — регионы новый оверлей
  #starsOverlay + msGetMore в 90-input). Три карточки-бандла (×5/30м лаванда,
  ×3/1ч лайм, ×2/1д жёлтый) + Upgrade-кнопки; десктоп 3-в-ряд ≥780 (порог НЕ
  700: 742 контента не влезали), мобайл — колонка, множитель слева/бонусы
  справа. Подложка — общий .overlay (fade+blur) z-40 поверх меню; своя
  прокрутка + flex-start/margin:auto (иначе на низких экранах срезало ВЕРХ без
  доскролла — та же грабля, что на винскрине). ПАКОВ ЗВЁЗД НЕТ (решение
  владельца «понятия пака не существует»). ⚠️ ПРОВОДКА ЗАГЛУШЕНА: buyBundle()
  ещё нет у Меты → console.warn + тост «Coming soon», НИЧЕГО НЕ НАЧИСЛЯЕТСЯ
  (мок валюты запрещён; проба: баланс до/после не изменился). bundleState()
  сознательно не потребляют до присланной сигнатуры — подключение вторым
  заходом. Сьют PASS.

- v1-test-131 · 2026-07-27 — Merge МЕТА: КАРКАС БУСТЕРА ОЧКОВ. Ветка
  meta-score-booster (2247905, off v128, final+PASS; конфликты с v129/130
  ad-hint — union-merge обеих добавок, probe_clock.js Меты в main не взят).
  Каркас: SCORE_BOOSTERS (пока старые цены — обновятся бандл-проходом),
  множитель ПОСЛЕДНИМ в стеке doMatch (комбо × накопление × бустер) + клад;
  ШТРАФЫ НЕ МНОЖАТСЯ (деньги усиливают награду, не наказание). Ручки:
  scoreBoostMult/LeftMs/scoreBoosters/grantScoreBoost (вход ИНТЕГРАЦИИ после
  оплаты). ⚠️ ЧАСЫ: моё «откат → бустер истёк» УБИТО прогоном Меты (один
  скачок вперёд (NTP) → монотонная метка впереди → каждый будущий купленный
  бустер умирал бы мгновенно = возвраты). Заменено ПЕРЕАНКОРОМ ОСТАТКА:
  анти-чит тот же (ни секунды сверх, ассерт), система самолечится, купленный
  после инцидента работает. Превентивно: мерж берёт пару множитель+срок
  целиком у копии с ДАЛЬНИМ сроком (иначе облачный короткий x5 апгрейдил бы
  дневной x2); докупка при активном — только тот же множитель (время
  складывается) — КАРКАСНОЕ правило, в бандл-проходе заменится на «сильный
  играет, своё время складывается, слабый в очередь» (бандл нельзя отклонить).
  Начисление проверено серией 8+8 матчей: ratio ровно 2.00. Сьют PASS.
  Дальше у Меты: бандл-расширение (цены $4.99/9.99/19.99, инвентарь встрясок,
  no-Ad окно, паки-мертвы) + матрица №3 — постановки у неё в очереди.

- v1-test-130 · 2026-07-27 — Merge ИНТЕРФЕЙС: UI ПОДСКАЗКИ ЗА РЕКЛАМУ —
  фича владельца ЦЕЛИКОМ в проде (механика v129 + UI v130). Ветка
  interface-adhint (41d63a5, off v129, final+PASS). Три состояния кнопки в
  updateHUD: заряды есть → число; 0 + ролик доступен → ЛАЙМ «Ad» (ноды
  778:723/783:93, кнопка активна) → requestAdHint(); 0 + кап исчерпан → «0»
  и .off. ⚠️ ГРАБЛЯ ПОЙМАНА интерфейсом: .off несёт pointer-events:none и
  вешался по одному hints()<1 — кнопка «Ad» была бы нарисована, но ненажимаема;
  теперь .off только когда И зарядов нет, И ролик недоступен. Событийный
  updateHUD достаточен (последнюю трату и награду сопровождают вызовы) —
  отдельного таймера нет. Верификация РЕАЛЬНЫМ потоком: 3 траты → «Ad» →
  ролик (he 3→4, показ сразу) → второй (4→5) → кап 2 исчерпан → «0»/off.
  Сьют PASS (+ад-ассерты меты зелёные на сборке). Разрешена чистка 14 старых
  interface-worktree (~1.5ГБ; ветки сохраняются).

- v1-test-129 · 2026-07-27 — Merge МЕТА: ПОДСКАЗКА ЗА РЕКЛАМУ (фича владельца).
  Ветка meta-ad-hint (bd7bcf9, off v127, final+PASS). Зеркало встряски:
  hints()==0 → ad-ветка showHint → Ads.showRewarded (78-ads, оба режима) →
  addHints(1) → заряд сразу тратится на показ. Кап AD_HINTS_PER_LEVEL=2
  пер-уровневый. Телеметрия rw/hint.
  ⚠️ ЗАКРЫТА ДЫРА (нашла Мета): заряд подсказки ПОЖИЗНЕННЫЙ (he), и кап на
  level-объекте обходился бы Restart'ом (2 тапа → бесконечные ролики). Кап
  привязан к НОМЕРУ уровня (adHintLevelNo/adHintCarry, 40-items) — переживает
  Restart, восстанавливается только на новом уровне. Ассерты на оба исхода.
  АНТИ-ДЮП: кап НЕ в сейве вообще (мерж max вернул бы ролик); награда — в
  монотонную пару he/hs. Ассерт начисления по монотонному he (3→4), не по
  остатку (свежий заряд сразу тратится). КОНТРАКТ ИНТЕРФЕЙСУ: __game.
  adHintAvailable() (bool — вешать ли лайм-бейдж «Ad»), __game.requestAdHint()
  (запуск ролика, false если недоступно). UI-обвязка — за интерфейсом.
  ⚠️ ВЛАДЕЛЬЦУ (развилка, дефолт диспетчера): кап РЕАЛЬНЫЙ = 2 ролика/уровень
  (подсказки остаются ресурсом, по его же спеке «старт 3, +1 за уровень»);
  альтернатива «подсказка = 1 ролик без ограничений» (больше показов, но
  подсказки перестают быть ресурсом) — переключение 1 строкой, по слову.

- v1-test-128 · 2026-07-27 — Merge ИНТЕРФЕЙС: порция 2/2 батча-б (6 пунктов).
  Ветка interface-buttons (154de6a, off v127, final+PASS, 3 коммита).
  (3) Кошелёк: ТОЧНОЕ число если влезает по горизонтали (1466/99999 точные;
  1234567 → K на мобайле); дофиксен их же фит-чек (nowrap Get More, обрезка
  имени). (4) Полоска карточек ВСЕГДА зелёная #9ce52e в покое (частичная тоже
  — трактовка диспетчера; только-полные = 1 строка). (5) Моб-винскрин по
  783:711: колонка шапка→TOP ITEMS(3)→кнопки, winTopN 3/5 по брейкпоинту 768;
  CLEANED-конфликт ноды (43.8) vs спека +30% (57.2) решён в пользу новой
  спеки. (6) Полоса над «My collection»: перекрытие прозрачной шапки 64→24 —
  схлопнулась (след их же #124). (7) ГЛАЗА десктопа ~×2 (бенто 465px=0.83
  ширины, стакед 480=0.72), верх 16px; зрачки-слежение пережило БЕЗ JS
  (viewBox-единицы, MAXOFF 29 во всех раскладках); мобайл не тронут.
  (8) СИСТЕМНОЕ правило цвета кнопок: --btn-bg/--btn-fg в :root, html.night
  инверсия; светлая=тёмные кнопки, тёмная=светлые (переворот круглых из v127
  по прямой спеке); на правиле iconBtn/shakeBtn/scope/magnet; акценты-лайм не
  тронуты; правило в CLAUDE.md. Сьют PASS.
  ⚠️ ВЛАДЕЛЬЦУ: (a) глаза наверху + кнопка внизу = пустота в СЕРЕДИНЕ Play-
  карточки (следствие его разметки; лечится ещё крупнее глазами или возвратом
  заполнения); (b) правило кнопок применено к ИГРОВОМУ экрану — кнопки МЕНЮ
  (Resume/Get More/Boost/Next…) на своей светлой подложке не перекрашиваются;
  «и меню тоже» = отдельная отмашка; (c) частичная полоска тоже зелёная —
  если хотел только полные, 1 строка.

- v1-test-127 · 2026-07-27 — Merge ИНТЕРФЕЙС: КНОПКИ ПО 14 FIGMA-НОДАМ +
  CLEANED БЕЛАЯ +30% (порция 1/2 батча-б). Ветка interface-buttons (e54d658,
  off v126, final+PASS). (1) CLEANED: белая заливка, кегль 44→57.2 (+30%),
  viewBox 220→286×78; ⚠️ обводка 27→4 ПО ДЕЛЕГИРОВАНИЮ (белая заливка + белый
  контур 27 = сплошное пятно, буквы нечитаемы; лесенка 0/4/8/12/16 → 4 чистые
  жирные литеры, жирность несёт Black 900; предел читаемости 8). (2) КНОПКИ:
  14 нод = 4 типа + 2 бейджа (остальное день/ночь-дубли): Shake-пилюля #2a2935
  (ночь — инверсия), круглые пауза/подсказка + ТЕМА ПО ВРЕМЕНИ СУТОК (раньше
  всегда белые), бейдж зарядов #hintCnt лайм #c0ff47/чёрная цифра. ЗАМЕНЫ:
  ad-Shake фиолет → слово «Ad» лаймом в пилюле; бейдж тёмный → лайм; круглые
  белые → темируемые. НЕСМАТЧЕНО: бейдж «Ad» (778:723/783:93) — в игре нет
  «подсказки за рекламу», НЕ заведён (решение диспетчера: не изобретать фичу
  из бейджа; владельцу флаг — если хочет rewarded-подсказку, это к Мете/
  Интеграции). Сьют PASS. Ждём порцию 2/2 (точное число, зелёная полоска,
  мобильный винскрин 783-711) + ФИЗИКУ (взрыв-как-shake).

- v1-test-126 · 2026-07-27 — БОМБА: ЗОНА ПОРАЖЕНИЯ ×2 (батч владельца
  2026-07-27-б, конфиг-часть диспетчера). BOMB_RADIUS 2.86→5.72 (история:
  2.2 → +30% → ×2); кап BOMB_MAX=7 НЕ тронут (спека «не более 5-7») — радиус
  теперь накрывает почти всё дно, реальный ограничитель — кап. Сьют PASS.
  Остальное батча-б разослано: ИНТЕРФЕЙС — CLEANED +30% размер и БЕЛАЯ надпись
  (ответ владельца на вопрос А: вместо выбора ×3/18 — белым и крупнее) +
  ВИЗУАЛЬНОЕ ОБНОВЛЕНИЕ КНОПОК по 14 Figma-нодам (769-109…778-728, Dev Mode);
  ФИЗИКА — взрыв бомбы «похож на эффект shake» (толчок всей куче как встряска,
  под новую зону ×2; трактовка уточнена в постановке).

- v1-test-125 · 2026-07-27 — Merge ИНТЕРФЕЙС: batch124 (5 пунктов владельца).
  Ветка interface-batch124 (a8291e1, off v124, «final» + SUITE PASS до сдачи).
  (1) ПОБЕДА: обводка CLEANED 9→27 (×3 буквально), ★ 9→18 (×2), TOP ITEMS
  3→5 (WIN_TOP_N); полоски анимируются (0→[291,254,216,178,141]). (2) БЕЙДЖ
  ×N: z-index:2 поверх спин-канваса графики (корень «свинья поверх ×1» — канвас
  монтировался позже бейджа), 40% на ховере И тач-спине (.spinning). (3) ШАПКА
  КОЛЛЕКЦИИ: при скроле прозрачная, карточки видны за ней (margin-top −64/
  padding-top 64), мобайл не тронут (там display:contents). (4) СЧЁТЧИК ЖИВЫХ
  ПРЕДМЕТОВ в правом-верхнем мобайла УДАЛЁН совсем (#plSvg/#pairsLeft; на
  десктопе его и не было). Побочно: зазор CLEANED↔время +6px; свой скролл
  #winOverlay на низких вьюпортах (360×640), общий .overlay не тронут.
  Сьют PASS.
  ⚠️ ВЛАДЕЛЬЦУ (открыто, не блокирует): (А) CLEANED ×3 слипается в белую
  плашку (де-факто «пилюля», которой нет в Figma) — компромисс ×2(18) одной
  строкой, ждём слова; (Б) полоски TOP ITEMS у свежего игрока пустые из-за
  нулевого накопления (0/100) — данные, не анимация; захочет «всегда
  наливаются» — вопрос к Мете.

- v1-test-124 · 2026-07-27 — ВИТРИНА: ПРАВИЛО ШИРИНЫ 2/3, CAMNEAR ОТМЕНЁН
  (спека владельца «на десктопе и планшетах панель не убирать; убираем только
  если ширина экрана меньше [порога] — панель 1/3»). Скрытие по приближению
  камеры (camnear v1-v3) ПОЛНОСТЬЮ РЕТИРИРОВАНО: удалены tickCamNear +
  vitrineGap/camnearThreshold-хуки (99-main), html.camnear-правило (shell),
  camnear-проверка якоря тоста (85-hud). Видимость — ЧИСТЫЙ CSS
  @media (min-width:813px) (порог = 3×271px занятой полосы панели, замер) +
  тот же порог в vitrineOn(). pointer:fine СНЯТ — ПЛАНШЕТЫ видят панель
  (раньше только десктоп ≥1160+fine). Тесты: camnear-блок переписан на
  правило ширины (390 нет / 1024 планшет ЕСТЬ / зум 9 — СТОИТ / 800 скрыта).
  CLAUDE.md-контракт обновлён. Сьют PASS.
  Параллельно РАЗОСЛАН новый батч владельца ИНТЕРФЕЙСУ (5): доводка экрана
  победы (обводка CLEANED ×3, ★ ×2, анимация полосок TOP ITEMS, топ-5),
  бейдж ×N поверх модели + 40% на ховере, прозрачная подложка шапки коллекции
  при скроле, убрать счётчик пар в правом-верхнем мобайла.

- v1-test-123 · 2026-07-27 — ИНТЕРФЕЙС batch118 РЕРАБОТКА #4 (я влил d73f230 в
  v122 преждевременно — до перевода #4 на контракт v121; интерфейс переделал →
  833e02a). Взял ТОЛЬКО дельту (85-hud, diff main↔833e02a = 24 стр, остальные
  файлы совпали): (1) #4 msCardTapSpin теперь через хук ГРАФИКИ thumbSpinToggle
  (был thumbSpinStart) — единый контракт с ховером. (2) ФИКС УТЕЧКИ (нашёл
  адверс-ревью интерфейса): closeMainScreen не гасил тап-спин → offscreen-WebGL
  rAF (256²) крутился ВЕСЬ геймплей после Resume (карточка в display:none, guard
  parentNode не срабатывал). Теперь closeMainScreen явно зовёт thumbSpinStop +
  msTapSpinRestore. Применил дельту через git checkout 833e02a -- 85-hud (shell
  diff = только маркер версии, пропущен). Сьют PASS. УРОК: не мержить сдачу
  направления, пока контракт кросс-зоны не финализирован — ждать «final».

- v1-test-122 · 2026-07-27 — Merge ИНТЕРФЕЙС: batch118 (6 UI-пунктов, ЗАКРЫВАЕТ
  батч владельца). Ветка interface-batch118 (d73f230, off v118). #5 Boost без ★
  ('Boost 2K'). #6 зазор имя↔строка ÷2 (10→5px, открытые .msc-cnt И локнутые
  .msc-lvl). #8a Resume диагональный блик (::after msGlint ~4.5с, off под
  reduce-motion). #8b глаза меню живые: десктоп зрачки за курсором, тач —
  зациклённая оглядка, только при открытом меню, зрачок в белке. #11 мобайл HUD:
  над очками УРОВЕНЬ через #lvlSvg (перенос в правый стек), #tmSvg скрыт на обеих
  раскладках (ассерт «время скрыто» цел — НЕ репёрпоузил слот). #12 экран победы
  по Figma 778:732. #4 тап=спин (тач монтирует канвас/прячет img, второй тап стоп;
  десктоп hover как был). Мерж off v118-базы на v121 — графика (thumbSpinToggle/
  256px/frameCylinder) и мои #9/#10/бомба СОХРАНЕНЫ (разные регионы 85-hud),
  index пересобран. Сьют PASS. БАТЧ ВЛАДЕЛЬЦА 2026-07-27 ЗАКРЫТ (12/12).
  ⚠️ ВЛАДЕЛЬЦУ #12: заметная дивергенция — CLEANED был ЛАЙМ-ПИЛЮЛЯ с белым
  текстом, в Figma это обведённый лайм-ТЕКСТ без пилюли (фон сквозь буквы).
  Интерфейс перевёл на .otext по макету + «+1» 39px, веса Next/× Heavy, тост
  145px, тоньше обводка времени. Анимацию/подложку не трогал. Хочешь пилюлю
  назад — одна правка.

- v1-test-121 · 2026-07-27 — Merge ГРАФИКА: батч портретов+бомба (4 пункта
  владельца). Ветка graphics-portq (db771bf, off v117). #7 КАЧЕСТВО: буферы
  превью 132/176→256px (был апскейл-мыл). #3 ШРИНК-ФИКС: itemThumb кадрировал
  по силуэту, спин по цилиндру → hover уменьшал; обобщено frameCylinder(cam,mesh),
  статика=спин бит-в-бит (ассерт thumbFrames.equal). #4 ТАП=ХОВЕР: добавлен
  thumbSpinToggle(item,host) + хук __game.thumbSpinToggleKey (мобайл — тап
  заводит/снимает спин, размер не дёргается). #2 БОМБА: радужный bombMatcap
  (MeshMatcapMaterial, разводы плывут по нормали при качении). Мерж off v117-
  базы — мой +30% радиус (v118, 2.86), #10 (v119), #9 (v120) СОХРАНЕНЫ (git
  ours/разные регионы), index пересобран. Сьют PASS (+4 ассерта графики).
  ⚠️ ИНТЕРФЕЙСУ (контракт #4): тап карточки вешать на thumbSpinToggle, ховер —
  start/stop как был. ⚠️ ВЛАДЕЛЬЦУ (опц.): в Hard вуаль тускнит бомбу ~30%
  (hue цел); если хочешь always-on перелив как у камня — `&& !it.bomb` в
  60-access (зона физики).

- v1-test-120 · 2026-07-27 — Merge МЕТА: #9 МАТРИЦА ЦЕНЫ ОТКРЫТИЯ (level-scaled,
  утв. диспетчером по делегированию владельца; владельцу показана таблица на
  твик «ощущения»). Ветка meta-unlock-matrix (ada5fcc, off v119). Цена открытия
  типа: флэт 700 → BASE 800 + PER_LEVEL 200·level (L1=1000, L10=2800, L50=10800).
  Линейная под линейный доход ~700/ур → вмятина ~29% банка на любом уровне,
  «трудно, но достижимо». БЕЗОПАСНОСТЬ спайка структурная (спавн-гейт), не
  ценой — владелец может двигать BASE/PER_LEVEL свободно (2 константы). Tripwire
  (ранний спавн → цена от дистанции) в доке §v3. Путь траты/дуп не тронут.
  Сьют PASS (L1/L10/L50 = 1000/2800/10800). Батч владельца: #9,#10 закрыты
  (Мета), #1,#2-радиус (я); ждём Интерфейс (6 UI) + Графику (бомба-мат/#3/#7).

- v1-test-119 · 2026-07-27 — Merge МЕТА: #10 ДЕНОМИНАЦИЯ В ПРОЦЕССЕ (батч
  владельца «числа понятны и в процессе, и в подсчёте»). Ветка
  meta-denom-inprocess (5419e1f). Решение (c): поп = scoreShownDelta(before,after)
  = floor(after/10)−floor(before/10) — ТЕЛЕСКОПИРУЕТ, Σ попов = прирост чипа
  БИТ-В-БИТ, ноль дрейфа (против моих (a) floor-поштучно с ±1 и (b) деноминация
  констант с рябью). Ядровые константы не тронуты, деноминирован только показ
  5 точек (матч/промах/сюрприз/помол/rock). Клампнутые в ноль промахи/помол
  (ур.1 без штрафов, ур.2-5 кламп) больше не рисуют холостой «−10» — нет
  изменения чипа → нет попа. E2E-ассерт: поп «+2» == прирост liveBalance-чипа.
  Мерж off v115-базы — v116/v117/v118 (lastAction, boost-cap, таймер15/бомба)
  сохранены, index пересобран. Сьют PASS. #9 (матрица цен открытия) — Мета несёт
  отдельно на утверждение.

- v1-test-118 · 2026-07-27 — ТЮНИНГ (батч владельца, конфиг-часть, диспетчер):
  #1 таймер помола ПЕРЕТЮН Easy 15 / Hard 10 (было 10/3.3 после ÷3; 3.3 нервно) —
  MIXER_IDLE_EASY/HARD, тест-ассерт обновлён на 15. #2 радиус бомбы +30%
  (BOMB_RADIUS 2.2→2.86; кап BOMB_MAX=7 не тронут). Сьют PASS. Остальные 10
  пунктов батча разосланы направлениям: ГРАФИКА (#2 переливающийся материал
  бомбы, #3 ховер-уменьшение портрета, #4 тап=ховер спин, #7 качество+matcap
  коллекции); ИНТЕРФЕЙС (#5 убрать ★ в Boost, #6 ÷2 отступ карточки, #8 блик
  Resume + глаза за курсором/цикл, #11 мобайл LV не время, #12 стили винскрина
  по Figma); МЕТА (#9 матрица цен досрочного открытия level-scaled, #10
  деноминация поп-очков в процессе). Мержу по мере сдач.

- v1-test-117 · 2026-07-27 — Merge МЕТА: фикс капа буста по код-ревью. Ветка
  meta-economy-numbers (d65a866). BOOST_TIER_CAP=5 → макс покупка ЛЮБОГО типа =
  2000·(2^5−1)=62000=пак-анкор Mega, УНИВЕРСАЛЬНО (раньше несыгранный докупленный
  тип бустился до ~1M по кап-accTier). Гейт boostPrice/buyBoost на isTypeUnlocked
  (буст только открытого типа, reason 'locked'). Стале-комменты 00-config
  (awardStarsForWin→bankLevelScore) и leaderboardScore уточнены. Мерж off v113-
  базы meta-ветки — проверил, что мои v114 (winscreen ÷10, монотон-колонки) и
  v116 (lastAction-фикс) СОХРАНЕНЫ (git 3-way оставил ours для 85-hud/shell),
  index.html пересобран. Сьют PASS. Экономика закрыта и по адверс-ревью.

- v1-test-116 · 2026-07-27 — ФИКС ПО КОД-РЕВЬЮ (адверсариальное ревью сессии
  v108→v115, workflow 5 линз + verify). Оба «major» ОПРОВЕРГНУТЫ verify-фазой
  (чип-оверфлоу — не регрессия сессии и не воспроизводится; флейк эндшпиля —
  ложная посылка, updateMatchRadius зовётся вне physAwake-гейта). Взял ОДНУ
  реальную minor из моей зоны: idle-помол ДОГРЫЗАЛ кучу после снятия тупика
  (stats.lastAction застыл дольше idleLimit) — теперь на переходе тупик→снят
  сбрасываю lastAction, помол встаёт ровно с появлением достижимой пары
  (99-main:533). +2 ассерта (закрыл гэпы ревью): помол встаёт со снятием тупика
  (не догрызает); таймер помола Easy=10 (÷3). Сьют PASS.
  ⚠️ ОТДАНО МЕТЕ (её зона): кап буста по accTier(сумма), не boughtTier —
  не сыгранный докупленный тип бустится до ~1M против пак-якоря 62000; гейт
  buyBoost на isTypeUnlocked. ⚠️ ВЛАДЕЛЬЦУ: экран победы = прирост за уровень
  (÷10), а чип/кошелёк = бегущий тотал (та же единица, разная величина —
  осознанно). Остальное — nit-комменты/гэпы тестов, в отчёте.

- v1-test-115 · 2026-07-27 — Merge МЕТА: ТАБЛИЦА ЦЕН №2 + ФИКСЫ A/B (мои
  решения по делегированию владельца). Ветка meta-economy-numbers (6b45652,
  off v113). ЦЕНЫ (00-config, под ~700 деноминир./уровень): BOOST_PRICE_BASE
  2000 (2000/4000/8000/16000/32000), TYPE_UNLOCK_PRICE 700, REWARD_STARS_PER_AD
  70 + REWARD_DAILY_CAP 5, STAR_PACKS 3000/19000/90000 ($0.99/$4.99/$19.99).
  ФИКС A (лидерборд НЕ pay-to-win): поле Save.tu (пополнения, монотон, мерж max);
  кошелёк starBalance=se+tu−ss; РАНГ leaderboardScore=se−max(0,ss−tu) —
  купленное в позицию не идёт (⚠️ кошелёк и борд осознанно расходятся: борд =
  сыгранное подмножество; по слову владельца «баланс = ИГРОВОЙ счёт»). ФИКС B
  (цена буста от boughtTier, кап на accTier): макс типа = 62000 как в пак-анкоре,
  не 248k+. Docs STARS-STORE-ECONOMY §v2 помечена утверждённой, свёрнута в этот
  коммит (meta-economy-table НЕ мержу — поглощена). ФИКС C (кап рекламы на
  server-времени) — заметка в Междузонных для ИНТЕГРАЦИИ, bridge не тронут.
  index.html пересобран (мерж off v113 + v114 win-screen). Сьют PASS.
  Meta-нота «win-экран на level.starsWon» — уже закрыта v114 (Интерфейс
  денумировал экран победы через floor(score/SCORE_DENOM), та же величина).
  ⚠️ РЕАЛЬНЫЕ ЦЕНЫ ПАКОВ ($0.99/4.99/19.99) — дефолт для тестбилда, владельцу
  на финальную сверку перед лончем.

- v1-test-114 · 2026-07-27 — Merge ИНТЕРФЕЙС: экран победы ÷10 + монотонные
  колонки (мои решения по делегированию владельца). Ветка interface-balance
  (d6c8815, off v113). (1) ★ на экране победы = floor(score/SCORE_DENOM) —
  денумированный прирост = ровно в баланс (bankLevelScore) и на сколько подрос
  чип: «одно число» держится на чипе/кошельке/лидерборде/победе (замер ★113
  при сыром ~1135). Макет иначе не тронут (1/2/3★ не возвращён). (2) КОЛОНКИ
  МОНОТОННО (горб убран): база .ms-grid 2 кол на весь <800, 800-1079=3,
  ≥1080=4 (замер 390/750=2, 850/1000=3, 1200=4). Чип-свитч 85-hud:307 не
  тронут. Сьют PASS. Осталось от Меты: таблица цен + честный борд + фикс буста.

- v1-test-113 · 2026-07-27 — Merge МЕТА: ЕДИНЫЙ БАЛАНС (очки=звёзды=баланс=
  лидерборд, финализация владельца) + переключение чипа. Ветка
  claude/meta-unified-balance (ce00e16, off v110). balance = se−ss = ЕДИНОЕ
  число в чипе, меню, лидерборде; se = пожизненный накопл. счёт (÷10 деном.),
  ss = траты; анти-дюп сохранён (без max-мерж поля). bankLevelScore на победе
  (se += floor(score/10)); awardStarsForWin-рейтинг-дельта убрана. Открытие
  типа за баланс (purchaseUnlock, Save.uk) — раскрывает в коллекции/портрете,
  НЕ меняет пул спавна genLevel. Лидерборд-хендл = баланс. Миграция grandfathered.
  КРОСС-ЗОНА: чип в игре ($('score')) переключён с per-level stats.score на
  liveBalance() = баланс + незабанкованный счёт уровня (÷10) — чип=кошелёк=
  лидерборд, непрерывно через bankLevelScore. Сьют PASS (+8 меты).
  ⚠️ ВЛАДЕЛЬЦУ (провизорно, ждёт таблицу №2): деноминация ÷10 роняет числа
  чипа ~10×; SCORE_DENOM=10, TYPE_UNLOCK_PRICE=500, Boost-лесенка 1500-ладдер
  (~2 ур/буст) — цены под новый earn ~600-800/ур НЕ откалиброваны, Мета несёт
  таблицу. Экран победы (★ 5038) показывает СЫРОЙ счёт уровня — расходится с
  деноминир. приростом баланса (503): согласовать в таблице №2.
  ⚠️ ПАРАЛЛЕЛЬНЫЙ АКТОР: v112 закоммичен в этом же клоне НЕ мной (identity
  ikorzun@playgama.com, не мой gmail) — второй диспетчер-актор. Флагнул владельцу.

- v1-test-112 · 2026-07-27 — Merge ИНТЕРФЕЙС ×2: (1) ЭКРАН ЗАВЕРШЕНИЯ УРОВНЯ
  по Figma 778:732 (ветка interface-winscreen b3b0b6d, off v108) — стикер-экран
  (Level N + CLEANED + время + ★-счёт + «+1» Hint + Next + TOP ITEMS), backdrop
  не тронут, checkEnd не тронут (рисует из скрытых #winHolders), Next=#againBtn.
  (2) РЕСТРУКТУРА ГЛАВНОГО ЭКРАНА (ветка interface-msresp2 7d931d3, off v110) —
  бенто только ≥1080, стакед-колонка <1080 до 700px; коллекция <700=3/700-800=2/
  800-1080=3/≥1080=4; ЗАМЕНЯЕТ старую 700-900 боковую (eyes-fill из narrow-fill
  сохранён, ребейз с v109→v110). Оба региона (.ms-*, #winOverlay) не пересеклись
  с витриной v109/camnear/тупиком v111 — авто-мерж чистый. Сьют PASS.
  ⚠️ ВЛАДЕЛЬЦУ (от интерфейса, не блок): (a) винскрин по макету = СЧЁТ вместо
  1/2/3★ рейтинга, награда только «+1 подсказка» (нет «+N★» и «Watch ×2») —
  вернуть? (b) коллекция «горб»: 2 кол в 700-800, 3 в <700 и 800-1080 — сделать
  монотонно?

- v1-test-111 · 2026-07-27 — ТУПИК → ПОМОЛ-ВЫРУЧАЛКА, НЕ ПОРАЖЕНИЕ (решение
  владельца через AskUserQuestion: «помол = штраф, не смерть»). Раньше тупик
  (нет достижимых пар + нет встрясок) через ~1.2 с звал showLose(). Теперь тот
  же путь ставит level.deadlock → кадровый gate гонит mixerGrind, разбирая кучу,
  пока не появится достижимая пара (флаг снят при ap>0 / возврате встрясок).
  Всегда сходится (при <2 любого типа → финал-зачистка → победа; проверено
  matchRadius=-9 «навсегда» разбирает до победы). Цена — очки (−20/помол) =
  лидерборд. showLose() из тупика больше не вызывается (единственный был путь) →
  уровень в норме НЕПРОИГРЫВАЕМ; UI поражения жив, но недостижим. Правки:
  40-items (level.deadlock:false), 99-main (gate + 600-мс тик set/clear),
  test.js (тупик-тест переписан на mill-rescue; ads-Retry-тест кажет loseOverlay
  напрямую). Замер: {lose:"", deadlock:true, over:false, grinding:"Grinding",
  144→140}; полный прогон → победа, 4 встряски. Сьют PASS.
  ⚠️ СЛЕДСТВИЕ ВЛАДЕЛЬЦУ: плейсмент «Continue за рекламу» (loseAdContinue) в
  норме больше не всплывает — экран поражения недостижим. Rewarded ×2/встряска/
  металлоискатель не затронуты. Флагнул в CLAUDE.md и владельцу.

- v1-test-110 · 2026-07-24 — Merge ИНТЕРФЕЙС: БЛОК ГЛАЗ ЗАПОЛНЯЕТ КОЛОНКУ
  ПО ВЕРТИКАЛИ (без дыры снизу). Спека владельца «блок с гибким размером по
  высоте — это блок с глазами; не оставляя дыр, заполняй им пространство по
  вертикали». Ветка claude/interface-narrowfill (e67b18e, база v107) —
  ТОЛЬКО .ms-* CSS в shell.html, регион НЕ пересекается с витриной/camnear,
  авто-мерж без конфликтов. Фикс: ≥700 — .ms-eyes flex:1 (тянет бокс глаз,
  центрируя зрачки), .ms-playbtn flex:none у низа; узкий 700-900 — 1fr переехал
  с ряда dev на play (карточка растёт донизу), .ms-play min-height 236→0.
  Проверка диспетчера (скрины 850/1000): глаза заполняют карточку, Resume у
  низа, пусто́т нет; сьют PASS. Мобайл <700 не тронут.

- v1-test-109 · 2026-07-24 — ВИТРИНА ТОЛЬКО ВЕРТИКАЛЬ + ТАЙМЕР ПОМОЛА ÷3
  (две прямые спеки владельца). (1) ВИТРИНА: «горизонтальной анимации быть
  не должно; вертикально меняй строки через фейд». Три анимации в shell.html
  переведены translateX→translateY+opacity: .vcell.out (уход строки вверх+гаснет),
  vIn (приход снизу), vReveal (каскад разворота снизу). Доказано computed-стилями:
  MAX|translateX|=0.00px по всем трём, вертикаль есть. (2) ПОМОЛ: «ускорить таймер
  начала помола в 3 раза (нет шейков, не дотянуться)». MIXER_IDLE_EASY 30→10,
  HARD 10→3.3 (00-config). Замер: помол на easy теперь на 10-й секунде.
  АДВЕРС-РЕВЬЮ (workflow, 4 линзы) вскрыла и я ЗАКРЫЛ побочку: idleLimit стал
  ≤ GRIND_LEAD(10) → на Hard небо было всегда ≥67% красным. Фикс — кап
  lead=min(GRIND_LEAD, idleLimit) в 99-main (Easy бит-в-бит, Hard честная рампа
  0→1; скрин hard-early подтвердил нейтраль). Обновлены устаревшие 30/10 в
  CLAUDE.md и комментарий test.js. Сьют PASS.
  ⚠️ НА СТОЛ ВЛАДЕЛЬЦУ (ревью, НЕ трогал без спроса): (a) быстрый таймер штрафует
  и ОБЫЧНОЕ обдумывание, не только stuck; ровно его случай «нет шейков+не
  дотянуться» сейчас = экран ПОРАЖЕНИЯ через ~1.2с, а не помол — предложен гейт
  «ускорять только при shakes==0»; (b) tier-up всплывашка #tierToast всё ещё
  въезжает сбоку (отдельный узел, не список) — распространять ли «без горизонтали»
  на неё; (c) ~1px пружинный доводчик в vIn/vReveal (незаметно) — сменить на
  ease-out для идеально чистого фейда, если строг.

- v1-test-108 · 2026-07-24 — CAMNEAR v3: УСТОЙЧИВЫЙ КРАЙ КУЧИ (владелец
  подтвердил: «устойчивый край кучи даёт край по предметам без моргания»).
  tickCamNear теперь считает край не по чаше, а по КУЧЕ предметов через
  устойчивый радиус охвата hullR = max(hypot(x,z)+r) по живым не-сюрприз/
  бомба/камень предметам; левую точку (0,cy,0) − hullR·right проецируем в
  экран. Радиус охвата инвариантен к повороту камеры → замер вращения (8
  углов): РАЗБРОС 0px (было 70px по мгновенному силуэту — оно и моргало).
  ⚠️ ПОРОГ поднят 42/49 → 130/150: куча уже чаши, её край дальше от панели
  (в покое зазор ~261, на макс.зуме camR 9 ещё 97), прятать «при касании»
  по куче нельзя — прячем когда куча заметно наехала. Точную линию владелец
  крутит на живой игре через __game.camnearThreshold(hide[,show]) — размер
  кучи плавает по уровням, вслепую не откалибровать. Контракт с ИНТЕРФЕЙСОМ
  не тронут: класс html.camnear тот же сигнал, CSS-фейд менять не надо.
  Сьют PASS (vitrineGap-ассерт зелёный). Замер: покой видна, camR 9 скрыта.

- v1-test-107 · 2026-07-24 — ИНТЕРФЕЙС: УЗКИЙ ДЕСКТОП 700-900 + ВСЯ
  PLAY-КАРТОЧКА КЛИКАБЕЛЬНА (2 спеки владельца). (1) 700-900: коллекция
  МАКС 2 колонки до 900 (3 с 901, 4 с 1080); ниже 900 левая часть
  ВЕРТИКАЛЬНО — шапка коллекции полосой сверху, слева стопка [Play(поджатые
  глаза, min-height 236) → Settings → Banner ПОД настройками → dev], справа
  коллекция 2 кол; >900 бенто side-by-side как был. (2) вся .ms-play →
  Resume: хендлер на карточку, клик по кнопке всплытием (отдельный хендлер
  кнопки снят — иначе двойной genLevel), cursor:pointer. Проверка
  диспетчера: 750/900=2кол, 950=3, 1200=4, переполнения 0; клик по ПУСТОМУ
  верху карточки → меню закрыто (resume), уровень НЕ регенерён. Скрин 850 —
  вертикальная стопка + коллекция 2 кол, музыка-слайдер виден. Сьют 122 PASS.
  ⚠️ ТОЛКОВАНИЕ (интерфейс отметил): «всяка область тапабельна» истолковано
  как ВСЯ Play-КАРТОЧКА (на скрине была она). Если владелец имел в виду
  ЛЮБУЮ пустую область экрана — расширить (но НЕ на контролы коллекции/
  слайдеры/Boost/Get More). Вопрос владельцу.
  ⚠️ CAMNEAR (запрос владельца «скрывать по синей линии = левый край объекта»)
  — НА ПОДТВЕРЖДЕНИИ ВЛАДЕЛЬЦА: замер вскрыл, что синяя линия 42% = левый
  край КУЧИ ПРЕДМЕТОВ (не чаши, та на 29%); прятать по куче вернёт МОРГАНИЕ
  (прошлая жалоба). Предложен компромисс — устойчивый край кучи (по размеру,
  не мгновенному силуэту). Правку tickCamNear НЕ делаю до его «да».

- v1-test-105 · 2026-07-24 — ИНСПЕКЦИЯ ПО ПРИКАЗУ ВЛАДЕЛЬЦА («проверь все
  чаты, инспекция, тест, мерж»): забраны ДВЕ ветки интерфейса + укреплён
  флейк вуали. АУДИТ: невлитого — только 3 ветки; physics-rocks (6 коммитов)
  = ТОЛЬКО приборы калибровки (forceTypes/dbgTypes/pairsBreakdown), осколки
  и вес давно в main через cherry-pick — НЕ мержу (инструментальное, не для
  сборки); worktrees чисты.
  (Г) BOOST-АНИМАЦИЯ: на buyBoost.ok карточка .boosted — полоска зелёная
  (#9ce52e) + доливка .55с, 12 DOM-частиц (.joyp, лайм+белые, .72с,
  самоудаление); reduced-motion гасит. ⚠️ Доливка ЦЕЛЕБРАЦИОННАЯ (текущая
  доля в зелёном), НЕ рост: buyBoost меняет mult, не count/next — полоска по
  ЗАРАБОТАННЫМ ступеням (вопрос владельцу а/б открыт). Проверка диспетчера:
  клик Boost → множитель ×1→×1.25, цена 1.5K→3K, .boosted+12 частиц, через
  1.2с 0/0 (переживает перерисовку карточки, утечки нет).
  (#ghost) ГХОСТ ЗАКРЫТЫХ: itemThumb(thumbItemForKey(r.key, true)) на
  локнутые вместо буквы; hover-спин НЕ на закрытых. Проверка: 55/55
  локнутых — силуэт-img, 0 букв; открытые цветные в новом ракурсе.
  ⚠️ ФЛЕЙК ВУАЛИ УКРЕПЛЁН (флагнул интерфейс, «181→181» 2/5 прогонов): тест
  «снятие пина» ждал фикс.паузу 700мс, а вуаль возвращается ЛЕРПОМ 0.25с +
  refresh-тик — на медленном прогоне не успевало. Переписан на ожидание
  условия (veiled < пик) с потолком 3с, как флейки осколков/радиуса. Прогоны
  зелёные (181→118/121/123). Сьют 122 PASS.

- v1-test-104 · 2026-07-24 — ИНТЕРФЕЙС: ВИТРИНА ТОП-5 ПО ПРОГРЕССУ (спека
  владельца «топ-5 видимых, ранг среди ВСЕХ, обгонщик вытесняет»). РОВНО 5
  строк, все типы уровня ранжируются по vitFrac (прогресс к след.ступени,
  тай-брейк accCount): vitAll+vitRankedAll+vitReconcile. Скрытый тип,
  набравший больше, ВХОДИТ в топ-5 (уезд .out 0.28с выбывшего → перезаполн.
  слота → въезд пружинкой .in), неизменившийся слот — vitUpdateCell (полоска
  +.hit-пульс). Старый механизм ротации (vitRotate/Resort/Done/AliveSet)
  удалён. ⚠️ RECT #vitrine БИТ-В-БИТ: РОВНО 5 ячеек всегда (LEVEL_TYPES_MIN
  =9≥5), смена = контент ВНУТРИ слота, не add/remove → высота #vGrid не
  меняется, camnear 42/49 и якорь тоста целы. ⚠️ ЗАКРЫВАЕТ «весь замес выше
  вьюпорта на глубоких» по построению (5 строк = фикс-высота). Проверка
  диспетчера: ур.1 топ Watermelon·Banana·Orange·Bee·Crab; накопил скрытому
  animalpig 95 → Pig ВОШЁЛ первым, Crab вытеснен, строк 5, rect 624/268
  identical до/после. Сьют 122 PASS.
  ⚠️ ДВА ВОПРОСА ВЛАДЕЛЬЦУ (интерфейс поднял): (1) собранный тип НЕ уходит
  из витрины (ранг по накоплению, не «жив в чаше») — это и есть спека «ранг»,
  подтвердить; (2) BOOST-АНИМАЦИЯ: полоска = ЗАРАБОТАННЫЙ прогресс (accCount/
  next), а Boost копит МНОЖИТЕЛЬ отдельно (boostTier) → после buyBoost
  полоска НЕ двигается; «заполнение по прогрессу» = целебрация текущей доли
  в зелёном + частицы, ИЛИ владелец хочет чтобы Boost РЕАЛЬНО двигал полоску
  (иная механика Boost). Интерфейс делает целебрацию до ответа.

- v1-test-103 · 2026-07-24 — ГРАФИКА: РАКУРС ПОРТРЕТА + ГХОСТ-МЕХАНИЗМ
  ЗАКРЫТЫХ (спеки владельца). (1) РАКУРС «лёгкий подъём вправо-вверх»:
  прежний top-down +0.42/+0.65 «нырял в нижний угол», новый −0.15/−0.6
  (взгляд снизу + 3/4). ⚠️ Требование «статика==спин» решено СТРУКТУРНО:
  поза в ЕДИНОМ источнике PORTRAIT_TILT_X/PORTRAIT_YAW0, им пользуются И
  itemThumb (статика) И спин — развести нельзя по построению; подмена
  img→канвас (v101) держится бесшовно. Ракурс ЖИВОЙ сразу (все портреты).
  (2) ГХОСТ — ⚠️ ОТМЕНЯЕТ «локнутые буквой» (было моё предположение,
  владелец просит силуэт): thumbItemForKey(key, true) → полупрозрачный
  (opacity 0.42) + матовый + бесцветный (uVeil-десат v84 на максимум,
  переиспользован не изобретён) силуэт, свой кэш '@g'. Грабля: userData.
  shader ставится в onBeforeCompile на ПЕРВОМ рендере — thumbR.compile
  перед чтением, иначе гхост выходил ЦВЕТНЫМ. МЕХАНИЗМ — ждёт обвязку
  интерфейса (повесить на локнутые вместо буквы = «заполнить коллекцию
  моделями»). Хуки: thumbURL(key,ghost), setPortraitPose(tx,yaw) — ⚠️ НЕ
  «тюнинг»: несущий тест-хук, стережёт инвариант позы (v1-test-146).
  Проверка диспетчера: цветной арбуз (геройский ракурс) vs гхост — серый
  полупрозрачный силуэт, форма читается, «покедекс». Перф: гхост-батч 55
  закрытых 263мс cold/0 warm, кэш раздельный. 3 ассерта. Сьют 122 PASS.

- v1-test-102 · 2026-07-24 — ИНТЕРФЕЙС: РЕЗИНОВОСТЬ МЕНЮ (спека владельца
  «мобильный вид <700px; до 700 сетка коллекции 4→3→2»). Порог мобайл/
  десктоп 1160→700. Сетка коллекции ступенями: 2 (700-859) / 3 (860-1039) /
  4 (≥1040); мобайл <700 = 3. Боковые колонки бенто гибкие (настройки ФИКС
  292 ради слайдера, баннер minmax(160,260), коллекция minmax(220,1fr)) —
  иначе 292+260 съедали ширину. ПОПУТНО 2 переполнения (вскрыл низкий
  порог): .msc min-width:0 (грид-карточки не ужимались, правый столбец
  резался, collClip 41→0) и .ms-collhead flex-wrap (профиль уезжал за
  вьюпорт → под заголовок на узкой; ≥~1080 строкой как было).
  ⚠️ #vitrine (игровой экран) НЕ тронут — его порог 1160 отдельный, это про
  МЕНЮ; camnear цел. Проверка диспетчера: 680=3кол, 700/800=2, 860/1000=3,
  1040/1160=4, hOverflow 0 и Get More виден на всех, 1160 прежний десктоп.
  Сьют 122 PASS. Интерфейс параллельно (отдельные ветки): top-5 витрины,
  Boost-анимация; графика — гхост закрытых + ракурс.

- v1-test-101 · 2026-07-24 — ИНТЕРФЕЙС: ФИКС ДВОЕНИЯ ПОРТРЕТА ПРИ HOVER
  (жалоба владельца). Спин-канвас графики прозрачный (alpha) — под
  вращающимся силуэтом просвечивал статический <img>. Лечение в зоне
  интерфейса (img — его): mouseenter прячет img (visibility:hidden, rect
  НЕ схлопывается — канвас на нём), mouseleave возвращает; спин стартует с
  угла статики → подмена бесшовна. Проверка диспетчера: hover → img hidden
  + канвас есть; leave → img visible + канвас снят; двоения нет. Сьют
  122 PASS.
  ⚠️ ДВА ЗАПРОСА ВЛАДЕЛЬЦА ГРАФИКЕ (машинерия портретов, релей): (1) РАКУРС
  — текущий наклон уводит модель в нижний-правый угол; владелец выбрал
  лёгкий подъём ВПРАВО-ВВЕРХ, потом спин по горизонтали. Перетюнить
  itemThumb rotation(0.42,0.65,0) + SPIN_TILT_X/SPIN_YAW0 СОГЛАСОВАННО (углы
  статики и спина обязаны совпадать — иначе скачок при hover). (2) ГХОСТ
  ЗАКРЫТЫХ — ⚠️ ОТМЕНЯЕТ МОЁ прежнее «локнутые буквой ради анти-спойлера»
  (это было МОЁ предположение, не спека): владелец прямо просит у закрытых
  типов ПОЛУПРОЗРАЧНЫЙ+МАТОВЫЙ+БЕСЦВЕТНЫЙ гхост модели вместо буквы
  («заполни коллекцию доступными моделями»; силуэт непойманного, как
  покедекс). Графика даёт рендер-режим гхоста для thumbItemForKey, интерфейс
  вешает на локнутые. «Музей» владельца = сетка My collection (Museum-оверлей
  недоступен — вход был в скрытой карточке паузы; владелец подтвердил).
  ИНТЕРФЕЙС далее (отдельными ветками): резиновость <700px (мобвид + сетка
  4→3→2 колонки) и top-5 витрины.

- v1-test-100 · 2026-07-24 — ГРАФИКА+ИНТЕРФЕЙС: ВРАЩЕНИЕ ПОРТРЕТОВ В
  КОЛЛЕКЦИИ (спека владельца «на витрине при наведении модель медленно
  вращается», объём B — портреты ВСЕМ открытым). Собрано ОДНОЙ веткой:
  интерфейс ответвился от graphics-thumbspin, положил обвязку поверх
  механизма — без ручного мержа. ГРАФИКА (механизм): itemMaterial вынесен
  из makeItem (портрет и боевой предмет — ОДИН материал, без дрейфа);
  thumbItemForKey(name) — портрет-меш по ключу БЕЗ тела Rapier, кэш общий с
  боевым (key='T'+idx); живой офскрин-рендер под курсором (не спрайт-лист),
  один spinR, rAF ТОЛЬКО на hover; Y-инвариантная рамка по охватному цилиндру
  (camW константна за оборот — не «дышит»). ИНТЕРФЕЙС (обвязка):
  буква→портрет у открытых (`itemThumb(thumbItemForKey(r.key))`), локнутые —
  буква; hover-спин на mouseenter/leave ТОЛЬКО при canHover (hover:hover +
  pointer:fine — не на тач). Гейт открытости — ЛОКАЛЬНЫЙ locked
  (unlockedTypeCount), не meta-поле (ветка от graphics без ручки меты).
  ПРОВЕРКА ДИСПЕТЧЕРА (живой hover, ур.30): 38 открытых с 3D-моделью, 55
  локнутых с БУКВОЙ (0 моделей — АНТИ-СПОЙЛЕР держится); наведение →
  spinState rafOn true, арбуз реально повёрнут между фазами, бейдж ×1
  поверх; mouseleave → rafOn false (ноль стоимости вне hover). Перф
  (интерфейс, ур.50, 58 типов): построение cold 66мс/warm 1мс, хитча нет,
  ленивое построение не понадобилось. Мобайл: canHover=false, hover нет,
  портреты статические. 7 ассертов спина. Сьют 122 PASS.

- v1-test-99 · 2026-07-24 — ИНТЕРФЕЙС: BENTO-ЭКРАН РЕЗИНОВЫЙ + 4 правки
  (пачка владельца). (1) главный/пауза резиновый — .ms-wrap 100vh+overflow
  hidden, коллекция скроллит ВНУТРИ грид-ячейки (min-height:0+overflow-y);
  (2) ползунок из макета 763:1428 — зелёная #9ce52e заливка + белый бегунок
  (--fill WebKit, ::-moz-range-progress FF), ширина = свитчеру через общую
  --ms-ctl; (3) локд-карточка «Level N» шрифтом счётчика (.msc-lvl=.msc-cnt
  10px/700/rgba(58,64,104,.6)); (4) зазор строк витрины #vGrid 12→6; (5)
  сортировка витрины по прогрессу УБЫВАНИЕМ (vitFrac к след.ступени, кап=1),
  FLIP-анимация на .vcell — ⚠️ rect #vitrine НЕ тронут (camnear-порог 42/49
  и якорь тоста целы, сьют подтвердил). Проверка диспетчера: bento
  wrapH==vh(900) на 1440, скролл страницы 0, гориз.переполнение 0; скрин —
  раскладка, крупные портреты, зелёный ползунок. Сьют 115 PASS.
  ⚠️ ДВА ТОЛКОВАНИЯ НА ПОДТВЕРЖДЕНИЕ ВЛАДЕЛЬЦА (интерфейс отметил): (а)
  «отступ строк /2» отнесён к ВИТРИНЕ (#vGrid) — не к настройкам/коллекции;
  (б) «резиновый по ширине» = во ВСЮ ширину вьюпорта (max-width снят) — не
  прежний потолок по центру. Оба обратимы одной правкой.

- v1-test-98 · 2026-07-24 — ИНТЕРФЕЙС: ВИТРИНА ДЕНЬ/НОЧЬ (уточнение владельца
  «днём и утром БЕЛАЯ, вечером и ночью ЧЁРНАЯ» — ОТМЕНЯЕТ v95 «тёмная
  всегда»; прежнее прочтение «тёмная тема для блока» было неполным).
  ДЕНЬ/УТРО (база): белая rgba(255,255,255,.16)+свечение, текст #3a4068,
  трек/бейдж белые. НОЧЬ (html.night по часам 18..5, тот же сигнал что у
  Shake/неба): тёмная rgba(42,43,50,.4)+border, текст #fff, трек/бейдж
  тёмные, тост rgba(0,0,0,.2). Зазор 12px и fit-content сохранены в обеих.
  Проверка диспетчера: день(13ч)/утро(8ч) — панель .16 белая, текст
  rgb(58,64,104); ночь(22ч) — 42,43,50 тёмная, текст #fff. Мета-ручка
  unlockedTypes мержем не откачена (4 вхождения в сборке). Сьют 115 PASS.
  ИНТЕРФЕЙС ведёт БОЛЬШУЮ пачку владельца отдельной веткой (bento резиновый
  на вьюпорт, ползунки 763:1428, «Level N» шрифтом счётчика, зазор строк /2,
  витрина сортируется по прогрессу с динамич. пересортом) — сдаст частями.

- v1-test-97 · 2026-07-24 — МЕТА: РУЧКА ОТКРЫТОСТИ ТИПОВ (для портретов
  коллекции, вариант B вращения). __game.unlockedTypes() → [type.name],
  isTypeUnlocked(name) → bool; accSnapshot дополнен per-тип полем `unlocked`
  (аддитивно). Правило ЕДИНОЕ с genLevel: 9+levelNum−1 по порядку TYPES,
  levelNum = достигнутый максимум — портрет появляется ровно когда тип
  впервые может выпасть, спойлера прогрессии нет. Графика строит меш-по-типу
  ТОЛЬКО для открытых (диспетчерское подтверждение: локнутые остаются серой
  буквой+«Level N»). 4 ассерта (ур.1: 9 типов; TYPES[0] открыт/TYPES[20]
  закрыт; ур.15: 23; несуществующий не открыт). Сьют 115 PASS.
  ⚠️ ДУБЛИРОВАНИЕ К СВЕДЕНИЮ (мета отметила, НЕ чинила — чужая зона):
  интерфейс имеет приватную unlockedTypeCount() в 85-hud с ТЕМ ЖЕ правилом;
  мета свела бит-в-бит. Две функции одного правила. Единый источник
  (интерфейс переключит свою на публичную мету) — НЕ срочно, не блокирует;
  зафиксировано, чтобы при расхождении правила открытости чинить ОБЕ.

- v1-test-96 · 2026-07-24 — ИНТЕГРАЦИЯ: МЕЖСТРАНИЧНАЯ ТОЛЬКО НА ПОБЕДЕ
  (уточнение владельца «в игре нет поражения... реклама только между
  уровнями раз в 5, либо когда кончились подсказки и шейки»). ФИКС = ОДНА
  СТРОКА: `Ads.maybeInterstitial()` убран из loseAgainBtn (90-input:128,
  Retry из тупика), оставлен только на againBtn (победный переход).
  Комментарий-щит на строке + переписан 78-ads («только победный»).
  ЗАДАЧА B (hints в гейт тупика) — вывод (б), КОДА НЕ ПОТРЕБОВАЛА: подсказка
  в тупике бесполезна (availablePairs===0 → findHintGroup null → showHint
  даже не тратит заряд) и разобрать тупик физически не может, только
  встряска; добавлять hints()===0 в гейт было бы ВРЕДНО (заперло бы игрока
  с бесполезными тут подсказками). Гейт «нет ходов + нет встрясок» уже и
  есть «выхода нет». ⚠️ ЗУБЫ АССЕРТА ПРОВЕРЕНЫ РЕВЕРСОМ: временно вернули
  вызов → оба ассерта упали (ролик 3→4, счётчик сброшен), восстановили →
  зелено. Новый ассерт кликает РЕАЛЬНУЮ loseAgainBtn в форс-тупике.
  ИТОГ ПОВЕДЕНИЯ: межстраничная только на победном переходе раз в 5;
  в тупике — rewarded Continue по выбору игрока; на Retry-после-тупика
  рекламы НЕТ вовсе. Сьют 111 PASS.
  СТАТУС hover-вращения портретов (спека владельца, вариант B — портреты
  всем открытым): контракт согласован (графика — механизм thumbSpinStart/
  Stop в 85-hud, живой офскрин-рендер + Y-инвариантная рамка; интерфейс —
  hover-обвязка на #msGrid, гейт по наличию портрета, авторасширяется с A
  на B; мета — ручка «открытые типы»). Кода ещё нет, направления кодят.

- v1-test-95 · 2026-07-24 — ИНТЕРФЕЙС: ТЁМНАЯ ТЕМА ВИТРИНЫ+ТОСТА (всегда) +
  зазор 12px + ДИСПЕТЧЕР: порог camnear 42/49. Две независимые части,
  скоординированы как первый camnear-контракт.
  ИНТЕРФЕЙС (CSS, ветка claude/interface-dark): тёмная тема ВСЕГДА
  (отменяет прежний html.night-сплит) — жалоба владельца была на светлую
  панель над оранжевой лихорадкой, теперь тёмная над ЛЮБЫМ полем. Витрина
  776:649: bg rgba(42,43,50,.4)+border, текст/буква #fff, трек rgba(0,0,0,.3)
  заливка #23ce1a, бейдж rgba(0,0,0,.4), inset-свечение снято, правила
  html.night .v* удалены. Тост 776:701: пилюля rgba(0,0,0,.2) без белого
  свечения, лаймовый бейдж как был. Зазор прогресс↔множитель 12px: .vbody
  фикс 140, .vmult margin-left:4 (=12 с gap 8), убран margin-left:auto,
  width:fit-content → панель 291.
  ДИСПЕТЧЕР (моя строка tickCamNear): порог 60/49→42/49 (спека владельца
  «прятать ближе к чаше, ~30% короче») — панель висит дольше, прячется у
  самой чаши. Монотонность 42<49 сохранена. Проба по ширинам: 1200 зазор
  24 → скрыта, 1280 зазор 64 → видна (старый порог 60 её резал раньше),
  1440 зазор 144 → видна. Тёмная панель над утренней лихорадкой читается
  (скрин). Сьют 113 PASS.

- v1-test-94 · 2026-07-24 — ИНТЕГРАЦИЯ: РЕКЛАМА КАЖДЫЙ 5 УРОВЕНЬ + доки
  прогресс-сейва (решения владельца). КАДЕНЦИЯ: INTER_MIN_WINS/INTER_GAP_MS
  («каждые 2 победы / раз в 3 мин») → INTER_EVERY_LEVELS=5; счётчик двигают
  ТОЛЬКО победы (noteWin), поражение/повтор не наращивают. Композится с
  пауза/мьютом v85 без правок (меняется лишь КОГДА зовётся showInterstitial).
  ⚠️ ЧЕСТНАЯ РАМКА: это НАШ ЗАПРОС, Poki/CrazyGames самопейсят и вправе
  пропустить — «каждый 5» верхняя граница, не гарантия.
  ⚠️ АДВЕРС-РЕВЬЮ ИНТЕГРАЦИИ НАШЛО ЛОЖНЫЙ КОММЕНТАРИЙ (не баг поведения):
  комментарий обещал «loseAgainBtn ролик не показывает», а уровень меняют
  и МИМО maybeInterstitial (msPlayBtn, pauseRestart — genLevel без сброса
  счётчика), так что накопленный показ может выстрелить на Retry после
  смерти. Поведение ДОПУСТИМО (площадки разрешают межстраничную на смерти/
  рестарте) и держит «раз в 5 побед» — исправлен комментарий + добавлен
  ассерт на отложенный показ. ⚠️ Если владелец захочет СТРОГО «ролик только
  на победе» — правка в loseAgainBtn (зона 90-input), по слову.
  ПОДПИСКА (вторая половина) НЕ делалась — блокеры на владельце: точка
  гварда готова (1 строка), флаг «реклама снята» = покупка в сейве (запрос
  МЕТЕ по решению), ⚠️ платежей через Bridge у POKI НЕТ ВОВСЕ (Yandex есть,
  Playgama/CrazyGames условно/Xsolla).
  ПРОГРЕСС-СЕЙВ (задача 2, решение владельца «технологии площадки, не
  Google»): КОДА НЕ ТРЕБУЕТСЯ — существующее облако bridge.storage (чинено
  и подтверждено на боевом SDK v81). docs/PROGRESS-SAVE.md фиксирует
  решение. ⚠️ У POKI и GameDistribution облачного сейва в Bridge НЕТ вовсе
  — межустройственного прогресса там нет ни с Google, ни без. Google-вход
  сам прогресс не хранит (нужен свой бэкенд), осмыслен только для standalone.
  5 ассертов каденции. Сьют 113 PASS.

- v1-test-93 · 2026-07-24 — ИНТЕРФЕЙС: ШАПКА ПРОФИЛЯ В ПРАВУЮ КОЛОНКУ НА
  ДЕСКТОПЕ (решение владельца по прошлому вопросу — строго по макету
  763:1031; отменяет прежнюю «полосу сверху»). Профиль (msStars/msGetMore/
  msUser) — ЕДИНЫЙ узел, перенос РАСКЛАДКОЙ, не копией: обёртка .ms-collhead
  на мобайле `display:contents` (профиль пилюлей наверх, заголовок к сетке —
  мобайл НЕ изменился), на десктопе реальный флекс-ряд в шапке правой
  колонки (заголовок слева, профиль справа без пилюли, порядок
  звёзды→Get More→аватар, имя скрыто). Проверка диспетчера: десктоп 1440 —
  звёзды X 1103 (правая половина), collhead display flex; мобайл 393 —
  звёзды Y 31 (шапка сверху), display contents; ошибок 0 на обоих. Скрин
  десктопа снят — «My collection · ★5K · Get More · аватар» в одну строку,
  сетка с Boost-ценами и локнутыми «Level N»/Open. Сьют 108 PASS.
  ИНТЕРФЕЙС продолжает НОВОЙ пачкой владельца (отдельная ветка): тёмная
  тема витрины+тоста по Figma (776:649 / 776:701), camnear короче на ~30%
  (панель висит дольше), зазор прогресс↔множитель 12px.

- v1-test-92 · 2026-07-24 — ИНТЕРФЕЙС: ВАЛЮТА И BOOST В МЕНЮ (живые ручки
  меты, плейсхолдеры «Coming soon» сняты). Шапка показывает starBalance(),
  а НЕ totalStars — решение интерфейса, верное: рейтинг уровней отдельный
  и не тратится, показывать его как валюту было бы враньём. onStarsChange
  живо обновляет и баланс, и цены без переоткрытия экрана. BOOST: цена из
  accSnapshot().price; при affordable:false кнопка ГАСНЕТ (не обещаем
  покупку, которую buyBoost отвергнет), при price===null — «Max» + disabled;
  ответы buyBoost разведены на тосты (insufficient / capped). Всё одним
  вызовом снапшота.
  ПРИЁМКА ДИСПЕТЧЕРА (независимо, сквозной сценарий): баланс 20000 → 18500,
  списано ровно 1500 = цена 1-й ступени, ступень 0 → 1, множитель 1.25,
  следующая цена 3000 (удвоение), ошибок 0 — сходится с замером интерфейса
  до единицы. Скрин меню на живой валюте снят.
  ⚠️ ГРАБЛЯ МОЕЙ ЖЕ ПРОБЫ (записываю, чтобы не повторить): сначала мерил
  через starAward(lv,stars) и получил «баланс 0 → 0», чуть не завернув
  здоровую сдачу. starAward — ЧИСТЫЙ КАЛЬКУЛЯТОР номинала («сколько
  заплатит»), начисляет awardStarsForWin (боевой путь победы), а для проб
  есть starGrant(n). Ошибка была в пробе, не в продукте.
  ОСТАЛИСЬ ЗАГЛУШКАМИ (ждут интеграцию/владельца): Get More, Subscribe
  $1.99. «Open» у локнутых типов — заглушка сознательно: открытие идёт
  ПРОГРЕССИЕЙ уровней, а не покупкой; если владелец захочет платное
  открытие — это запрос МЕТЕ на цену и ручку. Сьют 108 PASS.

- v1-test-91 · 2026-07-24 — ИНТЕРФЕЙС: ГЛАВНЫЙ ЭКРАН В БОЕВОМ ПОТОКЕ +
  ДИСПЕТЧЕР: ассерт «меню vs реклама» и починка флейка осколков.
  ИНТЕРФЕЙС (ветка claude/interface-menu, ОТДЕЛЬНАЯ — урок усвоен):
  #mainScreen заменил карточку #pauseOverlay (та осталась держателем
  soundToggle/hardToggle, но не показывается — вырезать каскадило бы по
  хендлерам); ⏸ открывает меню, большая кнопка = Resume при живой партии /
  Play Game когда партии нет. ВЛАДЕНИЕ ПАУЗОЙ по паттерну 78-ads: тихая
  pauseGame(true), флаг menuPaused, снимает ТОЛЬКО СВОЮ, над чужой не
  открывается (`if (!menuPaused && paused) return`). ЗАОДНО ВЫЛЕЧЕН СМЕЖНЫЙ
  БАГ: visibilitychange звал НЕтихий pauseGame() — после замены карточки
  игрок, вернувшись во вкладку, упёрся бы в осиротевший попап. Music-слайдер
  СКРЫТ до появления музыки (возврат одной строкой). Десктоп 763:1031: грид
  2 колонки; поймано скринами и починено — коллекция раздувала пилюлю шапки
  в арку (дефолтный stretch) и тянула грид на ~6000px без капа (все 93 типа).
  ДИСПЕТЧЕР: (1) закрыл пробел, честно названный интерфейсом, — ассерт
  «меню НЕ открывается поверх РЕКЛАМНОЙ паузы и не снимает её; после ролика
  игра разморожена, меню так и не открылось» на bridge-моке (контракт трёх
  систем: меню + реклама + visibility). (2) РАЗОБРАН ФЛЕЙК ОСКОЛКОВ, о
  котором сообщала мета: сначала не воспроизводился (4 прогона), потом
  поймался — «48→48», «51→36». Изолированная проба доказала ЧЕСТНЫЙ ДРЕНАЖ
  (33 → 21 к ~2с), то есть УТЕЧКИ НЕТ. Причин было ДВЕ: фиксированная пауза
  900мс (осколки догорают по своим часам, сьют приходит с разной загрузкой)
  и — главное — ассерт сравнивал ОБЩИЙ счётчик geoms сцены, а между base и
  after тикают соседние системы (витрина печёт портреты) — стабильное +2
  при 12 осколках. Ассерт переписан: ожидание условия с потолком 6с +
  ПОРОГ «остаток < половины залпа» вместо точного равенства (настоящая
  утечка дала бы +12 и больше). 3 прогона подряд: остаток +0. Сьют 108 PASS.
  ⚠️ ОСОЗНАННОЕ ОТЛИЧИЕ ОТ МАКЕТА (вопрос владельцу): на десктопе 763:1031
  шапка профиля (звёзды/Get More/аватар) идёт полосой СВЕРХУ на всю ширину,
  а в макете она в шапке правой колонки у «My Collection» — так обе
  раскладки живут на ОДНОЙ разметке без дублей узлов; переносится, если
  владелец захочет строго по макету.

- v1-test-90 · 2026-07-24 — МЕТА: ЗВЁЗДЫ = ВАЛЮТА + BOOST (спека владельца
  «звезды это валюта, очки которые можно тратить, никаких монет не нужно»;
  Boost «да»). ⚠️ ДЮП ЗАКРЫТ — предупреждение диспетчера отработало: схема
  как у монет, `se`/`ss` (earned/spent), баланс = разность, оба монотонные,
  мерж max; трата растит ss и откату не подлежит. Ассерт ровно на риск:
  снимок сейва ДО траты → трата 1000 → мерж старой копии → баланс НЕ
  вернулся. ДВЕ РОЛИ РАЗВЕДЕНЫ: рейтинг stars[lv] (max, не тратится) и
  кошелёк — разные поля, ассерт «трата не трогает рейтинг» PASS.
  МИГРАЦИЯ: накопленный рейтинг конвертируется в стартовый баланс по тому
  же номиналу (идемпотентна через монотонный флаг sm — второе устройство
  не начислит повторно). НОМИНАЛ: 1★=100, 2★=250, 3★=500 + 10×номер уровня.
  ⚠️ АНТИ-ФЕРМА (решение меты, важное): платится ДЕЛЬТА к прошлому рейтингу
  уровня, а не полная сумма — иначе ур.1 (короткий, лёгкие 3★) становится
  бесконечной фермой валюты; первое прохождение платит полностью, 1★→3★
  доплачивает разницу, повтор без улучшения = 0. BOOST: покупает ступень
  накопления типа (+0.25 к множителю, навсегда), цена 1500×2^ступень
  (1500/3000/6000/12000/24000) — экспонента сама тормозит скупку; купленные
  ступени в отдельном поле `bo`, счётчик спасённых в витрине остаётся
  честным. Сверка с макетом владельца: Boost 11k ≈ 4-я ступень, баланс
  166.5K = игрок с несколькими покупками. API для интерфейса: starBalance,
  starAward, spendStars, onStarsChange, boostPrice/canBoost/buyBoost/
  boostTier; accSnapshot дополнен boost/price/affordable. +14 ассертов.
  ⚠️ ЛОЖНАЯ ТРЕВОГА «main красный» (мета сообщила о падении ассерта осколков
  на чистом d8584da, база 52→64→54): у диспетчера НЕ воспроизвелось —
  3 прогона на чистом main и прогон после мержа зелёные, база 36→48→36.
  Числа базы разные ⇒ замер контекстно-зависим; вывод: это ФЛЕЙК ТАЙМИНГА
  ассерта (осколки догорают асинхронно, проверка после фиксированной паузы),
  а не утечка. ГРАФИКЕ: укрепить ассерт ожиданием условия вместо паузы.
  Сьют 105 PASS.

- v1-test-89 · 2026-07-23 — ГРАФИКА: ПОЛИРОВКА ОСКОЛКОВ (запрос физики из
  Междузонных, ветка claude/graphics-shards ОТДЕЛЬНАЯ — урок смешанной
  ветки интерфейса усвоен). Три пункта закрыты: (1) ФОРМА — makeShardGeo,
  4 угла тетраэдра сдвинуты ±38%, каждый скол уникальный обломок вместо
  регулярного «д4»; (2) ТИНТ ПО ГРАНЯМ — на плоском MeshBasicMaterial света
  нет, поэтому объём ПЕЧЁТСЯ В ВЕРШИННЫЕ ЦВЕТА: грань светлее/темнее по
  своей нормали к ключевому свету (тот же −0.36/0.60/0.72, что у matcap),
  множитель 0.55…1.32, цвет несёт material.color; (3) ЗВУК — crunch в
  75-audio: фильтрованный шум + сухие щелчки, СПЕКТР ВЫШЕ рокота grind
  (тот низкий: cutoff 300 + 70 Гц) — на куче не маскируются; на пути помола
  ложится ~350мс после 'grind' → «рокот лопастей… треск раскола».
  ГРАНИЦА СОБЛЮДЕНА: в 70-fx перенесён только ВИЗУАЛ shardFX, сигнатура та
  же — оба вызова физики (burstFX, grindShred) работают без правки; правило
  burstFX, тайминги grindShred и параметр тряски не тронуты.
  ПЕРФ: скол 12 вершин, ровно как прежний тетраэдр — регрессии нет; худший
  случай 49 осколков: +49 geoms/draw calls на пик, шаг физики p95 3.3 мс,
  после догорания geoms БИТ-В-БИТ в базу (stepFX диспозит геометрию И
  материал каждого скола). Сьют 91 PASS (+3 ассерта осколков: залп создал
  fx, свои геометрии на кадре 36→48, дренаж 48→36 без утечки).
  РЕШЕНИЯ ДИСПЕТЧЕРА по двум вопросам графики: (а) дебаг-хук
  __game.shardBurst(n,opts) в 99-main ОСТАВЛЕН — тот же класс тест-
  поверхности, что detonate/rocks/veilAll (прецедент есть), поведение не
  трогает; им же снят приёмочный скрин; (б) звук crunch на пути ПОМОЛА
  оставлен включённым — по спектру и задержке это последовательность, а не
  задвоение; если на слух владельца окажется лишним, снимается одной
  opts.sound:false в вызове grindShred (строка ФИЗИКИ — правку делает она).

- v1-test-88 · 2026-07-23 — ФИЗИКА (метка перенумерована из 87: занята витриной) (ветка worktree-physics-rocks, вливает
  диспетчер): ОСКОЛКИ (shardFX: brick/pirate/rock колются, не в труху) +
  ЭФФЕКТНЫЙ ПОМОЛ (grindShred: захват+сплющивание -> фонтан осколков). Обе
  спеки владельца 2026-07-23. Канон CLAUDE.md «brick/pirate труха» отменён
  (слово владельца дано). Ревью-фиксы: шинковка на реальных часах (десинк
  <20 FPS), финал спокойный. Перф п.9 чист (шаг 4.6 мс, геометрии в базу).
  Запрос ГРАФИКЕ на полировку осколков. SUITE PASS.
- v1-test-87 · 2026-07-23 — ИНТЕРФЕЙС: ВИТРИНА — тёмная тема, реакция на
  матч, разворот, раскладка (прямые правки владельца). ⚠️ ЗАБРАНА
  CHERRY-PICK только витрина (dbbeb5e); главный экран/пауза (519a6af,
  чекпоинт) ОСТАВЛЕН в ветке на debug-показе — ждёт визуального ОК и 4
  развилок владельца, в сборку тестеров НЕ идёт (проверено: 0 showMainScreen
  в index). Cherry-pick прошёл без конфликтов — витрина от главного экрана
  не зависит. СДЕЛАНО: (1) html.night инвертит имя/множитель/букву в белый +
  тёмная плашка (над ночным полем #3a4068 не читался; днём прежний тёмный);
  (2) реакция на матч — подскок портрета + вспышка полоски при росте
  accCount своего типа (диф счётчика, ядро не тронуто, лаг ≤150мс; починен
  косм. баг «2 матча типа за <460мс срывали пульс»); (3) разворот — каскад
  строк поверх clip-path (prefers-reduced-motion учтён); (4) показываю ВЕСЬ
  замес уровня (было 5 слотов + авторотация, VIT_SLOTS удалён); (5) Hint→
  вправо к Shake, витрина bottom 76→8, растёт вверх. ⚠️ ИНВАРИАНТ rect
  #vitrine держится бит-в-бит (анимация на ДЕТЯХ ячейки) — camnear и якорь
  всплывашки целы. Проверка диспетчера: день/ночь по часам, 9 строк, ошибок
  0; ночная тема читается. Сьют 88 PASS.
  ⚠️ ВОПРОС ВЛАДЕЛЬЦУ (интерфейс): «весь замес» на высоких уровнях (много
  типов) даёт панель ВЫШЕ вьюпорта, скролла нет (pointer-events:none). На
  ранних (9 типов) влезает. Нужен кап строк / уменьшение на глубине?

- v1-test-86 · 2026-07-23 — ФИЗИКА (ветка worktree-physics-rocks, вливает
  диспетчер): ВЕС ПРИ ВСТРЯСКЕ для новых пачек brick/pirate (владелец
  делегировал выбор). SHAKE_RESP += brick 0.72 / pirate 0.85 по плотности
  материала (кладка почти как камень; пиратское — тяжёлое дерево/металл).
  Замер velByTex на ур.70: отклики 0.63-0.70 / 0.78-0.89 при целях
  0.72/0.85 — в шуме; SUITE PASS. Геймплей-констант не трогал (вес — только
  рыхление/вибрация).
- v1-test-85 · 2026-07-23 — ИНТЕРФЕЙС: КРИТЕРИЙ camnear ПО ЧАШЕ (60px,
  спека владельца «панель моргает при кручении, хотя помещается; прячется
  если до чаши по горизонтали <60px») + ИНТЕГРАЦИЯ: ПАУЗА+МЬЮТ НА ВРЕМЯ
  РЕКЛАМЫ.
  ИНТЕРФЕЙС: моргание было НЕ от гистерезиса — критерий брал левый край
  КУЧИ ПРЕДМЕТОВ, а она при вращении меняет экранную ширину (драг дал
  разброс зазора 70px, класс щёлкал у порога). Лечение = требование
  владельца: считать до ЧАШИ (осесимметрична, левый край при повороте
  вокруг оси не меняется — дрожания нет ПО ПОСТРОЕНИЮ). Пороги 60/70
  (узкая полоса, широкий гистерезис не нужен). ⚠️ ВТОРАЯ ПРИЧИНА (общая
  грабля): орт «вправо» из camera.matrixWorld, а project() по
  matrixWorldInverse — во время драга расходятся на кадр;
  camera.updateMatrixWorld() перед расчётом → разброс за полный оборот
  РОВНО 0. Мой интро-гейт сохранён. Проверка диспетчера по ширинам:
  1160→−31 (скрыта, панель налезала бы на чашу), 1366→72, 1440→109,
  1920→349 — совпало с замером интерфейса до пикселя.
  ИНТЕГРАЦИЯ (78-ads, на примитивах диспетчера v83): пауза+мьют на входе
  в показ, снятие в ЕДИНОЙ точке endPending (туда сходятся награда,
  провал, watchdog, исключение SDK, cancel — «заморожено навсегда»
  закрыто по построению). Подписка на INTERSTITIAL_STATE_CHANGED (её не
  было вовсе — звали showInterstitial и не знали, когда кончилось;
  страховка 60с на OPENED без CLOSED). AUDIO_STATE_CHANGED подписан
  (пробел B): два источника тишины СКЛАДЫВАЮТСЯ, иначе конец ролика
  включил бы звук, который площадка просила выключить. 9 ассертов по
  БОЕВОМУ пути (клик #adYes → startAd → showRewarded), не по внутренностям.
  Сьют 88 PASS.
  ⚠️ НАЙДЕН ТРЕТИЙ ПРОБЕЛ (не трогали): Bridge шлёт PAUSE_STATE_CHANGED
  (портал открыл своё меню поверх игры) — не слушаем, тот же класс бага;
  ручки есть, но пересечение с pausedByAd/visibility надо продумать —
  отдельная сдача по слову.
- ⚠️ КАЛИБРОВКА HARD ЗАКРЫТА 2026-07-23 (физика; кода НЕ касается, правок
  геймплея НЕТ). ТЕЗИС «Hard на глубине непроходим» ОТОЗВАН самой физикой:
  (1) поражение гейтовано shakes==0 && adShakes==0 — с одной встряской в
  резерве тупик не убивает никогда; (2) мельница простоя (idle>10c в Hard)
  ест ПАРАМИ без лимита и ведёт к победе — тупик не смерть, а НАЛОГ ОЧКАМИ;
  (3) стенд физики исключал мельницу по построению (паузы бота короче
  idleLimit). Контроль: Easy на ур.41 у бота тоже за бюджетом — метрика
  винила БОТА, не Hard. ИЗМЕРЕНО ВЗАМЕН — цена тупика в очках (ответ
  владельцу): Hard ур.41 тупик открывается мельницей через 9-50c (−60..
  −500 очков), два за 60c не открылись (−560/−580, съедено до трети чаши);
  Easy реже, но столько же. Уровень ПРОХОДИТСЯ, страдают ОЧКИ И ЗВЁЗДЫ.
  ⚠️ КОРЕНЬ ДОЛГОГО ОЖИДАНИЯ: mixerGrind ест САМЫЙ НИЖНИЙ предмет
  (sort по p.y, 80-gameplay:449), а тупик живёт на ПОВЕРХНОСТИ — клапан
  приложен к другому концу чаши. Дешёвая ручка, если владелец захочет
  лечить: в состоянии «доступных пар нет» есть сверху. РЫЧАГИ честно:
  радиус вверх — СНЯТЬ (выгода нулевая, ломает 3 нерфа комбо); кап типов
  30 — ноль в шуме + скрытая цена (раздача i%N берёт ПЕРВЫЕ N — при капе
  часть типов не выпадет никогда, замирает накопление/музей); ЗАМЕНА —
  РОТАЦИЯ типов (N случайных из открытых, физика та же, коллекция идёт);
  КАНДИДАТ ИЗ ДАННЫХ — поднять порог эндшпильного ∞ с 8 до ~15-20 (дорогие
  тупики на 18-39 живых). ПЕРЕНОС НА ИГРОКА: ботоскор не отвечает (бот
  видит все пары перебором, но матчит парами, а тап человека берёт группу)
  — нужен ПЛЕЙТЕСТ, вопрос «ожидание мельницы — честная плата или
  наказание?». Приборы в ветке worktree-physics-rocks (инструментальные).
- v1-test-84 · 2026-07-23 — ГРАФИКА: ВУАЛЬ НЕДОСТУПНЫХ = ОБЕСЦВЕЧИВАНИЕ В
  ШЕЙДЕРЕ (спека владельца «в hard mode недоступным полностью уменьшать
  saturation»). ⚠️ ЭТО БЫЛА ПОЧИНКА БАГА, а не новая фича: старая вуаль
  лерпала material.color к серому, а у ТЕКСТУРНЫХ моделей color БЕЛЫЙ (цвет
  несёт атлас) — лерп лишь притемнял текстуру, тигр оставался рыжим.
  Работала вуаль только на кирпичах и процедурных, то есть на меньшинстве;
  на полной чаше 130 завуалированных из 183 были визуально неотличимы от
  доступных. В комментарии 40-items это было написано прямым текстом,
  следствие никто не посчитал.
  СДЕЛАНО: юниформа uVeil в matcapSpecPatch обесцвечивает ГОТОВЫЙ цвет
  (после сэмпла атласа) — три ALU без ветвления, при uVeil=0 тождество;
  сила общим объектом-юниформой (одна программа на все 183). VEIL_TARGET=1
  (прежние 0.65 калибровались ПОД ЛЕРП, где 1.0 давало заливку без формы —
  обесцвечивание светотень не трогает). Старый лерп жив фолбэком для
  материалов без патча (бомба, откат CFG.matcap=false). Портреты витрины
  гасят ОБЕ ручки — иначе обесцвеченный портрет осел бы в кэше навсегда.
  ПРОЗРАЧНОСТЬ (VEIL_MODE='fade') реализована, но НЕ включена, и решает
  это НЕ перф: недоступные — ровно те, кто не видит небо, то есть закрыты
  от камеры верхним слоем; сквозь них видна та же куча, скрины desat и fade
  неразличимы. Цена же платится ЗА ВСЕХ: three ставит предмет в прозрачную
  очередь по флагу material.transparent, доступные с opacity 1 уезжают туда
  же и теряют ранний Z, а дёргать флаг покадрово = перекомпиляция шейдера.
  ЗАМЕР (сид 101, ур.20, куча спит, 3×70 кадров): обесцвечивание +0.1 мс
  мобильный / +0.3 десктоп (шум); прозрачность +1.9 мс (+3.4%) / +0.4.
  ⚠️ Цифру прозрачности НЕ подавать как «дёшево»: headless — SwiftShader,
  а дорогой прозрачность делает именно то, чего у него нет (тайловый HSR
  мобильных GPU); на реальном телефоне будет хуже. ⚠️ Методика: сравнивать
  состояния можно только ВНУТРИ одной страницы — дрейф между запусками
  браузера доходил до 2× на одной сборке.
  ПРОВЕРКА ДИСПЕТЧЕРА: hard=false → veiled 0 (вуаль — механика Hard,
  включается галкой игрока); hard=true, ур.20 → veiled 138 из 183, max 1,
  доступных пар 12, ошибок 0; на скрине масса обесцвечена, доступные
  цветные. Сьют 88 PASS. Ползунки вуали в matcapTuner (26 ползунков).
  ВЛАДЕЛЬЦУ: эффект слабее СВЕРХУ и разителен СБОКУ — следствие правила
  доступности («видит небо»); сильный сигнал сверху — это не вуаль, а
  подсветка доступных, отдельная спека.
- v1-test-83 · 2026-07-23 — ДИСПЕТЧЕР: ПРИМИТИВЫ ПОД РЕКЛАМУ (запрос
  ИНТЕГРАЦИИ после её исследования адаптеров Bridge). Установлено ею по
  БАНДЛУ, а не по докам: Bridge НЕ ставит паузу, НЕ глушит звук и НЕ
  блокирует ввод — только транслирует колбэки площадки; единственные .mute
  в бандле — про ОБРАТНЫЙ поток (площадка просит игру заглушиться). Значит
  пауза/мьют на игре, а требуют их обе целевые площадки.
  СДЕЛАНО: (1) pauseGame(silent) — тихая пауза БЕЗ попапа настроек (иначе
  игрок возвращается из ролика в нашу карточку паузы) и ВОЗВРАЩАЕТ boolean
  «поставил ли ЭТОТ вызов»: 90-input ставит паузу по visibilitychange, а
  снимает её ТОЛЬКО игрок кнопкой Continue — авто-resume поверх чужой паузы
  вернул бы игрока в живую игру, которую он не возобновлял. (2) Sound.
  setMuted/isMuted — внешний мьют master-гейном, НЕЗАВИСИМЫЙ от CFG.sound:
  два владельца тишины (выбор игрока и требование среды) нельзя мешать,
  «сохранить-восстановить» CFG.sound — гонка с игроком. (3) __game.pause/
  resume/sound/pauseState для боевого потребителя и ассертов. Блок ввода
  НЕ понадобился: 90-input уже глушит тапы под паузой (находка интеграции).
  ⚠️ РЕГРЕССИЯ, ПОЙМАННАЯ СЬЮТОМ НЕМЕДЛЕННО: `pauseBtn` слушал pauseGame
  напрямую, и MouseEvent прилетал в новый аргумент silent (truthy) — попап
  паузы молча перестал показываться. Обёрнут в () => pauseGame().
  4 новых ассерта. Сьют 85 PASS.
  ОТКРЫТО ПОСЛЕ ИССЛЕДОВАНИЯ (владельцу): мы не шлём Poki сигналы
  GAMEPLAY_STARTED/STOPPED (их адаптер маппит их в gameplayStart/Stop —
  именно ими Poki решает, когда показывать рекламу: мало отключить свой
  таймер, надо начать говорить); не слушаем AUDIO_STATE_CHANGED (площадка
  просит заглушиться — закрывается той же ручкой setMuted).
- v1-test-82 · 2026-07-23 — ИНТЕРФЕЙС: TIER-UP ПО МАКЕТУ 769:56 (спека
  владельца). ПЕРЕДЕЛКА, не подгонка: вместо тёмной плашки с именем —
  светлая ПИЛЮЛЯ (радиус 1000, белый 16%, inset-свечение 28px белым 0.7),
  внутри картинка 44×44 cover + лаймовый бейдж #c0ff47 87×43 с «×1.25»
  (23px Heavy, чёрный); ttName удалён из разметки и 85-hud.
  ⚠️ ПРИЁМКА ПО ЧИСЛАМ DEV MODE (правило 11, впервые применено):
  диспетчер сверил собранную сборку с нодой — 179×68, padding
  12/16/12/20, радиус 1000, фон и тень, картинка 44×44 cover, бейдж 87×43
  #c0ff47, кегль 23/900, имени нет. ВСЁ СОШЛОСЬ, ошибок 0.
  Два ОСОЗНАННЫХ отступления интерфейса (объяснены, приняты): line-height
  бейджа задан явно 27px (у фолбэка ui-rounded `normal` даёт 30 и высота
  уезжала на 44); анимации в макете нет, но «красивый эффект» — прямая
  спека владельца от 22-го: вылет с пружиной и искры сохранены, добавлен
  «поп» бейджа, ЗОЛОТОЙ внешний глоу УБРАН (спорил бы с inset-свечением
  макета), искры перекрашены в лайм+чернильный (жёлто-белые терялись на
  светлой пилюле). ПОПУТНО ПОЧИНЕНО по своей же верификации: translateY
  в развороте витрины двигал её rect ~620мс, и ап ступени в это окно
  ставил тост вплотную (зазор 0 вместо 12) — разворот целиком на
  clip-path, rect неподвижен (min=max=532); тост стоял на left:16 при
  витрине и кнопке на 8 — теперь три элемента в одну вертикаль.
  Сьют 80 PASS.
- v1-test-81 · 2026-07-23 — ИНТЕРФЕЙС (4 правки витрины по спеке владельца)
  + ИНТЕГРАЦИЯ (потеря прогресса) + ДИСПЕТЧЕР (camnear в интро).
  ИНТЕРФЕЙС: (1) портреты заполняют бокс — АНАЛИТИЧЕСКИЙ фит (ортокамера +
  bbox проекций вершин), не пиксельный: readPixels давал стойл GPU→CPU
  2.4мс против 0.96 при той же точности; заполнение 0.55 → 0.909×0.924,
  буфер 96→132 (3×44 под ретину). (2) Разворот по хуку introdone —
  clip-path inset снизу (round 32) + подъём 12px, 0.5с/задержка 0.12с.
  ⚠️ НЕ scaleY/max-height: они двигают rect панели, а её живой rect читают
  ДВОЕ — якорь всплывашки и мой критерий camnear (scaleY(.6) увёл бы top на
  117px); НЕ opacity — там уже три состояния равной специфичности.
  (3) left:8 по rect кнопки подсказки. (4) Тень владельца как есть.
  ПОПУТНО ПОЧИНЕНО (все портили картинку МОЛЧА и НАВСЕГДА — снимок оседает
  в кэше): backdrop-filter у .overlay БЕЗ -webkit- (в Safari ≤17 размытие
  под ВСЕМИ оверлеями не работало вовсе); портрет брал самый тёмный тон
  кучи (matcap гасит диффуз по мировой высоте — меш поднят на y=100);
  портрет сереет, если снят в момент вуали недоступности (на рендер
  возвращаем baseColor); фолбэк портрета в музее сравнивал key с ярлыком.
  ИНТЕГРАЦИЯ: ⚠️ ПОТЕРЯ ПРОГРЕССА — commitSave ПИСАЛ в облако всегда, а
  bridgeSyncSave ЧИТАЛ только под гейтом isRewardedSupported: на площадке
  со storage без rewarded прогресс уезжал в один конец. Вызов поднят сразу
  после initialize(); порядок доказан (loadSave синхронно в IIFE до
  асинхронного Ads.init), мерж монотонный. 4 ассерта на поддельном SDK
  через локальный http-сервер; зубы проверены ДО фикса (storage.get 0).
  ДИСПЕТЧЕР: camnear НЕ считается во время интро — падающий столб ронял
  зазор ниже 200, класс вставал до начала игры, а на 1440 зазор ~225
  попадал в полосу гистерезиса и не отпускал: витрина не появлялась вовсе.
  Замер живого интро ПОСЛЕ фикса: 1440 зазор 241 → панель видна, 5 карточек,
  left 8; 1920 — 439. Спека владельца «200px до вещей» не тронута.
  Сьют 83 PASS.
- ⚠️ ДИАГНОСТИКА 2026-07-23 (НЕ ВЕРСИЯ, кода не касается) — ПРИРОДА ТУПИКОВ
  HARD НА ГЛУБИНЕ. Замер физики (ур.41, 14 тупиков) + сверка арифметики
  диспетчером. ЧТО УСТАНОВЛЕНО: доступные пары при ∞-радиусе = 9..18
  (медиана 13.5) — то есть близнецы на поверхности ЕСТЬ, но далеко друг от
  друга; версия «на поверхности меньше одной копии типа» ОПРОВЕРГНУТА самой
  физикой. Модель: λ = (все пары) × (доля доступных) × (доля в радиусе) —
  эффект МУЛЬТИПЛИКАТИВНЫЙ, отдельного «главного рычага» нет; рост от капа
  типов ЛИНЕЕН по числу копий (49→30 ≈ ×1.9), не квадратичен.
  Маргинально вуаль жёстче радиуса: снятие вуали +30 пар, снятие радиуса
  максимум +13 (упирается в потолок доступных близнецов).
  ⚠️ ЦЕНА РАДИУСНОГО РЫЧАГА (проверено по коду 60-access): комбо-строка
  r = max(r, r + (COMBO_RADIUS − r)·t) при r ≥ 1.1 — МАТЕМАТИЧЕСКИ NO-OP;
  значит baseRadius ≥ 1.1 убивает радиусную часть комбо-лестницы с первой
  секунды, а при ≥ ~1.6 умирает и ветка Power chain. Глобальный подъём
  радиуса = пересмотр всей комбо-лестницы, которую владелец трижды нерфил.
  ⚠️ ЦИФРА «58-81 ВСТРЯСКА» ОТОЗВАНА КАК НЕПОДТВЕРЖДЁННАЯ: стенд имел три
  дефекта (обрыв на 30-м матче — мерилась лёгкая фаза; бот без пауз держал
  Power chain непрерывно и досыпка возвращала предметы; λ снималась до
  смерти совмещённой пары). Стенд переписан; первый честный контроль —
  ур.5 Hard = 3 встряски при бюджете 5, уровень доигран до нуля.
  СТАТУС: идёт веер ур.41 (база / типы 30 / типы 25 / радиус 1.7 / relax,
  по 2 сида + контроль Easy), таблица «рычаг → встряски → чем платим»
  придёт диспетчеру. ДО НЕЁ ВЛАДЕЛЬЦУ РЕШЕНИЕ НЕ НЕСТИ.

- v1-test-80 · 2026-07-23 — ТРИ НЕДОСТАВЛЕННЫЕ СДАЧИ ЗАБРАНЫ (аудит хвостов
  по запросу владельца «что упустили»): ветки лежали готовыми БЕЗ ОТЧЁТА в
  диспетчер — так их никто не собирал. (1) МЕТА claude/meta-par-rocks:
  камни исключены из пар-скора — их type.name общий, и пара камней раздувала
  базу звёзд на 20 очков, которые игрок не может заработать никак (до 3
  фантомных пар = 60 очков с ур.31); звёзды на поздних уровнях были занижены.
  (2) МЕТА claude/meta-acclabel: ярлыки накопления — срез префиксов всех
  ПЯТИ пачек + развод коллизии Fish; проверено на ур.70 (93 ярлыка, склеек
  нет). (3) ГРАФИКА claude/vibrant-lederberg-7c5a5b: MATCAP-ТЮНЕР
  __game.matcapTuner() — 24 ползунка (3 света + 3×7 пресетов), живая
  пересъёмка текстуры в тот же DataTexture, Copy-JSON; 5 ассертов в сьюте
  (не 6 — поправка графики; пятый проверяет ПЕРЕСЪЁМКУ по контрольной сумме
  пикселей __game.matcapSum, иначе ассерт ловил бы только смену числа в
  объекте). Детали: свет Lx/Ly/Lz ОБЩИЙ для пресетов, shin — логарифмический
  ползунок, дребезг гейтован rAF (60 input → одна съёмка), z-index 21 ВЫШЕ
  оверлеев (иначе панель пропадала бы под паузой), keydown глушится на
  панели (Space на ползунке улетал бы во встряску), Copy дублируется в
  консоль (clipboard на file:// отказывает). ⚠️ Владельцу: пресет metal БЕЗ
  ПОТРЕБИТЕЛЕЙ (хром-примитивы удалены из пула) — заголовок это пишет;
  яркость текстурных моделей крутится НЕ через tex.amb, а через TEX_GAIN
  (аддитив владелец забраковал), поэтому в тюнер он не вошёл.
  ⚠️ ОРГВЫВОД (корень вскрыт графикой): отчёты уходили В ОТВЕТ ВЛАДЕЛЬЦУ,
  а не диспетчеру — канал односторонний. Правило 12 уточнено: сдача идёт
  через send_message; диспетчеру — самому аудировать невлитые ветки.
  Сьют 79 PASS ×2.
- v1-test-79 · 2026-07-22 — ДИСПЕТЧЕР: ХУК introdone (пропущенная запись,
  дописана при аудите): класс на <html> в finishIntro И skipIntro, снятие
  в startIntro — сигнал КОНЦА ОБЛЁТА для разворота витрины (спека владельца
  «блок плавно разворачивается после анимации облёта»). +ассерт контракта.
- v1-test-78 · 2026-07-22 — ФИЗИКА: findByTex v2 — ФЛЕЙК ТАПА ПО КАМНЮ
  ЗАКРЫТ (ветка worktree-physics-rocks; их метка была 77 — ПЕРЕНУМЕРОВАНА
  диспетчером, 77 занята витриной). Причина флейка: клик шёл в экранную
  проекцию ЦЕНТРА камня и попадал в загораживающий передний предмет (+120
  вместо −20). Лечение — мой вариант (а): рейкаст с камеры по центру + 8
  смещений, возвращается пиксель, где предмет — ПЕРВОЕ пересечение (та же
  логика, что у боевого handleTap); всё закрыто → {occluded:true}, секция
  камней делает встряску и повторяет (до 5 раз, потом честный FAIL).
  Хелпер общий — скрин-пробы бурстов получили защиту бесплатно. ИГРОВОЙ
  КОД НЕ ТРОНУТ (только __game-хелпер и сьют). SUITE PASS ×3 у физики,
  ×2 у диспетчера. Правило «красный камень → перегнать прогон» отменено.
- v1-test-77 · 2026-07-22 — ИНТЕРФЕЙС: ВИТРИНА ПО МАКЕТУ 768:1061 (ветка
  interface-vitrine). Визуал из design context: 300px/радиус 32/белый 16%,
  5 строк 44+12, имя SF Pro Rounded Bold 16 #3a4068, полоска 140×8 #23ce1a
  на треке белый-40%, множитель-пилюля; .vhead и «+N more» удалены (в
  макете их нет). Механика: 5 слотов + очередь в порядке замеса,
  авторотация (собранный уезжает 0.28с, следующий с пружинкой; по одной
  за тик; все собраны — панель растворяется). Триггер = предикат vitDone
  (одна строка на смену по ответу владельца). Проверки: интерфейс 1440 +
  диспетчер независимо (300px, 5 слотов, добил banana 14шт — слот занял
  Pig). Нюанс проб: последнюю пару типа добивать через cfg.baseRadius=9 +
  450мс. ⚠️ К ОТКРЫТОМУ ВОПРОСУ ВЛАДЕЛЬЦУ о триггере ротации: при дефолте
  «тип собран» сирота нечётной группы (бомба/трим дают нечётность) держит
  карточку до конца уровня. Сьют 73 PASS.
- v1-test-76 · 2026-07-22 — ДИСПЕТЧЕР: CAMNEAR v2 — «200px ДО ВЕЩЕЙ» (спека
  владельца по скрину: прежний camR<14.5 прятал панель слишком рано).
  Критерий теперь ЭКРАННЫЙ: зазор правый-край-панели ↔ левый-край-кучи
  (проекция предметов с экранным радиусом); <200px скрыть, >240px показать.
  Край панели — из живого rect (#vitrine): смена ширины макетом не сломает.
  Тик раз в 150мс. +__game.vitrineGap() для тестов. Проба 1600×900: дефолт
  328px (видима) / зум-9 179px (скрыта) / отдаление 362px (вернулась).
  Замечание: на узких десктоп-окнах (1160-1300) куча стоит ближе 200px
  постоянно → панель там прячется чаще — это буква спеки, владельцу
  доложено. ⚠️ ФИЗИКЕ передан флейк их тест-секции: тап по камню изредка
  попадает в ЗАГОРАЖИВАЮЩИЙ передний предмет (+120 вместо −20) — раскладка
  случайная; повтор зелёный. Сьют 73 PASS (повтор).
- v1-test-75 · 2026-07-22 — ИНТЕРФЕЙС: ВСПЛЫВАШКА ПРЫГАЕТ НАД ВИТРИНОЙ
  (соседство трёх в левом углу после переезда витрины). Решение интерфейса
  ПРОТИВ глушения тоста на десктопе: тост — прямая спека владельца
  («красивый эффект»), молча отключать её на платформе — решение уровня
  владельца; вместо этого перед показом bottom тоста = верх панели + 12
  (позиция на показ — высота витрины плавает с числом типов), без витрины/
  при camnear — прежний угол. Проверки: интерфейс 3 конфигурации; диспетчер
  независимо — честный ап ступени (accGrant 98 + matchType) → тост show,
  зазор 12px, left 16. ⚠️ Грабля пробы: зазор мерить ПОСЛЕ 900мс — на
  ~420мс пружина в overshoot (cubic-bezier 1.56). Сьют 73 PASS.
- v1-test-74 · 2026-07-22 — ДИСПЕТЧЕР: ВИТРИНА ПЕРЕЕХАЛА ВЛЕВО (спека
  владельца по скрину каркаса: «этот блок должен быть слева»; right:16 →
  left:16, HUD-однострочник). Замер: left 16, правый край 296 из 1440.
  ⚠️ ИНТЕРФЕЙСУ поставлено вдогонку: в левом углу теперь трое — витрина,
  всплывашка накопления (left:8, bottom:+72) и кнопка подсказки; развести
  (моё предложение: на десктопе при видимой витрине всплывашку глушить —
  тракт один, ап ступени виден в полоске карточки; решение за интерфейсом).
  Сьют 73 PASS.
- v1-test-73 · 2026-07-22 — ИНТЕРФЕЙС: ВИТРИНА УРОВНЯ, каркас (спека
  владельца «на больших мониторах мало жизни»; ветка interface-vitrine).
  Панель справа внизу (right:16/bottom:+76, 280px, ≤55vh): карточка на тип
  уровня с портретом и реалтайм-полоской накопления до следующего множителя;
  только ≥1160px И pointer:fine; pointer-events:none целиком; сетка 2×N без
  скролла, хвост в «+N more» (кап 36); реалтайм — точечный accCount видимых
  раз в 150мс; html.camnear → фейд .2s (драйв v72). Тик внутри tickFace.
  Две пойманные грабли в блоке ИНТЕРФЕЙСА: ключ накопления = type.name
  (НЕ it.key — близнецы), панель строится ПОСЛЕ интро (атласы декодируются,
  портреты чернели навсегда). Проверки: интерфейс headless 1440 (адресный
  реалтайм 0→6%, ап ступени синхронен всплывашке) + диспетчер независимо
  (9 карточек ур.1, фейд opacity→0 при camR=14, на 393 панели нет).
  МЕТЕ передана мелочь: accLabel не режет префиксы brick/pirate.
  ОЖИДАНИЕ: макет витрины от владельца — ляжет поверх каркаса. Сьют 73 PASS.
- v1-test-72 · 2026-07-22 — ДИСПЕТЧЕР: ДРАЙВ camnear ДЛЯ ВИТРИНЫ (контракт
  с интерфейсом подтверждён их ответом). Класс `camnear` на <html> при
  camR<14.5, снятие при >15.2 (гистерезис против мигания), тик после
  genLevel (просьба интерфейса — интро не тикает). +__game.setCamR
  (постановка сцен) и 4 ассерта контракта в сьюте (в т.ч. удержание в
  зазоре гистерезиса). Поправки интерфейса приняты: скролла в панели НЕТ
  (несовместим с pointer-events:none) — сетка 2×N, уплотнение, хвост в
  бейдж «+N ещё»; реалтайм — точечный accCount по видимым раз в 150мс.
  Сьют 73 PASS. Витрина в работе у интерфейса (каркас → макет владельца).
- v1-test-71 · 2026-07-22 — ДИСПЕТЧЕР: ЧИП СПРАВА = ОЧКИ УРОВНЯ (спека
  владельца 2026-07-22-б, отменяет «общие звёзды в чипе»): иконка ★
  остаётся, число — очки от совмещений/ошибок (stats.score). САМИ звёзды —
  только на экране завершения (winStars) и на БУДУЩЕМ ГЛАВНОМ ЭКРАНЕ
  (владелец покажет макет позже — ОЖИДАНИЕ). Ассерт сьюта переписан с
  паттерна на точное равенство чип=score (старый прошёл бы случайно).
  Сьют 69 PASS. Параллельно ИНТЕРФЕЙСУ поставлена задача «витрина уровня»
  на десктопе (см. блок ИНТЕРФЕЙСА/Междузонные): панель справа внизу с
  объектами уровня в музейной стилистике + реалтайм-полоски накопления до
  следующего множителя; скрывается при приближении камеры к чаше; макет
  владелец принесёт после каркаса.
- v1-test-70 · 2026-07-22 — ГРАФИКА: ГИБРИД НЕБА (решение владельца:
  «панорама на десктопе, на мобиле градиент»; ветка lederberg). 05-sky
  регенерирован и вернулся; различается ТОЛЬКО база — лихорадка/лесенка
  помола общие поверх. Критерий устройства — pointer:coarse (выбор графики
  с обоснованием: класс устройства, не ширина окна; согласован с DPR-капом
  в том же файле; ресайз-вопрос отпадает сам). Ветвление доказано счётчиком
  текстур GPU: десктоп 9, мобайл 8 (проверено графикой и диспетчером
  независимо). Мерж-конфликт с v69: ветка сдана до ноты о затемнении —
  разрешение «гибрид графики + удаление затемнения», действует для ОБЕИХ
  баз. Перф-поправка к v68 внесена выше (цена сэмпла = −3.4 мс кадра, шаг
  физики ни при чём). Вес вернулся к 6.88 raw/1.73 gzip. ⚠️ ТРАФИК-НЮАНС
  ВЛАДЕЛЬЦУ: панорама инлайнится в единый index.html — мобильный игрок её
  ВСЁ РАВНО СКАЧИВАЕТ (гибрид экономит декод/GPU/3.4 мс кадра, не трафик).
  Лечится выносом панорамы во внешний файл, но это ломает офлайн-запуск
  двойным кликом и меняет пакет порталов — только по слову владельца.
  Сьют 69 PASS.
- v1-test-69 · 2026-07-22 — ДИСПЕТЧЕР: УБРАНО СТАТИЧНОЕ ЗАТЕМНЕНИЕ ВЕРХА
  (приказ владельца: «градиент сверху/снизу — только при турбо или злости
  миксера»). Из шейдера неба удалена строка SKY_TOP_DIM/FROM (стояла
  с 2026-07-21 по запросу интерфейса под контраст HUD); константы вырезаны
  из 00-config с тумбстоуном «НЕ возвращать без слова владельца». Событийные
  слои НЕ тронуты: лихорадка uCombo (низ) и красная лесенка uGrind (верх)
  как были. Замер (день, кадр 390×780): кромка 156→208 (затемнение ушло),
  контроль над кучей 172→176 (небо не сдвинуто). Владелец предупреждён:
  белые глаза на светлом дневном небе ~1.6:1 против WCAG 3:1 (замер графики).
  Сьют 69 PASS. Графике: в 10-stage/00-config main новый коммит — подмержить
  до сдачи гибрида панорама-десктоп/градиент-мобайл.
- v1-test-68 · 2026-07-22 — ГРАФИКА: НЕБО-ГРАДИЕНТ ВМЕСТО ПАНОРАМ (спека
  владельца «отказать от фоновой картинки»; ветка lederberg). 05-sky.js
  (3 JPEG-панорамы) удалён; база — 3 опоры (зенит/горизонт/надир) линейно
  по d.y, цвета сняты с самих панорам (профиль почти линеен, smoothstep дал
  бы полосу у горизонта). Время суток — единая точка skyTimeNow (5-11 утро /
  11-18 день / 18-5 ночь). Ночные ЗВЁЗДЫ сохранены процедурно (3D-сетка по
  направлению; 3 провальных подхода записаны в блоке ГРАФИКИ). tintChrome
  переведён на прямое чтение skyChromeCSS — сэмпл/ретраи ушли, memo-
  ограничения схлопнуты в «верхний цвет градиента = тон хрома». Контраст
  HUD 3:1+ держится (3.46-16.06). ВЕС: −1.2% (6.87→6.79 raw, 1.73→1.68
  gzip) — панорамы были уже сжаты; главный резерв веса — rapier 2.24 МБ.
  ~~Бонус перфа: p95 шага 4.3-4.6~~ ПОПРАВКА (сдача графики к v70): шаг
  физики от неба НЕ зависит, та разница была разными состояниями кучи;
  честная цена сэмпла панорамы = −3.4 мс КАДРА (~6%). A/B-скрины утро/день/
  ночь сняты диспетчером (подмена getHours), вид совпадает; артефакт
  владельцу. Сьют 69 PASS. tools/sky2module.js и skyboxes оставлены —
  панорама на десктоп возвращается одной командой, если владелец захочет.
- v1-test-67 · 2026-07-22 — ДИСПЕТЧЕР: ГЛАЗА И ЧИСЛО −30% НА МОБАЙЛЕ
  (спека владельца). Одна переменная --eyeW ×0.7 (мобайл: min(147,
  (100vw−202)×0.7)) — глаза, огонь, лифт и число масштабируются долями её.
  Десктоп ≥768 возвращён к полному размеру отдельным @media НИЖЕ мобильного
  :root — специфичность равная, каскад решает позиция в файле (грабля
  About-Us, первый вариант в верхнем media молча проигрывал). Замер:
  мобайл 390 глаза 188→132, число 61→43; десктоп 210 без изменений.
  Сьют 69 PASS. Конструкция — зона ИНТЕРФЕЙСА, правка согласована как
  HUD-мелочь диспетчера, интерфейс уведомлён.
- v1-test-66 · 2026-07-22 — ГРАФИКА: ДОБОР ТИПОВ (пул 78→93) + ДИСПЕТЧЕР:
  iOS-ХРОМ. Графика (ветка lederberg): +11 еды и +4 машины после жёсткого
  отсева близнецов по цвету/габариту; Car — ПОТОЛОК различимости (остальные
  16 родовых «прямоугольников» конфликтуют цветом с занятыми). Тяжёлые
  модели сведены к 1200 тр.; p95 шага 6.2-6.5 мс, вес 6.87 МБ raw/1.73 гzip.
  ⚠️ СИГНАЛ ВЛАДЕЛЬЦУ: добор встаёт в ХВОСТ пула — новые типы откроются
  на ур. 67-83 (genLevel берёт первые 9+уровень−1). «Пестрее в начале» =
  перестановка порядка ценой чёткости старта — только по слову владельца.
  Грабля будущих партий: цвет машин по доминирующему UV врёт (шины/стёкла) —
  цвета трухи ставить по кузову. ДИСПЕТЧЕР (приказ владельца, скрин iOS):
  чёрные поля Safari у выреза/жестовой зоны — метод About-Us: meta
  theme-color (статусбар красится ТОЛЬКО им) + локстеп фона html/body
  в тон видимой полосы панорамы (сэмпл 10-30% высоты; НЕ зенит и НЕ
  заглушка 1×1 — ждать img.width>4). tintChrome в 99-main. Сьют 69 PASS.
- v1-test-65 · 2026-07-22 — ГРАФИКА: ПАК-ЭФФЕКТЫ В 70-fx + БАНАН +40%
  (ветка claude/vibrant-lederberg-7c5a5b, вливает диспетчер). Эффекты пачек
  переехали из 80-gameplay в 70-fx (просьба физики закрыта); круглые точки
  вместо квадратных (ленивые общие карты диск/звезда), звёзды пачек — один
  Points вместо 5 мешей (5→1 draw call, всегда лицом к камере). Спрайты
  сознательно НЕ взяты: в r149 THREE.Sprite делит одну геометрию, dispose
  догоревшего эффекта убил бы остальные. Банан +40% в геометрии + rc:1.4;
  попутно обезврежена мина: modelGeo мутировал модульные массивы 36-models
  без копии (.clone() добавлен — иначе при чистке geoCache банан рос бы
  каждый реген). Перф: чистая экономия (fx-дренаж 0, geoms 21→21).
  ⚠️ ВОПРОС ВЛАДЕЛЬЦУ (в Междузонных): у пачек Brick/Pirate пак-эффекта
  нет — лопаются трухой (burstFX знает food/car/animal). Оставить труху
  или заказать графике «хлопок пачки» для новых коллекций? Сьют 69 PASS.
- v1-test-64 · 2026-07-22 — МЕТА: КАЛИБРОВКА ЗВЁЗД ЗАКРЫТА (ветка
  claude/meta-stars-calib, вливает диспетчер). STAR2_K=1.5 / STAR3_K=2.1
  подтверждены ботами: пороги различают аккуратную и небрежную игру.
  Вскрыт дрейф шкалы: пар-скор занижен относительно живой игры в 3-4 раза
  (боты бьют парами, живые группы крупнее) → звёзды частично меряют номер
  уровня, а не мастерство. Рычаг — нормализация базы под ожидаемый размер
  группы; ВОПРОС ВЛАДЕЛЬЦУ, до решения пороги не трогаем. + тест-API
  bestTapTarget (прицел по группам для ботов). Организационное: ветка меты
  была создана в ОСНОВНОМ клоне (сессия от корня) — сборка 63 сначала уехала
  в неё; клон возвращён на main, всё влито, правило 1 дополнено (worktree
  обязателен для сессий от корня). Сьют 69 PASS.
- v1-test-63 · 2026-07-22 — ФИЗИКА: НЕСОВМЕЩАЕМЫЕ КАМНИ (rocks из Pirate,
  решение владельца; отдельный модуль 37-rocks — 36-models не тронут,
  предупреждение графики учтено). Все 8 пунктов спеки: рампа с 16-го
  (+1/5 ур., кап 6), плотность 2.6, убираются только бомбой (в капе 7),
  тап −2×MISS_PENALTY=20 через scorePenalty (ур.1/кламп уважаются),
  вне ∞-порога/пар-скора/автопана, вуали нет, SHAKE_RESP.rock=0.7.
  Перф п.9: шаг физики с 6 камнями в шуме (p95 6-7 мс, бюджет 25).
  Сьют +6 ассертов; попутно починен флейк мгновенного сэмпла ∞-радиуса.
  Грабля генератора (в CLAUDE.md): точечный модуль несёт дубль-const/
  функции — вырезать, канон 36-models один. ⚠️ БАЛАНС-СИГНАЛ ВЛАДЕЛЬЦУ
  (бот, ур.41, бюджет встрясок 5): Easy в бюджете; HARD НА ГЛУБИНЕ
  СЛОМАН НЕЗАВИСИМО ОТ КАМНЕЙ — {58-81} встрясок и без них {73,32}
  (49 типов × перекрытия душат пары); рычаги на выбор владельца: кап
  типов в Hard / бюджет встрясок с уровнем / смягчение доступности.
  Открыто: SHAKE_RESP и пак-эффекты для brick/pirate(+rock) — дефолты.

- v1-test-63 · 2026-07-22 — ФИЗИКА (ветка worktree-physics-rocks, вливает
  диспетчер): НЕСОВМЕЩАЕМЫЕ КАМНИ по 8-пунктной спеке владельца — модуль
  37-rocks (36-models не тронут), рампа 16-й/+1 на 5 ур./кап 6, плотность
  2.6, съём только бомбой, двойной штраф тапа, вне ∞/автопана/пар-скора,
  финал доедает; сьют +6 ассертов (секция в конце — сохранение контекста
  полного прогона), перф п.9 в шуме (шаг 6.0 vs 6.4 мс), попутно починен
  флейк сэмпла эндшпильного радиуса.
- v1-test-62 · 2026-07-22 — ГРАФИКА: BRICK И PIRATE В ПУЛУ (решение
  владельца; 63 -> 78 типов, замес 3:3:1:1:1 еда:звери:машины:кирпичи:
  пираты). Brick: из 185 файлов — 7 (185 = 23 формы × 8 кромок, сверху
  неотличимых; 23 формы = 11 футпринтов); ВСЕ кирпичи в атласе белые —
  ПОКРАШЕНЫ через material.color (ветка t.paint, выбор владельца шкалой).
  Pirate: 8 из ~20; корабли отклонены (близнецы сверху, охват 5-6.5,
  hull наврал бы). Перф вырос: треугольники −17%, кадр p95 −22% (новые
  модели легче вытесняемых). topY ур.20/80 = 7.54/8.77 (норма).
  ⚠️ Грабля конвертера в блоке ГРАФИКИ: ведущие цифры имён срезаются
  (1x2/2x2 схлопывались) — staging-имена без цифр; 36-models генерится
  ЦЕЛИКОМ по списку пачек — предупреждение ФИЗИКЕ передано (rocks — только
  отдельным модулем). Хвост: корабли ждут компаунда, если понадобятся.

- v1-test-61 · 2026-07-22 — МЕТА: НАКОПЛЕНИЕ ПО ТИПАМ + НОВАЯ БАЛАНС-
  ТАБЛИЦА (ветка stoic-rubin, влил диспетчер; сессия меты оборвалась
  переездом папки — работа была закоммичена). Счётчики совмещённых по
  типам в сейве (монотонные, gen-эпоха уважается), пороги 100/300/700/
  1500/3100... (×2+100), множитель +25%/ступень; пар-скор и звёзды
  СЧИТАЮТ множители; контракт v2 для UI (label/key, живой item в
  onAccTierUp, accSnapshot) — всплывашка и музей интерфейса переходят
  с DEMO на живые данные автоматически. Баланс: промах −10, уровень 1
  без штрафов, кламп нуля на ур.1-5, рыбка +150+5×lvl, пороги звёзд
  СТАРТОВЫЕ 1.5/2.1 — ⚠️ калибровка ботом НЕ ДОЕХАЛА (оборвана
  переездом), дострелить в следующей сессии МЕТЫ. ПЕРЕЕЗД: папка
  проекта переименована в Blender (владелец), worktree-связки починены,
  превью-конфиг Main:8779; старые чаты направлений отвязаны от путей —
  новые открывать от Blender/. Ассеты владельца Brick и Pirate
  закоммичены (назначение уточняется).

- v1-test-61 · 2026-07-22 — ПОВЕСТВОВАНИЕ/МЕТА (ветка claude/stoic-rubin,
  спека владельца через диспетчера; номер поднят с 60 — разминулись с UI
  накоплений интерфейса): НАКОПЛЕНИЕ ПО ТИПАМ — счётчики
  Save.ac, пороги 100/300/700/1500/3100/6300 (×2+100), множитель очков
  типа +25%/ступень (кап 9), событие onAccTierUp для всплывашки, пар-скор
  по типам × множители; БАЛАНС: промах −10, ур.1 без очковых штрафов,
  ур.1-5 кламп нулём, рыбка +150+5×ур, звёзды 2★/3★ пересчитаны с
  бот-калибровкой (три профиля). Сьют: +7 секций. SUITE: PASS.
  Ack просьб интерфейса: в accSnapshot добавлен label (человеческое имя),
  в onAccTierUp летит ЖИВОЙ item (mesh валиден, тело Rapier уже
  уничтожено, растворение стартует — портрет снимать сразу в колбэке).
- v1-test-60 · 2026-07-22 — ИНТЕРФЕЙС: UI НАКОПЛЕНИЙ (вперёд меты, на
  демо-данных): ВСПЛЫВАШКА АПА в левом нижнем углу над подсказкой (зазор
  4px) — однокадровый офскрин-портрет НАСТОЯЩЕГО меша 96×96 (кэш по типу;
  ⚠️ грабля r149: mesh.clone() душит JSON.stringify на теле Rapier в
  userData — портрет собирается вручную из geometry+material), слайд с
  пружиной + пульс + 6 искр, ~2.2 с, очередь, звук+вибро; МУЗЕЙ — каркас
  (кнопка Museum в паузе, список: миниатюра/имя/прогресс к порогу/×mult),
  ждёт макеты владельца в чате ИНТЕРФЕЙСА. Стыковка: typeof-проверки —
  подхватит accSnapshot()/onAccTierUp() меты автоматически; до тех пор
  бейдж DEMO и кнопка «Tier-up demo» в панели разработчика. Просьбы мете:
  человеческое имя в снапшоте, живой item в хуке.
- v1-test-59 · 2026-07-22 — ФИЗИКА (ветка claude/vibrant-golick-62b02a,
  влил диспетчер; номер поднят с 58 — разминулись с чипом звёзд):
  ЧЁРНЫЙ ШАР-БОМБА по спеке владельца — средний чёрный шар в середине
  кучи, тап взрывает <=7 ближайших (зазор 2.2) без очков; эффект: тёмная
  труха + пак-эффекты жертв + волна 2.2; миксер её не ест, финал доедает;
  перф по п.9: пик кадра = пику обычного матча, шаг физики p95 4.5 мс,
  тела/коллайдеры вернулись к базе. Звук взрыва временно 'shake';
  полировка визуала бомбы — в открытый запрос ГРАФИКЕ к пак-эффектам.
- v1-test-58 · 2026-07-22 — ЧИП ЗВЁЗД + правило п.9 (диспетчер). Чип
  справа показывает ОБЩИЕ звёзды из сейва (спека владельца: «выводить
  общее количество звезд, а не только за уровень»); очки уровня — в попах
  и на экране победы. Новое правило владельца п.9: поведение объектов —
  только через ФИЗИКУ с обязательным перф-замером вкладки; задача
  «чёрный шар-бомба» передана ФИЗИКЕ по этому правилу (спека в её чате).
- v1-test-57 · 2026-07-22 — ТРУХА ×2/÷2 (спека владельца: «при соединении
  уменьшить размер частицы вдвое, количество увеличить в два раза»;
  диспетчер, DUST_FRACTIONS в 70-fx): 1280 частиц (640×0.0225 +
  400×0.035 + 240×0.05) вместо 640. Труха общая — помол (bladeDustFX)
  тоже стал мельче/гуще. С учётом «пункта 5» ФИЗИКИ (v1-test-56) труха
  играет у пар/троек и помола; эффекты пачек групп >=4 — отдельные
  (их миниатюризация — по слову владельца, если понадобится).
- v1-test-56 · 2026-07-22 — ФИЗИКА (влита ветка claude/vibrant-golick-62b02a;
  в ветке шла как «46/47» до синхронизации с main): (1) ВЕС ПРИ ВСТРЯСКЕ,
  вариант 1 — SHAKE_RESP по пачкам (car 0.75 / animal 1.0 / food 1.15),
  только рыхление/подброс/вибрация, pullK не тронут; __game.velByTex() для
  тюнинга; экономика не ухудшена (A/B ботом). (2) «ПУНКТ 5» — эффекты
  совмещения правилом: пара/тройка — труха, группа >=4 ЛОПАЕТСЯ эффектом
  пачки (сок / искры+детальки / звёздочки; стартовые реализации в
  80-gameplay через addFX) + надувание перед хлопком + физволна blastWave
  (запрос ГРАФИКЕ на полировку — см. междузонные).
- v1-test-55 · 2026-07-22 — ОГОНЬ = ЭСКАЛАЦИЯ ПОМОЛА (правка владельца:
  «огонь оставляем только после 3 секунд начала Grinding; позиция глаз
  меняется на 3 секунду, до этого на привычном месте»; диспетчер):
  прежний триггер «за 5 с до помола» СНЯТ — телеграф приближения несёт
  красная лесенка неба; огонь и спуск конструкции включаются после
  FIRE_AFTER_GRIND_MS=3000 непрерывного Grinding (якорь grindStartMs,
  сдвигается паузой, матч рвёт помол — огонь гаснет). Сьют: ассерты
  «на 1-й секунде помола огня нет» / «после 3 с горит и глаза опустились».
- v1-test-54 · 2026-07-22 — ИНТЕРФЕЙС: корона огня вписана в экран
  (решение владельца «опустить конструкцию»): при огне #face съезжает на
  --fireLift = max(0, 0.196·eyeW − 8px − safe-area-top) — ровно чтобы
  пламя вписалось; на айфонах с вырезом лифт 0 (корона живёт в safe-area,
  ничего не дёргается); вниз 0.35 с / вверх 0.2 с синхронно огню. Режимы
  FIRE_DROP_MODE в 00-config: 'fire' (дефолт) / 'always' / 'off'.
  Попутно добита грабля разметки пламён (незакрытый тег вкладывал правое
  в левое — «пламя стопкой слева»); геометрия макета бит-в-бит по getBBox.
  Канон EYES-CHARACTER-SPEC §7б дополнен. SUITE: PASS.
- v1-test-53 · 2026-07-22 — ВРЕМЯ УРОВНЯ СКРЫТО ИЗ HUD (спека владельца:
  «скроем время уровня, покажем его только на экране завершения»;
  диспетчер): флаг LEVEL_TIME_IN_HUD=false в 00-config — узел #tmSvg
  прячется в layoutHUD (перенос узла жив), обновление textContent
  гейтится (fitStat не дёргается вхолостую); на экранах победы/поражения
  «Time: M:SS» осталось как было. Ассерт в финал-секции сьюта.
- v1-test-52 · 2026-07-22 — ИНТЕРФЕЙС: ОГОНЬ УГРОЗЫ (спека владельца
  «появление огня, если таймер меньше 5 секунд», макет 751:1122). По
  макету огонь — КОРОНА над злыми глазами (не по бокам отсчёта): слой
  #fFire за белками, 1:1 по design context. Триггер — тот же источник,
  что число отсчёта (остаток <5 с или Grinding, кэш lastFireOn в 99-main);
  тайминги согласованы с лесенкой неба (подъём 0.35 с / гашение 0.2 с),
  мягкое «дыхание» пламени. Грабля в канон EYES-CHARACTER-SPEC §7б:
  CSS-анимация transform перекрывает атрибут transform svg-группы —
  позиция и дыхание разнесены по вложенным группам. ⚠️ ВОПРОС ВЛАДЕЛЬЦУ:
  корона выше глаз на ~37px — на экранах без выреза верх режется кромкой;
  (а) опустить конструкцию ~30px / (б) уменьшить огонь / (в) принять срез.
- v1-test-51 · 2026-07-21 — КАМЕРА, финальная правка владельца («не
  поднимай ведро по вертикали в начале уровня... начни если остаётся 20%
  вещей; иначе ведро плавает»): непрерывное следование УБРАНО — весь
  уровень target стоит на 4.2; при живых <= 20% стартовой загрузки
  (aliveN0) защёлка camFollowOn один раз плавно съезжает к 3.2 и стоит
  (досыпки защёлку не снимают). Ручное положение до защёлки автоматика
  не трогает. Сьют: +ассерт «до порога камера не плавает (min ty 4.2)»,
  эндшпильный пере-сформулирован под защёлку. SUITE: PASS.
- v1-test-50 · 2026-07-21 — ТАЙМЕРЫ (жалобы владельца; диспетчер):
  1) отсчёт до помола «запаздывал и дёргался» — он обновлялся в 600-мс
  HUD-тике, и граница секунды проскакивала (смены то через 0.6 с, то через
  1.2 с); перенесён в каждокадровый блок с кэшем текста (DOM только при
  смене). Замер: интервалы смен 983–1011 мс. 2) Таймер времени уровня —
  ЧЁРНЫЙ (был зелёный #0caa1e из макета; fill в shell.html). SUITE: PASS.
  К сведению: ФИЗИКА работает над обновлением слияния объектов — ждём
  её сдачу.
- v1-test-49 · 2026-07-21 — ИНТЕРФЕЙС: eyes-5 на СЕРИИ ТУРБО (решение
  владельца «2 серии подряд»): при chainSeries>=2 глаза становятся
  асимметричными из ассета (левый зрачок 40 в белке 60, правый белок 44
  со зрачком 12), качение разнонаправленное, clampGaze держит каждый глаз
  по его размерам; series 1 — прежние сжатые катающиеся. Замер px
  подтвердил пропорции ассета. Из открытых вопросов интерфейса остался
  один: «закатанные» глаза в Hard недостижимы (скука 8 с > хитрые с 7-й
  при терпении 10 с) — ждёт владельца.
- v1-test-48 · 2026-07-21 — ГРАФИКА (небо, спека 2026-07-21-г) + КАМЕРА.
  Небо: цвет лихорадки по времени суток — голубой #8cc7ff только в тёмное
  (h<5||h>=18, границы панорам; feverColorNow в 10-stage), зелёный в
  светлое; красный верх — ЛЕСЕНКА УГРОЗЫ: за 10 с до помола растёт сам по
  таймеру (10−left)/10, при лопастях 1.0, матч гасит быстрее подъёма
  (0.2 с вниз / 0.35 с вверх). Контраст HUD замерен по всей лесенке
  0/0.25/0.5/0.75/1 — порог 3:1 везде (день 3.7→6.5:1). Hard-кейс
  зафиксирован: терпение 10 с = красный наливается весь цикл. КАМЕРА
  (правка владельца: «автоматом поднимать не выше 1/3, остальное игрок
  сам»): пол автопана AUTO_FOLLOW_MIN=3.2 — автомат проходит треть хода
  до дна, глубже только жестами; ассерт эндшпиля пере-калиброван
  (3.2±0.1). SUITE: PASS.
- v1-test-47 · 2026-07-21 — СЕРИЯ ТУРБО + досыпка (спека владельца;
  диспетчер, ядро): второе турбо, собранное ВНУТРИ активного, перезапускает
  окно цепи и растит chainSeries («Power chain ×N!»; >=2 — сигнал для глаз
  eyes-5 — СДЕЛАНО ИНТЕРФЕЙСОМ в v1-test-49; гейт !chainUntil снят — вопрос ревью
  про comboCount закрыт решением владельца). Досыпка турбо: интервал
  500 -> 417 мс (темп ×1.2), объём 2 -> 2.6/тик (+30%, дробный аккумулятор
  chainCarry; лимиты полноты чаши прежние). __game.combo().series; в сьюте
  секция «второе турбо внутри первого». SUITE: PASS.
- v1-test-46 · 2026-07-21 — ГРАФИКА, небо по спеке владельца (2026-07-21-в):
  лихорадка снизу — нежный ГОЛУБОЙ #8cc7ff вместо зелёного, БЕЗ выбеливания
  панорамы (свечение у нижней кромки, потолок примеси K=0.60 — небо читается
  и в цепной реакции); ПОМОЛ — мягкий красный градиент СВЕРХУ зеркально
  нижнему (uGrind, лерп 0.35 с; драйв 4 строки в 99-main у сигнала
  grinding). Контраст белого HUD на помоле замерен: красный ложится ДО
  затемнения и только темнит полосу — день 3.7→6.5:1, ночь 16→9.5:1,
  порог 3:1 везде. ВОПРОС ВЛАДЕЛЬЦУ: тон голубого — шкала из 4
  (#8cc7ff стоит / #a9d4ff бледнее / #5fb0f5 насыщеннее / #79d0ea
  бирюзовее), смена = FEVER_COMBO в 00-config.
- v1-test-45 · 2026-07-21 — ИНТЕРФЕЙС: естественный переход в грусть
  (нырок зрачков → нижние веки 0.22 с → хвост взгляда вниз; замер: грусть
  видна 273–941 мс от тапа). Там же развязка Code Connect: НЕВЫПОЛНИМ на
  текущем тарифе Figma — API требует Dev/Full seat на Organization/
  Enterprise плане; зафиксировано «не возвращаться без смены плана».
  Номер порции поднят диспетчером с 44 (бампы с автопаном разминулись).
- v1-test-44 · 2026-07-21 — АВТОПАН КАМЕРЫ (уточнение владельца: «чтобы
  камера сама опускалась за кучей по мере разбора, без жеста»; диспетчер,
  90-input tickCamFollow из loop): target следует за РАЗОБРАННОСТЬЮ —
  на старте уровня дефолтные 4.2 (вид не изменился), опускается на 0.65
  за каждый юнит ухода верха кучи ниже стартового topY0, к эндшпилю
  остатки на дне сами в центре кадра. Верх — 85-й перцентиль ниже кромки
  (защита от встряски/досыпки), пересчёт 500 мс, ведение лерпом 1.5/с,
  на паузе/в интро молчит. Ручные жесты v1-test-43 остались как ОВЕРРАЙД:
  перебивают автоматику на 4 с, потом она мягко возвращается. Сьют:
  +ассерт «камера опустилась к эндшпилю» в полном прогоне, допуски
  пан-секции под автоследование. SUITE: PASS.
- v1-test-43 · 2026-07-21 — ВЕРТИКАЛЬНЫЙ ПАН ВЗГЛЯДА (спека владельца:
  «чуть сместить камеру по вертикали, приподнять и рассмотреть остатки»;
  диспетчер, 90-input): target камеры ездит по Y 1.2–5.2 (дефолт 4.2 —
  вниз до дна). Жесты ДОПОЛНИТЕЛЬНЫЕ, основное управление не тронуто:
  движение ЦЕНТРА двухпальцевого щипка (зум остался расстоянием),
  вертикальный драг ПРАВОЙ кнопкой, Shift+колесо. Сброс на границах
  интро (resetPointers). __game.cam() отдаёт ty; в сьюте 5 новых
  ассертов (пан/клампы/зум не тронут/сброс на regen). SUITE: PASS.

- v1-test-38 · 2026-07-21 — СОСТАВ ПУЛА (решение владельца: «пончик убери
  из ранних уровней, процедурные формы удали совсем»): TYPES 80 -> 63 —
  только модели владельца (62 с атласами + стейк, он модель, не примитив);
  17 процедурных примитивов удалены из пула, их фабрики в 30-shapes живы
  (gemGeo — фолбэк сюрприза; вернуть примитив = строка в TYPES). Пончик
  переставлен в хвост (индекс 62) — открывается на 54-м уровне, чем закрыт
  вопрос дырки convex hull в Hard (выбран увод, не компаунд). Замер
  прогрессии ур.1/30/60 без ошибок; индексная целостность geoCache
  проверена (наружу typeIdx не сохраняется). Физике заметка о новых
  мёртвых ветках 50-physics.
- v1-test-37 · 2026-07-21 — ГРАФИКА: читаемость белых элементов HUD (запрос
  ИНТЕРФЕЙСА закрыт замером, не заливкой): предложенный #d0dff3 давал
  контраст 1.35:1 — ХУЖЕ панорамы (1.6:1) при пороге WCAG 3:1. Решение —
  затемнение ВЕРХНЕЙ полосы экрана в шейдере неба (SKY_TOP_DIM 0.60 /
  SKY_TOP_FROM 0.70 в 00-config): глаза/пауза теперь 3.5:1 утром и днём,
  16:1 ночью; низ панорамы не тронут. ⚠️ Урок: контраст считать по
  ЛИНЕАРИЗОВАННОЙ яркости WCAG, не по sRGB-байтам (цифры врут почти вдвое).
  Если владельцу нужен именно голубой фон ПО МАКЕТУ — отдельное решение
  (белым элементам тогда потребуется обводка/подложка от ИНТЕРФЕЙСА).
- v1-test-36 · 2026-07-21 — МЕРЖ ГРАФИКИ И ИНТЕРФЕЙСА (команда владельца
  «забери все обновления»). ГРАФИКА: замес типов 2:2:1 (меньше машин,
  больше фруктов и зверей, без пиццы; 62 модели + 18 процедурных), заметки
  ревью закрыты (докстринг glb2module по факту, argv-проверка sky2module,
  1×1-заглушка атласа — вспышку воспроизвести не удалось, оставлена как
  защита), блок WORKSTREAMS переписан. ИНТЕРФЕЙС (их v1-test-13..16):
  HUD по мобильному макету Figma 741:1738 — персонаж-глаза ОДНИМ SVG
  (7 эмоций, 4 слоя: эмоция/взгляд/реакция/моргание; зрачок = индикатор
  турбо, диск-полоска удалена), число отсчёта миксера под глазами
  (краснеет ≤3 с), стек предметы/время/очки справа, пауза сверху слева
  (⚙️ убрана — настройки из паузы), кнопки макета Hint-иконка и «Shake ×N».
  Разрешение конфликтов диспетчером: настоящая пауза pauseGame/resumeGame
  сохранена под их оверлеем (выходы Заново/Настройки резюмят), автопауза
  visibilitychange и Space-гейт сохранены, textContent-«😴» в pauseGame
  убран (SVG-слои), tickDepthTint + tickFace вместе в loop, Reset progress
  оставлен в ⚙️-панели, coinsChip/hintCnt-строки updateHUD сняты вместе с
  элементами (монеты и так скрыты COINS_ENABLED). Закрыта заметка
  ИНТЕРФЕЙСА: faceEvent('surprised') в collectSurprise. shot_*.png сняты
  с трекинга (gitignore). Их просьба ГРАФИКЕ про фон #d0dff3 — открыта.
- v1-test-35 · 2026-07-21 — РАЗМЕРЫ: первые 15 уровней все предметы одного
  размера (спека владельца; SIZE_UNIFORM_LEVELS=15 в 00-config, гейт в
  levelSize). Рампа разброса СДВИНУТА: стартует с 16-го уровня с ±10%
  (без скачка сразу на кап ±50%), потолок прежний. Досыпки цепи/Continue
  идут через тот же levelSize — тоже единый размер до ур.15. Новая ручка
  __game.sizes() + ассерт в test.js.
- v1-test-34 · 2026-07-21 — МОНЕТЫ СКРЫТЫ (спека владельца: «вместо них
  звёзды за прохождение, монеты не удаляй, а скрой»): флаг COINS_ENABLED=
  false в 00-config по образцу магнита/прицела. Спрятаны чип 🪙, «📺 ×2
  coins» на победе, покупная встряска за 25 (ветка в requestShake); экран
  победы показывает звёзды + «+1 💡». Начисление addCoins/ce/cs/Telemetry
  живы — баланс копится втихую к возврату фичи. Вопрос №1 отчёта (тупик
  при монетах на встряску) снят вместе с покупной веткой.
- v1-test-33 · 2026-07-21 — ФИКСЫ КОД-РЕВЬЮ пяти направлений (диспетчер,
  «примени фиксы и собери всё»). Реклама/мета: Ads.cancel в genLevel
  (протухший rewardCb не награждал новый уровень), уборка watchdog-сироты,
  исключение SDK без бесплатного стаба, тик lastAction на время показа,
  onFail для «×2» (кнопка возвращается), захват coinsWon при клике, гварды
  level.over в buyCoinShake/startAd, showLose прячет adAskOverlay, сейв —
  эпоха gen против воскрешения из облака после сброса. Ядро/физика:
  эндшпильный ∞ ПРИОРИТЕТНЕЕ цепи; лесенка/цепь двигают радиус только вверх;
  pairMatch без тела = false (не откатывает метрику v3), фильтр animating в
  availablePairs; setTimeout-хвосты (матч/сюрприз/помол/финал) ждут резюма
  паузы (afterPause-очередь); калм-сон гейтован !intro; автопауза при
  сворачивании вкладки; Space не трясёт под оверлеями рекламы. Графика:
  якоря FX-программ (нет рекомпиляции шейдеров в тапе), 1×1-заглушка неба
  (чёрная вспышка старта), dispose софтбокса, страж matcap-патча, ресинк
  shadowMap в genLevel, реюз геометрии рыбки. Звук: resume из iOS
  'interrupted'. Инфраструктура: test.js стал АССЕРТНЫМ (30 expect,
  exitCode 1 на FAIL; +секции эндшпиль-∞, комбо-лесенка, подсказка, пауза,
  отмена награды при регене; честные ожидания вместо слепых таймаутов;
  безопасный miss-клик), build.py — assert маркеров shell, экранирование
  </script у всех бандлов, utf-8. Доки: CLAUDE.md и блоки WORKSTREAMS
  синхронизированы с кодом (рыбка, TYPES=84, потолок 1.1, порт 8779).
  Вопросы владельцу (не правились молча): детектор тупика и покупная
  встряска; comboCount на конце цепи; levelNum в облако; дырка пончика;
  V-ориентация matcap. Прогон: SUITE PASS + проба паузы PASS.
- v1-test-32 · 2026-07-21 — ЗВУК (первая порция, спека владельца: «только
  интерфейс + перемалывание»): пакет владельца Audio/ (Kenney); клик UI на
  все кнопки (делегированный хук) + помол = 3 сэмпла spaceTrash вперемешку;
  m4a/AAC (ogg Safari не декодит), ffmpeg 32кГц/40кбпс, base64 в
  74-sfx-data (+30КБ, gzip 1.71МБ); декод лениво после unlock, при сбое —
  процедурный фолбэк. Остальные звуки пока процедурные.

- v1-test-31 · 2026-07-21 — СБОРКА ГРАФИКИ-2: небо-панорама (05-sky,
  tools/sky2module), еда и машины вперемешку со зверями, прозрачное стекло
  обновлено, тонировка глубины смягчена (низ не проваливается в темноту),
  турбо не гасит кучу. Мерж чистый (ветка синхронизировалась с main).

- v1-test-28 · 2026-07-21 — потолок зазора 1.1 ВЕЗДЕ (вкл. Power chain;
  «вся чаша» в цепи отменена владельцем; эндшпильный ∞ при <=8 сохранён);
  Space = встряска; пауза ⏸ у настроек (сдвиг всех часов на резюме);
  диск-заряд у курсора скрыт флагом. Бот: 3-4 встряски при бюджете 5 —
  впритык, следить на плейтесте. Публикация: git remote `blender`
  (github.com/ikorzun/Blender), пуш main при каждой сборке диспетчера;
  доступ тестерам — GitHub Pages (включает владелец, см. отчёт).

- v1-test-25 · 2026-07-21 — СБОРКА ГРАФИКИ: 24 зверя с родным атласом
  (первая девятка — самые различимые), 10 моделей владельца, тонировка по
  глубине (шаг 2 matcap), Blender-конвейер (tools/glb2module.py,
  blender-decimate.py), светлота разнесена. Сюрприз = ЗОЛОТАЯ РЫБКА
  (спека владельца в чате графики; диспетчер вернул спавн, затёртый мержем,
  и починил фолбэк teapotGeo -> gemGeo). Примитивы в хвосте пула (с 17-го
  уровня), 4 простых типа диспетчера сохранены. ВЕС: 4.47МБ raw / 1.22МБ
  gzip (бюджет «~1МБ» уплыл на +0.2 — на контроле ГРАФИКИ).

- v1-test-21 · 2026-07-20 — СБОРКА ФИЗИКИ: метрика v3 (истинный GJK-зазор,
  единая точка pairMatch), правило касания сферы, ореол-призрак; потолок
  серии перекалиброван диспетчером: 1.5 В НОВОЙ метрике (их 2.0 + владельческий
  нерф «слишком легко» — для шаров эквивалент нерфа, продолговатым метрика
  сама убрала фантомную щедрость). +4 простых типа ранее (v1-test-20).

- v1-test-15 · 2026-07-20 — МЕТРИКА v3 (спека владельца, выбор «A»): матч =
  истинный зазор поверхностей через GJK Rapier (contactCollider), фильтр
  грубой фазы по охватным сферам; MATCH_EDGE_PAD удалён, константы
  канонические (0.9/0.75/2.0 — истинные зазоры); телеграф — ореол-призрак
  формы вместо сферы (reachGhostFX в 80-gameplay, клон геометрии). FYI
  ИНТЕРФЕЙСУ: слайдер ⚙️ возвращён к 0.3–2.2/0.9. FYI ГРАФИКЕ: призрак
  строится через addFX/fresnelGhostMat, 70-fx не тронут (sphereFX жив,
  не зовётся).
- v1-test-14 · 2026-07-20 — компенсация сложности (вердикт владельца
  «слишком легко, особенно с комбо»): baseRadius 0.35 / MATCH_R_MIN 0.2 /
  COMBO_RADIUS 1.45 — эффективная кривая с запасом 0.55 = отятюненная
  бит-в-бит; честная сфера осталась. FYI ИНТЕРФЕЙСУ: атрибуты radiusRange
  в shell.html пересчитаны (min 0, max 1.65, value 0.35) — синхронизация
  слайдера с новой базой, не редизайн.
- v1-test-13 · 2026-07-20 — ПРАВИЛО КАСАНИЯ СФЕРЫ (спека владельца): матч =
  охватная сфера кандидата касается видимой сферы (порог +MATCH_EDGE_PAD
  0.55, единая точка pairMatch в 60-access); сфера теперь честна для любого
  размера кандидата. Экономика встрясок в бюджете (A/B-бот), test.js зелёный.
- v1-test-12 · 2026-07-20 — ФИЗИКА: соак 6×15 мин закрыт (утечек нет, сон
  честный, спасатель 0–1); фикс латентного floaters (toi→timeOfImpact) +
  проверка опоры контактами; __game.perfStats()/contacts(i); soak.js.
  Геймплей не менялся.
- v1-test-13 · 2026-07-20 — СБОРКА 4 направлений: ФИЗИКА (соак закрыт,
  перф-метр, фикс латентного floaters), ИНТЕРФЕЙС (карта экранов +
  кликабельный макет ui-proto.html, починка ⚙️-панели), ГРАФИКА (прототип
  matcap на приёмке, приёмка 3D-ассетов владельца, tools/audit-glb),
  МЕТА (docs/MUSEUM-SPEC.md — спека музея v1.1). Мерж диспетчером,
  полный сьют зелёный. Скриншоты прогонов (shot_*.png) в .gitignore.
- v1-test-11 · 2026-07-20 — окно серии 4с; глаза 71px; таймер в нижнем
  баре; полоска заряда цепи; тексты Combo/Radius Up/Power chain; метрика
  зазора; чаша ×1.15/181 предмет; экономика v1 целиком.
- v1-test-12 · 2026-07-20 — ИНТЕРФЕЙС: починена ⚙️-панель (потерянный
  CSS-селектор — не открывалась); карта экранов docs/UI-SCREENS-PLAN.md;
  кликабельный макет 15 экранов src/ui-proto.html. Геймплей не тронут.
- v1-test-13 · 2026-07-21 — ИНТЕРФЕЙС: персонаж собран в одну конструкцию
  #face своей строкой под чипами (глаза + отсчёт до перемолки + полоска
  турбо); время партии убрано; 7 эмоций по матрице владельца с морганием;
  скелет готов под SVG-ассеты. Геймплейные правила не тронуты.
- v1-test-14 · 2026-07-21 — ИНТЕРФЕЙС: HUD по макету Figma 741:1497
  (пауза/LV/время слева, ★ градиентом справа, Shake-пилюля и контурная
  подсказка снизу, монеты и ⚙️ убраны); глаза владельца одним инлайновым
  SVG с четырёхслойной анимацией (эмоция/взгляд/реакция/моргание), турбо
  показывают зрачки. Запрос в ГРАФИКУ на голубой фон #d0dff3.
- v1-test-15 · 2026-07-21 — ИНТЕРФЕЙС: переложен под МОБИЛЬНЫЙ макет
  741:1738 (стек справа вместо горизонтальной группы, глаза 210×105,
  подсказка вниз-влево, номер уровня убран); крупный счёт сжимается в
  «12.5k» — колонка макета рассчитана на 3 знака.
- v1-test-16 · 2026-07-21 — ИНТЕРФЕЙС: фон возвращён к белому. Указание
  владельца: фон целиком зона ГРАФИКИ, интерфейс его не трогает (ни поле,
  ни подложку html/body). Запрос на #d0dff3 остаётся в «Междузонных».
- v1-test-39 · 2026-07-21 — ИНТЕРФЕЙС: глаза по спеке владельца (зрачок не
  выходит за белок; набор буста растит зрачки 29→50, набранный буст сжимает
  до 15 и катает в разные стороны; дуги eyes-4-4 удалены; помол — eyes-3-1;
  вместо красного «0» слово Grinding); обводка чисел 12 тенями по кругу
  вместо -webkit-text-stroke (у него срезались углы); настройки игрока
  перенесены в попап паузы; кнопка подсказки белая как пауза; возвращён
  счётчик подсказок, потерянный при прошлом разрешении конфликтов.
- v1-test-40 · 2026-07-21 — ИНТЕРФЕЙС: отсчёт и Grinding переведены на
  SVG-текст с настоящей обводкой (тени волнили край, stroke резал углы —
  оба брака владельца); Grinding всегда чёрный, красный в тексте убран.
- v1-test-41 · 2026-07-21 — ИНТЕРФЕЙС: все контуры текста сведены в один
  механизм (SVG-<text> .otext, настоящий stroke, толщина одним числом) —
  стек, отсчёт/Grinding, попы очков; кнопка Shake ночью белая с чёрным
  текстом (границы часов как у панорам 05-sky); в междузонных — спека
  ГРАФИКЕ: голубая лихорадка без высветления, красный верх при помоле.
- v1-test-42 · 2026-07-21 — ИНТЕРФЕЙС: весь UI на английском (вкл. панель
  разработчика и тосты ядра — мандат «локализация текстов»); при помоле
  всегда злые глаза eyes-3-1 со сканирующими зрачками (клип по белку),
  промах — грусть вниз eyes-1-6, fSquint удалён; Grinding +30%; десктоп
  >=768: LV+время у паузы, справа только счёт (макет 747:1048); обводка
  звезды — общее правило .otext.
- v1-test-43 · 2026-07-21 — ИНТЕРФЕЙС: фикс десктопного бара — рамки LV и
  времени обжимаются по тексту (fitStat: viewBox+width), отступы
  pause↔LV↔время равны (12/12), время больше не наезжает на глаза.
- v1-test-45 · 2026-07-21 — ИНТЕРФЕЙС: переход в «ошибку» анимирован
  (нырок зрачков 80 мс → веки снизу 0.22 с → хвост взгляда вниз);
  CODE CONNECT закрыт как заблокированный тарифом Figma (нужен Dev/Full
  seat на Org/Enterprise — ответ API); правило п.8 принято к работе.
  (номер поднят диспетчером: 44 ушёл автопану камеры — бампы разминулись).
