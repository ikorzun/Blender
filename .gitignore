node_modules/
node_modules
release/*.zip
release/Mixer.html
shot_*.png
до-мержа/
.DS_Store

# ⚠️ СТЕНД РЕКЛАМЫ, НЕ РЕЛИЗ. Файл несёт forciblySetPlatformId — он ЗАСТАВЛЯЕТ
# SDK притвориться площадкой. Нужен, чтобы впервые прогнать боевую ветку
# рекламы ДО заливки на портал (иначе первое в истории исполнение кода, который
# приносит деньги, случится на живых игроках). Но уехав в релиз, он врал бы
# площадке и игрокам. Поэтому: закоммитить его нельзя ФИЗИЧЕСКИ, а не по
# договорённости. Как пользоваться — WORKSTREAMS, блок ИНТЕГРАЦИИ.
playgama-bridge-config.local.json
.claude/
