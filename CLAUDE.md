# Миксер v1 — ВЕТКА МЕТА-ГЕЙМПЛЕЯ (папка Blender; до 2026-07-22 — funnel-game-v1)

## РЕШЕНИЯ ВЛАДЕЛЬЦА 2026-08-06 (контент и рекорды)

1. **Доставка контента: сейчас ВАРИАНТ 1** — весь контент внутри билда,
   новая вещь = пересборка и перезаливка. **ПОСЛЕ РЕЛИЗА — ГИБРИД**
   (вариант 3): база остаётся в билде, дополнения приезжают паками с
   версией и кэшем, «только в конец списка типов», применение между
   уровнями, при недоступности пака игра молча играется базой.
   Дословно: «1, потом гибрид после релиза».
   ⚠️ Следствие для веса: зип пакета 6.79 МБ, ориентир 8 МБ — до гибрида
   каждая новая партия моделей тратит запас; собственный лимит Playgama в
   проекте НЕ ЗАФИКСИРОВАН, спросить у площадки.
2. **Лидерборды: модель — КАК У ПЛОЩАДКИ** (сервер хранит МАКСИМУМ).
   Дословно: «Рекорды — как у площадки». Это ОТМЕНЯЕТ прежнюю идею
   «падающего ранга» (заработал — поднялся, потратил — упал): она
   физически несовместима с сервером, который помнит только лучший
   результат, и дважды провалилась на живых прогонах.
   ⚠️ Следствие, которое ОБЯЗАНО быть объяснено на экране: число в таблице
   (лучший результат за всё время) и число в кошельке (непотраченный
   баланс) РАСХОДЯТСЯ — это задумано, прятать нельзя.

## РЕШЕНИЯ ВЛАДЕЛЬЦА 2026-08-07 (лидерборды и релиз)

1. **СВОЯ ТАБЛИЦА ЛИДЕРОВ.** Дословно: «делаем свою». Причина: он хочет,
   чтобы ТРАТА очков опускала в списке («Форбс»-модель), а сервер площадки
   хранит только МАКСИМУМ и понижать не умеет — проверено дважды. Значит:
   позиция = ТЕКУЩИЙ БАЛАНС, наш сервис; платформенная таблица остаётся
   второй вкладкой «рекорд за всё время» (она бесплатная, код готов).
2. **ГОСТЬ = СВОЙ УНИКАЛЬНЫЙ ID.** Дословно: «гостю нужно присваивать
   уникальный id и всегда показывать, условно автологин в игре, но не
   гугловый». То есть авторизации площадки не ждём: id выдаём сами при
   первом запуске, к нему уже привязаны имя-животное (Save.gn) и аватар.
3. **БАНДЛЫ В КАБИНЕТЕ ЗАВЕДЕНЫ** («мы уже это проходили») — пункт снят со
   списка ожиданий; проверка остаётся на живом смоуке после заливки.
4. **ANDROID В ПОРЯДКЕ** («в андроиде все ок») — перф-блокер Физики закрыт
   положительно, пул A2 остаётся отменённым.
5. **МУЗЫКА — ЕГО ТРЕК И ЕГО ПРАВА**, звук и музыку принесёт позже; вопрос
   лицензии закрыт, задача переехала в конец.
6. **МАТЕРИАЛЫ ВИТРИНЫ ЗА НИМ**, добавит в самом конце. Форматы переданы:
   иконка PNG 1024×1024 без скруглений, обложка 1920×1080 (+1080×1920),
   скриншоты 4-6 шт 1080×1920 и 1920×1080.
7. **НАКЛОН ЧАШИ — НЕ ДЕЛАЕМ** («наклон пока не делаем»), разбор сохранён
   в V2-IDEAS.md.

## КАБИНЕТ ПЛОЩАДКИ СВЕРЕН 2026-08-07 (скриншот владельца)

Пункт «сверка id борда с кабинетом» ЗАКРЫТ. В кабинете Playgama: APP Public
Token `cms68frqf0008ri0h81k1dujg`, Leaderboard **ID `Blendo`**, Name `Blendo`,
Type **Numeric**, Score order **Higher is better**. В `playgama-bridge-config
.json`: `saas.publicToken` и `leaderboards[0].id` — те же значения, `isMain:true`.
Совпадает символ в символ.
⚠️ МЕТОД: строку НЕ переписывали с картинки, а СРАВНИЛИ две готовые. В
25-значном токене ноль/буква путаются легко, и ошибка проявилась бы молчаливым
отказом записи на живой площадке. Любую будущую сверку делать так же.
⚠️ ТОКЕН ПУБЛИЧНЫЙ ПО НАЗНАЧЕНИЮ (так и назван в кабинете) — он лежит в
конфиге, который едет в пакет портала и виден в поставке. Это нормально;
секретом является только `ADMIN_TOKEN` нашего воркера, он в конфиг НЕ попадает.
✅ **«Higher is better» ПОДТВЕРЖДАЕТ ПОСТАНОВКУ ДВУХ ВКЛАДОК:** платформенная
хранит МАКСИМУМ, то есть честно «рекорд за всё время»; своя считает ТЕКУЩИЙ
баланс и падает при тратах. Вкладки не дублируются, расхождение их чисел
задумано и обязано быть объяснено на экране (решение владельца 2026-07-24).

## РЕШЕНИЯ ВЛАДЕЛЬЦА 2026-08-07-б (лидерборды, продолжение)

1. **Звёзды сами НЕ продаются**, продаются игровые множители ЗА звёзды
   («мы не будем продавать сами звезды, но продаем игровые множители на
   них»). Следствие для таблицы: пополнений (`tu`) нет, формула
   `leaderboardScore()` остаётся, и трата на множитель ЧЕСТНО роняет место —
   ровно та модель, которую он хотел.
2. **ЛИЧНОСТЬ СВОДИМ К ОДНОЙ** («лучше свести к одному»): имя и аватар
   ВЫВОДЯТСЯ из ключа игрока `Save.gid`, поэтому на двух устройствах игрок
   выглядит одинаково. Цена принята сознательно: у части игроков имя один
   раз сменится при первом слиянии.
3. **Токен платформенной таблицы — сейчас, обе вкладки в одном релизе**
   («сейчас, все вместе»).
4. **СИНХРОНИЗАЦИЯ МЕЖДУ УСТРОЙСТВАМИ НУЖНА ПОЛНАЯ** — «как по очкам и
   покупкам, так и по прогрессу». Уровень переехал из отдельного ключа
   localStorage в сейв (`Save.lv`, мерж max в обеих ветках); чтение берёт
   максимум из сейва и localStorage.
5. **Колонки коллекции — ТРЕТЬЯ редакция**: <360 → 2, 360-420 → 3,
   >420 → 4. Прежние числа (360, затем 380/381) ОТМЕНЕНЫ.
6. **Под островом iOS подложки быть не должно** — фон шапки в зоне Dynamic
   Island не рисуем, но рецепт Safari 26 из канона не ломаем.

## РЕШЕНИЯ ВЛАДЕЛЬЦА 2026-08-10-б: ТАБЛИЦА ЛИДЕРОВ — МАКЕТЫ И ВКЛАДКИ

**⛔ ВКЛАДОК НЕТ.** Дословно: «Только наша таблица, вкладки отменяются». Экран —
ОДИН список по макету. Следствия:
- платформенная отправка ОСТАЁТСЯ в коде и работает (бесплатна, даёт видимость
  на площадке) — игроку просто не показывается. ⚠️ Пометить комментарием, иначе
  следующий увидит отправку без экрана и решит, что это забытый хвост;
- **текст про расхождение чисел БОЛЬШЕ НЕ НУЖЕН** — объяснять нечего. Прежнее
  обязательство владельца написать его снято вместе со вкладками.
**Подзаголовок пишем МЫ** («придумайте сами»): 2-3 варианта по-английски, вариант
из макета оставить среди них. ⚠️ Единственное, что игрока по-настоящему удивит, —
что счёт может УПАСТЬ при трате звёзд на множители; это и стоит объяснить.

**МАКЕТЫ (Figma `0mTCxjPtORKQzV7EZ6kTfE`), читать Dev Mode'ом, не скриншотом:**
- `840:1269` — экран, мобильный 393×852; `840:1230` — десктоп 1280×680;
- `840:4328` / блок `840:4344` — ТОЧКА ВХОДА в меню, мобильный веб.

⚠️⚠️ **МОЯ ОШИБКА, ИСПРАВЛЕНА МАКЕТОМ: блок таблицы встаёт НЕ на место снятого
баннера «No more AD».** По макету он МЕЖДУ рядом профиля (y=8, h=72) и карточкой
Play (y=170), то есть ВВЕРХУ (y=88, h=74); баннер в макете `hidden` и лежит
внизу (y=712). Прежнее указание «вместо баннера» отменено.

**Несущее из спеки экрана:** пилюля счёта цветная ТОЛЬКО у трёх первых
(`#ffe627` / `#b8c9c9` / `#e8bb95`, текст тёмный `#2a2935`), с четвёртого фона
нет и текст белый; строка «YOU» — белая пилюля, на мобильном ЗАКРЕПЛЕНА
(`absolute`), на десктопе В ПОТОКЕ; `Pause-dark` по центру на мобильном и слева
на десктопе; строки 56px без зазора; левый блок фиксирован 196px.
**Точка входа:** фон `rgba(239,251,255,.6)`, рамка 1px белая, пилюля; «Leaderboard»
Heavy 22px чёрный + «You on 845» Bold 14px `#a2a2a8`; справа три аватара 40×40
(gap 2) и стрелка 32×32. Ей нужны ОБА источника: `me()` (своё место, только
точное) и `top()` (три аватара).
⚠️ Меню открывается часто — брать из ОДНОЙ точки получения с кэшем, не заводить
второй сетевой путь.

## ⛔ РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-09-б: ЗАЩИТЫ ОТ НАКРУТКИ ПОКА НЕТ

Дословно: «давай не будем мудрить и сейчас делать защиту от накрутки. Пока
сделай хорошую простую основу для лидерборда с показом результата сразу по
окончанию каждого уровня. Так же результат меняется, если игрок потратил деньги
в коллекции на множитель».

**ОТМЕНЯЕТ решение 2026-08-09 (×5) ниже — оно прожило полчаса.** Возрастной
потолок снимается ЦЕЛИКОМ: `GROW_BASE`, `GROW_PER_S`, обрезка счёта, авто-
скрытие. Сервер принимает присланный счёт как есть.
⚠️ Цена названа владельцу дважды и принята: счёт считает КЛИЕНТ, и без потолка
любой может прислать любое число. Это осознанный размен «простота сейчас»
против «честность таблицы»; вернуть — `git revert`, история цела.

⚠️⚠️ **ЧТО НЕ ЯВЛЯЕТСЯ «ЗАЩИТОЙ ОТ НАКРУТКИ» И ОСТАЁТСЯ** (снести заодно —
типичная ошибка): подпись `sig` — это ПРАВО СОБСТВЕННОСТИ на строку, без неё
посторонний перезапишет чужой результат и удалит чужие данные; ограничение
частоты — бережёт НАШ бесплатный тариф, а не честность; `/admin/hide` и флаг
`f` — ручная модерация, теперь единственная.

**ВТОРАЯ ПОЛОВИНА РЕШЕНИЯ, ПРО ПОКАЗ:** место игрока показывается НА ЭКРАНЕ
ПОБЕДЫ после каждого уровня, а не только на отдельном экране. Соседи сверху-
снизу (`up`/`dn` из `/v1/me`) важнее общего топа: «до следующего 300 очков»
мотивирует, «ты 4172-й» — нет. ⚠️ Врезка не смеет задерживать переход на
следующий уровень: данные асинхронно, не пришли за секунду — экран живёт без них.
**Трата на множитель обязана опускать в таблице СРАЗУ** (отправка по любому
изменению баланса, `onStarsChange`, а не по победе) — подтверждение «Форбс»-
модели, не новая фича.

⚠️ ИСТОРИЧЕСКОЕ: замер двух ступеней потолка (обрезка → скрытие: в игре 50 000,
в таблице 2 000; скрытие включалось со ВТОРОЙ отправки) описывал механику,
которой больше НЕТ — снята в `60eeec1`. Из `server/leaderboard/README.md` блок
удалён вместе с механикой; замер сохранён ЗДЕСЬ и только как объяснение, ПОЧЕМУ
от неё отказались. ⛔ Не искать его в README и не восстанавливать там.

## РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-08-09 (потолок доверия таблицы)

**`GROW_PER_S` 25 → 125 (×5).** Дословно: «достаточно Б, мы сильно много
перестраховываем». Вариант «не показывать заниженное число, пока сервер не
догнал» ОТКЛОНЁН — заниженное остаётся на экране.

⚠️⚠️ **ЭТО ТРЕТЬЯ КАТЕГОРИЯ ПРАВКИ: НЕ ПО ЗАМЕРУ И НЕ ПО ОПАСЕНИЮ, А ПО СЛОВУ
ВЛАДЕЛЬЦА.** Константа была помечена «ждёт данных» по правилу «правка по ЗАМЕРУ,
а не по опасению». Данных не появилось — появилось продуктовое решение. Кто
придёт крутить её назад, обязан видеть, что спорит не с измерением.
⚠️ ЦЕНА НАЗВАНА ВЛАДЕЛЬЦУ И ПРИНЯТА: сдерживание в реалистичном случае
(«обогнать верхнего игрока со 100 000») падает с ~часа до ~13 минут.
⛔ Фраза «мы сильно много перестраховываем» сказана про ЭТО ОДНО число и НЕ
является мандатом ослаблять прочие защиты — остальное по-прежнему по замеру.

## КОНТЕКСТ, ЗАЧЕМ ПОТОЛОК ВООБЩЕ (для будущих споров)

Счёт считает КЛИЕНТ, сервер проверить его не может. Без потолка любой шлёт
«миллион» и встаёт первым, не сыграв. Потолок = доверие копится со временем:
`GROW_BASE` 2000 сразу + `GROW_PER_S` в секунду.
⚠️ ЦЕНА ЧЕСТНОМУ ИГРОКУ, ЗАМЕРЕНА НА СТЕНДЕ (диспетчер, 2026-08-09): доверие
копится у СТРОКИ, а не у игрока, поэтому смена устройства / переустановка /
чистка кэша дают новую строку и обнуляют его. Две ступени: сперва счёт ОБРЕЗАН
и строка стоит в таблице с ЧУЖИМ числом (в игре 50 000, в таблице 2 000), и
только со ВТОРОЙ отправки строка скрывается; своё место отдаётся в обоих
случаях. ⛔ Ссылка на подробности в `server/leaderboard/README.md` СНЯТА: блок
удалён вместе с механикой (`60eeec1`), указывать туда больше некуда.

## ⛔ ПРАВИЛО ДИСПЕТЧЕРА №0 (слово владельца 2026-08-05, СТРОГО)

**Диспетчер ОБЯЗАН следить за изменениями своих агентов — постоянно, а не
только по факту их сдачи.** Дословно: «ты всегда должен следить за
изменениями своих агентов».

Сверка ОБЯЗАТЕЛЬНА каждый раз при возврате к работе (после паузы, смены
контекста, чужого сообщения):
1. `git fetch blender && git branch -a --sort=-committerdate | head -20` —
   какие ветки направлений появились или сдвинулись;
2. `git log --oneline -5 <ветка>` по новым — что там реально лежит;
3. `git status` в ГЛАВНОМ дереве «Blendo v2» — не оставил ли кто-то
   незакоммиченных правок в общем чекауте (случалось дважды);
4. `git ls-remote blender` — совпадают ли локальные головы с GitHub;
5. `mcp__ccd_session_mgmt__list_sessions` — кто писал последним, чьи сдачи
   ждут приёмки.
Никогда не считать, что «раз сообщения нет — работы нет»: направления
работают асинхронно и заканчивают между ходами диспетчера.

⚠️ Следствие: **упущенный пункт владельца — отказ ДИСПЕТЧЕРА, а не
забывчивость направления.** Каждую просьбу владельца заносить в задачи
(TaskCreate) сразу при получении и закрывать ТОЛЬКО по факту влитого кода,
а не по факту раздачи направлению.

⚠️⚠️ **ОБОРОТНАЯ СТОРОНА ПРАВИЛА, КОТОРОЙ ЗДЕСЬ НЕ БЫЛО И КОТОРАЯ СТОИЛА ДВУХ
ВЕТОК (2026-08-12): ЗАШЁЛ В ЗОНУ НАПРАВЛЕНИЯ — ПРЕДУПРЕДИ ЕГО В ТОТ ЖЕ ХОД.**
Правило №0 обязывает меня следить за направлениями; про обратное движение не
было сказано ничего, и вышло так: экран таблицы был передан ИНТЕРФЕЙСУ отдельным
разделом канона («передача состояния», с расписанным порядком шагов), а потом я
сделал его САМ и им не сказал. Они честно работали от переданной базы `2249cc1`
и принесли `claude/interface-lb-merge` + `claude/interface-div` — **обе целиком
перекрыты моей же работой**, прижилась ОДНА строка (их ассерт ширины оказался
строже: `=== 560` против моего коридора `520..560`).
⛔ И цена не только в потерянном времени: их редакция, будучи влитой, **откатила
бы два исправления, сделанных ПОСЛЕ их базы** — `margin:auto` в прокручиваемом
флексе (верх карточки уходил за границу без возможности докрутить, замер −1284)
и `position:absolute` у крестика вместо `sticky` (прямая просьба владельца «на
странице лидерборда кнопка закрытия стики»). То есть параллельная работа опасна
не дублем, а ТИХИМ ВОЗВРАТОМ вылеченного.
**Практика: перед тем как самому взяться за то, что записано за направлением, —
`send_message` в его чат («беру сам, не начинайте») и пометка в разделе передачи.
Одна строка сообщения дешевле двух веток и разбора на 46 строк.**
⚠️ И приёмочное: сдачу направления, чья база отстала, сверять НЕ мержем, а
построчно против ТЕКУЩЕЙ головы — трёхточечный `git diff v2...ветка` показывает
их правки против МЕРЖ-БАЗЫ и создаёт впечатление, будто у меня этого нет.

## СОСТОЯНИЕ ОБЪЕКТОВ — сверено прогоном 2026-07-31

⚠️ ВСЕ ЧИСЛА ЭТОГО РАЗДЕЛА СНЯТЫ ПРОГОНОМ И РАЗБОРОМ `30-shapes.js`, не по
памяти. Устареют они снова — сверяйся с прогоном, а не с этим абзацем; как это
делается, показано в хронологии ниже (`__game.accSnapshot()` на уровне 200
отдаёт весь пул, поле `tex` даёт пачку).

**⚠️⚠️ ПУЛ СОКРАЩЁН 120 → 88 ТИПОВ (слово владельца 2026-08-15, «убирай из
моделей совсем» + два пакета скриншотов).** Вырезаны 32 типа: `brickcorner`,
`brickstud`, `bricksquare`, `brickduo`; `piratecannon`, `piratecrate`,
`pirateball`, `piratetower`, `piratedoor`; `survivalbarrel`, `survivalbottle`,
`survivalbucket`, `survivalchest`, `survivaltoolaxe`, `survivaltoolhammer`,
`survivaltoolpickaxe`; `holidayhanukkahdreidel`, `holidaypresentaround`;
`foodcabbage`, `foodbeet`, `foodcoconut`, `foodcookie`, `foodleek`, `foodtaco`;
`factoryboxsmall`, `factorycogc`, `factorypistonround`; `toycaritemcoingold`,
`toycaritemcone`; `marketcashregister`, `marketshoppingbasket`;
`arcadeclawmachine`.
⚠️ РАЗВИЛКИ ЗАКРЫТЫ ЕГО ЖЕ СЛОВОМ, НЕ УГАДЫВАНИЕМ: «Chest» — их в игре было
ДВА, он выбрал `survivalchest` (пиратский остался); `brickbar` он ОСТАВИЛ,
узнав, что на нём калиброван порог просадки тонких предметов в пол.
⚠️ ЧТО ЭТО СДВИНУЛО: весь пул открыт с ~88-го уровня вместо 112-го (типов
9+уровень); `index.html` 10.34 → 10.34 МБ (геометрии остались в генерённых
модулях — вырезаны только строки TYPES, вернуть тип = одна строка).
⛔ **ГОЛОС «СТЕКЛО» ОСТАЛСЯ БЕЗ НОСИТЕЛЯ:** его нёс ЕДИНСТВЕННЫЙ `survivalbottle`.
Звук жив, предметов у него нет; ассерт стекла снят, а не подменён (страж
умирает вместе с механикой). Появится стеклянный предмет — вернуть и проверку.
⚠️ ТРИ СТРАЖА ПЕРЕЕХАЛИ, И ДВА ИЗ НИХ КРАСНЕЛИ НЕ ПО ДЕЛУ: порог «карточек
> 100» и «типов > 100» протухли от самого сокращения (проверяют размер пула, а
не своё свойство) — опущены до 80; сентинелы перемешивания взяты ЗАМЕРОМ
фактического состава (`piratepalm`/`cartaxi`/`foodeggplant`), а не по памяти —
моя первая замена промахнулась, снеговик к ур.20 ещё не открыт.

**Пул: `TYPES` = 120 типов, 12 атласов** (по полю `tex`):

| пачка | шт | пачка | шт | пачка | шт |
|---|---|---|---|---|---|
| food | 41 | pirate | 8 | toycar | 5 |
| animal | 24 | survival | 8 | factory | 4 |
| car | 12 | brick | 7 | market | 2 |
| | | holiday | 7 | arcade / forest | 1 / 1 |

- ЗАМЕС в массиве: базовый цикл `3 food : 3 animal : 1 car : 1 brick : 1 pirate`
  (решение владельца 2026-07-22), а 28 типов Kenney ВПЛЕТЕНЫ через каждые
  4 позиции начиная с индекса 15 (индексы 15, 19, 23 … 115, плюс 118 и 119) —
  это результат спеки владельца «перемешивай типы» (2026-07-30).
  ⚠️ ПОРЯДОК МАССИВА И ФОРМУЛА СПАВНА — РЫЧАГ СЛОЖНОСТИ, а не косметика: любая
  правка, меняющая СОСТАВ уровня (перестановка типов, другая выборка в genLevel),
  делается ТОЛЬКО ПО СПЕКЕ ВЛАДЕЛЬЦА. Запрет действующий, не исторический.
- ПРОГРЕССИЯ: `typesCount = LEVEL_TYPES_MIN + (уровень − 1)` = `9 + (уровень−1)`,
  типы открываются ПО ПОРЯДКУ массива. Отсюда: первый Kenney виден с ур.8,
  последний — с ур.112; ВЕСЬ пул открыт с ур.112.
- ⚠️ ПОНЧИК `fooddonutsprinkles` СТОИТ НА ИНДЕКСЕ 117 → открывается с ур.110,
  и с v181 он ДЕЙСТВИТЕЛЬНО попадает в кучу (см. хронологию: раньше хвост был
  мёртв, и «вопрос дырки convex hull» считался закрытым тем, что предмета в игре
  нет). ✅ **ЗАКРЫТО 2026-08-07 (ФИЗИКА): дырка настоящая** — флаг типа
  `phys:'ring'` (30-shapes) уводит модель ДО ветки hull, а `ringFromGeometry`
  (50-physics) строит кольцо капсул, ВЫВОДЯ числа из вершин: плоскость выбирается
  по наибольшему отношению rmin/rmax, радиусы кольца и трубки считаются замером.
  Сменится модель или её масштаб — кольцо переедет само.
  ⚠️ У НАШЕГО пончика дырка в **XZ (ось Y)**, а не в XY, как у процедурного
  `torus` из three: плоскость определяется ЗАМЕРОМ, а не по аналогии с соседним
  случаем — иначе повторилась бы грабля 2026-07 (компаунд перпендикулярно мешу
  «впаивал» предметы в видимое кольцо).
  ⚠️ Порог `ratio >= 0.25` — НЕ автодетектор кольца, а страховка при явном флаге:
  у плоской плашки `brickbar` отношение 0.599 при полном отсутствии дырки. Ставить
  `phys:'ring'` по одному отношению нельзя.
  ⚠️ Цена нулевая (ур.110, CPU ×4 на GPU, 6 сидов, 2 пончика): коллайдеров
  605 → 627, шаг физики p95 16.2 → 16.6 при разбросе по сидам 10-18.
- КИРПИЧИ в атласе белые — КРАШЕНЫ через `material.color` (ветка `t.paint`
  в 40-items); `paint` стоит ровно у 7 типов, все пачки brick.
- У 16 типов задан `wr` — override горизонтального габарита для теста стены
  (охватный радиус у плоских моделей его переоценивает, см. раздел физики).
- ⚠️ СОСТАВ КУЧИ НЕДЕТЕРМИНИРОВАН при `typesCount > pairsCnt` (с ур.83 — на 82-м
  ровно равенство 90 = 90, берутся ВСЕ открытые типы и набор ещё детерминирован): genLevel
  берёт СЛУЧАЙНЫЕ `pairsCnt` типов из открытых (Фишер-Йетс). Ассерты «тип X
  в куче» выше ур.82 обязаны собирать ОБЪЕДИНЕНИЕ по нескольким регенам —
  одиночный реген даёт ложное падение ~26%.

**Спецпредметы — ВНЕ пула `TYPES`** (прогрессию типов не двигают):
- СЮРПРИЗ — золотая РЫБКА (`animalfishGeo`, фолбэк `gemGeo`), `makeSurprise`
  в 40-items; одна на уровень, лежит на дне.
- БОМБА — переливающийся чёрный шар, `makeBomb`; одна на уровень.
- ⛔ КАМНИ УДАЛЕНЫ 2026-08-17 (слово владельца «модели плохие, они не нужны,
  удали их полностью из игры») — ни спавна, ни модуля, ни моделей. Прежняя
  рампа «с ур.16 одна штука, +1 каждые 5, кап 6» ИСТОРИЧЕСКАЯ.

**Заполнение и сборка:** `PAIRS` = 90, укороченные уровни 1-3 — `PAIRS_EARLY` =
[64, 71, 78]. ⚠️ ЧИСЛО ПРЕДМЕТОВ НА УРОВНЕ ЗАМЕРЕНО `__game.alive()`, а не
выведено формулой: ур.1 — **130**, ур.3 — 158, ур.4-15 — **182**, ур.16 — 183,
ур.21 — 184, ур.41 — 188. ⛔ ХОДЯЧАЯ ФОРМУЛА «пары×2 + 1 = 181» УСТАРЕЛА
и живёт в старых разделах ниже: она из эпохи ДО БОМБЫ. Сегодня в куче ещё и
бомба (одна всегда), а с ур.16 — камни; плюс трим может тихо срезать пары, из-за
чего на высоких уровнях бывает меньше кэпа (замер: ур.60 — 186 при 188 на ур.41).
Кап группы матча
`MATCH_MAX_N` = 8. Сборка — **21 модуль**; ⚠️ ВЕС ПЕРЕМЕРЕН 2026-08-07 (голова
69eeb7c, `zip -9` четырёх файлов): `index.html` **9.46 МБ**, ZIP пакета портала
(index + 2 bridge + music.mp3) **6.47 МБ**, запас до лимита 8 МБ — **1.53 МБ**
(80.9% лимита). Прежние 9.04 / 6.33 / 1.67 — состояние до партии правок августа.
⚠️ Мерить ЗИПОМ, а не суммой файлов: сырых там 13.9 МБ, и по ним вывод был бы
«лимит превышен». Число проверять при каждой партии моделей — до гибрида
(решение владельца 2026-08-06) каждая новая партия тратит этот запас.

**Чего в пуле НЕТ** (проверено тем же разбором — имён нет вовсе): стейк,
леденец, чайник, процедурные примитивы. Причины и порядок возврата — в
хронологии.

## Хронология партий объектов

Свод слоёв, которые раньше лежали в шапке друг поверх друга. Ценность — не в
числах (они устарели и заменены разделом выше), а в ПРИЧИНАХ решений и в
запретах: их формулировки сохранены дословно.

- **2026-07-20, базовая линия.** `docs/OBJECTS-STATE.md` + git-тег
  `objects-baseline-2026-07-20` — сняты ДО партии моделей ГРАФИКИ, читать как
  историческую точку отсчёта, а не как текущее состояние.
- **2026-07-21, примитивы вон.** Решение владельца «процедурные формы удали
  совсем»: 17 процедурных примитивов убраны ИЗ ПУЛА. Фабрики в 30-shapes живы
  намеренно — на `gemGeo` висит фолбэк геометрии сюрприза. Вернуть примитив =
  строка в TYPES. Чайник удалён насовсем (тип, геометрия и модель).
- **2026-07-22/23, партия владельца.** TYPES=93: 92 модели с родными атласами
  (24 зверя + 41 фрукт-овощ + 12 машин + 7 кирпичей + 8 пиратских, замес
  3:3:1:1:1) + стейк. ⚠️ Встречающееся в старых абзацах число 78 относилось
  к состоянию ДО добора Food/Car в v1-test-66. Rocks из Pirate в пул НЕ
  вводились — они у ФИЗИКИ под несовмещаемые камни.
- **2026-07-30, найден дефект «хвост мёртв» (ГРАФИКА, подтверждено
  диспетчером по исходнику).** `pairs.push({ type: i % typesCount })` в genLevel,
  где `i` пробегает 0..pairsCnt−1, а pairsCnt ≤ PAIRS = 90 → достижимы только
  индексы 0..89. Последние три типа не попадали в игру НИКОГДА (проверено на
  ур. 60/95/120 и подсчётом по TYPES). ⛔ Это ОТМЕНИЛО два прежних утверждения
  канона: (1) «пончик уведён в хвост, так закрыт вопрос дырки convex hull» — он
  не открывался никогда, дырка «закрыта» тем, что предмета нет в игре, а это не
  решение, а сокрытие; (2) «потолок TYPES.length» — реальным потолком был
  PAIRS = 90.
- **2026-07-30, v178/v181 — хвост оживлён.** Спека владельца «правь формулу
  спавна». ФИНАЛЬНАЯ РЕАЛИЗАЦИЯ — ФИШЕР-ЙЕТС ГРАФИКИ, а не развёртка v178: одну
  строку чинили ДВАЖДЫ параллельно, победила версия со случайной выборкой
  открытых типов на каждую раскладку (разнообразие против фиксированного
  поднабора). Ниже 82-го уровня поведение не изменилось бит-в-бит.
  ⚠️ ПЕРЕВЁРНУТУЮ развёртку пробовал и отверг ЗАМЕРОМ (гипотеза «последний тип
  срезает трим»): A/B 6 раскладок × 3 уровня дал 17/18 против 17/18 — разницы
  нет. Не изобретать заново.
  ⚠️ И методическое оттуда же: проверять достижимость хвоста ТОЛЬКО с 85-го
  уровня — на 84-м типа нет ПО ПРОГРЕССИИ, а не по формуле; на этом были
  потеряны два прогона и сочинено ложное объяснение.
- **2026-07-30, v179 — стейк ближе к началу.** Спека владельца «двигай стейк
  ближе к началу»: индекс 9, открытие со второго уровня. Перестановка БЕЗОПАСНА
  для сейвов: `ac`/`bo`/`uk` ключуются ИМЕНАМИ типов, а не индексами (проверено
  по 77-save ПЕРЕД правкой) — это правило действует и для любых будущих
  перестановок.
- **2026-07-30, партия Kenney (+28 типов).** ⚠️ Пришли с той же болезнью, что
  была у стейка: контент есть, а открывается на ур.86-113, то есть увидят
  единицы. Решение о перемешивании ближе к началу принимал владелец — оно и
  дало нынешнюю вплетённую раскладку.
- **2026-07-30, вечер — удаления по слову владельца.**
  ⛔ СТЕЙК УДАЛЁН СОВСЕМ (слово владельца Графике) — это ОТМЕНА его же утренней
  спеки «двигай стейк ближе к началу»: утром модель уводили на индекс 9 с
  формулировкой «обязана быть в игре», вечером владелец передумал. 35-steak.js
  удалён, страж «стейк со второго уровня» снят вместе с типом. Вернуть:
  35-steak.js из истории git + строка TYPES + ассерт. НЕ читать удаление как
  оплошность — причина в коммите 54597dc.
  Леденец `holidaycandycanered` удалён по скрину владельца («толстая полосатая
  труба, к стилю не шла»); сентинел стража перемешивания заменён на дрейдел.
- **2026-07-30, тест реалистичной стилистики.** Пак Dirty показан владельцу
  живым тестом и ОТВЕРГНУТ («выглядят сильно инородно и грязно»). Формулировка
  и подробности — в разделе «Отвергнутое», в пул не предлагать.

⚠️ Это «ветка» v1 по плану docs/DESIGN-ROADMAP.md. Стабильный релиз-кандидат
БЕЗ меты живёт в соседней папке funnel-game (не трогать при работе здесь).
Превью-сервер этой папки: launch.json конфиг `Main`, порт 8779 (переименован 2026-07-22; конфиги funnel/funnel-v1 удалены).

⚠️⚠️ ПАРАЛЛЕЛЬНАЯ РАЗРАБОТКА (с 2026-07-20): работа разделена по
направлениям (графика/физика/повествование/интерфейс/интеграция) в разных
чатах. ПЕРВЫМ ДЕЛОМ прочитай **WORKSTREAMS.md** — там зоны файлов, правила
(работать только в своей зоне, междузонные запросы, протокол завершения
сессии) и актуальное состояние каждого направления. Свой блок обновляй
в конце сессии.

## ⛔⛔ ОТМЕНЕНО 2026-08-17 — v1-тюнинг 2026-07-22-в: НЕСОВМЕЩАЕМЫЕ КАМНИ

> ⛔⛔ **КАМНЕЙ В ИГРЕ БОЛЬШЕ НЕТ.** Слово владельца по двум скриншотам: «это
> какие-то камни, модели плохие, они не нужны. Удали их полностью из игры».
> Это ОТМЕНА ЕГО ЖЕ СПЕКИ 2026-07-22 — читать раздел ниже только как историю
> решений, а не как описание кода. Что снято: `ROCK_TYPES`, `rocksForLevel`,
> `makeRock`, спавн в genLevel, модуль `37-rocks.js` (87 КБ), `DENSITY.rock`,
> `SHAKE_RESP.rock`, ветка тапа, флаг `it.rock` во ВСЕХ фильтрах (~30 мест),
> хуки `rocks()`/`rockIndex()`, секция сьюта (14 ассертов), модели из
> «3d assets». Сборка 23 → **22 модуля**, `index.html` −92 КБ.
> ⚠️⚠️ **ОДНА ВЕЩЬ ПЕРЕИМЕНОВАНА, А НЕ УДАЛЕНА, И ЭТО НЕСУЩЕЕ:** `penalizeRock`
> → **`penalizeDouble`** (80-gameplay). Её единственный живой потребитель —
> ГЛЫБА: спека льда дословно требует «штраф КАК У КАМНЯ». Снос функции вместе
> с камнями тихо изменил бы механику льда. Поймано ГРЕПОМ ПОСЛЕ вырезания —
> перепись потребителей делать ДО, а не после.
> ⚠️ Число предметов на уровне вернулось к базе: ур.16/21/41 было 183/184/188,
> стало 180-181 (разброс ±1 даёт бомба, она не на каждом уровне).
> Вернуть камни — `git revert`, вместе с ними вернётся и секция сьюта.

### История решения (ниже — как было до отмены)

## v1-тюнинг 2026-07-22-в: НЕСОВМЕЩАЕМЫЕ КАМНИ (спека владельца через диспетчера)

- Спека: rocks-a / rocks-sand-c из «3d assets/Pirate» — спецпредметы ВНЕ
  TYPES-пула (прогрессию типов не двигают). Модуль `37-rocks.js` —
  генерённый (tools/glb2module.py, точечный прогон по двум glb; 264/448
  треугольников, свой атлас); ⚠️ пост-генерация: из модуля УДАЛЕНЫ повторные
  декларации `const MODEL_ATLASES` и `const _atlasTex` (реестр и кэш живут
  в 36-models, дубль-const валил IIFE «already declared»; функции-хелперы
  дублируются легально — байт-в-байт).
- Правила: спавн rocksForLevel — с 16-го уровня 1 шт, +1 каждые 5 уровней,
  кап 6 (ROCK_* в 00-config); тяжёлые (DENSITY.rock=2.6); НЕ совмещаются
  (key 'ROCK#i' уникальны); убрать досрочно может только БОМБА (легальные
  жертвы detonateBomb, входят в кап 7); mixerGrind и trimOverfill их не
  трогают, финальная зачистка доедает; тап = penalizeRock (80-gameplay):
  ДВОЙНОЙ штраф 2×MISS_PENALTY=20 через единую точку scorePenalty (ур.1
  без штрафов, кламп ур.2-5 уважаются), misses/срез комбо — как у промаха,
  в финале штрафа нет; вне победы/эндшпиля/автопана: aliveCnt (∞<=8) и
  aliveN (пар-скор + порог 20%) камни не считают; вуалью НЕ мигают
  («иная природа»), но телами честно блокируют лучи доступности.
- makeRock (40-items): рецепт материала — КОПИЯ matcap-ветки makeItem
  (камни вне пула, makeItem индексирован по TYPES) — при правке материалов
  моделей синхронизировать; SHAKE_RESP.rock=0.7 (лениво).
- Сьют: секция камней В КОНЦЕ файла НАМЕРЕННО (setLevel/regen меняют
  контекст — в середине ломали «полный прогон» ур.1: 23 встряски, ранняя
  камера); ∞-цикл с передышкой раз в 10 матчей (иначе серия турбо держит
  чашу вечно — как в полном прогоне); сэмпл ∞-радиуса ждёт refresh-тик
  до 900 мс (мгновенное чтение ловило старый 1.1 — флейк, починен и в
  старом эндшпиль-ассерте). __game.rocks()/rockIndex(). findByTex — v2
  (флейк-репорт диспетчера v76: клик по проекции ЦЕНТРА попадал в
  загораживающий предмет, «−20» превращался в «+120»): рейкаст с камеры по
  центру + 8 смещениям, отдаётся пиксель где предмет ПЕРВОЕ пересечение;
  целиком закрыт -> {occluded:true}, тест встряхивает и повторяет (до 5).
- ПЕРФ по п.9: ур.41 (6 камней, тел 605) против ур.15 (0, тел 599): шаг
  физики p95 6.0/7.0 мс против 6.4/7.5 базы — в шуме, бюджет 25 с запасом;
  финал доедает камень (победа за 13.7 с на ур.16-одиночках). Экономика
  встрясок с 6 камнями — терпеливый бот, см. WORKSTREAMS.

## v1-тюнинг 2026-07-27-г: КАП РАЗМЕРА ГРУППЫ = 8 (спека владельца)

- Спека владельца (дословно, после вопроса «у нас стоит какой-то максимум на
  совмещение ближайших объектов?»): «поставь кап на 8». ДО ЭТОГО КАПА НЕ БЫЛО
  ВОВСЕ — тап уносил ВСЕ доступные однотипные в радиусе (doMatch без slice).
- MATCH_MAX_N=8 (00-config). Считается ВЕСЬ матч, ВКЛЮЧАЯ тапнутый.
  Отсечение по ДАЛЬНОСТИ: остаются ближайшие по истинному зазору (метрика
  бомбы) — «схлопнулось вокруг пальца»; лишние живут дальше и матчатся
  следующим тапом (сироты легальны, финал их доедает).
- ТОЧКИ ПРИМЕНЕНИЯ: handleTap (боевой матч) и findHintGroup (подсказка не
  должна обещать группу больше той, что реально соединится). Бот autoMatch
  матчит парами — его кап не касается; бомба живёт своим BOMB_MAX=7.
- ЗАМЕР ПОТОЛКА ПО ДОСТУПНОСТИ (до капа): ур.1 (9 типов) до 16 штук,
  ур.10 (18 типов) 10, ур.40 (48 типов) 3-4 (Hard 3). ⚠️ Значит кап реально
  работает ТОЛЬКО НА РАННИХ уровнях, на поздних не меняет ничего.
  Проверка боем (21 реальный тап на ур.1): распределение групп
  {1:2, 2:3, 3:2, 4:5, 5:4, 6:1, 7:1, 8:3} — три матча упёрлись ровно в кап.
- ⚠️ КАК ЧАСТО КАП СРАБАТЫВАЕТ — ИЗМЕРЕНО, ОТВЕТ: ПОЧТИ НИКОГДА (2026-07-28,
  два независимых замера после двух ошибочных оценок подряд — сперва моей,
  потом ФИЗИКИ). ⛔ ОТМЕНЯЕТ И «естественно 8 не бывает» (моё), И «срабатывает
  регулярно, каждый пятый матч» (ФИЗИКИ). Обе оценки были неверны.
  • ЗАМЕР ДИСПЕТЧЕРА (v158, 4 раскладки × 25 реальных тапов подряд, комбо
    копится как у человека): матчей 94, матчей размером 8 — восемь,
    НАСТОЯЩИХ срезов — НОЛЬ, макс raw НИ РАЗУ не превысил 8. Плюс более ранний
    прогон на пятой раскладке: 2 среза из 25.
  • ЗАМЕР ФИЗИКИ (30 тапов × 4 сида, счётчики разведены): 1 сид дал 2 среза
    при макс raw 10, остальные 3 — ноль срезов, макс raw 7.
  • ИТОГО ~2 среза на ~120 матчей, и то не на каждой раскладке.
  ⚠️ ЭТО ВЕРХНЯЯ ОЦЕНКА: замер ходит через `bestTapTarget` БЕЗ аргумента, а тот
  выбирает САМУЮ КРУПНУЮ группу на каждом тапе (модель внимательного игрока).
  Обычный игрок бьёт по первой замеченной паре и упрётся в кап ещё реже.
  ⚠️ ПОЧЕМУ «до 16 предметов за тап» НЕ ПРОТИВОРЕЧИТ: 16 — это потолок
  ДОСТУПНОСТИ (сколько копий типа одновременно доступно), а НЕ размер группы.
  В группу входят только те, кто попал в РАДИУС совпадения; даже на поднятом
  комбо-радиусе 1.1 их выходит 7-8, а не 16.
  ⚠️ МЕТОДИКА (на ней обожглись оба): «матч размером 8» НЕ доказывает срез —
  группа могла быть ровно 8 и без капа. Доказательство — `raw > 8` у
  `bestTapTarget` (размер ДО среза). Считать только по raw.
  ⚠️ ЧТО ЭТО ЗНАЧИТ ДЛЯ ФИЧИ: кап 8 — ПОТОЛОК-СТРАХОВКА от редких пиковых
  тапов, а не действующий регулятор экономики. Типичный матч (1-7) он не
  трогает. Детерминированный страж в сьюте (раздутый радиус) при этом
  обязателен — иначе механику капа не проверяет НИЧЕГО.
- ⚠️ СЛЕДСТВИЕ ДЛЯ ЗВЁЗД — СМЯГЧЕНО ЗАМЕРОМ (2026-07-28). Формула верна: цена
  группы 10·N·(N−1) КВАДРАТИЧНА, а пар-скор (finalizeFill) от размера группы
  НЕ зависит. ⛔ НО расчёт «16 -> 2400 очков за тап против 8 -> 560» ОПИСЫВАЕТ
  СЛУЧАЙ, КОТОРОГО В ЗАМЕРАХ НЕ БЫЛО НИ РАЗУ: групп по 16 не встречается,
  макс наблюдённый raw — 10. Реальная просадка раннего дохода МАЛА (срезы ~2
  на ~120 матчей), и прежнее «2★/3★ стали заметно труднее» — ПЕРЕОЦЕНКА,
  снята. Наблюдать всё равно стоит, но первым подозреваемым при жалобе
  «звёзды не даются» кап больше не считать.

## v1-тюнинг 2026-07-27-б: ВЗРЫВ БОМБЫ «ПОХОЖ НА SHAKE» (спека владельца)

- Спека владельца (батч 2026-07-27-б, дословно): «увеличь зону поражения бомбы
  в 2 раза + её взрыв похоже на эффект shake». Зона ×2 — часть ДИСПЕТЧЕРА
  (BOMB_RADIUS 2.86->5.72, v126). Здесь — вторая половина, зона ФИЗИКИ.
- ДВА СЛОЯ ВОЛНЫ (blastWave в 50-physics получил 4-й параметр jolt):
  (1) ПАНЧ — прежний радиальный удар в зоне поражения, квадратичное затухание
  (характер взрыва, чтобы он не стал копией встряски);
  (2) ДЖОЛТ — толчок ВСЕЙ куче БЕЗ отсечки по радиусу, мягкий спад 1/(1+d/R):
  вздрагивает весь миксер, включая верх — то самое «как shake». До правки верх
  кучи стоял неподвижно (панч до него не доставал).
- Константы (00-config, свои по правилу 3): BOMB_JOLT=2.5, BOMB_CAM_SHAKE=0.5
  (у встряски 0.42 — взрыву чуть больше), BOMB_WAVE_R_K=1.2 (радиус панча =
  BOMB_RADIUS×K, НЕ хардкод «+1.2»: зону владелец уже менял ×2, панч обязан
  ехать за ней).
- ⚠️ СИЛА ДЖОЛТА НАМЕРЕННО ~14% ОТ ВСТРЯСКИ (замер: верх кучи avg |v| 0.94
  против 6.6 у встряски). Цель — ОЩУЩЕНИЕ встряски, а не её механика: при
  сопоставимой силе бомба стала бы бесплатной встряской и обрушила экономику
  (бюджет 5 встрясок на уровень). Доступные пары после взрыва не растут —
  рыхления нет, только косметика.
- ⚠️ ГОРИЗОНТАЛЬ ДЖОЛТА ВЧЕТВЕРО СЛАБЕЕ ВЕРТИКАЛИ (замер 2026-07-27): при
  симметричном разбросе ±0.5j джолт ВЖИМАЛ предметы в стены и УДВАИВАЛ работу
  спасателя (4 телепорта против 2 на базе). После среза — 1 телепорт, то есть
  ЛУЧШЕ базы, а ощущение даже выросло (верх шевелится у 95% предметов против
  31% на симметричном варианте той же силы). Подброс вверх = «встряхнуло».
- ⚡ УСИЛЕНИЕ (спека владельца 2026-07-27-в «нужно усилить силу взрыва»):
  панч BOMB_WAVE_V 2.2->5.0, джолт BOMB_JOLT 2.5->6.0, камера 0.35->0.45,
  вибро [40,80,50]. Отклик верха кучи 0.84 -> 3.0-3.3 avg |v| (~12% силы
  встряски -> ~45%). Обе силы выведены в CFG (bombWaveV/bombJolt) — тюнинг
  и A/B без пересборки. ⚠️ ПОЧЕМУ НЕ СЛОМАЛО «не бесплатная встряска»:
  замер 3 сидов — доступные ПАРЫ не растут ни при какой силе (взрыв
  перемешивает, но не рыхлит; сама встряска на полной куче тоже даёт
  падение пар). Реальный ограничитель — СТЕНЫ, и там прирост в шуме:
  худший wallExcess 0.169 (прежняя сила) -> 0.177 (усиленная) при 0.141
  вовсе без волны; спасатель 1-4 телепорта в обоих случаях. Дальше 7.0/8.0
  не пошли — там шаг физики 8.9 мс против 6.5.
  ⚠️ ПОБОЧНОЕ НАБЛЮДЕНИЕ (не регрессия правки): на ур.40 wallExcess
  превышает историческую норму ~0.15 УЖЕ БЕЗ ВЗРЫВА (0.141) и при прежней
  силе (0.169) — норма ставилась для старой конфигурации (меньше типов и
  разброса размеров). Спасатель отрабатывает, висунов нет; при следующем
  соаке норму пересмотреть.
- ПЕРФ по правилу 9: шаг физики max 5.9 мс (база 4.6, бюджет 25), тела −8
  корректно, fx дренаж в 0, сон при v 0.4/1.1 (инвариант «сон при v>2.5 = баг»
  цел), wallExcess в шуме базы. Замерный хелпер __game.velByHeight() (отклик
  по слоям низ/середина/верх) — им и мерялось.

## v1-тюнинг 2026-07-22-б: ЧЁРНЫЙ ШАР-БОМБА (спека владельца через диспетчера)

- Спека (дословно): «добавь в игру черный шар, средний размер по отношению
  к другим вещам. Клик на него взрывает все вещи рядом в небольшом радиусе
  (не более 5-7 объектов)». Дефолты диспетчера: 1 шт на уровень, спавн в
  СЕРЕДИНУ столба заполнения, без очков, сюрприз/другие бомбы не задевает,
  миксер-наказание не ест, финал доедает, сироты от рваной чётности легальны.
- Реализация: makeBomb (40-items; сфера 0.95·MESH_SCALE «средний размер»,
  ПЕРЕЛИВАЮЩИЙСЯ материал — спека владельца 2026-07-23 «сделай переливающейся»:
  собственный радужный matcap `bombMatcap` (r149 не умеет
  MeshPhysicalMaterial.iridescence) + плоский MeshMatcapMaterial, разводы
  «плывут» по нормали в камере при качении; тёмная жемчужная база + тонко-
  плёночная радуга + узкая искра, света не требует. Было — плоский чёрный
  MeshBasicMaterial. ⚠️ Вуаль Hard (60-access) множит color на нейтральный
  DIM_GREY: завалённая бомба лишь тускнеет ~30%, hue цел. Тело через живую
  ветку 'ball' 50-physics, key 'BOMB' уникален — пары/подсказка/прицел/
  hasAnyPair её не считают); спавн в genLevel при n===pairsCnt (половина
  столба). detonateBomb (80-gameplay): жертвы = ближайшие по зазору
  охватных сфер <= BOMB_RADIUS (2.2), сортировка по дистанции, кап
  BOMB_MAX=7; эффект: тёмная труха бомбы + ПАК-ЭФФЕКТЫ жертв («пункт 5» —
  сок/искры/звёзды бесплатно разнообразят взрыв) + blastWave BOMB_WAVE_V=2.2
  + camShake/вибра; хвост удаления — паттерн doMatch (afterPause 150 мс,
  animating-гварды). Комбо/серии взрыв НЕ трогает; тап = действие
  (lastAction). Гварды: mixerGrind !bomb, trimOverfill !bomb.
  __game.bombIndex()/detonate(); тест-секция в сьюте (старт 111-130).
- ПЕРФ по правилу 9 (замер на полной куче 183, спящая база): пик окна
  взрыва frame.max 148.6 мс / step.max 4.8 мс = РОВНО пик обычного матча
  пары (контроль на той же куче); частиц взрыв даёт ~1.8к против ~2.5к у
  пары (труха 1280 у бомбы + ~500 пак-эффектов жертв); тела/коллайдеры
  вернулись ровно −8 (7 жертв + бомба), fx в 0. Митигаций не требуется.

## v1-тюнинг 2026-07-23: ОСКОЛКИ + ЭФФЕКТНЫЙ ПОМОЛ (спека владельца)

- Спека владельца (два пункта): «1. Сделай осколками» (пак-эффект бурста
  для brick/pirate/rock — было труха); «2. Нужно молоть эффектно, подумай
  над вариантом и реализуй».
- ОСКОЛКИ — shardFX (70-fx, ОТПОЛИРОВАН ГРАФИКОЙ 2026-07-23; перенесён из
  80-gameplay): угловатые куски цвета предмета (fxColor), разлёт баллистикой
  с кувырком, гаснут в полёте. НЕРЕГУЛЯРНАЯ форма (makeShardGeo — 4 угла
  тетраэдра сдвинуты ±38%, каждый скол уникален) + ТИНТ ПО ГРАНЯМ (на плоском
  MeshBasicMaterial света нет, объём печём в ВЕРШИННЫЕ ЦВЕТА по нормали грани
  к ключевому свету — тому же, что у matcap) + звук «хруст» (75-audio 'crunch',
  спектр выше рокота grind). КАЖДЫЙ осколок — СВОЯ геометрия+материал (stepFX
  диспозит и то, и другое; общий кэш отдавать нельзя — грабля Sprite/star).
  Роутинг в burstFX (80-gameplay, зона ФИЗИКИ) и вызовы grindShred не тронуты —
  сигнатура та же (pos, color, opts{count,life,up,spread,size}).
- ЭФФЕКТНЫЙ ПОМОЛ — grindShred (80-gameplay), общий помощник mixerGrind и
  finaleGrind. Двухфазно: (1) захват — предмет рывком с ускорением
  утягивается к плоскости ножей, дрожит и СПЛЮЩИВАЕТСЯ; (2) шинковка — меш
  гаснет, из-под лопастей фонтаном бьют ОСКОЛКИ + пылевой взрыв + тряска.
  ⚠️ Шинковка на РЕАЛЬНЫХ часах (setTimeout grab*1000), а НЕ в тике addFX:
  тик растёт на клампнутом dt, и на <~20 FPS шинковка по FX-часам наступала
  ПОЗЖЕ removeItem (560/410 мс) — фонтан отрывался от исчезнувшего меша
  (поймано ревью 2026-07-23). Тряска параметризована: наказание mixerGrind
  0.28, финал СПОКОЙНЫЙ 0.1 (finaleGrind раньше камеру не трогал — не
  дёргать её каждые 0.5 с весь финал). Лопасти уже раскручивались при
  grinding (20-arena) — теперь под ними виден результат.
- ПЕРФ по правилу 9 (худший случай — бомба взрывает до 7 осколочных жертв,
  до ~49 меш-осколков): пик шага физики 4.6 мс (бюджет 25), draw calls +20
  к базе (201->221), геометрии вернулись к базе бит-в-бит (96->96) — утечки
  нет, тела −8 корректно. Митигаций не требуется.

## v1-тюнинг 2026-08-01: СХЛОПЫВАНИЕ / РАСПИЛ / ОГОНЬ (выбор владельца по стенду)

Спека владельца: «нужно ещё проработать эффекты соединения предметов и помола.
Сейчас у многих мелкие частицы, поведение понятное, но скучное» + «покажи как
он может прям гореть». Показан СТЕНД из девяти вариантов на живой сцене
(ветка стенда удалена после выбора), владелец выбрал четырьмя ответами.

- **СОЕДИНЕНИЕ — СХЛОПЫВАНИЕ** (`collapseFX` в 70-fx): группа за `COLLAPSE_MS`
  =150 слетается в ТОЧКУ ТАПА, сжимаясь, и лопается ОДНИМ событием оттуда.
  Было: каждый предмет уменьшался на месте и выдавал своё облачко — эффект
  читался как «замена предмету», а не как удар. Правило пачки `BURST_MIN_N`
  СОХРАНЕНО: >= 4 — эффект пачки, пара/тройка — труха; изменились МЕСТО (точка
  тапа) и КОЛИЧЕСТВО (одно событие вместо N).
  ⛔ **ХИТ-СТОП ОТКЛОНЁН ВЛАДЕЛЬЦЕМ** — не возвращать. Он единственный в пакете
  трогал не картинку, а ОЩУЩЕНИЕ ввода (кадр замирал на 50 мс).
  ⚠️⚠️ **ХЛОПОК ВИСИТ НА ТЕХ ЖЕ РЕАЛЬНЫХ ЧАСАХ, ЧТО И `removeItem`** (общий
  `setTimeout` в doMatch), а НЕ на тике анимации: стягивание идёт по игровому
  времени, и на просевшем FPS тик до конца не доходит — хлопок, повешенный на
  него, не наступил бы вовсе. Разводить эти часы обратно нельзя.
- **ПАЧКИ КРУПНЕЕ**: еда — `juiceBigFX`, ⚠️ **ПЕРЕДЕЛАН 2026-08-02 ПО СЛОВУ
  ВЛАДЕЛЬЦА** (дословно: «после совмещения фруктов от них появляются какие-то
  капли и сейчас это выглядит плохо потому что: они не в плоскости блендера,
  они крупные. Замени эти капли на мелкие брызги»). Теперь МЕЛКИЕ БРЫЗГИ:
  54 точки размера 0.075 (класс крошки) коротким веером, жизнь 0.5 с.
  ⛔ Капли «на стекле экрана» (`screenDripsFX`, DOM поверх канваса) УДАЛЕНЫ
  ЦЕЛИКОМ — это и есть «не в плоскости блендера». Приём рабочий сам по себе, но
  НЕ в игре, где всё живёт внутри чаши; не возвращать без слова владельца.
  ⛔ И «жирная капля» 0.86 отменена: она была МОИМ прочтением спеки «крупнее
  куски», и на фруктах прочтение оказалось неверным — крупный сок читается не
  сочностью, а браком. Спека «крупнее» осталась верна для ОСКОЛКОВ и кусков,
  но не для брызг;
  машины — `sparkRicochetFX` (искры ОТСКАКИВАЮТ от стенок чаши + отлетает и
  укатывается колесо); звери — звёзды как были (владелец принял); твёрдые пачки
  — те же осколки, только `SHARD_BURST_N`=15.
  ⚠️ Отскок искр АНАЛИТИЧЕСКИЙ, по `radiusAt(y)` — без коллайдеров и тел.
- **ПОМОЛ — РАСПИЛ** (`sawFX`): предмет разваливается НА ДВЕ ПОЛОВИНЫ по
  плоскости реза; режем ПЛОСКОСТЬЮ ОТСЕЧЕНИЯ по той же модели, поэтому срез
  честный и приём работает на любом предмете пула без препроцессинга. Фаза
  захвата (сплющивание) осталась как была. Требует `renderer.localClippingEnabled`
  (ставится в 10-stage). ⚠️ Помол идёт У ДНА, под кучей — распил виден в полную
  силу в финале и на тонкой куче, а в начале уровня чаще скрыт соседями.
- **ОГОНЬ — ЯЗЫКИ ПО СИЛУЭТУ** (`fireSilhouetteFX`): раздутая копия меша с
  френель-шейдером, пламя облизывает форму предмета. Живёт неопределённо долго
  (свой список `fires` и тик из 99-main, не через addFX), тушится `extinguishAll`.
  ✅ **ТРИГГЕР ЕСТЬ (спека владельца 2026-08-01, дословно): «ДЕЛАЙ, только
  1 предмет за 30 секунд может загореться».** Планировщик `tickFireSpawn`
  (99-main): раз в `FIRE_EVERY_MS`=30000 вспыхивает ОДИН предмет, горит
  `FIRE_BURN_MS`=6000 и гаснет сам; следующая вспышка назначается от момента
  ЭТОЙ, а не от догорания. Часы идут ТОЛЬКО в живой партии (не в интро, не на
  паузе, не после конца) — иначе за рекламу накопился бы «долг» из вспышек.
  ⚠️ **ЖЕРТВА — ДОСТУПНАЯ И ИЗ ВЕРХНИХ, И НУЖНЫ ОБА УСЛОВИЯ.** `isAccessible`
  даёт справедливость (награда, до которой не дотянуться, — издевательство), но
  на лёгкой сложности он пропускает ВСЁ: закопанный предмет там тапается, а
  пламени на нём не видно, и про бонус игрок не узнает. Поэтому ещё и верхний
  срез `FIRE_TOP_N`=12. Спецпредметы (сюрприз/бомба/камень) не горят.
  ⚠️ **`extinguishAll` ОБЯЗАН ЧИСТИТЬ И СОСТОЯНИЕ `burningItem`, а не только
  пламя.** Первая версия гасила только `fires` — и планировщик, который
  проверяет «уже горит?», больше НИКОГДА не поджигал нового. Снаружи выглядело
  как рабочий огонь (первая вспышка была), и страж «спецпредметы не горят»
  честно печатал пять срабатываний, читая ОДНО И ТО ЖЕ имя пять раз. Поймано
  замером РАЗНООБРАЗИЯ: 14 вспышек — 1 тип при 129 доступных; после фикса
  6 типов из 14. В сьюте на это стоит отдельный страж.
  ⚠️ Стык для бонуса за сбор группы горящего — `burningName()` (70-fx),
  начисление за ДИСПЕТЧЕРОМ.
  ⚠️ **МАТЕРИАЛ ПРЕДМЕТА НЕ ТРОГАЕТСЯ НИ НА КАДР** — огонь только НАКЛАДКОЙ-
  ребёнком поверх меша: портреты коллекции рендерятся тем же классом материала,
  и «горячее» просочилось бы в музей (тот же класс граблей, что у двух
  потребителей uVeil). Страж в сьюте считает детей предмета.
- ⚠️⚠️ **ПРАВИЛО ПАКЕТА: КУСКИ ЭФФЕКТОВ — АНИМАЦИЯ, ТЕЛ RAPIER ИМ НЕ ВЫДАВАТЬ.**
  Формулировка «у кусков физика» читается как приглашение сделать их телами —
  но по замеру ФИЗИКИ (v1-test-219) цена кадра живёт именно в ДИНАМИЧЕСКОЙ
  КУЧЕ, а не в частицах. Телесность ИМИТИРУЕТСЯ, потому что она нужна глазу,
  а не солверу.
- ⚠️ **«МЕНЬШЕ ЧАСТИЦ, КРУПНЕЕ КУСКИ» — ПРИНЦИП ВКУСА, А НЕ БЮДЖЕТА.** Прежнее
  «частицы — дорогие кадры» СНЯТО: то число снималось в headless на SwiftShader
  (растеризация на CPU). На GPU частицы 0.7-1.5 мс, солвер 87-98% кадра.
  Экономим на телах, не на пыли. Сила ×1.7 вшита в константы 00-config.
- ⚠️ `keepGeo` в stepFX: у половин распила и у накладки огня геометрия ОБЩАЯ с
  предметом (кэш типа), диспозить чужое нельзя. ⛔ НО цена ошибки — ЛИШНЯЯ
  ПЕРЕЗАЛИВКА БУФЕРА В GPU, а НЕ исчезновение предметов: замерено диверсией,
  кадр со снятым флагом отличается от исправного на те же 6.2% (three не стирает
  атрибуты при dispose). Прежняя формулировка «погаснут все предметы типа»
  была неверна — не пугать ею.
  ⚠️ Клон геометрии половинам НЕ НУЖЕН (на стенде он был и стоил 3.20 мс под
  CPU ×4): плоскость реза живёт в МАТЕРИАЛЕ, он у каждой половины свой.

## v2 2026-08-06: УДАР ПРИ СОЕДИНЕНИИ (задача тестеров через владельца)

Дословно: «больше драйва при соединении объектов, больше эффектов, крупнее и
гуще частицы».

- **УДАР `impactFX` (70-fx) — НОВЫЙ ОБЩИЙ СЛОЙ в точке схлопывания**: кольцо
  ударной волны (билборд к камере), вспышка-ядро, крупные искры-стрелы
  (`IMPACT_*` в 00-config). Зовётся из хвоста `doMatch` НА ТЕХ ЖЕ РЕАЛЬНЫХ
  ЧАСАХ, что хлопок и `removeItem`.
  ⚠️ **ПОЧЕМУ ОБЩИЙ СЛОЙ, А НЕ УСИЛЕНИЕ ПАЧЕК:** пачечные эффекты достаются
  только группам >= `BURST_MIN_N`, а вялость тестеры видят и на ПАРАХ — там
  была одна труха. Удар даётся КАЖДОМУ соединению, а размер растёт с группой
  (0 у пары -> 1 у группы в кап), вместе с киком камеры.
- ⚠️⚠️ **УДАР ОБЯЗАН ПРОБИВАТЬ ГЛУБИНУ** (`depthTest:false` + `renderOrder`).
  Хлопок происходит в точке ТАПА, то есть ВНУТРИ плотной кучи: с тестом
  глубины кольцо радиусом до 2.5 закрыто предметами ЦЕЛИКОМ, эффект строится,
  тратит время и НЕ ВИДЕН ВОВСЕ. Пачечные этим не болеют — их частицы
  вылетают из кучи за десятки миллисекунд. Поймано съёмкой с замедлением ×10:
  хук `lastImpact()` честно показывал построенное кольцо, а в кадре пусто;
  чтением кода не заметно. Страж на это отдельный.
- ⚠️ **ЦВЕТ УДАРА — НАСЫЩЕННЫЙ, НЕ БЕЛЁСЫЙ, И КОЛЬЦО ТОНКОЕ.** Первая версия
  уводила тон к белому на 55% при профиле 0.66..1.0 — на светлой куче под
  светлым небом это муть, неотличимая от фона (тот же закон, что у молний:
  на светлом читается насыщенное, а не яркое). Сейчас `offsetHSL(0,+0.35)`
  и профиль 0.86..1.0.
- **ТРУХА КРУПНЕЕ И ГУЩЕ:** четвёртая фракция `{n:90, size:0.115}` — КУСКИ,
  против прежнего потолка 0.05; три старых подняты (1280 -> 1570 частиц).
  ⚠️ Это НЕ противоречит спеке владельца 2026-07-22 «вдвое мельче, вдвое
  больше»: тогда труха была из 70 КОМЬЕВ и читалась обломками мебели, сейчас
  из пыли — и пыль читается дымом. Мельчить дальше некуда.
- ⛔ **РАЗМЕР БРЫЗГ СОКА НЕ ТРОГАТЬ** (`JUICE_SIZE` 0.075): «крупнее» тестеров
  относится к залпу в целом, а про сок владелец 2026-08-02 сказал прямо —
  мелкие. Густоту растим ЧИСЛОМ: `JUICE_N` 54->78, `SPARK_N` 37->52,
  `SHARD_BURST_N` 15->21.
- **ПЕРФ (обязательная часть сдачи), троттлинг ×4, пар-путь:** постройка
  1.41 -> 1.75 мс/матч, из них удар 0.24; кадр p95 26.8 -> 26.4 (в шуме).
  ⚠️ Труха стала на 290 частиц гуще ПРАКТИЧЕСКИ БЕСПЛАТНО (1.02 -> 1.09):
  фракция = ОДИН Points, цена живёт в объекте, а не в частице — поэтому
  «гуще» дёшево, а «ещё один носитель» стоит. Осколки наоборот, они меши:
  0.021 мс на скол, 15->21 = +0.13 мс на залп.
- ⚠️ **СЕКЦИЯ СЬЮТА СТОИТ В КОНЦЕ И НА СВОЕЙ СТРАНИЦЕ.** В середине она
  обокрала соседа по кадрам: «🔥 БОНУС за горящий тип» с фиксированными
  паузами упал (d1=0) на ИСПРАВНОЙ сборке, а тот же сценарий в изоляции дал
  6/6 на ОБЕИХ сборках. Тот же приём, что у камней и меню.
- 🔵 **КОЛЬЦО ПЕРЕДЕЛАНО В ТОТ ЖЕ ДЕНЬ ПО СЛОВУ ВЛАДЕЛЬЦА** (посмотрел живьём):
  «нравится кольцо, но сделай его шире и увеличь прозрачность; попробуй для
  разных объектов разную ширину и форму». Обод 0.14 -> 0.32-0.52 доли радиуса,
  плотность 1.0 -> `IMPACT_ALPHA` 0.55.
  ⚠️ **ЭТИМ ОТМЕНЕНО МОЁ ЖЕ «кольцо обязано быть тонким»** (я сузила его часом
  раньше ради читаемости на светлом). Условие живо в другой форме: массу
  добираем ШИРИНОЙ при СНИЖЕННОЙ плотности, а не белым — насыщенность тона
  остаётся обязательной, иначе широкое бледное кольцо тонет.
  ⚠️ Владельцу сказано честно: на приглушённых цветах (кирпичи) кольцо стало
  деликатным. Если решит, что перестарались, — одно число `IMPACT_ALPHA`.
- 🔵 **ТРИ СЕМЕЙСТВА ФОРМЫ, И ОНИ БЕРУТСЯ ИЗ САМОГО ПРЕДМЕТА** (`ringFamFor`,
  70-fx; кэш по имени типа): вытянутые (elong >= `IMPACT_ELONG_AT` 1.45) —
  ОВАЛ, растянутый по длинной оси; твёрдые пачки brick/pirate/rock — 
  МНОГОУГОЛЬНЫЙ обод на 7 граней (те же пачки, что колются осколками);
  прочие — РОВНОЕ кольцо. Раскладка по пулу: 56 / 10 / 54 из 120.
  ⚠️⚠️ **ДЕТЕРМИНИРОВАННО ОТ ТИПА, А НЕ СЛУЧАЙНО**: один предмет обязан давать
  ОДНО кольцо — тогда это читается как его СВОЙСТВО, а случайная форма читается
  как глитч (тот же принцип, что у fxColor).
  ⚠️ И **НЕ ОТ ХЕША ИМЕНИ**: форма выведена из ГАБАРИТОВ геометрии и пачки.
  Хеш дал бы разнообразие без смысла — у банана (elong 3.78) вышло бы круглое
  кольцо, а у кирпича овал.
  ⚠️ Хук `__game.ringFams()` НЕСУЩИЙ: на нём стражи «три семейства живы» и
  «формы различаются»; считает по ВСЕМУ пулу, а не по девятке уровня.
- ⚠️ **ГРАБЛЯ ДИВЕРСИИ, СТОИВШАЯ ЛОЖНОГО ВЫВОДА** (записываю, потому что вывод
  был бы «страж слепой»): проверяя детерминизм формы, я сломала КЭШ
  (`RING_FAM`), а не детерминизм — функция осталась чистой, результат тот же,
  страж честно остался зелёным. Детерминизм ломается только внесением
  случайности В РЕЗУЛЬТАТ (`w` с `Math.random()`), и тогда страж краснеет.
  **Диверсия обязана бить в ТО САМОЕ свойство, а не в его соседа.**
- ⚠️ ПЕРФ ПОСЛЕ ПЕРЕДЕЛКИ: постройка 1.74 мс/матч против 1.75 до неё (удар
  0.32), кадр p95 26.2 — ширина и форма ничего не стоят: у многоугольника
  граней ВСЕГО 7 против 48, он даже дешевле.
- 🔥 **ВСПЛЕСК ОГНЯ ПРИ СОВМЕЩЕНИИ ГОРЯЩЕГО** (слово владельца 2026-08-06:
  «когда огненный объект совмещается, при совмещении сделай визуальный всплеск
  огня»). `fireBurstFX` (70-fx) — ДОПОЛНИТЕЛЬНЫЙ слой поверх удара и трухи,
  зовётся из того же хвоста `doMatch` при `fireHot`. Два носителя: ПЛЮМ (языки,
  всплывают с ускорением и остывают из белого в глубокий оранжевый) и УГЛИ
  (редкие искры баллистикой). Плюс кольцо удара у горящего матча — ОГНЕННОЕ и
  РВАНОЕ (`makeTornRingGeo`, профиль языками).
  ⚠️ **ПАЛИТРА ОБЩАЯ С ПЛАМЕНЕМ НА ПРЕДМЕТЕ** (`FIRE_HOT/DEEP/CORE` в 70-fx —
  те же числа, что в шейдере `fireSilhouetteFX`): всплеск обязан читаться как
  «этот же огонь рванул», а не как новый эффект. Держать вместе, как SHARD_LIGHT.
  ⚠️ **ЧЕТВЁРТОЕ СЕМЕЙСТВО КОЛЬЦА — ПО СОСТОЯНИЮ, А НЕ ПО ТИПУ**: горит сегодня
  один предмет, завтра другой, и рваное кольцо принадлежит СОБЫТИЮ. Детерминизм
  цел — профиль считается синусами от индекса, без `Math.random`.
- ⚠️⚠️ **ВСПЛЕСК РАСКРЫВАЕТСЯ ВШИРЬ, А НЕ СТОЛБОМ — ЭТО ТРЕБОВАНИЕ РАКУРСА.**
  Первая версия поднималась узким плюмом и на кадре читалась тугим комком у
  точки тапа. Механику проверила диагностическим красным (эффект рендерился) —
  невидим был именно ПОДЪЁМ: камера смотрит в чашу СВЕРХУ, и вертикальное
  движение сжато перспективой. `FIREB_PLUME_SPREAD` 1.5 -> 3.4. Тот же закон,
  из-за которого кольцо удара — билборд, а не лежит в плоскости чаши.
- ⚠️ **`CFG.fxSlow` — ДЕЛИТЕЛЬ ЧАСОВ ЭФФЕКТОВ (боевое 1), несущая ручка показа.**
  Боевой эффект живёт 150-600 мс, headless-скриншот стоит около секунды — на
  кадре его нет вовсе. ⛔ Растягивать ЖИЗНЬ константой нельзя: движение
  параметрическое от `t=k·life`, при большей жизни куски улетают в другую точку
  (искажается ровно то, что показываешь). Деление ЧАСОВ сохраняет траектории
  бит-в-бит. Понадобилось четвёртый раз подряд — потому и вшито.
- ⚠️ ПЕРФ ВСПЛЕСКА (троттлинг ×4): `fireBurst` **0.23 мс/матч** — тот же порядок,
  что удар (0.24-0.34); полная постройка горящего матча 1.87 против 1.90 у
  обычного (в шуме), кадр p95 26.3.
- ⚠️⚠️ **ГРАБЛЯ СТРАЖА, ПОЙМАННАЯ НА СЕБЕ: ПОРЯДОК ПРОВЕРКИ РЕШАЕТ.** Первая
  версия шла «сначала обычный матч, потом горящий» и ассертила «после обычного
  всплеска нет» — сравнивая `null` с `null`. Это ИСТИННО и на сборке, где
  всплеска нет вовсе. Верно — СНАЧАЛА горящий (снимок появился), ПОТОМ обычный
  (снимок НЕ обновился): проверяется ПЕРЕХОД. Диверсия «всплеск на каждом матче»
  ловится только переписанной версией.
- Хук `__game.lastImpact()` НЕСУЩИЙ: на нём стражи «растёт с группой» и
  «пробивает глубину». Диверсии: снятый вызов роняет три ассерта из четырёх,
  `depthTest:true` — ровно один, убранная фракция — ровно один, `k=0` —
  ровно один.

## v2 2026-08-06: НОЧНЫЕ ЗВЁЗДЫ — БОЛЬШЕ МЕЛКИХ + ПУЛЬС КАЖДОЙ ДЕСЯТОЙ

Слово владельца: «больше мелких звёзд НЕ МЕНЯЯ их текущее количество — это
добавит разнообразие» и «некоторые, например 1 из 10, совсем аккуратно
пульсируют, плавно меняя прозрачность».

- **РАЗМЕР — ТОЛЬКО ФОРМА РАСПРЕДЕЛЕНИЯ**: `sz = mix(STAR_SIZE_MIN, 1.0,
  pow(hash, STAR_SIZE_BIAS))`, BIAS=2.1 сдвигает выборку к нижней границе,
  MIN 0.55 → 0.34. ⚠️ `STAR_DENS` НЕ ТРОГАТЬ — от него зависит ЧИСЛО звёзд,
  а его просили сохранить. Замер кадра: медиана площади пятна 29 → 10 px,
  доля мелких (≤3 px) 14% → 30%, число пятен то же.
  ⚠️ Перебор `tools/star-cells-check.js` обязателен и чист (ядра целые 100%,
  срезов 0%, звёзд 593): размеры только УМЕНЬШАЮТСЯ, худший случай бюджета
  ячейки не меняется — но проверять всё равно перебором, не формулой.
- **ПУЛЬС** — отдельная волна поверх моргания: своя фаза от хеша ячейки, втрое
  медленнее (`STAR_PULSE_SPD` 0.42, период ~15 с), глубина `STAR_PULSE_AMP`
  0.55 (яркость 1.0 → 0.45, звезда не гаснет), доля `STAR_PULSE_FRAC` 0.10.
  Отбор — ОТДЕЛЬНЫЙ хеш ячейки, поэтому пульсирует всегда одна и та же звезда.
  ⚠️ Корреляции с отбором самих звёзд НЕТ — проверено перебором: из 593 звёзд
  пульсируют 59 (9.9%), распределение по децилям ровное.
- ⚠️⚠️ **ПАРАМЕТРЫ ПУЛЬСА — ЮНИФОРМЫ (`__game.starPulse(frac, amp)`), И ЭТО
  НЕСУЩЕЕ.** Пиксельный замер НЕ РАЗЛИЧАЕТ боевые 10% пульсирующих на фоне
  моргания ВСЕХ: размах 0.41 против 0.41 у базы. Единственный честный способ
  проверить тракт «доля → пиксели» — прогнать долю ручкой; при 1.0 отличие
  видно сразу. Страж гоняет долю, а саму боевую 0.10 ассертит отдельно.
- ⚠️⚠️ **ДВЕ ЛОВУШКИ ЗАМЕРА, ОБЕ ДАЛИ ЛОЖНЫЙ ОТВЕТ ДО ИСПРАВЛЕНИЯ:**
  (1) первый разбор ловил не звёзды, а ГРАДИЕНТ НЕБА — «звезда» площадью
  5057 px. Фон вычитается ПОСТРОЧНО (небо светлеет книзу), пятна крупнее
  200 px отбрасываются;
  (2) размах яркости мерился БЕЗ НОРМИРОВКИ КАДРА, и его определяла КРАСНАЯ
  УГРОЗА ПОМОЛА, набегающая за секунды простоя: все пятна ходили хором, и
  результат вышел ПЕРЕВЁРНУТЫМ («без пульса» размах больше, чем «пульс у
  всех»). Лечение: держать миксер спокойным (`stats().lastAction`) и делить
  яркость пятна на медиану пятен ЭТОГО кадра — общий множитель уходит.
- ⚠️⚠️ **ПОРОГ СТРАЖА ПУЛЬСА — ПО РАСПРЕДЕЛЕНИЮ, И У НЕГО ЕСТЬ ДЕФЕКТНАЯ ОПОРА.**
  Первые 40 стояли впритык к здоровому разбросу и ОДИН РАЗ ПОКРАСНЕЛИ НА
  ИСПРАВНОЙ СБОРКЕ (37.7% в чужом прогоне; правка Интерфейса физически не могла
  коснуться — звёзды живут на своей странице сьюта).
  ЛИНЕЙКА: headless на GPU (`--use-angle=metal`), 5 кадров на руку через 1.8 с,
  верхние 45% кадра, пятно от 12 над фоном СТРОКИ, «глубокий» — размах > 0.45.
  ЗАМЕР: 6 проб (3 в покое, 3 под идущим полным сьютом) + 3 прогона сьюта.
  Здоровые при доле 1.0: 37.7 / 55.8 / 57.1 / 57.5 / 61.5 / 61.7 / 64.1 / 64.1 /
  71.8 / 78.3 — минимум **37.7**. При доле 0 — **РОВНО 0 во всех**. Коридор пуст,
  порог **20** стоит в его середине.
  ⚠️ Условие сравнивает РАЗНОСТЬ РУК, а не абсолют: обе руки меряются в одном
  прогоне под одной нагрузкой, поэтому общий множитель уходит — тот же приём,
  что нормировка кадра по медиане пятен.
  ⚠️ И честно: 37.7% САМА НЕ ВОСПРОИЗВЕЛА (под полным сьютом получала не ниже
  55.8) — значит моя нагрузка слабее реальной, и порог ставился по НАБЛЮДЁННОМУ
  минимуму, а не по своей выборке. Увидите ниже 37.7 — опускать порог.
- ⚠️ **СТАТИСТИКУ ВЫБИРАТЬ ЛЕСЕНКОЙ, А НЕ УГАДЫВАТЬ.** Медиана размаха к доле
  пульса почти глуха (0.306 при доле 0 против 0.569 при 1.0 — и 0.273 при
  боевой 0.10, то есть ниже нуля-варианта). Разделяет НАЧИСТО доля «глубоко
  гуляющих» (размах > 0.45): 0% → 2.5% → 22.5% → 55% → 77.5% при долях
  0 / 0.1 / 0.3 / 0.6 / 1.0. Признак выбран по этой лесенке.

## v2 2026-08-06: РАЗЛЁТ ЧАШИ — ОДИН МЕШ, СКОЛ ВОРОНОГО (примеры владельца)

Владелец прислал два репозитория со взрывами и сказал «делай». Техники в них
РАЗНЫЕ, и выбор между ними — суть правки:
- **bobbyroe/explode-transition** = то, что у нас БЫЛО: расколотая модель, каждый
  кусок отдельный меш, движение считает JS каждый кадр. Каждый лишний кусок
  стоит и draw call, и работы процессора.
- **akella/ExplodingObjects** = взятое: куски слиты в ОДНУ геометрию, а
  принадлежность к куску записана В ВЕРШИНЫ (центр/ось/скорость/кувырок),
  разлёт гонит вершинный шейдер по одной юниформе. Один объект, одна цифра
  в кадр, ноль работы процессора на кусок.

| | было | стало |
|---|---|---|
| кусков | 30 | **160-165** |
| кадр взрыва | 5.0 мс | **2.8-3.1 мс** |
| объектов сцены | +30 | **+1** |
| работа в полёте | 30 тиков/кадр | одна юниформа |

- ⚠️ **ГЕОМЕТРИЯ ПЕЧЁТСЯ ОДИН РАЗ, НА СТАРТЕ УРОВНЯ** (`restoreBowlVis`): чаша
  неизменна. Выпечка 15 мс, старт уровня 900-950 мс при разбросе ±50 — она
  внутри шума. ⚠️ Сравнивать надо ИЗОЛИРОВАННУЮ цену выпечки со стартом, а не
  средние «первого» и «последующих» стартов: геометрия печётся уже при загрузке
  страницы, и «первый regen» её не застаёт.
- ⚠️⚠️ **КУСКИ — ЯЧЕЙКИ ВОРОНОГО, А НЕ РЕШЁТКА.** Первая версия резала оболочку
  правильной сеткой ряды×секторы; вердикт владельца: «сетка слишком стерильная
  и равномерная, в жизни стекло так не трескается». Считаем в развёртке
  (u = длина дуги, v = высота): ячейка = домен, обрезанный биссектрисами до всех
  прочих центров. ⚠️ Шов по кругу закрыт копиями центров на ±2πR — иначе у стыка
  ячейки выходят прямоугольными и шов видно.
- **ДВА КЛАССА КУСКОВ** (слово владельца «сделай крупные пластины и облако
  мелочи вокруг»): `BOWL_PLATE_N`=13 пластин с минимальным зазором между
  центрами (крупный кусок = там, где рядом никого) + `BOWL_FINE_N`=150 крошек
  вокруг `BOWL_IMPACTS`=2 точек удара, плотность падает от центра. Так и бьётся
  стекло: у удара пыль, дальше пластины.
- ⚠️ **ИМЯ АТРИБУТА `aCen`, А НЕ `centroid`**: `centroid` — ЗАРЕЗЕРВИРОВАННОЕ
  СЛОВО GLSL (квалификатор интерполяции). Шейдер не собирается вовсе, а three
  печатает ошибку в чужой строке своего префикса — искать долго.
- ⚠️ **ФЛАГ `bowlBroken` НУЖЕН ПОТОМУ, ЧТО ВИДИМОСТЬЮ СТЕКЛА ВЛАДЕЕТ ЦИКЛ.**
  В `loop` каждый кадр стоит `bowlMesh.visible = k > 0.02` (растворение при
  приближении камеры), и наше `visible=false` в разлёте жило ОДИН кадр — силуэт
  возвращался (жалоба владельца «после взрыва не должно быть силуэта, только
  осколки»). Чужое состояние не снимают руками, его ГЕЙТЯТ у владельца.
- **СБОР ПОСЛЕ ВЗРЫВА — ТЕМ ЖЕ ЯЗЫКОМ, ЧТО ОБЫЧНОЕ СОВМЕЩЕНИЕ** (слово
  владельца): куча слетается в центр и лопается одним хлопком (`collapseFX` +
  `impactFX` + труха), а не тает на месте.
  ⚠️⚠️ **СБОР ИДЁТ ПО РЕАЛЬНЫМ ЧАСАМ** (4-й аргумент `collapseFX`): тик addFX
  живёт по ИГРОВОМУ времени, а удаление предметов — по `setTimeout`. На 150 мс
  матча разница незаметна, на 620 мс сбора часы расходятся: ПОД НАГРУЗКОЙ куча
  исчезала, не долетев до центра. В изоляции не воспроизводилось — поймал страж
  в полном сьюте (радиус встал на 1.73 вместо 0, пять сэмплов вместо
  восемнадцати). Тот же закон, по которому хлопок матча висит на часах
  `removeItem`.
- ⚠️ **ДВЕ СОБСТВЕННЫЕ ОШИБКИ В СТРАЖЕ СЛЁТА, обе найдены диверсиями:**
  (1) в условии стояла лазейка `|| живых === 0`, а к моменту замера предметы уже
  удалены — ассерт был истинным ПРИ ЛЮБОМ поведении, диверсия «не слетаются»
  прошла зелёной; (2) починив это, мерила радиус по `itemsGeo`, а он отдаёт
  ФИЗИЧЕСКИЕ координаты — слёт двигает МЕШИ, тела к тому моменту уничтожены,
  и метрика показывала разлёт вместо схождения (красным стало всё, включая
  исправную сборку). Верно — опрос по мешам, пока предметы живы, и минимум за
  окно.
- ⚠️ Хуки `__game.bowlShardsInfo()` (разбита/кусков/испечена/geoId/размеры/
  стеклоВидно/радиусМешей) — НЕСУЩИЙ: на нём все шесть стражей. Диверсии:
  «цикл возвращает стекло» роняет ровно силуэт, «центры по решётке» — ровно
  разброс площадей (×1 против ×100-1350), «не печём заранее» — выпечку и кэш,
  «кэш мимо» — только кэш, «не слетаются» — только слёт.
- 🔵 **КЛАД ЛЕТИТ В ЦЕНТР ВМЕСТЕ СО ВСЕМИ + ПОП В ХЛОПКЕ** (ловля диспетчера
  по букве обещания «сливаются как при обычном совмещении»): раньше сюрприз
  вынимался из выборки и лопался НА МЕСТЕ, пока прочие ещё летели, — обещание
  ломалось на самом заметном предмете. Теперь он в общем слёте, а начисление и
  его эффекты идут в момент хлопка, в точке сбора. Плюс `popFX`, которого у
  сбора не было, а у матча есть.
  ⛔ `burstFX` В СБОРЕ НЕ ЗОВЁМ И НЕ НАДО: он выбирает пак-эффект ПО ТЕКСТУРЕ
  предмета, а в сборе смешаны все пачки разом — любой выбор был бы произволом.
  Язык хлопка — «пара/тройка»: труха + удар + поп.
  ⚠️⚠️ **ЯКОРЬ ЭФФЕКТОВ (`p`) ДВИГАЕМ, МЕШ — НЕТ.** Первая версия копировала и
  `mesh.position`, то есть ТЕЛЕПОРТИРОВАЛА клад в центр, — и страж «клад летит
  со всеми» проходил зелёным даже когда клад вынут из слёта: он мерил телепорт,
  а не полёт. Третий подряд случай, когда диверсия ловит не механику, а способ
  измерения; хук `кладДоЦентра` считает по МЕШУ.
- ⚠️⚠️ **ПЕРФ-ЧИСЛО БЕЗ ЛИНЕЙКИ — НЕ ЧИСЛО** (расхождение с независимой
  перепроверкой диспетчера, 2026-08-06). Я сдала «кадр взрыва 5.0 → 2.8 мс», он
  не воспроизвёл (получил 1.2 → 0.6). Обе цифры верны: мои сняты при **CPU ×4**,
  его — без троттлинга, отношение ×4 сходится один в один (перемер: база 5.0
  против 1.3, новое 2.9 против 0.7). ⚠️ И формулировка была неточна: это
  **синхронная постройка внутри вызова**, а НЕ «кадр, где осколки рисуются
  впервые» — там выигрыша нет (headless даже хуже), он живёт на всём 1.4-сек
  полёте: минус 60 draw calls. Рядом с перф-числом обязаны стоять троттлинг,
  headless/GPU и ЧТО именно обёрнуто замером.
- ⚠️ Флаг `sharedFx` в stepFX — НЕ `keepGeo`: по `keepGeo` страж считает
  ПОЛОВИНЫ РАСПИЛА поимённо, и повесить её на кэш разлёта значило бы тихо
  испортить чужой счётчик.

## v1-тюнинг 2026-07-21-е: ЭФФЕКТЫ СОВМЕЩЕНИЯ «ПУНКТ 5» (спека владельца)

- Спека: «разнообразие правилом» — эффект по ПАЧКЕ и по РАЗМЕРУ ГРУППЫ.
  Пара/тройка — труха (dissolveFX, как раньше); группа >= BURST_MIN_N (4,
  00-config) ЛОПАЕТСЯ: короткое надувание меша ×1.22 -> хлопок + эффект
  пачки: food — СОК (крупные капли fxColor баллистикой), car — ИСКРЫ +
  3 кубика-детальки кувырком, animal — ЗВЁЗДОЧКИ веером (starGeo); без
  пачки (стейк) — труха. Комбо/цепь поверх не менялись (молнии как были).
  ⚠️ Brick/Pirate/Rock — ОСКОЛКИ (спека владельца 2026-07-23 «сделай
  осколками», ОТМЕНЯЕТ прежнее «оставляем в труху» 2026-07-22 — было
  «ждём нового слова», слово дано): твёрдые пачки при бурсте КОЛЮТСЯ на
  угловатые куски (shardFX в 70-fx, см. раздел 2026-07-23), а не в
  труху. Rock попадает в burstFX только как жертва бомбы.
- ФИЗВОЛНА blastWave (50-physics): при бурсте соседи вздрагивают радиально
  от тапнутого; пик BURST_WAVE_V=0.9, затухание квадратичное к BURST_WAVE_R=
  2.4, умножается на shakeK пачки. КОСМЕТИКА, не рыхление (встряска даёт
  ~9); бот матчит только пары — экономика встрясок не затронута по
  построению. Правило выбора — burstFX в 80-gameplay.
- Реализация эффектов — СТАРТОВАЯ, в 80-gameplay через публичный addFX
  (70-fx не тронут; материалы/геометрии персональные — stepFX диспозит;
  баллистика ПАРАМЕТРИЧЕСКАЯ от t=k·life — FPS-независимо; у искр normal
  blending — additive на светлой панораме невидим). Полировка визуала —
  запрос ГРАФИКЕ в WORKSTREAMS. Скрин-пробы: __game.findByTex(tex) +
  реальный mouse.click; 3 пачки по 14 шт — эффекты в кадре, волна в psLog
  ('burst'), fx/geoms дренаж в базу, SUITE PASS.

## v1-тюнинг 2026-07-21-д: ВЕС ПРИ ВСТРЯСКЕ — вариант 1 (спека владельца)

- Спека: «делай вес при встряске, вариант 1» (дискретные множители отклика).
  Материалы хром/пластик умерли вместе с примитивами (все модели — плотность
  пластика), поэтому «по материалу» стало «ПО ПАЧКЕ» (tex в TYPES):
  SHAKE_RESP = { car: 0.75, animal: 1.0, food: 1.15, rock: 0.7, brick: 0.72,
  pirate: 0.85 } (00-config; стейк/сюрприз/бомба без tex = 1.0). Металл-машины
  лениво шевелятся, фрукты подпрыгивают. Пачки brick/pirate добавлены
  2026-07-23 (выбор ФИЗИКИ по делегированию владельца, «выбирай сам»): brick
  0.72 — кладочные блоки почти как камень; pirate 0.85 — тяжёлое дерево/
  металл/камень, лёгкая пальма тянет вверх. Шкала по плотности материала:
  rock .70 < brick .72 < car .75 < pirate .85 < animal 1.0 < food 1.15.
- Применение: item.shakeK ставится в createItemBody (50-physics); множитель
  ТОЛЬКО на случайное рыхление/подброс/вращение performShake и вибрацию
  миксера (99-main). ПРИТЯЖЕНИЕ пар (pullK) И ИНТРО-ОСАДКА НЕ трогаются —
  функциональные (сходимость последних пар, экономика встрясок).
- Замер (__game.velByTex, сразу после shake на полной куче): car/animal
  0.72–0.79, food/animal 1.12–1.13 — цели 0.75/1.15. A/B терпеливым ботом
  (полный ур.5, сиды 7/8/9, Easy+Hard): база без веса {5,6,6}/{5,4,4} vs
  с весом {5,4,5}/{4,3,5} — вес экономику НЕ ухудшил (в общий журнал обе
  фичи ветки вошли как v1-test-56). ⚠️ Замечание вне правки: базлайн Easy
  на полном уровне БЕЗ веса сам вылезает за бюджет 5 (после нерфа
  комбо-потолка до 1.1 и перехода на модели) — сигнал владельцу.

## Витрина уровня: портреты предметов (2026-07-22)

Панель витрины (десктоп ≥1160px) и музей/всплывашка показывают ПОРТРЕТЫ
предметов — офскрин-рендер живого меша, общий кэш `thumbCache` по типу.

- КАДР ПО СИЛУЭТУ, аналитически: ортокамера + bbox ПРОЕКЦИЙ ВЕРШИН
  геометрии (проекция выпуклой оболочки = оболочка проекций), одна
  полурамка на обе оси — пропорции целы, вытянутое не растягивается.
  Заполнение 0.91–0.92 бокса при MARGIN 4%. Прежняя нормировка по
  ОПИСАННОЙ СФЕРЕ вокруг повёрнутого AABB давала двойную переоценку —
  силуэт занимал ~55% кадра (жалоба владельца «объекты слишком мелкие»).
  Чтение пикселей (readPixels/getImageData) НЕ нужно и вдвое дороже.
- Буфер 256×256 (качество, спека владельца 2026-07-28 «низкое качество
  коллекции»; было 132 → апскейл на ~150px карточку мылил) и СТРОГО
  КВАДРАТНЫЙ: у потребителей `img` 100%/100% без object-fit — неквадрат
  сплющит. THUMB_PX (статика) и SPIN_PX (спин) оба 256 — размеры обязаны
  совпадать (см. шринк-фикс ниже).
- ⚠️ ДВА БАГА, из-за которых портрет молча портился и оседал в кэше
  НАВСЕГДА: (1) matcap-патч гасит диффуз по МИРОВОЙ высоте (vWorldY
  против uPileTop) — портрет на y=0 всегда выходил самым тёмным тоном
  кучи; лечение: портретный меш и камера на y=100; (2) вуаль
  недоступности лерпает material.color к серому (tickVeil) — снимок в
  этот момент давал СЕРЫЙ портрет; лечение: на время рендера
  возвращаем item.baseColor.
- ⚠️ Кэш ОДИН на трёх потребителей (витрина 44, музей 44, всплывашка 56)
  и ключом служит item.key — разные размеры для разных мест невозможны
  без ключа key+'@'+size (удвоит рендеры и память). Один размер на всех.
- ⚠️ Портреты строить ТОЛЬКО после интро: атласы моделей декодируются
  асинхронно, ранний снимок выходит чёрным и кэшируется навсегда.
  ⚠️⚠️ ДОПОЛНЕНО 2026-07-30 (ИНТЕРФЕЙС, жалоба владельца «где превью у всех
  новых объектов?»): «после интро» НЕ ДОСТАТОЧНО — правило защищало от РАННЕГО
  вызова, а холодной может быть ПАЧКА. Атлас пачки, которой нет в текущей
  партии, к моменту снимка не декодирован (`modelColormap` отдаёт белую 1×1
  заглушку, `needsUpdate` только в img.onload): портрет выходит ПОЛНОСТЬЮ
  ПРОЗРАЧНЫМ (0 непрозрачных пикселей, два разных типа дают побайтово
  одинаковый PNG 3174 Б) и оседает в thumbCache НАВСЕГДА. На main было 29
  пустых карточек из 122 — все пачки Kenney. Правило теперь двухчастное:
  (1) после интро И (2) `itemThumb` не снимает и не кэширует, пока у материала
  map без декодированного изображения (map.image.width === 1 → ждать);
  buildMainCollection добирает опоздавшие карточки таймером НА МЕСТЕ, без
  пересборки сетки. Страж: «живая картинка у всех карточек» (порог по длине
  data-URL: пустышка ровно 3174 Б). Отвергнуто замером, не изобретать заново:
  альфа-канал атласов (нет tRNS), палитровый PNG (старый food тоже colorType 3
  и работает), «поможет второй рендер подряд» (оба кадра по 3174 Б — ждать
  надо СОБЫТИЯ декода, не лишнего кадра; на медленном устройстве вернулось бы).
- ПОРТРЕТ ПО КЛЮЧУ + ВРАЩЕНИЕ ПРИ HOVER (спека владельца 2026-07-24, объём B;
  ГРАФИКА даёт механизм, ИНТЕРФЕЙС вешает): `thumbItemForKey(type.name)`
  (85-hud) строит портрет-меш для типов ВНЕ текущей партии (материал — общий
  `itemMaterial`, ВЫНЕСЕН из makeItem в 40-items). Живой спин —
  `thumbSpinStart(item,host)/thumbSpinStop()`: один общий офскрин `spinR`,
  rAF только на hover, Y-инвариантная рамка (по охватному цилиндру, силуэт
  под Y-поворотом инвариантен, модель не «дышит»/не клипается).
  Детали и замеры — WORKSTREAMS, блок ГРАФИКА.
- ⚠️ ШРИНК-ФИКС #3 (спека владельца 2026-07-28 «размер при hover не должен
  меняться»): статика (itemThumb) и спин ОБЯЗАНЫ кадрировать ОДНИМ
  `frameCylinder(cam, mesh)` (обобщён из `frameSpinCylinder`). Раньше статика
  фреймила по СИЛУЭТУ (тесно), спин — по цилиндру (шире, запас на поворот):
  цилиндр ≥ силуэт → на hover подмена img→канвас УМЕНЬШАЛА объект. Теперь обе
  по цилиндру → thumbW===spinW бит-в-бит (хук-регрессия `thumbFrames(key)`,
  сьют ассертит `.equal`). Развести рамки нельзя — вернётся шринк.
- TAP=HOVER #4 (спека владельца «один компонент, ховер = тап»): на мобиле нет
  mouseleave, поэтому `thumbSpinToggle(item, host)` (85-hud) — тап по неактивной
  заводит спин (общий канвас сам снимает прежнюю), повторный тап по ТОЙ ЖЕ
  снимает. Интерфейс вешает ОДИН тап-обработчик; хук `thumbSpinToggleKey(key,sel)`.
- ⚠️ ПОЗА ПОРТРЕТА — ЕДИНЫЙ ИСТОЧНИК `PORTRAIT_TILT_X/PORTRAIT_YAW0` (85-hud),
  им пользуются И статика (itemThumb) И спин: развести нельзя — иначе скачок
  при подмене img→канвас на hover. Спека владельца 2026-07-24-в «лёгкий подъём
  вправо-вверх»: `-0.15/-0.6` (взгляд чуть СНИЗУ, перёд приподнят; прежний
  top-down `+0.42/+0.65` «уводил в нижний угол» — забракован).
  ⚠️ ЧЕМ СТЕРЕЖЁТСЯ: ассерт «поза статики и спина — ОДИН источник» через
  тест-хук `__game.setPortraitPose` (99-main). Хук НЕСУЩИЙ, не временный —
  на нём стоит единственная защита этого инварианта, и снести его = тихо
  убрать стража. Getter вместо мутации НЕ годится: статика и спин читают одну
  переменную, сверка «getter против getter» пуста и зелена всегда. Проверено
  симуляцией 2026-07-27 (дали спину свою копию yaw → ассерт упал, −0.6 вместо
  0.2). РАМКУ стережёт отдельный `thumbFrames`, ПОЗУ — только этот хук.
- ГХОСТ ЗАКРЫТЫХ ТИПОВ (спека владельца 2026-07-24-в «не открытые модели —
  прозрачные, немного матовые, но бесцветные» + «заполни музей моделями»;
  ОТМЕНЯЕТ прежнее «локнутые серой буквой»): `thumbItemForKey(key, true)` →
  гхост-item (свой кэш-ключ '@g', материал `transparent`), itemThumb при
  `item.ghost` жмёт `uVeil=1` (десат вуалью, переиспользована) + `opacity=
  GHOST_ALPHA` 0.42. ⚠️ `thumbR.compile` ПЕРЕД чтением userData.shader —
  иначе на свежем гхост-материале shader ещё null и uVeil не применится
  (гхост выходил цветным). Интерфейс вешает на ЗАКРЫТЫЕ карточки вместо буквы.

## Витрина: видимость — ПРАВИЛО ШИРИНЫ 2/3 (2026-07-27, ОТМЕНЯЕТ camnear)

Спека владельца 2026-07-27: «на десктопе и планшетах панель НЕ убирать;
убираем только если ширина экрана меньше [порога] — панель 1/3, игре 2/3».
Скрытие по приближению камеры (camnear v1-v3: пороги по чаше 42/49, затем
устойчивый край кучи hullR 130/150) ПОЛНОСТЬЮ ОТМЕНЕНО — при любом зуме
панель стоит. Видимость — ЧИСТЫЙ CSS `@media (min-width:813px)` (порог =
3×271px занятой полосы панели; сменится ширина панели — перемерить) в
shell.html + тот же порог в `vitrineOn()` (85-hud). `pointer:fine` СНЯТ —
планшеты видят панель. JS-машинерии camnear больше нет (tickCamNear,
vitrineGap, camnearThreshold удалены из 99-main). История критерия ниже
оставлена как архив приёмов (устойчивый край, updateMatrixWorld).

⚠️ СЧИТАТЬ ПО ЧАШЕ, А НЕ ПО КУЧЕ ПРЕДМЕТОВ. Прежний критерий брал левый
край кучи — при вращении камеры она меняет экранную ширину, замер драгом
дал разброс зазора **70 px за оборот**, и у порога панель МОРГАЛА
(жалоба владельца). Чаша осесимметрична: её левый край при повороте
вокруг оси не меняется вовсе — дрожания нет ПО ПОСТРОЕНИЮ, а не за счёт
гистерезиса. Левая точка кольца = центр − R·(орт камеры «вправо»), пять
колец по высоте через `radiusAt(y)`.

⚠️ ПЕРЕД РАСЧЁТОМ ОБЯЗАТЕЛЕН `camera.updateMatrixWorld()`: орт берётся из
`matrixWorld`, а `project()` считает по `matrixWorldInverse` — во время
драга они расходятся на кадр, и край чаши дрожал даже на осесимметричной
фигуре (замер: разброс 56 px; после синхронизации — РОВНО 0).

Поведение по ширинам в покое (замер): 1160 → −31 (скрыта), 1280 → 29
(скрыта), 1366 → 72, 1440 → 109, 1600 → 189, 1920 → 349 (видна).

## Разворот витрины и её состояния видимости

У `#vitrine` ЧЕТЫРЕ независимых управляющих сигнала, и они СОЗНАТЕЛЬНО
разведены по разным CSS-свойствам:

| Сигнал | Свойство |
|---|---|
| база / `.vempty` (всё собрано) | `opacity` |
| `@media (min-width:813px)` — правило 2/3 | `display` |
| `html.introdone` (облёт закончился) | `clip-path` + `transform` |

(`html.camnear` ОТМЕНЁН 2026-07-27 — камера панель больше не гасит.)
⚠️ НЕ добавлять лишнее правило на `opacity`: одинаковая специфичность
(0,2,1), спор решался бы порядком в файле — фейд умер бы молча.
⚠️ НЕ разворачивать через `transform:scaleY`/`max-height`:
оба меняют rect панели, а её ЖИВОЙ rect читает якорь всплывашки
(85-hud); `clip-path` не трогает ни layout,
ни rect (замерено бит-в-бит). Все переходы — в ОДНОЙ декларации
`transition`: отдельное правило ниже перезаписало бы шорткат.

## Витрина: тёмная тема, реакция на матч, каскад разворота (2026-07-23)

Три правки по спеке владельца (ветка claude/interface-vitrine). ⚠️ ОБЩИЙ
ПРИНЦИП: любая новая анимация — ТОЛЬКО на ДЕТЯХ ячейки (transform/opacity/
filter), НИКОГДА на самом `#vitrine`: его rect читает
якорь всплывашки (85-hud). Проверено — rect бит-в-бит одинаков при
introdone off/on, `.hit`, `.rin` (замер playwright).

- **ТЕМА ВИТРИНЫ/ТОСТА ПО ВРЕМЕНИ СУТОК (2026-07-24, макеты 776:649/776:701).**
  ⚠️ ДЕНЬ/УТРО = БЕЛАЯ, ВЕЧЕР/НОЧЬ = ЧЁРНАЯ (спека владельца дословно; сначала
  я сделал «тёмная всегда» — владелец поправил: над СВЕТЛЫМ дневным полем
  белая панель читается, тёмная нужна только над ночным). Сигнал — тот же
  `html.night`, что у Shake/выбора неба (updateHUD по часам 20..5 — границы
  живут в SKY_DAY_FROM/SKY_NIGHT_FROM, 00-config; было 18..5 до 2026-07-31).
  • ДЕНЬ (база): панель `rgba(255,255,255,.16)` + белое inset-свечение, имя/
    множитель/буква `#3a4068`, трек/бейдж `rgba(255,255,255,.4)`; тост —
    светлая пилюля + свечение. (Совпадает с макетом «Level items white».)
  • НОЧЬ (`html.night`): панель `rgba(42,43,50,.4)` + border `rgba(0,0,0,.07)`,
    свечение снято; текст `#fff`; трек `rgba(0,0,0,.3)`; бейдж `rgba(0,0,0,.4)`;
    тост — пилюля `rgba(0,0,0,.2)` без свечения. Лаймовый бейдж тоста `#c0ff47`
    + чёрная цифра — в ОБЕИХ темах.
  Замер: день panelBg .16/текст #3a4068, ночь panelBg 42,43,50/текст #fff.
  ⚠️ ЗАЗОР ПРОГРЕСС↔МНОЖИТЕЛЬ ровно 12px (спека владельца) = `.vcell` gap 8 +
  `.vmult margin-left:4` (БЕЗ `margin-left:auto` — тот уводил бейдж вправо на
  ~58px). `.vbody` фикс `width:140px` (= прогресс; имя обрезается по нему), а
  `#vitrine width:fit-content` («подтянуть по ширине» — панель по контенту,
  ~291 вместо 300). Замер: gap ровно 12, панель 291.
  ⚠️ `.vmult` — вес **700, КАК У ИМЕНИ** (слово владельца 2026-08-13 «стиль
  шрифта множителя такой же как стиль названия объекта»); прежний 900 был
  ЕДИНСТВЕННЫМ отличием от `.vname` — семья/кегль/цвет уже были общими.
- **РЕАКЦИЯ НА СОВМЕЩЕНИЕ (интерактивно, ненавязчиво):** `vitUpdateCell`
  ловит РОСТ accCount слота (`grew`, монотонный счётчик) и вешает `.hit` —
  подскок портрета `scale 1.2` + вспышка полоски (`filter:brightness`),
  460 мс, рестарт через reflow. Лаг до 150 мс (тик витрины) — матч ловится
  диффом счётчика, ядро (80-gameplay) не трогаем. first-set (`last=-1`) не
  пульсит.
- **КАСКАД РАЗВОРОТА:** к clip-path-вытеснению панели `buildVitrine` вешает
  на каждую строку `.rin` (`vReveal`, приезд справа `translateX(28→0)` +
  fade) с `animation-delay = i·step` (step капится `min(.07, .45/count)` —
  разворот не тянется при многих строках); снимается таймером `520+i·step·
  1000` мс, иначе остаточный inline `animation-delay` задержал бы будущие
  `.hit`/`.in` (ротация). Всё под `@media (prefers-reduced-motion)`.
- **ВСЕ ТИПЫ УРОВНЯ + НИЗ-ЛЕВО (спека владельца 2026-07-23):** панель
  показывает НЕ 5 слотов с авторотацией, а ВЕСЬ замес типов уровня строкой
  (`buildVitrine` строит `count = vitQueue.length`, `VIT_SLOTS` удалён).
  ⚠️ ЭТА СТРОКА УСТАРЕЛА, ИСПРАВЛЕНО 2026-07-28: «весь замес» продержался
  недолго — кап и ротация по прогрессу вернулись 2026-07-27, а спекой владельца
  2026-07-28 («давай сюда всё же три строки») кап равен `VIT_MAX = 3` (85-hud).
  Я разослал направлению задание по этому устаревшему абзацу, и ИНТЕРФЕЙС
  вернул поправку — доверять надо 85-hud, а не этому описанию.
  Собранный тип уезжает и исчезает (очереди на замену нет). Позиция опущена
  `bottom 76px → 8px`: Hint-кнопка переехала ВПРАВО к Shake (разметка
  bottomBar — hint в правый `.grp`), низ-лево освободился под витрину, она
  растёт ВВЕРХ. ⚠️ На высоких уровнях (много типов) панель может уйти выше
  вьюпорта — ⛔ ВОПРОС ЗАКРЫТ ЗАМЕРОМ 2026-07-30 (ИНТЕРФЕЙС): НЕ МОЖЕТ. Абзац
  писался, когда панель строила ВЕСЬ замес; кап `VIT_MAX = 3` (85-hud) вернулся,
  и высота панели ФИКСИРОВАНА 170px независимо от уровня. Замер на уровнях
  1/20/40/93 × вьюпорты 900×640 / 900×568 / 1440×700: три строки, высота 170,
  top 462/390/522 — выше вьюпорта не уходит нигде.

## ГЛАВНЫЙ ЭКРАН = ПАУЗА (2026-07-23, макеты 770:1271 моб. / 763:1031 деск.)

Спека владельца «это и главный экран и пауза»: `#mainScreen` ЗАМЕНИЛ карточку
`#pauseOverlay` (та осталась в разметке как ДЕРЖАТЕЛЬ состояний `soundToggle`/
`hardToggle`, но НЕ показывается). Кнопка ⏸ открывает меню; большая кнопка —
«Resume» при живой партии и «Play Game» когда партии нет (`!level ||
level.over` -> `genLevel()`). Вход в отладочную панель переехал неприметной
ссылкой `#msDev` на сам экран.

⚠️⚠️ ВЛАДЕНИЕ ПАУЗОЙ — ГЛАВНАЯ ГРАБЛЯ. `pauseGame(silent)` (99-main) отдаёт
`true` ТОЛЬКО если паузу поставил ИМЕННО этот вызов, а `resumeGame()` снимает
её БЕЗУСЛОВНО. Паузой пользуется и РЕКЛАМА (78-ads, `pausedByAd`), и уход
вкладки в hidden (90-input). Поэтому меню:
1. ставит паузу **тихо** — `pauseGame(true)`, иначе поверх меню вылезла бы ещё
   и карточка pauseOverlay;
2. запоминает владение в `menuPaused` и **снимает только свою** паузу;
3. **не открывается поверх ЧУЖОЙ паузы** (`if (!menuPaused && paused) return`) —
   иначе игрок закрыл бы меню во время ролика и разморозил игру под рекламой.
Паттерн скопирован с `pausedByAd` в 78-ads — при правках держать их синхронно.
Ассерты сьюта на ТИХУЮ паузу (реклама/межстраничная) стерегут этот контракт —
не ослаблять; ассерт игровой паузы переведён с `#pauseOverlay` на `#mainScreen`.

- **МУЗЫКА ЕСТЬ** (v1-test-106, спека владельца «добавь регулятор музыки,
  этот трек на фоне»): регулятор Music показан, фоновый трek — ВНЕШНИЙ
  `music.mp3` рядом с index.html, `<audio loop preload="none">` (ленивая
  загрузка после первого жеста — политика автоплея; громкость 0..1 в
  localStorage mixer_music, 0=пауза). ⚠️ НЕ инлайн: 4.4МБ base64 раздули бы
  index.html 7→12.6МБ (×3.5 старт), музыка некритична. ⚠️ ПАКЕТ ПОРТАЛА =
  index.html + 2 bridge + **music.mp3** (был 3 файла); зип тестеров тоже
  несёт music.mp3 рядом с Mixer.html. index.html без music.mp3 играет
  грациозно-без-музыки. WebAudio-SFX (75-audio) — отдельный тракт, не тронут.
- **Профиль** — плейсхолдер аватар+имя под будущую Google-авторизацию
  (решение владельца «нужна авторизация через гугл для сохранения прогресса»).
  Кнопку «Войти» НЕ рисуем: ИНТЕГРАЦИЯ проверяет, что Google в iframe портала
  может не работать и вместо него будет авторизация площадки.
- **ЗВЁЗДЫ = ВАЛЮТА** (решение владельца «это валюта, очки которые можно
  тратить, никаких монет не нужно»). Показываю `starBalance()` (НЕ totalStars —
  тот рейтинг, не тратится); Boost/цены/гашение — живые ручки МЕТЫ (см. раздел
  «звёзды = валюта + Boost»). Subscribe/Get More — заглушки за ИНТЕГРАЦИЕЙ.
- **ШАПКА ПРОФИЛЯ: одна на две раскладки (2026-07-24, «перенеси по макету»).**
  Профиль (`#msStars`/`#msGetMore`/`#msUser`) — ЕДИНЫЙ узел (дублировать нельзя,
  там id), поэтому переносится РАСКЛАДКОЙ, а не копией. Обёртка `.ms-collhead`
  держит заголовок «My collection» + профиль: на МОБАЙЛЕ `display:contents`
  растворяет её, дети встают прямыми флекс-детьми `.ms-wrap` и раскладываются
  по `order` (профиль пилюлей наверх −1, заголовок к сетке 4); на ДЕСКТОПЕ она
  реальный флекс-ряд в шапке правой колонки (макет 763:1031: заголовок слева,
  профиль справа, БЕЗ пилюли, порядок звёзды→Get More→аватар, имя скрыто).
  ⚠️ Заголовок один — вынесен из `.ms-coll` в `.ms-collhead` (в `.ms-coll`
  осталась только сетка). Замер: десктоп title/head в одном ряду (cy 40),
  мобайл head y8 пилюлей / title y748 у сетки.

## Глаза: КАНОН — docs/EYES-CHARACTER-SPEC.md (живой документ)

Спека владельца 2026-07-21: отдельный документ «как работают глаза»,
ВЕСТИ при каждой правке. Любое изменение глаз = правка раздела там +
строка в его «Журнале». Исторические заметки о глазах в этом файле
оставлены как хронология решений, но канон — спека.

## v1-тюнинг 2026-07-21-г: EN-интерфейс, глаза-2, десктопная раскладка

- ВЕСЬ ИНТЕРФЕЙС НА АНГЛИЙСКОМ (спека владельца), включая панель
  разработчика, тосты, оверлеи, fatal-экраны и <title>. Строки в
  80-gameplay/78-ads/99-main переведены чатом ИНТЕРФЕЙСА по мандату
  «локализация текстов» (только литералы, логика не тронута). Комменты
  в коде остаются русскими. Метка версии: «build vN».
- ГЛАЗА ПРИ ПОМОЛЕ ВСЕГДА ЗЛЫЕ (eyes-3-1): помол перебивает реакции,
  моргание и faceHold; реакции на падение счёта ГЛУШАТСЯ при помоле
  (иначе штраф −20 каждый помол дёргал грусть поверх злых). Зрачки злых
  ПОДВИЖНЫ — сканируют чашу влево→вправо→вниз шагом 0.8 с; это отдельные
  circle с клипом ПО БЕЛКУ-клину (за пределы не выходят).
- ПРОМАХ = ГРУСТНО СМОТРЯТ ВНИЗ (eyes-1-6, узел 741:1336, слой fSad).
  ВХОД АНИМИРОВАН (спека владельца «сделай естественнее»): зрачки НЫРЯЮТ
  вниз 80 мс на круглой паре (faceHoldFrom задерживает hold) → нижние
  веки .lid ВЫЕЗЖАЮТ снизу 0.22 с (CSS-переход translateY 34→0 на
  #fSad.on) → держится ~0.7 с → веки уезжают, взгляд ещё ~1 с висит
  внизу (lookVec-хвост). Замер rAF: грусть видна 273→941 мс от тапа.
  Зажмур fSquint УДАЛЁН вместе с разметкой.
- CODE CONNECT НЕВОЗМОЖЕН на текущем тарифе Figma: API отвечает «You need
  a Dev or Full seat on an Organization or Enterprise plan». Задача
  закрыта как заблокированная тарифом (2026-07-21), не возвращаться без
  смены плана.
- GRINDING +30%: кегль 40.7, обводка 11.4 (SVG-stroke).
- ДЕСКТОП/ПЛАНШЕТ ≥768px (макет 747:1048): LV и время партии — В СТРОКУ
  У ПАУЗЫ слева (кегль 34 = ×1.545 от мобильного), справа ТОЛЬКО счёт;
  счётчика предметов в макете нет — скрыт. Стековые svg масштабируются
  через viewBox + CSS-размеры; узел времени физически переносит
  layoutHUD (90-input, на resize) — id не дублируются. LV-элемент
  #lvlSvg живёт в левой группе всегда, на мобильном скрыт CSS.

## v1-тюнинг 2026-07-21-б: HUD по макету Figma + живые глаза

Макеты владельца: Figma 741:1497 (Game-Screen) и 741:1420 (Eyes), ассеты —
папка `Interface/`. Читать через Dev Mode MCP. ⚠️ Встроенный коннектор в
сессии стучится в старый `/sse` и отдаёт заглушку «включите Dev Mode»;
живой эндпоинт — `http://127.0.0.1:3845/mcp` (streamable HTTP).

- ГЛАЗА — ОДИН ИНЛАЙНОВЫЙ SVG, а не 25 картинок. Ключевое наблюдение: почти
  весь набор владельца это два круга с разными числами (белок r60, зрачок
  cx/cy/r). Круглая пара ПАРАМЕТРИЧЕСКАЯ — зрачок ездит ±24 и меняет размер
  15..50 в единицах viewBox 240×120, чем покрыты семейства eyes-0 (взгляд,
  размер), eyes-2 (хитрые) и eyes-5 (подмигивание). Отдельными слоями лежат
  только несводимые формы: `fAngry` (eyes-3), `fArc` (eyes-4-4, добрые),
  `fX` (eyes-4, поражение), `fSquint` (eyes-4-3, зажмур на промахе).
- АНИМАЦИЯ — ЧЕТЫРЕ НЕЗАВИСИМЫХ СЛОЯ (85-hud): ЭМОЦИЯ (`eyesMood`, 7 состояний)
  + ВЗГЛЯД (`gazeFor`) + РЕАКЦИЯ (`faceEvent`/`facePulse`) + МОРГАНИЕ.
  Взгляд: в покое лениво бродит ±10, на тапе провожает палец (`faceLook`
  из 90-input), в турбо мечется раз в ~200 мс, в «закатанных» уходит вверх.
- ТУРБО ПОКАЗЫВАЮТ ЗРАЧКИ, полоски накопления НЕТ (спека владельца):
  серия копит размер зрачка 29 -> 50 (`pupilScale` от comboCount/CHAIN_COMBO_AT),
  в самом Power chain они распахнуты на максимум. `#chainBar` удалён.
- РЕАКЦИИ БЕЗ ПРАВОК В ЯДРЕ: `tickFace` следит за `stats.score` — вырос
  (матч/сюрприз) даёт пульс зрачка, упал (промах −10, помол −20; под бустером ×множитель) — зажмур.
- HUD ПО МОБИЛЬНОМУ МАКЕТУ 741:1738 (393×852, он главный — десктопный
  741:1497 был черновиком): слева ⏸ 56, справа ВЕРТИКАЛЬНЫЙ СТЕК по 22px
  (предметов / зелёное время партии / ★ очки градиентом #ff70b5→#f2ff00),
  по центру в ТОЙ ЖЕ строке конструкция (глаза 210×105 + чёрное число
  отсчёта 54.8px внахлёст на низ глаз, краснеет при ≤3 с), снизу СЛЕВА
  контурная подсказка, снизу справа «Shake ×N» пилюлей #1d1c26. Поля 8.
  НОМЕРА УРОВНЯ на игровом экране нет, монет тоже (кошелёк — в меню),
  ⚙️ УБРАНА — панель открывается из ПАУЗЫ (`#pauseOverlay`).
- ГЛАЗА, ПРАВИЛА ВЛАДЕЛЬЦА 2026-07-21 (85-hud): (1) ЧЁРНЫЙ ЗРАЧОК НИКОГДА
  не выходит за белок — `clampGaze` режет вектор взгляда по свободному месту
  (радиус белка − радиус зрачка − 1); без этого распахнутый зрачок при
  взгляде вбок вылезал наружу; (2) покой — зрачки бродят + моргание;
  (3) НАБОР БУСТА — зрачки растут 29→50; (4) БУСТ НАБРАН — резко сжимаются
  до 15 (eyes-0-1) и КАТАЮТСЯ В РАЗНЫЕ СТОРОНЫ (один по часовой, другой
  против, оборот ~1.2 с); (5) ДУГИ eyes-4-4 УДАЛЕНЫ совсем — «добрые»
  показываем размером зрачка, а не формой; (6) помол — злые eyes-3-1
  (узел 741:1136); (7) при помоле вместо красного «0» слово «Grinding»
  тем же стилем, кегль 0.13 ширины конструкции; (8) СЕРИЯ ТУРБО
  (chainSeries>=2 из 60-access, решение владельца) — асимметрия eyes-5:
  левый зрачок 40, правый белок 44 со зрачком 12, качение как в турбо
  (clampGaze сам держит каждый глаз по его размерам).
  ⚠️ Слои SVG перечислены в `setFace` — удалив слой из разметки, УБЕРИ его
  из списка: `$('fArc')` вернул null и валил тик каждый кадр, из-за чего
  вставал весь игровой цикл (поймано тестом: бот не мог добить уровень).
  Не задействовано: семейство eyes-1 кроме 1-6 (промежуточные веки).
- ⛔ «ГЛАЗА ПРОПАДАЮТ В ТУРБО» — ВОПРОС ЗАКРЫТ ЗАМЕРОМ 2026-07-30 (ИНТЕРФЕЙС),
  НЕ ВОСПРОИЗВОДИТСЯ. Механическая причина, почему и не может: лихорадка живёт
  у НИЗА экрана (`fever = uCombo · (1 − smoothstep(0, FEVER_SPAN, sy))` в шейдере
  неба), а глаза сверху. Замер строкой пикселей через центр глаз на ДНЕВНОМ небе:
  фон L=204 в покое и L=204 в турбо, контраст белка к фону 51 → 51, почти-белых
  пикселей в фоне 0 из 60. Жалоба относилась к эпохе ЧИСТО БЕЛОГО поля;
  светло-голубая панорама её и закрыла.
  ⚠️ ЧТО ОСТАВАЛОСЬ РЕАЛЬНЫМ РИСКОМ: контраст белка к фону по панорамам суток —
  ночь 209, ДЕНЬ ВСЕГО 50 из 255 (в отношениях WCAG это ~1.6:1).
  ✅ РИСК ЗАКРЫТ ДВАЖДЫ, ОБА РАЗА — 2026-07-31. (1) Палитры владельца сами
  подняли дневной контраст с 1.6 до ~3.0 (панорам давно нет, 05-sky.js удалён —
  число выше ИСТОРИЧЕСКОЕ, с текущими замерами его не сверять). (2) Поставлен
  страж пола в test.js, секция «ПОЛ КОНТРАСТА HUD К НЕБУ», по глазам И по кнопке
  паузы, день и ночь. Подробности и действующие числа — в разделе «ДВА ИНВАРИАНТА
  НЕБА». ⚠️ Про кнопку паузы: она давно НЕ белая днём — по правилу цвета кнопок
  в светлой теме она тёмная, поэтому её риск ОБРАТНЫЙ (гаснет на потемнении
  неба), и у неё свой пол.
  ⚠️ И методическое: ПЕРВАЯ проба ИНТЕРФЕЙСА измерила это НЕВЕРНО (точка «белок»
  попала в распахнутый зрачок, точка «фон» ушла за кадр и дала чёрный) — верен
  профиль СТРОКИ пикселей, а не две точки. Если где-то всплывёт первое число —
  не доверять.
- ⚠️ КОЛОНКА МАКЕТА РАССЧИТАНА НА ТРЁХЗНАЧНЫЕ ОЧКИ, а игра к 3-му уровню
  даёт пятизначные: «★ 12480» рвал стек на две строки и наезжал на глаза.
  Лечение — счёт ≥10000 сжимается в «12.5k» (85-hud) + резерв ширины
  `--eyeW: min(210px, 100vw − 202px)`. Цена: на 393 глаза 191 вместо 210,
  на 360 — 158. Полноразмерные 210 только с 430px.
- ФОН #d0dff3 вместо белого — ЗАПРОС В ГРАФИКУ, интерфейс его НЕ трогает
  (указание владельца): и поле (шейдер неба), и подложка html/body —
  их зона. Без этой правки белые глаза и белая кнопка паузы невидимы
  на белом поле.
- ШРИФТ макета SF Pro Rounded есть только на Apple: берём родовое
  `ui-rounded` с откатом на `system-ui` (вебфонт ради HUD не тянем).
- ⚠️ **ОБВОДКА = ОДНА ПЕРЕМЕННАЯ (спека владельца 2026-07-28): `--otl` —
  ВИДИМАЯ толщина в px, `--otl-color` — цвет. Пишем ТОЛЬКО их.**
  Внутри `.otext text` стоит `stroke-width: calc(var(--otl,2) * 2)` —
  удвоение спрятано, потому что половина обводки уходит под заливку
  (`paint-order:stroke`). Снаружи про ×2 знать не нужно: `--otl:8` даёт
  ровно 8px на экране. Дефолты: `--otl:2`, `--otl-color:#fff`.
  ⚠️ ВСЕ прежние `stroke-width` переведены на ПОЛОВИНУ своих чисел, вид
  бит-в-бит (замер computed до/после совпал): `.stat` 4→`--otl:2`,
  `win-level` 9→4.5, `win-time` 5→2.5, `win-score` 18→9, `#mixerTimer`
  17.6→8.8 (grind 11.4→5.7), `.pop` 4→2 (+`--otl-color:#000`), `.pop.big`
  6→3. Новое правило пишем СРАЗУ в `--otl`, `stroke-width` руками больше
  не трогаем.
  ⚠️ Обводка работает ТОЛЬКО на SVG-`.otext`: HTML-текст обвести нечем
  (`-webkit-text-stroke` запрещён — срезает углы). Понадобилась обводка —
  сперва переводим узел в `<svg class="otext"><text>` (так сделано с ×N
  на карточках More Stars); `text-shadow` на SVG не действует — тень
  вешаем `filter:drop-shadow` на сам svg.
  ⚠️ ТОЛСТАЯ ЦВЕТНАЯ ОБВОДКА СМЫКАЕТСЯ В ПЯТНО — и это ЗАДУМАНО: CLEANED
  на экране победы = белые буквы + лайм `#c0ff47` при `--otl:10`, что и
  даёт «лайм-пилюлю» из рендера Figma (старый спор закрыт). Браком
  смыкание было только когда обводка совпадала с заливкой по цвету
  (белое на белом — каша, см. историю 2026-07-28).
- ЕДИНЫЙ МЕХАНИЗМ КОНТУРА (спека владельца 2026-07-21-в «собери все
  реализации в одно решение»): весь обведённый текст — SVG-<text> класса
  `.otext` (shell.html): настоящий векторный stroke, stroke-linejoin:round,
  paint-order:stroke. Толщина настраивается ОДНИМ stroke-width = 2× видимой
  обводки (половина уходит внутрь под заливку). Потребители: число
  отсчёта/Grinding (обводка 7), стек справа — предметы/время/очки (обводка
  2; градиент очков — SVG linearGradient #gScore, обводке не мешает),
  всплывающие попы очков (белые с ЧЁРНОЙ обводкой 2; рендер в scorePopScreen,
  70-fx — правка ИНТЕРФЕЙСА по прямому указанию владельца; параметр color
  сохранён в API, но игнорируется). ЗАПРЕЩЕНЫ: -webkit-text-stroke (срезает
  углы miter'ом) и обводка тенями по кругу (волнит край) — оба брака
  владелец видел на скриншотах.
- ⚠️ **СИСТЕМНОЕ ПРАВИЛО ЦВЕТА КНОПОК (спека владельца 2026-07-28) — СТАНДАРТ
  ДЛЯ ВСЕХ БУДУЩИХ КНОПОК: в СВЕТЛОЙ теме кнопки ТЁМНЫЕ, в ТЁМНОЙ
  (`html.night`) — СВЕТЛЫЕ.** Пара переменных в `:root` shell.html:
  `--btn-bg:#2a2935 / --btn-fg:#fff`, под `html.night` — наоборот. Новые
  кнопки просто берут `var(--btn-bg)/var(--btn-fg)` и наследуют правило;
  точечные отклонения — переопределением поверх («править по мере
  необходимости»). Сейчас на правиле: `.iconBtn` (пауза, подсказка),
  `#shakeBtn`, `#scopeBtn/#magnetBtn`. ⚠️ ПРАВИЛО ПЕРЕВЕРНУЛО дневные/ночные
  пары Figma-нод v127 у КРУГЛЫХ кнопок (там день = БЕЛЫЙ круг) — прямая
  спека новее макета. ⚠️ НЕ трогает АКЦЕНТЫ: лаймовый бейдж зарядов
  (`#hintCnt`) и слово «Ad» лаймом внутри Shake. ⚠️ ГРАНИЦА: правило про
  кнопки ИГРОВОГО экрана (их красит `html.night`); кнопки МЕНЮ живут на
  собственной светлой подложке и в правило пока не включены — если владелец
  захочет и их, это отдельное слово. ⚠️ ИСКЛЮЧЕНИЕ v211: крестик More Stars
  (`#starsClose`) — ВСЕГДА белый с чёрным крестом (спека владельца 2026-07-31
  «цвет иконки не зависит от времени суток»): оверлей тёмный в обе темы, а
  правило давало днём тёмную кнопку на тёмном фоне. Оформлено
  переопределением поверх (.st-close), страж в сьюте держит обе темы.
- ТЕМА КНОПКИ SHAKE по времени суток (спека владельца 2026-07-21-в; с
  2026-07-28 — частный случай правила выше): днём
  чёрная пилюля с белым текстом, НОЧЬЮ инверсия — белая с чёрным. Класс
  `night` на <html> ставит updateHUD (isNightSky, 85-hud) по тем же
  границам часов, что градиент неба в 10-stage (skyTimeNow).
  ⚠️⚠️ ГРАНИЦЫ БОЛЬШЕ НЕ ДУБЛИРУЮТСЯ РУКАМИ (2026-07-31): обе функции читают
  `SKY_DAY_FROM`/`SKY_NIGHT_FROM` из 00-config. Прежний абзац разрешал вписать
  час в каждую функцию отдельно («цена ошибки лишь оттенок кнопки») — спека
  владельца «день до 20:00, ночь с 20:00» показала, чего это стоит: правка
  одной функции дала бы с 20 до 22 ДНЕВНОЕ НЕБО ПРИ НОЧНОЙ ТЕМЕ КНОПОК.
  Действующие границы — ДЕНЬ 5–20, НОЧЬ 20–5; менять только константы.
  ⚠️ Час обе берут через `skyHourNow()` (10-stage), в нём же форс-хук
  `?hour=N` — им проверяются темовые фичи, раньше требовавшие подмены Date.
- МЯГКАЯ ТЕНЬ под числом отсчёта (0 4.7px 16px из макета) СНЯТА: владелец
  читал её как «градиентный фейд» под глазами.
- НАСТРОЙКИ ИГРОКА (Звук, Сложность Hard) живут В ПОПАПЕ ПАУЗЫ, а не в
  ⚙️-панели (спека владельца): в панели остались только отладочные ручки
  (радиус, зазор, подсветка, New level, Reset progress), вход в неё —
  неприметной ссылкой внизу карточки паузы.

## v1-тюнинг 2026-07-21: персонаж = одна конструкция, 7 эмоций

- КОНСТРУКЦИЯ `#face` (спека владельца): глаза + отсчёт до перемолки
  (`#mixerTimer`) + полоска накопления турбо (`#chainBar`) — один блок
  СВОЕЙ СТРОКОЙ под чипами (220×123, top safe-area+56). Причина: по центру
  верхнего бара свободно 0–2 px на 320/360 при значениях ур.3 — глаза
  съедали таймер и монеты (замер в docs/EYES-CHARACTER-SPEC.md §2).
  Отменяет спеку 2026-07-20-б «таймер в центр НИЖНЕГО бара».
- ВРЕМЯ ПАРТИИ УБРАНО (чип `#timer` + его обновление в 99-main): на экране
  остаётся один таймер — до перемолки. Два таймера путали.
- 7 ЭМОЦИЙ вместо разрозненных эмодзи: спокойные / добрые / злые /
  удивлённые / закрытые / хитрые / закатанные. Приоритет и триггеры —
  docs/EYES-CHARACTER-SPEC.md §1. Лесенка угрозы: спокойные → закатанные →
  хитрые (≤3 с до перемолки) → злые (помол). Плюс моргание 120 мс раз в
  4–7 с. `eyesMood` отдаёт ИМЯ состояния, отрисовка — в `setFace`
  (сейчас эмодзи-заглушки, ассеты SVG со слоем зрачков в работе);
  `faceEvent(state, ms)` — короткие реакции поверх состояния.

## v1-тюнинг 2026-07-20-г: МЕТРИКА v3 — истинный зазор поверхностей (спека владельца)

- Спека владельца: «нужна не честная сфера, а что-то умнее, понимающее
  топологию, особенно для продолговатых». Диагноз: зазор ОХВАТНЫХ сфер
  анизотропно щедрил вытянутым — стейк (0.12×0.81×0.53) плашмя-стопкой
  «матчился сквозь воздух»: видимый зазор 1.0 = охватный −0.46; вдоль
  длинной оси метрика была почти точна. Один скаляр это не чинит.
- МЕТРИКА v3: матч = ИСТИННЫЙ зазор между физическими поверхностями
  <= matchRadius, через GJK Rapier (collider.contactCollider с prediction).
  Охватный зазор (pairDist) остался ФИЛЬТРОМ грубой фазы — он нижняя
  граница истинного, отсечение честное. Компаунды — перебор пар коллайдеров
  с ранним выходом (дырка тора настоящая). При matchRadius>=9 (цепь/
  эндшпиль) — всё матчится без GJK. Единственная точка — pairMatch
  (60-access); MATCH_EDGE_PAD удалён, константы владельца канонические:
  база 0.9, пол 0.75, комбо-потолок 2.0 — теперь это истинные зазоры.
- ТЕЛЕГРАФ: сфера заменена ОРЕОЛОМ-ПРИЗРАКОМ формы (reachGhostFX в
  80-gameplay, fresnel-материал как у сфер): сама форма предмета, раздутая
  на R по каждой локальной оси (у стейка зона — плита, у шара — шар).
  ⚠️ Геометрия призрака КЛОНИРУЕТСЯ — stepFX делает dispose по завершении,
  общий кэш геометрий типов отдавать нельзя. sphereFX в 70-fx жив, не зовётся.
- Перф: GJK микросекунды; узкое место — availablePairs на полной куче
  (см. замер в верификации). Тик HUD 600 мс, тап-путь <1 мс.

## v1-тюнинг 2026-07-20-в: ПРАВИЛО КАСАНИЯ СФЕРЫ (спека владельца)

- Жалоба владельца: «объекты соединяются по центрам, а человек ориентируется
  на близость поверхностей — близкие краями должны соединяться». Метрика УЖЕ
  была зазором поверхностей (2026-07-18), реальный источник ощущения —
  РАССИНХРОН ПРАВИЛА И ВИЗУАЛА: сфера тапа рисовалась с запасом «типичный
  чужой радиус» (+0.55), а правило запас не учитывало — мелкий кандидат
  КРАЕМ уже в сфере, но не матчился (центру не хватало до 0.55).
- НОВОЕ ПРАВИЛО: матч = охватная сфера кандидата КАСАЕТСЯ видимой сферы;
  порог везде matchRadius + MATCH_EDGE_PAD (0.55, 00-config — общая с
  визуалом sphereFX). Единственная точка сравнения — pairMatch (60-access);
  все проверки (тап/бот autoMatch/подсказка/прицел/availablePairs/детект
  тупика) ходят ТОЛЬКО через неё — новые писать так же.
- Эффективные пороги выросли на 0.55: база 0.9->1.45, пол 0.75->1.3,
  комбо-потолок 2.0->2.55 (владельцу предложена компенсация базы/потолка,
  если станет «слишком легко» — его решение). Слайдер ⚙️ и HUD показывают
  matchRadius БЕЗ запаса.
- Верификация: test.js зелёный (тупиковых встрясок 2); A/B-бот на полном
  уровне 181 (4 конфига, сиды 7/8): старая {3,3,4,4} vs новая {5,3,3,4}
  встрясок — шум, бюджет 5 держится; доступных пар на равном срезе ~+80%.
  Тест форс-тупика (matchRadius=-9) валиден: -9+0.55 всё равно недостижим.
- КОМПЕНСАЦИЯ (в тот же день, вердикт владельца «стало слишком легко,
  особенно с комбо»): константы опущены на величину запаса — baseRadius
  0.9->0.35, MATCH_R_MIN 0.75->0.2, COMBO_RADIUS 2.0->1.45; эффективная
  кривая (raw + 0.55) совпала с отятюненной бит-в-бит: база 0.9 -> пол 0.75,
  комбо-лесенка до 2.0. Честная сфера ОСТАЛАСЬ (рисуется на 0.55 меньше
  прежней и не врёт). Слайдер ⚙️ пересчитан: 0-1.65 (эффективные 0.55-2.2),
  дефолт 0.35.

## v1-тюнинг 2026-07-24: ЕДИНЫЙ БАЛАНС (финализация модели владельцем)

Владелец дословно: «Очки и звезды это одно и тоже, все что заработано в
рамках игрового уровня это и есть баланс. Его можно тратить на открытие
вещей, буст вещей и так же он влияет на позицию в лидерборде.» ОТМЕНЯЕТ
прежнюю модель «рейтинг≠кошелёк» (раздел 2026-07-23 ниже — историчен).

- ⚠️ **ОДНО ЧИСЛО — ЭТО СЕМАНТИКА, НЕ СХЕМА ХРАНЕНИЯ.** balance = `se−ss`
  показывается ВЕЗДЕ (чип в игре, кошелёк в меню, лидерборд). Внутри —
  всё те же ДВА монотонных счётчика (анти-дюп): se = ПОЖИЗНЕННЫЙ
  накопленный игровой счёт (деноминир.), ss = пожизненные траты. Поле-
  баланс с max-мержем ЗАПРЕЩЕНО (дюпило бы — грабля монет). Ассерт дюпа
  сохранён.
- **se кормит `bankLevelScore(score)`** на победе (80-gameplay checkEnd):
  `se += floor(score/SCORE_DENOM)`. Раньше кормила `awardStarsForWin`
  (дельта рейтинга) — УБРАНА. starAward остался ТОЛЬКО для grandfather-
  миграции.
- ⚠️ **ФАРМ НЕ ГРОЗИТ структурно:** игра линейна, реплея пройденных уровней
  нет (levelNum только растёт), счёт банкуется раз за уровень. Прежний
  анти-ферма (дельта рейтинга) не нужен. ⚠️ Если появится level-select/
  реплей — вернуть «лучший счёт за уровень» (Save.sc[lv], банк дельты).
- **ДЕНОМИНАЦИЯ ×10** (SCORE_DENOM=10, решение владельца, охват «делить всё»
  — рабочее допущение диспетчера 2026-07-24): в кошелёк score/10, цены
  Boost/unlock в тех же мелких единицах. Earn rate ~600-800 деноминир./
  уровень.
- **РЕЙТИНГ `stars[lv]`** остаётся индикатором качества (setStars, ★ на
  экране победы) — валюту БОЛЬШЕ НЕ несёт.
- **ЧИП В ИГРЕ = `liveBalance()`** (запрос ИНТЕРФЕЙСУ): баланс +
  незабанкованный счёт текущего уровня (floor/10); на победе непрерывен.
  ⚠️ Интерфейс должен переключить чип с `stats.score` на `liveBalance()`.
- **ОТКРЫТИЕ ТИПА ЗА БАЛАНС** (`purchaseUnlock`, Save.uk мерж OR, цена
  TYPE_UNLOCK_PRICE=700): раскрывает тип в КОЛЛЕКЦИИ/портрете (+буст), НЕ
  меняет пул спавна genLevel (ранний спавн = правка ядра, не делал). Трата
  через ss. isTypeUnlocked/unlockedTypes/accSnapshot учитывают uk (поля
  `bought`/`unlockPrice`/`canUnlock`).
- ⚠️ **ЛИДЕРБОРД ≠ PAY-TO-WIN (фикс A, таблица №2):** пополнения
  (реклама/IAP) идут в отдельное поле `Save.tu`, НЕ в se. КОШЕЛЁК =
  `starBalance()` = se+tu−ss; РАНГ = `leaderboardScore()` = se−max(0,ss−tu)
  (трата сперва ест tu, ранг падает лишь при трате СВЕРХ пополнения). Так
  купленное тратится, но НЕ поднимает ранг; трата сыгранного на буст/анлок
  осознанно роняет позицию. `addStars` пишет в tu. Сам лидерборд-фича ждёт
  площадки (Playgama/Yandex да, Poki нет) — пока число-хендл.
- ⚠️⚠️ **МОДЕЛЬ РАНГА УТВЕРЖДЕНА ВЛАДЕЛЬЦЕМ 2026-07-29 — «КАК В РЕЙТИНГЕ ФОРБС».**
  Дословно: «нет такого понятия, как покупается. Игрок может купить бустер и
  набрать много очков, за счёт этого подняться в таблице. Но он может так же
  потратить свои очки и опуститься в самый низ таблицы. Эта очень странная
  механика, даже новаторская, но я хочу её использовать».
  ⛔ ЭТИМ ЗАКРЫТ ВОПРОС «БУСТЕР ЛОМАЕТ ЗАЩИТУ ОТ PAY-TO-WIN» — то, что я нашёл
  как дыру (`scoreBoostMult` множит счёт, а пар-база звёзд нет, 80-gameplay:101
  против 99-main:106), владелец объявил ЗАДУМАННЫМ. Ранг — не «пожизненное
  достижение», а ТЕКУЩЕЕ СОСТОЯНИЕ: заработал — поднялся, потратил — упал.
  Не переделывать и не «чинить».
  ⚠️ ФОРМУЛУ МЕНЯТЬ НЕ НУЖНО — она уже это делает. Проверка на числах: играл
  5000 → ранг 5000; купил бустер ×5 и наиграл 25000 → 25000; потратил половину
  → 12500; спустил всё → 0 (низ таблицы).
  ⚠️ ТОНКОСТЬ, КОТОРАЯ ДЕЛАЕТ МОДЕЛЬ ЧЕСТНОЙ: САМО пополнение ранг НЕ поднимает
  (se 5000 + tu 3000 → кошелёк 8000, ранг 5000). Купить можно МНОЖИТЕЛЬ к
  заработку, а не место — деньги надо отыграть. Поэтому «покупается» и правда
  неверное слово.
  ⚠️ СЛЕДСТВИЕ ДЛЯ ЭКРАНА: число в таблице и число в кошельке РАСХОДЯТСЯ, пока
  у игрока есть непотраченное пополнение. Это надо объяснить, а не прятать.
- ⚠️ **ГОСТИ В ЛИДЕРБОРД НЕ ПОПАДАЮТ (решение владельца 2026-07-29):** «чтобы
  попасть в лидерборд, нужно залогиниться». ⚠️ СТРОЖЕ, ЧЕМ ДЕЛАЕТ SDK: его
  гейт пропускает по НЕПУСТОМУ идентификатору, а у гостя он непустой (замер
  ИНТЕГРАЦИИ) — без нашей проверки гость бы отправлялся. Гейт свой, по признаку
  авторизации. Побочно закрывает засорение таблицы: гостевой идентификатор
  НОВЫЙ НА КАЖДУЮ СЕССИЮ, один человек плодил бы строки при каждом заходе, а
  удаления в SDK нет.
- ⚠️ **ТАБЛИЦА №2 УТВЕРЖДЕНА** (диспетчер по делегированию владельца
  2026-07-24, docs/STARS-STORE-ECONOMY.md §v2) под earn ~700 деноминир./
  уровень: BOOST_PRICE_BASE=2000 (лестница 2000/4000/8000/16000/32000, цена
  от boughtTier — фикс B, иначе «макс любимого» раздувался), TYPE_UNLOCK
  700, REWARD_STARS_PER_AD=70/кап 5, STAR_PACKS 3000/19000/90000. Фикс C
  (кап рекламы на server-времени) — зона ИНТЕГРАЦИИ при вводе топапа.
- API (все в __game): `starBalance`, `liveBalance`, `leaderboardScore`,
  `spendStars`, `onStarsChange`, `boostPrice/canBoost/buyBoost/boostTier`,
  `typeUnlockPrice/canUnlockType/purchaseUnlock`; тест-ручки
  `bankScore/clearBought/starGrant/starMigrate/saveRaw/mergeRaw`.

## ⛔ ОТМЕНЕНО (2026-07-24/27) — v1-тюнинг 2026-07-23: ЗВЁЗДЫ = ВАЛЮТА + BOOST

> ⛔ РАЗДЕЛ ИСТОРИЧЕСКИЙ. Модель «дельта рейтинга как валюта» ОТМЕНЕНА
> владельцем: действует ЕДИНЫЙ БАЛАНС (очки=звёзды=баланс=лидерборд,
> деноминация ÷10, банк накопленного счёта на победе). Паки звёзд и
> звёзды-за-рекламу УДАЛЕНЫ. Читать только как историю решений.
 (решения владельца) — ⚠️ ЧАСТИЧНО ОТМЕНЁН 2026-07-24 (см. «ЕДИНЫЙ БАЛАНС» выше: кошелёк теперь = накопленный счёт, а не дельта рейтинга)

Спека владельца: «звезды это валюта, очки которые можно тратить, никаких
монет не нужно» + Boost («прокачка предмета за валюту») — «да».

- ⚠️ **У ЗВЁЗД ДВЕ РОЛИ, ОНИ РАЗВЕДЕНЫ ПО РАЗНЫМ ПОЛЯМ** — это главный
  инвариант раздела: (а) **РЕЙТИНГ** уровня 1★/2★/3★ — `Save.stars[lv]`,
  НЕ тратится, мерж max (для рекорда max корректен); (б) **КОШЕЛЁК** —
  `Save.se`/`Save.ss` (earned/spent), баланс = разность. Одним полем делать
  НЕЛЬЗЯ: игрок потратил бы валюту и потерял свои 3★ на пройденных уровнях.
- ⚠️ **ПОЧЕМУ ПАРА СЧЁТЧИКОВ, А НЕ ПОЛЕ-БАЛАНС** (та же грабля, что была у
  монет): при max-мерже облака потраченное ВОССТАНАВЛИВАЛОСЬ бы из
  отставшей копии — валюта дюпится бесконечно. Оба счётчика монотонные,
  мержатся по max; трата растит `ss` и откату не подлежит. Сьют держит это
  ассертом «мерж со старой облачной копией НЕ вернул потраченное».
- НАЧИСЛЕНИЕ — **ДЕЛЬТОЙ РЕЙТИНГА** (`awardStarsForWin`): номинал
  `STAR_AWARD[звёзды] + STAR_LEVEL_BONUS×уровень` = 100/250/500 + 10×ур;
  за повтор уровня платится только РАЗНИЦА к прошлому рейтингу.
  Перепрохождение без улучшения даёт 0 — иначе короткий ур.1 с лёгкими 3★
  стал бы бесконечной фермой валюты (та же болезнь, что у фарма серии).
- **BOOST** — покупка ступени накопления: `Save.bo[тип]` (монотонно, мерж
  max). Итоговая ступень = `accCountTier` (заработанные совмещениями) +
  `boostTier` (купленные), общий кап `ACC_TIER_CAP`. Купленное живёт
  ОТДЕЛЬНО от `ac`: счётчик спасённых в витрине остаётся честным, а
  прогресс-полоска (`accNext`) считается по ЗАРАБОТАННЫМ ступеням.
  Цена `BOOST_PRICE_BASE×2^ступень` = 1500/3000/6000/12000/24000 —
  экспонента сама тормозит скупку, отдельный кап не нужен (макет владельца:
  Boost 11k ≈ 4-я ступень, баланс 166.5K = игрок с несколькими покупками).
- МИГРАЦИЯ существующих сейвов (`migrateStarsToWallet`, флаг `sm`):
  накопленный рейтинг конвертируется в стартовый баланс по тому же
  номиналу, прогресс НЕ обнуляется. Идемпотентна — `sm` монотонный и
  мержится по max, поэтому второе устройство повторно не начислит.
- API для ИНТЕРФЕЙСА (меню/Boost): `starBalance()`, `starAward(lv,stars)`,
  `spendStars(n)`, `onStarsChange(cb)` ({balance,earned,spent}),
  `boostPrice(key)` (null на капе), `canBoost(key)`, `buyBoost(key)` →
  {ok,price,tier,mult,balance,next}, `boostTier(key)`; `accSnapshot()`
  дополнен полями `boost`/`price`/`affordable`. Всё продублировано в
  `__game`; тест-ручки `starGrant/starMigrate/saveRaw/mergeRaw`.

## v1-тюнинг 2026-07-22: НАКОПЛЕНИЕ ПО ТИПАМ + новая баланс-таблица (спека владельца через диспетчера)

- НАКОПЛЕНИЕ: пожизненные монотонные счётчики совмещённых предметов КАЖДОГО
  типа — `Save.ac[имя типа]` (77-save; мерж max по ключу, gen-эпоха
  уважается; при смене партии моделей осиротевшие ключи НЕ теряются —
  console.warn в accAuditOrphans). Пороги ступеней — ряд ×2+100, утверждён
  владельцем: 100/300/700/1500/3100/6300… = 100·(2^n−1); первая ступень
  случается в первой сессии (~ур.7). МНОЖИТЕЛЬ ОЧКОВ типа = 1+0.25×ступень
  (ACC_MULT_STEP), кап ACC_TIER_CAP=9 (×3.25). Ступень/множитель ВЫЧИСЛЯЮТСЯ
  из счётчика — в сейве не дублируются.
- Инкремент — в doMatch НА N предметов группы, ДО подсчёта очков: матч,
  пересёкший порог, уже идёт по новому множителю (ап и жирные очки — одним
  моментом). Событие `onAccTierUp(cb)` с {name, tier, mult, item} кидается
  В МОМЕНТ пересечения (для всплывашки ИНТЕРФЕЙСА); Telemetry `acc_up`.
  БОМБА накопление НЕ копит (уничтожение — не совмещение; согласовано с
  лором STORY-SPEC «жертвы бомбы не спасены»).
- ПАР-СКОР (finalizeFill) считает базу ПО ТИПАМ × их множители — иначе
  прокачанные типы давали бы 2★/3★ автоматом (связка (а) спеки); сюрприз
  и бомба в пары не входят. API (контракт с ИНТЕРФЕЙСОМ, v2): accCount/
  accTier/accMult/accNext(ключ ассета) в 77-save; ГЛОБАЛЬНЫЕ accSnapshot()
  ({name: ярлык accLabel, key, count, tier, mult, next, _item: живой
  предмет}) и onAccTierUp(cb) ({name: ярлык, key, tier, mult, item —
  mesh жив, тело уже нет, портрет снимать сразу}); тест-хелперы
  `__game.accGrant/aliveByType/setLevel/matchType`.
- БАЛАНС-ТАБЛИЦА: промах −7 → **−10** (MISS_PENALTY); УРОВЕНЬ 1 — БЕЗ
  очковых штрафов вовсе (поп «−N» не рисуется, промахи считаются);
  уровни 1-5 — счёт КЛАМПИТСЯ снизу нулём; помол −20 остаётся; механика
  миксера (съедание) от уровня не зависит — только очки. Единая точка —
  scorePenalty (80-gameplay). РЫБКА: +150 + 5×уровень (SURPRISE_LEVEL_BONUS).
  Пороги звёзд STAR2_K=1.5 / STAR3_K=2.1 — ПОДТВЕРЖДЕНЫ калибровкой
  2026-07-22 (полный отчёт в WORKSTREAMS, блок МЕТА). ⚠️ ГЛАВНОЕ ЗНАНИЕ
  ОТТУДА: пар-скор считает «всё парами без комбо», а реальная цена
  предмета зависит от РАЗМЕРА ГРУППЫ (10·(N−1) на предмет, и группа от 3
  сама включает комбо ×2) — поэтому боты, матчащие пары, занижают счёт
  втрое-вчетверо, и калибровать звёзды ими НЕЛЬЗЯ (только реальными
  тапами: `__game.bestTapTarget()`). Средняя группа падает с числом типов
  (3.28 на ур.1 -> 2.19 на ур.20), из-за чего одна и та же игра даёт
  ratio 4.95 на ур.1 и 1.2 на ур.20: звёзды сейчас меряют НОМЕР УРОВНЯ
  не меньше, чем мастерство. Лечится только нормализацией базы под
  ожидаемый размер группы — открытый вопрос владельцу.
- Сборка в worktree: node_modules симлинк на основной клон
  (`ln -sfn ../../..../funnel-game-v1/node_modules node_modules`), иначе
  build.py молча собирает нечего.

## v1-тюнинг 2026-07-20-б: большие глаза + таймер в нижний бар

- ГЛАЗА ×3, затем −30% (в коде font-size 71px, top +4) — персонаж доминирует
  сверху по центру; полоска заряда цепи опущена под них (top +82).
  ⚠️ При «богатых» значениях (ур.12, 12:45, 🪙1240, ★12480) глаза перекрывают
  таймер и монеты на всех телефонных ширинах — замер и варианты правки в
  docs/UI-SCREENS-PLAN.md, решение за владельцем.
- ТАЙМЕР МИКСЕРА переехал в ЦЕНТР НИЖНЕГО БАРА (средний ребёнок flex
  space-between между группой ⚙️ и группой Hint/Shake) — спека владельца
  «логичнее между настройками и подсказкой». JS-логика не менялась.

## Зазор: ПОТОЛОК 1.1 ВЕЗДЕ (2026-07-21, спека владельца)

- COMBO_RADIUS = 1.1 (история: 3.5-центры -> 2.0 -> 1.5 -> 1.1 в истинной
  GJK-метрике). Power chain БОЛЬШЕ НЕ раздаёт «всю чашу» — в цепи радиус
  тоже 1.1; её ценность: досыпка, молнии, ×2 и продление серии. Эндшпильный
  ∞ при <=8 живых СОХРАНЁН (анти-фрустрация, отдельное правило — убрать
  одной строкой в updateMatchRadius, если владелец скажет).
- Space = встряска (90-input, гварды requestShake).

## Рендер: MATCAP ПРИНЯТ (2026-07-20)

- Владелец принял matcap-рендер предметов (прототип ГРАФИКИ): блики
  стабильны по построению, draw calls −43%, теневой пасс выключен.
  CFG.matcap — аварийный откат на MeshStandard+софтбокс. Детали и грабли
  (emissive-патч для Hint, DataTexture, sRGB) — в WORKSTREAMS, блок ГРАФИКА.

## v1-тюнинг 2026-07-20-в: 2-й нерф радиуса серии

- COMBO_RADIUS 2.0 -> 1.5 зазора («в серии всё же слишком большой радиус,
  играть слишком легко»). Лесенка: 0.9 -> 1.02 -> 1.14 -> 1.26 -> 1.38 -> 1.5.
  История нерфов потолка: 3.5 (центры) -> 2.0 (зазор, −25%) -> 1.5 (−45% буста).
  Power chain (вся чаша) не тронут. Бот-контроль экономики обязателен.

## v1-тюнинг 2026-07-20: телеграф цепной реакции (спека владельца)

- «Непонятно, когда начнётся цепная реакция» -> ПОЛОСКА ЗАРЯДА (#chainBar)
  под глазами: видна пока горит серия, заполняется comboCount/10 (зелёная);
  во время Power chain — оранжевая, показывает ОСТАТОК времени (тик каждый
  кадр, tickChainBar в 85-hud). Плюс фон-подогрев: интенсивность зелёного
  низа растёт с длиной серии 0.3->0.8 (вместо фиксированной 0.55).
  В кармане: отсчёт-попы «Chain in 2/1» на 8-9 матче (не реализовано).
- СМЯГЧЕНИЕ (спека владельца, вторая итерация): промах срезает 2 УСПЕШНЫХ
  ШАГА заряда и 2 ступени радиуса, но серию НЕ обнуляет («слишком резко
  сбрасывали power chain»); серию убивает ТОЛЬКО пауза без матчей >
  COMBO_MS = 4 с (спека владельца 2026-07-20; было 2 с).
  Правило «на нуле ступеней серия гаснет» отменено.

## v1-тюнинг 2026-07-19-г: тексты комбо + бесшовный финал облёта

- Тексты комбо БЕЗ ЭМОДЗИ (спека владельца): зажигание — «Combo ×2», через
  0.8 с С ТОГО ЖЕ МЕСТА тем же эффектом «Radius Up»; очки в серии — сразу
  результат умножения («+80», без метки «×2»); цепная реакция — «Power chain!».
- ФИНАЛ ОБЛЁТА БЕЗ СКАЧКА: облёт заканчивается ровно в 2π (≡0); раньше
  кончался на 0.35+2π, а finishIntro ставил 0 — дёргался последний кадр
  (заметно стало после ускорения перехода). Формула: az = 0.35 + e·(2π−0.35).

## v1-тюнинг 2026-07-19-в: таймер-градиент, без баннера, встряска +20%

- ТАЙМЕР МИКСЕРА: чип ВТОРОЙ СТРОКОЙ под левой группой (в один ряд упирался
  в глаза по центру на телефонных ширинах), БЕЗ иконки, подложка плывёт
  hsl(140->0) — зелёная на старте, красная к нулю; при помоле «0 с» красный.
- КРАСНЫЙ БАННЕР УДАЛЁН ПОЛНОСТЬЮ (вместе с «Нет пар — встряхните»):
  коммуникацию несут таймер-чип и глаза (😠 при помоле). #banner-элемента
  больше нет; test.js проверяет чип, не баннер.
- ВСТРЯСКА ×1.2: импульсы 9/5.4+6/спин 7.2, притяжение 7.8, camShake 0.42.

## v1-тюнинг 2026-07-19-б: HUD-перегруппировка + глаза (спека владельца)

- ГЛАЗА МИКСЕРА (#eyes): интерактивный персонаж по центру сверху (из
  дизайн-плана «блендер с глазами»). Эмоции по состоянию (eyesMood в
  85-hud, тик 600мс): 👀 обычные, 😄 комбо, 🤩 цепная реакция, 😠 помол,
  🥱 скука (до миксера <5с), 🥳 победа, 😵 поражение. Тап — 😉 + bounce
  + звук. Пока эмодзи; спрайты персонажа — при работе над визуалом.
- HUD-группы (зазор 12px, .grp): слева-сверху [уровень][время],
  справа-сверху [монеты][★очки]; ⚙️ — левый нижний; [Hint][Shake] —
  правый нижний, высота обеих 54px. mixerTimer/banner опущены под глаза.
- «Прицел» 🎯 скрыт флагом SCOPE_ENABLED=false (как магнит) — код живой.
- Интро-физика ×1.7 (владелец дважды просил «+30%»); облёт по real-time.
- Переход drop->orbit ускорен (спека владельца): min 0.8 с (было 1.3),
  порог скорости 3.5 (было 2.0 — ждал почти-штиля впустую), жёсткий предел
  1.4 с (было 2.2). Куча доседает во время облёта; трим ждёт штиля как раньше.

## v1-тюнинг 2026-07-19: визуальные правки (спека владельца)

- ВИНТ У ДНА: лопасти опущены к самому дну (group y 0.75 -> 0.28) и
  увеличены (размах 1.73 -> 2.13 при дне 2.4, ступица/лопасти толще) —
  владелец: «не видно винт». Верх лопастей ~0.6 < FLOOR_REST 1.15 —
  предметы не задевают. Если захочется показать винт ещё сильнее —
  ручка: поднять FLOOR_REST (шире пустая зона у дна), но это пере-
  проверка заполнения/трима.

- Лихорадка комбо/реакции: низ экрана ЗЕЛЁНЫЙ (был красный) — RGB
  (0.30,0.87,0.50) в шейдере неба.
- Кнопки ПЛОСКИЕ (сплошной цвет, без градиентов/теней) и ПО-АНГЛИЙСКИ
  (Hint/Shake/Next/Retry/Watch/Cancel/Continue/Look around/×2 coins);
  остальные тексты пока русские — полная локализация позже.
- Всплывающие очки (.pop): белые с ЧЁРНОЙ обводкой 2px
  (-webkit-text-stroke + 8-направленная text-shadow; inline-цвет из
  scorePop перебивается !important — параметр цвета в API сохранён).
- Интро: физика заполнения ×1.3 (INTRO_TIME_SCALE, только пока intro);
  камера-облёт по real-time — скорость поворота ПРЕЖНЯЯ.

## v1-тюнинг 2026-07-18-б: МЕТРИКА ЗАЗОРА + нерф комбо (спека владельца)

- РАССТОЯНИЕ ПАРЫ = ЗАЗОР МЕЖДУ ПОВЕРХНОСТЯМИ (pairDist в 60-access:
  центры минус оба охватных радиуса), НЕ между центрами. Причина (вопрос
  владельца): с разбросом размеров ±50% метрика центров наказывала крупные
  пары — их центры далеко даже при касании. Все пороги пересчитаны в зазоры:
  base 0.9, пол MATCH_R_MIN 0.75, комбо-потолок 2.0; слайдер ⚙️ 0.3-2.2.
  Сфера-визуал на тапе рисуется как зазор + свой радиус + 0.55 (типичный
  чужой), кап 4.2. В тестах тупик форсится matchRadius = -9 (зазор ниже ~0
  не бывает — «0.001» в новой метрике матчил бы касающиеся).
- НЕРФ КОМБО −25% («окружность растёт быстро, легко убирать»):
  потолок 3.5(центры)≈2.45(зазор) -> 2.0, ступеней 4 -> 5 — максимум
  теперь на 6-м матче серии, каждая ступень мельче. Бот-проверка: 1-2
  встряски на полном уровне 181 (бюджет 5) — экономика цела.

## v1-тюнинг 2026-07-18 (вторая порция, спека владельца)

- ЧАША ×1.15: FUNNEL R0=2.4, R1=4.1 (H прежняя). Камера дальше: дефолт
  camR 16.2, интро 17.8, зум 9-21. Спавн слоями ПО 8 (чаша шире).
- НАПОЛНЕНИЕ: PAIRS=90 (181 предмет на полном уровне), PAIRS_EARLY=[64,71,78].
  Заполнение: полный уровень top≈7.5 (нижний край красной линии), стены
  держат (excess ~0.1), перф Hard-веера на 181: ~17 мс (бюджет 25).
- РАЗМЕРЫ: ⚠️⚠️ ЧИСЛА ПЕРЕСМОТРЕНЫ 2026-08-15 ПО ЖИВОЙ ИГРЕ ВЛАДЕЛЬЦА
  (дословно: «давай сделаем разброс с 20 уровня и с меньшим процентом. И
  минимальный размер предмета даже к последнему уровню не должен быть меньше
  70% от размера предмета на 1 уровне. Я поиграл и собирать из-за размера
  предметы становится некомфортно»). ⛔ ОТМЕНЯЕТ спеку 2026-07-21 (старт с
  16-го, +4%/ур, потолок ±50%). ДЕЙСТВУЮЩЕЕ: первые 19 уровней ВСЕ предметы
  одного размера; с 20-го рампа ±10%, +2%/уровень, ПОТОЛОК ±30% — то есть
  предмет НИКОГДА не мельче 0.70 и не крупнее 1.30 базового. Замер по уровням:
  19 → ровно 1.0; 20 → 0.90..1.03; 25 → 0.82..1.02; 30 → 0.71..1.15;
  40 и 60 → 0.703..1.29 (пол держится). Заполнение не поехало: живых 181-187,
  верх 7.3-8.7. ⛔ ПОТОЛОК ВЫШЕ 0.30 нарушает прямое требование владельца.
  ⚠️ Страж держит ГРАНИЦУ с обеих сторон (19 одинаковые / 20 разные) И ПОЛ 0.70
  на далёком уровне — прежний ассерт проверял только «на ур.1 все одинаковые»
  и пережил бы возврат старых чисел зелёным.
  (SIZE_SPREAD_* в 00-config, levelSize()
  в 40-items; близнецы пары — один размер; геометрия от размера не зависит —
  кэш по типу).
- МЕТАЛЛОИСКАТЕЛЬ СКРЫТ флагом MAGNET_ENABLED=false (владелец: «непонятна
  помощь на практике») — код и rewarded-плейсмент живые, вернём с внятной
  подачей. Визуал предметов (текстуры) и интерфейс — ПОЗЖЕ, осознанно.

## ⛔ ОТМЕНЕНО (2026-07-21/24) — v1: экономика и первая сессия

> ⛔ РАЗДЕЛ ИСТОРИЧЕСКИЙ. Монетная экономика СКРЫТА флагом COINS_ENABLED=false
> (код жив по решению владельца «не удаляй, скрой»), звёзды больше не
> рейтинговая валюта. Действующая модель — «ЕДИНЫЙ БАЛАНС» ниже.
 (реализовано 2026-07-18)

⚠️ 2026-07-21 (спека владельца): МОНЕТЫ СКРЫТЫ флагом `COINS_ENABLED=false`
(00-config) — награда за прохождение уровня ТОЛЬКО звёзды (система звёзд
как была) + 1 подсказка. Спрятаны: чип 🪙 в HUD, «📺 ×2 coins» на победе,
покупная встряска за 25. НЕ удалено: начисление addCoins на победе живёт
(ce/cs копятся в сейве — при возврате фичи баланс на месте), весь монетный
код и Telemetry сохранены. Абзацы про монеты/магазин ниже читать с этой
поправкой.

- МОНЕТЫ: за победу COIN_BASE=20 + 1 за каждые 500 очков (комбо выгодны).
  Хранение — ПАРА МОНОТОННЫХ счётчиков earned/spent в 77-save (баланс =
  разность): мерж расхождений localStorage/Bridge через max НЕ дюпит валюту
  и не откатывает траты (вердикт аудита плана — наивный max по балансу
  запрещён). Bridge storage синкается после init (bridgeSyncSave из 78-ads).
- МАГАЗИН: доп. встряска 25 🪙 — ТОЛЬКО после исчерпания rewarded-капа
  (монеты не конкурируют с бесплатной рекламой — корректировка аудита);
  «Прицел» 🎯 15 🪙 — подсветить все доступные пары на 5 с (зелёный пульс).
- ЗВЁЗДЫ 1-3★: пар-скор base = (живые пары ПОСЛЕ трима) × 20 (finalizeFill);
  2★ = очки >= base×1.3, 3★ = ×1.7. Скилл в ОЧКАХ, спецусловия típa
  «обязательная цепная реакция» отвергнуты аудитом. Лучший результат
  по-уровнево в сейве; total открывает будущие скины (v1.2).
- ПЕРВАЯ СЕССИЯ: уровни 1-3 укорочены — PAIRS_EARLY=[50,55,60] пар
  (в funnel-game «PAIRS не менять» касалось ПОЛНОГО уровня — укороченные
  первые уровни это осознанное исключение плана v1; чаша заполнена ниже
  красной линии — так и задумано, «легче и быстрее»).
- REWARDED ×4 плейсмента: встряска (было), Continue после поражения
  (1 раз/уровень: +CONTINUE_DROP=10 предметов сверху + 1 встряска,
  loseAdContinue — ⚠️ с 2026-07-27 экран поражения из тупика не всплывает
  (помол-выручалка), этот плейсмент в норме недостижим), ×2 монет на победе (winX2Btn), «Металлоискатель» 🧲
  (1 раз/уровень, подсветка столба над сюрпризом 10 с + маркер).
- INTERSTITIAL: только на «Дальше»/«Заново», не раньше INTER_MIN_WINS=2
  побед сессии, не чаще INTER_GAP_MS=3 мин, только в bridge-режиме
  (в стабе не раздражаем). Каденция при выходе на площадки может стать
  per-platform конфигом (вердикт аудита).
- ТЕЛЕМЕТРИЯ (79-telemetry): каркас sendBeacon-батчей; ВЫКЛЮЧЕНА, пока
  URL='' — включается одной строкой (endpoint Cloudflare Worker владельца).
  События: level_start/win/lose/continue/rw/spend/inter.
- UI: чип 🪙 в топ-баре; кнопки 🎯/🧲 в нижнем баре (⚠️ у кнопок бара
  ОБЯЗАТЕЛЕН pointer-events:auto — .bar глушит события, новая кнопка без
  этого кликается «сквозь» в канвас); экран победы: звёзды + монеты + ×2;
  оверлей встряски: 📺/🪙-кнопки по состоянию капа.
- Отладка: __game.wallet() (coins/ce/cs/stars), __game.grant(n).
- Новые модули: 77-save.js, 79-telemetry.js (порядок склейки важен:
  77 < 78-ads < 79 < 80).

# Миксер — прототип match-pair 3D игры

Казуальная браузерная игра (HTML5, мобайл-first). Стеклянный стакан миксера
заранее заполнен 3D-предметами; игрок разбирает массу сверху вниз группами
одинаковых, миксер на дне давит таймером. Прототип для проверки
геймдизайн-гипотез; арт — стилизованный (пастель, LEGO-пластик).

## Занавес площадки и ТРЕТЬЯ ТОЧКА GAME_READY (2026-07-30, спека владельца)

Жалоба владельца: «пропала анимация заполнения корзины, сразу попадаю на её
разворот». Причина — не графика, а ПОРЯДОК СОБЫТИЙ с площадкой.

- ЗАМЕР ДО ПРАВКИ (стенд `?platform_id=playgama`): игра давала первый кадр на
  1706 мс, предметы сыпались 1706→3200, а непрозрачный сплэш площадки
  (`#loading-overlay`, фон `rgb(36,36,36)`) висел 1778→3947. **Вся анимация
  заполнения играла в закрытый занавес**, игрок видел только хвост облёта.
- ⚠️ МОЙ ПЕРВЫЙ ВЫВОД БЫЛ НЕВЕРЕН и исправлен ИНТЕГРАЦИЕЙ: из «занавес ушёл на
  3947, а GAME_READY послали на 4359» я заключил, что SDK на наш сигнал не
  реагирует. РЕАГИРУЕТ, и мгновенно (узел удаляется СИНХРОННО внутри
  `sendMessage`), просто в том прогоне его опередил собственный автотаймер SDK
  (≈2.1 с после резолва init). Кто первый — тот и снял. Момент снятия мы не
  угадываем, а НАЗНАЧАЕМ.
- ⚠️⚠️ НАЙДЕНО ПОПУТНО И ХУЖЕ ИСХОДНОЙ ЖАЛОБЫ: если `initialize()` НЕ
  РЕЗОЛВИТСЯ, отправить GAME_READY нечем, собственный `.finally` SDK не
  наступает — и занавес висит НАВСЕГДА (замер: 20 с и не собирался уходить).
  Это чёрный экран при любом сбое инициализации площадки. Лечится страховкой
  ИНТЕГРАЦИИ на ПУБЛИЧНОМ `setGameLoadingProgress(100)`; за приватный
  `#loading-overlay` не цепляемся (id недокументирован, отвалится на обновлении).
- РЕШЕНИЕ, ДВЕ ПОЛОВИНЫ:
  (1) ИНТЕГРАЦИЯ: `Ads.curtainGone` — однократный промис, ВСЕГДА резолвится
      (сразу на `file://` и без SDK, по game_ready, страховкой, жёстким
      пределом `CURTAIN_MAX_MS`). Плюс `Ads.curtainWhy` для отладки.
      ⚠️ Предел 12000, а НЕ 8000: при 8000 он ОБГОНЯЛ страховку и обещание
      разрешалось над ещё висящим занавесом — тот же баг, только короче.
  (2) ДИСПЕТЧЕР: у интро появилась ФАЗА `'wait'` перед `'drop'` (99-main).
      В ней физика НЕ ШАГАЕТ — предметы стоят над чашей, их никто не видит.
      На втором тике (первый кадр уже ушёл на экран) зовётся `Ads.gameReady()`
      — это и есть **ТРЕТЬЯ ТОЧКА**: уровень сгенерирован, чаша нарисована,
      предметы ещё не тронулись. По `curtainGone` фаза переходит в `'drop'`.
- ⚠️ ПОЧЕМУ ЭТО НЕ РЕГРЕССИЯ «предметы висят в воздухе»: та была ФОРС-СНОМ по
  чистым часам ПОСРЕДИ падения столба, на глазах игрока и на неопределённое
  время. Здесь пауза ДО первого шага физики, под занавесом, и снимается
  гарантированно.
- ⚠️ ПОЧЕМУ НЕ ПЕРЕНЕСЛИ `genLevel` ЗА ЗАНАВЕС: `loop` и весь HUD читают
  `level`, до `genLevel` его нет — гейтить пришлось бы десяток мест.
- ⚠️ СТАРЫЙ ЗАПРЕТ В СИЛЕ: GAME_READY НЕЛЬЗЯ слать из `Ads.init()` — там ещё
  нет ни genLevel, ни декодированных атласов, площадка сняла бы лоадер над
  ЧЁРНЫМ экраном. Комментарий в 78-ads ДОПОЛНЕН, а не заменён.
- СТРАЖ В СЬЮТЕ ПЕРЕПИСАН: прежний снимал флаг ПОСЛЕ того, как игра давно
  рисует, и не различал «отправили над чёрным экраном» от «отправили над
  нарисованной чашей». Теперь мок фиксирует ОБСТАНОВКУ в момент отправки
  (кадров нарисовано, предметов создано) — ассерт падает, если вызов вернуть
  в `Ads.init()` (там `alive()===0`). 266 PASS.
- ЗАМЕР ПОСЛЕ ПРАВКИ: занавес убран 3426 → засыпание началось 3552 (через
  126 мс) → интро кончилось 7168. ⚠️ Проверять ТОЛЬКО через `/index.html?...`:
  превью-сервер отдаёт 404 на query-строку у корня (`/?platform_id=...`).

## Правила игры (актуальные, утверждены владельцем)

- ИНТРО уровня (по мокапу владельца): старт с вида СБОКУ, чаша пустая
  (на дно первым ложится сюрприз — золотая рыбка), предметы слоями сыплются
  сверху ЖИВОЙ физикой (~2 с), затем 2-секундный облёт камеры вокруг чаши
  с плавным переходом на игровой вид сверху. Ввод, миксер и HUD-таймер
  на время интро заблокированы; отсчёт партии — от конца интро.
  Тесты обходят интро через __game.skipIntro() (синхронная осадка).
  ВЕСЬ ввод в интро глушится НА ВХОДЕ (pointerdown тоже), а на границах
  интро жесты сбрасываются (resetPointers) — зажатый в интро палец не
  должен превращаться в драг со старой базой камеры (был баг).
  На время интро терминальная скорость падения снижена до 11 (setFallCap;
  боевая 16): столб с 30+ юнитов на v=16-18 пробивал стены — 3-4 спасения
  за интро на глазах игрока. ТРИМ и база радиуса topY0 считаются НЕ в
  finishIntro, а ОТЛОЖЕННО (pendingTrim -> finalizeFill при штиле): трим
  по ещё летящей куче тихо удалял до 16 предметов, а topY0 по ней ломал
  динамический радиус. После трима куча будится (wakePhysics('trim')) —
  изъятие близнецов из глубины оставляет полости, масса должна доосесть.
- Заполнение — до «красной линии» владельца: topY 7.5-9.0 при кромке 9.2.
  Конвейер: спавн слоями по 6 (шаг 1.35 — глубокие стартовые перекрытия
  взрывали столб), вибро-утряска ВСЕЙ массы (арки-мосты в конусе держат
  кучу рыхлой; трясти только верхние бесполезно), затем trimOverfill —
  всё, что торчит выше линии, ТИХО изымается ПАРАМИ (чётность цела).
  Сюрприз на время осадки ПРИБИТ ко дну (fixed-тело): вибрация выталкивала
  его наверх (эффект бразильского ореха); отпускается в finishIntro.
  Верхние стены над кромкой скользкие (friction 0.02) и наклонены внутрь —
  на них и их стыках ничего не может устоять.
- Доступность физическая и КАМЕРОНЕЗАВИСИМАЯ — ВЕЕР ЛУЧЕЙ «К НЕБУ»:
  предмет доступен, если хоть одна точка его физформы видит небо в конусе
  ~34° (вертикаль + 6 наклонных направлений; Rapier castRay из сэмплов
  buildAccessSamples — точек СТРОГО ВНУТРИ коллайдеров, своё тело
  исключается из каста). Стены чаши лучи честно блокируют. Перекрытые —
  не нажимаются, показаны серой вуалью; вуаль накладывается ПЛАВНО
  (tickVeil, лерп ~0.25 с) — мгновенная смена цвета всей кучи читалась
  как «цвета скачут». ⚠️ ИСТОРИЯ ТРЁХ багов владельца (НЕ возвращать):
  1) луч в ЦЕНТР предмета пролетал сквозь дырку тора — бублик матчился
  с одного ракурса и не матчился с другого; 2) лучи ОТ КАМЕРЫ меняли
  вуаль при вращении — «предметы тускнеют в зависимости от ракурса»;
  3) ЧИСТО ВЕРТИКАЛЬНЫЕ лучи хоронили визуально открытые предметы под
  нависшим соседом («два открытых додекаэдра рядом не матчились» — над
  одним нависал конус). Перф: three-raycast по мешам 86-106 мс ->
  Rapier-вертикаль 1.4 мс -> веер 7 лучей ~11 мс (бюджет 25; аудит
  подтвердил камеронезависимость бит-в-бит на 4 ракурсах и топ-предмет
  доступен 10/10 сидов; доля доступных на полной чаше 21-27%). В штиле
  (physAwake=false) refresh не тикает вовсе — куча неподвижна; финальный
  срез делает sleepPhysics. Тап остаётся рейкастом от камеры (это выбор
  предмета пальцем, не доступность).
- СЛОЖНОСТЬ (спека владельца 2026-07): по умолчанию (Easy) доступна ЛЮБАЯ
  пара — перекрытия не проверяются, вуали нет, лучи доступности не бьются
  вовсе (бонус к перфу). Тумблер «Сложность Hard (перекрытия)» в ⚙️ включает
  механику веера лучей + вуаль; выбор живёт в localStorage `mixer_hard`.
  Гейт — внутри isAccessible (60-access), поэтому покрывает и тапы, и
  подсказку, и счётчики. ⚠️ СЮРПРИЗ всегда «по-честному» перекрыт в ОБОИХ
  режимах — иначе +150 снимался бы тапом с первой секунды.
- Групповой матч ПО ТИПУ (размер не важен): тап по доступному предмету
  удаляет ВСЕ одинаковые по типу доступные предметы в сфере радиуса
  совпадения (мин. 2, можно нечётным числом). Удаление — расщепление
  «В ТРУХУ» (dissolveFX: 1280 частиц ТРЕМЯ фракциями — мука 640×0.0225 +
  крошка 400×0.035 + обломки 240×0.05, вершинный разброс оттенков ±0.22 L;
  история: 70 крупных -> 320 мелких -> 640 разнообразных -> 1280 «вдвое
  мельче, вдвое больше» (2026-07-22), всё по просьбам владельца; труха
  общая для матча и помола). Затягивание в лопасти: предмет тонет с
  вращением и УМЕНЬШАЕТСЯ (scale ×(1−k·0.9) в mixerGrind/finaleGrind —
  это осознанная анимация «уходит под ножи»), в конце — bladeDustFX,
  пылевой взрыв радиально из-под лопастей. Диагностический маркер скрытой
  пары — мягкая пульсирующая френель-сфера (fresnelGhostMat), НЕ wireframe
  (владелец принял сетку за артефакт). Очки: `10·N·(N−1)`, при N>2
  всплывает множитель ×(N−1).
- Радиус совпадения ДИНАМИЧЕСКИЙ (спека владельца): стартует с
  CFG.baseRadius (0.9 истинного зазора; слайдер в ⚙️ регулирует базу) и сжимается по мере
  оседания кучи пропорционально radiusAt(верх кучи)/radiusAt(старт);
  пол MATCH_R_MIN (v1: 0.75 ЗАЗОРА — см. раздел «метрика зазора»; updateMatchRadius в 60-access, тик 300 мс).
  ЭКОНОМИКА СОШЛАСЬ (аудит 2026-07): после веера лучей (вылечил
  захоронения — 0 встрясок до ~63-го матча) и эндшпильного радиуса боту
  нужно 2-3 встряски при бюджете 3+2=5 (история: 14-27 у вертикальных
  лучей, 5-7 у веера без эндшпильной ручки). «Слишком легко» не стало:
  доля доступных на старте ~21-26%, копание и миксер-угроза работают.
  ЭНДШПИЛЬ: при <=8 живых предметах (без сюрприза) радиусная проверка
  СНИМАЕТСЯ (matchRadius=99, HUD показывает ∞, сфера-эффект рисуется не
  больше чаши 3.6): аудит ботом показал, что 100% встрясок эндшпиля были
  из-за расстояния на дне (доступная пара есть, но дальше зажатого в пол
  1.8), ни одной из-за захоронения.
  КОМБО-БУСТ (спека владельца 2026-07; актуальные числа — 00-config):
  группа 3+ одним тапом ИЛИ вторая склейка в пределах COMBO_CHAIN_MS=1.5 с
  зажигают серию (ОЧКИ ×2 сразу), а радиус растёт ЛЕСЕНКОЙ: +1 ступень из
  COMBO_STEPS=5 за каждый матч серии, потолок зазора COMBO_RADIUS=1.1
  (история нерфов: 3.5-центры -> 2.0-зазор -> 1.5 -> 1.1, все по «слишком
  легко»; мгновенный максимум владелец забраковал). Лесенка и цепь двигают
  радиус только ВВЕРХ от динамического (ручной слайдер выше потолка не
  срезается). ПРОМАХ сбивает COMBO_MISS_DROP=2 ступени радиуса и 2 шага
  заряда цепи, но серию НЕ гасит (смягчение 2026-07-20); серию убивает
  ТОЛЬКО пауза без матчей > COMBO_MS=4 с. Каждый матч в «лихорадке»
  ПРОДЛЕВАЕТ окно заново (гаснущий посреди цепочки фивер злит). Первый матч
  цепочки — по обычной цене; ×2 идёт с матча, зажёгшего серию. Всплывашка
  очков в серии оранжевая с меткой ×2.
  Активация озвучена: Sound 'combo' (глиссандо вверх + искорка, старт +60 мс
  чтобы не маскировать «буль» матча) + вибро-паттерн [20,40,30].
  ФОН-ЛИХОРАДКА: во время комбо низ ЭКРАНА наливается красным с переходом
  в белый кверху (uCombo/uResY в шейдере неба, лерп 0.35 с; 0.55 — комбо,
  1.0 — цепная реакция). ⚠️ Градиент ЭКРАННЫЙ (gl_FragCoord.y/uResY, uResY
  обновляет resize) — мировой по vDir.y из камеры сверху заливал всё красным.
  ЦЕПНАЯ РЕАКЦИЯ (спека владельца): серия из CHAIN_COMBO_AT=10 матчей ->
  на CHAIN_MS=10 с радиус к потолку 1.1 («вся чаша» отменена спекой
  2026-07-21; эндшпильный ∞ при <=8 живых ПРИОРИТЕТНЕЕ цепи) + досыпка
  CHAIN_DROP_N=2.6 СЛУЧАЙНЫХ предметов (НЕ парами — сироты легальны; дробь
  копится аккумулятором chainCarry) раз в CHAIN_DROP_MS=417 мс живым
  падением (спека 2026-07-21: «в турбо сыпаться на 20% быстрее, количество
  на 30% больше»). СЕРИЯ ТУРБО (спека 2026-07-21): второе турбо, собранное
  ВНУТРИ активного (comboCount снова до 10), ПЕРЕЗАПУСКАЕТ окно цепи и
  инкрементит chainSeries (поп «Power chain ×N!»); chainSeries>=2 — сигнал
  интерфейсу для глаз eyes-5. Старое «зажечь следующую сразу после» этим
  заменено (гейт !chainUntil снят — вопрос ревью закрыт решением владельца). Досыпка — РЕГУЛЯТОР полноты: заполненность меряется по
  куче НИЖЕ кромки (летящие свежесыпанные не блокируют следующий тик —
  душили темп), лимиты: 141 живых, top>H-1 «чаша полна», <=8 в воздухе.
  Итог: чаша держится у красной линии весь режим. МОЛНИИ: при матче —
  разряды от тапнутого к каждому предмету группы; амбиентный треск — БОЛЬШЕ
  МЕЛКИХ (спека владельца 2026-07-28): BOLT_PER_TICK=2 разряда раз в 0.13-0.24 с
  между верхними предметами в радиусе BOLT_MAX_D=4.2 (было: одна дуга раз в
  0.2-0.36 с на 5.5). Сам разряд (boltFX в 70-fx) = основная дуга + BOLT_FORKS=5
  коротких ОТВЕТВЛЕНИЙ, всё тоньше прежнего: зигзаг-ТРУБКИ TubeGeometry
  с мерцанием, ядро+оболочка; ⚠️ НЕ линии — WebGL рисует их
  1px, было еле видно; ⚠️ НЕ additive — на белом фоне свечение невидимо.
  ⚠️ ВСЕ ФИЛАМЕНТЫ СЛОЯ СЛИТЫ В ОДНУ ГЕОМЕТРИЮ (mergeTubeGeos — руками, в UMD
  r149 BufferGeometryUtils НЕТ; копируется только position+index, у
  MeshBasicMaterial normal/uv не читаются): на разряд по-прежнему 2 объекта /
  2 материала / 2 draw call, густота досталась даром. Материалы — free-list
  (boltMat + флаг userData.poolBolt, stepFX кладёт в пул вместо dispose);
  ⚠️ шарить ОДИН материал между живыми молниями нельзя — opacity мерцает
  индивидуально. ⚠️ Оболочка:ядро ~3:1 (0.075/0.024), а НЕ 2.3:1 как у толстой
  версии: при утоньшении синий ореол первым уходит в субпиксель, и филаменты
  выходят БЕЛЫМИ вместо электрических (поймано крупным планом).
  Дебаг: `__game.boltProbe(ms)` — сыпать разряды непрерывно (жизнь 0.16 с,
  случайный скрин их не ловит). Гаснет: таймер / CHAIN_MISSES=2
  промаха (по stats.misses от старта — без связывания с penalize) / финал
  или конец уровня (досыпать пары в финал миксера НЕЛЬЗЯ — он бы прервался).
  Счётчик серии comboCount копится и во время реакции — быстрый игрок может
  зажечь следующую сразу (лимитирует заполнение чаши). Поп «☄️ ЦЕПНАЯ
  РЕАКЦИЯ!», Sound 'chain', вибро [30,50,30,50,60]. Отладка: __game.combo(). Телеграф: поп «Combo ×2» при активации + ореол-призрак
  на тапе сам большой. Буст только ВВЕРХ от динамического (лесенкой до 1.1) —
  эндшпильный ∞ не трогает; гаснет и на спящей куче (проверка в loop —
  refresh в штиле не тикает, а тап читает CFG.matchRadius напрямую).
  Константы — 00-config (COMBO_RADIUS 1.1, COMBO_MS 4000, COMBO_CHAIN_MS
  1500); состояние comboUntil/lastMatchMs (60-access), сброс в genLevel.
  В тестах форсить тупик через cfg.baseRadius = -9 (matchRadius
  перезаписывается; ⚠️ метрика v3: малый ПОЛОЖИТЕЛЬНЫЙ радиус вроде 0.001
  матчит касающиеся предметы — тупика не будет).
- «ЗАРЯД ТИПА» (спека владельца 2026-07-31, три сообщения одной сессии):
  на зажигании Power chain может выпасть заряд случайного типа (из живых с
  >= CHARGE_MIN_COPIES=6 копий), 1/уровень, слот у кнопки подсказки с
  портретом. ЖИВЁТ <= 7 c (CHARGE_TTL_MS) и растворяется — «важно сразу
  активировать, а не оставить на потом»; потому состояние ЧИСТО РАНТАЙМОВОЕ
  (chargeName/chargeUntil в 80-gameplay), сейв-поля НЕТ И НЕ НАДО (первая
  версия с Save.oc жила меньше часа и снята поправкой про TTL — не возвращать).
  Клик = detonateCharge: снимает ВСЕХ живых типа ВКЛЮЧАЯ недоступные (сила —
  раскопка без встряски). Это СПАСЕНИЕ (решение владельца): accAdd копит все n,
  музей/вехи честны, миксер злится (faceEvent angry). Очки: формула группы с
  капом MATCH_MAX_N, ×множитель типа, БЕЗ комбо-×2 (иначе +25% дохода уровня
  одним кликом). comboCount заряд НЕ копит, окно серии продлевает (действие).
  Стражи: снятие всех/кап цены/спасение/TTL/выпадение из честной цепи.
- ТУРБО УЖЕСТОЧЕНО (спеки владельца 2026-07-31): (1) вход ДОРОЖАЕТ с уровнями —
  chainComboAt() = 10 + ⌊ур/CHAIN_AT_STEP=8⌋, кап CHAIN_AT_CAP=14 (числа —
  первая итерация диспетчера под «аккуратно», тюнить по плейтесту; потребители
  зовут ФУНКЦИЮ, не константу); (2) ОДИН промах гасит цепь (CHAIN_MISSES_*=1,
  отменяет 4/3 по сложности); (3) досыпка укладывается в 3 СЕКУНДЫ: окно
  CHAIN_DROP_WINDOW_MS=3000 от старта цепи, тик 417→125 мс — тиков столько же
  (24), СУММА ПРЕЖНЯЯ, высыпается втрое плотнее. Старт цепи восстанавливается
  из chainUntil − CHAIN_MS (паузо-сдвиги двигают его сами).
- Сфера радиуса при тапе: белая — матч состоялся, красная — промах.
- Ошибка (пустой тап / перекрытый / нет пары в сфере): −7 очков (1/3 пары).
- Миксер-наказание: простой > лимита (матч/встряска сбрасывают; лимит по
  СЛОЖНОСТИ: **Easy 15 с, Hard 10 с** — перетюн батча владельца 2026-07-27
  (`304fdf0`, «3.3 на Hard нервно»); ⛔ прежние «10/3.3» из спеки 2026-07-24
  здесь простояли ПРОТУХШИМИ две недели и чуть не попали в разбор бага паузы
  как действующие; рампа по уровням убрана;
  переключение тумблера обновляет и текущий уровень) — раз в 2 с
  нижний предмет + его пара уходят в лопасти, −20 очков. Таймер-чип «🌀 N с»
  виден в ожидании (≤5 с краснеет), а ПРИ РАБОТЕ ВИНТОВ прячется — остаётся
  только нижняя плашка-баннер (спека владельца: две красные плашки дублировались).
- ТУПИК → ПОМОЛ-ВЫРУЧАЛКА, НЕ ПОРАЖЕНИЕ (спека владельца 2026-07-27 «помол =
  штраф, не смерть»): когда нет ДОСТИЖИМЫХ пар (availablePairs==0, но пары есть)
  И нет встрясок (shakes==0 && adShakes==0; слово владельца 2026-08-01: встряска ЗА РЕКЛАМУ — тоже агентность, при adShakes>0 тупик не объявляется — с безлимитной рекламой ветка фактически резервная для площадок без rewarded; кучу при простое всё равно разбирает idle-помол), после ~1.2 с штиля ставится
  `level.deadlock` → кадровый gate гонит mixerGrind, разбирая кучу, пока не
  появится достижимая пара (флаг снимается при ap>0 / возврате встрясок). Помол
  всегда сходится: при <2 любого типа включается финал-зачистка → победа. Цена
  выручки — очки (−20/помол), они же = лидерборд. showLose() из тупика БОЛЬШЕ НЕ
  вызывается (единственный был этот путь) → в норме уровень НЕПРОИГРЫВАЕМ; UI
  поражения (loseOverlay) жив, но недостижим геймплеем. ⚠️ СЛЕДСТВИЕ: плейсмент
  «Continue за рекламу» (loseAdContinue) в норме больше не всплывает.
- Миксер-финал: когда пар по типам не осталось вовсе — миксер зачищает
  остатки по одному раз в 0.5 с до победы, ОЧКИ НЕ ТРАТЯТСЯ И НЕ
  НАЧИСЛЯЮТСЯ (спека владельца 2026-07); тапы игрока в финале тоже БЕЗ
  штрафа −7 (finale-гвард в handleTap). Исключение: тап/автосбор
  раскопанного СЮРПРИЗА даёт свой законный +150. Сироты от нечётных групп
  легальны. Экран поражения в этом режиме не срабатывает.
- Лопасти СТОЯТ, пока миксер не работает (холостое вращение нервировало
  владельца); при перемалывании разгоняются до 14 рад/с с лерпом, а нижние
  слои массы мелко ВИБРИРУЮТ (импульсы в loop) — «миксер трясёт».
- Сон физики: в покое интегратор ВЫКЛЮЧЕН (physAwake, 99-main) — предметы
  лежат абсолютно неподвижно; вечная микродрожь плотной кучи нервировала
  владельца. Будится на матч/встряску/перемалывание/сюрприз; засыпает при
  штиле (maxV<0.25 держится 0.4 с) ИЛИ форс-сном через 3 с бодрствования,
  НО форс-сон гейтован: ТОЛЬКО при maxV<2 (почти-штиль, докатывание),
  НЕ в интро и не при анимациях. ⚠️ РЕГРЕССИЯ 2026-07 (не возвращать
  форс-сон по чистым часам!): он замораживал столб, падающий на v≈17-19,
  посреди интро — «предметы зависают в воздухе», «цвета скачут» (вуаль по
  дёргающейся куче), трим резал замороженный столб (тихо -16 предметов),
  на слабых машинах в разы хуже. wakeAtMs сбрасывается в finishIntro
  (свежий 3-сек бюджет после интро). Если sleepPhysics-спасатель кого-то
  телепортировал — сон ОТМЕНЯЕТСЯ (спасённый доседает). Диагностика:
  __game.psLog() — журнал сна/пробуждений; сон при v>2.5 = баг.
- Встряска: мгновенная, без подтверждения; 3 бесплатных + до 2 за rewarded
  (через адаптер `Ads`, сейчас заглушка 3 с). К КОНЦУ УРОВНЯ встряска
  ПРИТЯГИВАЕТ пары (спека владельца: «иначе последние пары не совместить»):
  доля притяжения к ближайшему близнецу растёт с опустошением чаши
  (>=40 живых — чистое рыхление, <=12 — почти чистое притяжение;
  performShake, pullK). Сюрприз притяжением не трогается.
  ⚠️ ТУПИК → ПОМОЛ-ВЫРУЧАЛКА, НЕ ПОРАЖЕНИЕ (решение владельца 2026-07-27
  «помол = штраф, не смерть», v1-test-111): пары есть, но недоступны и
  встрясок нет → level.deadlock → помол разбирает кучу, пока не появится
  достижимая пара. Цена — очки (−20/помол, под бустером ×множитель).
  Экран поражения (showLose) из тупика БОЛЬШЕ НЕ ВЫЗЫВАЕТСЯ — уровень в
  норме непроигрываем; UI поражения жив, но геймплеем недостижим (прежний
  текст «→ экран поражения с Оглядеться» ОТМЕНЁН).
- Камера: драг — орбита (phi 0.32–1.35, максимум — вид сбоку на миксер),
  пинч/колесо — зум (camR 9–21). ВЕРТИКАЛЬНЫЙ ПАН ВЗГЛЯДА (спека владельца
  2026-07-21, «рассмотреть остатки»; ФИНАЛЬНАЯ правка «не поднимай ведро в
  начале уровня — иначе оно плавает, это неудобно»): ОДИН ШАГ В ЭНДШПИЛЕ —
  весь уровень камера СТОИТ на 4.2; когда живых <= 20% стартовой загрузки
  (CAM_FOLLOW_FRAC, aliveN0 из finalizeFill) — защёлка level.camFollowOn,
  target один раз плавно съезжает к AUTO_FOLLOW_MIN 3.2 (треть хода) и
  больше не двигается (досыпки цепи защёлку не снимают — обратного
  плавания нет). Глубже (до 1.2, остатки в центре кадра) — только ручными
  жестами (центр щипка / правый драг / Shift+колесо, target.y 1.2–5.2),
  они перебивают автоматику на 4 с; ручное положение до защёлки автоматика
  не трогает. Сброс на границах интро (resetPointers). СТЕКЛО РАСТВОРЯЕТСЯ при приближении
  (спека владельца: вблизи чаша мешает совмещать): полная плотность при
  camR>=13.5, полностью тает к camR<=10 (smoothstep по bowlMat.opacity
  в loop; bowlMesh.visible гасится — draw call экономится).
- Прогрессия уровней (`levelNum`, живёт в localStorage `mixer_level`,
  ап при победе): типов 9+уровень (потолок `TYPES.length`; типы открываются
  по порядку массива — актуальные числа в разделе «СОСТОЯНИЕ ОБЪЕКТОВ» вверху,
  на 2026-07-31 это 120 типов и пончик с ур.110);
  терпение миксера — НЕ по уровню, а по сложности (Easy 10 / Hard 3.3);
  радиус в рампе НЕ участвует — он динамический (см. выше).
  ГЛАВНЫЙ рычаг сложности — число типов (замер ботом: тупики зависят
  от него, радиус влияет слабо). PAIRS не менять — заполнение стакана
  завязано на объём.
- Сюрприз («археология» из концепции): золотая РЫБКА на дне (модель
  animalfish, фолбэк — процедурный кристалл; makeSurprise в 40-items), не
  матчится, не вуалится (светится сквозь щели, emissive; материал
  MeshStandard даже в matcap-режиме — настоящий золотой блеск выделяет клад),
  миксер-наказание её не ест. Тап по раскопанной — +150; в финале собирается
  автоматически с бонусом. Из-за неё в стакане на один предмет больше, чем
  пары×2. ⚠️ НО СЧЁТ УРОВНЯ НЕ РАВЕН «пары×2 + 1»: сверху ещё бомба (всегда) и
  камни (с ур.16) — замеренные числа в разделе «СОСТОЯНИЕ ОБЪЕКТОВ» вверху.
  Плотность золота 5 — она тяжёлая.
- Подсказка: зелёная кнопка «💡 Подсказка» в нижнем баре — подсвечивает
  ЛУЧШУЮ доступную группу (максимум одинаковых в радиусе): жёлтая
  сфера-призрак радиуса + пульс emissive на предметах группы (2.2 с).
  Бесплатная, без лимита, миксер-таймер НЕ сбрасывает; экономика
  (заряды/реклама как у встряски) — открытый вопрос. Уровень в HUD
  переехал в верхний чип («Ур.N · M») — внизу место под кнопку.
- Звук: процедурный WebAudio (`Sound`, 75-audio) — матч (арпеджио по
  размеру группы), промах, встряска, перемалывание, сюрприз, победа,
  поражение; вибрация на Android. Разблокировка по первому жесту (iOS).
  Тумблер «Звук» в ⚙️-панели (CFG.sound).

## Структура

- `index.html` — собранный автономный файл (three.js + все модули инлайн),
  открывается двойным кликом, работает офлайн. НЕ редактировать руками.
- `playgama-bridge.js` + `playgama-bridge-config.json` — Playgama Bridge SDK
  (не инлайнится: LGPL + грузится только на http/https). ПАКЕТ ДЛЯ ПОРТАЛА =
  эти два файла + index.html, одной папкой/зипом.
- `src/shell.html` — HTML/CSS/UI-каркас с плейсхолдерами `/*THREE_JS_INLINE*/`
  и `/*APP_JS_INLINE*/`; WebGL-fallback экран.
- `src/app/*.js` — модули игры, склеиваются по номерам в ОДИН IIFE-скрипт
  (общая область видимости, это не ES-модули; порядок = порядок объявления
  top-level const/let, функции хойстятся):
  - `00-config.js` — все константы тюнинга (геометрия стакана, очки, миксер).
  - `10-stage.js` — рендерер, камера, свет, софтбокс→PMREM, matcap-фабрика
    (makeMatcap/matcapSpecPatch, тонировка по глубине кучи), небо-ГРАДИЕНТ
    по времени суток (skyTimeNow + SKY_STOPS из 00-config, многостопная рампа
    buildSkyRamp; ⚠️ SKY_GRAD с тремя опорами БОЛЬШЕ НЕТ — заменён 2026-07-31
    палитрами владельца, день 7 стопов / ночь 12). ⚠️ Модуля
    `05-sky.js` (base64-панорамы) БОЛЬШЕ НЕТ — удалён 2026-07-30 по спеке
    владельца «убери в десктопе на фоне картинку, сделай как в мобиле».
    ⛔⛔ **ИСХОДНИКОВ «3d assets/skyboxes» БОЛЬШЕ НЕТ (удалены 2026-08-17 по
    слову владельца «вычищай»), и обещание «регенерится одной командой» ОТМЕНЕНО.**
    `tools/sky2module.js` цел, но кормить его нечем: захочет владелец картинку
    обратно — сперва нужны новые исходники неба. Прежний текст оставлен бы
    ложным приказом, поэтому переписан, а не дополнен.
  - `20-arena.js` — лопасти миксера (чаша невидима — физика в 50), radiusAt.
  - `30-shapes.js` — фабрики геометрий, TYPES (состав и число — в разделе
    «СОСТОЯНИЕ ОБЪЕКТОВ» вверху; процедурные удалены из пула, фабрики живы),
    палитра candyColor.
  - ⛔ `35-steak.js` УДАЛЁН (v187, слово владельца «удали совсем») — не
    искать файл; история и рецепт возврата в шапке этого документа.
  - `36-models.js` — партия моделей «3d assets» (24 зверя с палитровым
    атласом, еда, машины) — ГЕНЕРЁННЫЙ файл (tools/glb2module.py), руками
    не править.
  - `40-items.js` — makeItem (материалы предметов), makeSurprise, genLevel
    (прогрессия уровня), levelNum.
  - `50-physics.js` — обёртка Rapier: мир, контейнер-панели, временная
    стена осадки, createItemBody (формы/плотности), buildAccessSamples
    (сэмплы доступности из физформ), bodyToItem, syncMeshes, степпер,
    sleep/wake/impulse-хелперы.
  - `60-access.js` — isAccessible (вертикальные Rapier-лучи)/
    refreshAccessibility/availablePairs/hasAnyPair.
  - `70-fx.js` — addFX/stepFX, sphereFX, dissolveFX, popFX, boltFX (молнии
    цепи), scorePop, penalize, якоря шейдерных программ (fxProgramAnchors).
  - `74-sfx-data.js` — base64 аудио-сэмплы (m4a 32кГц; ogg Safari не декодит).
  - `75-audio.js` — процедурный звук (Sound) + сэмплы (playBuf) + vibrate.
  - `78-ads.js` — rewarded-реклама: Playgama Bridge (github.com/playgama/bridge)
    с фолбэком-заглушкой. На http/https динамически грузит лежащий рядом
    `playgama-bridge.js` (релиз v2.0.2, LGPL — потому НЕ инлайнится в
    index.html), `bridge.initialize()` читает `playgama-bridge-config.json`;
    если платформа поддерживает rewarded (`isRewardedSupported`) — режим
    'bridge': награда строго по состоянию REWARDED, CLOSED до награды = без
    награды, FAILED = тост; во время рекламы lastAction обновляется (миксер
    не ест предметы). На file:// и на mock-платформе (localhost) — режим
    'stub' (3-сек заглушка). После init шлётся PLATFORM_MESSAGE.GAME_READY.
    Проверено: http://localhost → bridge грузится, платформа 'mock',
    rewarded false → stub; file:// → SDK не грузится → stub; на портале
    платформа будет 'playgama' → bridge.
  - `80-gameplay.js` — doMatch, handleTap, collectSurprise,
    mixerGrind/finaleGrind, встряска, checkEnd/showLose.
  - `85-hud.js` — $, show/hide, toast, updateHUD.
  - `90-input.js` — поинтеры (тап/драг/пинч), колесо, кнопки UI.
  - `99-main.js` — главный цикл, планировщик миксера, баннеры/таймер,
    `window.__game`, старт.
- `src/vendor/rapier.js` — IIFE-бандл @dimforge/rapier3d-compat
  (window.RAPIER, wasm внутри base64); команда пересборки — в шапке build.py.
- `build.py` — сборка: `npm i three@0.149.0 @dimforge/rapier3d-compat esbuild`,
  затем `python3 build.py` (инлайнит three + rapier + модули; ~7.3 МБ raw /
  ~1.7 МБ gzip — лимиты порталов 8-50 МБ держим с запасом).
- `test.js` — headless-прогон Playwright (`npx playwright install chromium`,
  `node test.js`): полное прохождение, мгновенная встряска, тупик/поражение,
  заполнение (topY), очки/промахи, миксер-наказание, финальная зачистка,
  эндшпиль-∞, комбо-лесенка, подсказка, пауза, отмена награды при регене.
  С 2026-07-21 сьют АССЕРТНЫЙ: ожидания через expect(), любой FAIL (или
  ошибка страницы) даёт exitCode 1 — «зелёный» снова что-то значит.
- `soak.js` — соак длинной сессии (`node soak.js --minutes=15 --seed=101
  --hard=0|1 --idle=0.25 --out=…jsonl`): автобот играет реальным временем
  через ЖИВЫЕ интро, фазы «серии матчей (комбо/цепь)»/«простой под
  помол»/встряски; каждые 5 с — JSONL-срез диагностики и вердикт по
  инвариантам (сон при v>2.5, спящие висуны без контактов, wallExcess>0.20 —
  порог переставлен с 0.18 по распределению 8856 сэмплов, см. раздел физики;
  провал в пол; NaN, тренд кучи после GC). Прогон 2026-07-20 (6×15 мин,
  Easy/Hard/Hard-простои): чисто, утечек нет, шаг физики p95<=14 мс на 181.
- Превью-сервер этой папки: launch.json конфиг `Main`, порт 8779
  

## Рендер и стиль (эталоны владельца)

- **three.js r149 UMD**. НЕ поднимать выше r160 (UMD-сборку удалили;
  с r152 сломаются outputEncoding/sRGBEncoding). Дальше апгрейд = переход
  на ES-модули + бандлер, это отдельное решение.
- **Свет** (эталон webgl_batch_lod_bvh + webgl_loader_ldraw): софтбокс-
  окружение (10-stage) через PMREMGenerator как scene.environment; ACES,
  exposure 0.8; направленный свет 0.55 ТОЛЬКО ради теней и рельефа.
- **Перф-меры аудита 2026-07** (не откатывать без замеров): pixelRatio
  cap 1.5 на тач-устройствах (DPR2 дороже DPR1 в ~1.8 раза; HUD — DOM,
  остаётся резким; десктоп — cap 2); тени shadowMap.autoUpdate=false,
  needsUpdate ставится в loop только когда physAwake/интро/миксер/эффекты
  (свет статичен — в штиле ~150 теневых draw calls шли впустую). Итог всех
  мер (стекло+тени+гейты): headless-кадр 16 -> 78 FPS. Резерв на будущее,
  если не хватит на слабых телефонах: InstancedMesh по (тип,размер) —
  дорого по рефакторингу, делать только по реальным замерам с устройств.
- **СВЕТ ОДИН НА ИГРУ + значения владельца из панели (2026-08-04).** Владелец
  сам крутил `?matcap=1` и прислал панель скриншотом («забери эти параметры
  matcap»): `MATCAP_LIGHT.x −0.36 → 0` (свет сверху-СЛЕВА-спереди стал
  сверху-ПО ЦЕНТРУ-спереди) и `DEPTH_TINT_MIN 0.65 → 0.89` (00-config).
  ⚠️ ВТОРОЕ ЧИСЛО — И ЕСТЬ ЕГО «объекты чуть светлее»: он не тронул ни gain,
  ни contrast, а поднял ДНО тонировки по глубине. Значит темновато ощущался
  НИЗ КУЧИ, а не картинка целиком, и общая яркость была бы неверным лечением;
  прежнее «нижние совсем тёмные» этим ОСЛАБЛЕНО, а не отменено.
  ⚠️⚠️ `MATCAP_LIGHT` (10-stage) — ЕДИНСТВЕННЫЙ ИСТОЧНИК направления света.
  Осколки (`SHARD_LIGHT` в 70-fx) держат ПРОИЗВОДНУЮ и пересчитывают её на
  каждый скол (`syncShardLight()` в makeShardGeo) — снимок на старте отстал бы
  от живого ползунка. До этой правки констант было ДВЕ, равными их держали
  руками, и правка через панель (она правит только MATCAP_LIGHT) развела бы
  освещение надвое: куча по новому свету, её обломки по старому, причём в кадре
  взрыва они рядом. Страж — секция сьюта у тюнера, ходит ЧЕЛОВЕЧЕСКИМ путём
  (ползунком) и читает `__game.shardLight()` ПОСЛЕ `shardBurst` (то есть свет,
  которым скол уже запечён). Диверсии: «две константы» роняет все три ассерта,
  «снимок на старте» — два про живое следование.
- **Материалы v4 + окружение-СОФТБОКС** (цикл по жалобе владельца: «свет
  скачет при повороте, объекты серые, кубы неразличимы»). ДИАГНОЗ: зеркальные
  материалы (roughness 0) отражали RoomEnvironment — тёмную комнату с ЯРКИМИ
  прямоугольными «окнами»; при повороте камеры отражение скользит по граням
  и то вспыхивает белым, то гаснет в серость; белый хром на белом фоне
  сливался. РЕШЕНИЕ: (1) окружение — самодельный «софтбокс» (сфера с плавным
  вертикальным градиентом, PMREM.fromScene в 10-stage) — бликам не по чему
  «скакать», RoomEnvironment НЕ возвращать; (2) цветные предметы — мягкий
  глянец `metalness 0, roughness 0.18, envMapIntensity 0.5`; (3) кубы —
  ГРАФИТОВЫЙ металлик (0x424a56, metalness 1, roughness 0.3) — читаются
  на белом. Проверено скринами с трёх азимутов — яркость стабильна.
  Прозрачности у тетраэдров нет. Текстур нет — только цвет.
  История финишей: сатин-металл → LEGO-пластик → зеркальный лак (fasthdr) →
  v4 мягкий глянец + графит (текущий).
- **Палитра**: candyColor — сочная карамель: HSL(h типа, 0.75, 0.55)
  в sRGB + convertSRGBToLinear. История: линейная пастель L=0.5 была
  «слишком ванильной», тёмный вариант до неё — слишком глухим; текущий —
  третья итерация, менять только по запросу. Тёмно-зелёный и коричневый
  исключены. **ВУАЛЬ НЕДОСТУПНЫХ — В ШЕЙДЕРЕ (2026-07-23, спека владельца
  «полностью уменьшать saturation, они получатся светло серые»)**: юниформа
  `uVeil` в matcapSpecPatch обесцвечивает готовый цвет в яркость и поднимает
  к светло-серому (`VEIL_LIGHT`/`VEIL_LIFT`, ползунки в matcapTuner).
  ⚠️ ПОЧЕМУ НЕ ЛЕРПОМ `material.color`, как было до этого: у ТЕКСТУРНЫХ
  моделей (114 предметов из 130 в кадре) color БЕЛЫЙ, цвет несёт атлас —
  лерп белого к серому лишь ПРИТЕМНЯЛ текстуру, тигр оставался рыжим, и
  на скрине полной чаши 130 завуалированных были неотличимы от доступных.
  Старый путь (`lerp(DIM_GREY, …)`, DIM_GREY 0xb6bcc6 линейный) жив
  фолбэком для материалов без патча (бомба, откат `CFG.matcap=false`).
  Сила в бою — `VEIL_TARGET`=1 (полное обесцвечивание); 0.65 осталось
  только за режимом 'tint'. `VEIL_MODE='fade'` (прозрачность) РЕАЛИЗОВАН,
  но НЕ включён — замер и причина в WORKSTREAMS, блок ГРАФИКА.
  ⚠️⚠️ **ЧИТАЕМОСТЬ ВУАЛИ РЕШАЕТ `VEIL_LIFT`, А НЕ ОТТЕНОК** (правило по замеру
  2026-07-29, диспетчер просил закрепить — это ровно тот случай, где следующий
  начнёт крутить хью и потеряет день). Lift поднимает яркость к светлому тону,
  и он один утягивает кучу в фон: на ОДНОМ тоне контраст кучи к небу lift 0.55
  -> 0.169, lift 0.25 -> 0.278 (у сцены БЕЗ вуали 0.314). Тон при этом двигает
  результат куда слабее. ПОРЯДОК ПРАВКИ: сперва lift, оттенок — потом.
  ⚠️ ПРОВЕРЯТЬ ЗАМЕРОМ, А НЕ ГЛАЗОМ: средняя яркость кучи против неба на
  ур.20 Hard при вуали, прибитой всем (`__game.veilAll(1)`); цель — контраст
  НЕ НИЖЕ сцены без вуали. Действующие 0x6f9fd8 + lift 0.35 дают 0.359.
  Живой подбор без пересборки — `__game.veilTune(hex, light, lift)`.
  ⚠️⚠️ **У `uVeil` ДВА ПОТРЕБИТЕЛЯ С ПРОТИВОПОЛОЖНЫМИ ТРЕБОВАНИЯМИ.** Бой —
  ЦВЕТНАЯ вуаль (спека «светло-синяя, не серая»); гхост-портреты закрытых типов
  в коллекции — БЕСЦВЕТНЫЕ (спека «прозрачные, немного матовые, но бесцветные»).
  Юниформа одна, поэтому смена тона МОЛЧА красит и коллекцию: замер силуэта
  после v165 дал rgb(81,117,161), синева b−r = +80. Лечение живёт в `itemThumb`
  (85-hud): на время СЪЁМКИ гхоста тон возвращается в нейтраль и сразу
  восстанавливается — как соседние сохранения color/opacity/uVeil. После:
  rgb(191,191,191), b−r = 0. **МЕНЯЕШЬ ТОН ВУАЛИ — ПРОВЕРЬ ГХОСТ** (`__game
  .thumbURL(key, true)`, разброс каналов обязан быть 0).
- **Стекло ВОЗВРАЩЕНО и БЕЗ transmission** (история: толстое transmission ->
  незаметное -> удалено -> возвращено «незаметным» -> transmission ВЫРЕЗАН
  аудитом 2026-07). ⚠️ ПЕРФ: любой видимый transmission>0 заставляет three
  r149 рендерить ВЕСЬ мир второй раз в FBO с MSAA — замер: ~55% КАЖДОГО
  кадра (мобильный вьюпорт 235->110 мс). При ior 1.0 стекло всё равно
  ничего не преломляло — теперь transparent + opacity 0.08 (20-arena),
  вид тот же, цена — доли процента. transmission НЕ возвращать нигде;
  прозрачные предметы — тоже только opacity.
  Проникновение предметов в стекло починено НА СТОРОНЕ ФИЗИКИ:
  стены Rapier стоят ВНУТРИ стекла с зазором WALL_GAP=0.12 — предметы
  останавливаются, не доходя до стеклянной поверхности (виден воздушный
  зазор).

- **ДВА ИНВАРИАНТА НЕБА (ГРАФИКА, панель «оживить день» 2026-07-31):**
  (1) ⛔ У НАС НА ЭКРАНЕ НЕТ НЕБА НАД ГОРИЗОНТОМ — камера смотрит сверху в
  чашу, при любом допустимом наклоне верх кадра это направление
  вниз-в-сторону. Любая будущая идея «на горизонте» (облака-силуэты, гряда,
  дымка, равнина) отпадает арифметикой ДО реализации — четыре так и умерли.
  (2) Дневная палитра МОНОТОННА по яркости, а рампа разложена по экрану:
  t=0 = самый тёмный стоп = ВЕРХ кадра с белыми глазами и паузой. Поэтому
  ЛЮБОЙ будущий декор дня делается сдвигом чтения рампы В МИНУС — плюсовой
  сдвиг физически роняет контраст HUD (замер: минус 2.95→3.18, плюс 2.82
  при пороге 3:1), и гаснет у кромок кадра (иначе ломается рецепт тинта
  полос Safari — кромки обязаны совпадать со стопами, Δ0).
  ⚠️ САМОГО ДЕКОРА В ИГРЕ БОЛЬШЕ НЕТ: страты и пятна отменены владельцем
  2026-07-31 (см. «Отвергнутое»), вместе с ними ушёл и страж монотонности
  `checkDayRampMonotonic` — он существовал ровно затем, чтобы декор не мог
  осветлить верх кадра. Рецепт выше сохранён как ТРЕБОВАНИЕ К БУДУЩЕМУ
  декору, а не как описание текущего кода.
  ⚠️ ЦЕНА ОТКАТА, НАЗЫВАЮ ЧЕСТНО: страты ПОДНИМАЛИ контраст HUD, и вместе
  с ними он упал обратно. Замер профилем строки через центр глаз на самой
  откаченной сборке (2026-07-31, сразу после действия — пока угроза помола
  на нуле): ДЕНЬ 2.96:1 при пороге 3:1 (со стратами было 3.18 — порог
  брался), НОЧЬ 13.07:1.
  ⚠️⚠️ ЭТО ЧИСЛО ДЕСКТОПНОЙ РАСКЛАДКИ 900×640. На мобильной 390×780 порог
  БЕРЁТСЯ: 3.03 тем же профилем и 3.08 линейкой стража. Раскладка двигает
  контраст сильнее, чем весь запас над полом: конструкция глаз там мельче и
  стоит выше, а небо вверху экрана темнее. Говоря «порог не берётся», всегда
  называть вьюпорт — иначе следующий увидит в логе сьюта 3.08 и решит, что
  канон врёт.
  ⚠️ СТРАЖ ТЕПЕРЬ ЕСТЬ (заказ диспетчера, test.js, секция «ПОЛ КОНТРАСТА HUD
  К НЕБУ»): пол по глазам И по кнопке паузы, день и ночь, с диверсией внутри
  каждого прогона. Его линейка — МАКСИМУМ внутри рамки против МЕДИАНЫ двух
  полос неба по бокам на тех же строках, вьюпорт 390×780; на 2026-07-31 даёт
  глаза 3.08/13.48, паузу 4.23/11.07 при полах 3.0/12.5 и 3.5/9.0.
  ⛔ НЕ СВЕРЯТЬ ЭТИ ЧИСЛА С 2.96 НАПРЯМУЮ — это разные линейки на разных
  раскладках, а не «контраст вырос».
  ⚠️ И ПАРА ПО НАПРАВЛЕНИЯМ: белок гаснет, когда небо СВЕТЛЕЕТ, а тёмная
  дневная кнопка паузы — когда небо ТЕМНЕЕТ. Рецепт декора «сдвиг рампы в
  минус» бьёт именно по кнопке (замер: затемнение верха на 30% роняет её до
  2.5:1, при этом глаза дают 5.7 и молчат), поэтому полов ДВА.
  ⛔ НЕ ПУТАТЬ С ЧИСЛОМ «50 из 255» из старого абзаца про глаза: оно снято
  ПО ПАНОРАМАМ (эпоха до 2026-07-30, модуль 05-sky.js давно удалён) и в
  отношениях WCAG это ~1.6:1. Палитры владельца сами по себе подняли
  контраст с 1.6 до 2.96 — тот исторический риск снят, действующий дефицит
  измеряется десятыми, а не разами. Одна величина, две эпохи и две шкалы —
  сверяться только с датированным замером.
- **Поле БЕЛОЕ** (по требованию владельца; история: светлое -> чёрное ->
  белое). ВАЖНО: небо — ShaderMaterial, он МИНУЕТ тонмаппинг и
  sRGB-конвертацию рендерера, поэтому его uniforms задаются сырыми
  sRGB-значениями БЕЗ convertSRGBToLinear (#ffffff = настоящий белый);
  с конвертацией небо выглядит серым. Туман — «сверхбелый» Color(1.5,...)
  чтобы пробить ACES.
- Известные внешние референсы, которые НЕ применимы в текущем однофайловом
  формате: DracoLoader (нужны wasm-декодер и внешние ассеты),
  KHR_materials_dispersion / DragonDispersion (нужен three r167+, см. выше).
- **Nexus (nexus.dimforge.com)** — новый GPU-мультифизический движок от
  авторов Rapier (compute-шейдеры через WebGPU, тысячи тел, MPM-жидкости
  и DEM-гранулярка в разработке). Владелец спрашивал: ОЦЕНЁН, пока НЕ
  берём — проект молодой (rigid bodies v0.x), требует WebGPU (покрытие
  мобильного веба хуже WebGL2 — критично для порталов), а наша нагрузка
  (141 тело, ~1 мс на CPU-Rapier) GPU не требует. НАБЛЮДАТЬ: если появится
  фича «настоящая труха/жидкость в миксере» (DEM/MPM) — это их территория;
  миграция Rapier→Nexus у одного вендора будет естественной.

## Физика (Rapier, см. docs/ADR-001)

- **Rapier WASM** (@dimforge/rapier3d-compat, бандл src/vendor/rapier.js
  2.24 МБ инлайнится в index.html). Мир: gravity −22 (земная в нашем
  масштабе ~0.45 м/юнит), timestep 1/60, **до 2 подшагов за кадр**
  (было 3 — A3, см. ниже), numSolverIterations=8 (плотные стеки),
  maxCcdSubsteps=4.
- ⚠️⚠️ **ПОТОЛОК ПОДШАГОВ ЗА КАДР = 2** (A3, перф мобильного тира, 2026-08-01;
  жалоба владельца «игра подтупливает на мобиле, особенно на взрыве и когда
  объекты сыплются»). Аккумулятор фиксированного шага — не просто цена, а
  **УСИЛИТЕЛЬ**: медленный кадр -> больше накопленного dt -> больше вызовов
  `world.step()` -> кадр ещё медленнее. На осыпании p95 подшагов упирался
  ровно в потолок, а солвер держит 87-98% кадра.
  ДАЁТ (осыпание, CPU ×4 на настоящем GPU, 6 сидов): солвер p95 36.7 -> 22.5
  (−39%), кадр p95 41.4 -> 27.9. СТОИТ: середина полёта чуть отстаёт и
  сходится к 2.6 с (верх кучи 8.65 -> 10.79 на 2000 мс, 7.70 -> 7.96 на
  2600 мс). Длительность осыпания по стенным часам НЕ выросла (−3% = шум).
  ⚠️ ИТОГ ЗАПОЛНЕНИЯ СВЕРЕН ОТДЕЛЬНО, и это не формальность: интро кончается
  по часам КАМЕРЫ, а не по «куча улеглась», поэтому другой потолок мог застать
  осадку в другой стадии и трим срезал бы другое число пар. Не срезал (8 сидов):
  живых 182/182, верх 7.73 -> 7.72, wallExcess max 0.141 -> 0.098, под полом 0.
  ⛔ **НЕ ДЕЛАТЬ СТУПЕНЬЮ ПЕРФ-ТИРА** — довод структурный: `tickPerfTier`
  ПРОПУСКАЕТ интро, то есть ступень физически не может сработать до конца
  ПЕРВОГО осыпания, ровно того момента, на который жаловался владелец.
  ⛔ **«НА БЫСТРОЙ МАШИНЕ КАП НЕ СВЯЖЕТ» — ПРОВЕРЕНО И НЕВЕРНО:** без
  троттлинга p95 подшагов тоже 3, потому что в интро dt множится на
  INTRO_TIME_SCALE=1.7 (16.7×1.7 = 28 мс). Осыпание меняется одинаково на всех
  устройствах — сознательный выбор в пользу однородного ощущения.
  ⛔ **≤1 ПРОБОВАЛИ И ОТВЕРГЛИ:** −78% солвера, но к 2.6 с куча ещё в воздухе
  (верх 16.2 против 7.7) — видимое замедленное кино. Возврат только словом
  владельца. Ручка для замеров: `__game.physKnobs({maxSub:N})`.
  ⚠️⚠️ **ЛОЖНАЯ ТРЕВОГА, НЕ ПЕРЕОТКРЫВАТЬ:** соак A3 дал 41 телепорт спасателя
  против 6 у контроля — это НЕ регрессия, а РАЗНЫЙ ОБЪЁМ РАБОТЫ (A3 прошёл три
  уровня и 2 победы, контроль все 12 минут просидел на первом с нулём побед).
  Нормировка развела: чистых интро 0/0 (32 прогона), смен уровня 0 против 1
  (48 смен), распределение выступа за стену на равной работе совпало (p99
  0.078/0.076, max 0.173/0.180, выше нормы 0.20 ноль у обоих), спасений на
  равной работе 66 (≤3) против 42 (≤2). ⛔ **ПРАВИЛО: тотал редких событий за
  прогон сравним ТОЛЬКО при равном объёме сыгранного** — соак не
  рандомизированный эксперимент, более быстрый кадр меняет сам ход партии.
- ⚠️ СПАСАТЕЛЬ ПОЛА (2026-07-30, v185, жалоба владельца «дыра в объектах» —
  стейк и посох лежали на лопастях НИЖЕ невидимого пола). КОРЕНЬ: солвер терпит
  глубокое проникновение ПЛОСКИХ форм под нагрузкой кучи, а глобальный сон
  выключает интегратор — утонувшее к моменту сна остаётся утопленным навсегда.
  Жертвы: стейк 13/17 (самый тонкий, 0.12; жив только с v178 — анти-туннель
  калибровался без него), посох, пряник.
  ⛔ ДВЕ ГИПОТЕЗЫ ДИСПЕТЧЕРА ОТВЕРГНУТЫ ЗАМЕРОМ ФИЗИКИ (не поднимать заново):
  «виноват усиленный взрыв 15/18» — нет, худший случай (pen 0.224) на ЧИСТОЙ
  осадке без бомбы; «солвер не успевает на пике» — нет, шаг на взрыве = шагу
  на встряске (p95 7.7-10.9), дорогие кадры это ЧАСТИЦЫ ⛔ (ПОСЛЕДНИЕ ТРИ
  СЛОВА ОТМЕНЕНЫ перф-исследованием v219: то число снято в headless на
  SwiftShader; на настоящем GPU кадр держит СОЛВЕР 87-98%, у частиц дёшев
  ТИК 0.7-1.5 мс, дорога ПОСТРОЙКА до 12.3 мс на взрыве — это разные вещи;
  вывод «не солвер» для этой правки остаётся верным).
  ⚠️ ЧИСЛА ЭТОГО РАЗВОРОТА СНЯТЫ ПРИ CPU-ТРОТТЛИНГЕ ×4 (прокси мобильного
  ядра) НА НАСТОЯЩЕМ GPU (`--use-angle=metal`) — без этой оговорки их нельзя
  сверять ни с замерами эпохи SwiftShader, ни с будущими цифрами с телефона.
  Без троттлинга та же постройка это 0.9 мс на матч, а не 12.3. Проект уже
  дважды обжигался на сравнении одной величины по разным линейкам. Митигация «импульс
  на 2 кадра» НЕ НУЖНА ни для перфа, ни для провала — спека силы взрыва цела.
  РЕШЕНИЕ: спасатель по ИСТИННОМУ проникновению контактного манифолда
  (floorPenetration, порог FLOOR_PEN_MAX=0.12 — середина пустого коридора
  бимодального распределения: здоровый максимум 0.083, дефект 0.224); подъём
  ровно на глубину, XZ/вращение не трогаются. ⚠️ ПОРОГ НЕЛЬЗЯ СТРОИТЬ ПО
  «НИЖНЕЙ ТОЧКЕ» габарита — у здоровой кучи она уходит под пол до 0.39 чистой
  арифметикой (та же грабля, что была у wallR). Плюс два закрытых окна сна:
  rescueSweep(true) из sleepPhysics (форс-сон при maxV<2 замораживал только
  что утонувшего) и второй ключ по ПРОСАДКЕ 1.5 с (при помоле куча не спит
  вовсе, стейк сидел в плите 30 с — видимо, это владелец и видел). ⛔ УТОЛЩЕНИЕ ПЛИТЫ
  ОТМЕНЕНО ВЛАДЕЛЬЦЕМ в тот же день («откати толщину плиты, оставь только
  спасателя», v189): полутолщина снова 0.3 бит-в-бит. Замер «0.28 просадки при
  полутолщине 0.30, 7% до переворота нормали» сохранён комментарием у строк
  плиты вместе с «пробовали, нейтрально, отклонено владельцем» — НЕ выводить
  заново и не возвращать молча. Откат фикс не трогает: максимум просадки на
  спящей куче 0.109 против 0.116 у толстой, выше порога 0 из 57. Соак:
  UNDER FLOOR 3→0 на 274 сэмплах, шторма нет (0-3 подъёма/12 мин при тревоге
  12). ⚠️ Хук `__game.rescueNow()` НЕСУЩИЙ — на нём страж пола (place() физику
  не будит, без хука тест-гонка). ⚠️ Критерий спасателя ДВУХПРИЗНАКОВЫЙ:
  манифолд обновляется только в world.step(), сразу после телепорта он от
  старой позиции.
- Анти-туннель комплекс (по доке Rapier + issues #286/#302 — CCD не
  всесилен, особенно на мелких сферах компаундов): CCD на всех телах
  (setCcdEnabled), стены толщиной 0.6, конус продлён НИЖЕ пола лопастей
  (LOW=0.5 — там была дыра), дно НЕ шире конуса (вылетевшему негде лежать
  «в стекле»), ТЕРМИНАЛЬНАЯ скорость падения MAX_FALL=16 (v>20 пробивала
  компаунды), и спасатель в stepPhysics: раз в 0.5 с предмет за пределами
  чаши тихо возвращается внутрь с console.warn (координаты в логе).
  Интро-утряска бьёт только по осевшей массе (maxBodySpeed<3).
- Коллайдеры по типам (50-physics createItemBody): куб=cuboid,
  шар=ball, цилиндр=cylinder, пилюля=capsule, конус/окта/доде/тетра/
  звезда/сердце=convex hull ИЗ РЕНДЕР-ГЕОМЕТРИИ; тор/узел/спираль =
  ЦЕПОЧКИ КАПСУЛ по точной кривой формы (addCapsuleChain: тор 12 капсул
  по кольцу, узел 18 по параметрике TorusKnot p=2 q=3, спираль 12 по
  хеликсу); чайник=компаунд шаров. ⚠️ ГРАБЛЯ: three строит тор/узел
  в плоскости XY — первые компаунды шаров стояли в XZ (перпендикулярно
  мешу!), предметы «впаивались» в видимое кольцо; плюс редкие шары давали
  зубчатую поверхность. Кривые компаундов ВСЕГДА сверять с плоскостью
  геометрии three. ВЕС: масса из ПЛОТНОСТИ — хром 7.8, золото 5,
  пластик 1.2 (тяжёлый куб реально давит). friction 0.5, restitution 0.12.
- «Катучие» формы (шар/тор/цилиндр/узел/спираль/пилюля) — angularDamping
  2.5 (в Rapier нет трения качения, иначе катаются вечно), прочие 1.2.
- ✅ ЗАКРЫТО v220 (ФИЗИКА, 2026-08-01): π/2 СНЯТ во всех трёх местах —
  кольцо вместо частокола; ГРАНЬ КОЛЬЦА ПО y0, НЕ по midY (найдено замером:
  ступень кольца 0.725, конус за неё +0.134 — грань по середине делала низ
  кольца шире конуса, спасатель честно телепортировал лежащих; с midY
  телепортов было ВДВОЕ больше). Соак 12 мин: спасений 29→11, заполнение
  не поехало (PAIRS не трогали). A1: контейнер одним фикс-телом (599→183
  тел) — оставлен НЕ ради перфа (−64 создания/сноса на genLevel).
  ⛔ ОТВЕРГНУТО ЗАМЕРОМ, не изобретать: число стенных КОЛЛАЙДЕРОВ — не
  рычаг (417→209 = −4.5% солвера; цену держит ДИНАМИЧЕСКАЯ куча, как и с
  телами); RINGS=24 хуже по всем трём (телепорты/солвер/прокси).
  ✅ НОРМА wallExcess ЗАКРЫТА СЛЕДУЮЩИМ ЗАХОДОМ (ФИЗИКА, влито в main перед
  v221): **0.18 -> 0.20 ПО РАСПРЕДЕЛЕНИЮ**, 8856 сэмплов через новый хук
  `__game.wallExcessAll()` (выступ КАЖДОГО живого предмета — против ОДНОГО
  сэмпла у `maxWallExcess`). Кольцо лучше частокола по всему хвосту:
  p99 0.075 против 0.102, max **0.181** против 0.226, доля >0.18 — 0.01%
  против 0.05%. Порог обязан стоять выше здорового максимума, иначе ловит
  норму (прежние 0.18 давали по 4 тревоги за 12 мин на ЗДОРОВОЙ сборке).
  ⚠️ Число зависит от ФОРМЫ предметов — сменится партия моделей, перемерить.
  Исторический разбор ниже — как было найдено.
- ⚠️⚠️ НАЙДЕНО РЕВЬЮ 2026-07-30 И ПОДТВЕРЖДЕНО СЧЁТОМ ДИСПЕТЧЕРА: **ПАНЕЛИ
  СТЕН ПОВЁРНУТЫ НА 90°, СПЛОШНОГО КОЛЬЦА НЕТ.** В 50-physics поворот задан
  `Euler(0, -a + Math.PI/2, 0)` (три вхождения: кольца, временная стена, дно-
  окрестность). При повороте вокруг Y на θ локальная X уходит в
  (cos θ, 0, −sin θ); при θ = −a это РАДИАЛЬ (правильно), а при θ = −a+π/2 —
  ТАНГЕНЦИАЛЬ. То есть полутолщина 0.30 стоит поперёк, а `chord/2` (посчитанная
  как ТАНГЕНЦИАЛЬНАЯ ширина сектора) торчит РАДИАЛЬНО. Вместо кольца — частокол
  радиальных рёбер.
  ЧИСЛА (кромка, radiusAt≈4.1, faceR=3.98, центр 4.28, chord=0.864): шаг между
  центрами панелей 0.84 при ширине панели 0.6 → ЩЕЛЬ 0.24; у внутренних концов
  рёбер щель 0.156; внутренняя граница уходит ВНУТРЬ на ~0.13 от задуманной
  `radiusAt(midY)−WALL_GAP`. Щели есть в 9 из 12 колец (по счёту ревью).
  ⚠️ ЭТО ОБЪЯСНЯЕТ ДАВНЮЮ АНОМАЛИЮ, записанную как «пересмотреть при соаке»:
  `wallExcess` на ур.40 превышает норму ~0.15 УЖЕ БЕЗ ВЗРЫВА (0.141). Между
  рёбрами — карманы, предметы в них заклиниваются и торчат, спасатель их
  вытаскивает (отсюда видимые рывки и 1-4 телепорта).
  ⚠️ И ЭТО РОВНО ТЕ «КЛИНЬЯ-КАРМАНЫ», от которых проект уходил, отказываясь от
  нахлёста панелей — формула `chord` выведена под ВСТЫК по тангенциали и при
  таком повороте не работает.
  ⚠️ ПОЧЕМУ НЕ БЛОКЕР И ПОЧЕМУ ПРАВКА НЕ В ОДИН ШАГ: охватный радиус предметов
  0.6–1.0 много больше щели, предметы не убегают — игра проходима. Но снятие
  π/2 МЕНЯЕТ эффективный радиус чаши (до +0.13 у кромки), а на текущей
  геометрии снята калибровка PAIRS/topY и норма wallExcess. Правка обязана идти
  ОДНИМ пакетом с перезамером заполнения и соаком (долг ФИЗИКИ №1).
- Контейнер (ЗАМЫСЕЛ; фактическую ориентацию см. пункт выше): СТУПЕНЧАТЫЙ конус — 12 колец × 32 ВЕРТИКАЛЬНЫХ сегмента
  (толщина 0.6, без наклонов, только yaw; грань кольца = radiusAt(midY)
  − WALL_GAP) + 32 вертикальных скользких сегмента над кромкой + дно.
  ⚠️ ИСТОРИЯ: длинные НАКЛОННЫЕ панели с кватернион-поворотом стояли
  не по конусу (у дна грань уезжала на ~0.3 наружу) — предметы «в стекле»
  по всему низу, спасатель штормил сотнями телепортов; владелец
  трижды показывал скрины. Наклонные панели НЕ возвращать — только
  ступенчатые кольца с тривиальной геометрией. Панели ВСТЫК (2R·tan(π/32)
  +0.08) — нахлёст создавал клинья-карманы. WALL_GAP=0.12 внутрь от
  стекла. Метрика контроля: __game.maxWallExcess() — норма ≤ ~0.15
  (считается по item.wallR); спасатель rescueSweep: край предмета глубже
  0.18 в стекле → телепорт ЛОКАЛЬНО внутрь на той же высоте (телепорт на
  верх чаши был виден как «прыжок» и затягивал осадку) + console.warn;
  вызывается перед сном, и если кого-то спас — СОН ОТМЕНЯЕТСЯ (иначе
  замораживали спасённого в воздухе). Горизонтальный габарит для теста
  стены — item.wallR: у плоских моделей охватный r переоценивает ширину
  (полуразмеры стейка 0.12×0.81×0.53 при rc=0.85 — шторм ложных спасений,
  4-8 за интро, все по стейку; wr=0.53 в TYPES). При стоящей временной
  стене спавна легальный радиус — R1 на любой высоте (сравнение с конусом
  телепортило легально падающие у края ПРЯМО В ПОЛЁТЕ). После фиксов:
  0 спасений за 3 интро. На время осадки genLevel
  ставится ВРЕМЕННАЯ высокая стена, после осадки снимается.
- Осадка: ЖИВАЯ во время интро (без предварительных шагов в genLevel);
  для тестов — skipIntro (300 шагов + вибро-утряска + трим). Спавн слоями
  над чашей, раскидан на всю ширину кромки.
- Сон — НАША глобальная схема поверх Rapier (авто-сон Rapier медленный:
  круглые формы докатываются): physAwake в 99-main; штиль = maxBodySpeed
  < 0.25 держится 0.4 с ИЛИ форс через 3 с РЕАЛЬНОГО времени; sleepAll
  (обнулить скорости + body.sleep) / wakeAll на любое событие. Форс-сон
  ПО-ТЕЛЬНО не делать — каскадные пробуждения раскачивают кучу (замерено).
- Вращение мешей ЧЕСТНОЕ — mesh.quaternion из тел (syncMeshes). Удаление
  предмета: destroyItemBody СРАЗУ при animating (Rapier будит соседей).
- Встряска/вибрация лопастей — applyImpulse по массе (impulseBody/spinBody).
- item.r остаётся ОХВАТНЫМ радиусом для геймплея (доступность, эффекты).
- radiusAt (конус до кромки, цилиндр выше) остаётся для СПАВНА и
  динамического радиуса совпадения — физика стен на панелях Rapier.
- Тики HUD/доступности/миксера — по performance.now (НЕ по накоплению dt):
  при низком FPS детекты не растягиваются.

## Тюнинг (00-config.js)

⚠️ КАНОН ЧИСЕЛ — САМ 00-config.js, здесь только карта. Ключевое на
2026-07-21: `FUNNEL` R0=2.4 R1=4.1 H=9.2 (чаша ×1.15); `PAIRS`=90 (укороченные
1-3: `PAIRS_EARLY`=[64,71,78]; трим может тихо изъять пары на рыхлых сидах;
⚠️ ЧИСЛО ПРЕДМЕТОВ НА УРОВНЕ — в разделе «СОСТОЯНИЕ ОБЪЕКТОВ» вверху, оно
ЗАМЕРЕНО: формула «пары×2 + 1 = 181» устарела с появлением бомбы и камней);
`FLOOR_REST`=1.15; `G`=22;
`MATCH_SCORE`=10; `MISS_PENALTY`=**10** (не 7 — таблица 2026-07-22); `MIXER_PERIOD`=2; `MIXER_PENALTY`=20;
⚠️ ШТРАФЫ МНОЖАТСЯ БУСТЕРОМ (2026-07-28, единая точка scorePenalty).
`SURPRISE_BONUS`=150; терпение миксера по СЛОЖНОСТИ `MIXER_IDLE_EASY/HARD`=
**15/10 с** (перетюн `304fdf0` 2026-07-27; прежние 10/3.3 протухли в каноне); прогрессия: `LEVEL_TYPES_MIN`=9 +1/уровень
до TYPES.length; комбо/цепь: COMBO_RADIUS=1.1 (потолок ВЕЗДЕ), COMBO_MS=4000,
COMBO_STEPS=5, COMBO_MISS_DROP=2, CHAIN_COMBO_AT=10, CHAIN_MS=10000,
CHAIN_MISSES=2; размеры: ур.1-15 один размер (SIZE_UNIFORM_LEVELS=15),
дальше рампа SIZE_SPREAD 0.10 +0.04/ур до 0.50;
matchRadius — динамический (updateMatchRadius в 60-access, пол MATCH_R_MIN=
0.75). Экономика: COIN_BASE=20, COIN_PER_SCORE=500, PRICE_SHAKE=25,
подсказки старт 3 +1/уровень (77-save). Физика: FRICTION 0.5, RESTIT 0.12,
плотности и MAX_FALL/fallCap — в 50-physics. Камера: phi=0.45, r=16.2,
target y=4.2.

## Хром iOS: чёрные поля Safari 26 (рецепт, НЕ ломать)

Safari 26 (Liquid Glass) **игнорирует `theme-color`** и красит свои бары сверху и
снизу по `background-color` **самих `html` и `body`**. Но fixed-элемент поверх
ОТРАВЛЯЕТ этот тинт своим фоном, причём `transparent` трактуется как
«прозрачный ЧЁРНЫЙ» — отсюда чёрные поля. Рецепт (обкатан на лендинге
playgama.com/about-us, применён здесь 2026-07-29):

1. `viewport-fit=cover` в мете — без него iOS леттербоксит страницу.
2. `tintChrome` (99-main) пишет цвет в `documentElement.style.backgroundColor`
   И `document.body.style.backgroundColor` (+ meta для Android/macOS).
3. **Каждому fixed-элементу — фон цвета своей кромки с альфой 0.01**:
   `#topBar`/`#face` берут `--sky-top-rgb`, `#bottomBar` — `--sky-bot-rgb`
   (обе ставит 10-stage из SKY_STOPS — ПЕРВЫЙ и ПОСЛЕДНИЙ стоп палитры).
   ⚠️ НЕ заменять на `transparent` — вернутся чёрные поля.
   ⚠️ СОВПАДЕНИЕ С КРОМКОЙ КАДРА ДЕРЖИТСЯ НА `SKY_MAP='screen'` (раскладка
   стопов по экрану): при ней первый/последний стоп РОВНО и есть верхний/нижний
   пиксель кадра (замер: Δ0 в обеих темах). Переключите на 'view' — переменные
   начнут врать про кромку (замер: Δ90), и полоса Safari получит цвет, которого
   на экране нет.
   ⚠️⚠️ ОГОВОРКА (ГРАФИКА 2026-07-31, оплачена двойной ложной правкой):
   «совпадает по построению» верно ТОЛЬКО В ПОКОЕ. Красная УГРОЗА ПОМОЛА
   (`uGrind`) наливается сверху весь простой (замер: Δ0→27→57→86→119→152 за
   7 секунд БЕЗ всякого декора) — во время неё верх кадра красный НАМЕРЕННО,
   это игровой сигнал, а не поломка рецепта. КРОМКУ МЕРИТЬ В ПЕРВУЮ СЕКУНДУ
   ПОСЛЕ ДЕЙСТВИЯ, пока угроза на нуле (тогда Δ0 при любой силе декора);
   замер на простое загрязнён угрозой и ведёт к лечению несуществующей
   болезни (Графика так расширила огибающую облаков и сама же откатила).
4. Скрытые полноэкранные оверлеи прятать `display:none` (Safari семплит пиксели
   `opacity:0`/`visibility:hidden`). У нас `hide()` в 85-hud так и делает.
5. Проверить можно ТОЛЬКО на устройстве — headless этого не воспроизводит.

## Верификация

- Только headless Playwright (`node test.js`) — превью-таб Claude
  замораживает rAF (таймеры/детекты не тикают), годится лишь для статики.
- ⚠️ ЛОВУШКА ШРИФТА СТЕНДА (ИНТЕРФЕЙС 2026-07-31, дырки цифр — стенд врал
  ВТРОЕ): боевая `--font-round` начинается с `ui-rounded`, который в headless
  НЕ резолвится — живая страница рисует SF Pro Rounded, а стенд со своей
  цепочкой падал в system-ui и мерил ДРУГОЙ шрифт. Мерить только КЛОНОМ
  живого узла и в масштабе 1:1 к viewBox (скрытый оверлей даёт нулевой rect —
  страж-тавтология «нет пикселей — нет дырок»).
- ⚠️ ЗАЛИВКА ПРОГАЛОВ ГЛИФОВ (8/0/9 в .otext): ⛔ feMorphology dilate радиусом
  ≤ обводки — ТОЖДЕСТВЕННАЯ ОПЕРАЦИЯ (при paint-order:stroke заливка+обводка
  УЖЕ равна дилату той же величины) — доказано Интерфейсом, направление
  диспетчера было пустым; dilate+erode закрывает щель, но растит контур до
  +31px. РАБОЧИЙ приём: blur от SourceAlpha с ПОРОГОМ 0.5 цветом обводки под
  оригинал — у прямой кромки уровень 0.5 совпадает с исходным контуром
  (порог 0.4 раздувал на +220px). Болели счёт победы и «×N» карточек;
  #mixerTimer ЗДОРОВ — фильтр ему не ставить, нечего чинить.
- ⚠️⚠️ ПРОКРУТКУ МЕРИТЬ НАСТОЯЩИМ КОЛЕСОМ (`page.mouse.wheel` / CDP), НЕ
  присваиванием scrollTop (ИНТЕРФЕЙС 2026-07-31, блокер меню v207):
  присваивание перескакивает ЦЕЛЫЙ КЛАСС дефектов прокрутки. Механика блокера:
  sticky-шапка меню ужималась В ПОТОКЕ (72→48 классом .stuck) — Blink
  компенсировал усадку своим scroll anchoring (вычитал те же 24px из
  scrollTop), scrollTop падал ниже порога класса, класс снимался, шапка
  отрастала — цикл; меню НЕ ПРОКРУЧИВАЛОСЬ ВООБЩЕ (колесо 8px×10 →
  scrollTop [0×10]), а все ассерты были зелёными — они ходили присваиванием.
  В WebKit механизма нет — на iOS не воспроизводится, скрин-контроль слеп
  по построению. Следствие в коде (с v208): в потоке меню НИЧТО не меняет
  высоту — плавающая шапка это ОТДЕЛЬНЫЙ fixed-узел `#msSticky` (класс `.on`
  по уходу `.ms-coll-title` за верх), а не sticky с усадкой; промежуточную
  v207-механику «коробка-72 + пилюля ::before» Интерфейс снял вместе с
  причиной — не возвращать sticky с меняющейся высотой ни под каким видом.
  И парное правило оттуда же (ловля диспетчера v207): у `openMainScreen`
  ДВА пути с РАЗНЫМИ ожиданиями — переоткрытие СБРАСЫВАЕТ прокрутку,
  visibility-вызов на открытом меню СОХРАНЯЕТ; оба закрыты стражами.
- ⚠️ ЛОВУШКА ЗАМЕРА ПЕРЕПОЛНЕНИЯ ТЕКСТА (ИНТЕРФЕЙС 2026-07-31, чипса ×1.25):
  `scrollWidth <= clientWidth` — ЗЕЛЁНЫЙ И БЕСПОЛЕЗНЫЙ страж для центрованного
  текста: в LTR scrollWidth НЕ считает вылет ВЛЕВО, а центрованный текст
  вылезает В ОБЕ стороны — на сломанной сборке метрика честно отдавала
  равенство. Плюс clientWidth округлён до целого (ложные 0.3px). Верно:
  `Range.getBoundingClientRect().width` против ДРОБНОЙ rect.width − паддинги.
- ⚠️⚠️ ПОДВИД ФЛЕЙКА «ПОЙМАЛ МОМЕНТ, А НЕ СОСТОЯНИЕ» (имя дал ИНТЕРФЕЙС
  2026-07-31; правило выведено независимо ТРИЖДЫ за сутки — 0b2de04,
  transitionrun, проба v210 — значит канон). Формулировка Интерфейса
  дословно: «фиксированная пауза измеряет часы стенда, а не состояние
  страницы; под нагрузкой они расходятся. Ждать надо опросом до факта
  (нужный computed style / устоявшийся rect / событие движка), с потолком
  времени как страховкой». Две грани, обе ловлены в бою:
  (а) ВРЕМЯ вместо ФАКТА — замер по фикс-таймеру меряет переход, а не покой
  (видимость шапки переключается через 220 мс часами АНИМАЦИИ, под нагрузкой
  сьюта тики starve'ятся — пауза 320 мс иногда истекала раньше движка);
  (б) момент пойман, но состояние НЕ УДЕРЖАНО: страж руками снял класс,
  которым владеет живой scroll-слушатель, а поздний scroll-ивент (лаг
  события от присваивания — 65 мс по пробе, под нагрузкой больше) вернул
  класс до замера. Правила: мерить УСТОЯВШЕЕСЯ состояние осадка-опросом
  с потолком-страховкой (и класть «дождались/не дождались» в отчёт — таймаут
  опроса должен отличаться от честного провала ассерта); НЕ трогать руками
  состояние, которым владеет живой слушатель, — приводить его НАСТОЯЩИМ
  путём (прокруткой, кликом), тогда слушатель сам удержит его при любых
  поздних событиях. И третья грань (ИНТЕРФЕЙС, тот же день): НЕНЕСУЩАЯ
  фикс-пауза опаснее несущей — сегодня она ничего не держит и не падает,
  а завтра смена соседнего перехода молча делает её единственной и
  флейкующей опорой; нашёл паузу, которая «и так работает», — замени
  опросом факта или удали.
- ⚠️⚠️ ФОРМУЛА-САМОПРОВЕРКА В БОЕВОМ КОДЕ — НЕ ГАРАНТ, если она не считает
  ВСЮ геометрию (ГРАФИКА, звёзды 2026-07-31, формулировка дословно): «у
  звёзд checkStarBudget считал смещение внутри ячейки и молчал про то, как
  сфера ячейку пересекает: 9.3% срезанных ядер и 21.1% пропавших звёзд при
  ЗЕЛЁНОЙ самопроверке. Настоящий гарант — перебор tools/star-cells-check.js,
  проверенный ДВУСТОРОННЕ». Это тот же класс, что мы выкорчёвываем в
  тестах («инструмент не видит того, что проверяет»), только живущий в
  боевом коде и ВЫГЛЯДЯЩИЙ как защита. Правило: инвариант-самопроверка
  сдаётся как страж — с показом, что она КРАСНЕЕТ на конфигурации,
  нарушающей инвариант; не можешь показать — пиши перебор в tools/.
- ✅ ЗАКРЫТО ЗАМЕРОМ — НЕ ДЕФЕКТЫ, НЕ ПРОВЕРЯТЬ ЗАНОВО (раздел по предложению
  ИНТЕРФЕЙСА 2026-07-31; жанр «проверено, дефекта нет» — каждый пункт
  экономит следующему полдня; дописывать с датой и автором замера):
  • `focus()` НЕ даёт ложного вывода в страже a11y, если кнопка была
    сфокусирована ДО скрытия: браузер САМ снимает фокус при
    `visibility:hidden` (замер: сразу после скрытия в фокусе BODY) — порядок
    фаз стража значения не имеет (Интерфейс, 2026-07-31).
  • `#mixerTimer` здоров на обоих шрифтах — SVG-фильтр заливки прогалов ему
    не ставить, нечего чинить (Интерфейс, 2026-07-31).
  • grep «кадр на включение видимости» по test.js даёт 1 — это ЦИТАТА в
    объясняющем комментарии, самой фикс-паузы в коде нет (снята v210+).
    Общее правило: сверять КОД, а не счётчик вхождений — фраза переживает
    механику (на этом чуть не сделан ложный вывод дважды за вечер).
  • `PAGEERROR: Cannot read properties of null (reading 'boom')` в хвосте
    КАЖДОГО прогона — СИНТЕТИЧЕСКИЙ креш самого сьюта (тест fatal-экрана,
    `null.boom()` в test.js), оба гейта ошибок его намеренно фильтруют.
    В src его нет и искать не надо (диспетчер, 2026-07-31; Графика
    споткнулась о него же в ночь на 01.08 — потому и записано).
- ⚠️ ДОПОЛНЕНИЕ К ДВУСТОРОННЕМУ ПРАВИЛУ (ИНТЕГРАЦИЯ 2026-07-31, три пустых
  замера подряд в одной правке): «спрашивай не „что я хочу проверить", а
  „ЧТО ИМЕННО ЛОМАЕТСЯ от этой правки", и меряй ровно это». Страж,
  проверяющий ПОСЛЕДСТВИЕ, у которого есть второй источник, зелен на
  сломанном (страховку занавеса взводил другой путь — латч ни при чём);
  красный тест может «подтверждать» баг, которого не касается (мок-событие
  с неверным именем — ассерт-санитар «событие дошло» обязателен); а
  `false === false` истинно и когда механики нет вовсе — ассертить ПЕРЕХОД
  состояния, не значение.
- ⚠️⚠️ ТРЕТИЙ КЛАСС ЛОЖНЫХ ЗАМЕРОВ: **МЕТРИКА ПРАВДОПОДОБНА, НО МЕРЯЕТ НЕ ТО,
  ЧТО ТЫ НАЗЫВАЕШЬ ВСЛУХ** (ГРАФИКА 2026-07-31, три случая за одну сессию —
  значит не случайность). Он тише двух соседних правил: тут ничего не падает
  и не зеленеет ложно, число просто НЕ ТО, а на вид разумное.
  • СЧЁТ АССЕРТОВ. `grep -c PASS` по логу сьюта ловит и финальную строку
    вердикта `SUITE: PASS` — КАЖДОЕ моё число за сессию было завышено ровно
    на 1 (353 вместо 352, 349 вместо 348, 322 вместо 321). Продержалось ТРИ
    сдачи: цифра выглядела нормально и монотонно росла. Верно —
    `grep -c '^PASS:'`. Поймал диспетчер сверкой с бит-в-бит теми же файлами.
  • ПИКСЕЛЬНАЯ «АСИММЕТРИЯ ПЯТНА» для звёзд ловила в выборку HUD и край кучи
    (пятна радиусом до 64 px) и объявила, что после правки стало ХУЖЕ.
    Метрику выбросил, а не подогнал порог: доказательство осталось на точной
    геометрии и кропе ×8.
  • «РАЗМАХ СЛОЁВ 19.5%» у дневной слоистости мерил САМ ГРАДИЕНТ, а не страты:
    колонка сэмплов шла через весь кадр. Верно — дельта ОДНОГО кадра с
    эффектом и без него.
  ⚠️ ПРИЁМ: проверять не «сходится ли число с ожиданием», а «ЧТО ИМЕННО оно
  считает» — один раз, на маленьком примере, глазами. Все три случая ловятся
  за минуту и ни один не ловится перечитыванием кода.
  ⚠️ И ПАРНОЕ (та же сессия, shell здесь **zsh**): `for x in "a b c"; do cmd $x`
  НЕ разбивает `$x` на аргументы — четыре прогона перебора подряд молча ушли
  на дефолтных параметрах, и едва не родился вывод «правка не помогает».
  Аргументы передавать явно либо `${=x}`.
- ⚠️ МЕТРИКИ С ОДНИМ СЭМПЛОМ НА ПРОГОН НЕ СЕРТИФИЦИРУЮТ РЕДКОЕ СОБЫТИЕ
  (ФИЗИКА, стены v220): счётчик спасений на двух коротких прогонах С
  ЧЕРЕДОВАНИЕМ вариантов дал ПРОТИВОПОЛОЖНЫЙ порядок (0.9 против 2.2 и
  4.38 против 2.63) — телепорт зависит от траектории→dt→загрузки машины.
  Чередование вариантов внутри сида ОБЯЗАТЕЛЬНО (без него сравниваются
  условия, не варианты), но его одного мало: редкие события сертифицирует
  СОАК (сотни сэмплов) либо РАСПРЕДЕЛЕНИЕ по всем предметам вместо
  максимума. maxWallExcess болен тем же. Физика на этом ЗАТОРМОЗИЛА сдачу
  вместо выбора удобного из двух прогонов — эталон поведения.
- ⚠️ ДВЕ ЛОВУШКИ ОБНОВЛЕНИЯ БРИДЖА (ИНТЕГРАЦИЯ, 2.0.2, 2026-08-01):
  (1) GitHub Releases у playgama/bridge показывает ТОЛЬКО 2.0.0 — патчи
  живут в npm и ТЕГАХ; проверка «по странице релизов» даёт уверенное ложное
  «мы на последней». Сверять по тегам/npm. (2) Зелёный сьют ≠ смоук
  совместимости SDK: бридж-секции гоняют МОКИ, живой вендорный файл в них
  не участвует — совместимость проверяется эмпирической сверкой поверхности
  (обе сборки в браузер, словари констант/методов бит-в-бит) + живым A/B на
  ?platform_id=playgama. Диспетчер ошибся ровно этой фразой — не повторять.
  ⛔ И МИНА 2.0.2: ключ `advertisement.interstitial.autoShow` в конфиге
  заставляет SDK САМОМУ показывать межстраничную по PLATFORM_MESSAGE — мимо
  нашей каденции и тихой паузы. У нас ключа НЕТ и НЕ ДОЛЖНО ПОЯВИТЬСЯ без
  слова диспетчера.
- ⚠️ ПОДВИД: СТРАЖ, ЧИТАЮЩИЙ КЭШИРОВАННОЕ СОСТОЯНИЕ N РАЗ, — ОДНА ПРОВЕРКА
  В ГРИМЕ N (ГРАФИКА, огонь 2026-08-01): extinguishAll не сбрасывал
  burningItem → планировщик после ПЕРВОЙ вспышки не поджигал никогда, а
  страж «спецпредметы не горят» печатал пять срабатываний, читая ОДНО имя
  пять раз — «пять из пяти» звучит убедительнее «одной», потому и опаснее.
  Сьют был ЗЕЛЁНЫЙ на сломанной механике; поймано ЗАМЕРОМ РАЗНООБРАЗИЯ
  (14 вспышек — 1 тип из 129 доступных; после фикса 6 из 14). Правило:
  повторные чтения обязаны наблюдать НЕЗАВИСИМЫЕ события (страж
  разнообразия/смены состояния между чтениями), иначе N замеров = один.
- ⚠️⚠️ СТРАЖ НЕ СДАН, ПОКА НЕ ПОКАЗАНО: ПАДАЕТ НА СЛОМАННОЙ СБОРКЕ И ЗЕЛЕН НА
  ИСПРАВНОЙ. Проверка ОБЯЗАТЕЛЬНАЯ, не подтверждающая (формулировка ИНТЕРФЕЙСА
  2026-07-31, и она точна: правило спасает не тех, кто его помнит, а тех, кто
  ошибся — их первый страж растворения имел неверный порог и БЕЗ двустороннего
  прогона сдал бы красный на собственной исправной ветке). За проект ПЯТЬ
  тавтологичных/флейковых стражей поймано именно этой проверкой, ноль —
  чтением кода. Односторонний прогон «падает на сломанном» не защищает от
  флейка; односторонний «зелен на исправном» — от тавтологии. Нужны оба.
- В тестах закрывать оверлеи перед координатными кликами — они перекрывают
  канвас.
- ⚠️ `bestTapTarget` СЧИТАЕТ ГРУППУ ВОКРУГ КАЖДОГО КАНДИДАТА, а не вокруг
  «якоря подсказки»: `pairMatch` — отношение БЛИЗОСТИ, НЕ класс
  эквивалентности, поэтому у соседа по цепочке набор соседей свой, и «тап по
  любому члену даст тот же матч» — ЛОЖЬ (замер v157: 9 расхождений из 14).
  Отдаёт `n` (что реально снимется, с капом), `raw` (то же до капа) и
  `{n:0, occluded:true}`, если все кандидаты закрыты от камеры.
- ⚠️⚠️ **ИМЕНОВАННЫЕ ФАЗЫ БЕЗ ОСТАТКА ВСЕГДА ВРУТ НА НОВОМ КОДЕ, И ВРУТ ОНИ
  АДРЕСНО: «ОБВИНЁН ТОТ, КОГО ВИДНО»** (ФИЗИКА 2026-08-01, разбор хвоста тапа).
  Разбор, где сумма названных колонок не сверяется с итогом, при появлении новой
  дорогой строки не краснеет — он молча приписывает её тому, кто в таблице уже
  есть. Живой случай: у разбора тапа не было колонки для физволны, неопознанные
  2.2 мс отнесли к `blastWave`, и он полгода числился подозреваемым. Замер: он
  стоит **0.1 мс**, а хвост это эффекты (1.5) и выбор предмета рейкастом (0.9).
  ⛔ ПРАВИЛО: у любой перф-разборки обязана быть колонка `rest` = итог минус
  сумма названных. Пока она мала — искать нечего; выросла — появилась статья,
  и вот тогда её и разбирать. То же и на уровне кадра: `outside` = кадр минус
  вся работа цикла (см. `perfStats().worstFrame`).
  ⚠️ ГРАБЛЯ ПОРЯДКА, НА КОТОРОЙ ЭТО ЛЕГКО СДЕЛАТЬ ПУСТЫМ: в `loop` сначала идёт
  `tapMsTake()`, и он ОБНУЛЯЕТ `tapMs`; читая итог оттуда в `tapPhasesTake()`,
  остаток выходил бы всегда нулём — страж зелен, потому что меряет пустоту.
  Итог передаётся аргументом. Любой «take»-счётчик проверять на порядок вызова.
- ⚠️⚠️ ТЕСТ-ХУК, РАБОТАЮЩИЙ В СТРАНИЦЕ, ОБЯЗАН СТОЯТЬ ВНЕ ОКНА ПЕРФ-ЗАМЕРА
  (ФИЗИКА 2026-08-01, исследование A2). `bestTapTarget` и `findByTex` — это
  СИНХРОННЫЕ рейкасты внутри страницы (`refreshAccessibility` + перебор
  кандидатов + до 5 встрясок), на CPU ×4 они занимают сотни миллисекунд. Вызов
  между `perfReset()` и замером даёт «кадр 260 мс» — и это кадр ИНСТРУМЕНТА, а
  не игры. Замер до исправления: матч 96-98 мс, после переноса цели за `perfReset`
  — 18-27 мс. ⚠️ ДИАГНОСТИЧЕСКАЯ СИГНАТУРА: `raw` большой, а `work` почти ноль
  (солвер 0, постройка 0) — значит игра в этом кадре не делала НИЧЕГО, и время
  съел кто-то снаружи цикла. Смотреть первым делом на свой стенд.
- ⚠️ ПЕРФ ЭФФЕКТОВ МЕРИТЬ ТОЛЬКО НА ПРОГРЕТОЙ СТРАНИЦЕ: первый вызов каждого
  вида платит разовое (ленивая DataTexture `fxDotTex`/`fxStarTex`, компиляция
  шейдерной программы, JIT). Замер: труха помола 3.36 мс на холодной против
  1.02 на прогретой — завышение ВТРОЕ. У игрока страница прогрета первым матчем.
- ⚠️ ТЕСТОВЫЕ КЛИКИ ПО КООРДИНАТАМ — ТОЛЬКО ЧЕРЕЗ `visiblePixel(it, ctx)`
  (99-main): и `findByTex`, и `bestTapTarget` подбирают пиксель, где предмет —
  ПЕРВОЕ пересечение луча (центр + 8 смещений на 0.55·r). Проекция голого
  центра дважды рожала флейки: v76 (клик уходил в загораживающий предмет,
  «−20» вместо «+120») и v157 (клик ассерта капа детонировал БОМБУ до секции
  бомбы — три ассерта падали через прогон). Новые тест-хуки с экранными
  координатами писать так же.
- `window.__game`: alive, availablePairs, autoMatch, shake, cfg, regen,
  level, stats, levelNum, topY, leaveSingles, adsMode, scanNaN,
  forceRefresh, accessibleList (индексы!), typesSnapshot (по типам:
  живых/доступных), maxWallExcess, skipIntro; диагностика регрессий:
  awake() (physAwake/sinceWakeMs/maxV), psLog() (журнал сна/пробуждений),
  floaters() (зазор>0.35 под нижней точкой; ⚠️ поле луча timeOfImpact, НЕ
  toi — с toi конечные зазоры молчали, латентный баг найден соаком
  2026-07-20; «мост» — плоский предмет концами на соседях — даёт зазор при
  contacts>0 и НЕ баг; баг только sleeping:true при contacts:0),
  contacts(i) (пары/касания нарровой фазы; живут и на спящей куче),
  perfStats() (p95 кадра/шага физики + счётчики утечек: тела/коллайдеры/
  сцена/FX/геометрии/DOM/куча — для соака и замеров на устройствах),
  accFlips() (мигание вуали), place(i,x,y,z) (телепорт для постановки сцен).
- ⚠️ ГРАБЛЯ Rapier для тестов: после place()/setTranslation query pipeline
  (castRay) видит старые позиции коллайдеров до world.step() — place()
  сам зовёт propagateModifiedBodyPositionsToColliders(); если телепортируешь
  тела иначе — прокачай pipeline, не то лучи бьют по фантому.
- Исторический флейк `computeBoundingSphere: NaN` относился к самописной
  физике (гвард удалён вместе с ней); `__game.scanNaN()` оставлен для
  ручной проверки. На Rapier не воспроизводился.
- В headless уровень растёт по ходу теста (победа в полном прогоне даёт
  level 2 последующим секциям) — ожидания в логах это учитывают.

## Отвергнутое (НЕ возвращать без запроса владельца)

- ВЕСЬ ДЕКОР ДНЕВНОГО НЕБА (страты v213 + облачные пятна с параллаксом v215).
  Вердикт владельца дословно: «верни все к обычному градиенту, так как
  получилось не очень», уточнение — «я только про день, ночь со звездами
  оставь». Откачено одним пакетом: `b5352ba` (слоистость, CIRRUS_*, uCirrus)
  и `28c978a` (пятна, CLOUD_*, uCloud/uCloudRot) вместе с драйверами и
  ручками `__game.cirrus()`/`__game.clouds()`. День — ЧИСТЫЙ многостопный
  градиент палитры владельца. НОЧЬ НЕ ТРОГАТЬ: звёзды-«искра», приглушённый
  хвост (вариант C) и вся ночная палитра остаются в игре по прямому слову.
  ⚠️ Не читать как «декор не получился технически» — обе фичи работали и
  были замерены; владельцу не понравился РЕЗУЛЬТАТ. Приёмы (сдвиг рампы
  в минус, огибающая у кромок ради полос Safari, «нет неба над горизонтом»)
  сохранены в разделе «ДВА ИНВАРИАНТА НЕБА» — они понадобятся любому
  будущему декору дня, если владелец его снова захочет. Код — в истории
  git, возврат = `git revert` отката.
  ⚠️ ТУДА ЖЕ, НО ВЛАДЕЛЬЦУ НЕ ПОКАЗЫВАЛОСЬ: «тёплый низ» и «тень проходящего
  облака по куче» были следующими в той же очереди и отменены тем же словом.
  По тени успел накопиться замер, который стоит знать: ГЛОБАЛЬНОЕ затемнение
  предметов НЕВИДИМО на безопасной силе (при 14% средняя дельта 9 из 255,
  кадры «с тенью» и «без» неразличимы) — читается только ДВИЖУЩИЙСЯ КРАЙ
  плюс сдвиг ТОНА в холод. И отдельно: привязывать тень к реальным пятнам
  неба НЕЛЬЗЯ — перебор полного оборота показал, что пятна проходят над
  кучей 8% времени при phi 0.45 и НИ РАЗУ при phi 0.32 и 1.35, то есть тень
  зависела бы от НАКЛОНА КАМЕРЫ (ровно та жалоба, из-за которой доступность
  увели с лучей от камеры).
- РЕАЛИСТИЧНАЯ «ГРЯЗНАЯ» СТИЛИСТИКА ПРЕДМЕТОВ (пак Dirty, 97 моделей с
  личными фототекстурами) — живой тест v193: 5 предметов (бургер/
  огнетушитель/D20/камера/бутылка) в базовой девятке ур.1, скрин владельцу.
  Вердикт дословно: «выглядят сильно инородно и грязно». Снято в v194.
  Ассеты лежат в «3d assets/Dirty», генератор извлечения — в истории коммита
  v193; в пул не предлагать.
- PBR-материалы clearcoat/дерево/камень с текстурами; жёсткий toon
  с обводкой; мягкий toon; сатиновый металл (metalness 1 / roughness 0.8).
- Обводка предметов (инвертированная оболочка) — «слишком грубая».
- Умножение цвета для недоступных.
- Попап подтверждения встряски (в обеих версиях: раз-за-уровень и каждый раз).
- Светлый фон (заменён чёрным по требованию).
- WALL_EXT (расширение стенки над кромкой) — предметы выходили за пределы.
- Чётное ограничение групп матча (сироты теперь легальны, их ест финал).
- Доступность лучами ОТ КАМЕРЫ и/или лучом В ЦЕНТР предмета — оба корневых
  бага владельца (тор «матчится с одного ракурса», вуаль «тускнеет от
  ракурса»); только вертикальные лучи по сэмплам физформ.
- Сэмплы доступности с РЕНДЕР-геометрии — расходились с физколлайдерами
  (спираль/узел: ложно недоступные одиночки); только из физформ
  (buildAccessSamples).
- Transmission у стекла (двойной рендер мира, ~55% кадра) — только opacity.
- Форс-сон физики по чистым часам без гейта скорости (замораживал падающий
  столб — «предметы висят в воздухе»; сон только при maxV<2 и не в интро).
- Трим/topY0 по НЕосевшей куче (тихо удалял до 16 предметов из замороженного
  столба) — только через pendingTrim/finalizeFill при штиле.
- Телепорт спасателя на верх чаши (виден как «прыжок») — только локально.
- Мгновенная вуаль без tickVeil-лерпа («цвета скачут»).

## СОСТОЯНИЕ ПАПКИ «3d assets» ПОСЛЕ ЧИСТКИ 2026-08-17

Слово владельца: «собери 3д модели, которые используются в игре, в отдельную
папку, внутри по папкам. Те, которые мы убирали, удали совсем» + «вычищай».

**`3d assets/_В игре/` — ВСЁ, ЧТО РЕАЛЬНО В ИГРЕ**, по папкам: Звери 24,
Еда 35, Машины 12, Праздник 5, Кирпичи 3, Пиратское 3, Машинки 3, Завод 1,
Выживание 1, Лес 1 = **88 моделей, ровно 88 типов пула**. В каждой папке лежит
и `colormap.png` пачки.
⛔ ПАПКА «Камни» СНЯТА 2026-08-17 вместе с механикой камней, исходники
`rocks-a.glb`/`rocks-sand-c.glb` удалены и из `Pirate/Предметы`. Спецпредметов
в выгрузке больше нет вовсе: клад берёт геометрию рыбки из пачки зверей, бомба
процедурная. Прежние «90 файлов» и «11 атласов» — историческое число.
⚠️ АТЛАСЫ СВЕРЕНЫ НЕ НА ГЛАЗ: md5 каждого сравнён с тем, что ЗАШИТ в сборку —
11 из 11 совпали. Попутно это подтвердило, что камни красятся ПИРАТСКИМ атласом.
⚠️ КОНТРОЛЬНАЯ СУММА КАРТЫ: в `.lowpoly` было ровно 120 файлов = 88 живых + 32
вырезанных, и у КАЖДОГО из 88 типов файл нашёлся. Ни одного «типа без модели».

**УДАЛЕНО СОВСЕМ (417 файлов, ~26 МБ):**
- 32 вырезанных типа — из `.lowpoly` и оригиналы тех же имён в своих пачках
  (56 файлов). Список совпал с каноническим один в один;
- пачки, не участвовавшие в игре: `Dirty` (забракован владельцем), `LEV`
  (дублирующая библиотека), `Test`, `Blaster`, `Dino`, `Pickups`, `skyboxes`,
  `Arcade`, `Market`. В `3d assets` остались РОВНО десять пачек-источников
  живых типов плюс `_В игре`.

⚠️⚠️ **ЦЕНА, КОТОРУЮ НАДО ЗНАТЬ ДО СЛЕДУЮЩЕЙ ПЕРЕГЕНЕРАЦИИ.** Геометрия
вырезанных типов ПОКА ЖИВА в `36-models.js`/`38-kenney.js` (проверено:
`M_SURVIVALCHEST_POS`, `M_PIRATECANNON_POS` на месте), поэтому правило «вернуть
тип = одна строка в TYPES» СЕГОДНЯ ещё верно. Но генератор «ВСЁ ИЛИ НИЧЕГО»:
первая же перегенерация перепишет модуль из папок, и вырезанные исчезнут
окончательно — вернуть их будет нечем. **Собираешься регенерировать модули —
сперва убедись, что ни один из 32 не понадобится.**
⚠️ Пачки `Arcade` и `Market` УДАЛЕНЫ ТОЖЕ (вторым словом владельца «убери
Arcade и Market тоже», ещё 40 файлов / 1.6 МБ): все их типы были вырезаны, в
`.lowpoly` не осталось ничего. ⚠️ Следствие для будущей перегенерации:
`38-kenney.js` больше нельзя собрать с атласами `arcade`/`market` — но ни один
из 88 живых типов их и не использует (проверено по полю `tex`), так что
регенерация оставшихся пачек законна.
⚠️ `Animals/Archive-2026-08-13` (3.1 МБ) ЦЕЛ — это единственный бэкап ПРЕЖНЕЙ
партии зверей, снос сделал бы замену необратимой.
⚠️ Сборка после всей чистки НЕ ИЗМЕНИЛАСЬ НИ НА БАЙТ: геометрия зашита в
модули, `build.py` ассеты не читает. `3d assets/` в `.gitignore` — коммитить
там нечего, и БЭКАПА У НЕГО НЕТ.

## Пайплайн 3D-моделей предметов

РЕАЛИЗОВАН и обкатан на модели владельца «стейк» (тип `steak`, TYPES[5]):

- Формат: .obj+.mtl принят и работает (.glb тоже ок). Полигонаж малый —
  у стейка 144 треугольника (на экране предметов до 141, экономно).
- Конвертация НА ЭТАПЕ РАЗРАБОТКИ, не в рантайме (однофайловый index.html,
  никаких лоадеров): python-скрипт читает OBJ (n-гоны — веером в
  треугольники), центрирует, нормализует под охватный радиус rc, пишет
  data-модуль `src/app/NN-имя.js`: POS (Float32Array, non-indexed),
  пер-вершинные ЦВЕТА из Kd материалов MTL (sRGB -> linear), фабрика
  `имяGeo()` -> BufferGeometry + computeVertexNormals (плоская фасетка —
  стилизации подходит).
- Материал в makeItem — ветка `mat:'model'`: БЕЛЫЙ MeshStandardMaterial
  + vertexColors (metalness 0, roughness 0.18, env 0.5 как soft). Белая
  база обязательна: серая вуаль недоступности умножается на вершинные
  цвета. Труха/пыль (dissolveFX/bladeDustFX) берут `item.fxColor`
  (усреднённый цвет типа в linear) — baseColor у моделей белый.
- Физика: convex hull из рендер-геометрии (ветка default createItemBody)
  подходит почти-выпуклым моделям; сильно вогнутым — компаунд вручную
  (как тор/чайник). Сэмплы доступности hull-типов строятся автоматически
  (центроиды граней ×0.6). Прозрачные предметы — opacity, НЕ transmission.
- Владелец может редактировать Kd в .mtl — цвета подтянутся при
  переконвертации.
- ⚠️⚠️ **ОХВАТНЫЙ РАДИУС ПРИ ЗАМЕНЕ МОДЕЛИ МЕНЯТЬСЯ НЕ ДОЛЖЕН — ЭТО ИНВАРИАНТ
  ГЕНЕРАТОРА, И ПРОВЕРЯТЬ НАДО ЕГО.** `glb2module.py` нормирует КАЖДУЮ модель под
  `RC = 1.00`, поэтому полигонаж, сварка вершин и снятые атрибуты на `item.r` не
  влияют по построению. Сверка новой пчелы (2026-08-11), два независимых способа:
  охват прямо по массиву `M_ANIMALBEE_POS` — **1.0001 до и 1.0001 после** при
  вершинах 1132 → 479 и том же габарите 1.180 × 1.768 × 1.144; в игре на ур.10
  все 14 пчёл дают `item.r` **0.62** в обеих сборках. Дельта 0.0%.
  **Мерить всё равно надо** — но как проверку инварианта: поехал радиус, значит
  массивы правили руками мимо генератора или сменился сам генератор.
  ⚠️⚠️ **И МЕРИТЬ ДЕТЕРМИНИРОВАННО, ИНАЧЕ ЗАМЕР ВРЁТ. МОЯ ОШИБКА, СТОИВШАЯ
  НЕВЕРНОГО ПРАВИЛА В ЭТОМ ФАЙЛЕ:** я снял по ОДНОЙ пчеле на ур.20, получил
  0.549 против 0.52 и записал «−5%, питч звука поднялся на 2.7%». Ни того, ни
  другого нет. С 16-го уровня `levelSize` даёт разброс размеров ±(10 + 4·(ур−15))%
  СЛУЧАЙНО НА ПРЕДМЕТ (близнецы пары — одним размером), и на ур.20 в одной куче
  живут пчёлы 0.508 / 0.603 / 0.635. Одиночный сэмпл сравнивает не МОДЕЛИ, а два
  случайных размера. Верно: уровень ≤ 15 (`SIZE_UNIFORM_LEVELS`, там размер один
  для всех) либо прямо по массиву модуля — оба способа детерминированы.
  ⛔ Ошибку нашла ГРАФИКА разбором моей сдачи, и это тот же закон, что у нас
  записан про соак: редкое/разбросанное событие сертифицирует РАСПРЕДЕЛЕНИЕ, а не
  один сэмпл. Правило «мерить и называть дельту вслух» осталось — заменены его
  числа и способ замера.
  ⚠️ Что при замене модели проверять ОБЯЗАТЕЛЬНО, кроме радиуса: атлас
  ПОПИКСЕЛЬНО (у новой пчелы текстура лежит ВНУТРИ glb и байтами отличается — по
  одному MD5 вывод был бы ПРОТИВОПОЛОЖНЫМ и неверным; 0 отличий из 262 144) и
  семейство кольца удара (`ringFams`) — оно выводится из ПРОПОРЦИЙ, и у
  «похудевшей» модели овал молча стал бы кругом.

## Архитектурные решения

- docs/TECH-REVIEW-2026-07.md — переоценка стека по запросу владельца
  (вердикт: ОСТАВИТЬ three r149 + Rapier + один файл; ~1.0 МБ gzip против
  лимитов порталов 8-50 МБ; план апгрейда three на модули ПОСЛЕ релиза;
  портируемость: Bridge = 28 платформ, Telegram Mini Apps, Capacitor).
- docs/ADR-001-physics-engine.md — анализ двух фундаментальных проблем
  (физика объектов и их взаимодействие) и выбор основы для перехода
  прототип → игра. Рекомендация: three.js + Rapier (WASM); Godot отклонён
  из-за веса web-экспорта (30-50 МБ) и COOP/COEP-требований, которыми не
  управляем на чужих порталах. СТАТУС: ждёт решения владельца — до «да»
  физику не переписывать.

## Открытые вопросы / дальше

1. Плейтест на телефоне: кривая прогрессии (типы/терпение миксера),
   баланс очков, ощущение эндшпильного ∞-радиуса. Экономика встрясок
   на ур.1 сходится (боту 2-3 при 5 доступных).
2. (закрыт 2026-07) Transmission-стекло вырезано по замерам аудита.
3. Playgama Bridge подключён; не проверено на живом портале (нужна заливка
   на developer.playgama.com и smoke-тест rewarded). Bridge также даёт
   storage/leaderboards/interstitial — пока не используем.
4. Генерация с гарантией решаемости без встрясок — не делалась.
5. Мета сверх уровня: коллекция сюрпризов, рекорды очков — не делалась.

## v2-процесс 2026-08-02: главное дерево v2-клона — диспетчеру

Пойман инцидент: направление создало ветку в клоне «Blendo v2» и
ПЕРЕКЛЮЧИЛО общий чекаут — коммит диспетчера `git add -A` уехал на чужую
ветку вместе с чужими незакоммиченными правками (расплетено reset'ом и
разносом патчей). Правила с этого дня:
- **Главное дерево «Blendo v2» — только ветка v2 и только диспетчер.**
  Сервер владельца (localhost:8781) отдаёт index.html главного дерева —
  чужая промежуточная сборка там показала бы владельцу недоделку.
- **Направления в v2 работают ТОЛЬКО в воркри** `.claude/worktrees/<имя>`
  (как Графика в v1), node_modules — симлинк (правило выше), `.claude/`
  в .gitignore.
- **Адресный `git add` обязателен ВЕЗДЕ, не только в релизах v1.**
  `git add -A` в общем дереве — это и был корень инцидента; перед любым
  коммитом в общих деревьях — `git status` на предмет чужих правок.

## Стендовые уроки 2026-08-03/04 (сводка сдач Интерфейса/Физики/Интеграции)

- **«Флаг о поведении ≠ поведение: двусторонность проверяй по СЛЕДУ, не по
  флагу»** (Физика, гейт спасателя): при диверсии страж флага остался зелёным
  — флаг выставлялся, а спасатель работал. Ловит только поведенческий счётчик
  (след в консоли/метрике). Пару «флаг + след» держать целиком.
- **«Ломай ВСЕХ держателей свойства»** (Интерфейс, глаза): диверсия легла, а
  свойство выжило — его держали ТРИ независимых пути; сломав один, не сломал
  ничего, и это читалось как «страж слепой». Перед диверсией — grep по флагу
  (не по строке), ломать разом всех держателей.
- **«Диверсии протухают вместе с боевой строкой»** (Интерфейс): patch-regex
  молча перестал совпадать после фикса — прогон прошёл по ЗДОРОВОЙ сборке и
  дал пять зелёных. Маркер в каждом патче + `grep -q` маркера перед прогоном.
- **«Осадка ждёт ФАКТ, который ассертит, а не стабильность»** (Интерфейс):
  «два одинаковых замера подряд» принимали «ещё не началось» за «уже
  кончилось» — синхронный залп блокирует главный поток, переход стоит.
- **«level/stats в v2 — ФУНКЦИИ»**: `g.stats.lastAction = x` молча пишет в
  объект-функцию; правильно `g.stats().lastAction`. Симптом маскируется под
  поломку исправной механики.
- **«Симптом ≠ механизм»** (Физика, занавес): опыт с инертным балластом
  (+2.3КБ комментов → красный) верно нащупал ось «вес сборки», но диагноз
  «чувствителен к размеру» лечил бы не то. Механизм: больше index.html →
  сервер сьюта (node в том же процессе) отдаёт мок дольше → страховка
  стреляет ДО существования window.bridge. Лечение по построению: мок —
  через page.addInitScript ДО любого скрипта страницы, сервер отдаёт
  пустышку. Приём «чистая база + балласт того же объёма» — канонический
  способ отделить содержание правки от её веса, с оговорками: сперва
  исключить причинность (порядок секций в логе), формулировать симптом,
  механизм — за владельцем зоны.
- **«Метку дедупликации ставь ДО буфера»** (Интеграция, дубль crash):
  пометка после ev() опоздала — автофлаш по 12 событиям унёс запись раньше.
- **«Ловушка постановки: фича глушит соседнюю механику во всех следующих
  ассертах»** (Интеграция, платежи-vs-интерstitial): секции, включающие
  долгоживущее состояние (окно «без рекламы»), — в конец страницы, как камни.

## Правило ребейза: сверять СБОРКУ, а не факт запуска build.py

Урок Повествования 2026-08-05. У `index.html` стоит атрибут `-merge`, поэтому
при конфликте git оставляет ОДНУ сторону целиком — без маркеров. «Разрешение»
через `git add` после ребейза внесло в коммит чужую сборку, где правок автора
не было вовсе, и это молчит: `build.py` отработал, статус чист, сьют зелен
(его секции ещё не написаны).
**Обязательный шаг после ребейза/мержа: `grep -c <своя_новая_функция> index.html`
и сверка с числом в `src/`.** «Сборка прошла» — не доказательство, что в ней
твой код. Побочно: после вывода `node_modules` из-под версии симлинк исчезает
в свежих воркри — пересоздать перед сборкой.

## Эффекты: цена живёт в ОБЪЕКТЕ, и эффект в куче не виден (уроки 2026-08-05)

- **«Гуще» почти бесплатно, «ещё один носитель» — платно.** Замер Графики:
  труха +290 частиц = +0.07 мс, потому что фракция — ОДИН `Points`, и цена
  постройки в объекте, а не в частице. Исключение — МЕШИ: осколки 0.021 мс
  за скол (15→21 = +0.13 мс на залп). Отсюда правило усиления эффектов:
  сперва растить густоту существующих фракций, и только потом добавлять слой.
- **Эффект, рождённый ВНУТРИ кучи, может не существовать для игрока.** Удар
  строится в точке тапа: с `depthTest:true` кольцо радиусом до 2.5 закрыто
  предметами целиком — время тратится, картинки нет, а хук честно
  показывает «эффект построен». Лечение: `depthTest:false` + `renderOrder`.
  Тот же закон, что у молний и трещин: белёсое на светлом фоне тонет —
  цвет насыщенный, кольцо тонкое.
  ⚠️ Ловится только съёмкой с ЗАМЕДЛЕНИЕМ (боевые 260 мс headless-скриншот
  не поймает) и диагностическим цветом, а не ассертом на факт постройки.

## Диверсия обязана бить в СВОЙСТВО, а не в его соседа (Графика, 2026-08-05)

Первая диверсия детерминизма форм кольца ломала КЭШ `RING_FAM`, а не чистоту
функции: результат не менялся, страж законно оставался зелёным — и это
читается как «страж слепой». Красное получено, только когда случайность
внесли В РЕЗУЛЬТАТ. Перед выводом «страж не ловит» проверь, что диверсия
изменила именно ту величину, которую страж утверждает.

Там же — ОТМЕНА собственного правила часом раньше: «кольцо удара обязано
быть тонким» заменено на «массу добираем ШИРИНОЙ при сниженной плотности»
(слово владельца «шире и прозрачнее»). Условие видимости живо в другой
форме: насыщенность тона обязательна, белым массу не набирать.

## Дубль ключа в `__game` молча съедает хук (ловля 2026-08-05)

`__game` — один объектный литерал на всю игру, и ДВА определения одного
имени не конфликтуют: побеждает последнее, первое исчезает без ошибки.
Так столкнулись мой `itemsBrief` (геометрия: x/z/acc/key) и Физикин
(диагностика пола: low/pen/sleeping). Страж читал `undefined` и выдавал
ПРАВДОПОДОБНЫЕ нули («доступных 0» при 10 живых), то есть отладочная
ручка врала ровно так, как труднее всего заметить.
**Правило: перед добавлением ручки — `grep -n "имя(" src/app/99-main.js`;
имя ручки принадлежит зоне, как файл.** Развели: `itemsGeo` (геометрия,
диспетчер) и `itemsBrief` (физдиагностика, Физика).

## Страж перехода: порядок фаз решает (Графика, 2026-08-05)

Ассерт «после обычного матча всплеска НЕТ» в порядке «обычный → горящий»
сравнивал `null` с `null` — истинно и на сборке, где эффекта нет вовсе.
Верный порядок: сперва событие, которое ДОЛЖНО дать след (снимок появился),
затем событие, которое НЕ должно (снимок не обновился) — проверяется ПЕРЕХОД,
а не значение. Диверсию «эффект на каждом матче» ловит только такая версия.

Там же: **вертикаль в этой камере съедена перспективой** — эффект, растущий
столбом, читается комком у точки тапа. Раскрывать вширь (та же причина, по
которой кольцо удара — билборд). И: **жизнь эффекта нельзя растягивать
константой** — движение параметрическое от `t = k·life`, куски улетят в
другую точку; для показа замедлением есть общий делитель часов `CFG.fxSlow`.

## Разлёт на GPU и три урока измерения (Графика, 2026-08-05)

Техника взята из примера akella (второй пример, bobbyroe, — это то, что у нас
БЫЛО): куски слиты в ОДНУ геометрию, принадлежность записана в вершины,
разлёт гонит вершинный шейдер по одной юниформе. Кадр взрыва 5.0 → 2.8-3.1 мс,
объектов сцены +30 → +1, кусков 30 → 162. Форма скола — ячейки Вороного в
развёртке (шов по кругу закрыт копиями центров), 13 крупных пластин + 150
крошек вокруг точек удара: «сетка слишком стерильная» — слово владельца.

Уроки (все — ошибки, найденные проверками, а не чтением):
- **Игровые часы против реальных.** Сбор шёл по игровым часам, удаление — по
  `setTimeout`: на 150 мс матча разницы нет, на 620 мс сбора часы расходятся,
  и под нагрузкой полного сьюта куча исчезала, не долетев (радиус 1.73 вместо
  0). В изоляции не воспроизводилось. Долгие последовательности — на реальных
  часах, как хлопок матча.
- **Лазейка в ассерте:** `радиус < X || живых === 0` истинно при любом
  поведении — к моменту замера предметы уже удалены. Условие «или пусто» в
  ассерте почти всегда дыра.
- **Метрика читала не то:** радиус считался по физическим координатам
  (`itemsGeo`), а слёт двигает МЕШИ при уничтоженных телах.
- **`centroid` — зарезервированное слово GLSL**: шейдер с таким атрибутом не
  собирается, а three печатает ошибку в строке СВОЕГО префикса.
- **Флаг для кэша заводить свой** (`sharedFx`), а не вешаться на чужой
  (`keepGeo`): по нему чужой страж считает половины распила поимённо.

## Перф-число без ЛИНЕЙКИ — не число (2026-08-05)

Разлёт чаши: Графика дала «5.0 → 2.9 мс», независимая перепроверка дала
«1.3 → 0.7 мс». Обе верны — первая снята при CPU-троттлинге ×4 (наш прокси
мобильного ядра), вторая без него; отношение ровно ×4. Полдня спора и почти
отозванное владельцу число.
**Правило: в любой перф-таблице рядом с числом обязана стоять линейка**
(троттлинг, headless/GPU, что именно обёрнуто замером). И называть ЧТО
измерено: «постройка внутри вызова» ≠ «кадр, в котором это впервые
рисуется» — второе меряет ещё и растеризацию с компиляцией шейдера.

## Ломается не механика, а способ её наблюдать (Графика, три подряд)

За одну неделю три одинаковых по природе ошибки — все в ИНСТРУМЕНТЕ, не в
игре, и все давали ЗЕЛЁНЫЙ на сломанной сборке:
1. лазейка в ассерте (`радиус < X || живых === 0` — истинно всегда);
2. метрика по физическим координатам, когда двигаются МЕШИ при уничтоженных
   телах;
3. страж мерил ТЕЛЕПОРТ вместо полёта: правка сама переставляла `mesh.position`
   в центр, и ассерт «клад летит со всеми» проходил даже при диверсии «клад
   вынут из слёта».
**Проверочный вопрос перед сдачей стража: что именно я наблюдаю — свойство
или его подделку, созданную моей же правкой?** Красный обязан появляться,
когда механизм выключен, а не когда изменилась форма записи о нём.

## Три ловушки пиксельного замера (Графика, звёзды 2026-08-05)

Все три дали ЛОЖНЫЙ ответ до исправления — при работе со скриншотами
проверять каждую:
1. **Разбор ловит фон, а не объект.** «Звезда» площадью 5057 px оказалась
   градиентом неба. Фон вычитать ПОСТРОЧНО (небо светлеет книзу) и
   отбрасывать пятна больше разумного порога.
2. **Красная угроза помола красит ВЕСЬ кадр** и набегает за секунды простоя:
   все пятна ходят хором, и вывод получился ПЕРЕВЁРНУТЫМ («без пульса»
   размах больше, чем «пульс у всех»). В любом замере длиннее секунды —
   держать миксер спокойным (`stats().lastAction`) и нормировать яркость
   пятна на медиану пятен ТОГО ЖЕ кадра.
3. **Статистику подбирать лесенкой, а не угадывать.** Медиана размаха почти
   глуха к доле пульсирующих (0.306 при 0 против 0.569 при 1.0), а на боевых
   10% вовсе ниже нулевого варианта. Разделяет доля «глубоко гуляющих»:
   0% / 2.5% / 22.5% / 55% / 77.5% при долях 0 / 0.1 / 0.3 / 0.6 / 1.0.
   ⚠️ И честная граница: страж проверяет ТРАКТ, боевые 10% на 40 видимых
   пятнах от шума неотличимы — саму долю ассертить константой.

## Лечение бывает не там, где болит (Физика, докидка 2026-08-07)

Замер: на ур.40 докидка спавнит партнёров до высоты 67.4 и получает 23
спасения — но стены и пол чисты, значит спасатель ловит не вылет, а ВЫСОТУ
(`it.p.y > 60`). Предложенное самой Физикой лечение «зажать лесенку высоты»
оказалось РЕГРЕССИЕЙ ВЧЕТВЕРО: кап 30 → 37 спасений, кап 10 → 102. Решает
ПЛОТНОСТЬ спавна, а не высота: ниже потолок — гуще слой — сильнее толкаются.
Сделано наоборот: поднят потолок спасателя (`RESCUE_CEIL = 90`), докидка не
тронута; ур.40 — 23 → 2 спасения.
**Правило: прежде чем чинить по своей же гипотезе, померь ОБА направления —
лечение может усилить болезнь.** И страж сверяется с ХУКОМ (`rescueCeil()`),
а не с литералом: иначе он разъедется с кодом при следующей правке числа.

## Потолок — не страж границы (Интерфейс, колонки 2026-08-07)

Диверсия «граница 421 → 472» уронила ровно ассерт перехода, а ассерт потолка
(«не больше 4») остался ЗЕЛЁНЫМ: 600 и 799 больше 472, четыре колонки там как
были. Отсюда правило: **проверка «значение не превышает N» НЕ заменяет проверку
ГРАНИЦЫ** — границу держат два ассерта по обе стороны названного числа.
Формулировка «столбцов стало больше» была бы зелёной при ВСЕХ трёх редакциях
владельца (360 / 380 / 420), то есть как страж числа она пустая.

Там же: **auto-fill не выражает произвольную лесенку.** Из «359→2 и 360→3»
следует ровно `min=104`, но тогда четвёртая колонка приходит с 472, а нужна с
421. Когда владелец называет ДВЕ границы — ставим прямые медиазапросы:
правка становится заменой числа, а не пересчётом полей и gap.

## Разлёт чаши — про него обязан знать КАЖДЫЙ потребитель геометрии

Соак дал 7 тревог «за стеной» и 21 «под полом» — все 21 в ОДНОМ сэмпле, с
`pen: null` и нулём контактов: дно и стены в разлёте СЕНСОРЫ, и инварианты
мерили расстояние до чаши, которой в тот момент нет. Тот же класс, что был у
спасателя (`rescueSweep` без гейта `bowlOpen`).
**Правило: любой код, считающий чашу существующей, обязан гейтиться на окно
разлёта.** Сделано: спасатель (50-physics), соак (`bowl` в сэмпле —
НЕСУЩЕЕ поле, плюс `bowlSkipped` в сводке; тревог 7 → 2, underHits 21 → 3).
⚠️ Третий кандидат на проверку — стражи сьюта, меряющие геометрию кучи.

## Базис соака 2026-08-07 (линейка: headless на GPU через angle=metal, БЕЗ CPU-троттлинга, 12 мин, Hard)

сид 101 / сид 202: шаг физики p95 3.7 / 3.5 мс (max 5.6), кадр p95 79.6 / 75.8
(max ~107), wallExcess p99 0.321 / 0.146, спасений 94 / 51, подъёмов пола 24 / 0,
куча +2.1 / +4.9 МБ, ошибок 0.
⛔ **Со старыми соак-числами НЕ сравнивать**: между ними изменился ОБЪЁМ
РАБОТЫ (прогрессия размера уровня 130 → 82 на ур.1 и фикс `lastAction`, из-за
которого раньше в ботах параллельно шёл idle-помол). Это новый базис.

## Слепое пятно по построению и два диагноза диверсии (Интеграция, сервер 2026-08-07)

- **«Тестов было 24, и они не покрывали этот путь ПО ПОСТРОЕНИЮ».** Ошибка в
  расчёте места (+1 у всех ниже сотого) жила на ветке «через корзину», а на
  маленькой базе снимка нет, `bound === null`, и ветка не исполняется вовсе.
  Правило: если код ветвится по РАЗМЕРУ данных — тестовый сид обязан
  дотягиваться до обеих ветвей (здесь: 50 000 строк, места 1/100/101/25037/
  49999 сверяются поимённо).
- **Диверсия, развалившая сборку, — НЕ доказательство слепого стража.** Патч с
  синтаксической ошибкой ронял прогон целиком, и отчёт читался как «страж не
  ловит». `break.js` теперь различает «красный ассерт» и «сборка не собралась».
- **Тавтология в страже скрытия:** ассерт «скрытого нет в таблице» проверял имя
  `Cheater`, а помощник слал имя по умолчанию — `UPDATE` переименовывал строку,
  и условие было истинно при любом поведении.
- **Правило, наказывающее не того:** флаг скрытия сделан НЕ липким, потому что
  возрастной потолок меряет возраст СТРОКИ, а не игрока — вернувшийся после
  чистки кэша пробивал бы его первой победой и исчезал навсегда.

## Зонд без конвейера и пустой замер (Физика, пончик 2026-08-07)

- ⚠️⚠️ **СВЕЖЕСОЗДАННЫЙ КОЛЛАЙДЕР ЕЩЁ НЕ В QUERY-КОНВЕЙЕРЕ RAPIER** — он
  обновляется шагом мира. Первая версия зонда дырки пускала ЛУЧ вдоль оси и
  объявила «дырка есть» У ВСЕХ, включая заведомо сплошные модели: луч не
  встречал НИКОГО. Зелёный-на-всём зонд, который легко принять за
  доказательство. Лечение: спрашивать саму ФОРМУ (`collider.containsPoint`) —
  ей конвейер не нужен. Родственно старой грабле `place()` (там лечение —
  `propagateModifiedBodyPositionsToColliders`).
- ⚠️ **КОНТРОЛЬ В СТРАЖЕ ОБЯЗАТЕЛЕН, А НЕ ЖЕЛАТЕЛЕН:** зонд, который проверяет
  только целевой предмет, ПОДТВЕРЖДАЕТ, а не РАЗЛИЧАЕТ. Сплошные модели обязаны
  отвечать «закрыто» тем же вызовом — иначе дефект зонда неотличим от успеха.
- ⚠️⚠️ **ПУСТОЙ ЗАМЕР ВЫГЛЯДИТ КАК ХОРОШИЙ.** Перф-замер пончика ставил уровень
  через `localStorage`, тот не применился — намеряно ур.1 с 82 предметами и
  НУЛЁМ пончиков, а таблица чисел выглядела совершенно нормально. Правило:
  уровень задавать хуком (`setLevel` + `regen`), и рядом с перф-числом ВСЕГДА
  печатать, сколько целевых предметов было в куче. Число без счёта предмета,
  ради которого замер делался, — не число (парное к «перф-число без линейки»).

## Мягкая деградация ослепляет проверку по статусу (лидерборд, 2026-08-07)

Наш воркер намеренно НЕ отдаёт 503, когда снимка нет: «таблица — украшение,
игру она не блокирует никогда». Значит признак поломки переезжает ИЗ КОДА
ОТВЕТА В ТЕЛО (`{"r":[],"stale":1}`, `t:0`, `max-age` 30 вместо 60). Смоук
проверял `200 && Array.isArray(r)` — истинно и при мёртвом кроне; репро на
воркере без снимка дал зелёный шаг.
**Правило: где мы сознательно деградируем мягко, проверка ОБЯЗАНА читать
маркер деградации, а не код ответа.** Иначе продуктовое решение «не падать»
превращается в «не сообщать».

⚠️ И парное, о доке: **абзац, заранее объясняющий симптом как норму, — это
выключенный страж.** README писал «пустой топ сразу после деплоя — норма», а
пустой топ ровно и есть единственный видимый признак того, что крон не
заработает НИКОГДА. Объяснение симптома допустимо только вместе с
ПРОВЕРЯЕМЫМ действием, которое отличает «ещё не успело» от «не будет»
(здесь — второй прогон: `t` обязан сдвинуться).

⚠️ Третье оттуда же: у состояния «ещё не готово» должен быть СВОЙ исход.
Ассерт «снимок свежий» краснел бы на ЗДОРОВОМ свежем деплое — значит нужен
жёлтый: не зелёный (работа не доказана) и не красный (ничего не сломано).
Два исхода на трёхзначном состоянии всегда врут в одну из сторон.

## Ассерт на ГЛАДЯЩЕЙСЯ величине: порог в 0.5% от старта решает случай

Страж «защёлка 20%» упал на прогоне 2 и прошёл на прогоне 1 ОДНОЙ И ТОЙ ЖЕ
сборки: `ty 4.19` против `4.13` при условии `< 4.19`. Соседние ассерты в обоих
прогонах зелены — до порога камера стоит на 4.2, в финале приезжает ровно на
3.2, — то есть механика цела, врал СПОСОБ НАБЛЮДЕНИЯ.

Механизм: наблюдатель писал `ty` на ПЕРВОМ тике с `camFollowOn`, то есть кадр,
где защёлка встала, а лерп ещё не сдвинул камеру. Сообщение ассерта при этом
утверждает «камера ПОШЛА вниз» — свойство, которого в этот момент ещё нет.
Старт 4.2, порог 4.19: зазор 0.01, попадание в него решает планировщик.

**Правило: если величина ГЛАДКО едет из A в B, ассерт обязан читать ФАКТ
(минимум/максимум за окно, либо устоявшееся значение), а не мгновенный сэмпл, и
порог не ставится в доли процента от стартового значения.** Родственно
«поймал момент, а не состояние», но отдельный подвид: там наблюдатель не успел,
здесь — успел слишком рано.

⚠️ Правку показывать двусторонне ОБЯЗАТЕЛЬНО, иначе это неотличимо от
подгонки порога под красный. Проверено четырьмя сценариями тиков: «исправно,
защёлка поймана мгновенно» было КРАСНЫМ, стало зелёным (тот самый ложный
красный); «исправно, замечена позже» зелено в обоих; «камера не поехала» и
«защёлка не встала» КРАСНЫ в обоих. Способность ловить поломку не изменилась.

⚠️⚠️ **И ВТОРАЯ ПОЛОВИНА ПРАВКИ, БЕЗ КОТОРОЙ ПЕРВАЯ — ПОЛУМЕРА.** Наблюдатель
внутри страницы копил минимум честно, а ВНЕШНИЙ цикл замораживал его первым же
ненулевым чтением (`if (endgameTy === null) endgameTy = …` каждой итерацией) —
то есть брал минимум за первые миллисекунды после защёлки. Замер: 4.11 при
пороге 4.19, запас 0.08 вместо полного хода. Лечится чтением ПОСЛЕ цикла (окно
закрыто, таймер снят): стало ровно **3.2**, запас единица. Урок общий: **если
наблюдатель копит величину, а потребитель «защёлкивает» первое значение, копит
он зря** — искать надо оба конца тракта, а улучшившееся, но не устоявшееся
число («стало 4.11 вместо 4.19, порог берётся») выглядит как решение и
возвращается следующим прогоном.

## Абсолютный порог исключает всех, кто тоньше него (Физика, brickbar 2026-08-07)

`FLOOR_PEN_MAX = 0.12` — порог просадки в плиту пола. У модели `brickbar`
полуразмеры 0.977 × 0.175 × **0.121**: просадка 0.103 означает, что предмет
утоплен НА 85% своей толщины, но абсолютных 0.12 он не достигает НИКОГДА —
спасатель не приходит к нему ПО ПОСТРОЕНИЮ. Это тот же дефект, с которого
спасатель начинался («дыра в объектах»), только для предмета тоньше порога.
**Правило: порог в АБСОЛЮТНЫХ единицах молча исключает всё, чей собственный
размер меньше порога. Там, где величина сравнивается с размером предмета, порог
обязан быть `min(абсолютный, доля собственного размера)`.**
Сделано: `floorPenLimit(it) = min(FLOOR_PEN_MAX, 0.8 · downReach(it))`, вынесено
ФУНКЦИЕЙ — страж и диагностика обязаны читать ту же величину, что решает бой,
а не копию формулы (наружу `itemsBrief().penLim`).

⚠️ **ДВЕ ПЕРВЫЕ ПОПЫТКИ БЫЛИ ХУЖЕ БАЗЫ, и разница между ними — суть задачи**
(соак, сид 101): база 24 подъёма / 3 просадки; доля 0.6 по МИНИМАЛЬНОЙ оси —
**115 подъёмов (шторм)**; доля 0.6 по ВЕРТИКАЛИ — 36; доля 0.8 по вертикали —
**8 подъёмов / 0 просадок**. Минимальная ось занижала порог любому предмету с
одной тонкой стороной, даже лежащему на толстом боку: важна толщина В ТУ
СТОРОНУ, КУДА ПРЕДМЕТ УТОПЛЕН, а не наименьшая вообще. Доля выбрана лесенкой
(859 сэмплов): под порог попало бы 17% / 13% / 12% / 6% / 0% мгновенных сэмплов
при долях 0.3 / 0.4 / 0.5 / 0.6 / 0.8. Итог лучше базы ПО ОБЕИМ осям сразу.
⚠️ И оговорка, снимающая кажущееся противоречие «0% сэмплов, а лечит»: сэмпл под
порогом ≠ телепорт — гейт покоя и счётчик тиков требуют, чтобы просадка
ДЕРЖАЛАСЬ, поэтому доля, не задевающая мгновенные сэмплы, всё равно ловит
залипший случай (0.103 против порога 0.097 у плашки).

## Дополнение к порогу просадки: «зелёно дважды» ≠ нет дефекта (2026-08-07-д)

⛔ **ДОЛЯ 0.8 БЫЛА ДЕФЕКТНОЙ И ПРОШЛА ДВА ПОЛНЫХ ЗЕЛЁНЫХ ПРОГОНА.** Проценты
лесенки считались от ГЕОМЕТРИЧЕСКОЙ полутолщины 0.121, а спасатель берёт
`downReach` = `min(охватный r, проекция коробки)` = **0.1085**. Порог выходил
0.0868 — НИЖЕ здорового максимума самой плашки (0.091), и спасатель поднимал
нормально лежащие предметы. Действующая доля **0.9** → порог 0.0977: выше
здорового хвоста, ниже наблюдённого дефекта 0.103. Окно узкое по физике задачи.
**Правило: считать долю от ТОЙ ЖЕ величины, которой пользуется боевой код,
а не от геометрии, из которой она выведена** (частный случай «страж читает ту
же функцию, что и бой» — здесь на нём же обожглись при ВЫБОРЕ числа).
⚠️ И про верификацию: дефект пережил сьют ×2 у диспетчера, потому что ловящий
его страж срабатывает не на каждом прогоне. Поймал ДЕТЕРМИНИРОВАННЫЙ `penProbe`,
добавленный тем же заходом. **Два зелёных прогона не доказывают отсутствия
вероятностного дефекта — доказывает детерминированный страж.**

⚠️ ОХВАТ ПРАВКИ ЗАМЕРЕН ПО ВСЕМУ ПУЛУ (диспетчер, зонд по 120 типам): под
относительной ветвью РОВНО ДВЕ модели — `brickbar` (толщина 0.1085 → порог
0.0976) и `factorycogc` (0.1308 → 0.1177, то есть на 2% ниже абсолютного, ветка
её едва задевает). Остальные 118 не затронуты — «правка хирургическая»
подтверждена измерением, а не рассуждением. Появится в пуле модель тоньше
0.1333 — она автоматически попадёт под ветку, перемерить этим же зондом.

## Метрика стен знает, где стены КОНЧАЮТСЯ (Физика, 2026-08-07-е)

Физические стены кончаются на `WALL_TOP_Y` (конус до `FUNNEL.H` + пояс над
кромкой), а `radiusAt(y)` выше кромки отдаёт `R1` НАВСЕГДА — то есть «выступ за
стену» у летящего предмета сравнивался со стеной, которой на той высоте нет.
Разбор ВСЕХ соак-журналов (19 тревог, разлёт исключён): ниже кромки 8, В САМОМ
ПОЯСЕ, где стена есть, — **0**, выше всех стен — **11**. Больше половины сигнала
инварианта было шумом метрики.
⚠️ **Доказательство здесь — РАСПРЕДЕЛЕНИЕ, а не «тревог стало меньше».** Ноль из
пояса при одиннадцати выше стен отличает починку метрики от глушения сигнала;
«стало тише» одинаково выглядит в обоих случаях.
Сделано: `maxWallExcess` отдаёт `y` и `walled`, соак поднимает тревогу только
там, где стена есть, летящие считаются отдельным счётчиком `flyAbove` — сигнал
переименован в то, чем является, а не выброшен.
⛔ СПАСАТЕЛЯ ЭТО НЕ КАСАЕТСЯ: у него та же формула значит другое — предмет выше
чаши и снаружи R1 упадёт МИМО чаши, вернуть его внутрь правильно. Слепое пятно
было у ДИАГНОСТИКИ, а не у механики.
⚠️ ОСТАЛОСЬ РЕАЛЬНЫМ: 8 тревог ниже кромки, чаще `foodbanana` на y≈1.2-2.0 у
дна. Метрика там честная; развилка «вжимается / переоценка вытянутой формы» —
разбирать замером, как плашку (у стейка это лечилось полем `wr` в TYPES).

## Запас метрики больше порога тревоги = тревога не несёт информации

Физика, банан 2026-08-07-ж. Развилка «предмет вжимается в стену / метрика
переоценивает вытянутую форму» разрешена ЗАМЕРОМ: зонд `reachProbe` на 24
детерминированных позах сравнил оценку `radialReach` (min сферы и коробки) с
ИСТИНОЙ (опорная функция по вершинам).

| модель | запас медиана | запас максимум |
|---|---|---|
| foodbanana | 0.087 | **0.360** |
| piratepalm | 0.109 | 0.328 |
| animalpig | 0.062 | 0.193 |
| brickbar | 0.001 | 0.035 |

⛔ **У банана запас 0.36 при пороге тревоги 0.20** — то есть тревога 0.22-0.29
может быть ЦЕЛИКОМ запасом формы, и метрика физически не способна отличить
«вжало в стену» от «форма такая». Все восемь тревог ниже кромки попадали в этот
диапазон. **Правило: прежде чем разбирать тревогу, сравни ЗАПАС метрики с
ПОРОГОМ. Запас больше порога — сигнала нет вовсе, чинить надо линейку.**
Диагностика (`maxWallExcess`/`wallExcessAll`) переведена на точный охват:
p50 +0.086 → −0.141, max 0.692 → 0.397, выше 0.20 осталось 2 сэмпла из 126.
⛔ СПАСАТЕЛЬ ОСТАВЛЕН НА ОЦЕНКЕ СВЕРХУ НАМЕРЕННО: там консерватизм полезен
(лучше вернуть невылетевшего, чем упустить вылетевшего), и цена другая — он
ходит по всей куче дважды в секунду, а перебор вершин это ~90 тыс. поворотов
на развёртку; диагностике точность по карману, она раз в 5 секунд.
⚠️⚠️ СЛЕДСТВИЕ: **норма 0.20 калибровалась по ПРЕЖНЕЙ, завышающей линейке.**
Линейка сменилась — порог выводить заново по распределению на нескольких сидах,
как делали 0.18 → 0.20. До этого числа сравнивать со старыми НЕЛЬЗЯ.

⚠️ И ТРЕТИЙ ЗА СЕССИЮ СЛУЧАЙ, КОГДА ЗОНД СПАСЛА КОНТРОЛЬНАЯ МОДЕЛЬ: первая
версия домножала вершины на `MESH_SCALE` ВТОРОЙ раз (`it.scl` его уже содержит),
«истина» выходила в 0.62 меньше, и зонд приписывал метрике запас, которого нет.
Поймано сверкой на КРУГЛОЙ модели — у неё оценка и истина обязаны совпасть.
**Зонд без контрольной модели врёт в свою пользу: он подтверждает гипотезу, ради
которой написан.** Ставить контроль ОДНОВРЕМЕННО с зондом, не после.

## Как переставлять порог тревоги (Физика, соак 2026-08-07-з)

Оба соак-порога выведены заново по распределению 6 сидов; прежние ловили НОРМУ.
⚠️ ЛИНЕЙКА (обязательна рядом с числом): headless Chromium на настоящем GPU
(`--use-angle=metal`), БЕЗ CPU-троттлинга, Hard, idle 0.25, бот + встряски,
обёрнута ПОЛНАЯ сессия с уровня 1; разлёт исключён, выступ — точным охватом и
только там, где стена есть. ⛔ СО СТАРЫМИ СОАК-ЧИСЛАМИ НОВЫЕ НЕ СРАВНИВАТЬ.

1. **ПОРОГ-СКОРОСТЬ, А НЕ ТОТАЛ.** Подъёмы пола: 1/мин → **2.5/мин**
   (`FLOOR_LIFT_PER_MIN`). Тотал за прогон меняется вместе с длительностью и
   объёмом сыгранного — скорость нет. Прежняя единица срабатывала на ЧЕТЫРЁХ
   здоровых прогонах из пяти, то есть ловила норму.
2. **ЛУЧШЕЕ ОБОСНОВАНИЕ — ПУСТОЙ КОРИДОР между здоровым максимумом и известным
   дефектом.** Здоровые дают максимум 1.9/мин; дефектные варианты (наши же, из
   задачи плашки) — 3.0 и 9.6/мин. Между 1.9 и 3.0 пусто, туда и ставим.
3. ✅ **ОПОРА ДОБЫТА НАМЕРЕННОЙ ПОЛОМКОЙ — ОГОВОРКА «ПОТОЛОК» СНЯТА (2026-08-12,
   ФИЗИКА).** Здесь стояло «настоящего застревания не наблюдалось, пустой коридор
   показать нечем; появится случай — перемерить». ⛔ **Ждать было НЕЛЬЗЯ по
   построению:** если механика здорова, случай может не наступить НИКОГДА, то
   есть «перемерим потом» означало «останется потолком навсегда». Опора берётся
   ГРАДУИРОВАННОЙ поломкой — ослаблением допуска спасателя по стене (`wallTol`,
   боевое 0.18), стены и `radiusAt` при этом не трогаются.
   **ВЕРДИКТ: `wallExcess` = 0.45 ОСТАЁТСЯ.** Коридор **0.407 … 0.519**, порог в
   нём. Линейка: headless на GPU, без троттлинга, Hard, 12 мин, полная сессия с
   ур.1, руки чередуются внутри сида, посторонний сид 707, объём работы сверен
   (побед 47/52/53). Здоровая 500 сэмплов, максимум 0.407, тревог 0; лёгкая
   (допуск 0.85) максимум 0.519, 1 тревога; грубая (допуск снят) максимум 13.562,
   10 тревог и 4 провала под пол.
   ⚠️ **ПРОЧНОСТЬ НЕРАВНОМЕРНАЯ, И ЭТО ЧАСТЬ ВЕРДИКТА:** грубый отказ порог ловит
   уверенно, ЛЁГКИЙ — только хвостом (1 сэмпл из 502 и лишь на ОДНОМ сиде из
   четырёх: максимумы 0.405 / 0.363 / 0.446 / 0.519). Говорить «надёжно ловит
   лёгкий случай» нельзя.
   ⚠️⚠️ **ЗАПАС НАД ЗДОРОВЫМ МАКСИМУМОМ 10%, А НЕ 18%.** Прежние 18% считались
   против устаревшего максимума 0.382; на свежих прогонах он **0.407**. Здоровую
   сторону ФИЗИКА вывела ЗАНОВО, а не переиспользовала 616 старых сэмплов —
   правильно: между замерами владелец сменил подачу бомбы (с ур.5 с разрывом) и
   `LEVEL_TYPES_MIN` 9→3, то есть ОБЪЁМ РАБОТЫ изменился. **Правило: после правки
   геймплея здоровая сторона порога протухает так же, как дефектная.**
   ⚠️⚠️ **ПРИЗНАК ФОРМУЛИРУЕТСЯ НЕ «ИЗМЕНИЛАСЬ ЛИ МЕХАНИКА», А «МЕНЯЛОСЬ ЛИ
   ЧТО-УГОДНО, ВЛИЯЮЩЕЕ НА ОБЪЁМ РАБОТЫ»** (уточнение ФИЗИКИ, и оно несущее).
   Подача бомбы и число типов — не физика, а порог ФИЗИЧЕСКИЙ; по признаку
   «трогали ли физику» обе правки прошли бы мимо. ⚠️ И честно про соблазн: 616
   готовых сэмплов лежали рядом, а мысль «здоровое-то не могло измениться»
   звучит убедительно — спасло не чутьё, а ПАМЯТЬ О ЧУЖИХ ПРАВКАХ В ПРОМЕЖУТКЕ.
   Значит на память полагаться нельзя: сверять `git log` между двумя замерами.
   ⛔ Опускать порог ближе к 0.407 НЕЛЬЗЯ: он встанет в доли процента от здорового
   максимума, а посторонний сид уже показывал, что здоровый диапазон шире выборки.
   ⚠️⚠️ **ПОБОЧНОЕ, И ОНО ЦЕННЕЕ САМОГО ПОРОГА: УДЕРЖАНИЕ У СТЕНЫ ДЕРЖИТ СОЛВЕР,
   А НЕ СПАСАТЕЛЬ.** Ослабление допуска ВПЯТЕРО (0.18 → 0.85) срезало спасения
   161 → 102 за 48 минут, а распределение выступов почти не сдвинуло. Механика
   либо держит, либо рвётся сразу — «лёгкого» отказа удержания в ней почти не
   существует, и узость коридора этим и объясняется. Спасатель — страховка на
   грубый случай, а не постоянный участник.
   ⚠️ СЛЕДСТВИЕ ДЛЯ ЧТЕНИЯ СОАКОВ: **рост числа спасений — сигнал о РАБОТЕ
   страховки, а не о поломке удержания.** Раньше мы читали одно как другое.
   ⛔⛔ **ГРАНИЦА (оговорка ФИЗИКИ, не снимать): ВЕРНО ТОЛЬКО ДЛЯ СТЕНЫ.** У ПОЛА
   картина может быть обратной — там спасатель ловит ПРОСАДКУ, которая сама не
   расходится (см. историю плашки `brickbar`: предмет сидел в плите 30 секунд и
   выбирался только телепортом). Переносить вывод на пол ПО АНАЛОГИИ нельзя,
   проверять отдельно тем же приёмом.
   ⚠️ **ГРАДУИРОВКУ ПРОВЕРЯТЬ ЗАМЕРОМ, А НЕ НА ГЛАЗ:** допуски 0.4 и 0.7 дают
   распределение, НЕОТЛИЧИМОЕ от здорового (максимумы −0.053 и 0.345). Причина —
   ручка живёт в пространстве ОЦЕНКИ СВЕРХУ, а тревога в ТОЧНОМ охвате, и запас
   между ними у вытянутых форм доходит до 0.36.
   ⚠️ **ОДИНОЧНЫЙ СИД НЕ СЕРТИФИЦИРУЕТ И ПОЛОМКУ ТОЖЕ:** допуск 1.0 дал максимум
   1.203 на сиде 101 и **−0.087 на сиде 202** — дефект не проявился вовсе.
   ✅ **ПРЕДРЕГИСТРАЦИЯ ИСХОДОВ — ПРИЁМ, А НЕ РАЗОВАЯ АККУРАТНОСТЬ.** Физика
   записала три возможных вердикта и разбор ДО первого прогона; это стоило пяти
   минут и сняло целый класс споров с самой собой (тот самый, где порог
   незаметно подгоняется под уже увиденные числа).
   ⚠️ **УСЛОВИЕ, БЕЗ КОТОРОГО ПРИЁМ ПУСТ: исходы писать В ФАЙЛ, а не держать в
   голове** — в голове они переписываются задним числом и незаметно.

⚠️⚠️ **ПАРА «ХЕШ + ЧИСЛО PASS» ОБЯЗАНА БЫТЬ ИЗ ОДНОГО ПРОГОНА** (ловля диспетчера
2026-08-12). В сдаче пришло «база `573ad44`, сьют 666», а эта голова даёт **667**:
прогон был снят ДО ребейза, и в отчёт ушла пара «хеш ПОСЛЕ + число ДО». Ассерт не
пропал, но принимающий обязан это проверить — то есть цена ошибки ход разбора, а
в неудачный день пропущенная регрессия, если число случайно совпадёт.
⚠️ Это тот же закон, что «перф-число без линейки — не число», просто счётчик
ассертов не выглядит замером и проверять его забывают. **Ребейзнул после
прогона — перегоняй либо сдавай хеш, на котором мерил.**
4. ⚠️⚠️ **ПОСТОРОННИЙ СИД ОБЯЗАТЕЛЕН.** Сид 606 не участвовал в выборе и дал 23
   подъёма против прежнего здорового максимума 21 — здоровый диапазон шире, чем
   показала выборка, и по пяти сидам запас вышел бы на 10% меньше заявленного.
   Проверочный прогон — часть перестановки порога, а не приятное дополнение.
5. ⚠️ **РАЗБОР ЧИТАЛ ПОСЛЕДНЮЮ СТРОКУ ЖУРНАЛА, А ТАМ СВОДКА** (`{summary,
   problems, errors}`), не сэмпл — выходило «подъёмов ноль вовсе», вывод,
   ПРОТИВОПОЛОЖНЫЙ правде (16-23). Тот же жанр, что двойной `MESH_SCALE`:
   разбор правдоподобен и молчит. Ловится сверкой с печатной сводкой соака.

## Литерал в чужом страже ломается от правки константы (4-й случай за сессию)

Интеграция, при правке `GROW_PER_S` (ветка отменена владельцем, но УРОК ОСТАЁТСЯ):
чужой страж «честный возвращенец» слал **литерал 6 000**, а поднятый потолок
обрезал значение до 7 000 — «чистый рост» превратился в ПАДЕНИЕ, проверяемая
ветка не исполнялась вовсе, и ассерт краснел на ИСПРАВНОЙ сборке.
**Правило: страж, которому нужно «число выше/ниже текущего состояния», обязан
считать его ОТ СОСТОЯНИЯ (`сохранённое + дельта`), а не вписывать литерал.**
Литерал верен ровно до первой правки константы, о которой его автор не узнает.
⚠️ И парное, о ГРАНИЦАХ ПОЧИНКИ: чинить такой страж надо МИНИМАЛЬНО и не
превращать его во второй детектор изменённой константы — иначе одна диверсия
роняет два ассерта, и по красному больше не понять, что именно сломалось.

⚠️ ЧЕТВЁРТЫЙ СЛУЧАЙ ЗА СЕССИЮ НА ОДНОМ ЗАКОНЕ (копия числа/формулы/признака
рядом с рабочей величиной): `WALL_TOP_Y` из литералов, порог просадки от
геометрии вместо `downReach`, `stale` в смоуке, теперь литерал в страже. Это не
невнимательность отдельных исполнителей, а СВОЙСТВО кода, где величины
выводятся: копия всегда совпадает в момент написания и расходится потом.

## Платформенная таблица ПАДАТЬ НЕ УМЕЕТ — следствие для «Форбс»-модели

Найдено Интеграцией 2026-08-09 при разборе требования «результат меняется, если
игрок потратил на множитель». Платформенный сервер хранит **МАКСИМУМ** и меньшее
значение молча игнорирует (наш же замер 2026-07-29: отказ невидим даже по
статусу, признак только в теле). Значит:
- **опускать игрока умеет ТОЛЬКО НАША таблица**;
- перевешивать платформенную отправку на изменение баланса — работа без эффекта;
- требование владельца «трата опускает СРАЗУ» живёт целиком в своей таблице.
⚠️ Фразу «платформенная падать не умеет» держать рядом с `setScore` в 78-ads:
следующий увидит требование и полезет чинить путь, где чинить нечего.

⚠️ **МОЯ ПРЕДПОСЫЛКА О ЧАСТОТЕ БЫЛА НЕВЕРНА, ЗАПИСЫВАЮ ЧЕСТНО:** я заказал
коалесцинг отправки, утверждая, что `onStarsChange` дёргается на каждом
начислении внутри партии. Проверка грепом (Интеграция): `fireStarsChange` зовут
РОВНО СЕМЬ мест (облачный синк, `bankLive`, `bankLevelScore`, `addStars`,
`spendStars`, `buyBoost`, `purchaseUnlock`), за матч он не дёргается вовсе —
счёт банкуется раз за уровень. Лечил несуществующую болезнь.
✅ **Настоящее узкое место — наш `RATE_SEC = 20`,** и оно бьёт в САМЫЙ типичный
путь: победа шлёт счёт → игрок тут же на экране победы покупает множитель →
вторая отправка внутри 20 с → **429**, место не обновится ровно в сценарии,
который заказал владелец. Правило: **429 = ОТЛОЖИТЬ, а не выбросить**; задержку
выводить из `RATE_SEC`, а не из плотности событий.

## Зоны: протокол — Интеграции, экран — Интерфейсу (правка 2026-08-09)

`src/app/82-lb.js` (клиент своей таблицы) сперва был отдан ИНТЕРФЕЙСУ вместе с
экраном — **ошибка распределения, исправлена до первой строки кода**. Модуль это
ПРОТОКОЛ: подпись HMAC, формат запроса, коды 400/401/409/429/503, политика
отложенной отправки, «простые» запросы по CORS. Признак, по которому ошибка
опознана: исполнитель сказал, что первым делом будет читать чужой `src/index.js`,
чтобы узнать имена полей и схему подписи.
**Правило: протокольный код принадлежит тому, кто владеет протоколом; экран —
тому, кто владеет экраном. Шов между ними — ОПИСАННЫЙ API, и он пишется ДО
реализации,** иначе верстают против догадки.

## Пустая лесенка отвечает «место 1» КАЖДОМУ (замер диспетчера 2026-08-09)

Найдено прогоном Интерфейса, диагноз ими дан НЕВЕРНЫЙ («estimateRank считает
наоборот»), проверено мной по коду и стенду:
- лесенка строится `WHERE rn % 100 = 0` (`server/leaderboard/src/index.js:303`),
  то есть по каждому СОТОМУ игроку;
- при 24 подходящих строках в ней нет НИ ОДНОЙ ступени;
- `estimateRank` уходит в ветку `if (!ladder.length) return 1` (:98).

⛔ **Следствие: пока в таблице меньше сотни игроков, ответ на отправку говорит
«место 1» КАЖДОМУ — то есть врёт ровно в первые недели после запуска.**
Формально контракт честен (рядом `exact: 0`), но поле называется `rank`, и живой
потребитель прочёл его как настоящее место в первый же день.
**Правило: значение, которое нельзя перепутать, лучше пометки, которую можно не
заметить.** Пустая лесенка → `rank: null`, а не уверенная единица.
⚠️⚠️ **МОЙ ДИАГНОЗ БЫЛ НЕПОЛОН, И НЕДОСТАЮЩАЯ ПОЛОВИНА ПЕРЕЖИЛА БЫ ПОЧИНКУ**
(нашла Интеграция): та же ложь жила на шаг глубже — при НЕПУСТОЙ лесенке игрок
выше первой ступени (`lo === 0`) тоже получал единицу, хотя про него известно
лишь «где-то в первой сотне». То есть починка одного случая оставила бы дефект
у ЛУЧШИХ игроков большой таблицы. Оба случая теперь `null`.
✅ ПРОВЕРЕНО ДИСПЕТЧЕРОМ НА ОБОИХ: база 25 строк → `null`, база 302 строки с
заведомо высшим счётом → `null` (было бы `1`).
⚠️ И следствие для смоука: условие `typeof rank === 'number'` пришлось заменить
на `rank === null || rank >= 100` — прежнее краснело бы на ИСПРАВНОЙ сборке,
потому что честный ответ маленькой базы теперь `null`. Проверка ТИПА пережила
смену контракта хуже, чем проверка СМЫСЛА.
⚠️ ДЛЯ ЭКРАНА, независимо от правки сервера: место показывать ТОЛЬКО из
`/v1/me` (`exact: 1`), никогда из ответа на отправку.

## Два протокольных закона, найденных ПРОГОНОМ клиента (Интерфейс, 2026-08-09)

1. **«Тело разобралось» ≠ успех.** Повторная отправка внутри окна частоты
   вернула клиенту «принято», хотя сервер отказал: ошибка живёт полем `err` в
   ТЕЛЕ, а не кодом ответа — потому что мы сами решили деградировать мягко и на
   упавшей базе отвечать 200. Тот же закон «мягкая деградация ослепляет проверку
   по статусу», ударивший с клиентской стороны. Ни `res.ok`, ни успешный разбор
   JSON ничего не доказывают.
2. **HTTP-кэш браузера бьёт поверх нашего `max-age: 60`.** После сброса
   СВОЕГО кэша клиент перезапросил `/v1/top` и получил старые строки с тем же
   `t` — ответил кэш браузера. Число не менялось бы минуту ровно там, где
   обязано: после победы и после траты. Лечение — РАЗОВЫЙ обход (метка ставится
   при сбросе, тратится первым успешным чтением); постоянный обход неверен,
   те 60 секунд намеренные.
⚠️ Обе находки невозможно получить чтением кода — только прогоном против
живого стенда с убийством базы. Это довод в пользу стенда как обязательной
части задачи, а не удобства.

## СЛЕПОЙ БЫВАЕТ НЕ СТРАЖ, А ДИВЕРСИЯ (Интеграция, 2026-08-09)

Проверяя снятие потолка, Интеграция написала диверсию «вернуть автоскрытие»
как `f = (s > 2000)` в SET-выражении UPDATE — а SET читает СТАРОЕ значение
колонки, не присланное. Диверсия не срабатывала НИКОГДА, и отчёт честно
напечатал «страж слеп». Слеп был не страж.
**Правило: перед выводом «страж не ловит» докажи, что ДИВЕРСИЯ вообще
состоялась** — следом в логе, изменившимся ответом, чем угодно наблюдаемым.
Родственно «диверсия обязана бить в СВОЙСТВО, а не в его соседа», но здесь
случай тяжелее: там диверсия била мимо, здесь — не била вовсе, и отчёт в обоих
случаях выглядит ОДИНАКОВО.
⚠️⚠️ **ДВА ПРИЗНАКА, И ОНИ РАЗЛИЧАЮТСЯ ПО НАБЛЮДАЕМОСТИ** (уточнение Интеграции
по её же двум ошибкам в одной правке): ПРОТУХШУЮ диверсию инструмент видит —
`break.js` печатает «строка не найдена». НЕ СРАБОТАВШУЮ не видно НИЧЕМ:
подстановка удалась, текст изменился, поведение — нет. Именно вторая и даёт
ложный вывод «страж слеп». **Отсутствие следа НЕ является доказательством
исправности.** До появления автоматической проверки единственная защита —
руками убедиться, что сломанная сборка ВЕДЁТ СЕБЯ иначе, а не только выглядит
иначе. Заказано `break.js`: прогонять патч на заведомо красном сценарии ПЕРЕД
основным, то есть требовать от диверсии смены ПОВЕДЕНИЯ.
⚠️ Второй раз за ту же правку сработало старое правило «диверсии протухают
вместе с боевой строкой» (патч ссылался на `s0`, строка стала `s`) — то есть
из двух ошибок в диверсиях одна была новой, одна известной. Обе поймал
двусторонний прогон, ни одну — чтение.

## Удаление механики: страж УМИРАЕТ вместе с ней, а не переписывается

При снятии потолка снято три стража (кламп, возрастной потолок, «честный
возвращенец»). ⚠️ Последний именно УДАЛЁН, а не переписан под новое поведение:
он лечил болезнь, которой больше нет, и переписывание сохранило бы счётчик
PASS ценой стража, ничего не стерегущего. **Падение числа стражей при удалении
механики — правильный признак, а не потеря.**
✅ Взамен один ПОЛОЖИТЕЛЬНЫЙ («счёт как есть»), утверждающий ДВА признака сразу
(миллиард сохранён без обрезки И строка не скрыта) — потому что обрезку и
автоскрытие можно вернуть порознь, и на него поставлены ДВЕ диверсии.
Скрытие проверяется НАСТОЯЩИМ путём (`/admin/hide`), а не подсовыванием `f`
в INSERT — тот же закон, что «приводить состояние настоящим путём».

## Признак готовности стенда обязан быть УСПЕХОМ, а не подстрокой

Диспетчер, 2026-08-09, вторая за сессию ошибка этого класса у меня же. Ждал
стенд циклом `until grep -q '8788'` — а стенд УПАЛ с `EADDRINUSE`, потому что
предыдущий процесс был жив, и число `8788` нашлось В ТЕКСТЕ ОШИБКИ. Цикл вышел,
запросы ушли в СТАРЫЙ процесс с кодом до правки, и я получил «правка не
работает» на исправной сборке — то есть чуть не отправил направлению ложный
разбор.
**Правило: ждать по строке, которая печатается ТОЛЬКО при успехе
(«СТЕНД ТАБЛИЦЫ ЛИДЕРОВ: …»), и в том же цикле проверять признак ПАДЕНИЯ
(`Unhandled`/`EADDRINUSE`) с выходом.** Первый случай за эту же сессию — ожидание
сьюта по слову `Error`, которое совпало с текстом штатного PASS про
синтетический креш. Один и тот же промах: признак взят по подстроке, а не по
событию.
⚠️ И гигиена стенда: перед стартом убивать прежний и ПРОВЕРЯТЬ, что порт
свободен (`lsof -nP -iTCP:<порт> -sTCP:LISTEN`), иначе замер молча уходит в
чужой процесс — а он отвечает правдоподобно, просто устаревшим кодом.

## Первоисточник бьёт пересказ, особенно убедительный

Интерфейс, 2026-08-09, самодиагноз после моего НЕВЕРНОГО обвинения (я решил, что
они отстали по репозиторию — проверка `git merge-base` показала, что база
правильная). Настоящая причина: раздел канона, отменяющий прежнее решение, лежал
у них в контексте, и состояние было построено по ПЕРЕСКАЗУ, хотя первоисточник
держали в руках.
**Правило: сверяться с первоисточником даже тогда — особенно тогда, — когда
пересказ звучит связно.** Хеш в сообщении тут не помог бы: ошибка не в том,
ЧТО читали, а в том, что не читали вовсе.
⚠️ За эту же сессию я допустил тот же промах дважды (частота `onStarsChange`
по памяти вместо грепа; «правка не работает» из старого процесса стенда) —
значит правило общее, а не про одно направление.

⚠️⚠️ **КОРЕНЬ, НАЙДЕННЫЙ ПОЗЖЕ (2026-08-12, после ДВУХ потерь за сутки подряд):
В КАНОНЕ ЖИВУТ ДВА РАЗНЫХ ЖАНРА АБЗАЦЕВ, И ПРОТУХАЕТ ТОЛЬКО ОДИН.**
- **РЕШЕНИЕ ВЛАДЕЛЬЦА** («потолок 0.8», «вкладок нет») — не протухает ОТ ДРЕЙФА
  КОДА. Спорить с ним замером бессмысленно: это не утверждение о мире, а выбор.
  ⚠️⚠️ **НО ОНО ОТМЕНЯЕТСЯ ЕГО ЖЕ БОЛЕЕ ПОЗДНИМ СЛОВОМ, И ТОГДА ОПАСНЕЕ ЛЮБОГО
  УСТАРЕВШЕГО ОПИСАНИЯ.** Пример, стоивший ИНТЕРФЕЙСУ красного стража на здоровой
  сборке: раздел «кромки нейтральные» простоял действующим двое суток, хотя
  владелец отменил нейтраль В ТОТ ЖЕ ДЕНЬ. Описание можно проверить кодом;
  отменённое решение выглядит как ПРИКАЗ, спорить с которым замером «не
  положено», — поэтому его и не проверяют. **Отменяя решение, ставь надгробие в
  шапке ЕГО раздела, а не только в новом.**
- **ОПИСАНИЕ СОСТОЯНИЯ** («сейчас свёрстано так», «осталось сделать то»,
  «в пуле нет компаундов») — протухает при первой же правке кода, причём
  МОЛЧА: текст остаётся связным и убедительным.

Я держал оба жанра в одном регистре, и это стоило за сутки: (1) ИНТЕРФЕЙС
переделал экран, который я уже сделал сам; (2) он же не начал десктопную
карточку, потому что мой абзац-задание сообщал, что она не сделана — сутки
спустя после того, как она была сделана. ФИЗИКА тем же днём дважды упёрлась в
то же самое (мой абзац про компаунды в пуле — перепись дала ноль; мой прогноз
«избирательный CCD даст те же −30%» — замер не подтвердил).
**Практика: у абзаца-СОСТОЯНИЯ ставить дату и проверять его кодом перед тем,
как на него опереться; у абзаца-РЕШЕНИЯ дата не нужна, нужна цитата владельца.
Абзац-ЗАДАНИЕ вычёркивать В ТОТ ЖЕ КОММИТ, которым задача закрыта, — переживший
свою задачу, он работает как ЛОЖНЫЙ ПРИКАЗ.**
⚠️ И следствие для направлений, сказано всем троим: **разошлись канон и код —
прав КОД.** Сообщать, но не ждать моего подтверждения, чтобы так считать.

## Отказ на вопросе ДОВЕРИЯ должен быть закрытым (лидерборд, экран)

Интерфейс предложил трактовать ОТСУТСТВИЕ поля `exact` как «показывать можно»
(довод: переименуют — замолчит весь экран). **Решение диспетчера: наоборот.**
Цена молчания — пустой виджет; цена ложного «можно» — показанное неверное место,
то есть ровно тот дефект, который мы в этот день вычищали. `/v1/me` сегодня
всегда несёт `exact`, но завтра там появится деградационный путь, и он придёт
без поля.
**Как правильно: нет признака достоверности → не показываем, НО ГРОМКО** —
страж утверждает, что ответ несёт `exact`. Тогда переименование ломает ТЕСТ, а
не гасит экран у игрока молча. Опасение «тихо замолчит» лечится стражем, а не
доверием по умолчанию.

## Слот врезки существует, только когда фича ВКЛЮЧЕНА

Врезка на экране победы держит высоту во всех состояниях намеренно — иначе
приход ответа двигал бы кнопку «Next» из-под пальца. ⚠️ Но пока фича выключена
(пустой URL, модуля нет), слота не должно быть в разметке ВООБЩЕ: иначе экран
победы носит 161 px пустоты и `TOP ITEMS` уезжает вниз без всякой причины.
**Правило: резервировать место под ОЖИДАЕМЫЕ данные — да; под данные, которых
не может быть в этой сборке, — нет.**

## Прогон диверсий, правящий БОЕВОЙ артефакт, врёт о самом себе

Интеграция, 2026-08-09, третья её ошибка в одной сдаче и новый подвид закона
«слепой бывает не страж, а диверсия». `break.js` патчил САМ `index.html`; убитый
по SIGPIPE (вывод был направлен в `head`, тот закрыл трубу и ушёл) он не дошёл
до восстановления и **оставил боевую сборку изувеченной**. Снаружи следующая
диверсия отчиталась «строка не найдена» — то есть инструмент СОЗДАЛ сломанную
сборку и отрапортовал о ней как о протухшей строке.
**Правило: прогон диверсий работает на КОПИИ артефакта и в конце сверяет
побайтово, что оригинал не тронут. Иначе его собственная смерть выглядит как
чужая ошибка.**
⚠️ И практическое, касается всех: `| head -N` закрывает трубу и убивает
писателя сигналом. Для прогонов, которые ДОЛЖНЫ дойти до уборки за собой,
конвейер с ранним выходом запрещён — либо `tail`, либо файл.
⚠️ Проверять целость просто: `python3 build.py` и `git status --porcelain --
index.html` — пусто значит сборка совпала с исходником.

## ЧЕТЫРЕ СТРАЖА ОТПРАВКИ В ТАБЛИЦУ: ЧЕМ ИХ ПРОВЕРЯТЬ (ИНТЕГРАЦИЯ, 2026-08-10)

Скрипт двустороннего прогона удалён вместе со сменой владения стражами. Список
диверсий сохранён здесь, потому что он объясняет, ЗАЧЕМ каждый ассерт устроен
именно так; скрипт умирает, причины — нет.

1. **НОЛЬ НЕ ОТПРАВЛЯЕТСЯ.** Диверсия: снять `if (!(s > 0)) return …` в
   `lbSubmit`. ⚠️ Санитар «адрес задан» ОБЯЗАН стоять в том же ассерте:
   «отправок 0» истинно и на выключенной таблице. ⚠️ И страж обязан ИСПОЛНИТЬ
   путь (`bankScore(0)`), а не ждать его: на `file://` облачный синк не
   запускается, событие баланса не придёт вовсе, и диверсия ничего не сломает —
   отчёт печатает «покраснели: НИЧЕГО» при мёртвой проверке.
2. **ОДНА ОТПРАВКА НА ПОБЕДУ.** Диверсия: снять память об отправленном
   (`if (s === lbSentScore) return …`) — победа шлёт дважды. ⚠️ Выигрывать КАК
   ИГРОК и ждать ФАКТ «уровень закончен И счёт забанкован»: `leaveSingles`
   доедает финал по предмету за 0.5 с, замер приходил ДО банка, и страж краснел
   на ИСПРАВНОЙ сборке.
3. **429 НЕ ТЕРЯЕТСЯ.** Диверсия: снять ветку `refused && err === 'rate'`.
   ⚠️ Различающий признак — мок отдаёт `retry: 1`, а собственный фолбэк клиента
   30 с; окно ожидания 5 с разводит «прочитал тело» и «держит свою константу».
4. **НЕТ СТРОКИ — НЕ ОТКАЗ.** Диверсия: `r.err === 'none'` → `false`.
   ⚠️ Признак того, что диверсия СРАБОТАЛА, а не просто применилась: деталь
   обязана показать `state:"refused"` — изменилось ПОВЕДЕНИЕ, а не текст.

⚠️ Общее для всех четырёх: мок отвечает ФОРМАМИ НАСТОЯЩЕГО сервера
(`404 {"err":"none"}`, `429 {"err":"rate","retry":N}`), а не выдуманными —
иначе проверяется путь, которого в бою нет. Прогон патчит КОПИЮ сборки и в
конце сверяет оригинал побайтово.

⚠️⚠️ **ЧИСТКА ЧУЖИХ СТРОК — ТОЛЬКО `/admin/hide`, НЕ `DELETE`** (рекомендация
ИНТЕГРАЦИИ, принята): скрытая строка остаётся в базе, и если это владелец, его
прогресс не потеряется, а вернуть видимость — одна команда. Необратимое
удаление на чужих данных — только после его прямого слова «удаляй».

## СТРАЖ НЕ ЛОМАЕТСЯ, А ЗАМОЛКАЕТ (формулировка ИНТЕРФЕЙСА, 2026-08-10)

Место игрока переехало из ВТОРОЙ строки точки входа в ПЕРВУЮ (обновлённый
макет). Мой страж «без точного места числа нет» читал только вторую строку и
заголовок — он пережил бы правку **ЗЕЛЁНЫМ** и перестал бы стеречь ровно то,
ради чего написан: утечка прикидки в новую строку прошла бы молча.
**Правило: перенося ПОКАЗ величины, перечисли ВСЕ точки её показа и заставь
стража читать каждую.** Красный страж виден сразу; замолчавший неотличим от
исправного, и цена ошибки та же, что у выключенного.
⚠️ Родственно «страж, который сам создаёт предпосылку», но здесь предпосылка не
исчезает, а ПЕРЕЕЗЖАЕТ — и это тише.

## РАЗБОР ЧУЖИМ ГЛАЗОМ ЛОВИТ ТО, ЧЕГО НЕ ЛОВИТ СВОЙ СТРАЖ (2026-08-10)

Правка звука (разнообразие трёх записей владельца) прошла мой замер, мой
двусторонний прогон и зелёный сьют — и содержала ТРИ дефекта, найденных
разбором и подтверждённых потом моим же замером:
1. **питч был сдвинут вверх ВСЕГДА.** `it.r` = `rc · размерУровня · MESH_SCALE`
   (40-items:117), у 107 типов из 120 он **0.62**, а не 1 — пивот на единице
   зашивал множитель 1/√0.62 = 1.27. Замер: 12 боевых матчей из 12 выше
   единицы, медиана 1.12. Плюс Blink и WebKit ресемплят линейной
   интерполяцией, и повышение тона — децимация без антиалиасинга;
2. **панорама съедала 3 дБ.** Все три записи МОНО; моно через StereoPanner
   идёт equal-power и теряет РОВНО −3.01 дБ при ЛЮБОМ значении панорамы
   (замер в OfflineAudioContext: 0.25 → 0.125 при pan 0/0.33/0.6). Записи
   владельца звучали тише процедурного «буля», а на старом iOS без паннера —
   на 3 дБ ГРОМЧЕ, то есть один звук разной громкости на разных телефонах;
3. **мой страж был слеп к СНОСУ ПРОВОДКИ.** Без `r:` в вызове фолбэк даёт
   калибр стандартного предмета, числа около единицы — «не сдвинуто вверх»
   остаётся зелёным. Закрыто детерминированным ассертом на КОНКРЕТНОМ типе
   (банан `rc 1.4`, коридор 0.79..0.90 не пересекается со стандартным).

⚠️⚠️ **ОБЩЕЕ И ГЛАВНОЕ: я кормил стража ВЫДУМАННЫМИ входами** (радиусы
0.6/1.0/1.4), и он проверял МОЁ ПРЕДСТАВЛЕНИЕ о игре, а не игру. Синтетика
законна для граничных случаев, но у каждой фичи обязан быть ассерт на БОЕВОМ
пути — там, где значения приходят из кода, а не из теста.
⚠️ И про перенос формулы владельца: она приходит в ЕГО единицах («размер около
единицы»), а в коде живут СВОИ (радиус с общим масштабом моделей). Нормировка
теряется молча и выглядит как работающая фича.

## БЛОКЕР В НАЧАЛЕ ПРОГОНА — ЭТО ТИХОЕ ВЫКЛЮЧЕНИЕ ВСЕГО ХВОСТА (2026-08-10)

Экран новой вещи ронял прогон на 31-м ассерте (см. раздел ниже). Пока он там
стоял, **575 ассертов не исполнялись вовсе** — и всё, что дописывалось в этот
день, попадало в мёртвую зону. Первый дошедший до конца прогон вскрыл ЧЕТЫРЕ
дефекта разом, ни один из которых не был новым:
1. блок кромки читал `lbPage`, которую сосед закрывает у себя (падение БЕЗ
   вердикта на 579-м) — закон «склейка двух правильных блоков»;
2-3. **три стража отправки мерили ГЕЙТ, а не отправку.** `LB_NOSEND` глушит по
   ДВУМ признакам (`file:` и `navigator.webdriver`), сьют попадает под оба, и
   «отправок 0» истинно на сборке, где отправки нет вовсе. Написаны они были
   при прежнем гейте и НИ РАЗУ не исполнялись;
4. **сьют на каждом прогоне ходил в БОЕВОЙ сервер таблицы.**

⚠️ **ПРАВИЛО: красный/обрыв в начале прогона чинится ПЕРВЫМ ДЕЛОМ, а число
PASS до починки не считается показателем.** «606 из 606» и «31 из 606» дают
одинаково спокойный лог, если смотреть только на отсутствие FAIL.
⚠️ И следствие для приёмки: **фича, включённая впервые, впервые же исполняет
ВЕСЬ свой код.** Первый полный прогон после включения стоит трёх обычных —
планировать его как отдельную работу, а не как формальность перед пушем.

## СВОЙ ЖЕ ШТАТНЫЙ 4xx ВАЛИТ ГЕЙТ ОШИБОК (2026-08-10)

Открытие меню зовёт `/v1/me`; у игрока БЕЗ строки сервер честно отвечает
**404 `err:"none"`** — это документированная норма, на ней стоит целый раздел
канона. Но Chromium пишет ЛЮБОЙ 4xx в консоль, гейт ошибок сьюта это ловит, и
прогон краснел при 606 зелёных ассертах.
⛔ Фильтровать «404» из гейта НЕЛЬЗЯ: текст в консоли не несёт адреса, и наш
законный 404 стал бы неотличим от чужой пропавшей картинки.
✅ Лечение: сьют не ходит в боевой сервер вовсе — заглушка `fetch` ставится
ПЕРВОЙ на каждой странице и отдаёт формы настоящего сервера; секции со своими
моками заворачивают её сверху и остаются главнее.
⚠️⚠️ И ЭТО НЕ ТОЛЬКО ПРО ЛОГ, вторая половина важнее: до правки КАЖДЫЙ прогон
стучался в боевой воркер владельца. Пишет только `submit` и он загейчен, но
чтение — внешняя зависимость: сервер лёг или включил ограничение частоты, и
«сьют покраснел» без единой правки в игре.

## СЕКЦИЯ НА СВОЕЙ СТРАНИЦЕ НЕ ВИДИТ, ЧТО ФИЧА ЛОМАЕТ ОСНОВНУЮ (2026-08-10)

Экран новой вещи проверялся отдельной страницей `noPage` — четыре зелёных
стража: модель живая, цепочка колбэков цела, анимация есть. А на ОСНОВНОЙ
странице он вставал полноэкранным `.overlay` в цепочку победы и перехватывал
все клики: прогон умирал на 31-м ассерте с `#newObj intercepts pointer events`,
**без вердикта и без единого FAIL** — снаружи неотличимо от зависания.

⚠️ Причина структурная, а не забывчивость: секция на своей странице проверяет
фичу В ИЗОЛЯЦИИ и по построению слепа к тому, что фича делает с ОКРУЖЕНИЕМ.
Родственно «страж, который сам создаёт предпосылку», но зеркально: там страж не
видел ОТСУТСТВИЯ фичи в поставке, здесь — её ПРИСУТСТВИЯ в чужом сценарии.
**Правило: фича, добавленная в общую цепочку (победа, старт уровня, пауза),
обязана получить звено в ОСНОВНОМ прогоне — с ассертом, что звено сработало.**
Молчаливый пропуск («если открыт — закрой») мало чем лучше отсутствия: он даёт
прогону дойти до конца и скрывает исчезновение фичи из цепочки.
⚠️ Закрывать — НАСТОЯЩЕЙ кнопкой, не `newObjHide()`: иначе проверяется обход
механики. И ждать ФАКТ закрытия опросом, а не фиксированной паузой.

## Страж обязан ИСПОЛНЯТЬ проверяемый путь, а не ждать его

Там же: страж «ноль не отправляется» был зелен по ПУСТОЙ причине — на `file://`
облачный синк не запускается, событие баланса не приходило вовсе, и диверсия
ничего не ломала (отчёт честно печатал «покраснели: НИЧЕГО»). **Ожидание
события — не проверка события.** Если путь в тестовой среде не наступает сам,
его вызывают ЯВНО.
⚠️ Родственная ошибка оттуда же: страж «одна отправка на победу» краснел на
ИСПРАВНОЙ сборке, потому что победа достигалась `leaveSingles`, а одиночки
доедает финал по предмету за полсекунды — банк не успевал. Лечение: выигрывать
КАК ИГРОК и ждать ФАКТ (уровень закончен И счёт забанкован), а не часы.

## ТРИ ПРИЗНАКА НЕСРАБОТАВШЕЙ ДИВЕРСИИ, и только один бесплатен

Итог работы Интеграции 2026-08-09 над `break.js`. Формулировка её же, проверена
диспетчером прогоном (14 диверсий + 4 смоука, оригиналы не тронуты):

**Отсутствие следа — не доказательство исправности.**
- **диверсия ПРОТУХЛА** — видно бесплатно: «строка не найдена»;
- **диверсия ПРОШЛА МИМО** (применилась, но бьёт не в наблюдаемое) — видно
  только по СОВПАДЕНИЮ СТРОКИ САМОГО СТРАЖА бит-в-бит;
- **диверсия НЕ СРАБОТАЛА ВОВСЕ** — видно по совпадению ВСЕГО вывода.

⚠️⚠️ **СТРОКУ ДЛЯ ДИВЕРСИИ БРАТЬ ИЗ ФАЙЛА, А НЕ ПИСАТЬ ПО ПАМЯТИ О НЁМ**
(ИНТЕРФЕЙС, 2026-08-11, поймано ДО прогона — вторая ловля на одном законе за
вечер). Шаблон диверсии нёс ДВА пробела отступа, а боевая строка — ЧЕТЫРЕ:
патч не совпал бы и напечатал «протухла», то есть инструмент снова назвал бы
своей слепотой чужую исправность. Регистр, отступ, пробелы вокруг оператора —
три способа промахнуться, и все три снимаются одним приёмом: скрипт ЧИТАЕТ
боевую строку и строит патч ИЗ НЕЁ.

⚠️⚠️ **ЧЕТВЁРТЫЙ ПРИЗНАК, И ОН ПРО САМ ИНСТРУМЕНТ (ИНТЕРФЕЙС, 2026-08-11):
СЛЕПОЙ БЫВАЕТ ПОИСКОВАЯ СТРОКА.** Диверсия «три аватара» СРАБОТАЛА и страж её
ПОЙМАЛ (390 → 3, 320 → 2), а прогон напечатал «секция не исполнилась»: искали
фразу «ТРИ аватара», в сообщении стояло «три аватара» строчными. То есть к трём
известным причинам («протухла» / «прошла мимо» / «не сработала») добавляется
четвёртая — **инструмент не нашёл того, что искал, и назвал это отсутствием
события**. Той же опечаткой была съедена и САМОПРОВЕРКА, то есть проверка
проверки молча выключилась.
⛔ Отсюда практическое: строку стража искать по УСТОЙЧИВОМУ куску (без регистра
и без эмодзи) либо по идентификатору ассерта, а не по красивой фразе целиком.

⚠️⚠️ **СТРАЖ МОЖЕТ БЫТЬ ТАВТОЛОГИЧЕН ПРОТИВ ОДНОГО СВОЙСТВА И ЧЕСТЕН ПРОТИВ
ДРУГОГО** (там же, находка про мой страж `exact`). Ассерт «без точного места
число не показывается» стоит на сценарии НОВИЧКА (`/v1/me` → 404 `err:"none"`),
а там ранга НЕТ ВООБЩЕ — утекать нечему, и против «прикидка просочилась в показ»
он тавтологичен при ЛЮБОЙ реализации. Против «новичку показали мусор» тот же
ассерт честен. **Один сценарий не покрывает два свойства**; нужен второй, где
сервер отдаёт `200` с рангом и `exact:0` — ровно тот ответ, который он умеет
давать (прикидка по лесенке) и который показывать нельзя.

⚠️⚠️ **ПОПРАВКА К ЭТОМУ ЗАКОНУ (ИНТЕРФЕЙС, 2026-08-11, принята): у НАШЕГО сьюта
третий признак НЕ РАБОТАЕТ.** Он печатает замеры и времена, и вывод отличается
между двумя прогонами ОДНОЙ И ТОЙ ЖЕ сборки — сравнение «всего вывода» даст
расхождение всегда и ничего не различит. Значит «прошла мимо» и «не сработала
вовсе» у нас сливаются в ОДИН вердикт, и делать вид, что мы их различаем,
нельзя. Работающий признак остаётся один — **строка ТОГО САМОГО стража
бит-в-бит, напечатанная ДО и ПОСЛЕ**: по ней видно, наблюдал ли страж перемену.
⛔ Не «починить» это добавлением фильтров вывода: замеры в логе несущие, они и
есть доказательная часть отчётов.

⚠️ **Первая попытка закрыла закон НАПОЛОВИНУ, и это выяснила эмпирика.**
Сравнение всего вывода ловит только совсем пустые диверсии: исторический патч
`f = (s > 2000)` менял флаг у ЧУЖИХ строк, их числа поехали, признак «поведение
изменилось» сработал — а строки стража патч не касался, и инструмент снова
сказал «страж слеп». Точный признак — строка ТОГО САМОГО стража.

✅ Вердикт теперь ТРЁХЗНАЧНЫЙ: **страж слеп** / **диверсия прошла мимо** /
**диверсия развалила сборку**. Двузначный вердикт на трёхзначном состоянии
всегда врёт в одну из сторон (то же, что жёлтый исход у смоука).

✅ **САМОПРОВЕРКА ИНСТРУМЕНТА — ПОСТОЯННОЙ ЗАПИСЬЮ В СПИСКЕ:** правка
комментария заведомо ничего не меняет, и прогон обязан назвать её пустой. Без
неё проверка поведения осталась бы непроверенной — она ведь тоже может
замолчать. Проверено: «САМОПРОВЕРКА: правка комментария поведения не меняет →
инструмент верно назвал пустую диверсию пустой».

⚠️⚠️ **ГРАНИЦА, БЕЗ КОТОРОЙ ЗАКОН ОБМАНЫВАЕТ: признак работает, ПОКА АССЕРТ
ПЕЧАТАЕТ СВОИ ЧИСЛА.** Страж вида `expect(x === y, 'ЧТО-ТО ЦЕЛО')` без значений
в сообщении по строке неотличим, и для него инструмент по-прежнему скажет
«слеп». **Правило проекта «печатай замеренное в сообщении» из вежливости стало
НЕСУЩИМ** — на нём теперь держится различение трёх признаков.

⛔⛔ **И ВТОРАЯ ГРАНИЦА, ОТМЕНЯЮЩАЯ БУКВУ ПЕРВОЙ (ФИЗИКА, 2026-08-12, поймано
САМОПРОВЕРКОЙ): сравнение строки БИТ-В-БИТ ВРЁТ У СТРАЖЕЙ, ПЕЧАТАЮЩИХ ЗАМЕР.**
Абзац выше говорит «печатает числа — значит различим», и это оказалось неточно
в обе стороны: у стража, печатающего `шагов 21, step 20, narrow 8`, числа гуляют
между двумя прогонами ОДНОЙ И ТОЙ ЖЕ сборки, и инструмент объявил замеченной
ПУСТУЮ диверсию (правку комментария). То есть у стражей БЕЗ чисел признак даёт
ложное «слеп», а у стражей С ЗАМЕРАМИ — ложное «заметил», и второе опаснее:
оно подтверждает, что проверка работает, когда она не работает.
✅ **РАБОЧИЙ ПРИЗНАК — ПРИСТАВКА `PASS:`/`FAIL:` У СТРОКИ ТОГО САМОГО СТРАЖА**,
а напечатанные числа остаются справкой для человека. Требование «печатай
замеренное» в силе — меняется способ СРАВНЕНИЯ, а не способ писать стражей.
⚠️ Найдено ТОЛЬКО самопроверкой (заведомо пустая диверсия обязана быть названа
пустой). Без неё инструмент врал бы в свою пользу и этого никто бы не увидел —
ровно то, ради чего самопроверка и заведена постоянной записью в списке.

⚠️ **КОПИЯ СБОРКИ ОБЯЗАНА ЛЕЖАТЬ РЯДОМ С ОРИГИНАЛОМ** (там же, вторая ошибка
оснастки). Копию унесли в скрэтчпад, а игра тянет ОТНОСИТЕЛЬНЫЕ пути
(`avatars/…`) — упали ТРИ ЧУЖИХ ассерта, то есть инструмент СОЗДАЛ поломку и
приписал её сборке. Тот же класс, что «прогон, правящий боевой артефакт, врёт
о самом себе», только с другого конца: там инструмент портил оригинал, здесь —
окружение копии.
⚠️ Обходной путь, если копию всё же нужно держать в стороне: отдавать её со
СВОЕГО http-стенда, а все прочие пути резолвить в настоящий каталог проекта
(так сделан прогон диспетчера по «одному числу» — чужие ассерты не пострадали).

## ПОЛОСЫ SAFARI 26: РЕЦЕПТ УЖЕ БЫЛ РЕШЁН НА ЛЕНДИНГЕ — ИСКАТЬ ТАМ

Владелец 2026-08-10: «ты знаешь причину и способ как убрать полосы в сафари,
мы это уже делали, найди». Нашлось — **`/Users/ikorzyn/Desktop/Claude/About Us`**,
`design-system.md:187` и `main.js:138-150`. ⛔ Прежде чем выводить механику
заново, читать ЭТИ ДВА МЕСТА: на них потрачен цикл через устройство владельца.

**`design-system.md:187` (дословно):** «полосу статус-бара/Dynamic Island в iOS
Safari красит **КОРНЕВОЙ CANVAS (background html)**, а theme-color тонирует
**только нижний бар**».
**`main.js:138-150`** — рабочая реализация: `html` и `body` ведутся В ЛОКСТЕПЕ
на каждой смене секции; комментарий уточняет для Safari 26, что тинт берётся из
**body**, и что «каждая секция имеет СВОЙ явный фон, поэтому цвет body на самой
странице никогда не проступает».
⚠️ Две записи РАСХОДЯТСЯ (остров от html + низ от theme-color / обе полосы от
body). Обе наши, обе с устройства, разных дат. Это СПИСОК КАНДИДАТОВ, а не
догма: на телефоне проверяется за минуту, спорить по памяти можно вечно.

⚠️⚠️ **ПОЧЕМУ В ИГРЕ НЕ СРАБОТАЛО — ВЕДУЩАЯ ГИПОТЕЗА (НЕ ПРОВЕРЕНА НА
УСТРОЙСТВЕ).** Канон говорит «fixed-элемент ОТРАВЛЯЕТ тинт своим фоном», но у
нас `#bottomBar` покрашен с альфой **.01** — слишком слабо, чтобы отравить, и
полоса берёт цвет из `html`, куда `tintChrome` пишет **ВЕРХНИЙ** стоп неба
(`skyChromeCSS = skyStops[0]`, 10-stage:578; вызов РОВНО ОДИН, 99-main:280).
Отсюда: ночью низ экрана малиновый, а полоса под ним тёмно-синяя.
**Ключевое наблюдение: альфа .01 бралась, чтобы не появилась видимая пластина.
Но если цвет элемента РАВЕН цвету экрана в этом месте, полная непрозрачность
тоже невидима — а полосу красит правильно.**
⚠️ Если сплошной фон окажется ВИДЕН (небо градиентное и внутри самой полосы
меняется) — фон полосы делать ГРАДИЕНТОМ, повторяющим небо на её высоте.
⚠️ Почему жалоба именно про ТЁМНУЮ тему: днём верхний и нижний стопы близки и
один цвет обслуживает обе кромки терпимо; ночью они расходятся максимально.

⛔ **МОЙ ВЫВОД «одним цветом две кромки не покрасить» ОТМЕНЁН** — он был сделан
до того, как я нашёл лендинг. Урок для меня: **владелец помнит решённые задачи
лучше, чем я помню свои документы; «мы это уже делали» — повод ИСКАТЬ, а не
объяснять, почему невозможно.**

## ⛔ УБИВАТЬ ТОЛЬКО СВОИ ПРОЦЕССЫ (ошибка ДИСПЕТЧЕРА, 2026-08-10)

Найдено ГРАФИКОЙ: их прогон умер с кодом 137 на 553 зелёных без вердикта, и
виноват был не их код и не память. В системе висел мой шелл с командой
```
kill -9 $(ps -eo pid,comm,args | awk '$2 ~ /node$/ && /test\.js/ {print $1}')
```
— то есть **kill -9 по ВСЕМ `node test.js` в системе**, без различения, чей это
прогон и в каком дереве. У нас параллельно работают ПЯТЬ направлений, каждое
гоняет сьют по 13 минут. Такая уборка молча убивает соседа, а выглядит это как
«сьют упал сам» — и стоит уже не одного прогона, а ложного разбора.

**ПРАВИЛО: убивать только СВОИ процессы.** PID запомнить при запуске
(`echo $! > …pid`) и бить по нему; если PID потерян — различать по рабочему
каталогу (`lsof -a -p <pid> -d cwd`), а не по имени скрипта.
⚠️ И зеркальное следствие для СЧЁТА процессов: `pgrep -f "node test.js"` тоже
считает чужие прогоны и собственные сторожевые циклы (в их командной строке та
же подстрока). «Занято» по имени скрипта — не признак; считать надо СВОЁ.
⚠️ Это уже ЧЕТВЁРТЫЙ за сессию случай одного класса у меня же: признак берётся
по совпадению подстроки, а не по принадлежности (слово `Error` в тексте PASS,
число `8788` в тексте EADDRINUSE, `pgrep` на своих же ожиданиях, теперь `kill`
по чужим прогонам). Общее правило: **прежде чем действовать по совпадению
строки, спроси, ЧЬЁ это и МОЁ ЛИ.**

⚠️⚠️ **ПРОВЕРКА «ПО РАБОЧЕМУ КАТАЛОГУ» ТОЖЕ ЛОВИТ ЧУЖИХ, И ЭТО НЕ ТЕОРИЯ**
(2026-08-12, поймано ДО удара). Правило выше советует различать процессы по
`cwd` — но воркри направлений лежат ВНУТРИ главного дерева
(`Blendo v2/.claude/worktrees/<имя>`), то есть подстрока `Blendo v2` есть и у
них. Отбор по ней показал два «моих» процесса, оба оказались сьютом ФИЗИКИ.
**Сверять cwd ЦЕЛИКОМ (главное дерево не равно воркри) либо смотреть на
`ppid`** — свой процесс всегда потомок своей же оболочки. Если PID записан при
запуске, ничего этого не нужно: бить по нему.

⚠️ **И ПРО SHELL: zsh НЕ ДОПУСКАЕТ КИРИЛЛИЦУ В ИМЕНАХ ПЕРЕМЕННЫХ.** `МОЙ=$(…)`
и префикс `СБОРКА=… node …` падают в `command not found`, причём в составной
команде это ЛЕГКО НЕ ЗАМЕТИТЬ: следующая строка печатает бодрое «убит», а
`kill $МОЙ` не сделал ничего — я запустил второй сьют поверх живого первого.
Комментарии и строки на русском можно и нужно, ИМЕНА переменных — только ASCII.
Соседняя грабля того же zsh про `for x in "a b c"` записана ниже.

## «Поправь ветку» предполагает, что ветка ЖИВАЯ — проверь замером

Интеграция, 2026-08-10. Я заказал дописать `exact: false` в ветку `code === 404`
клиента таблицы. Они проверили ЗАМЕРОМ против стенда, а не чтением, и нашли, что
**ветка недостижима**: `lbFetch` (82-lb.js:111) превращает ЛЮБОЕ поле `err` в
теле в `state:'refused'`, а сервер на «строки нет» отдаёт `{"err":"none"}` (404,
index.js:238) — значит `else if (r.code === 404)` стояло ПОСЛЕ общего выхода
`r.state !== 'ok'` и не исполнялось никогда.

⛔ **ЦЕНА В ПРОДУКТЕ, КОТОРУЮ Я ПРОГЛЯДЕЛ:** каждый игрок ДО ПЕРВОЙ ПОБЕДЫ
получал отказ, а экран по закрытому правилу (моё решение: нет признака
достоверности → не показываем) прятал блок МОЛЧА. То есть **новичок был
неотличим от мёртвого сервера** — на самом частом пути первого запуска.

**Правило: буквальное исполнение просьбы про мёртвый код даёт поле в
недостижимой ветке и абзац в доке про случай, которого не бывает, — выключенный
страж в документации.** Исполнять НАМЕРЕНИЕ: сделать ветку рабочей, и
остановиться ровно там, где намерение кончается (соседние ветки не «причёсывать»).

⛔ **ТРИГГЕР ПО `err`, А НЕ ПО КОДУ ОТВЕТА — НЕСУЩЕЕ.** У сервера ДВА разных 404:
`{"err":"none"}` (строки игрока нет, это норма) и `{"err":"route"}` (кривой
адрес, это поломка). Ветка по `code === 404` выдала бы игроку «ты просто не в
таблице» вместо аварии — ложное «ок» того же класса, что мы вычищали весь день.
`err:'none'` в сервере ровно одно вхождение.

⚠️ И УТОЧНЕНИЕ МОЕЙ ЖЕ ФОРМУЛИРОВКИ КОНТРАКТА: инвариант не «каждый ответ `ok`
несёт `exact`», а **«любой ответ, НЕСУЩИЙ МЕСТО, несёт признак его
достоверности»**. У `top()` места нет вовсе — поле там было бы обрядом, а не
проверкой. Интеграция это увидела и спросила, вместо того чтобы исполнить букву.

## Страж ПРИВОДИТ нужное состояние сам, а не наследует от соседей по странице

Интерфейс, 2026-08-10, поймано НА СЕБЕ (оба конца их): починив страж контракта,
они задали на общей странице адрес таблицы — и этим **включили фичу**, отчего
соседний страж «при выключенной фиче врезки нет» покраснел на ИСПРАВНОЙ сборке
(161 px против 161).

**Оба стража утверждали предпосылку, которую считали САМО СОБОЙ РАЗУМЕЮЩЕЙСЯ**
(«адрес не задан» / «модуля нет») и которая случайно выполнялась. Пока
выполнялась — оба были зелёными и выглядели рабочими. Сломались НЕ от правки
механики, а от того, что СОСЕД изменил обстановку на общей странице.

**Правило: страж обязан ПРИВОДИТЬ нужное состояние сам** (здесь — убрать модуль
на время замера и вернуть в `finally`), а не наследовать его. Родственно
«исполнять путь, а не ждать его», но отдельный подвид: там не хватало ДЕЙСТВИЯ,
здесь предпосылка молча пришла ИЗВНЕ.
⚠️ Признак риска: страж живёт на СТРАНИЦЕ, общей с другими стражами. Всё, что
ставит соседняя секция (localStorage, подмена сети, класс на html), для него —
невидимая предпосылка.

## Срывы сьюта без следа — это был мой `kill`, а не нестабильность

Интерфейс сообщил о ЧЕТЫРЁХ молчаливых обрывах за день (451 из 581, 475-я
строка, места разные, следа нет, `FAIL` при этом ноль). Графика независимо нашла
причину: мой `kill -9` по всем `node test.js` в системе (см. раздел «убивать
только свои процессы»). **Сигнатура совпадает: внешний `kill -9` не пишет в лог,
обрывает где застал, и отчёт выглядит как «прогон прошёл, красных нет».**
⛔ Поэтому задачу «сьют нестабилен» НЕ заводим, пока не повторится при
соблюдении правила про свои процессы.
⚠️ И цена, которую это уже стоило: у Интерфейса — ложный вывод «срыв от моих
правок» (один прогон базы против двух сорванных) и почти переписанные ЧЕТЫРЕ
здоровых стража, потому что прогон диверсий до них не доходил, а отчёт
сорванного прогона неотличим от отчёта прошедшего.

## ⛔⛔ КРОМКИ SAFARI: НЕЙТРАЛЬ ПО ТЕМЕ — ОТМЕНЕНО В ТОТ ЖЕ ДЕНЬ, РАЗДЕЛ ИСТОРИЧЕСКИЙ

⚠️⚠️ **ПЯТАЯ И ДЕЙСТВУЮЩАЯ РЕДАКЦИЯ (2026-08-13): КАЖДЫЙ ЭКРАН ВЕДЁТ КРОМКИ
САМ — «цвет каждого экрана от верхнего края до нижнего» (слово владельца,
отменившее чёрную редакцию).**

⚠️⚠️ **НАСТОЯЩИЙ ЗАКОН КАНАЛА — УСТАНОВЛЕН ЭКСПЕРИМЕНТОМ, И ОН ОТМЕНЯЕТ МОЙ
«ЗАКОН ЗАГРУЗКИ»: Safari 26 тонирует полосы ЖИВЫМ СЕМПЛИНГОМ FIXED-ЭЛЕМЕНТОВ
У КРОМОК** (финальный ресёрч About-Us: «полосы тонируются семплингом
fixed/sticky у краёв, theme-color игнорируется» + «Раунд 2: fixed перебивает
body своим background-color»). Доказательство наше: чёрная редакция
перекрасила ТОЛЬКО фикс-бары (`rgba(0,0,0,.01)`) — и полосы стали чёрными на
ВСЕХ экранах разом, включая меню и попап, где body другой.
⛔ **МОЙ ПРЕЖНИЙ ВЫВОД «Safari фиксирует цвет на загрузке» БЫЛ НЕВЕРЕН.**
Скриншот a771543 (полоса цвета неба над меню) объяснялся не «заморозкой», а
тем, что НЕ МЕНЯЛИСЬ БАРЫ: перекрашивались html/body/мета, а сэмплируемый
источник — бары — держал небесную переменную. Пять подгонов гоняли НЕ ТОТ
КАНАЛ. Урок: «канал, который мы меняли, не менял полосу» ≠ «полосу нельзя
менять» — сперва найди, ЧТО именно семплируется.

**УСТРОЙСТВО 5-Й РЕДАКЦИИ:** водитель ОДИН — `chromeSync` (85-hud): пишет
`--edge-top-rgb`/`--edge-bot-rgb` (их читают фоны `#topBar`/`#face` и
`#bottomBar` в альфе .01 — это и есть канал), фон html/body и мету в локстепе
(мета — для Android/macOS). Приоритет: тёмный оверлей (`10,14,22`) → меню
(`217,244,255`) → небо (живые `--sky-*-rgb`, день/ночь приезжают сами).
Зовётся из: show/hide (все .overlay), open/closeMainScreen, смены палитры в
updateHUD, загрузки. Хук `__game.chromeInfo()`.
⚠️⚠️ **ВИДИМОСТЬ ОВЕРЛЕЕВ — ЖИВЫМ ОПРОСОМ DOM, НЕ СЧЁТЧИКОМ:** первая версия
держала счётчик в show/hide, и он ДРЕЙФОВАЛ — часть кода и тестов прячет
оверлеи прямой записью `style.display` мимо `hide()`; кромки залипали
тёмными. У видимости уже есть первоисточник — DOM.
⚠️ Уборка кромок вскрыла ДВА унаследованных состояния в сьюте: ранняя секция
оставляла winOverlay открытым, и (1) стражи кромок краснели на чужом экране —
приведение добавлено; (2) санитар «экран статистики дошёл» был зелен ЧУЖИМ
экраном и после уборки вскрыл, что своя победа в 9 с под нагрузкой не влезает
— переведён на ожидание ФАКТА с потолком 45 с.
⚠️ Верх и низ РАЗНЫЕ по построению (два бара) — градиент экрана доезжает до
обеих кромок. Проверка — ТОЛЬКО телефоном владельца: стенд утверждает наш
канал, семплинг Safari утверждает устройство.

⛔ Ниже — история четырёх отменённых редакций (4-я, чёрная, сохранена как
КОНТРОЛЬНЫЙ ЭКСПЕРИМЕНТ, доказавший канал).

⛔⛔ **ЧЕТВЁРТАЯ РЕДАКЦИЯ (ОТМЕНЕНА НА СЛЕДУЮЩИЙ ДЕНЬ, оставлена как контрольный эксперимент канала) (2026-08-12, вечер): КРОМКИ ЧЁРНЫЕ
ВСЕГДА, ОБА КАНАЛА, СТАТИЧЕСКИ. СКРУГЛЕНИЕ ВЬЮ СНЯТО СЛОВОМ ВЛАДЕЛЬЦА
(«убирай вариант скругления вью, возвращайся к поиску решения. МНЕ НУЖНО
РЕШЕНИЕ»).**

⚠️⚠️ **ЗАКОН, ЗАКРЫВШИЙ ВСЕ ПЯТЬ ПОДГОНОВ, — И ОН ДОКАЗАН СКРИНШОТОМ ВЛАДЕЛЬЦА
ПРОТИВ КОДА: Safari 26 ФИКСИРУЕТ ЦВЕТ ПОЛОС НА ЗАГРУЗКЕ; рантайм-перекраска
(класс на html, style, мета) НЕ ПРИМЕНЯЕТСЯ.** Сборка `a771543` перекрашивала
при открытии меню ОБА канала (`html.menuopen ... !important` +
`chromeMeta(menuChrome())` — сверено по `git show`), а на скриншоте владельца
полоса над открытым меню осталась цвета НЕБА. Прежний замер «Safari читает
мету» (2026-08-10) доказывал чтение НА ЗАГРУЗКЕ (правка → редеплой → перезаход),
а не в рантайме — мы приняли одно за другое, и на этом сгорели все пять
подгонов. ⛔ Следствие: полоса держит ОДИН цвет за сессию, любое «подкрасить
под экран» мертво ПО ПОСТРОЕНИЮ — не изобретать шестой подгон.

**ПОЧЕМУ ИМЕННО ЧЁРНЫЙ ТЕРМИНАЛЕН** (а не «ещё один цвет»):
1. сверху он сливается с САМИМ ОСТРОВОМ — тот физически чёрный, полоса
   читается как рамка устройства, а не как «не тот цвет неба»;
2. тёмный фейд попапов ему родной — попапы были ЕДИНСТВЕННЫМ классом экранов,
   который не решался вообще никаким подгоном (фейд темнит страницу, полосу
   темнить нечем);
3. тёмный Safari его НЕ перекрашивает (яркие цвета он адаптирует);
4. он совпадает с режимом ОТКАЗА Safari: «отравленный» прозрачным тинт = чёрный
   (рецепт About-Us существовал ровно против этого) — даже если Safari
   проигнорирует все каналы, полосы выйдут ТЕ ЖЕ. Решение, чей failure mode
   равен ему самому.
День/ночь, меню и попапы НЕ ТРОНУТЫ — жертвы владельца («отказаться от
времени суток», «свести градиент к одному цвету») НЕ ПОНАДОБИЛИСЬ; к тому же
единый цвет кромок всё равно не чинил попапы и ронял пол контраста HUD (3:1).
⚠️ Побочное, названо владельцу: ПЕРВЫЙ КАДР загрузки теперь чёрный, а не белый
(заливка неба #skyFill едет по чёрному) — прежнее «сначала белый» жило на белом
фоне документа. Вернуть белый старт нельзя без пятна на кромках.
⚠️ Идеал «градиент до самого края» в браузерном Safari НЕДОСТИЖИМ ФИЗИЧЕСКИ
для нескроллящейся страницы (страница не рисует под статусбаром); он существует
только в standalone («На экран Домой») и на портале Playgama (там полосы —
портала). Проверка 4-й редакции — ТОЛЬКО телефоном владельца.

⛔⛔ **ТРЕТЬЯ РЕДАКЦИЯ (2026-08-12, день, ОТМЕНЕНА В ТОТ ЖЕ ДЕНЬ): кромки по теме
устройства + скругление вью 40px.** Дословно: «сделаем скругление вью по 40 px для мобильного. Фон
при этом под островом и строкой поиска будет зависеть от темы устройства». Мы
БОЛЬШЕ НЕ КРАСИМ полосы ничем: `html` берёт цвет из `prefers-color-scheme`, мета
— две статические `<meta media=...>`, `tintChrome` пуста (надгробие в 99-main),
`CHROME_SWAP` удалён, правило `html.menuopen` кромку не трогает.
⚠️ ЗАМЕР ПЕРЕД ПРАВКОЙ (390×780 / 360×740 / 320×568 / 430×932): срез убирает
1292 пикселя ФОНА и РОВНО НОЛЬ пикселей краски HUD. ⛔ Мерить это габаритом
НЕЛЬЗЯ — два первых замера так и соврали «срезано», считая пустые углы
bounding box круглых кнопок. Верный признак: пиксель, ставший фоном ПОСЛЕ
клипа, сверяется со своим цветом ДО клипа.
⚠️ `clip-path`, а не `border-radius`+`overflow`: канвас и бары — `position:fixed`
прямые дети `body`, overflow предка их не режет. И `html` обязан иметь СВОЙ фон,
иначе фон `body` распространяется на холст и скругления не видно вовсе.
⚠️⚠️ **НА УСТРОЙСТВЕ НЕ ПРОВЕРЕНО** — как и весь этот рецепт, он подтверждается
только телефоном владельца. Стенд показывает, что цвета и клип встают верно.

⛔ Ниже — история двух прежних редакций.

⛔ **ПЕРВОЕ РЕШЕНИЕ ПРОЖИЛО ОДИН ДЕНЬ И БЫЛО ОТМЕНЕНО СЛЕДУЮЩИМ СЛОВОМ**
(2026-08-10, коммит `e962931`): «если у нас под островом ничего нет, то фоновый
градиент должен заполнять всё от самого верха до самого низа». **Действующее
правило — ПОДБОР ПОД ПАЛИТРУ, а не нейтраль**: игра — `html/body` верхний стоп
неба, мета `theme-color` нижний (обмен одной константой `CHROME_SWAP`); меню —
оба канала фон меню `--ms-bg` `#d9f4ff`. Стражи в сьюте это и утверждают, причём
у каждого вторая половина «**И НЕ нейтраль**» — иначе возврат белого прошёл бы
молча.
⚠️⚠️ **РАЗДЕЛ ПРОСТОЯЛ ДЕЙСТВУЮЩИМ ДВОЕ СУТОК И УСПЕЛ НАВРЕДИТЬ:** ИНТЕРФЕЙС
построил по нему стража второго канала, тот потребовал совпадения обоих каналов
и **покраснел на ЗДОРОВОЙ сборке** — каналы при `CHROME_SWAP=false` несут РАЗНОЕ
по построению (замер: фон `rgb(3,29,131)`, мета `rgb(216,38,186)` — верх и низ
ночной палитры). Требование «оба канала несут одно значение» было верно ТОЛЬКО
для нейтрали.
⚠️⚠️ **И ЭТО ПОПРАВКА К МОЕМУ ЖЕ ПРАВИЛУ ПРО ДВА ЖАНРА АБЗАЦЕВ** (см. раздел
«Первоисточник бьёт пересказ»). Я написал, что абзац-РЕШЕНИЕ «не протухает
никогда». Неточно: он не протухает от дрейфа кода, но **отменяется более поздним
решением владельца — и тогда обязан быть помечен, а не остаться стоять**.
Отменённое решение опаснее устаревшего описания: описание можно проверить кодом,
а решение выглядит как приказ, спорить с которым замером «не положено».
**Практика: у решения писать ДАТУ и, отменяя его, ставить надгробие ЗДЕСЬ ЖЕ, в
шапке раздела, а не только в новом разделе.**

Ниже — как было ДО отмены, оставлено ради причин и замеров.

Дословно: «можешь убрать заливку цветом под островом?» → затем «на игровом тоже
сделай нейтральной». **Обе полосы, оба экрана: светлая тема — чистый БЕЛЫЙ,
тёмная — чистый ЧЁРНЫЙ.** Подбор под палитру неба ОТМЕНЁН.

✅ **ЗАМЕР, ЗАКРЫВШИЙ СТАРЫЙ СПОР: Safari 26 ЧИТАЕТ `theme-color`.** До правки
полоса в меню была `#6e86ff` (верхний стоп неба), после смены меты стала светлой
— на устройстве владельца. Наши два документа противоречили друг другу; теперь
у нас есть наблюдение, а не версия.
⚠️ Почему подбор под тон отменён и не подлежит возврату: **меню ПРОКРУЧИВАЕТСЯ**,
и под островом оказывается то карточка, то фон, то сетка коллекции. Одним
значением в прокручиваемое содержимое попасть нельзя ПО ПОСТРОЕНИЮ.

**ОБА КАНАЛА НЕСУТ ОДНО ЗНАЧЕНИЕ** (фон `html`/`body` и мета) — тогда вопрос
«какой из них читает Safari» перестаёт иметь значение. Это и было главной
неопределённостью двух суток.

⚠️⚠️ **ТЕМА БЕРЁТСЯ ЧЕРЕЗ `isNightSky()`, А НЕ ЧЕРЕЗ КЛАСС `html.night`**
(находка Интерфейса замером): `tintChrome` зовётся РАНЬШЕ первого тика HUD,
который класс и вешает — по классу ночью вышел бы БЕЛЫЙ. Пятый случай на законе
«копия признака расходится с источником».
⚠️ **У МЕНЮ НЕТ НОЧНОГО ВАРИАНТА** — `--ms-bg` объявлен ровно один раз, меню
светлое в обеих темах; «чёрный ночью» там был бы неверен.
⚠️ **ТЕМА МЕНЯЕТСЯ В ЖИВОЙ СЕССИИ** (граница 20:00): перекраска на ПЕРЕХОДЕ, не
каждый тик и не поверх открытого меню.
⛔ Красная угроза помола полос БОЛЬШЕ НЕ КАСАЕТСЯ — нейтраль от палитры не
зависит. Не «чинить» связь, которой нет.

## Стражи ПЕРЕЕЗЖАЮТ за сменой правила, а не «чинятся»

При отмене подбора под палитру пять ассертов покраснели — и ни один не был
поломкой: они утверждали «кромка в тоне НЕБА» и «в тоне МЕНЮ», то есть правило,
которое владелец только что заменил. **Правило сменилось — страж обязан
переехать за ним.**
⚠️ И ДВЕ ТОНКОСТИ, БЕЗ КОТОРЫХ ПЕРЕЕЗД БЫЛ БЫ ФОРМАЛЬНЫМ:
1. **У каждого ассерта вторая половина несущая:** «нейтраль И НЕ равна стопу
   неба». Без неё ассерт остался бы зелёным при возврате подбора под палитру —
   то есть ровно при том дефекте, который правка и убирает.
2. **Страж отменённого градиента СОХРАНЁН С ОБРАТНЫМ УТВЕРЖДЕНИЕМ, а не удалён.**
   Соблазн при смене правила — снести мешающий ассерт; тогда возврат старого
   поведения прошёл бы молча. Переворот стоит одной строки и оставляет дверь
   под наблюдением.
⚠️ Ассерт, выродившийся в тавтологию после смены правила («переход небо → меню»
стал «белый = белый»), заменён на другое утверждение («после закрытия ничего не
залипло»), а не оставлен зелёным украшением.

## Склейка двух ПРАВИЛЬНЫХ блоков даёт неправильный порядок (диспетчер, 2026-08-10)

Разрешая конфликт в `test.js`, я склеил подряд две стороны: блок Интеграции
(стражи отправки) и блок Интерфейса (стражи экрана таблицы). Обе стороны были
верны ПООДИНОЧКЕ: первая ЗАКРЫВАЛА за собой страницу в конце, вторая работала на
УЖЕ ОТКРЫТОЙ. После склейки прогон падал `Target page has been closed` на 559-м
ассерте — **без вердикта, то есть выглядел как «оборвался сам»**.
**Правило: при склейке конфликта проверять не только СОДЕРЖИМОЕ сторон, но и их
КРАЯ — что каждая сторона ожидала до себя и что оставляет после.** Ресурс,
который одна сторона закрывает, а другая наследует, — типичный случай.
⚠️ Симптом неотличим от внешнего обрыва: зелёные есть, красных нет, вердикта
нет. Проверять `SUITE:` в логе, а не только счётчик FAIL.

## ТРИ рукотворных источника «сьют оборвался сам» — и ни одного настоящего флейка

За 2026-08-10 у нас было три разных обрыва, все выглядели ОДИНАКОВО (зелёные
идут, `FAIL` ноль, вердикта нет) и ни один не был нестабильностью:
1. **`kill -9` по имени скрипта** (диспетчер) — гасил чужие прогоны пяти
   направлений;
2. **вырезанный диапазон текста** (Интерфейс) — унёс соседнее объявление
   `hudWasNight`, страница падала на первом `updateHUD`;
3. **склейка конфликта** (диспетчер) — закрытая страница у следующего блока.
⛔ Вывод: **прежде чем заводить задачу «сьют нестабилен», исключить рукотворное.**
Три случая подряд объяснились полностью, и в каждом первая гипотеза была
«флейк».

## ПЕРЕДАЧА СОСТОЯНИЯ: экран таблицы, следующая сессия ИНТЕРФЕЙСА (2026-08-10)

Работа остановлена на исчерпании контекста, НЕ на середине правки — в общей
ветке недоделки нет. Порядок первых шагов:
1. **Снять вкладки** (решение владельца: «только наша таблица»), убрать
   `TEXT_PENDING` и его диверсию; оставить стражи «крестик закрывает» и «список
   не пуст молча» — они про общее. Комментарий «платформенная таблица живёт, но
   НЕ показывается» — у экрана и у `setScore`.
2. **Точка входа** в меню (`840:4344`), она раньше полного экрана: видна каждому,
   а экран — только нажавшему.
3. Полный экран (`840:1269` моб. / `840:1230` десктоп).

⚠️⚠️ **ТРИ ПОПРАВКИ ДИСПЕТЧЕРА К УМОЛЧАНИЯМ ПРЕДЫДУЩЕЙ СЕССИИ** (письмо могло
разойтись с их ходом — читать ОТСЮДА):
- ⛔ **ЗВЕЗДА ЕСТЬ:** `Interface/Star.svg`, прислана владельцем 2026-08-07, уже
  используется (2 вхождения в `shell.html`). Символом `★` НЕ верстать. Два глифа
  макета — одна звезда в двух цветах; перекраска — `filter: invert/sepia/
  hue-rotate` (единственный приём, что не сломался; `mask-image` НЕ пробовать).
- ⛔ **ТОКЕН — ЛОВУШКА ГЛУБЖЕ, ЧЕМ ВЫГЛЯДИТ.** `Carbon-Purple-400` из макета =
  `#2a2935`, а `--ink` это `#1d1c26` (комментарий у него ВРЁТ, исправить
  надгробием). ⚠️ И `--btn-bg` брать НЕЛЬЗЯ, хотя значение совпадает: он
  ПЕРЕВОРАЧИВАЕТСЯ (`html.night { --btn-bg:#ffffff }`), и тёмный текст пилюли
  ночью стал бы белым на жёлтом. Заводить настоящий токен `--carbon-400` на
  `:root`, НЕ переворачивающийся; локальную копию у экрана НЕ делать.
- ✅ **ДУБЛЬ СТРОКИ «YOU» — умолчание принято**, владельца не тревожим:
  исключать себя из списка, пока строка ЗАКРЕПЛЕНА (мобильный). ⚠️ На десктопе
  она В ПОТОКЕ — там исключать НЕ надо, там она и есть место в списке. Правило
  не делать общим на оба режима.
- ✅ Место в кружке — существующий `winFmtScore` (12480 → «12.5k»), второго
  правила не заводить.

**ДАННЫЕ ТОЧКИ ВХОДА:** оба вызова через `__lb` — он уже кэширует `top()` и
`me()` и умеет разовый обход HTTP-кэша при сбросе (добавлен после живого
прогона: чужой кэш браузера бьёт поверх нашего `max-age`, и число не менялось бы
минуту ровно после победы и траты). Второй сетевой путь НЕ заводить — меню
открывается часто.
⚠️ «You on 845» — только из `me()` и только при `exact`; нет точного — строки с
числом нет, блок живёт без неё. **Прикидку из ответа на отправку не показывать
НИГДЕ.**

**Подзаголовок — 4 варианта на выбор владельца**, рекомендованный первым:
«Your place is what you have now, not what you once earned.» (объясняет МОДЕЛЬ,
а не механику покупки).

**Долги, никого не блокируют:** диверсии по переехавшим стражам кромки; замер
«что заезжает под кромки при прокрутке меню».

## ⚠️⚠️ ФИЧА БЫЛА ВЫКЛЮЧЕНА ЦЕЛИКОМ И МОЛЧА: `LB_URL` НЕ БЫЛ ОБЪЯВЛЕН НИГДЕ

Найдено диспетчером 2026-08-10 при реализации точки входа. `LB_BASE` (82-lb.js)
падает в ПУСТУЮ строку, если `LB_URL` не объявлен, а на пустом адресе честно
молчат ВСЕ три потребителя разом: врезка победы, точка входа в меню и сам экран
таблицы. То есть **сервер был развёрнут и зелен по смоуку, экран свёрстан,
клиент написан — и в поставке таблицы не существовало.**

⛔ **НИ ОДИН СТРАЖ ЭТОГО НЕ ВИДЕЛ, И ЭТО НЕ СЛУЧАЙНОСТЬ.** Каждая секция сьюта
задаёт адрес себе САМА (`localStorage.mixer_lb_url`), потому что иначе не
проверила бы механику вовсе. Значит все они проверяют поведение ПРИ ЗАДАННОМ
адресе и слепы ровно к тому, что в сборке его нет. **Общее: страж, который сам
создаёт предпосылку, никогда не проверит её наличие в поставке.** Лечится
отдельным ассертом, читающим СБОРКУ (`index.html`), а не страницу.

Действующее — в конце 00-config.js:
```js
const LB_URL = (typeof location !== 'undefined' && location.protocol === 'file:')
  ? '' : 'https://lb.blendo.monster';
```
⚠️ Собственный домен, а НЕ `*.workers.dev`: поддомен у воркера выключен
(`workers_dev = false`), такой адрес отвечал бы отказом. Страж держит оба
признака — задан И не workers.dev. Переопределение для разработки прежнее и
приоритетнее: `?lb=1`, `?lb=<адрес>`, `localStorage.mixer_lb_url`.

⚠️⚠️ **ГЕЙТ ЛОКАЛЬНОГО ХОСТА — НЕСУЩИЙ, И ОН ПОЙМАН НА СЕБЕ ДВАЖДЫ ПОДРЯД.**
Сьют, соак и превью направлений доигрывают до победы; победа банкует счёт, тот
дёргает `onStarsChange` — и без гейта каждый прогон заводил бы в БОЕВОЙ таблице
настоящую строку. Отправка проходит: CORS у воркера открыт намеренно
(`ACAO: *`), гость у нас полноправен (решение владельца), а `localStorage
.clear()` в тестовых секциях выдаёт свежий id — то есть НОВУЮ строку на прогон.
- **ЗАХОД ПЕРВЫЙ (гейт по `file:`).** Замер: прогон, убитый через ~5 минут,
  успел записать ДВЕ строки (`Tanuki` 83, `Perch` 100). Удалены.
- ⛔ **ЗАХОД ВТОРОЙ: ГЕЙТ ПО ПРОТОКОЛУ ПРОПУСКАЛ.** Сьют поднимает СВОЙ
  http-сервер и открывает игру по `http://127.0.0.1:<порт>/index.html` (четыре
  секции моста и площадки) — там протокол уже не `file:`. Замер: два зелёных
  прогона занесли в живую базу ТРИ строки (`Perch`/`Teal`/`Magpie`, счёт 100).
  Тоже удалены; сейчас `SELECT COUNT(*) FROM p` = 0.
  **Признак «локальность» обязан читаться по ХОСТУ, а не по протоколу** —
  иначе он описывает частный случай и молчит про остальные.

- ⛔ **ЗАХОД ТРЕТИЙ: И ПО ХОСТУ ПЕРЕЧИСЛЕНИЕ БЫЛО НЕПОЛНЫМ.** Нашла ИНТЕГРАЦИЯ
  прогоном списка хостов (не чтением): `<имя>.local` — **Bonjour, именно так
  телефон ходит на Mac** — и адреса домашней сети (`192.168.`, `10.`, `172.16-31.`,
  `169.254.`) гейт пропускал. То есть первый же ТЕЛЕФОННЫЙ плейтест сыпал бы
  строки в боевую таблицу. ⚠️ Регулярка `/\.localhost$/` `.local` НЕ ловит.
  **Третий раз подряд один закон: гейт перечисляет случаи, которые ВСПОМНИЛИ, а
  не те, которые есть.** Значит проверять надо ПЕРЕЧИСЛЕНИЕ целиком, таблицей.

Действующий признак — `lbHostIsLocal(protocol, hostname)` (00-config), а
`LB_LOCAL` просто зовёт её от `location`: `file:` / пустой хост / `localhost` и
`*.localhost` / **вся петля `127/8`** / **`*.local`** / `10.` / `192.168.` /
`172.16-31.` / `169.254.` / IPv6 `::1`, `fe80::/10`, `fc00::/7`.
⚠️⚠️ **IPv6 ПРИХОДИТ В СКОБКАХ:** `location.hostname` даёт `[fe80::1]`, поэтому
префикс проверяется ПОСЛЕ снятия скобок. Прежняя версия закрывала `::1` только
потому, что рядом стоял литерал `[::1]`, — на `fe80:` этот приём уже не работал.
⚠️ И условие «это вообще IPv6» (есть двоеточие) обязательно: без него `/^f[cd]/`
объявил бы локальным любой хост вида `fd-server.example.com`. В таблице стража
это стоит отдельными строками (`fd-server.example.com`, `fe80.example.com`,
глобальный `[2a00:1450::200e]` — все ЧУЖИЕ).
⚠️⚠️ **ЧИСТАЯ ФУНКЦИЯ — НЕ КОСМЕТИКА, А УСЛОВИЕ ПРОВЕРЯЕМОСТИ:** таблицу хостов
через `location` не прогнать (браузеру произвольное имя не задать), а без
таблицы проверяется один вспомненный случай. Хук `__game.lbHostIsLocal`.
⛔ **БЕЛЫЙ СПИСОК НЕВОЗМОЖЕН** (боевых хостов много, порталы заранее не
известны) — значит перечисляем ЛОКАЛЬНОЕ и обязаны держать список полным.
⚠️ **ЦЕНА НАЗВАНА ЯВНО: на превью-порту владельца (8781) таблицы тоже нет.**
Живая проверка «игра → сервер» делается ССЫЛКОЙ с явным адресом:
`http://localhost:8781/index.html?lb=https://lb.blendo.monster` (параметр
приоритетнее константы). Размен осознанный: молчание на локальном стенде
лечится одной ссылкой, засорённая ботами таблица — только руками в базе.
⚠️⚠️ **СТРАЖ ГЕЙТА — ПОВЕДЕНЧЕСКИЙ, А НЕ ТЕКСТОВЫЙ, И ЭТО ГЛАВНЫЙ УРОК ЗАХОДА.**
Первая версия ассертила подстроку `file:` в выражении константы — и была бы
ЗЕЛЁНОЙ на той самой сборке, которая писала в продакшн: она проверяла
ФОРМУЛИРОВКУ, а не следствие.
**Сейчас стражей ТРИ, и каждый закрывает свою дыру:**
1. **сквозной** — сьют поднимает настоящий локальный сервер и читает
   `__lb.base()` (обязан быть пуст) + подмена `?lb=` доказывает, что ноль
   получен ГЕЙТОМ, а не поломкой тракта;
2. **таблица хостов** по `lbHostIsLocal` — и обязательно С ГРАНИЦАМИ: `172.15`
   и `172.32` (соседи диапазона RFC1918 16..31) и `localhost.evil.com`
   (подстрока — не якорь) обязаны считаться ЧУЖИМИ. Без этой второй стороны
   страж зелен у функции `return true`;
3. **сквозной `.local`** — отдельный браузер с
   `--host-resolver-rules=MAP <имя>.local 127.0.0.1`. ⚠️ Замечание ИНТЕГРАЦИИ,
   принятое дословно: сквозной страж на `127.0.0.1` доказывает случай, который
   работал и ДО правки; телефонный путь им не проверяется по построению.
Замер: `http://127.0.0.1` → `""`, `<имя>.local` → `""`, `file://` → `""`,
`?lb=http://lb.probe` → `http://lb.probe`, таблица чиста в обе стороны.
⚠️ И мелочь того же класса, пойманная на себе: `srvLB.close()` стоял ПОСЛЕ
первой проверки, а следующие пользуются тем же портом — страж падал бы на
«connection refused», то есть не на проверяемом свойстве, а на своей уборке.
⚠️ И МЕТОДИЧЕСКОЕ: заметить это можно было ТОЛЬКО по чужому серверу — ни один
ассерт не покраснел бы, сборка зелёная, игра работает. Класс тот же, что у
«мягкая деградация ослепляет проверку по статусу»: побочный эффект наружу
стражи внутри страницы не видят по построению.

⚠️⚠️ **ЦЕНА ГЕЙТА, НАЗЫВАЮ ЗАРАНЕЕ (оговорка Интеграции): собранный `index.html`,
открытый ДВОЙНЫМ КЛИКОМ, в таблицу не пишет вовсе.** Это `file://`. Живую связку
«игра → сервер» проверять только с превью-порта (владелец — 8781) и БЕЗ `?lb=`,
с чистым `localStorage.mixer_lb_url`; иначе уйдёшь на стенд и увидишь пустую
боевую таблицу при исправно работающей отправке.
⚠️ Признак, что попало правильно: `n` в `/v1/top` останется 0 до ближайшего тика
крона (топ идёт из СНИМКА), а сама строка видна сразу через `/v1/me`.
⚠️ И замер Интеграции, уточняющий масштаб: **одна победа = ДВЕ попытки
`POST /v1/score`** (вторая из `lbAgain`), то есть на прогон это строка ПЛЮС
апдейт, а не одна запись. Совпало с живым наблюдением: убитый через ~5 минут
прогон оставил ровно две строки.

## ТОЧКА ВХОДА В ТАБЛИЦУ — РЕАЛИЗОВАНА (диспетчер, 2026-08-10)

Один узел `#msLbEntry`, два представления. **ЖИВЁТ ВНУТРИ `.ms-collhead` ПО
ПОСТРОЕНИЮ, а не для порядка в файле:** на мобайле обёртка `display:contents`
растворяется и блок встаёт прямым ребёнком `.ms-wrap` со своим `order:1` —
ровно между профилем (−1) и Play (2), как в макете. На десктопе `.ms-wrap` —
грид с ИМЕНОВАННЫМИ областями, и блок-сирота без `grid-area` встал бы
автоплейсментом, ломая раскладку; внутри обёртки он наследует `chead` даром.

- **Действие — ОДНА настоящая кнопка** (`#msLbeOpen`), слушатель ОДИН и висит
  на РЯДЕ: клик по кнопке (мышью или Enter'ом) всплывает туда сам. Второй
  слушатель на кнопке давал бы ДВА открытия на нажатие, то есть два сетевых
  захода. Страж считает кнопки (ровно 1).
- **Токен `--carbon-400: #2a2935`** заведён на `:root`; у `--ink` поставлено
  надгробие (его комментарий «carbon-purple из макета» врал). `--btn-bg` не
  берём, хотя значение то же: он переворачивается в тёмной теме.
- **Временная строка «Leaderboard [Open]» из настроек снята.** ⚠️ Запрет,
  который переездом НЕ отменён: в САМ ряд профиля второй элемент не ставить —
  на 320 он вылезал из пилюли на 29px. Новый блок стоит ПОД рядом.
- **Данные — только через `__lb`** (`top()` → три аватара, `me()` → место),
  обновление в `openMainScreen` ПОСЛЕ гварда чужой паузы: при отказе открыться
  сетевого захода быть не должно. Своя эпоха на выброс ответа — меню закрывают
  быстрее, чем отвечает сеть.
- ⚠️ Место — ТОЛЬКО точное (`exact`); нет — числа нет ни строкой, ни в
  заголовке. Страж проверяет это на пути НОВИЧКА (`/v1/me` → 404 `err:"none"`),
  то есть на самом частом первом запуске.
- Стражи: место в раскладке (между профилем и Play — прежнее указание «вместо
  баннера» отменено макетом, и без ассерта возврат к нему прошёл бы молча),
  одна кнопка, отсутствие числа без `exact`, нулевая высота при выключенной
  фиче (состояние ПРИВОДИТСЯ хуком `__game.lbEntryRefresh()`, а не наследуется).
- ⚠️ **ТРИ АВАТАРА ВСЕГДА, И ЭТО ОТМЕНИЛО ЗАМЕР** (слово владельца 2026-08-10
  «всегда показывай 3 аватарки»): прежнее правило прятало третий на узких
  экранах. ЗАМЕР, КОТОРЫМ ОНО БЫЛО ОБОСНОВАНО, ОСТАЁТСЯ ВЕРНЫМ И ОБЪЯСНЯЕТ
  ЧИСЛА: на 320 ряд не влезал РОВНО НА 7px, поэтому вместо скрытия — СТОПКА
  внахлёст (`margin-left` −10 / −12 при ≤389 с аватаром 34 / −15 при ≤359 с
  28) плюс белая обводка 2px, чтобы стопка читалась как стопка. Пустой слот —
  `div`, а не `img`: страж обязан считать `> *`, иначе у новичка «аватаров 0».

## Плейсхолдеры пустых слотов ломают счётчик, считающий картинки

Отдельным правилом, потому что грабля общая: у ряда аватаров точки входа слот
без данных рисуется `div`-заглушкой. Страж, считающий `img`, у НОВИЧКА (топ ещё
не пришёл) насчитает ноль и будет зелен при поломке — «три аватара» надо считать
по ДЕТЯМ контейнера, а не по типу узла. Тот же класс, что «страж читает
кэшированное состояние»: метрика правдоподобна и молчит.

✅ **ПО ДЕСКТОПУ ВСЁ СДЕЛАНО (2026-08-11/12) — АБЗАЦ-ЗАДАНИЕ СНЯТ.**
⛔⛔ **ОН ПРОСТОЯЛ УСТАРЕВШИМ И СТОИЛ ИНТЕРФЕЙСУ НАЧАТОЙ РАБОТЫ:** они пришли
сверяться перед десктопной карточкой, прочли этот список и доложили, что
«карточки и разделителя нет», а `.ms-collhead` из дерева исчез. Ни то, ни другое
не было правдой уже сутки. Ровно та грабля, что записана у нас как «первоисточник
бьёт пересказ», только пересказом был МОЙ СОБСТВЕННЫЙ канон.
**Правило: закончил пункт — вычеркни его в ТОТ ЖЕ коммит, а не «потом».
Абзац-задание, переживший свою задачу, работает как ложный приказ.**

Действующее состояние (проверять КОДОМ, не этим абзацем):
- `.ms-card` (`shell.html`) — белая карточка, радиус 32, паддинг 16, зазор 16,
  `grid-area:prof`; оборачивает ряд профиля `.ms-head`, разделитель и точку входа;
- `.ms-card-sep` — линия во всю ширину (`840:4688`), **только десктоп**: на
  мобайле это две отдельные пилюли, делить нечего;
- ⚠️⚠️ **ПЕРЕНОСА УЗЛОВ НЕ ПОТРЕБОВАЛОСЬ, и это несущее.** На мобайле
  `.ms-card { display:contents }` растворяет обёртку, дети встают прямыми
  детьми `.ms-wrap` со своими `order`; на десктопе она настоящая карточка в
  области `prof`. Прежнее опасение «карточка требует переноса профиля в правую
  колонку через `layoutHUD`» СНЯТО: профиль уехал в верх ЛЕВОЙ колонки
  (`grid-template-areas:"prof prof chead"`, макет `763:1031` + `840:4679`), а
  заголовок коллекции встал по центру правой. Решение «одна разметка на обе
  раскладки, без дублей узлов» ЦЕЛО;
- имя игрока на десктопе видимо: `.ms-uname { display:block; 22px/900 }`;
- кнопка `More` на `#ffc800` (`840:4685/4687`), лайм отменён.

⚠️ **НОДА `840:4618` УСТАРЕЛА** — по ней больше не верстать и не искать её.
Действующие узлы десктопной шапки: карточка/пилюля `840:4679`, разделитель
`840:4688`, точка входа `840:4633`, паддинг слева `840:4689`, группы справа
`840:4692`, кнопка `840:4685/4687`.
⚠️ Страж «на десктопе заголовок и профиль в одном ряду (cy 40)» ПЕРЕЕХАЛ вместе
с правилом (профиль слева, заголовок по центру правой колонки), а не «починен».

## ТОЧКА ВХОДА В ТАБЛИЦУ — ДЕСКТОП (макет `763:1031`, 2026-08-10; ⚠️ СПЕКА НИЖЕ ЧАСТИЧНО УСТАРЕЛА)

⚠️⚠️ **ЧИТАТЬ ВМЕСТЕ С АБЗАЦЕМ ВЫШЕ: раздел писался по ноде `840:4618`, которой
БОЛЬШЕ НЕТ, и часть его чисел отменена более поздними макетами владельца.**
Действующие узлы перечислены выше; здесь оставлено то, что объясняет РЕШЕНИЯ.
⛔ Конкретно отменено: «одна строка `Leaderboard • 678`, место цветом `#b0bcd0`»
— место идёт ДВУМЯ строками, как на телефоне (слово владельца 2026-08-10), и
`.ms-lbe-dot` на десктопе больше не показывается; зазор аватаров 6 отменён
макетом `840:4679` в пользу 2 при аватаре 40.

Владелец обновил ПРАВУЮ ВЕРХНЮЮ часть десктопа. ⚠️ Это НЕ адаптив мобильного
блока — **два разных решения**, верстать порознь.

**Карточка `840:4618`:** белая, `border-radius 32`, padding 16, колонка, gap 16.

**Ряд 1 — профиль (`770:1369`), space-between:**
- слева: аватар 48 + имя, gap 6; имя **Heavy 22px**, цвет `--carbon-400 #2a2935`;
- справа, gap 12: звезда 25×24, счёт **Black 32px ЧЁРНЫЙ** («166.5K» — сжатый
  формат, у нас это `winFmtScore`), кнопка-пилюля.
⚠️⚠️ **КНОПКА ИЗМЕНИЛАСЬ: было «Get More» ЛАЙМОВАЯ, стало «More» ЖЁЛТАЯ
`#ffc800`**, текст Bold 16px чёрный, паддинги 16/13. Это правка существующего
элемента, а не новый — не пропустить при вёрстке.

**Разделитель:** горизонтальная линия на всю ширину карточки.

**Ряд 2 — точка входа (`840:4633`):** белая пилюля, `padding-left 8`,
space-between:
- слева ОДНОЙ строкой: «Leaderboard » **Heavy 22px чёрный** + «• 678» тем же
  кеглем, но цветом **`#b0bcd0`**;
- справа, gap 12: три аватара 40×40 (gap 2) + **кнопка «Open»** фоном `#d9f4ff`,
  текст Bold 16px чёрный, пилюля.

⚠️ **ЧЕМ ОТЛИЧАЕТСЯ ОТ МОБИЛЬНОЙ** (`840:4344`) — не перепутать:
| | мобильная | десктоп |
|---|---|---|
| текст | ДВЕ строки: «Leaderboard» + «You on 845» 14px `#a2a2a8` | ОДНА: «Leaderboard • 678», место цветом `#b0bcd0` |
| фон | `rgba(239,251,255,.6)` + рамка 1px белая | белый |
| действие | стрелка 32×32 | КНОПКА «Open» `#d9f4ff` |

Данные те же: место — только из `me()` и только при `exact`; три аватара — из
`top()`; оба через `__lb`, второго сетевого пути не заводить.

## СБРОС ПРОГРЕССА ОБЯЗАН ДОЕХАТЬ ДО ТАБЛИЦЫ (жалоба владельца 2026-08-11)

Дословно: «я сбросил прогресс через панель разработчика, но остался в таблице
лидеров на прошлом месте». Дефектов оказалось ДВА, и каждый молчал по-своему.

1. **СБРОС НИКОГО НЕ ИЗВЕЩАЛ.** `resetProgress` (77-save) был ЕДИНСТВЕННЫМ
   изменением баланса без `fireStarsChange` — семь других мест его зовут, он
   нет. Подписчик в 82-lb (забыть кэш + отправить новое число) просто не
   просыпался. Лечение — вызов события, а НЕ свой сетевой путь: копия чужого
   тракта рядом с работающим разошлась бы с ним при первой правке.
2. **НОЛЬ НЕ ОТПРАВЛЯЛСЯ НИКОМУ.** Правило `if (!(s > 0)) return` защищало от
   реального дефекта («зашедший на десять секунд гость строку не плодит»), но
   било по ДВУМ случаям сразу: и по созданию строки, и по её ОБНОВЛЕНИЮ.
   ⚠️⚠️ И ЭТО ЛОМАЛО НЕ ТОЛЬКО СБРОС: «Форбс»-модель владельца обещает
   «спустил всё → 0, низ таблицы» (канон 2026-07-29) — последний шаг ДО НУЛЯ
   не доезжал до сервера НИКОГДА. Жалоба про сброс вскрыла общий дефект.
   Действующее условие: `!(s > 0) && !(lbSentScore > 0)`.
   ⚠️ Признак «строка есть» — именно `lbSentScore > 0`, а не `lbRegistered()`:
   регистрация помнит отправленный КЛЮЧ, а нужно знать, что на сервере лежит
   положительное число, которое ноль перезапишет. Он же переживает отложенную
   посылку: после 429 `lbSchedule` зовёт `lbSubmit()` БЕЗ аргументов, и
   флаг-параметр здесь потерялся бы.
⚠️ Проба двурукая (стенд по http, `LB_NOSEND` на `file:` глушит отправку —
первая версия пробы мерила собственную заглушку): «строка есть» → после сброса
уходит `s:0`; НОВИЧОК → не уходит ничего. ⛔ Контрольная рука обязана быть
именно новичком: если он сперва заработает и отправит, строка появится, и ноль
станет ПРАВИЛЬНЫМ — на этом первая версия пробы себя же и обманула.

## ДВА ЧИСЛА ОДНОЙ СТРОКИ ИЗ РАЗНЫХ ЭПОХ (там же, второй дефект скриншота)

На том же скриншоте: «Goldeneye • You 8 668» стоит ВОСЬМЫМ между 6 500 и 5 300,
а меню тут же показывает «4 place». Механика: топ приходит из ЧАСОВОГО СНИМКА,
своё место и счёт — ЖИВЫЕ. `lbLoadOurs`, найдя себя в снимке, брал СЧЁТ живой,
а НОМЕР — снимка («тогда список нумерован из одного источника»). Полагание
оказалось неверным: колонка счёта переставала быть убывающей, то есть таблица
противоречила сама себе, и противоречие видно глазом.
**Правило: строка собирается ИЗ ОДНОЙ ЭПОХИ ЦЕЛИКОМ.** Своя — живая, значит и
место живое: `m.rank`. Ранг считает сервер по живой базе, снимок — её
приближение, поэтому вставка по рангу САМА восстанавливает монотонность
счёта — отдельной сортировки не нужно.
⚠️ ДЛИНА ОТРЕЗКА НЕ МЕНЯЕТСЯ, и это решает «вставка или замещение»: нашли себя
в снимке — старая строка удалена, дыру закрывает ВСТАВКА; не нашли — строка под
нашим живым номером и есть наша, её ЗАМЕЩАЕМ. Иначе первый случай терял игрока
из показа, а второй дописывал снимку номер, которого в нём не было.
⚠️ Номер строки — это её ИНДЕКС в присланном отрезке, другого источника
нумерации нет вовсе; поэтому после вставки список перенумеровывается подряд, и
своя строка получает ровно `m.rank` без второго присваивания.
⚠️ Диверсия — ОДНА строка (`const j = слот >= 0 ? слот : m.rank - 1`), потому
что индекс найденного слота оставлен переменной НАМЕРЕННО. Прогон: исправная
даёт индекс 3 / место «4» / колонка убывает; диверсия — индекс 7 / место «8» /
колонка НЕ убывает, то есть ровно скриншот владельца.

## ТРЕТЬЯ ЭПОХА ТОГО ЖЕ ЧИСЛА: СЧЁТ ПАРТИИ (жалоба владельца 2026-08-12)

Дословно: «разные значения» — в шапке меню **9445**, в строке таблицы **9 367**.
⛔ Это НЕ третий заход на дефект «двух эпох» выше: там расходились МЕСТО и СЧЁТ
одной строки, здесь — два ЧЕСТНЫХ числа, просто снятых в разные моменты.

**РАЗНИЦА ОБЪЯСНЕНА ЗАМЕРОМ, А НЕ РАССУЖДЕНИЕМ, и числа владельца воспроизвелись
бит-в-бит: 78 = счёт текущей партии, ещё не забанкованный.** Шапка меню читает
`liveBalance()` (банк + партия), а на сервере лежит только ЗАБАНКОВАННОЕ — счёт
уезжает в `se` раз за уровень, на победе.

⛔ **ОБРАТНЫЙ ПУТЬ ЗАПРЕЩЁН ПРЕЖНИМ СЛОВОМ ВЛАДЕЛЬЦА (2026-07-27):** показывать
в меню `starBalance` уже пробовали, и жалоба звучала «во время игры одно число,
а на пузе второе». Значит расхождение сводится ВВЕРХ, а не вниз: `openMainScreen`
зовёт `bankLive()` — тот самый «банк по требованию», которым пользуется покупка.
Своего тракта не заводили: водяной знак `level.banked` защищает от двойного
банка, упавший потом счёт корректируется через `ss` в `bankLevelScore`, а без
заработка `bankLive` возвращает 0 и события не шлёт — переоткрытие меню сеть не
дёргает.
⚠️ **ПРОДУКТОВОЕ СЛЕДСТВИЕ, НАЗВАНО ВЛАДЕЛЬЦУ:** пауза посреди уровня теперь
отправляет счёт в таблицу. Это ровно его «Форбс»-модель (заработал — поднялся),
но раньше отправка случалась только на победе.
⚠️ **ТРЕТЬЕГО ПОТРЕБИТЕЛЯ НЕТ:** врезка на экране победы снята его же словом
(`renderWinLb` не зовётся), поэтому «мгновенный пересчёт» закрывают ровно два
места — пилюля меню и экран таблицы. Не искать врезку и не чинить её.
⚠️ Страж утверждает ПЕРЕХОД: сперва показывает, что расхождение РЕАЛЬНО (иначе
«равны» истинно и там, где заработка не было вовсе), потом что оно ушло, и
третьим признаком — что холостое переоткрытие сеть не дёргает. Мок отдаёт ЭХОМ
присланное число: захардкоженное в `/v1/me` проверяло бы фантазию, а не тракт.

## МОК, СЧИТАЮЩИЙ ПОПЫТКИ, — ЭТО ДОГАДКА О СРЕДЕ, А НЕ ПРОВЕРКА МЕХАНИКИ

Страж «мгновенный пересчёт» краснел ТОЛЬКО в полном прогоне, а отдельная проба
(включая ту, что копировала сетевую заглушку сьюта) проходила. Обе версии мока
были «правильные»: он менял место со ВТОРОЙ отправки, считая первой стартовую —
ту, что дёргает облачный синк. **В полном прогоне её нет: мост там живёт по
своему сценарию.** То есть страж проверял не механику, а мою догадку о том,
сколько запросов случится до интересного.
**Правило: мок реагирует на СОДЕРЖАНИЕ запроса (присланный счёт), а не на его
ПОРЯДКОВЫЙ НОМЕР.** Номер зависит от окружения — от моста, от облачного синка,
от того, успел ли сосед по странице что-то отправить; содержание не зависит ни
от чего. Тот же закон, по которому литерал в страже расходится с константой.
⚠️ И парное, уже известное, но сработавшее снова: первая версия ждала фиксированные
2.5 с. На свободной машине укладывалась, под сьютом — нет. Опрос факта, потолок
времени страховкой.

## ОДНА КОНСТАНТА ВЛАДЕЛЬЦА УРОНИЛА ПЯТЬ СТРАЖЕЙ В ЧЕТЫРЁХ СЕКЦИЯХ

Прогон после партии правок владельца дал 5 FAIL при 636 PASS, и ни один не был
поломкой игры. Разбор ценен составом:
- ТРИ стража держали КОПИЮ числа, которое владелец правил вживую (слот заряда
  64 при новых 83, зазор аватарок 6 при новых 4, подзаголовок таблицы). Такие
  переезжают за правилом — это правильная работа, а не помеха;
- ДВА уронила ОДНА константа `LEVEL_TYPES_MIN` (9 → 3, слово владельца «на
  первом уровне 3 вещи»), причём в РАЗНЫХ секциях и по-разному: страж «новой
  вещи» держал индексы `9/12/27` (копия формулы), а страж КОЛЕЦ УДАРА искал
  кирпич на первом уровне — где его теперь нет ПО ПРОГРЕССИИ, — и крутил
  `regen()` вхолостую двенадцать раз.
⚠️ **ТРЕТИЙ СЛУЧАЙ, ИЗ ДРУГОГО НАПРАВЛЕНИЯ (Повествование, 2026-08-11):** от той
же константы поехали ВЕХИ СЮЖЕТА, и `grep LEVEL_TYPES_MIN` не находит ни
`stFullSet`, ни `stTieredPacks` — они зависят от неё ЧЕРЕЗ СОСТАВ УРОВНЯ. Замер:
полный зал (К4) уехал позже РОВНО НА +6 у всех шести пачек (самая ранняя,
holiday, 36 → 42), а ранние вехи наоборот придвинулись — на открытый тип теперь
втрое больше копий. Сюжет растянулся с ОБЕИХ сторон, и это побочное следствие
рычага сложности, а не решение по сюжету. Таблица — в WORKSTREAMS, блок
ПОВЕСТВОВАНИЯ.

⚠️⚠️ **ГЛАВНОЕ: у рычага сложности есть НЕВИДИМЫЕ ИЖДИВЕНЦЫ.** Правя его,
искать надо не только «кто читает константу» (grep нашёл бы двоих), но и «кто
молча рассчитывает на состав первого уровня» — второй класс grep'ом не
находится. Лечение обоим одно: считать от ЖИВОГО числа (`levelTypesMin()`) и
подбирать уровень под тип, а не брать текущий.
⚠️ И шестой случай закона «поймал не свойство, а его подделку»: мой страж
полоски прогресса мерил ШИРИНУ в `#vGrid` и давал ноль во ВСЕХ трёх
состояниях — на той странице витрину никто не тикает, то есть измерялся неживой
DOM. Переведён на `__game.vitFrac(k)` и на СВОЙСТВА («доля строго между 0 и 1»,
«покупка не меняет», «совмещения растят»), а не на литералы 25/55: проценты
зависят от того, сколько тип накопил к моменту замера, а дефект — залипание.

## РАДИУС СОВПАДЕНИЯ: ШТРАФ ЗА ПРОМАХ + ЧЕТВЁРТЫЙ НЕРФ (спека владельца 2026-08-11)

Дословно: «сейчас слишком легко тыкать во все объекты и они будут совмещаться,
нужно чуть усложнить, процентов на 15%. вероятно, при ошибки на какое-то время
нужно сильно сбрасывать этот параметр до 0.3 условно, но через несколько секунд
возвращать до 0.4-0.5 и увеличивать при метчах максимум до 0.8».

**ЧИСЛА ПРОЧИТАНЫ БУКВАЛЬНО, И ЭТО РЕШЕНИЕ ОБОСНОВАНО ЗАМЕРОМ, А НЕ ВКУСОМ.**
У проекта записан урок «формула владельца приходит в ЕГО единицах, а в коде
живут СВОИ», поэтому первым делом искали шкалу, в которой он назвал числа.
Нашли: ползунок `#radiusRange` в панели разработчика — **min 0.3, max 2.2, шаг
0.05**, пишет прямо в `CFG.baseRadius`, рядом подпись «Gap:» с живым
`CFG.matchRadius`. Все четыре числа кратны шагу 0.05, а **0.3 — ровно левый
упор ползунка**. Второй шкалы (проценты, доли, множители) в игре нет вовсе.
⚠️ **«ПРОЦЕНТОВ НА 15» И ЕГО ЖЕ ЧИСЛА РАСХОДЯТСЯ ВТРОЕ, ЭТО НАЗВАНО ВЛАДЕЛЬЦУ.**
Замер доступных пар на старте уровня (3 раскладки на уровень): ур.1 232 → 116,
ур.5 160 → 82, ур.20 116 → 56, то есть **−50%**, а не −15%. Взяты ЧИСЛА (они
конкретны и в правильных единицах), «чуть усложнить» прочитано как намерение.

**ДЕЙСТВУЮЩИЕ ЗНАЧЕНИЯ** (00-config): `BASE_RADIUS_DEFAULT` 0.45 (было 0.9),
`MATCH_R_MIN` 0.375 (было 0.75), `COMBO_RADIUS` **0.8** (было 1.1),
`MATCH_R_MISS` 0.30, `MATCH_R_MISS_MS` 3000.
- ⚠️ **ПОЛ ЕДЕТ ЗА БАЗОЙ, СОХРАНЯЯ ДОЛЮ (5/6), А НЕ ЧИСЛО.** Оставь 0.75 — он
  оказался бы ВЫШЕ базы, формула выродилась бы в `r = base`, и динамическое
  сжатие «куча оседает — радиус сжимается» исчезло бы молча. Владелец пола не
  называл, значит сохраняем форму механики, а не число, потерявшее смысл.
- ⚠️ Потолок 0.8 — ЧЕТВЁРТЫЙ нерф одного числа (3.5 по центрам → 2.0 → 1.5 →
  1.1 → 0.8), и все четыре по одной причине «слишком легко». Инвариант «потолок
  ОДИН ВЕЗДЕ, включая Power chain» цел.
- ⛔ **ЭНДШПИЛЬНЫЙ ∞ (≤8 живых) ШТРАФОМ НЕ ОГРАНИЧЕН** — он стоит первым в
  `updateMatchRadius` и выходит раньше всех веток. Это анти-фрустрация, а не
  буст; трогать нельзя.

**МЕХАНИКА ШТРАФА** (60-access): `missRadiusAt` — момент промаха; потолок
`MATCH_R_MISS + (база − MATCH_R_MISS)·t` линейно за `MATCH_R_MISS_MS`.
- ⚠️ Это **ПОТОЛОК (`Math.min`), А НЕ ПРИСВАИВАНИЕ И НЕ ПОЛ.** Пол сломал бы
  канонический рецепт форсирования тупика `cfg.baseRadius = -9` СРАЗУ В ПЯТИ
  секциях сьюта (найдено разведкой ДО правки, а не прогоном).
- ⚠️ Ветка цепи больше не выходит досрочно: иначе «промах сильно сбрасывает» не
  выполнялось бы ровно там, где игрок разогнался.
- ⚠️ Возврат идёт по РЕАЛЬНЫМ часам и тикает из `loop` — `refreshAccessibility`
  в штиле не работает вовсе, и без тика радиус залипал бы на 0.3 навсегда у
  игрока, который промахнулся и замер. Тот же закон, что у «комбо-буст обязан
  погаснуть и на СПЯЩЕЙ куче». Якорь сдвигается паузой, сбрасывается в genLevel.
- ⚠️ Точек промаха ДВЕ (`penalize` в 70-fx и `penalizeRock` в 80-gameplay), обе
  получили штраф — иначе камни были бы дешевле обычного промаха.
- ⚠️ Штраф ложится ПОВЕРХ уже существовавшего наказания (срез комбо-лесенки на
  `COMBO_MISS_DROP`) — за один промах теперь два разных следствия. Так и
  задумано владельцем, но знать об этом надо.

⚠️⚠️ **ГЛАВНАЯ ЛОВУШКА, НАЙДЕННАЯ РАЗВЕДКОЙ ДО ПЕРВОЙ СТРОКИ КОДА: ПРОВАЛ
РАДИУСА УМЕЕТ ВЫДАВАТЬ СЕБЯ ЗА ТУПИК.** `availablePairs` кормит детектор тупика
и бесплатную авто-встряску, обоим хватает ДВУХ стабильных тиков (~1.2 с), а
штраф живёт 3 с. Без гейта игра сама объявляла бы тупик и начинала молоть кучу
ЗА ОЧКИ — наказание за промах превратилось бы в списание очков помолом.
Лечение: `noMoves` не считается, пока штраф активен.

**ЦЕНА ЗАМЕРЕНА A/B, ЛИНЕЙКА: headless Chromium без CPU-троттлинга, вьюпорт
900×640, уровень 5, бот матчит ПАРАМИ, встряска только когда пар не осталось,
4 раскладки на руку, руки чередуются внутри одного прогона.**

| рука | старые числа | новые числа |
|---|---|---|
| ровный бот (не промахивается) | 1.25 встряски | 1.5 |
| неуклюжий (промах каждый 8-й) | 1.5 | **2.75** |

Застрявших 0 из 8 прогонов, бюджет владельца — 5 встрясок. ⚠️ И честно: бот НЕ
ПРОМАХИВАЕТСЯ, поэтому рука «ровный» меряет только базу/пол/потолок — это НИЖНЯЯ
оценка подорожания; вся цена штрафа видна во второй руке (+83%).

⚠️ **ПЕРВАЯ ВЕРСИЯ СТРАЖА «ШТРАФ ≠ ТУПИК» БЫЛА ТАВТОЛОГИЧНОЙ, И ПОЙМАЛ ЭТО
ТОЛЬКО ДВУСТОРОННИЙ ПРОГОН.** Она промахивалась на живой куче и ждала
`deadlock === false` — а он там false ПРИ ЛЮБОМ поведении: пар на полном уровне
хватает даже при радиусе 0.3. Диверсия «снять гейт» осталась зелёной. Верная
постановка — детерминированно обнулить пары каноническим `baseRadius = -9` и
сравнить ДВА состояния: со штрафом тупик молчит, без штрафа объявляется.

## ГРАНИЦА ГЕЙТА НУЛЯ: `mixer_lb_sent` ЖИВЁТ ВНЕ СЕЙВА (замечание Интеграции 2026-08-11)

Признак «строка на сервере есть» (`lbSentScore`) хранится в `localStorage`, то
есть НЕ синхронизируется между устройствами. Следствие: на ВТОРОМ устройстве
память об отправке пуста, хотя строка на сервере есть, — и если первым же
действием там окажется спуск ровно в ноль, он не уедет.
⛔ **Лечить НЕ НАДО, и это осознанно:** слать ноль «на всякий случай» значит
вернуть создание строки не игравшим гостем — ровно тот дефект, ради которого
правило и заводилось. Случай узкий (обычно раньше случается любое положительное
начисление, и оно чинит признак). Записано как граница, а не как задача.
⚠️ И хорошее следствие оттуда же: правка САМОИЗЛЕЧИВАЮЩАЯСЯ — если игрок сбросил
прогресс и закрыл вкладку раньше отложенной отправки, на следующем запуске
облачный синк дёрнет `onStarsChange` при `s=0` и `lbSentScore>0`, и ноль уедет
сам. По построению; прогоном не проверялось.

## ПРОСАДКА КАДРА ПРИ НАСЫПАНИИ: ЗАМЕР 2026-08-11 (жалоба владельца про iPhone 17)

**ЭТО ПОВТОРНАЯ ЖАЛОБА** — 2026-08-01 по ровно такой же («подтупливает, особенно
когда объекты сыплются») уже была правка A3, потолок подшагов 2. Она в силе и
работает; исчерпана — на насыпании p95 подшагов ВСЁ РАВНО упирается в потолок.

**ЛИНЕЙКА:** headless Chromium на настоящем GPU (`--use-angle=metal`), CPU ×4
(наш прокси мобильного ядра), вьюпорт 390×780, ур.20 = 183 предмета, окно —
РОВНО интро (perfReset на первом кадре падения, снимок в `finishIntro`).

| | насыпание | покой |
|---|---|---|
| солвер p95 | **22-25 мс** | 10 мс |
| кадр p95 | 27-29 | 23 |
| кадр max | **35-44** | 27-31 |
| подшагов p95 | 2 (упор в потолок) | 1 |

⚠️ **НА УР.1 ПРОСАДКИ НЕТ ВОВСЕ** (82 предмета: солвер 8.6 против 6 в покое,
кадр 26 в обоих). То есть жалоба — про НАПОЛНЕННЫЕ уровни, и мерить надо там.
⚠️ Кадр держит СОЛВЕР: рендер 2-3 мс, ui 1, эффекты 0 — подтверждение прежнего
вывода v219, а не новое наблюдение.

**ЧТО ПРОВЕРЕНО И НЕ РАБОТАЕТ:** итерации солвера 8 → 6 → 4 → 2 дают 22.2 /
22.3 / 20.2 / 22.8 — шум. Ручку `iters` можно вычеркнуть из кандидатов.

**ЕДИНСТВЕННЫЙ НАЙДЕННЫЙ РЫЧАГ — CCD-ПОДШАГИ** (`world.maxCcdSubsteps`, боевое 4):

| | солвер p95 | кадр p95 | кадр max |
|---|---|---|---|
| боевое (4) | 23.2 | 28.3 | **44** |
| 2 | 22.0 | 27.3 | 34.6 |
| 1 | 18.4 | 26.3 | **29.1** |
| CCD выкл | 15.8 | 24.7 | 27.4 |

⛔ **ЦЕНА ИЗМЕРЕНА СОАКОМ, А НЕ ТРЕМЯ ПРОГОНАМИ** (10 мин, сид 101, Hard, пара
рук): спасений 30 (3.0/мин, 10 побед) против **54 (5.4/мин, 13 побед)** — то
есть ~+38% телепортов спасателя НА УРОВЕНЬ; выступ за стену max −0.003 против
**0.388** при пороге тревоги 0.45. Провалов под пол и подъёмов пола 0 в обеих,
проблем и ошибок 0 в обеих. **Вывод: CCD-подшаги действительно держат
анти-туннельный комплекс, и 1 его ощутимо ослабляет.** Решение — за владельцем
(правило у `physKnobs`: «итерации и подшаги — это ПОВЕДЕНИЕ, а не качество
картинки»), поэтому в бой НЕ уходит.

✅ **РЕШЕНИЕ ВЛАДЕЛЬЦА ПРИНЯТО В ТОТ ЖЕ ДЕНЬ: «ЗАМЕДЛИТЬ САМО НАСЫПАНИЕ»** —
`INTRO_TIME_SCALE` 1.7 → **1.3** (его коридор 1.2-1.3). Замер трёх рук по
4 раскладки: солвер p95 18.9 → 12.8 (1.3) → 13.3 (1.2), худший кадр 31.9 → 28.5
→ 28.6. Руки 1.3 и 1.2 неотличимы, взята 1.3 как меньшее отступление от прежней
спеки. ⚠️ ЗАПОЛНЕНИЕ СВЕРЕНО ОТДЕЛЬНО (фаза падения ограничена РЕАЛЬНЫМ
временем, значит медленная физика застаёт осадку в другой стадии, а трим режет
по штилю): живых 183 во всех раскладках всех рук, верх кучи 7.5-8.4 при норме
7.5-9.0, спасений и выступа за стену не прибавилось.
⛔ CCD-подшаги в бой НЕ ушли — владелец выбрал другой вариант; замер и цена
сохранены выше на случай, если понадобится ещё запас.

⚠️⚠️ **ГЛАВНОЕ ЗНАНИЕ: ОСТАВШИЕСЯ РЫЧАГИ — НЕ ТЕХНИЧЕСКИЕ, А ПРОДУКТОВЫЕ.**
Стоимость насыпания задана самим насыпанием: `INTRO_TIME_SCALE = 1.7` (спека
владельца «+30%», дважды) множит dt, отчего каждый кадр интро честно гонит ДВА
подшага 183 тел. Дешевле сделать можно тремя способами, и все три меняют
ОЩУЩЕНИЕ, а не картинку: замедлить засыпание (снизить 1.7), лить меньше
предметов, ослабить CCD. Технических запасов в кадре насыпания больше нет —
рендер, эффекты и ui вместе дают 4 мс из 28.

**ПРИБОР ДЛЯ ЖИВОГО УСТРОЙСТВА (сделан этим же заходом):** `perfReport()` теперь
несёт ЗАМОРОЖЕННЫЙ срез `насыпание` — иначе этот момент не виден вовсе:
`frameRing` скользящий на 600 кадров (~10 с), а игрок доберётся до панели через
паузу и меню, и кадры насыпания к тому времени вытеснены. Срез снимается ТЕМ ЖЕ
`perfStats()` (копию метрики рядом с работающей не заводим), стоит в отчёте
ПЕРВЫМ полем и под стражем сьюта («срез есть, пережил игру, отличается от
общего»).

## ⚠️⚠️ СОАК МОЛЧА СТОЯЛ НА ЭКРАНЕ НОВОЙ ВЕЩИ (найдено 2026-08-11)

Экран новой вещи встал в цепочку победы 2026-08-10 — и ОСТАНОВИЛ соак: он
полноэкранный и ждёт кнопки, а бот про него не знал. **Симптом обманчив:** в
журнале идут ровные строки `alive=0 rescues=0 problems=0`, прогон выглядит
ЗДОРОВЫМ и просто ничего не проверяет (замер: соак стоял на экране победы вторую
минуту при `wins=1`). Тот же класс, что «блокер в начале прогона — тихое
выключение хвоста», только тише: сьют хотя бы падает, а соак досиживает срок и
печатает благополучную сводку.
**Правило: фича, вставшая в цепочку победы, обязана быть проучена ОБОИМ ботам —
сьюту (`passNewObj`) и соаку.** Проверять по журналу: `alive` обязан меняться,
`wins` расти вместе с `lvl`.

## ПАКЕТ ВЛАДЕЛЬЦА 2026-08-11-в: экран победы, тряска, мягкий эндшпиль

**ЭКРАН ПОБЕДЫ, ТРИ ЕГО СЛОВА.** (1) Плашка «×N» — зелёная ТЕМ ЖЕ цветом, что
полоска прогресса, текст чёрный; (2) блок кнопок ПОД списком TOP ITEMS;
(3) заголовок TOP ITEMS снят.
⚠️ Порядок сменён В РАЗМЕТКЕ, а не `order` в стилях: на мобильном он уже
задавался через `order:2/3`, и держать одно правило в двух местах — та самая
копия, что расходится при первой правке. Мобильные `order` сняты, отступ у
`.win-actions` переехал снизу наверх (блок стал последним).
⚠️ Страж утверждает порядок ГЕОМЕТРИЕЙ (низ списка выше верха кнопок), а цвет
плашки — РАВЕНСТВОМ с цветом полоски, а не литералом: владелец сказал «как цвет
прогресса», то есть требование — совпадение двух мест.

**ТРЯСКА ЧАШИ НА СОВМЕЩЕНИИ СНЯТА** (`MATCH_CAM_SHAKE = false`, «убери пока» —
флаг, а не удаление). Гасятся РОВНО два места в `doMatch`; встряска, бомба,
помол, финал и разлёт чаши — другие события, их владелец не называл.
⚠️ Страж мерит САМУ СИЛУ (`cam().shake`), и поле заведено ради него: первая
проба смотрела на дрожание ЦЕЛИ камеры, а трясётся ПОЗИЦИЯ — она была бы зелена
при любой силе. Контроль в том же ассерте: встряска обязана дать > 0.2.

**МЯГКАЯ СТУПЕНЬ ЭНДШПИЛЯ** (слово владельца: «в конце уровня увеличивай радиус…
если их меньше 10 и встрясок мало или не осталось. Иначе слишком сложно»):
`ENDGAME_SOFT_ALIVE = 10`, `ENDGAME_SOFT_SHAKES = 1`.
⛔ Жёсткая ступень «<=8 → ∞» НЕ тронута: она безусловна и несущая.
⚠️ Встряски считаются СВОИ (бесплатные + купленные). Рекламные НЕ в счёт: они
безлимитны, и условие «мало» с ними не наступило бы НИКОГДА — правило было бы
мёртвым при живом виде.
⚠️ Страж переключает ОДНУ переменную на неизменной сцене (10 живых): при 3
встрясках радиус 0.8, при 1 и 0 — ∞, при возврате 3 снова 0.8. Обратимость —
тоже утверждение.

✅ **ПРОВЕРЕНО ПО ПРОСЬБЕ ВЛАДЕЛЬЦА: ТУРБО ГАСНЕТ ПОСЛЕ ОДНОГО ПРОМАХА.** Замер
на боевом пути (настоящий клик в пустоту): до промаха `chain:true`, после
одного — `chain:false, count:0`. Константы `CHAIN_MISSES_EASY/HARD` обе равны 1.

⚠️⚠️ **НАБЛЮДЕНИЕ, КОТОРОЕ ВАЖНЕЕ САМОЙ ПРАВКИ: НА НОВОМ РАДИУСЕ БОТ ЗАСТРЕВАЕТ
ОКОЛО 28 ПРЕДМЕТОВ**, а не около десяти. Пробник, разбиравший кучу матчами,
упирался в «доступных пар нет» задолго до окна мягкой ступени и продолжал только
через встряски. То есть порог «<10» лечит самый хвост, а трудное место наступает
раньше. Владельцу сказано; поднимать порог — его слово.

## ПЛАШКА ТАБЛИЦЫ В МЕНЮ — ПЕРЕДЕЛАНА ПО МАКЕТАМ 840:3910 / 840:4679 (2026-08-11)

Читано Dev Mode'ом (файл был открыт в другой вкладке — читать удалось только
через MCP с явным `fileKey`, а не через активную вкладку; на будущее: у второго
сервера Figma есть параметр `fileKey`, он не зависит от того, что открыто).

**ЧТО СМЕНИЛОСЬ ПО СУЩЕСТВУ, А НЕ КОСМЕТИЧЕСКИ:**
- слева встал **ЗНАЧОК НАПРАВЛЕНИЯ 48×48** (ассеты владельца `Interface/
  Arrow-up.svg` #9DE530 / `Arrow-down.svg` #FF7869), инлайном — сборка обязана
  открываться офлайн одним файлом;
- место переехало в ПЕРВУЮ строку (Heavy 18 моб. / 22 деск., чёрным), подпись
  «on leaderboard» — Bold 14 `#a2a2a8`;
- слово «Leaderboard» и точка с местом (`msLbeDot`) УДАЛЕНЫ;
- аватары 40, зазор 2, кнопки «Open» на мобильном НЕТ (ряд нажимается целиком).

⚠️⚠️ **ЭТИМ МАКЕТОМ ОТМЕНЕНЫ ЖИВЫЕ ПРАВКИ ВЛАДЕЛЬЦА «аватарки 32» и «зазор 4»**
(2026-08-10/11). Они относились к ПРЕЖНЕЙ плашке, где слева стояло слово
«Leaderboard» 22px и справа жила стрелка: там три аватара по 40 физически не
помещались (замер: не хватало 7px на 320). Новая раскладка короче слева, и 40/2
сходятся с запасом. Владельцу сказано; вернуть 32/4 — одна строка.

⚠️ **НАПРАВЛЕНИЕ ЗНАЧКА — ПО СРАВНЕНИЮ С ПРОШЛЫМ ВИДЕННЫМ МЕСТОМ** (`localStorage
mixer_lb_seen_rank`), не по знаку счёта: владелец дал ДВА значка, значит оба
состояния обязаны случаться, а растёт и падает тут только само место.
⚠️ **ПЕРВОЕ ОТКРЫТИЕ — БЕЗ ЗНАЧКА** (сравнивать не с чем), **новичок без места —
слово «Leaderboard» одной строкой, без подписи и без значка**: этого случая в
макете нет, решение диспетчера, названо владельцу.

## ⚠️⚠️ МЯГКАЯ СТУПЕНЬ ЭНДШПИЛЯ ЗАДЕВАЕТ РОВНО ОДНО ЗНАЧЕНИЕ СЧЁТЧИКА
### ⛔ НАДГРОБИЕ 2026-08-13: порог поднят до 16 ПО СЛОВУ ВЛАДЕЛЬЦА («повышаем»,
### число делегировано диспетчеру и взято замером — раздел ниже). Окно теперь
### 9..15, довод «одно значение» ИСТОРИЧЕСКИЙ — он и был причиной «повышаем».

Владелец: «увеличивай радиус… если их меньше 10». Жёсткая ступень уже даёт ∞
при `<= 8`, значит новая добавляет РОВНО состояние `9` — и матч снимает ПАРУ,
то есть счётчик идёт через два и это одно значение проскакивается через раз.
**Правило почти ненаблюдаемо в бою, и это сказано владельцу числом.** Поднимать
порог — его слово, сам не двигаю.

## ПОРОГ МЯГКОГО ЭНДШПИЛЯ = 16: ЧИСЛО ИЗ ЗАМЕРА (2026-08-13)

Слово владельца «повышаем» + «радиус проверь сам» — число взято тремя руками
замера (headless, боевой радиус 0.45, Easy, без CPU-троттлинга):

1. **Переписи застреваний бота (2 руки × 9 прогонов, ур.5/12/20 × 3 раскладки):
   объективных тупиков в хвосте НЕТ.** И со встрясками, и вовсе без них каждый
   прогон застревал РОВНО ОДИН раз и рано (80-132 живых, там встрясок полно),
   дальше катился до победы: каждое снятие вскрывает закопанное, а встряска к
   концу тянет пары друг к другу. ⛔ Прежнее «бот застревает около 28» на этих
   руках НЕ воспроизвелось. Значит «слишком сложно» в конце — про ДЕФИЦИТ
   вариантов (искать почти нечего), а не про их отсутствие.
2. **Кривая дефицита на СРЕЗАХ (`cull` до N живых, ур.5/20 × 2 раскладки,
   осадка 2 с):** доступных пар при 30 живых — 5-11, при 25 — 3-7, при 20 —
   1-4, **при 15 — ровно ~1, при 12 появляются нули, при 10 даже встряска не
   помогает (после неё 0-1)**. Зона «вариантов почти нет» начинается с ~15 —
   порог 16 её накрывает (активация с 15 и ниже).
3. **Цена:** с гейтом «своих встрясок ≤ 1» это остаётся спасением, а не
   скидкой — в переписи бот ни разу не опустился ниже 5 встрясок, окно ≤1
   не наступило вовсе; под ∞ уходит максимум последние ~7 матчей и только у
   игрока, спалившего встряски.

⚠️ ГРАНИЦЫ ЗАМЕРА, честно: бот не промахивается и знает все пары (нижняя
оценка сложности); всё снято на Easy (дефолт) — на Hard дефицит только хуже,
порог тем более оправдан.
⚠️⚠️ **АРТЕФАКТ ЗОНДА, СТОИВШИЙ РУКИ:** кривая «по ходу доигрывания» с
расклейкой радиусом (baseRadius 2.4 на один матч) ловила обвал — под комбо
расклейка снимала ГРУППЫ, куча улетала в ноль за десяток шагов, хвост кривой
не снялся вовсе. Для узких окон сцену СТАВИТЬ (`cull`), а не доигрываться —
правило из канона, подтверждено четвёртый раз.
⚠️ И грабля `cull(n)`: он СНИМАЕТ n предметов, а не «оставляет n» — первая
версия среза мерила не хвост, а середину кучи (живых 81-110 при «цели 10»).
Ставить сцену как `cull(alive - цель)`.
⚠️ Страж границы — ДВУСТОРОННИЙ («потолок — не страж границы»): 17 счётных
без встрясок → радиус обычный, 15 при ≤1 → ∞, при 3 → обычный; глубина окна
(9) осталась под прежним ассертом. Откат порога к 10 роняет ровно `с15при1`.
⚠️ Отсюда же урок про ЗАМЕР: три попытки «доиграть ботом до окна» приезжали к
НУЛЮ живых — на новом (меньшем) радиусе бот застревает, зовёт встряски, а финал
за это время доедает кучу. Сцену для такого узкого окна надо СТАВИТЬ ПРЯМО
(тест-рычаг `__game.cull(n)`), а не пытаться попасть в неё игрой.
⚠️ И счётчик у стража ОБЩИЙ с боевым (`aliveCountForRadius`, наружу через
`missRadius().счётных`): свой подсчёт живых у стража означал бы, что он
проверяет своё представление о правиле (клад и камни в счёт не идут).

## МУЗЫКА «С ЗАДЕРЖКОЙ»: ДЕЛО НЕ В СЕТИ (разбор 2026-08-11)

Жалоба владельца: «музыка начинает играть с задержкой». Первое подозрение —
внешний `music.mp3` 4.4 МБ с `preload="none"`. **ЗАМЕР ЕГО НЕ ПОДТВЕРДИЛ.**
Линейка: свой http-сервер, троттлинг сети через CDP ПОСЛЕ загрузки страницы
(иначе замер съедает сама сборка в 10 МБ), вьюпорт 390×780, событие `playing`
(не `play()` — тот резолвится раньше звука).

| сеть | от жеста до звука |
|---|---|
| 8 Мбит | 191 мс |
| 4 Мбит | 253 мс |
| 1.5 Мбит | 467 мс |

**НАСТОЯЩАЯ ПРИЧИНА — ЖДАЛИ КАСАНИЯ.** Три сценария на живой сборке:
- тап во время интро — музыка ЗАВОДИТСЯ (интро его не съедает, проверено);
- **КЛАВИША не заводила ВООБЩЕ** — на десктопе игрок, жмущий пробел (встряска),
  оставался без музыки навсегда;
- не трогает экран — тишина, сколько угодно.
То есть между запуском и первым касанием игра молчит, и это и читается как
«задержка». Лечение: разблокировка по ЛЮБОМУ жесту
(`pointerdown/touchstart/mousedown/keydown/click`, `capture:true` — чтобы чужой
`stopPropagation` не проглотил), плюс ОДНА попытка `play()` сразу на старте:
политика автоплея её обычно отклоняет, но там, где жест уже был (портал,
десктоп), музыка начинается сразу и ждать нечего.

⛔⛔ **ПРОГРЕВ БУФЕРА (`load()` заранее) ПРОБОВАЛИ И СНЯЛИ — НЕ ИЗОБРЕТАТЬ.**
Замер: 466 мс против 469 на 1.5 Мбит, то есть НИЧЕГО. И он стоил дефекта:
`load()` на ИГРАЮЩЕМ элементе ОБРЫВАЕТ звук — тапнувший во время интро терял
музыку через секунду (поймано собственной пробой, не прогоном). Цена без
выигрыша: 4.4 МБ трафика тому, кто, может, и не тронет экран.
⚠️ Урок общий: правка по ЗАМЕРУ, а не по правдоподобной причине. Гипотеза «файл
большой, поэтому долго» звучала убедительно и была неверна.

## МУЗЫКА: 4.4 МБ → 2.0 МБ, ПАКЕТ ПОРТАЛА 6.62 → 4.46 МБ (2026-08-11)

Владелец: «4.4 мб это файл музыки? если да, то попробуй оптимизировать».
Файл был 2:11 стерео 48 кГц на **267 кбит/с** — втрое больше, чем нужно фоновой
петле. Перекодирован в **128 кбит/с** тем же кодеком и с тем же расширением.
⛔ **РАСШИРЕНИЕ НЕ МЕНЯЛОСЬ** (`.mp3` → `.mp3`) — прямое правило владельца
«никогда не переводить его ассеты в другой формат». Ни ogg, ни opus, ни m4a не
предлагались, хотя они дали бы больше: сперва его слово.

**ПОЧЕМУ 128 БЕЗОПАСНЫ — ЗАМЕР, А НЕ ВКУС.** Спектр оригинала: выше 15 кГц
**−63.8 дБ**, выше 16 кГц −65.9, выше 18 кГц −72.8 при среднем уровне программы
−17.3 дБ. То есть верха в треке практически нет, и то, что режет кодек на 128,
там уже пусто. Остаток после вычитания из оригинала почти не зависит от
битрейта: −41.1 дБ на 160к, −40.4 на 128, −39.9 на 96, −39.4 на 80 — разницу
держит само перекодирование, а не битрейт.

| битрейт | файл | выше 15 кГц |
|---|---|---|
| 267 (было) | 4.39 МБ | −63.8 дБ |
| 160 | 2.50 | −65.2 |
| **128 (стоит)** | **2.00** | −66.8 |
| 96 | 1.50 | −71.2 |
| 80 | 1.25 | −75.8 |

**ЦЕНА ПАКЕТА — ГЛАВНЫЙ ВЫИГРЫШ.** ZIP портала (index + 2 bridge + music):
**6.62 → 4.46 МБ** при ориентире 8 МБ, то есть запас 1.38 → **3.54 МБ**. Это
прямо снимает давнюю оговорку канона «до гибрида каждая новая партия моделей
тратит запас».
⚠️ Мерить ЗИПОМ, а не суммой файлов — правило прежнее.

⚠️ **ПЕТЛЯ НЕ ПОЕХАЛА:** длительность бит-в-бит та же (131.24 с), `loop` цел,
от жеста до звука 9 мс, ошибок страницы нет. Проверено на живой сборке через
свой http-стенд.
⚠️ Оригинал 267к лежит в истории git (`git show 6cc36e9:music.mp3`) — возврат
одной командой. Владельцу отправлены три отрывка 40-60 с (267 / 128 / 96) на
слух: скажет «не слышу разницы» — ставим 96 и пакет теряет ещё полмегабайта.

## УБОРКА 2026-08-12 (приказ владельца «удали старое и неиспользуемое»)

Перепись ВСЕХ top-level символов src/app (1559 объявлений) по числу
употреблений в src, довырезание до фикс-пойнта. **ВЫРЕЗАНО** (возврат — git):
кластер врезки победы (WIN_LB_MS, winLbStop/Source/Adapt/Render, renderWinLb,
хуки winLbStub/winLbInfo, CSS .win-lb/.wl-*), кластер пилюли ступени
(#tierToast: JS, разметка, CSS, keyframes — его комментарий сам просил снять
«вместе с разметкой»), tintChrome+chromeMeta+menuChrome+skyChromeCSS (4-я
редакция кромок оставила их без читателей), LB_LOCAL (комментарий «на ней
стоит страж» был неправдой — страж стоит на lbHostIsLocal), CHAIN_RING_ENABLED
(флаг-сирота: фича давно удалена, «код жив» в комментарии врал), bakeShardTint
(тинт осколков до GPU-разлёта). Сборка −28 КБ.
⛔ **СЕМЬ ЖИВЫХ МЕРТВЕЦОВ ОСТАЮТСЯ ПО РЕШЕНИЯМ ВЛАДЕЛЬЦА** — не вырезать:
MAGNET_ENABLED/SCOPE_ENABLED/PRICE_SCOPE/scopeHighlight/detectorHighlight
(«вернём с внятной подачей»), buyCoinShake (монеты: «не удаляй, скрой»),
showLose (UI поражения жив, недостижим геймплеем).
⚠️ **ПРОВЕРКА СПАСЛА ДВАЖДЫ:** BOWL_SHARD_TINT_LO/HI выглядели сиротами
bakeShardTint, а их читает ЖИВОЙ GPU-путь (20-arena:249) — правило «перепись
сверяет КОД, а не счётчик вхождений» сработало в обратную сторону; и срез
«регуляркой с .*?» завис катастрофическим бэктрекингом — длинные вырезы резать
балансом скобок. Плюс список занавеса в test.js держал tierToast поимённо —
узлы, вырезанные из разметки, искать и в СПИСКАХ тестов.

## ПЕРФ-ЗАХОД 2026-08-12 (приказ «самостоятельно, на основе всех данных»)

**ПРОФИЛЬ УСТОЯВШЕЙСЯ ИГРЫ** (GPU, CPU ×4, ур.20 = 183, комбо каждые 250 мс):
кадр p95 30.7, из них СОЛВЕР 22.6 — **75% кадра**; рендер 1.6-2.1, fx 0.8,
ui 1-1.7 — копейки. Хвост бодрствования после матча 1-3.3 с (норма, не рычаг).
⛔ **A/B ручек (iters 4, ccdSub 2) НЕ СЕРТИФИЦИРОВАН — контроль опроверг сам
стенд:** база-1 солвер 18.7 против базы-2 9.3 при ТЕХ ЖЕ ручках; дрейф стенда
больше эффекта. По канону такие числа ничего не доказывают; ccdSub 2 остаётся
единственным замеренным запасом (кадр max 44→34.6 на насыпании) и ждёт
СОАКА + слова владельца — это поведение.
✅ **ВВЕДЁН КАП КАДРОВ 60 FPS** (`CFG.fpsCap`, порог пропуска 840/кап = 14 мс):
iPhone Pro гонит rAF на 120 Гц — игра рисовала и синкала ВДВОЕ чаще, чем
задумана, грея телефон до троттлинга. Кап чисто презентационный: пропуск стоит
ДО любой работы, lastT не двигается, dt копится — фикс-шаг симуляции бит-в-бит
тот же. На 60 Гц не меняет ничего (16.7 > 14, ни один кадр не скипается).
⚠️⚠️ **СТРАЖ КАПА: ПОРОГ ПРОВЕРКИ ОБЯЗАН ЛЕЖАТЬ ВЫШЕ ЛЮБОЙ НАГРУЗКИ СТЕНДА.**
Первая версия проверяла капом 30 (порог 28 мс) и ПОКРАСНЕЛА под полным сьютом:
базовые кадры нагруженного стенда уже 34.7 — порог не связывал ничего, страж
мерил нагрузку (изолированная проба при этом проходила: 19.5 → 44.8). Проверка
капом 12 (порог 70): под сьютом 52.6 → 93.9 → 30.9, связывает при любой
реальной нагрузке. Хедлес не отпускает vsync — 120 Гц на стенде не создать,
потому порог и выводится из ручки (840/cap), а не литерал.

## ЗАМОРОЖЕННАЯ ГЛЫБА (спека владельца 2026-08-13, ответы на 13 вопросов)

Один предмет ПАРЫ вморожен в полупрозрачную низкополигональную глыбу (два слоя
поверх меша — приём накладки огня, материал предмета НЕ трогается). Не
матчится (ключ подменён — приём камней, все парные механики исключают её
автоматически). Условие: собрать **FROZEN_PAIRS_N=3 пары ТОГО ЖЕ типа**
(зачёт ШТУКАМИ, 2 штуки = пара — нечётные группы не теряют половинку); после —
глыба **ПУЛЬСИРУЕТ и ждёт ТАПА**; тап разбивает (ледяные осколки shardFX),
предмет становится ОБЫЧНЫМ и **«всё равно нужна пара»** — потому морозим
одного ИЗ ПАРЫ, чётность цела по построению. Очки разбития: «чистые очки
предмета ×3» = MATCH_SCORE×3×множитель типа×бустер. **Трещины** (слово
владельца): вершинный шум глыбы углубляется по ступеням зачёта, узор
детерминирован — углубляется, а не мигает.
Появление: с **11-го** уровня (клад с 10-го — «разведи»; с кладом в одной куче
не живёт, очередь сдвигается), рандомно через 1-3 уровня, 1-2 глыбы на уровень
РАЗНЫХ типов, очередь В ПАМЯТИ СЕССИИ (как у бомбы). Тап до срока — **штраф
как у камня** (двойной, 2×10=20) + тост «сколько пар осталось». **Бомба
разбивает лёд** (досрочно, предмет жив, БЕЗ очков ×3 — умолчание названо).
⚠️⚠️ УТОЧНЕНО ВЛАДЕЛЬЦЕМ 2026-08-13 (выбор «2» по замеру диспетчера): бомба
ломает лёд только ВПЛОТНУЮ — `FROZEN_BOMB_RADIUS = 2.86` (историческая зона
до удвоения), НЕ полной зоной 5.72. Замер-первопричина: полная зона при чаше
~8 в поперечнике доставала глыбу из ЛЮБОЙ точки кучи (10 глыб из 10 на 7
взрывах) — условие «собери N пар» обесценивалось одним тапом по бомбе.
Константа НАМЕРЕННО своя, не BOMB_RADIUS/2: зону бомбы владелец менял и
может менять снова. Страж двусторонний с контролем соседнего свойства:
дальняя глыба (зазор ~4.5) цела ПРИ живых жертвах взрыва, вплотную ломается.
Пары от **заряда типа идут в зачёт** (заряд бьёт по имени типа — глыба
исключена из его жертв отдельно). Финал доедает глыбу без награды; помол,
трим, докидка, счётные эндшпиля, вуаль, витрина — исключают, как камни.

⚠️⚠️ **АРИФМЕТИКА, ПРОДИКТОВАВШАЯ ДИЗАЙН** (замер, а не вкус): пар ОДНОГО типа
на уровне = 90/типов: ур.10 → 7, ур.20 → 4, ур.28 → медиана 3 при минимуме 1,
ур.60 → 1. Жёсткое N=3 с ~28-го уровня часто невыполнимо, поэтому **тип для
глыбы ВЫБИРАЕТСЯ из тех, у кого копий ≥ 2N+2** (N свободных пар + партнёр);
нет годных — глыбы нет, очередь на следующий уровень. Менять N — сперва эта
таблица.

⚠️ ГРАБЛИ СБОРКИ ФИЧИ (все пойманы пробами, не чтением):
- **КЛЮЧ ≠ ИМЯ ТИПА** (ключи вида «T5», зачёт идёт по type.name) — тип хранится
  ОТДЕЛЬНО (frozenType), ключ (frozenKey) только для возврата в парность;
- тест-хуки `matchType` и `cull` фильтруют по имени типа МИМО ключа — обе
  двери закрыты `!frozen`, иначе стенд сматчивал САМУ глыбу;
- `visiblePixel` отдаёт поля **px/py**, а не x/y; клики стражей — ТОЛЬКО
  настоящей мышью Playwright: MouseEvent('click') из evaluate игра не слышит
  (ввод на pointer-событиях) — четыре ассерта мерили пустоту;
- дубль `spinState` в __game (повторная ловля канонной грабли) — грепать имя
  ПЕРЕД добавлением ручки;
- **страж зачёта поднимает `baseRadius` на время `matchType` и возвращает в
  конце**: на боевых 0.45 оставшиеся пары типа бывают дальше зазора, и зачёт
  ЧЕСТНО стопорился на 4/6 (12 вызовов подряд без прироста) — флейк ловил
  РАДИУС, а не механику зачёта.
Хуки: `frozenInfo()` (тип/собрано/нужно/готова/пульс), `frozenNextAt()`,
`frozenBreak(i)` (форс), `pixelOf(i)`.

### ЛЁД ФИНАЛЬНЫЙ: ИНЕЙ-КОРКА КУСКАМИ (выбор владельца 2026-08-13, вторая половина дня)
Слово владельца по стенду: «нравится вариант иней-корка, только чуть толще
(больше отступ от объекта) и добавить немного внутреннего свечения, словно
лёд. Проверить по производительности. Трескается он и ломается как чаша —
разные куски». Сделано (40-items):
- корка = копия меша ПРЕДМЕТА ×**1.14** (было 1.07), порезанная на
  **ICE_CHUNKS=12 кусков Вороного по треугольникам** — техника разлёта чаши:
  aCen/aDir/aSpin в вершинах, один меш, один draw call; ⚠️ атрибут `aCen`,
  не `centroid` (зарезервировано GLSL);
- шейдер: френель (тело 0x8fd4ff → ободок 0xdff4ff) + **внутреннее свечение
  АНТИ-френелем** (uGlow 0xbfeaff × pow(dot(N,V),2) — светится середина, «словно
  лёд»); НЕ additive — на светлом небе additive тонет;
- **трещины = щели между кусками** (uGap 0.012 → 0.072 по ступеням зачёта,
  юниформа): узор детерминирован выпечкой, углубляется, не мигает; свежая
  глыба — волосяные швы («сразу колотая»);
- **разлом = GPU-разлёт кусков** (uBoom 0..1 за ICE_BOOM_MS=700 по РЕАЛЬНЫМ
  часам, tickIceBooms из loop): скорлупа `scene.attach` отцепляется В МИР
  (предмет живёт и двигается дальше), куски кувыркаются (Родригес по aSpin),
  летят по aDir и оседают, альфа тает; после полёта геометрия/материал
  диспозятся. shardFX остался мелкой крошкой поверх (12 шт).
- **ПЕРФ (GPU metal, CPU ×4):** выпечка В ШУМЕ genLevel (с глыбой 83-98 мс
  против 82-113 без), кадр с коркой в покое p95 32.3 (норма устоявшейся ~31),
  окно разлёта без пика (max 34.2). Замер — ice-perf.js той сессии.
- Стражи: «швы углубляются» (щель0 → щель1 в frozenInfo), «разлетелась кусками
  и прибрана» — полёт ловит СТОРОЖ-ОПРОС В СТРАНИЦЕ, поставленный ДО клика
  (полёт 700 мс, одиночное чтение из теста под нагрузкой опаздывает).
- ⛔ removeIceShell СРЕЗАН (единственный вызыватель перешёл на iceBoomStart);
  хук `iceBoomsInfo()` — доли полёта живых разлётов.

### ⛔ СТЕНД ВИЗУАЛА ЛЬДА — ИСТОРИЧЕСКИЙ (владелец выбрал, стенд срезан в тот же день)
(2026-08-13, жалоба «визуально шумно и не похоже на лёд»)
Причина шума диагностирована: у скорлупы НЕ БЫЛО ЗАТЕНЕНИЯ (MeshBasic — плоский
цвет), обе стороны просвечивали, вершины смещены «трещинами» — глаз видел только
альфа-кашу рёбер. Лёд читается гранями разной яркости, ободком по краю и
спокойным силуэтом. Сделан переключатель `ICE_STYLE` (40-items): **0 — боевой
(прежний), 1 френель-лёд (ободок по ПЛОСКИМ нормалям — рим квантуется по граням,
гладкий читался мыльным пузырём), 2 кристалл (холодный маткап `iceMatcapTex`:
хотспот мал и НЕ белый, обод ТЁМНЫЙ — бесплатный псевдо-френель; рёбра-линии
сняты, «сетку» владелец браковал), 3 иней-корка (раздутая копия ПРЕДМЕТА с
холодным френелем — приём накладки огня, силуэт кучи не меняется), 4 ледяной куб
(бокс + белые рёбра, мультклассика), 5 молочный мороз.** Переключение: `?ice=N`
или `__game.iceStyle(N)`; `iceCracks` для стилей 1-5 — no-op (нет iceBase).
⚠️ Ждёт выбора владельца; после выбора победителя сделать стилем 0, лишние
срезать, трещины — языком победителя (кандидаты панели: крек-текстура
alphaTest'ом навью-линиями; для куба/кристалла — структурные щели по чанкам;
пульс готовности — ТЕМНЕЮЩИЙ, не светлеющий: светлеющий тонет на светлом фоне).
⚠️ Грабли стенда (все пойманы кадрами): угроза помола красит верх кадра за
секунды простоя — на каждой пересъёмке держать `lastAction` свежим; предмет для
показа брать ВЫТЯНУТЫЙ (на шаре скорлупа облегает вплотную и стили сливаются);
DOM-хром на съёмке прятать CSS'ом, глаза сидят ровно над верхом кучи.

## ЭКРАН НОВОЙ ВЕЩИ: КАЧЕСТВО + ВРАЩЕНИЕ ПАЛЬЦЕМ (слово владельца 2026-08-13)

Буфер спина = размер узла × DPR (кап 768; прежние 256 растягивались втрое и
мылились на телефоне), параметром `thumbSpinStart(item, host, px)` —
коллекция остаётся на 256, у общего канваса КАЖДЫЙ старт выставляет размер
заново (страж: после экрана вещи спин коллекции снова 256). Драг пальцем/
курсором ведёт угол (`thumbSpinNudge`), авто-вращение глушится на время драга
и возвращается на отпускании; `touch-action:none` на узле — жест не отдаётся
прокрутке. Проверка буфера ТОЛЬКО при deviceScaleFactor>1 — на DPR=1 формула
честно даёт прежние 256, и страж был бы пуст.

**ВРАЩЕНИЕ ПО ВСЕМ ОСЯМ (второе слово владельца 2026-08-13):** вторая ось
`spinTilt` (85-hud) — горизонталь драга ВЕРТИТ (yaw), вертикаль НАКЛОНЯЕТ
(tilt, тот же коэффициент 0.012/px); авто-вращение осталось одноосным, наклон
двигает только драг, старт спина сбрасывает его в позу портрета. ⚠️ Рамка
ортокамеры при заданном px расширена **×1.22** — без запаса наклон клипал
углы модели (Y-инвариантность рамки верна только для чистого yaw). Коллекцию
расширение не трогает (px там не передаётся). Страж: pointermove со сдвигом
по Y обязан дать наклон ≈0.6; `spinState()` несёт поле `tilt`.

## ЗАМЕНА ПАРТИИ ЖИВОТНЫХ (3D-шник владельца, 2026-08-13)

Все 24 зверя заменены на обновлённые из `3d assets/Animals/Update`; старые —
в `3d assets/Animals/Archive-2026-08-13/` («текущих убери в архив», слово
владельца; ⚠️ `3d assets/` ВНЕ git — архивная папка и есть единственный
бэкап). Проверено по канону замены моделей, все проверки ЧИСТЫЕ:
- набор имён идентичен (24/24, diff пуст) — прогрессия/сейвы целы (ключи —
  имена типов);
- атлас `MODEL_ATLASES['animal']` БАЙТ-В-БАЙТ тот же (md5) — новые glb без
  своих текстур, палитра общая `Animals/Textures/colormap.png`;
- item.r на юниформ-уровне: расхождений НОЛЬ; семейство кольца удара
  (`ringFams`) — ни один из 24 не сменил;
- портретный монтаж «БЫЛО/СТАЛО» отправлен владельцу, пустышек 3174 Б нет.
⚠️ ГРАБЛИ ЗАХОДА: (1) `bee1.glb` в Update байт-в-байт равна УЖЕ внедрённой
пчеле, а генератор нормализует имя в `animalbee1` — класть её надо под именем
`bee` (иначе тип разъедется с TYPES); (2) генератор «ВСЁ ИЛИ НИЧЕГО»:
`glb2module.py` перезаписывает файл целиком — 36-models регенерится ТОЛЬКО
пятью пачками разом (Animals/Brick/Car/Food/Pirate, все из `.lowpoly`);
Kenney-пачки живут в 38-kenney и этим вызовом не трогаются.
✅ **ВЫИГРЫШ ВЕСА:** новые модели легче — 36-models 4.67 → 4.01 МБ,
index.html 10.34 → 9.69 МБ, **ZIP пакета портала 4.46 → 3.91 МБ** (запас до
8 МБ теперь 4.09 МБ).

## ТАБЛИЦА МГНОВЕННА ПОСЛЕ ПОКУПКИ (жалоба владельца 2026-08-13, скриншот 7 406/1 406)

Пилюля меню тратится сразу, а строка таблицы ждала сервер (частота отправки
20 с + кэши) — ощутимый гэп. Правка в 85-hud (экран — зона диспетчера):
- счёт СВОЕЙ строки = ЖИВОЙ `leaderboardScore()`, не `m.score` сервера;
- ⚠️⚠️ ЭПОХА СТРОКИ ОДНА (урок «двух эпох»): пока сервер НЕ ДОГНАЛ (живой !=
  m.score), место выводится ИЗ ТОГО ЖЕ живого счёта — вставкой по убыванию в
  видимый отрезок; догнал — прежний путь по `m.rank` (живое место сервера);
- `onStarsChange` перерисовывает ОТКРЫТЫЙ экран сразу (из кэшей `__lb`, сети
  не дёргает); «Loading…» — только на пустом списке, иначе мигало бы;
- точка входа («N place») осталась строго серверной (`exact`) — правило
  закрытого отказа не тронуто; захочет владелец мгновенное число и там — его
  слово, локальная оценка вне видимого отрезка была бы прикидкой.
⚠️ Страж: мок me ПРОДОЛЖАЕТ отвечать старым счётом — новое число на экране
доказуемо только живой веткой; вторым признаком колонка счёта остаётся
убывающей (возврат «двух эпох» роняет ровно его).

## БОНУСНЫЙ УРОВЕНЬ: ВИТРИНА (переделка по слову владельца 2026-08-17)

Дословно: «нужно переделать бонусный уровень. 1. Нет времени до измельчения
2. Нет лопастей блендера 3. Уровень нельзя вращать, игрок смотрит на вещи в
профиль». Раскладку он выбрал сам из двух показанных — **«Витрина: узкий ящик,
камера спереди»**; показ таймера — **«не показывать вовсе»**.

**ЧТО ЭТО ЗА РАСКЛАДКА.** Контейнер — неглубокий ЯЩИК (`BONUS_W` 7.4 × `BONUS_D`
2.8, дно 1.0, стены до 20). Камера стоит СПЕРЕДИ, азимут 0, `phi = π/2` —
строго горизонтальный взгляд, и не вращается вовсе. Вещи лежат столбом и видны
СБОКУ, силуэтами и забивают портретный экран по всей ширине. Ни чаши, ни
лопастей, ни отсчёта.
⛔ **«ДО ГЛАЗ МИКСЕРА» ОТМЕНЕНО ЕГО ЖЕ СЛОВОМ 2026-08-17-г** — столб кончается
на 2/3 высоты кадра, см. подраздел «ВИТРИНА БЕЗ АНИМАЦИИ» ниже.

⚠️⚠️ **ГЛУБИНА — ЭТО РЫЧАГ ВЫСОТЫ СТОЛБА, А НЕ ТОЛЬКО «ТОНКОСТИ».** Замер: при
D=4.2 те же 216 предметов давали верх 8.55 — половина кадра пустая. Глубина НЕ
ВИДНА (её закрывает передний слой), но съедает предметы в задние ряды. Урезав её
до 2.8, тот же счёт тел встал в полтора раза выше. **Заполнять кадр глубиной —
платить телами за невидимое.** Высоту добираем глубиной и числом пар, а не
камерой.

⚠️⚠️ **ДОСТУПНОСТЬ НА ВИТРИНЕ — ПЕРЕКРЫТИЕ СПЕРЕДИ, А НЕ ЛУЧИ К НЕБУ, И ЭТО
НЕСУЩЕЕ.** Столб стоит стеной в дюжину рядов: небо видит только ВЕРХНИЙ ряд, то
есть веер «к небу» загнал бы под вуаль всё остальное — а владелец играет на Hard
и увидел бы серую витрину. ⛔ Очевидная замена «пустить веер ВПЕРЁД, к камере»
САМА СЕБЯ УБИВАЕТ: у ящика есть собственная передняя стена, луч упирается в неё,
и недоступным снова становится всё. Поэтому `bonusAccessible` (60-access) — не
луч, а ГЕОМЕТРИЯ: предмет доступен, если его не накрывает сосед, стоящий ближе к
передней грани, в плоскости экрана (`BONUS_OCC_K` = 0.62 от суммы радиусов).
Замер на Hard: доступно 62 из 240 (25.8%), доступных пар 9 — играбельно и не
вырождено.
⚠️ **ПРИЗНАК ОСТАЁТСЯ КАМЕРОНЕЗАВИСИМЫМ** (канон, три бага владельца на этом):
«перёд» — это внешняя нормаль ВИТРИНЫ (+Z), свойство контейнера, а не ракурса.
Камера тут и так заперта, но правило важнее случая.
⚠️ Следствие: `refreshAccessibilityNear` на витрине вырождается в полный обход.
Его геометрия («цилиндр по XZ, ниже точки не трогаем») выведена из ВЕРТИКАЛЬНЫХ
лучей; здесь открывается тот, кто ЗА снятой группой по Z, а он бывает и ниже.
Полный обход дёшев — перекрытие это арифметика, а не касты.

⚠️⚠️ **СПАСАТЕЛЬ СМОТРИТ НА ЦЕНТР, А НЕ НА ОХВАТ — ВЫВЕДЕНО ЗАМЕРОМ, ДВЕ ВЕРСИИ
ДО ЭТОГО ШТОРМИЛИ НА ЗДОРОВОЙ КУЧЕ.**
1. Радиальная (`d + reach` против `wallDistAt`) СМЕШИВАЕТ оси: полуглубина 1.4,
   предмет у центра (|x|≈0, |z|≈1.1) даёт 1.74 против «стены» 1.4 и объявляется
   вылетевшим, хотя по X до стены два метра. Замер: 113 телепортов за прогон.
2. Пооосевая ПО ОХВАТУ упёрлась в канонное «запас метрики больше порога тревоги»:
   `radialReach` переоценивает габарит (у банана до 0.36) при полуглубине 1.4 —
   переоценка съедает допуск целиком.
**ЦЕНТР от переоценки не зависит вовсе.** ЗАМЕР ЗДОРОВОЙ КУЧИ (4 раскладки × 3
встряски, 240 предметов): |x| max **3.56** при грани 3.70, |z| max **1.27** при
грани 1.40. Порог = грань + 0.25 стоит в пустом коридоре (запас 0.39 и 0.38).
Итог: 113 → **3** спасения за прогон, то есть уровень нормы.
⚠️ И диагностика переведена туда же: `maxWallExcess` на витрине ДЕЛЕГИРУЕТ
`bonusWallExcess` (выход центра за грань) — иначе она печатает бессмыслицу вроде
`wall=1.49` для предмета в двух метрах от боковой стены, и на этом уже потерян
один разбор.

**ЧТО ЕЩЁ ПРИШЛОСЬ ВЫКЛЮЧИТЬ, И ПОЧЕМУ КАЖДОЕ — БЛОКЕР:**
- ⚠️⚠️ **ТРИМ.** `trimOverfill` изымает пары выше «красной линии» чаши (≈9.0), а
  столб витрины стоит до ~13: под нож ушла бы вся верхняя половина уровня,
  ТИХО и парами — без единого симптома, кроме «уровень почему-то короткий».
  Следствие, на которое опирается пополнение: `aliveN0` на витрине равен полному
  числу выданных предметов.
- **ПОЛЁТ КАМЕРЫ К ПОДСКАЗКЕ** (`hintCamFly`) — это ВРАЩЕНИЕ (азимут + наклон),
  то есть прямое нарушение пункта 3. Подсветка группы осталась; лететь на витрине
  некуда, весь уровень и так в кадре.
- **ОБЛЁТ В ИНТРО** — по той же причине; вместо оборота идёт наезд спереди.
  ⚠️ ФАЗЫ И ТАЙМИНГ ИНТРО НЕ ТРОНУТЫ: в этом окне живёт подача волн тел, и
  работа, уехавшая за край окна, уже стоила проекту ложного вывода.
- **ЛОПАСТИ** (пункт 2) гейтятся В ЦИКЛЕ, как и стекло чаши: снятие `visible` в
  20-arena прожило бы один кадр (грабля `bowlBroken`).

⚠️ **ПОКАЗА ТАЙМЕРА НЕТ, МЕХАНИКА 60 СЕКУНД ЖИВА** (его выбор). СЛЕДСТВИЕ, о
котором надо знать: единственным телеграфом стока осталась КРАСНАЯ ЗАЛИВКА НЕБА
(ветка `grinding`). Снимут её — сток начнёт наступать без предупреждения вовсе.

⚠️⚠️ **СТРАЖ ПРОЗРАЧНОСТИ ПРИЗРАКА ПЕРЕДЕЛАН, ПОТОМУ ЧТО СТАРЫЙ МОЛЧА ОСЛЕП.**
Прежняя редакция мерила ДОЛЮ ДОСТУПНЫХ на ур.3 (коридор 43-52% против 0%), и он
был выведен против круглого «купола» на высоте 6.1. У ящика призрак другой формы,
и перезамер дал ПЕРЕКРЫТИЕ: здоровые 39.7-53.3% против сломанных 29.8-43.1% —
порог 20% зелен в обоих случаях. Действующий страж — ПРЯМОЙ ЗОНД
(`__game.sensorProbe()`): луч вниз вдоль колонны призрачной боковой стены, и
рядом контрольный каст с видимыми сенсорами. Замер: сквозь призрак **11.7**,
контроль **5.0**; диверсия (снять флаг) даёт 5.0/5.0 — красный.
⚠️ **ЗОНД ИДЁТ БОЕВОЙ ФУНКЦИЕЙ `skyCast`** (вынесена в 60-access ради этого):
собственный каст со своим флагом проверял бы Rapier, а не нас.
⚠️ **ПРАВИЛО, КОТОРОЕ ОТСЮДА СЛЕДУЕТ: страж, чей коридор выведен против КОНКРЕТНОЙ
геометрии, обязан быть перемерен при её смене.** Он не краснеет и не падает — он
ЗЕЛЕНЕЕТ НАВСЕГДА, а это неотличимо от исправности.

### ⚠️⚠️ ВИТРИНА БЕЗ АНИМАЦИИ, СТОЛБ НА 2/3 КАДРА (слово владельца 2026-08-17-г)

Дословно: «анимации на 10 уровне быть не должно, игрок видит сразу объекты
заполненные по всей ширине вью и на 2/3 по высоте».
⛔ **ЭТИМ ОТМЕНЁН ЕГО ЖЕ ПРЕЖНИЙ ВЫБОР «ДО ГЛАЗ МИКСЕРА»** — столб больше не
доходит до глаз, он кончается на двух третях кадра. Константа переименована
(`BONUS_FILL` → `BONUS_FILL_VIEW`) вместе со сменой смысла: было «доля видимой
высоты выше взгляда», стало «доля ВЬЮПОРТА от его низа». Протухшее имя здесь —
протухшая формула через месяц.

**КАК СДЕЛАНО «БЕЗ АНИМАЦИИ».** Предметы рождаются РЕШЁТКОЙ на своих местах и
укладываются СИНХРОННО в конце `genLevel` (`settleBonusNow`); фазы `drop` и
`orbit` пропускаются — из `wait` идём прямо в `finishIntro` (`bonusOrDrop`).
Первый нарисованный кадр уже показывает готовый ящик.
⛔⛔ **И ИМЕННО ТАК, А НЕ «finishIntro ИЗ genLevel»** — три причины, каждая
блокер: (1) `finishIntro` шлёт GAME_READY, а на ХОЛОДНОМ старте прямо на
бонусном уровне кадров нарисовано НОЛЬ — занавес площадки снялся бы над пустым
экраном, запрет «третьей точки»; (2) там же заводятся часы раунда — 60 секунд
горели бы под занавесом (класс бага 2026-08-12); (3) в `wait` живёт пролог.
Фаза `wait` остаётся, она просто НЕВИДИМА: куча уже на месте.
⚠️ Ветка наезда камеры в облёте (редакция 2026-08-17-б) стала мёртвой — снята с
надгробием, а не оставлена «на вид живой».

⚠️⚠️ **ТРИ ГРАБЛИ УКЛАДКИ, ВСЕ ПОЙМАНЫ ЗАМЕРОМ, НЕ ЧТЕНИЕМ:**
1. **Шаг решётки считался ПО КАЖДОМУ предмету, а счётчик рядов общий.** Один
   банан (r=0.87 против типичных 0.62) резал вместимость ряда вчетверо и уезжал
   в 45-й ряд: куча стартовала с высоты **53** и за 90 шагов не ложилась.
   Решётка считается ОДИН РАЗ на уровень, шаг — по СРЕДНЕМУ габариту, подъём
   первого ряда — по максимальному.
2. **Вместимость считалась «пролёт минус ЦЕЛЫЙ габарит»** — при глубине 2.8 и
   шаге 1.14 выходил ОДИН слой по Z вместо двух, и столб вставал вдвое выше.
3. **Типы ложились БЛОКАМИ** (решётка заполняется в порядке создания пар): на
   кадре — выложенная витрина магазина, а не куча, и матчи тривиальны. Ливень
   мешал их сам; здесь мешаем явно — перемешиваются ПОЗИЦИИ, не состав.
⚠️ По вертикали решётка сжата сильнее (`BONUS_LATTICE_KY` 0.62): вертикальный
нахлёст солвер разжимает ВДОЛЬ гравитации, то есть быстро; горизонтальный
требовал бы перестроения рядов.
⚠️ Осадка выходит ПО ФАКТУ ШТИЛЯ (`maxBodySpeed < 0.35`), кап 260 — страховка.
Замер: типично 110-200 шагов, остаточная скорость 0-0.33 (движения не видно).
**ЦЕНА, НАЗЫВАЮ ЧЕСТНО:** genLevel на витрине стоит ~1.5-1.9 с под CPU ×4 и
~0.3-0.4 с без троттлинга. Это разовая заминка ПРИ ВХОДЕ в уровень, спрятанная
занавесом площадки (холодный старт) или экраном победы (переход).

⚠️⚠️ **СТРОГОСТЬ ПЕРЕКРЫТИЯ ПРИШЛОСЬ ОСЛАБИТЬ, И ЭТО НЕ ПОБЛАЖКА, А ЧЕСТНОСТЬ.**
`BONUS_OCC_K` 0.62 → **0.42**. Тап ходит рейкастом по МЕШАМ: предмет, накрытый
наполовину, игрок видит и попадает по нему — а строгий порог объявлял его
недоступным, то есть ШТРАФОВАЛ ЗА ПОПАДАНИЕ ПО ВИДИМОМУ. Ящик глубиной в два
слоя даёт «спереди» примерно половину кучи, и доля доступных обязана быть того
же порядка. ЗАМЕР (Hard, ур.10/20/30 — доля доступных | доступных пар):

| K | доля | пар |
|---|---|---|
| 0.62 | 22/24/23% | 13/16/15 |
| 0.50 | 32/32/37% | 27/21/33 |
| **0.42** | **43/43/36%** | **47/34/24** |
| 0.34 | 51/49/57% | 59/50/68 |

При 0.62 витрина переставала быть бонусом — пар столько же, сколько на обычном
уровне.

**СЛЕДСТВИЯ ЧИСЕЛ:** пар на уровне ~91 (182 предмета) вместо 130/260; худший
случай стока для пассивного игрока — **~91 секунда**, не 120.

### ⚠️⚠️ ВИДОВ НА ВИТРИНЕ НЕ БОЛЬШЕ ПЯТИ (слово владельца 2026-08-17-в)

Дословно: «предметов на бонусном уровне должно быть не более 5, иначе очень
сложно». Читать как ВИДЫ, а не штуки: штук там 260 по построению, и «не более 5
штук» противоречило бы всей остальной спеке; сложность же на витрине задаёт
именно разнообразие.

**ПОЧЕМУ ЭТО САМЫЙ СИЛЬНЫЙ РЫЧАГ ИМЕННО ЗДЕСЬ.** Канон давно фиксирует: «ГЛАВНЫЙ
рычаг сложности — число типов (замер ботом: тупики зависят от него, радиус влияет
слабо)». На витрине он бьёт вдвойне — стена в 260 предметов, и пару надо найти
ГЛАЗОМ среди двух десятков видов. `BONUS_TYPES_MAX = 5`, применяется `Math.min`
поверх обычной прогрессии.
⛔ **ЭТО ПОТОЛОК, А НЕ ФИКСАЦИЯ:** на ранних уровнях открыто меньше пяти типов, и
выдумывать недостающие нельзя.
⚠️ **ПОПОЛНЕНИЕ ПИТАЕТСЯ ОТТУДА ЖЕ** (`level.typesCount`), то есть досыпает из ТЕХ
ЖЕ пяти видов. Бери оно весь открытый пул — витрина к середине партии расползлась
бы обратно в два десятка видов, и потолок был бы косметикой.

**ЗАМЕР (HARD, живое интро):**

| уровень | видов | предметов на вид | доступных пар |
|---|---|---|---|
| 10 (витрина) | 5 | 52 | **26** |
| 11 (обычный) | 14 | 13 | 16 |
| 20 (витрина) | 5 | 52 | **27** |
| 21 (обычный) | 26 | 7 | 9 |
| 30 (витрина) | 5 | 52 | **23** |

То есть на витрине доступных пар ВТРОЕ больше, чем на соседнем обычном уровне, и
разрыв растёт с номером — ровно то «иначе очень сложно», о котором он сказал.
⚠️ Страж держит ОБА конца: потолок на витрине И контроль на обычном уровне (без
него «видов ≤ 5» зелено и у сборки, где пул схлопнулся целиком), плюс следствие
(пар на витрине больше) и сохранение потолка после пополнения.

### ⚠️⚠️ ЯЩИК = ВЬЮПОРТ (уточнение владельца 2026-08-17-б)

Дословно: «почти хорошо, но я хотел бы чтобы боксом был именно вьюпорт и
как-то правильно умел реагировать на изменение ширины». Это УТОЧНЕНИЕ витрины,
а не отмена: раскладка та же, ширина перестала быть константой.

**ПОЛУШИРИНА ВЫВОДИТСЯ ИЗ ПРОПОРЦИЙ КАДРА** (`bonusFreezeBox` в 20-arena): такой,
чтобы ЗАДНЯЯ грань — самая дальняя, а значит самая узкая в кадре — проецировалась
ровно на кромку экрана (+4% запаса против волосяной щели). Закрыта задняя —
закрыто всё. Рамки `BONUS_HX_MIN/MAX` 2.6..7.0.

⚠️⚠️⚠️ **ГЛАВНОЕ ПРАВИЛО ФИЧИ: ШИРИНА ЗАМОРАЖИВАЕТСЯ НА УРОВЕНЬ, ЖИВОЙ ОСТАЁТСЯ
ТОЛЬКО КАМЕРА.** Физические стены ставятся ОДИН РАЗ на genLevel; читай
потребители ширины (спасатель, спавн, метрика, зонд, временная стена) ЖИВОЙ
аспект — ресайз посреди партии развёл бы формулу с ФАКТИЧЕСКИМИ стенами, и
спасатель начал бы телепортировать здоровую кучу. **Этот класс дефекта в витрине
ловился уже дважды** (радиальная метрика, охват вместо центра) — третий раз не
нужен. Все читают `bonusHalfX()`; на ресайз отвечает `bonusCamR()` обратной
формулой: стало шире — камера подъехала, покрытие сохранилось, куча не
шелохнулась. ⚠️ Заморозка стоит В САМОМ НАЧАЛЕ genLevel, до временной стены и
спавна.

⚠️ **ЧИСЛО ПАР ТОЖЕ ВЫВОДИТСЯ** (`bonusPairs`) — из объёма, который надо
заполнить (плотность 0.95 замерена: 240 предметов в объёме 7.4×2.8×11.7 дали
0.99), с капом 130 пар. Без этого широкий кадр требовал бы ~900 тел, а узкий
переливался бы через верх.

⚠️ **ЗУМ НАРУЖУ ОГРАНИЧЕН ПОДГОНКОЙ** — шаг назад, и по бокам полезет фон, то
есть «ящик = вьюпорт» перестанет быть правдой. Приближение осталось. И боевой
пол `CAM_R_MIN = 9` тут не годится: на широком экране подгонка даёт ~6.7.

⚠️⚠️ **ПОПУТНО НАЙДЕН И ИСПРАВЛЕН НАСТОЯЩИЙ ДЕФЕКТ: `buildTempTallWall` ЧИТАЛ
ПРЕДЫДУЩИЙ УРОВЕНЬ.** Он зовётся из genLevel ДО создания нового `level`, а решал
по `level.bonus` — то есть на переходе «витрина → обычный» стена осадки строилась
ЯЩИКОМ поверх круглого спавна чаши. Решает теперь `isBonusLevel(levelNum)`.
**Правило: всё, что зовётся из genLevel раньше строки `level = {...}`, обязано
решать по `levelNum`, а не по `level`.**

✅ **МУТАЦИЯ КОЛЛАЙДЕРОВ ПРОВЕРЕНА, А НЕ ПРЕДПОЛОЖЕНА** (канон запрещает
пересоздание: «WASM Rapier падал unreachable»): `setHalfExtents`+`setTranslation`
— другой путь. Проба: 100 мутаций со `step` между ними на живой куче — ноль
ошибок страницы, предметов 260 → 260, выступ −0.131. ⚠️ После мутации
query-конвейер видит старые позиции до шага мира (тот же класс, что грабля
`place()`) — `syncBonusContainer` прокачивает его сразу.
⚠️ Мутируем ТОЛЬКО на genLevel: посреди партии куча уже лежит, и депенетрация
вышвырнула бы её.

**ЗАМЕР ПО ВЬЮПОРТАМ (HARD, живое интро, ур.10):**

| вьюпорт | аспект | полуширина | camR | пар | верх кучи | кадр закрыт |
|---|---|---|---|---|---|---|
| 390×844 | 0.462 | 3.60 | 13.0 | 130 | 14.7 | да (±1.04) |
| 360×740 | 0.486 | 3.79 | 13.0 | 130 | 13.9 | да |
| 430×932 | 0.461 | 3.60 | 13.0 | 130 | 14.2 | да |
| 1280×800 | 1.600 | 7.00 (кап) | 6.68 | 130 | 8.7 | да |

**РЕСАЙЗ ПОСРЕДИ ПАРТИИ** (390 → 300 → 500 → 800): ширина ящика ОСТАЁТСЯ 3.602,
camR идёт 13.0 → 17.3 → 9.8 → 5.6, покрытие держится, живых 260 без изменений,
выступ −0.132 без изменений, ошибок страницы ноль.
⚠️ **СЛЕДСТВИЕ ДЛЯ ДЕСКТОПА, НАЗЫВАЮ ЧЕСТНО:** на широком кадре ширина упирается
в кап, камера подъезжает вплотную, и «до глаз» превращается в «сплошь» — полоски
неба почти нет. Целевая раскладка портретная; десктоп остаётся играбельным.

**ЗАМЕР (линейка: headless на GPU `--use-angle=metal`, 390×844, HARD, живое
интро, ур.10):** живых 260, верх 14.2, выступ центра −0.13..−0.28 (внутри граней),
спасений 0 за прогон, доступных 28.1%. Переходы 10↔11 и 20↔21
проверены игрой: контейнер, кадр, лопасти и видимость чаши возвращаются в обе
стороны.

⚠️ **ВСЕ ПРОБЫ ЭТОЙ ПЕРЕДЕЛКИ ГОНЯЛИСЬ НА HARD С ПЕРВОГО ПРОХОДА** — намеренно.
Доступность здесь центральная механика раскладки, а на Easy `isAccessible`
выходит первой строкой и не исполняет её вовсе; ровно это слепое пятно накануне
стоило пропущенного сенсорного дефекта.

## ⛔ БОНУСНЫЙ УРОВЕНЬ, ПЕРВАЯ РЕДАКЦИЯ (КОВЁР СВЕРХУ) — ОТМЕНЕНА 2026-08-17

⛔⛔ **РАЗДЕЛ ИСТОРИЧЕСКИЙ ЦЕЛИКОМ В ЧАСТИ РАСКЛАДКИ.** Владелец посмотрел живьём
и заказал переделку тремя пунктами: «1. Нет времени до измельчения 2. Нет
лопастей блендера 3. Уровень нельзя вращать, игрок смотрит на вещи в профиль».
Действующая раскладка — **ВИТРИНА**, раздел «БОНУСНЫЙ УРОВЕНЬ: ВИТРИНА» ниже.
⚠️ ЧТО ИЗ ЭТОГО РАЗДЕЛА ОСТАЛОСЬ ДЕЙСТВУЮЩИМ: период (каждый 10-й), 60 секунд,
пополнение 2× до 50% при <20%, сток парами по секунде «пока не исчезнут все»,
цена «как у помола», отсутствие спецпредметов, переключение контейнера сенсором,
дефект `castRay`+сенсоры. ⛔ ЧТО ОТМЕНЕНО: круглый ковёр `BONUS_R`, поднятое дно
под камеру сверху, кадр `BONUS_CAM_*` прежних значений, показ таймера под
глазами. Числа ковра ниже читать только как историю.

## БОНУСНЫЙ УРОВЕНЬ: КАЖДЫЙ 10-Й (спека владельца 2026-08-15)

Дословно, пять пунктов: «Хочу каждый 10 уровень сделать бонусным: 1. Логика
соединения остается 2. Все элементы интерфейса остаются и они поверх игровых
объектов 3. Вместо чаши все пространство окна игры заполняется объектами до
уровня глаз 4. Пополнение происходит 2 раза, когда вещей становится меньше 20%
от начальных, пополняются каждый раз до отметки 50% от количества на старте
уровня 5. Если время выходит, то объекты исчезают парами каждую секунду, очки
от этого так же множителями отнимаются».
⚠️ ЧЕТЫРЕ ЧИСЛА — ЕГО ВЫБОР ИЗ ПРЕДЛОЖЕННЫХ, не мой подбор: длительность
**60 секунд**, высота «**до глаз миксера**», финал «**пока не исчезнут все**»,
цена пары «**как у помола**» (MIXER_PENALTY × множитель типа × бустер).

**УСТРОЙСТВО.** Константы `BONUS_*` в 00-config, флаг снимается ОДИН РАЗ в
genLevel (`level.bonus`) — все потребители читают его, а не пересчитывают
`isBonusLevel` у себя: пересчёт развёл бы их между собой при смене периода
посреди партии. Часы заводятся в `finishIntro` (не в genLevel — иначе интро
съело бы у игрока две секунды), тикают из `loop` через `tickBonus`.
⚠️ `isBonusLevel` требует `n >= BONUS_EVERY`: без этого `isBonusLevel(0)`
истинно, а ноль приходит отовсюду (levelNum до восстановления сейва, `||0` у
необязательных аргументов).

**ЧТО НА БОНУСЕ ВЫКЛЮЧЕНО, И ПОЧЕМУ КАЖДОЕ — БЛОКЕР, А НЕ УБОРКА:**
- ⛔⛔ **СПЕЦПРЕДМЕТЫ (клад, бомба, камни, глыба) — СТРУКТУРНОЕ ТРЕБОВАНИЕ
  ПУНКТА 5.** Сток уносит ПАРАМИ «пока не исчезнут все», а спецпредмет пары не
  имеет и стоком (как и помолом) не трогается — уровень не кончился бы НИКОГДА.
  Дефект был виден в первом же замере и читался как безобидный: живых **261 =
  130 пар × 2 + ОДИН клад**. Нечётное число в куче пар — вот его сигнатура.
- **НАКАЗАНИЕ ЗА ПРОСТОЙ И ТУПИК-ВЫРУЧАЛКА.** Конец уровня назначает ТАЙМЕР.
  Детектор тупика при этом опаснее помола: под конец стока `availablePairs`
  честно падает в ноль, и выручалка пошла бы молоть кучу ПАРАЛЛЕЛЬНО стоку,
  списывая очки дважды.
- **ФИНАЛЬНАЯ ДОКИДКА ПАР И ЗАЧИСТКА ОСТАТКОВ.** Обе включаются по «пар не
  видно» — на бонусе это наступает задолго до таймера, и финал доел бы кучу
  до срока.
- ⚠️⚠️ **ТРЕЩИНЫ ЧАШИ (`bowlCrackAdd`).** Чаши на бонусе нет, но счётчик серий
  жил бы своей жизнью, и на пятой `shatterBowl` уронил бы стены (`dropWalls`) и
  собрал ВСЮ кучу разом: бонус кончался бы сам собой в середине, мимо таймера,
  стока и пунктов 4-5. Невидимый механизм, ломающий фичу целиком.
- **КРАСНАЯ УГРОЗА ПОМОЛА (`uGrind`).** Она телеграфирует наказание за простой,
  которого здесь нет. Красным наливается только САМ сток — сигнал остаётся
  честным.
- **АВТОПАН ЭНДШПИЛЯ.** Его порог `CAM_FOLLOW_FRAC` = 0.2 СОВПАДАЕТ с порогом
  пополнения `BONUS_REFILL_AT`: защёлка срабатывала бы РОВНО в момент первого
  пополнения и уводила взгляд под ковёр, из которого как раз сыплются предметы.

**КАДР (пункт 3).** ⚠️ ГЛАВНЫЙ РЫЧАГ «заполнено до глаз» — КАМЕРА, А НЕ ЧИСЛО
ПРЕДМЕТОВ. Ковёр лежит на `BONUS_FLOOR`, а боевая камера смотрит в y=4.2, то
есть НИЖЕ него — куча вставала серединой кадра с пустотой сверху и снизу.
Свой кадр `BONUS_CAM_TY/PHI/R` = 8.2 / 0.32 / 11.5 (перебор четырёх вариантов
на 390×844), ставится в `finishIntro` там же, где боевой.
⚠️⚠️ **PHI РОВНО 0.32 — ЭТО НИЖНИЙ УПОР ДРАГА (90-input), А НЕ КРАСИВОЕ ЧИСЛО:**
кадр 0.28 был чуть плотнее, но первый же драг игрока защёлкнул бы камеру в 0.32
и вернуть прежний вид было бы нечем. **Кадр обязан жить внутри коридора, который
игрок может воспроизвести руками.**
⚠️ И `panLimits()`: коридор вертикального пана на бонусе едет вверх за ковром.
Боевой потолок 5.2 НИЖЕ ковра (6.4) — первый же щипок зажал бы взгляд под кучу
НАВСЕГДА (клампу всё равно, кто просит). Через ту же функцию ходит и полёт
подсказки.

**ПОЛ ПОДНЯТ — ЭТО РЕШЕНИЕ, А НЕ ТЮНИНГ.** Цилиндр R=7.6 до глаз — объём
примерно в пять чаш; залив его честно, мы получили бы ~500 тел при 182 боевых.
Игрок при этом видит ТОЛЬКО ВЕРХНИЙ СЛОЙ (камера сверху). Поэтому вместо объёма
— ШИРОКИЙ КОВЁР: дно поднято к камере (`BONUS_FLOOR`), предметы лежат слоем в
3-4 штуки, экран забит от края до края, тел вдвое меньше честного залива.

**КОНТЕЙНЕР ПЕРЕКЛЮЧАЕТСЯ СЕНСОРОМ, А НЕ ПЕРЕСОБИРАЕТСЯ.** ⚠️⚠️ Это прямое
следствие грабли, записанной у `dropWalls`: удаление и пересоздание стен на
genLevel валило WASM Rapier «unreachable» в первом же шаге. Поэтому бонусный
цилиндр (`buildBonusContainer`) строится ОДИН РАЗ на старте рядом с чашей, а
`ensureWalls` решает, чей набор твёрдый.
⚠️ **`initPhysicsWorld` ЗОВЁТСЯ ОДИН РАЗ ЗА ЗАГРУЗКУ** — на этом легко
обмануться: правка стен «под бонус» прямо в нём работает на пробе (она грузит
страницу с уже выставленным уровнем) и НЕ работает в игре, где 9→10 идёт через
genLevel. Проверять переход надо ИГРОЙ, а не загрузкой на нужном уровне.
⚠️ ЦЕНА НАЗВАНА: +33 коллайдера к 599 боевым (~5%), прокси висят на ВСЕХ
уровнях. Дёшево потому, что бонусный контейнер — ПРЯМОЙ цилиндр: ему не нужны
12 ступенчатых колец конуса, хватает одного пояса из 32 сегментов и дна.
⚠️ `floorCol` стал АКТИВНОЙ плитой (переключает `ensureWalls`): спасатель пола
мерит по ней истинное проникновение, и на бонусе он обязан мерить БОНУСНОЕ дно,
иначе ловил бы плиту шестью юнитами ниже кучи, то есть молчал бы всегда.
⚠️ Временная стена осадки берёт радиус из `radiusAt`, а не из литерала
`FUNNEL.R1`: на бонусе спавн раскидан до 6.5, и стена на 4.1 оставила бы столб
ПАДАТЬ СНАРУЖИ неё — спасатель считал бы это вылетом.

**ПОПОЛНЕНИЕ (пункт 4).** Порог и цель — от `aliveN0` (снимок ПОСЛЕ осадки и
трима), а не от `BONUS_PAIRS×2`: трим тихо срезает пары, и «20% от начальных»
по задуманному числу означало бы не то, что игрок видел на старте.
⚠️⚠️ **СЫПЛЕМ ПАРАМИ, А НЕ РОССЫПЬЮ.** Досыпка турбо (`dropOneFromSky`) плодит
сирот легально — их доедает финал. На бонусе финала нет: одна нечётная сирота
повесила бы уровень навсегда.
⚠️ Спавн — над ЖИВОЙ кучей, а не над чашей: `dropOneFromSky` метит в
`FUNNEL.H+2`, то есть на бонусе ВНУТРЬ ковра (он лежит много выше).
⚠️ И грабля, стоившая молчаливого пополнения: **`topY` — это ТЕСТ-ХУК в
99-main, а не функция игры.** Вызов из 40-items падал ReferenceError ВНУТРИ
тика, пополнение не приходило, а счётчик его исправно тратил — снаружи
выглядело как «сработало и ничего не сделало». Грепать, а не помнить.

**СТОК (пункт 5).** Цена берётся ТЕМ ЖЕ `scorePenalty(MIXER_PENALTY)`, что у
помола, а не своей формулой: «как у помола» — это ТРЕБОВАНИЕ СОВПАДЕНИЯ, и копия
формулы рядом с работающей разошлась бы при первой правке. Оттуда же бесплатно
приезжают льготы новичка и множитель бустера.
⚠️ Почему не переиспользован `mixerGrind` целиком: (1) он тянет предмет В
ЛОПАСТИ, а они на бонусе далеко под кадром — игрок видел бы, как предметы
уезжают в никуда, поэтому расщепляем НА МЕСТЕ; (2) ему близнец необязателен
(`twin ? [low, twin] : [low]`), а здесь пара обязательна — нечётный съём оставил
бы сироту. Берём ВЕРХНИЕ, а не нижние: сток должно быть видно.
⚠️ Хвостовая ветка «пар не осталось — берём двух любых» несущая: игрок может
домолоть кучу до сирот разных типов, и без неё хвост висел бы вечно.

⚠️⚠️⚠️ **САМЫЙ ДОРОГОЙ ДЕФЕКТ ЭТОЙ ПРАВКИ, И БИЛ ОН ПО ОБЫЧНЫМ УРОВНЯМ:
`castRay` В RAPIER ПО УМОЛЧАНИЮ ВИДИТ СЕНСОРЫ.** Неактивный контейнер — именно
сенсор, и его дно (диск радиуса 7.8 на высоте 6.1) встало КУПОЛОМ РОВНО НАД
ЧАШЕЙ: каждый луч доступности упирался в него, `isAccessible` возвращал false
ВСЕМУ, и на Hard вся куча уходила под вуаль. Лечение — `QueryFilterFlags
.EXCLUDE_SENSORS` в обоих кастах (`60-access` и `floaters` в 99-main);
семантика верна и сама по себе: сквозь призрак небо видно.
⚠️ **ЧЕТВЁРТЫЙ ПАРАМЕТР, А НЕ ТРЕТИЙ.** Сигнатура `castRay(ray, maxToi, solid,
filterFlags, …)`; первая правка встала в слот `solid` и молча сдвинула всё
остальное. Слот сверять по сигнатуре, а не по счёту запятых.
⚠️⚠️ **ПОЧЕМУ МОИ СОБСТВЕННЫЕ ПРОБЫ ЭТОГО НЕ ВИДЕЛИ: ОНИ ХОДИЛИ НА EASY.**
`isAccessible` на Easy выходит ПЕРВОЙ СТРОКОЙ и лучей не пускает вовсе — три
зелёные пробы подряд (механики, переходы, кадр) физически не исполняли
сломанную ветку. Тот же закон, что уже записан про веер доступности: **прежде
чем верить зелёному, спроси, какие ВЕТКИ твой стенд вообще исполняет.**
⚠️⚠️ **И ПРО ПОСТАНОВКУ СТРАЖА НА ЭТОТ ДЕФЕКТ — ПЕРВАЯ РЕДАКЦИЯ ДИВЕРСИЮ НЕ
ПОЙМАЛА.** Страж стоял на ур.11 с порогом 5%: там куча торчит выше 6.1, часть
предметов видит небо мимо купола, урон лишь частичный (63 → 30 доступных), и
сломанная сборка оставалась ЗЕЛЁНОЙ. Полным дефект становится там, где куча
ЦЕЛИКОМ под куполом — на ранних уровнях. Замер обеих рук (доля доступных,
Hard): ур.2 52.2% против 0, ур.3 44.0% против 0, ур.5 42.1% против 0, ур.7
39.7% против 2.1%. Страж переехал на **ур.3 с порогом 20%** — в середину
пустого коридора, и рядом с ним САНИТАР «верх кучи < 6.1», иначе следующая
правка размеров тихо вернёт частичный случай и стража.

**ЗАМЕР (линейка: headless Chromium на GPU `--use-angle=metal`, 390×844, живое
интро, уровень 10):** живых 254-260, верх кучи 8.9-9.0, выступ за стену −0.09
при норме 0.45, спецпредметов 0. Переходы 9→10→11→20→21 проверены игрой:
контейнер, кадр и видимость чаши возвращаются в обе стороны.
**ПЕРФ (та же линейка + CPU ×4, окно устоявшейся игры, 6 с после встряски):**
бонус ур.10/20 — кадр p95 **32.1/32.4**, шаг физики 20.8/19.8, рывков >50 мс
НОЛЬ; соседние обычные ур.11/21 — кадр p95 32.7/33.5, шаг 16.4/21.5. То есть
**43% лишних предметов кадру ничего не стоят** — куча плоская и быстро засыпает,
а прежние «25.7 мс» были замером ОКНА НАЛИВА, а не игры.

⚠️⚠️ **СЬЮТ: 14 ЧУЖИХ СТРАЖЕЙ СТОЯЛИ НА УРОВНЯХ, КРАТНЫХ 10, И МОЛЧА СТАЛИ
БОНУСНЫМИ.** `setLevel(10/20/40/50)` — обычный способ «взять уровень повыше»,
и после этой спеки каждый такой страж мерил СОВСЕМ ДРУГУЮ игру: клад, которого
там нет; спасателя пола на поднятом дне; состав кучи из 130 пар вместо 90.
Переведены на 11/21/41/51 — сами утверждения не тронуты, это переезд за сменой
правила. **Правило на будущее: вводя правило «каждый N-й уровень особый», сразу
грепай сьют и соак на кратные N — молчаливая смена смысла страшнее красного.**
⚠️⚠️ **И ГРЕПАТЬ НАДО НЕ ОДНУ ФОРМУ, А ПЕРЕЧИСЛЕНИЕ ЦЕЛИКОМ — ЭТО ЛОВИЛОСЬ ТРИЖДЫ
ПОДРЯД.** Первый обход искал только `setLevel(N)` и пропустил массивы
(`for (const lv of [1, 5, 11, 20])`); второй пропустил хелперы (`at(160)`).
Каждый раз казалось, что список полный. Тот же закон, что у гейта локальных
хостов: **перечисляются случаи, которые ВСПОМНИЛИ, а не те, которые есть.**
⚠️ И ВТОРАЯ ВОЛНА ПРИШЛА ОТ ПОТОЛКА ВИДОВ: страж хвоста TYPES стоял на ур.160,
где теперь витрина с пятью видами, — последний тип не спавнится ПО ПОТОЛКУ, и
страж краснел на исправной сборке. То есть каждое НОВОЕ свойство бонусного
уровня заново меняет смысл чужих стражей на кратных десяти. Проверять при
каждом, а не один раз при вводе периода.

## ⚠️⚠️ «ТУПИТ» ОКАЗАЛОСЬ СКОРОСТЬЮ АНИМАЦИИ, А НЕ НАГРУЗКОЙ (2026-08-15)

**ЗАМЕР С ТЕЛЕФОНА ВЛАДЕЛЬЦА ЗАКРЫЛ СПОР** (скриншоты, iPhone, ур.3, DPR 1.5,
счётчик `?fps=1`): **FPS 60, худший кадр 17-19 мс, шаг физики 1-2 мс.** То есть
запаса вдоволь, тормозов НЕТ ВОВСЕ — и его же вывод: «тупит это скорость
анимации насыпания и оборота вокруг чаши, давай попробуем ускорить всё».
⛔ ЧЕМУ ЭТО УЧИТ НАС ОБОИХ: слово «тупит» от НЕ-РАЗРАБОТЧИКА означает
«ощущается вяло», а не «мало кадров». Полдня перф-раскопок (стенд в двух
движках, шесть фронтов ревизии) искали недостающие миллисекунды, которых не
было. ⚠️ ПРАВИЛО: жалобу на «тормоза» ПЕРВЫМ ДЕЛОМ закрывать замером С
УСТРОЙСТВА жалующегося — счётчик `?fps=1` заведён ровно для этого и стоит
дешевле любой ревизии.
⚠️ И ПЕРФ-РАБОТА НЕ ПРОПАЛА: доступность на Hard (кадр 89-106 → 35), якоря
шейдеров (компиляций 55 → 0), Rapier 0.20 (шаг −30%, вылет за стену 2.4 → 0.33)
— всё это осталось запасом, который и позволил ускорить анимацию без цены.

**СДЕЛАНО: РУЧКА СКОРОСТИ НАСЫПАНИЯ `INTRO_SPEED` (00-config), боевое 1.0.**
Множит физику падения (`INTRO_TIME_SCALE = 2.0 × speed`, было 1.3) и длину
фазы насыпания (`INTRO_DROP_MIN/MAX` 0.55/1.0 с, было 0.8/1.4). Терминальная
скорость падения в интро 14 (было 11) — на Rapier 0.20 запас по удержанию
стен вырос вчетверо. Ручка `?intro=N` (0.4..3) — сравнивать темп на телефоне.

⚠️⚠️ **ОБЛЁТ КАМЕРЫ РАЗВЯЗАН С ЭТОЙ РУЧКОЙ И ВЕРНУЛСЯ К 1.0 с** (владелец
посмотрел на телефоне: «скорость насыпания нравится, но облёт нужно вернуть,
иначе слишком быстро»). Я сжимал его до 0.65 с СВОИМ добором к его «ускорить
всё» — он развёл эти две вещи. `INTRO_SPEED` на облёт больше НЕ влияет, иначе
ускорение наливания снова тянуло бы камеру за собой; отдельная ручка
`?orbit=N` (0.3..3 с). Значение 1.0 — его СТАРАЯ спека («облёт за 1 секунду»),
менять только его словом.
⛔ УРОК: «ускорить всё» от владельца — это НАПРАВЛЕНИЕ, а не разрешение сжать
каждую часть до предела. Части, которые он потом различит на глаз, лучше сразу
делать РАЗДЕЛЬНЫМИ ручками — тогда правка его вердикта стоит одну строку.
**Итоговый тайминг интро: 2.79 с (до) → 1.69 с (мой перегиб) → 2.02 с (боевое,
насыпание быстрое + облёт прежний).**

⚠️ ЗАПОЛНЕНИЕ СВЕРЕНО ОТДЕЛЬНО (канон: интро кончается по часам КАМЕРЫ, значит
другая скорость застаёт осадку в другой стадии), ур.20, по 3 прогона на темп:

| темп | интро | до штиля | живых | верх кучи |
|---|---|---|---|---|
| 0.6 (как было) | 2.79 с | 6.6-8.9 с | 182 | 7.52-7.73 |
| **1.0 (боевое)** | **1.69 с** | 4.6-5.5 с | 182 | 7.90-8.15 |
| 1.5 | 1.13 с | 4.1-5.8 с | 182 | 7.81-8.16 |

Живых 182/182 везде, верх в норме 7.5-9.0 — заполнение не поехало ни на одном
темпе; куча вдобавок УСПОКАИВАЕТСЯ БЫСТРЕЕ (падают быстрее — оседают раньше).
⚠️ Сьют 680 PASS / 0 FAIL, включая секцию «пауза во время интро» (она ходит
через НАСТОЯЩЕЕ интро и первой поймала бы сжатые пороги фаз).

## RAPIER 0.19.3 → 0.20.0: ВЗЯТО (слово владельца «делай», замер 2026-08-14)

Ревизия назвала апгрейд единственным настоящим рычагом внутри физики (0.20
переписала broad- и narrow-phase — ровно те фазы, что у нас дороги). Взято
после A/B; ⚠️ ЛИНЕЙКА: headless GPU metal, CPU ×4, 390×844, **HARD** (режим
владельца), сборки-близнецы отличаются ТОЛЬКО вендорным бандлом.

| | 0.19.3 | 0.20.0 |
|---|---|---|
| шаг физики в игре, ур.11 / ур.20 | 14.5 / 15.1 мс | **10.4 / 10.5 мс** (−28-30%) |
| время до полного покоя кучи | 5.5 / 6.1 с | 5.1 / 5.5 с |
| соак 12 мин (сид 303, Hard): спасений | 94 | **8** |
| рост кучи памяти за 12 мин | +5.9 МБ | **+1.7 МБ** |
| ZIP пакета портала | 3.91 МБ | 4.16 МБ (запас до 8 — 3.84) |

⚠️⚠️ **ГЛАВНОЕ — НЕ СКОРОСТЬ, А УДЕРЖАНИЕ. И ЭТО ДОКАЗАНО РАСПРЕДЕЛЕНИЕМ, А НЕ
СЧЁТЧИКОМ СПАСЕНИЙ** (канон: падение числа спасений само по себе двусмысленно —
может значить и «держит лучше», и «страховка ослепла»). Выступ за стену по
15-16 тыс. сэмплов соака:

| | 0.19.3 | 0.20.0 |
|---|---|---|
| p99 | +0.820 | **−0.126** |
| максимум | **+2.415** | **+0.330** |
| сэмплов выше нормы 0.45 | **227** | **0** |

То есть на 0.20 максимум выступа НИЖЕ нашей нормы тревоги — предметам просто
нечем вылезать, и спасателю нечего ловить. Это снимает и старую оговорку
«норма 0.45 стоит впритык к здоровому максимуму 0.407»: запас стал двукратным.
⚠️ СЛЕДСТВИЕ: пороги `WALL_EXCESS_NORM` и подъёмов пола калибровались под
0.19.3 и теперь ЗАВЕДОМО консервативны (сигнал не пропадёт, но чувствительность
упала). Переставлять их — отдельная работа по распределению, НЕ в этом заходе.
✅ ПРОВЕРКИ: сьют 680 PASS / 0 FAIL на 0.20 (включая все калиброванные пороги),
соак 12 мин Hard — problems 0, errors 0, провалов и подъёмов пола 0, ошибок
страницы 0 на обеих сборках A/B. API-совместимость подтверждена эмпирически:
ни одного `PAGEERROR` ни на одном из наших путей (компаунды, капсульные цепи,
кольцо пончика, contactCollider-метрика матча, castRay-веер, setEnabled волн).
⚠️ ЦЕНА НАЗВАНА: бандл 2.24 → 2.85 МБ, `index.html` 9.72 → 10.33 МБ, ZIP
пакета +0.25 МБ. Против этого — «половина index.html это геометрия моделей»
(ревизия): если вес станет проблемой, резать надо ТАМ, а не откатывать физику.
⚠️ Пересборка вендора — командой из шапки `build.py`, версия закреплена в
`package.json` (^0.20.0). Откат: `git revert` + `npm i @dimforge/rapier3d-compat@0.19.3`.

**ТРИ РИСКА ИЗ НЕЗАВИСИМОГО РАЗБОРА API — ПРОВЕРЕНЫ ПОИМЁННО.**
1. ⛔ **«На активной куче 0.20 ХУЖЕ на 14%, p95 удваивается» — НЕ ПОДТВЕРДИЛОСЬ
   НА НАШЕЙ СЦЕНЕ.** Разбор мерил СИНТЕТИКУ (80 тел, ни одного convex hull и
   компаунда, десктоп без троттлинга, искусственная каденция пробуждений раз в
   60 шагов — она и могла бить в перестройку графа контактов). Наш замер по
   фазам (ур.20, HARD, CPU ×4, 2 прохода, руки чередуются): активная куча
   **p95 15.7/15.8 → 11.6/11.6, max 16.3/16.7 → 12.8/12.9** — то есть на 26%
   ДЕШЕВЛЕ. Насыпание в шуме (14.4/14.6 → 15.4/14.5), засыпание не сломано
   (2.3-2.8 с → 1.2-2.8 с). **Урок: чужой перф-вывод проверять на СВОЕЙ
   сцене — у нас компаунды, капсульные цепи и hull, синтетика их не имеет.**
2. ⚠️ **Калибровка порогов протухла — ПРИНЯТО, ЖИВЁМ.** Просадка в пол упала
   ~50× (разбор), поэтому страж «спасатель вынул всех» теперь ловит пустоту.
   ⚠️ НО ОТНОСИТЕЛЬНАЯ ВЕТКА ЖИВА И ПРОВЕРЯЕТСЯ: детерминированный `penProbe`
   на 0.20 зелен и печатает работу («порог 0.0976 при толщине 0.1085, ниже
   абсолютного 0.12»). Пороги стали КОНСЕРВАТИВНЫ (сигнал не пропадёт,
   чувствительность упала) — переставлять по распределению отдельным заходом.
3. ⚠️ **CCD-семантика инвертирована — ПРИБОР МЁРТВ, ИГРА ЦЕЛА.** Подробности и
   следствия — надгробием у `setCcdSel` (50-physics). Боевое значение выключено,
   против стен CCD и раньше был нужен; опасность только в том, что ручкой
   больше нельзя мерить.
⚠️ И честная оговорка о зрелости: 0.20.0 вышла 2026-08-08, патчей ноль — то
есть ни багов не всплыло, ни времени их найти. Наши гейты (сьют + соак + A/B
по фазам) это не заменяет, но других у нас и нет.

## ⚠️⚠️ ГЛАВНЫЙ КАНДИДАТ НА «ТУПИТ»: ВЕЕР ДОСТУПНОСТИ НА HARD (замер 2026-08-14)

Жалоба владельца «стало тупить больше». A/B трёх сборок регрессии НЕ показал —
ни в Chromium (CPU ×4), ни в WebKit 26 (движок его Safari): кадр одинаков.
⚠️⚠️ **ПОТОМУ ЧТО ВЕСЬ СТЕНД МЕРИЛ EASY** — боевое умолчание `hard:false`, а
`isAccessible` на Easy выходит ПЕРВОЙ СТРОКОЙ и не кастует ни одного луча.
Замер по обоим режимам (headless GPU metal, CPU ×4, 390×844, ур.11/20, бот
матчит каждые 0.5 с):

| | кадр p95 | худший кадр | один тик доступности |
|---|---|---|---|
| Easy | 33.5 / 33.8 мс | 76 мс | 0 мс |
| **HARD** | **93.0 / 104.6 мс** | **164 / 180 мс** | **79-86 мс** |

Механика: `refreshAccessibility` пускает веер (до 8 сэмплов × 7 направлений)
по КАЖДОМУ предмету — на ур.20 это ~10 000 кастов Rapier за тик, а тик шёл
каждые 300 мс, пока куча бодрствует. То есть каждые 0.3 с кадр застревал на
0.08-0.09 с. Это ВТРОЕ дороже всего, что стоит на экране, и вчетверо дороже
солвера.
✅ **ВЛАДЕЛЕЦ ПОДТВЕРДИЛ: У НЕГО HARD ВКЛЮЧЁН** — гипотеза стала диагнозом.

**СДЕЛАНО, ДВЕ ЧАСТИ.**
1. Периодический тик — ЧАСТИЧНЫЙ: 1/8 кучи каждые 100 мс (работа та же, полный
   цикл 0.8 с) вместо всей кучи каждые 300 мс.
2. После матча — ЛОКАЛЬНЫЙ пересчёт `refreshAccessibilityNear(точка)` вместо
   полного обхода. Обоснование физическое: лучи идут ВВЕРХ, удаление группы
   открывает небо тем, кто лежал под ней и рядом; далёкий предмет свою
   доступность не меняет. Берём цилиндр R=4.2 по XZ и всех, кто выше точки
   минус запас. Точность держит ФОН (полный круг за 0.8 с), скорость —
   локальность. Механика не тронута: те же лучи, тот же результат на предмет.

⚠️⚠️ **ЛИНЕЙКА ПЕРЕДЕЛАНА, И ЭТО ЧАСТЬ РЕЗУЛЬТАТА: ПЕРВЫЕ ЧИСЛА HARD БЫЛИ
ЗАВЫШЕНЫ МОИМ ЖЕ СТЕНДОМ.** Замер гонял матчи хуком `autoMatch`, а тот САМ
зовёт полный `refreshAccessibility` — то есть в окно попадали 80 мс, которых
в бою нет (канонная грабля «тест-хук внутри окна перф-замера», записана
2026-08-01 и повторена здесь). Честный путь — `bestTapTarget` ВНЕ окна, затем
`perfReset`, затем НАСТОЯЩИЙ `mouse.click`.
**A/B БОЕВЫМИ ТАПАМИ (HARD, CPU ×4, GPU metal, 390×844, 6 матчей на руку):**

| | ДО правок | ПОСЛЕ |
|---|---|---|
| ур.11, кадр p95 | 89.3 мс | **35.6 мс** |
| ур.11, худший | 106 мс | 74.8 мс |
| ур.20, кадр p95 | 106.4 мс | **34.9 мс** |
| ур.20, худший | 115.7 мс | 81.2 мс |

То есть на Hard кадр упал ВТРОЕ и сравнялся с Easy (33-35). Худший кадр всё
ещё 75-81 мс — остаток на будущее, но это уже редкий пик, а не постоянный фон.
⚠️ **УРОК О СЛЕПОТЕ СТЕНДА:** «регрессии нет» четырёх независимых замеров
означало лишь «нет в том режиме, который мерили». Прежде чем закрывать поиск,
проверь, какие ВЕТКИ кода твой стенд вообще исполняет — гейт `if (!CFG.hard)`
выключал предмет замера целиком.

## ⛔ «МЕДЛЕННЫЙ ХОЛОДНЫЙ СТАРТ» — ЭТО ПРОЛОГ, А НЕ ТОРМОЗА (замер 2026-08-13)

Проверка живой ссылки с телефонного вьюпорта дала «до начала игры 10.5 с» и
чуть не отправила меня чинить производительность старта. **Причина —
`storyPrologue` в фазе `wait`:** новому игроку показываются 2 панели комикса,
и он их ЧИТАЕТ. Замер по двум игрокам (file://, без троттлинга, 2 прогона):

| | до первого кадра | до начала игры |
|---|---|---|
| НОВЫЙ (пролог играет) | 0.26-0.39 с | **10.5-10.6 с** |
| ВЕРНУВШИЙСЯ (`Save.st` помечен) | 0.26 с | **2.65 с** |

⚠️⚠️ **СЛЕДСТВИЕ ДЛЯ ЧУЖИХ ЗАМЕРОВ: внешнее ревью намеряло «до первого
интерактивного кадра 6.4-8.0 с» и отнесло это к нагрузке — на самом деле оно
мерило ПРОЛОГ на чистом профиле.** Любой headless-прогон стартует новым
игроком, то есть ловит пролог ВСЕГДА; на живом устройстве владельца и у
вернувшегося игрока его нет. Пункт их списка «применять perf-tier до genLevel
ради первого запуска» этим ОСЛАБЛЕН: 10 из 10.5 с там были чтением комикса.
⚠️ A/B ПО ИСТОРИИ (три сборки, `git show <хеш>:index.html` — он самодостаточен,
копии кладутся РЯДОМ ради относительных путей): 4de462a / 29abe8b / 7ba6861
дали 10.5 / 10.5 / 10.5 с — то есть волны к старту непричастны вовсе.
⚠️ И грабля собственного зонда: `window.__game` в БОЕВОЙ сборке отсутствует
(спека владельца 2026-07-29, открывается `?dev=1`) — зонд по живой ссылке без
этого флага висит на `waitForFunction` и выглядит как «игра не поднялась».
Признак старта без хуков — класс `introdone` на `<html>` либо живой канвас.

## ВОЛНЫ ТЕЛ НА НАСЫПАНИИ: ПРИНЯТО + ДВА ИСПРАВЛЕНИЯ ПРИЁМКИ (2026-08-13)

Сдача ФИЗИКИ (`55467a2`, задание #51 по заказу владельца): тела создаются, но
ВЫКЛЮЧЕНЫ (`setEnabled(false)`) и включаются слоями по 8 по РЕАЛЬНЫМ часам —
шаг физики в окне интро 17.5 → 8.5 p95 (−51%), заполнение цело на 8 сидах.
Влито. При приёмке диспетчер нашёл ДВЕ вещи, которых в сдаче не было.

**(1) СПАСАТЕЛЬ ДЁРГАЛ ЖДУЩИЕ ТЕЛА — ЭТО И БЫЛА «ЦЕНА» СДАЧИ.** Физика честно
назвала рост спасений 5-6 → 18-20 и отнесла его к «столбу налива». Замер
диспетчера показал ПРИЧИНУ: все лишние спасения приходятся на высоты **22.9 и
26.07** (ур.11 и 20) — это ВЫКЛЮЧЕННЫЕ тела, стоящие в позициях спавна; без
волн их РОВНО НОЛЬ. Спавн раскидан на ширину кромки, выше кромки `radiusAt`
даёт R1, и стоящее у края тело проходит по условию выхода, как только снята
временная стена (без волн все успевали упасть раньше — дефекта не было).
⛔ ЦЕНА БЫЛА НЕ КОСМЕТИЧЕСКОЙ: телепорт зажимает высоту к `FUNNEL.H`, то есть
ждущее тело сбрасывалось в чашу ВНЕ ОЧЕРЕДИ — наливание для него ломалось.
Лечение — одна строка в `rescueSweep`: `if (!it.body.isEnabled()) continue;`
(выключенное тело не участвует в симуляции и «уехать» не может по построению).
Это и есть «ЖИВОЕ условие на момент срабатывания», которого не хватило
отклонённому гейту по временной стене. После: спасения интро сравнялись с
рукой «без волн» (0 против 0 на ур.11, 3 против 3 на ур.20).

**(2) ⚠️⚠️ ВЫИГРЫШ ЖИЛ ТОЛЬКО ВНУТРИ ОКНА ЗАМЕРА — РАБОТА УЕХАЛА ЗА ЕГО КРАЙ.**
`waveTick` тикал ТОЛЬКО в фазе `drop`, а она кончается по `t > 0.8 && maxV <
3.5` — с волнами куча в начале пустая, скорости малы, значит переход идёт по
САМОМУ РАННЕМУ порогу. Остаток очереди (до ~100 тел из 182) вываливал
`waveReleaseAll` в `finishIntro` РАЗОМ, и досыпка шла уже ПОСЛЕ интро, на
глазах игрока: время до полного штиля 5.5 → 6.0-6.6 с (ур.11, CPU ×4).
Метрика окна интро этого не видит ПО ПОСТРОЕНИЮ — окно кончается ровно там,
куда переехала нагрузка. **Это канонная грабля «метрика меряет не то, что
называешь вслух», в самом дорогом её виде: число честное, вывод неверный.**
Лечение: `waveTick()` тикает и в облёте (канон прямо разрешает досыпку там:
«куча доседает уже во время облёта», утряска гейтится `maxV < 3` и по летящему
столбу не бьёт) + темп **80 → 55 мс**, чтобы очередь укладывалась в окно
подачи: 23 слоя × 80 = 1.84 с против окна `drop 0.8 + облёт 1.0` = 1.8 с (не
влезало), при 55 — 1.27 с с запасом.
⚠️ РАЗМЕН НАЗВАН ЧЕСТНО: 55 даёт шаг в интро ~11 вместо 8.5 (то есть −35% от
базы 17.5 вместо −51%), зато время до штиля почти возвращается к базе
(медианы ур.20, CPU ×4, 3 прогона: без волн 5.50 с, при 55 — 5.74 с, при 80 —
6.42 с). ⚠️ И честная граница: разброс ВНУТРИ руки доходит до 1.3 с, то есть
у выбора 55 против 80 опора на грани шума — обе точки внутри спеки владельца
(50-80), число менять только вместе с пересчётом арифметики окна подачи
(число слоёв = живых/8 и РАСТЁТ с уровнем, окно фиксировано).
⚠️ Страж приёмки «волны тел живы»: в настоящем интро пик выключенных > 20,
`skipIntro` дозревает все в ноль. Диверсия `waves:false` роняет первую
половину, сломанный релиз в скипе — вторую.

## ⚠️⚠️ ХОЛОДНЫЙ СТАРТ ШЕСТЬ ДНЕЙ ТЕРЯЛ УРОВЕНЬ: TDZ + ПУСТОЙ CATCH (2026-08-13)

Дефект нашло ВНЕШНЕЕ ревью владельца (Codex), у нас он не всплыл ни одним
стражем. Механика: восстановление `levelNum` жило в 40-items и читало
`typeof Save` — а `Save` объявлен `const` в 77-save, склейка сортирует модули
ПО ИМЕНИ (40 < 77), и на верхнем уровне IIFE это TDZ: **`typeof` на
TDZ-константе БРОСАЕТ** (он безопасен только для НЕОБЪЯВЛЕННЫХ имён). Пустой
`catch` глотал ReferenceError, и вместе с веткой Save гибла и ветка
localStorage — она стояла В ТОМ ЖЕ try ВЫШЕ строки падения. Итог: с
2026-08-07 (перенос уровня в сейв) КАЖДЫЙ перезапуск начинал игру с 1-го
уровня. Репро: mixer_level=11 → куча 80 предметов (ур.1); после фикса 181.
- Лечение: восстановление переехало в 77-save ПОСЛЕ `loadSave()`; в 40-items
  надгробие «НЕ возвращать чтение Save в модуль с номером меньше 77».
- Страж: холодный старт с mixer_level=11 обязан дать кучу 11-го уровня И
  глыбу (два независимых признака); ключ прибирается — localStorage у страниц
  сьюта общий.
- ⚠️⚠️ ПОЧЕМУ НЕ ПОЙМАЛИ САМИ: ни один страж не ходил ХОЛОДНЫМ стартом на
  сохранённый уровень — все секции ставят уровень хуком (setLevel+regen), то
  есть страж сам создавал предпосылку и был слеп к её отсутствию в бою (тот
  же закон, что у LB_URL). Плюс сьют «выигрывал» от бага: свежие страницы
  всегда бутились на ур.1 независимо от накопленного mixer_level.
- ⚠️ ОБЩИЙ ЗАКОН СКЛЕЙКИ: порядок модулей = порядок ИНИЦИАЛИЗАЦИИ верхнего
  уровня; const/let из старших модулей НЕДОСТУПНЫ в top-level коде младших
  (функции — доступны, они зовутся после инициализации). Топ-левел код,
  читающий чужие const, обязан жить в модуле с БОЛЬШИМ номером, чем владелец.

## ⛔⛔ ИНЦИДЕНТ 2026-08-17: БОНУСНЫЙ УРОВЕНЬ СЛОМАЛ ОСНОВНУЮ ИГРУ

Владелец на 201-м уровне увидел предметы СНАРУЖИ чаши и под её дном. Работа по
бонусному уровню откачена с v2 (три коммита обратными правками) и живёт в ветке
**`claude/bonus-level`**. Здесь — причина и правило, чтобы это не повторилось.

**КОРЕНЬ ОДИН И ОН НЕ ПРО БОНУС: Я СДЕЛАЛ `radiusAt()` ЗАВИСЯЩИМ ОТ УРОВНЯ, А
ЕГО ЧИТАЕТ `initPhysicsWorld`, КОТОРЫЙ ВЫПОЛНЯЕТСЯ РОВНО ОДИН РАЗ ЗА ЗАГРУЗКУ.**
Кольца контейнера строятся так: `faceR = radiusAt(y0) - WALL_GAP`. Пока
`radiusAt` был чистой функцией высоты, это было верно всегда. Как только он стал
отвечать по-разному на бонусном и обычном уровне, ЗАГРУЗКА НА БОНУСНОМ УРОВНЕ
собрала чашу ЦИЛИНДРОМ радиуса ~3.96 вместо конуса 2.4→4.1 — и такой она
осталась **до конца сессии**, на всех последующих обычных уровнях. Внизу конус
уже цилиндра почти вдвое, поэтому предметы спокойно уходили за стекло и падали
мимо дна.

**ЗАМЕР, ПОДТВЕРЖДАЮЩИЙ ДИАГНОЗ ДВУСТОРОННЕ** (одна и та же сборка, разница
только в сохранённом уровне на момент загрузки; после старта уходим на 201-й):

| старт | предметов снаружи чаши | ниже дна | худший выход |
|---|---|---|---|
| обычный 201 | **0** | 0 | 0 |
| бонусный 200 | **33** | 6 | 0.68 |

После отката: 0 и 0 при обоих стартах.

⚠️⚠️ **ПОЧЕМУ ЭТОГО НЕ ПОЙМАЛИ НИ 715 АССЕРТОВ, НИ ДЕСЯТОК ПРОБ.** Все они
загружают страницу на уровне 1 и переходят дальше ХУКОМ (`setLevel`+`regen`),
то есть `initPhysicsWorld` у них ВСЕГДА отрабатывает на обычном уровне.
Единственный путь к дефекту — ХОЛОДНЫЙ СТАРТ НА СОХРАНЁННОМ БОНУСНОМ УРОВНЕ, а
им не ходил никто. Это ровно тот же класс, что записан в разделе про TDZ
(«ни один страж не ходил холодным стартом на сохранённый уровень»), и он
повторился ВТОРОЙ РАЗ — значит правило было сформулировано слишком узко.

⛔⛔ **ПРАВИЛО, ВЫВЕДЕННОЕ ИЗ ИНЦИДЕНТА (несущее):**
**1. ФУНКЦИЯ, КОТОРУЮ ЧИТАЕТ ОДНОРАЗОВАЯ ИНИЦИАЛИЗАЦИЯ, НЕ ИМЕЕТ ПРАВА ЗАВИСЕТЬ
ОТ ИЗМЕНЧИВОГО СОСТОЯНИЯ.** `initPhysicsWorld` строит ПОСТОЯННУЮ геометрию —
значит все его входы обязаны быть КОНСТАНТАМИ. Понадобилась переменная ширина —
её нельзя брать из общей функции; либо строить свой набор коллайдеров (как это и
сделано для витрины), либо явно писать формулу конуса из `FUNNEL`.
**2. ПЕРЕД ТЕМ КАК СДЕЛАТЬ ОБЩУЮ ФУНКЦИЮ УСЛОВНОЙ — ГРЕПНИ ЕЁ ПОТРЕБИТЕЛЕЙ И
РАЗДЕЛИ ИХ НА «СЧИТАЮТ КАЖДЫЙ КАДР» И «ПОСТРОИЛИ ОДИН РАЗ».** У `radiusAt` их
было около двадцати; я проверил, что каждый читает ПРАВИЛЬНОЕ значение, и не
спросил, КОГДА он его читает. Правильное значение в неправильный момент —
и есть весь этот инцидент.
**3. У КАЖДОЙ ФИЧИ, МЕНЯЮЩЕЙ УСЛОВИЯ УРОВНЯ, ОБЯЗАН БЫТЬ СТРАЖ ХОЛОДНОГО
СТАРТА НА ТАКОМ УРОВНЕ.** Не «на следующем за ним», а именно НА НЁМ.
⚠️ И горькое уточнение: страж холодного старта на бонусном уровне я написал —
в том самом коммите, который не успел дойти до основной ветки. Он ловит ошибки
страницы и состав уровня, но НЕ проверял бы геометрию чаши на СЛЕДУЮЩЕМ уровне
той же сессии. То есть даже он поймал бы дефект лишь случайно: **проверять надо
не только «поднялся ли особый уровень», но и «цела ли игра ПОСЛЕ него».**

⚠️ **ЧТО ЕЩЁ ВСКРЫЛОСЬ ПОПУТНО, ТОГО ЖЕ КЛАССА** (исправлено в ветке): 
`buildTempTallWall` зовётся из genLevel ДО строки `level = {...}` и решал по
`level.bonus`, то есть читал ПРЕДЫДУЩИЙ уровень. Общее у обоих дефектов одно:
**значение зависит от уровня, а точка чтения живёт вне его жизненного цикла.**

## ПАРТИЯ 2026-08-17: МАТЧЕПЫ ВЛАДЕЛЬЦА, ЖИВОЕ МЕСТО, РЕДАКТОР МАТЕРИАЛОВ

**МАТЧЕПЫ — КАРТИНКИ ВЛАДЕЛЬЦА, НЕ ПЕЧЁНЫЕ.** `06-matcap-metal.js` (лопасти и
ступица) и `07-matcap-bomb.js` (бомба) — его PNG инлайном base64: сборка обязана
открываться ОДНИМ файлом офлайн. Оба модуля НОМЕРОМ < 20 намеренно: лопасти
строятся top-level кодом 20-arena, а `const` старших модулей для него в TDZ
(инцидент 2026-08-13). Ассеты берём КАК ЕСТЬ — без пережатия и смены расширения.
⛔ ПРОЦЕДУРНЫЙ РАДУЖНЫЙ МАТЧЕП БОМБЫ СНЯТ (надгробие в 40-items). Причина, по
которой он был, НЕ исчезла: three r149 не умеет `iridescence`, перелив живёт
ТЕКСТУРОЙ. Вернуть — `git revert`.
⚠️ ЦЕНА ЗАМЕРЕНА ЗИПОМ, а не суммой файлов: пакет портала 4.17 → 4.33 МБ на
металле (52.1% → 54.1% лимита 8 МБ), бомба добавила ещё ~166 КБ base64.

**ОБВОДКА ОЧКОВ — 7 ЦВЕТОВ СЛУЧАЙНО** (`POP_OTL_PALETTE` в 70-fx): яркие, худший
контраст к белой цифре 4.50:1, жёлтых нет (слово владельца «желтый не нужно
использовать… рандомно, без какой либо системы, думаю 7 цветов, как радуга»).
⚠️⚠️ **ГЛАВНЫЙ УРОК ПАРТИИ — «ИСКЛЮЧЕНИЕ СЪЕДАЕТ ПРАВИЛО».** Я оставил в комбо
жёсткий оранжевый как «сигнал состояния», не посчитав, ЧТО игрок почти всё время
играет ВНУТРИ комбо (оно зажигается группой от трёх или вторым матчем за 1.5 с).
Замер серии из 8 быстрых матчей: 7 обводок из 8 были оранжевыми — цветным
выходил только первый. Владелец увидел это как «обводка ДО СИХ ПОР оранжевая».
⚠️ И СТРАЖ БЫЛ ЗЕЛЕН: он проверял ОДИНОЧНЫЙ матч, то есть единственный случай,
где исключение не действует. **Страж обязан исполнять путь, на котором
исключение ДЕЙСТВУЕТ, иначе он зелен ровно тогда, когда правило не работает.**
Сейчас берётся серия подряд, и требуется, чтобы все обводки были из палитры и
цветов встретилось больше одного.

**ЖИВОЕ МЕСТО ПРИ ТРАТЕ** (`lbRankNow` в 85-hud, жалоба «всё такая же задержка
при трате очков»). Плашка обновлялась только при ОТКРЫТИИ меню и на `onSent`, а
тратит игрок СТОЯ В МЕНЮ — до ответа сервера (частота 20 с) висело старое место.
⚠️⚠️ **ИСТОЧНИК — СОСЕДИ `/v1/me` (`up`/`dn`, `NEAR_N`=5), А НЕ ОТРЕЗОК ТОПА.**
Топ приходит из ЧАСОВОГО снимка и накрывает первую сотню: игрок с местом 3000 не
получал мгновенного пересчёта вовсе. Соседи запрошены живой базой и есть на любом
месте. Отрезок топа остался запасным путём.
⛔ Правило закрытого отказа ЦЕЛО: вышли за окно соседей — отдаём `null`, остаёмся
на серверном числе. Прикидку (`exact:0`) по-прежнему не показываем нигде.
⚠️ Равенство счётов решается порядком сервера (`ORDER BY s DESC, u ASC`): сосед
снизу с ТЕМ ЖЕ счётом стоит ниже по идентификатору — строгое сравнение.
⚠️ Стрелка направления сравнивает ПОКАЗАННОЕ число, а не серверное (эпоха строки
одна). ⛔ Ветка «список соседей сверху пуст — значит игрок первый» СНЯТА: пустой
список значит «не прислали», а не «их нет», и она глушила расчёт по топу.
⚠️ **429 В КОНСОЛИ ВЛАДЕЛЬЦА — НЕ ПОЛОМКА, А ДОКАЗАТЕЛЬСТВО:** две отправки
(`openMainScreen → bankLive` и `buyBoost`) внутри окна частоты; клиент откладывает
и переотправляет. Сеть тут ничего не ускорит — лечится только счётом на клиенте.

**РЕДАКТОР МАТЧЕПОВ** (`12-matcap-edit.js`, режим разработчика): холст 512 с
кистью, загрузка PNG, пять целей (текстурные, крашеные, хром, лопасти, бомба),
«применять сразу», экспорт PNG. Живое применение ужимает холст до размера боевой
текстуры — видно ровно то, что в бою.
⚠️⚠️ **ПОКА ПАНЕЛЬ ОТКРЫТА, ЧАСЫ УРОВНЯ ЗАМОРОЖЕНЫ** (слово владельца «фризим
таймер миксера»). Приём `resumeGame` — якоря двигаются вместе с часами, поэтому
остаток ЗАСТЫВАЕТ на своём значении. ⛔ НЕ «освежать lastAction, как под
рекламой»: миксер тоже не ел бы, но таймер прыгал бы на максимум. ⛔ И НЕ пауза —
весь смысл редактора в живом бое. Часы бонусной витрины в том же списке: забыть
их значит повторить баг 2026-08-12 (сток съел бы уровень, пока крутишь материал).
⚠️ Признак открытости — функция `mceIsOpen()` от самой переменной панели, а не
флаг рядом (копия признака расходится с источником).

⚠️⚠️ **МОЯ ОШИБКА ПАРТИИ, ЧЕТВЁРТЫЙ СЛУЧАЙ ОДНОГО КЛАССА: СТРАЖ НА ОБЩЕЙ СТРАНИЦЕ
НАСЛЕДУЕТ ЧУЖОЕ СОСТОЯНИЕ.** Новый страж жал `#pauseBtn` на странице секции
таблицы, а соседи оставляют её в своём состоянии — меню не открылось, и прогон
умер таймаутом БЕЗ ВЕРДИКТА на 580 зелёных (снаружи неотличимо от благополучного
лога: красных ноль). Лечение: своя страница со своим стендом и своим моком.
**И наблюдатель за прогоном обязан ловить ОБРЫВ, а не только строку `SUITE:`** —
иначе о смерти прогона узнаёшь по тому, что ждёшь его вечно.

⚠️ **ПРОБА ДЕШЕВЛЕ ПРОГОНА.** Каждая правка этой партии проверялась отдельной
пробой ДО тринадцатиминутного сьюта: A/B обводок (7 из 8 оранжевых против нуля),
A/B заморозки (0.00 против 1.41 за 1.4 с), таблица случаев `lbRankNow`, боевая
трата (845 → 846 при сервере, отвечающем 845). Три из них поймали дефект,
который иначе стоил бы полного прогона.

## НАСТРОЙКИ НА МОБИЛЬНОМ — ТОЛЬКО СВИТЧЕРЫ (макеты 870:1544 / 1536 / 1539, 2026-08-17)

Слово владельца: «переделай этот блок, только для мобильной версии… в мобильной
изменение звука происходит физическими кнопками, поэтому оставляем только
свитчеры». Ползунки Sound/Music на телефоне СНЯТЫ, вместо них два свитчера в
ОДНУ строку (половинками), строка сложности — во всю ширину и БЕЗ подписи.
Десктоп НЕ ТРОНУТ: там прежние ползунки и подпись «Difficult».

⚠️⚠️ **«ТОЛЬКО МОБИЛЬНАЯ» ВЫПОЛНЕНО ПО ПОСТРОЕНИЮ, А НЕ АККУРАТНОСТЬЮ:** вся
правка живёт внутри `@media (max-width:1079px)`, поэтому базовые и десктопные
правила её ФИЗИЧЕСКИ не видят. Разметка одна на обе раскладки (дублировать
секцию значило бы дублировать id и обработчики), различие несёт только медиаблок.

**ЧИСЛА СВИТЧЕРА — ИЗ DEV MODE, И ОДНО ИЗ НИХ ДОБЫТО НЕОЧЕВИДНО.** Дорожка
52×32, ручка-эллипс 28×28 с отступом 2 → ход РОВНО 20 (x 2 → 22), радиус 1000,
тень `Shadow/BottomDay/Close` = 0/2/5 `#0000001A`.
⚠️ Включённая дорожка — токен `Experimental/Salad` **#9ce52e**. У ВЫКЛЮЧЕННОЙ
ТОКЕНА НЕТ (`get_variable_defs` отдаёт только белую ручку и тень), то есть она
литерал. Значение снято ЧТЕНИЕМ САМОГО SVG-ЭКСПОРТА — **#E7EDF8**.
⛔ Это НЕ отменяет запрет канона на Figma-экспорты: запрещено ИСПОЛЬЗОВАТЬ их
как ассеты (`preserveAspectRatio="none"` даёт искажения), а читать из них
ЗНАЧЕНИЯ — законно и точнее, чем подбирать по скриншоту.
⚠️ Локальный ассет-сервер Figma (`localhost:3845`) на просроченный узел отвечает
`Error getting image` (19 байт) — рабочий путь у УДАЛЁННОГО сервера
(`figma.com/api/mcp/asset/…`, живёт 7 дней). И `fileKey`-параметр второго
сервера не зависит от активной вкладки — правило канона подтвердилось снова.

**ПОВЕДЕНИЕ — НА ТОМ ЖЕ ТРАКТЕ, ЧТО У ПОЛЗУНКОВ.** Свитчеры ходят через
`applySoundVol`/`applyMusic`; включение возвращает ПОСЛЕДНЮЮ НЕНУЛЕВУЮ громкость,
а не 100. У звука пара `soundVolPrev` уже была, музыке заведена такая же
(`musicVolPrev`, 85-hud). ⚠️ Второй тракт рядом с работающим НЕ заводился —
он разошёлся бы с первым при первой правке; синхронизация свитчеров стоит в том
же `refreshMainSettings`, что и ползунков.
⚠️ Хвост «Easy game» из макета — отдельным `<span class="ms-seg-long">`, видимым
только в медиаблоке: текст кнопки общий на обе раскладки, а менять ДЕСКТОП нас
не просили. Псевдоэлементом не делали — подпись кнопки должна быть настоящим
текстом.
⛔ `:has()` НЕ ИСПОЛЬЗУЕМ (Safari 15.4+, свод правил проекта держит его в
списке «избегать по умолчанию») — строка сложности помечена классом `ms-set-diff`.

**СТРАЖИ (4, все двусторонние по построению):** мобильная половина на 390 и 320
(ползунков нет, свитчер 52×32 с ходом 20 и лаймом, Sound+Music одной строкой,
подписи Difficult нет), ДЕСКТОПНАЯ половина (ползунки на месте, свитчера нет,
подпись видна, «Easy» без хвоста) и ПОВЕДЕНИЕ (поставили 40 ползунком, погасили
свитчером, включили — обязано вернуться 40).
⚠️ Без десктопной половины «мобильная правка» была бы НЕОТЛИЧИМА от правки,
снёсшей ползунки везде.
⚠️ Прогон диверсий (копия сборки РЯДОМ с оригиналом, md5 сверен): «свитчер и на
десктопе» роняет ровно десктопную, «включение ставит 100» — ровно поведение,
пустая правка комментария названа пустой. Диверсия «снять свитчеры на мобильном»
роняет ДВЕ половины (мобильную и поведение) — и это правильно: без свитчера по
нему нечем кликать.
⚠️ И повтор канонной грабли на себе: строку для диверсии я написал ПО ПАМЯТИ
(«строки сложности» вместо «строка сложности») — инструмент честно напечатал
«ПРОТУХЛА». Брать строку ИЗ ФАЙЛА, как и записано.

**MUSIC ПО ЛЕВОМУ КРАЮ, ЗАЗОР 48 (слово владельца 2026-08-17: «Music так же по
левому краю, но с отступом от свитчера Sound равным 48 px» + «на узких ужимай
зазор, оставь одну строку»).** Прежнее «пополам, музыка вправо» ОТМЕНЕНО: обе
группы по своему содержимому и обе у ЛЕВОГО края.
⚠️ ЗАЗОР МЕРЯЕТСЯ ОТ СВИТЧЕРА ДО ПОДПИСИ, а не между боксами рядов — так сказал
владелец. Это `column-gap` ряда; внутренний «подпись↔свитчер» живёт отдельно
(`gap:12` у `.ms-set`) и в 48 не входит.
⚠️⚠️ **НА 320 ОДНОЙ СТРОКОЙ НЕ ВЫХОДИТ НИКАКИМ ЗАЗОРОМ, И ЭТО АРИФМЕТИКА, А НЕ
ВКУС:** содержимое двух групп при кегле 22 занимает **259**, а в карточке
доступно 318 / 303 / 288 / **248** на 390 / 375 / 360 / 320. Не хватает 11px
САМОМУ содержимому. Раз «одна строка» — требование, там ужимается и оно:
кегль 22→19, свитчер 52×32→**44×28** (ход 20→16). Ступени зазора взяты
ЗАМЕРОМ: 48 (≥390) / 32 (≤389) / 20 (≤369) / 12 (≤339).
⛔ Это ЕДИНСТВЕННОЕ место, где мы отступаем от макета, и отступаем по прямому
требованию владельца. На 360 и выше макетные размеры целы.
⚠️ СТРАЖ ЗНАЕТ ОБА НАБОРА ЧИСЕЛ (390 → 52×32/ход 20, 320 → 44×28/ход 16) —
иначе он утверждал бы «свитчер есть», а не «свитчер такой, какой задуман».
И ЗАЗОР АССЕРТИТСЯ ОТДЕЛЬНО: «одна строка» истинна и при нулевом отступе,
поэтому правка «сжать всё в кашу» без этого ассерта прошла бы молча.

**ЗАМЕР (headless, computed styles):** 390 — сегмент 318, 320 — 248, ход ручки
20 на 390 и 16 на 320; выключенная дорожка `rgb(231,237,248)`, включённая `rgb(156,229,46)`.
⚠️ Первый замер снимался через 120 мс после клика и поймал СЕРЕДИНУ перехода
(`.18s`): фон `rgb(217,235,209)`, сдвиг 3.8 вместо 20. Устоявшееся состояние
ждать опросом либо паузой заведомо длиннее перехода — прежнее правило «поймал
момент, а не состояние», в самой безобидной его форме.
⚠️ Паддинг карточки НЕ ТРОГАЛИ: в макете 12 у карточки + 8 у внутренней колонки
= 20 по горизонтали, и это ровно то, что уже стояло (`12px 20px`).

## ⚠️⚠️ ДЕПЛОЙ: ikorzun.github.io/Blender/ = PAGES ИЗ ВЕТКИ `main` ЭТОГО ЖЕ РЕПО

Слово владельца 2026-08-12: «на телефоне и у тестеров эта ссылка, нужно её
всегда держать актуальной». Механика выяснена замером, а не настройками (в
Pages-конфиг доступа нет): сайт раздаёт `index.html` РОВНО того коммита, на
котором стоит `main` (сверка байт-в-байт), пуш в `v2` сайт НЕ обновляет.

**ОБЯЗАННОСТЬ ДИСПЕТЧЕРА: после КАЖДОГО пуша в `v2` — `git push blender
v2:main` (fast-forward) и проверить курлом, что сайт пересобрался:**
```
curl -s -o /tmp/dep.html -w "%{size_download}\n" "https://ikorzun.github.io/Blender/?cb=$(date +%s)"
```
размер обязан совпасть с `git cat-file -s v2:index.html`; пересборка Pages —
1-3 минуты.

⚠️⚠️ **ДЕПЛОЙ PAGES УМЕЕТ ПАДАТЬ, А НЕ ТОЛЬКО ЗАДЕРЖИВАТЬСЯ — И ПАДАЕТ МОЛЧА
(2026-08-17).** Деплой коммита `8c39e42` УПАЛ: `Failed to create deployment
(status: 503)`, `HttpError: No server is currently available to service your
request`, `Creating Pages deployment failed`. Снаружи это неотличимо от очереди:
`git ls-remote` показывает верный `main`, в репозитории всё чисто, а сайт
двадцать пять минут отдавал ПРЕДЫДУЩУЮ сборку. Обновился он только потому, что
СЛЕДУЮЩИЙ пуш (`790002d`) запустил новый деплой, и тот прошёл.
⛔ **МОЯ ПЕРВАЯ ТРАКТОВКА «просто стоит в очереди» БЫЛА НЕВЕРНА** — записываю,
потому что она успокаивает и ведёт к бесконечному опросу вместо разбора.
**ПОРЯДОК, КОГДА КУРЛ НЕ СХОДИТСЯ БОЛЬШЕ ~5 МИНУТ:** (1) githubstatus.com —
`curl -s https://www.githubstatus.com/api/v2/incidents.json`; (2) вкладка
Actions репозитория, воркфлоу `pages-build-deployment`; (3) перезапустить деплой
либо сделать пустой коммит. Опрашивать дольше бессмысленно: упавший деплой сам
не оживёт.
⚠️ **ВОРКФЛОУ PAGES НЕ ЛЕЖИТ В РЕПОЗИТОРИИ** (`.github/workflows` у нас нет ни в
одной ветке и не было никогда) — в режиме «Deploy from a branch» GitHub
генерирует `pages-build-deployment` сам. Отсюда следствия: версии экшенов внутри
него мы не пиним (предупреждение «Node.js 20 is deprecated,
actions/checkout@v4 / upload-artifact@v4 forced to Node 24» — ИХ, не наше, и
падения оно не вызывало), а чтобы получить контроль, пришлось бы переводить
Pages на собственный воркфлоу. Пока не нужно.
⚠️ ТОТ ЖЕ ИНЦИДЕНТ GitHub в тот же час уронил и ЧУЖОЙ деплой воркера площадки
(429 от `codeload.github.com` при скачивании `cloudflare/wrangler-action` +
500 от API Cloudflare). Два разных репозитория, один корень — сверяться со
статусом ДО того, как искать вину в своём коде.

⚠️⚠️ **ЦЕНА, КОТОРУЮ ЭТО УЖЕ СТОИЛО (2026-08-12): сайт ТРИ ДНЯ раздавал
`a771543`, и все три редакции решения кромок Safari не тестировались вовсе.**
Владелец вынес «скругление не работает» сборке, в которой скругления НЕ БЫЛО, —
и заказал новый раунд исследования уже решённой задачи. Свежесть проверять
курлом по МАРКЕРУ новой правки (грепом), а не по ощущению «я же пушил».
⚠️ Локальный `main` при этом был отставшим и путал: я сверил размер с
локальным `main:index.html`, не совпало, и чуть не ушёл искать «третий
источник» — сверять с `blender/main`, а не с локальной веткой.

## ПАУЗА РАЗРЕШЕНА ВО ВРЕМЯ ИНТРО (слово владельца 2026-08-12)

Жалоба: «иногда на паузе таймер игры не останавливается и через какое-то время
миксер начинает работать». МЕХАНИКА НАЙДЕНА ВОСПРОИЗВЕДЕНИЕМ: `pauseGame` в
интро ОТКАЗЫВАЛ (гейт `intro`), а меню при отказе всё равно открывалось — его
гвард умеет отступить только перед ЧУЖОЙ паузой (реклама), случай «поставить
не смог вовсе» предусмотрен не был. Игрок смотрит в меню, игра живёт, простой
тикает, миксер ест. Замер: пауза в интро → меню открыто, куча **80 → 68 за 30
секунд**.
⚠️ **ВХОДА В ДЫРУ ДВА, и второй объясняет «иногда»:** кнопка паузы в окне
насыпания (~9 с) И уход вкладки в фон — `visibilitychange` зовёт
`openMainScreen`, то есть свернуть телефон во время насыпания хватало.
ЛЕЧЕНИЕ: `intro` снят из гейта `pauseGame` (слово владельца «разреши паузу во
время интро»). Пауза замораживает интро ЧИСТО ПО ПОСТРОЕНИЮ: гейт `if (paused)`
в loop стоит РАНЬШЕ `tickIntro`, интро тикает игровым временем — на резюме
продолжается с того же места; якоря `resumeGame` интро не трогают
(`level.nextGrind` в интро равен 0 и пропущен).
Страж: своя страница, НАСТОЯЩЕЕ интро (skipIntro запрещён до конца), фаза
падения ловится опросом «верх кучи движется» — пауза в фазе ожидания дала бы
тавтологию заморозки; ассерты: пауза ПОСТАВЛЕНА, падение заморожено (topY
равен через 500 мс), резюме карточкой Play, партия играется после цикла.

## ПОДАЧА БОМБЫ: С 5-ГО УРОВНЯ, С РАЗРЫВОМ, И ЗА ТРИ СЕРИИ (2026-08-12)

Спека владельца дословно: «добавляй бомбу с 5 уровня, но через каждый 1-3 уровня
по умолчанию, а так же если игрок выбьет 3 серии».
⛔ **ОТМЕНЯЕТ «ОДНА БОМБА НА КАЖДОМ УРОВНЕ»** (спека 2026-07-22): до пятого её
теперь нет ВОВСЕ. `BOMB_FROM_LEVEL = 5`, `BOMB_GAP_MIN/MAX = 1/3`,
`BOMB_SERIES_REWARD = 3`.

⚠️ **«СЕРИИ» — ЭТО ЕГО ЖЕ `level.bowlCracks`**, единица, которой он мерил «5-7
серий за уровень» при вводе разлёта чаши. Второй счётчик рядом с работающим не
заводился: копия расходится с оригиналом при первой правке.
⚠️ **НАГРАДА ПАДАЕТ В ЭТОТ ЖЕ УРОВЕНЬ** (бомба сыплется с неба тем же путём, что
досыпка турбо), а не «на следующем»: иначе игрок не свяжет её со своими сериями.
Гварды награды: не раньше пятого, один раз за уровень, и НЕ ВТОРАЯ бомба —
инвариант «одна в чаше» цел.
⚠️ Разрыв живёт В ПАМЯТИ СЕССИИ, а не в сейве: это ритм подачи, а не прогресс.
Поле сейва потребовало бы мержа, и два устройства спорили бы о том, чей разрыв
верный. Цена: после перезагрузки очередь начинается заново.
⚠️ Разрыв назначается ТОЛЬКО когда бомба реально выдана — иначе пропущенный
уровень тихо сдвигал бы очередь.

**ЗАМЕР:** уровни 1-4 — бомб 0 во всех; с пятого выдачи на 5, 6, 8, 9, 11, 12,
13, 15, 16 (разрывы 1/2/1/2/1/1/2/1 — все в пределах); награда: на второй серии
бомбы нет, на третьей появляется, на четвёртой ВТОРОЙ не появляется.

⚠️⚠️ **ЦЕНА ЧУЖОГО КОНТЕКСТА ПРЕДЪЯВЛЕНА СРАЗУ.** Секцию бомбы пришлось поднять
на пятый уровень (раньше бомбы нет), и весь хвост прогона, рассчитанный на
первый (бюджет встрясок, камера, сценарий тупика), лёг: прогон умер ТАЙМАУТОМ на
ожидании `deadlock`. Лечение — возврат `setLevel(1) + regen` в конце секции.
**Правило: секция, поднявшая уровень, обязана вернуть его на месте, а не
надеяться, что соседи переживут.**
